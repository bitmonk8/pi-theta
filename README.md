# pi-loom

A [Pi Coding Agent](https://pi.dev) extension that introduces **loom**, a
purpose-built scripting language for authoring parameterized, programmatic
templates that drive an LLM conversation.

A `.loom` file interleaves ordinary code (variables, loops, conditionals,
functions) with literal text destined for the model. Evaluating a loom
appends turns to a conversation — either the caller's current conversation
(*prompt mode*) or a fresh isolated conversation (*subagent mode*) — and
evaluates to a final value (its last expression or `return expr`),
available to programmatic callers and propagated across the subagent
boundary. Loom evaluation itself produces no file outputs; any file writes
occur only through Pi tools the loom explicitly admits via frontmatter
`tools:` (e.g. `write`, `edit`). `.warp` files are library modules that
share loom's grammar and type system and are imported by `.loom` files.

The full design lives in [`docs/spec.md`](./docs/spec.md). The implementation roadmap
lives in [`docs/plan.md`](./docs/plan.md).

## Status

Implementation in progress. Track progress against
[`docs/plan.md`](./docs/plan.md) and the
[spec coverage matrix](./docs/plan_topics/coverage-matrix.md).

| Leaf | Status | Date | Notes |
| V9a | Complete | 2026-06-30 | Capability probe (Step 0) (paired with `V9a-T`): fills `runCapabilityProbe(host)` in `src/extension/capability-probe.ts` as a pure function over the injected `ProbeHost` snapshot — no ambient `process.versions` / global `AbortSignal` read. Runs the five checks in the fixed short-circuit order `(a)` Node floor → `(b)` `AbortSignal`/`AbortController` shape → `(c)` nine factory-probable SDK members → `(d)` four-package peer-dep lock-step → `(e)` `typebox` `Type.Unsafe`, stopping at the first failure and returning one `loom/load/host-incompatible` refusal or a pass. `(a)` uses `semver.satisfies(.., ">=22.19.0", {includePrerelease:true})` with `semver.valid` routing a non-SemVer version to `probe-failed`; `(b)` is `typeof`-only for the seven `typeof`-checked members and `in`-only for the `aborted`/`reason` accessor properties (PIC-3, never reads the member it checks; PIC-4, never constructs); `(c)` names the first-failure `member` under the normative listed order; `(d)` iterates the four `@earendil-works/*` peers in order, tilde-testing each installed version against `~0.75.5` (condition-4 raw string / conditions-1–3 `<unresolvable>` for `peer-dep-malformed-version`, out-of-range otherwise); `(e)` is `typeof Type.Unsafe === "function"`. Every step is `try`/`catch`-wrapped (PIC-6) routing any throw to `probe-failed` with `step`/`package`/coerced `cause` and `<unreadable>` observed/required sentinels (underlying-error coercion). Exports `FACTORY_PROBABLE_CAPABILITIES = [1,2,3,4,6]` (the V18a/V18c partition-target symbol) and `SHUTDOWN_AWAIT_CAP_MS = 2000` (cancellation-runtime constant, owned by V17a, imported by V9g/V9i/V17a). Observed/required substrings sourced from diagnostics/placeholder-rendering-b.md's per-`kind` enumeration. No factory wiring or new diagnostics-registry code added here (the probe is the deliverable; refusal enforcement at the `PiExtensionAPI` adapter layer is a later seam). No module-level mutable state; the four `catch (e: unknown)` sites carry `// allow-broad-catch: PIC-6` (category-2 self-failure traps); no ambient primitive / Promise combinator / Sync call. All 21 `V9a-T` tests green; full suite 580 green, typecheck + lint + H3a ambient scan clean. |
| V9a-T | Tests red | 2026-06-30 | Capability-probe (Step 0) tests (paired `V9a` impl): the `src/extension/capability-probe.ts` seam — the `runCapabilityProbe(host)` pure-function probe over an injected `ProbeHost` snapshot (no ambient `process.versions` / global `AbortSignal` read), the closed `HostIncompatibleKind` discriminator union, `ProbeFailureDetails` / `ProbeOutcome` shapes, and the `FACTORY_PROBABLE_CAPABILITIES` constant (an `Object.freeze([])` stub kept off the H2a mutable-binding scan; V9a fills `[1, 2, 3, 4, 6]`) — plus `tests/capability-probe.test.ts` with 21 failing tests pinning `PIC-3`/`PIC-4`/`PIC-5`/`PIC-6` and the seven-kind `loom/load/host-incompatible` discriminator (capability-probe.md, host-prerequisites.md). PIC-3: a host whose `AbortSignal` `aborted`/`reason` accessor getters throw on read still passes (the probe uses `in`, not a value read) — also witnessed against the real WHATWG global. PIC-4: a constructor that throws on `new` still passes (no construction); zero-arity function members satisfy the `typeof` checks (no arity sniffing). PIC-5: a recording host proves exactly the five enumerated check categories (a/b/c/d/e) are consulted and no sixth; `FACTORY_PROBABLE_CAPABILITIES` enumerates exactly inventory items 1/2/3/4/6. PIC-6: a hostile `nodeVersion` / peer-version read routes to `probe-failed` (with `step` / `package` / `cause`) rather than escaping. Discriminator coverage: `node-floor` (sub-floor `observed`/`required >=22.19.0`; a qualifying prerelease passes; non-SemVer → `probe-failed`), `abortsignal-shape` (`typeof`-checked member → observed `undefined` / required `function`; absent prototype-property → observed `absent` / required `present`), `sdk-capability-missing` (`member` names the first-failure path under listed order), `peer-dep-out-of-range`, `peer-dep-malformed-version` (raw string for condition 4, `<unresolvable>` for 1–3), `typebox-shape`, `probe-failed` (`<unreadable>` sentinels), and the `(a)`→`(b)` short-circuit (first failure wins, no aggregation). Observed/required substrings sourced from the diagnostics/placeholder-rendering-b.md per-`kind` enumeration. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 21 tests red on their primary behavioural assertions (the probe returns an off-union sentinel refusal / `FACTORY_PROBABLE_CAPABILITIES` is empty) because the V9a probe body is absent — fail-red-for-the-intended-reason; the other 558 tests stay green. |
| V8e | Complete | 2026-06-30 | `FileWatcher` and `TokenEstimator` seams (paired with `V8e-T`): fills the `PiFileWatcher` chokidar production adapter (`src/seams/pi-file-watcher.ts`), the `PiTokenEstimator` production adapter (`src/seams/pi-token-estimator.ts`), and the test fakes `tests/helpers/fake-file-watcher.ts` / `tests/helpers/fake-token-estimator.ts` against `PIC-14` and `PIC-16`. PIC-14: `PiFileWatcher.watch(roots, handler, onTerminate?)` wires a chokidar watcher with `ignoreInitial`, routing only the three load-bearing change kinds (`add`/`change`/`unlink`) to the handler (chokidar `addDir`/`unlinkDir`/`ready`/`raw` never wired, filtered out by construction) and conveying a chokidar `error` over the terminal-signal channel (`onTerminate({roots})`) — a stopped-delivering observation distinct from the three change kinds; the returned `Unsubscribe` closes the watcher and is idempotent (a `void watcher.close()` guarded by an `active` flag, not awaited because `Unsubscribe` is sync). `FakeFileWatcher` is the conformance vehicle: `watch` records the handler/`onTerminate` and returns an idempotent `Unsubscribe` that drops them, `emit(event)` synchronously delivers one change-kind event, `terminate(termination)` drives the terminal channel (reaching `onTerminate`, never the change handler — a no-op with no channel attached); per-instance state only, parallel instances dispatch independently. PIC-16: `PiTokenEstimator.estimate(message)` delegates to the `estimateTokens` named import, returning its result unchanged (redefining none of Pi's algorithm), relied on as a deterministic pure function of `message` at the fixed Pi-SDK pin; `FakeTokenEstimator` returns the constructor-injected per-message count (keyed by message identity), throwing loudly on an unconfigured message rather than a silent zero. No new diagnostics-registry codes. Stateless production adapters / instance-only fake state — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 12 `V8e-T` tests green; full suite 559 green, typecheck + lint + H3a ambient scan clean. |
| V8e-T | Tests red | 2026-06-30 | `FileWatcher` / `TokenEstimator` seam tests (paired `V8e` impl): extends the H3a `FileWatcher` seam (`src/seams/file-watcher.ts`) with the enumerated terminal-signal channel — a `WatchTermination` payload + `OnWatchTerminate` callback added as an optional third `watch(roots, handler, onTerminate?)` parameter (GOV-18 arm (a)-permitted, illustrative shape) keeping the stopped-delivering observation structurally distinct from the three change kinds — re-exported via `src/seams/index.ts`; adds the `PiFileWatcher` chokidar production-adapter stub (`src/seams/pi-file-watcher.ts`) and `PiTokenEstimator` production-adapter stub (`src/seams/pi-token-estimator.ts`), plus the `FakeFileWatcher` (`tests/helpers/fake-file-watcher.ts`, `emit`/`terminate` injection points) and `FakeTokenEstimator` (`tests/helpers/fake-token-estimator.ts`, per-message-count map) test-fake stubs — all members throw, none referencing an ambient primitive so the H3a real-tree scan stays green — and `tests/watch-token-seams.test.ts` with 12 failing tests pinning `PIC-14` and `PIC-16` (host-interfaces-services.md). `PIC-14`: `FakeFileWatcher` `watch()` returns an idempotent `Unsubscribe`, the handler observes the three change kinds `add`/`change`/`unlink` (delivery contract), the terminal-signal channel conveys a stopped-delivering observation distinct from the three change kinds (`onTerminate` fires, the change handler does not), `terminate()` with no channel attached is a no-op, two instances dispatch independently; `PiFileWatcher` `watch()` returns an idempotent-teardown `Unsubscribe` over a real temp dir and two instances are independent. `PIC-16`: `PiTokenEstimator` `estimate(message)` delegates to the `estimateTokens` named import (`estimate === estimateTokens` for the same message), is a deterministic pure function of its argument, and is per-runtime; `FakeTokenEstimator` returns the configured per-message integer (not a content-derived count) and two instances report independent counts. No new diagnostics-registry codes. Inert stub seams: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 12 tests red on the stub `not implemented` throw because the V8e adapters/fakes are absent — fail-red-for-the-intended-reason; the other 547 tests stay green; the H3a ambient scan stays green. |
| V8d | Complete | 2026-06-30 | `Clock` and `IdSource` seam adapters (paired with `V8d-T`): fills the production adapters `src/seams/wall-clock.ts` and `src/seams/crypto-id-source.ts` and the test fakes `tests/helpers/fake-clock.ts` / `tests/helpers/fake-id-source.ts`. PIC-12: `WallClock` is the sole exempt `src/**` timing site — `now()`→`performance.now()` (monotonic ms), `wallNow()`→`Date.now()` (epoch ms), and `setTimeout`/`clearTimeout` delegate to the globals, each direct timing reference carrying its own same-line `// allow-ambient: <primitive> — Clock` comment; `FakeClock` drives deterministic timing — `advance(ms)` fires due timers in deadline order (equal deadlines in registration order, a 0-ms timer under `advance(0)`), `clearTimeout` is a no-op for an already-fired handle, `now()`/`wallNow()` are not implicitly advanced, and parallel instances hold independent timer queues. PIC-20: `CryptoIdSource` is the sole direct `crypto.randomUUID` site — both `newInvocationId()` and `newToolCallId()` delegate to `crypto.randomUUID()` returning the canonical lowercase 8-4-4-4-12 hex form, each call carrying its own `// allow-ambient: crypto.randomUUID — IdSource` comment; `FakeIdSource` hands out a constructor-injected sequence across both members (throwing loudly on exhaustion), parallel instances independent. No new diagnostics-registry codes. Stateless production adapters / instance-only fake state — no module-level mutable state, no broad catch / ambient primitive outside the exempt sites / Promise combinator / Sync call. All 18 `V8d-T` tests green; full suite 534 green, typecheck + lint + H3a ambient scan clean. |
|---|---|---|---|
| V8a | Complete | 2026-06-30 | `Checkpoint` seam (paired with `V8a-T`): fills `src/seams/production-checkpoint.ts` — `ProductionCheckpoint` (constructor takes the injected `Clock`), one instance per `loomAbort` (per invocation). `before(kind, site)` implements `PIC-10` (host-interfaces-services.md): for `loop-iter` it returns a promise resolved by `clock.setTimeout(resolve, 0)` — one macrotask turn scheduled through the injected `Clock` seam's PIC-12 timer surface, never a bare global timer — so a Pi-dispatched abort can land before the next loop-iteration signal-check; for every other kind (`query`/`tool-call`/`invoke`/`binder-call`) it returns `Promise.resolve()`, resolving on the microtask queue because those checkpoints precede real async I/O that already yields. No `before(...)` sites beyond the five enumerated `CheckpointKind`s (structurally pinned by the paired tests' exhaustive `Record`). No new diagnostics-registry codes. Stateless adapter (only the injected `Clock` held) — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / `Sync` call. All 13 `V8a-T` tests green; full suite 547 green, typecheck + lint clean. |
| V8a-T | Tests red | 2026-06-30 | `Checkpoint` seam tests (paired `V8a` impl): the `ProductionCheckpoint` production-wiring stub (`src/seams/production-checkpoint.ts`, constructor takes the injected `Clock`; `before(kind, site)` throws) plus `tests/checkpoint-seam.test.ts` with 13 tests pinning `PIC-10` (host-interfaces-services.md). `loop-iter` stays pending across a microtask drain and resolves only after `clock.advance(0)` fires the 0-ms timer the seam schedules through the injected `Clock.setTimeout(fn, 0)` (a `RecordingClock` wrapper asserts exactly one 0-ms schedule — never a bare global timer); every non-`loop-iter` kind (`query`/`tool-call`/`invoke`/`binder-call`) resolves on the microtask queue with no `clock.advance` and schedules no timer; each kind's resolution matches its declared yield kind; and the `CheckpointKind` union is exactly the five enumerated cancel sites — pinned structurally by an exhaustive `Record<CheckpointKind, …>` so adding/removing a member fails type-check (`adds no extra sites`); per-invocation independence of two checkpoints from the same clock. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 12 behavioural tests red on the stub `not implemented` throw because the V8a `ProductionCheckpoint` is absent — fail-red-for-the-intended-reason; the structural union-exhaustiveness assertion is green; the other 534 tests stay green. |
| V6b | Complete | 2026-06-30 | `params` and defaults (paired with `V6b-T`): fills `src/parser/params.ts` — `parseParams(fields, bodyTypes, site)` implements the `params:` field contract of frontmatter/frontmatter-fields-a.md §params and §Defaults. Whole-file `NamedType` resolution: each field's RHS type lowers against a `bodyTypeMap` built from every `BodyTypeDeclaration` regardless of source order, so a frontmatter-to-body forward reference (`author: Author` with `schema Author` declared later) resolves and lowers to an in-document `{ "$ref": "#/$defs/Author" }` registered under `$defs`; an identifier-shaped named type with no body declaration fires `loom/parse/unresolved-named-type` (E, message anchored to code-registry-parse.md, naming the type). Ordering: a non-defaulted field after a defaulted one fires `loom/parse/non-trailing-default` (E) once, naming the first offender. Defaults: each `= <rhs>` default is run through the `V2a` `checkLiteralSublanguage` is-literal check, surfacing `loom/parse/default-not-literal` (E) naming the offending sub-expression. Type lowering (primitive → `{type}`, `array<T>` → `{type:array, items}`, union → SUBS-1 via `lowerUnion`, named → `$ref`) produces an AJV-validatable per-loom object document (`type:object`, non-defaulted fields `required`, `additionalProperties:false`, resolved named types under `$defs`) the `V8c` `AjvSchemaValidator` compiles; the document is returned only when the block lowers cleanly (no unresolved/ordering/non-literal error). No new diagnostics-registry codes. Pure functions, immutable module-level constants only — no mutable cross-invocation state, no broad catch / ambient primitive / Promise combinator / Sync call. All 7 `V6b-T` tests green; full suite 516 green, typecheck + lint clean. |
| V6b-T | Tests red | 2026-06-30 | `params` and defaults tests (paired `V6b` impl): the new `src/parser/params.ts` seam — `parseParams(fields, bodyTypes, site)` (inert stub → `{ diagnostics: [] }`), the `ParamFieldInput` (`name`/`typeSource`/optional `defaultSource`/`range`), `BodyTypeDeclaration` (a whole-file `schema`/`enum`/imported `.warp` named type carrying its lowered `$defs` fragment), `ParamsParseSite`, and `ParamsParseResult` (`diagnostics` + optional `loweredSchema`) shapes — plus `tests/params-defaults.test.ts` with 7 tests pinning frontmatter/frontmatter-fields-a.md §params and §Defaults. `loom/parse/non-trailing-default` (E): a non-defaulted param after a defaulted one fires naming the first offender (message anchored to code-registry-parse.md), while all-trailing defaults raise none. `loom/parse/default-not-literal` (E): a function-call default RHS fires naming the offending sub-expression (message anchored to code-registry-parse.md), while a primitive-literal default raises none. `params` lower to an AJV-validatable schema document the real `V8c` `AjvSchemaValidator` accepts a well-typed argument set against and rejects a mistyped one. A `params:` RHS forward-referencing a body `schema` resolves whole-file (no `loom/parse/unresolved-named-type`) and validates a correct value while rejecting one missing a required field. `loom/parse/unresolved-named-type` (E): a named type with no body declaration fires naming the type (message anchored to code-registry-parse.md). No new diagnostics-registry codes (all three cited codes already in code-registry-parse.md). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 5 obligation tests red on their own primary assertions (absent diagnostic, undefined lowered schema) because the V6b `params:` contract is absent — fail-red-for-the-intended-reason; the 2 negative-case assertions pass; the other 511 tests stay green. |
| V6a | Complete | 2026-06-30 | Frontmatter field contract (paired with `V6a-T`): fills `src/parser/frontmatter.ts` — `parseFrontmatter(source, { file, modelMatcher })` parses the leading `---`-fenced YAML block via the `yaml` dependency (`parseDocument` + `LineCounter` for best-effort located diagnostics), recognises the loom 1.0 field vocabulary (`description`/`argument-hint`/`mode`/`model`/`bind_model`/`bind_context`/`bind_echo`/`tools`/`system`/`respond_repair`/`tool_loop`/`params`), and applies the field contract: required `mode:` → `loom/load/missing-mode` (E, message anchored to code-registry-load.md) and the loom is not registered; any unknown top-level key → `loom/load/unknown-frontmatter-field` (W, one per key) and the loom still registers (forward-compat seam); `timeout:` → `loom/parse/timeout-field-rejected` (E, NOCEIL-1 seam, message anchored to code-registry-parse.md); a present `model:` resolved through the injected `ModelReferenceMatcher.resolve(raw)` seam — `loom/load/model-unresolved` (E) on `no-match`/`ambiguous` with the `<value>` placeholder rendered as the unquoted YAML scalar, and the loom is not registered. Registration = no error-severity diagnostic; `ParsedFrontmatter` (`mode`, optional `model`) is returned only when registered. No new diagnostics-registry codes. Module-level vocabulary set is immutable readonly — no mutable cross-invocation state, no broad catch / ambient primitive / Promise combinator / Sync call. All 8 `V6a-T` tests green; full suite 501 green, typecheck + lint clean. |
| V6a-T | Tests red | 2026-06-30 | Frontmatter field-contract tests (paired `V6a` impl): the new `src/parser/frontmatter.ts` seam — `parseFrontmatter(source, { file, modelMatcher })` (inert stub → `{ registered: false, diagnostics: [] }`), the **model-reference-matcher injection seam** `V6a` defines (`ModelReferenceMatcher.resolve(reference) → "resolved" \| "no-match" \| "ambiguous"`, the interface the parser's `model:` resolution hook calls; the concrete matcher binds `V11a`'s exact-match resolver over `ctx.modelRegistry.getAvailable()` and is injected by `V9b`), plus the `ParsedFrontmatter`/`FrontmatterParseResult`/`ParseFrontmatterOptions`/`LoomMode`/`ModelMatchOutcome` shapes — plus `tests/frontmatter-contract.test.ts` with 8 tests pinning frontmatter/frontmatter-fields-a.md §Field contract. `loom/load/missing-mode`: frontmatter omitting `mode:` fires the load error (message anchored to code-registry-load.md) and the loom is not registered, while a valid `mode: subagent` resolves (registers, `mode` parsed). `loom/load/unknown-frontmatter-field` (W): an unknown top-level key warns and the loom still loads (tolerated). `loom/parse/timeout-field-rejected` (NOCEIL-1 seam): a `timeout:` field is rejected (message anchored to code-registry-parse.md). `loom/load/model-unresolved`: a present `model:` value resolving to no available model — a non-string scalar, a `provider/modelId` no-match (driven through the injection seam, asserting the hook hands the matcher the raw reference), and a provider-ambiguous bare `modelId` — fails the load and the loom is not registered, while a resolving reference registers; the no-match message is anchored to code-registry-load.md with the unquoted-YAML-scalar `<value>` rendering. No new diagnostics-registry codes (all four cited codes already present). Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 8 tests red on their own primary assertions (absent diagnostic, un-registered loom, undefined parsed field, un-consulted matcher) because the V6a frontmatter parser is absent — fail-red-for-the-intended-reason; the other 493 tests stay green. |
| V2e | Complete | 2026-06-30 | Wire-name translation boundary (paired with `V2e-T`): fills the behaviour-bearing bodies of `src/runtime/wire-translation.ts`. **Inbound** (`translateInbound`, model output → loom value, post-AJV): `rebuildInbound` walks the validated JSON, renames object keys wire→loom from the `V5f` `SchemaSidecar.wireNames` map (un-renamed fields keep their name; loom code never sees a wire name at any depth), reattaches the declaring-enum tag (`V2c` `makeEnumValue`) at each named-enum position (resolved from `namedEnumPositions`, whose `/properties/<wire>` lowered-fragment pointers map to the data field) so the result compares equal to a locally constructed variant while `JSON.stringify` still yields the bare wire string, and leaves anonymous string-literal-union positions (absent from the sidecar) as plain strings. **Outbound** (`translateOutbound`, loom value → JSON): `lowerOutbound` renames keys loom→wire and collapses an enum value (boxed `String`) to its bare wire string. Both recurse through arrays and nested objects. **Divergence (logged):** the `V5f` sidecar carries no per-field `$ref` target, so nested-object recursion resolves the nested `$defs` sidecar by matching the field's wire name against a `$defs` key (`sidecars.get(wireKey)`) — faithful for the fixtures, not for the general `field: NamedSchema` case; a fully faithful boundary needs `V5f` to emit per-field ref targets. Defaults bypass inbound translation (frontmatter defaults arrive already branded and loom-side-named). No new diagnostics-registry codes. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 6 `V2e-T` tests green; full suite 493 green, typecheck + lint clean. |
| V2e-T | Tests red | 2026-06-30 | Wire-name translation boundary tests (paired `V2e` impl): the new `src/runtime/wire-translation.ts` seam — `translateInbound` / `translateOutbound` over `InboundTranslationInput` / `OutboundTranslationInput` (validated/value + per-`$defs` `SchemaSidecar` map + root `$defs` name), both inert identity passthrough stubs — plus `tests/wire-name-translation.test.ts` with 6 tests pinning runtime-value-model.md §"Wire-name translation" (RVM code-keyed area) and schemas.md §"Wire-name renaming". Inbound (model output → loom value): rebuilds loom-side names from the `V5f` wire-name sidecar so loom code never sees wire names (top-level and one level nested); reattaches the declaring-enum tag at a named-enum position (compares equal to a `V2c` `makeEnumValue` variant; `JSON.stringify` still bare wire string); a named-enum position is tagged while an anonymous string-literal-union position (absent from the sidecar) stays a plain string. Defaults bypass: a frontmatter default arrives already branded and loom-side-named, equal to the inbound-translated model output without passing through `translateInbound`. Outbound (loom value → JSON): rewrites loom-side names to wire names. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 6 tests red on their own primary assertions (wire key survives inbound, enum tag not reattached, named/anonymous positions not discriminated, default not equal to translated output, loom key survives outbound) because the V2e translation pass is absent — fail-red-for-the-intended-reason; the other 487 tests stay green. |
| V5f | Complete | 2026-06-30 | Schema lowering and canonical hash (paired with `V5f-T`): fills the behaviour-bearing bodies of `src/parser/schema-lowering.ts`. §Canonical schema hash: `canonicalForm` serialises a `LoweredJsonValue` to the deterministic UTF-8 JSON form — object keys sorted by Unicode code-point (a code-point comparator, not UTF-16-unit `<`), no insignificant whitespace, numeric `const`/`enum` literals delegated to the `V2d` `renderCanonicalNumber` keyed off the declared `integer`/`number` kind (never runtime integrality), array elements left in lowering order, strings RFC 8259 minimal-escaped via `JSON.stringify`; `canonicalHash` is SHA-256 (node:crypto `createHash`) over those bytes; `schemaSlug` is the first 16 lowercase hex. SUBS-1: `lowerUnion` lowers an all-primitive union (with `null` a primitive) to `{ type: [...] }` in source order with `"null"` last, and any non-primitive arm to `{ anyOf: [...] }` in source order (primitive arm → `{ type: <name> }`, non-primitive arm carries its lowered fragment). Step 5: `buildSidecar` builds the wire-name translation map (renamed fields only) and the named-enum-position map keyed by JSON Pointer (present iff the source type was a named `enum`). `dedupInlineSchemas` groups by slug, byte-compares canonical-form bytes on a slug match, dedups byte-identical fragments silently to one `$defs` entry, and raises `loom/load/schema-slug-collision` (message anchored to code-registry-load.md) refusing to merge on a byte-mismatch. No new registry codes. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 12 `V5f-T` tests green; full suite 487 green, typecheck + lint clean. |
| V5f-T | Tests red | 2026-06-30 | Schema-lowering and canonical-hash tests (paired `V5f` impl): the new `src/parser/schema-lowering.ts` seam — `canonicalForm`/`canonicalHash`/`schemaSlug` over the typed `LoweredJsonValue` domain (inert: return `""`), `lowerUnion` (inert: returns `{ anyOf: [] }`), `buildSidecar` (inert: empty maps), and `dedupInlineSchemas` (inert: dedups nothing, raises no diagnostic), plus `SlugKeyedFragment`/`SidecarFieldInput`/`SchemaSidecar`/`LoweredUnionArm` shapes — plus `tests/schema-lowering-hash.test.ts` with 12 tests pinning schema-subset.md (§Canonical schema hash steps 2–4, §Lowering Algorithm step 3 SUBS-1, step 5 sidecar, step 2 `$defs` dedup + §Schema-slug collision posture). §Canonical schema hash: the canonical form sorts object keys by code point, emits no whitespace, and binder-number-renders numeric `const`/`enum` literals (`1e21`→`1000000000000000000000`, `42.0`→`42`); the slug is the first 16 lowercase hex of SHA-256 over the canonical-form bytes (digest computed in-test via `node:crypto`). SUBS-1: an all-primitive union (with `null` a primitive) lowers to `{ type: [...] }` in source order with `"null"` last; any non-primitive arm forces `{ anyOf: [...] }`. Step 5: the sidecar captures a wire-name translation map (renamed fields only) and a named-enum-position map keyed by JSON Pointer (present iff the source type was a named `enum`; anonymous string-literal-union positions absent). `loom/load/schema-slug-collision` (message anchored to code-registry-load.md): two non-byte-identical fragments sharing a slug fire; byte-identical ones dedup silently to one `$defs` entry. Each accept/silent arm is paired so it cannot pass vacuously against the inert stub. Typecheck + lint clean; the 12 tests red on their own primary assertions (empty canonical form, wrong `anyOf` union shape, empty sidecar maps, absent collision diagnostic, un-deduped entries) because the V5f lowering/hash bodies are absent — fail-red-for-the-intended-reason; the other 475 tests stay green. |
| V4b | Complete | 2026-06-30 | Runtime panics (paired with `V4b-T`): fills the behaviour-bearing seams of `src/runtime/runtime-panics.ts` and closes the `match-error` message template in `src/runtime/match-result.ts`. The six closed panic sources each raise their panic carrying the registered message template (sourced from diagnostics/code-registry-runtime.md): `evaluateIndexAccess` raises `IndexOutOfBoundsPanic` (`index out of bounds: <i> not in 0..<length>`) on an out-of-range array index, `MissingObjectKeyPanic` (`missing object key: <key>`) on an absent object key, and `NullIndexAccessPanic` (`null index access: [<i>]`) on a `null` target; `evaluateMemberAccess` raises `NullMemberAccessPanic` (`null member access: .<field>`) on a `null` target; `enterInvokeFrame` raises `InvokeDepthExceededPanic` (`invoke chain depth exceeded: <depth> > 32`) past the depth-32 cap; and `evaluateMatch` raises `MatchError` (`MatchError: no arm matched <scrutinee summary>`) — `MatchError` now extends `LoomPanic`, and the scrutinee summary renders per the category-2 runtime-value rule (decimal numbers, truncated strings, `Ok`/`Err`, compact JSON objects/arrays, bare enum wire). Numeric placeholders render via `renderInteger`. Panics bypass `?` (`evaluateQuestion` invokes the operand outside any catch, so a thrown panic propagates) and `match` (a panic is a thrown value, never an arm selection). `evaluateQuestion` applies `?` propagation: `Ok(v)`→`value`, `Err(e)`→`propagate`. `surfaceUnexpectedThrow` routes an unexpected throw to `loom/runtime/internal-error` (`internal error: <error.message>`, `hint`=`error.stack` or `"<no stack available>"`), passes a `LoomPanic` through unreclassified (`undefined`), and emits no diagnostic for a NOCEIL-3 `HostFatal`. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 23 `V4b-T` tests green; full suite 475 green, typecheck + lint clean. |
| V4b-T | Tests red | 2026-06-30 | Runtime-panics tests (paired `V4b` impl): the new `src/runtime/runtime-panics.ts` seam — the `LoomPanic` base + five panic classes (`IndexOutOfBoundsPanic`/`MissingObjectKeyPanic`/`NullIndexAccessPanic`/`NullMemberAccessPanic`/`InvokeDepthExceededPanic`), the accessor seams `evaluateIndexAccess`/`evaluateMemberAccess`/`enterInvokeFrame` (inert: raise nothing), the `?`-propagation seam `evaluateQuestion` (inert: returns a `value` sentinel without invoking the operand), the `HostFatal` NOCEIL-3 marker, and `surfaceUnexpectedThrow` (inert: returns a wrong-code sentinel diagnostic for every input) — plus `tests/runtime-panics.test.ts` with 23 tests pinning errors-and-results/error-model.md §"Runtime panics" (closure into invocation.md §"Invocation depth bound" for `loom/runtime/invoke-depth-exceeded` and hard-ceilings/ceiling-invariants-and-audit.md §"No additional ceilings" for the NOCEIL-3 carve-out). The six closed panic sources (`loom/runtime/index-out-of-bounds`/`missing-object-key`/`null-index-access`/`null-member-access`/`match-error`/`invoke-depth-exceeded`) each emit their registered message template (sourced from the diagnostics registry via `registryMessage`, placeholders filled per the rendering categories — e.g. `index out of bounds: 5 not in 0..3`, `MatchError: no arm matched 5`, `invoke chain depth exceeded: 33 > 32`) and bypass `?` (raised in a `?` operand, propagates past `evaluateQuestion`) and `match` (raised in a `match` arm body, escapes `evaluateMatch`; `match-error`'s own bypass-of-`match` is intrinsic and V4a-covered, so it is exercised for `?`-bypass only). An unexpected thrown value (`TypeError`, the catchable `RangeError` allocation family) surfaces as `loom/runtime/internal-error` (`internal error: <error.message>`, `hint` = `error.stack`); an already-classified `LoomPanic` is not reclassified; the NOCEIL-3 uncatchable host-fatal (modelled by `HostFatal`) emits no diagnostic at all. No new diagnostics-registry codes (all seven cited codes already in code-registry-runtime.md). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 23 tests red on their own primary assertions (the accessor raises nothing; the `match-error` message is the V4a `"no arm matched"` stub not the template; `evaluateQuestion` neither throws nor propagates; `surfaceUnexpectedThrow` returns the wrong-code sentinel where an `internal-error` diagnostic or `undefined` is required) because the V4b panic surface is absent — fail-red-for-the-intended-reason; the other 452 tests stay green. |
| V3d | Complete | 2026-06-30 | Functions and return (paired with `V3d-T`): fills the parse/type seam `src/parser/functions.ts` and the runtime seam `src/runtime/function-result.ts`. `FN-1`: `checkFnPlacement` returns `loom/parse/nested-fn` for a nested `fn`, `checkFunctionReference` returns `loom/parse/function-as-value` for a function used outside call position, and `resolveFnCall` resolves a name against the hoisted top-level `fn` set (`resolved`/`unresolved`), so hoisted mutual recursion resolves regardless of order. `FN-2`: `buildFnDeclaration` preserves a `///` doc comment on the AST node, and `lowerFnDescription` returns the empty fragment (functions have no JSON Schema, so the doc never lowers into a provider payload). `FN-3`/`RET-1`: `resolveReturnType` infers an annotation-less body's return type as the LUB (under `⊑`, via `checkCompatible`) of its tail/`return` operand types (`integer ⊔ number = number`), wraps in `Result<T, QueryError>` when the body bears `?` or any `Result`-typed contribution (reconciling success payloads), fires `loom/parse/return-no-common-type` when operands share no common upper bound, and type-checks each operand against an explicit annotation (per-operand `⊑` outcome) instead of inferring. `FN-4`: an empty-tail body infers the `null` literal type, a `?`-bearing empty-tail body infers `Result<null, QueryError>`, and `discardForVoid` discards a `void` function's tail value (produces `null`). `RET-2`/`RET-3`: `checkBareReturn` returns `loom/parse/bare-return-in-non-void` outside a `void` function (incl. top-level), and `checkUnreachableCode` warns `loom/parse/unreachable-code` (warning severity) for code after a `return`. `FN-5`: `functionResult` exposes the body's produced value on the success path and reports it absent on fail/cancel. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 24 `V3d-T` tests green; full suite 452 green, typecheck + lint clean; H2a/H3a scans green. |
| V3d-T | Tests red | 2026-06-30 | Functions and return tests (paired `V3d` impl): the parse/type seam `src/parser/functions.ts` (`checkFnPlacement`, `checkFunctionReference`, `resolveFnCall` → `"unchecked"`, `buildFnDeclaration` dropping the doc, `lowerFnDescription` wrongly carrying it, `resolveReturnType` → `"unchecked"`, `checkBareReturn`, `checkUnreachableCode` — inert stubs) and the runtime seam `src/runtime/function-result.ts` (`functionResult` → unimplemented sentinel for every outcome, `discardForVoid` not discarding), plus `tests/functions-and-return.test.ts` with 24 tests pinning functions.md and return.md. `FN-1`: a nested `fn` fires `loom/parse/nested-fn`, a function-as-value fires `loom/parse/function-as-value`, and hoisted mutual recursion resolves. `FN-2`: the `///` doc is preserved on the AST and does not lower into the provider payload. `FN-3`/`RET-1`: an annotation-less body infers the LUB of its tail/`return` operands (`integer ⊔ number = number`), wraps in `Result<T, QueryError>` on a `?`/`Result` contribution, fires `loom/parse/return-no-common-type` for incompatible operands, and an explicit annotation type-checks each operand instead of inferring. `FN-4`: an empty-tail body infers `null`, a `?`-empty-tail body infers `Result<null, QueryError>`, and a `void` function discards its tail value. `RET-2`/`RET-3`: bare `return` fires `loom/parse/bare-return-in-non-void` outside a `void` function (incl. top-level); post-`return` code warns `loom/parse/unreachable-code`. `FN-5`: the function-result seam exposes the produced value on success and reports it absent on fail/cancel. No new diagnostics-registry codes (all five cited codes already in code-registry-parse.md). Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 20 tests red on their own primary assertions (absent diagnostic, the `"unchecked"` sentinel, a missing AST doc, a wrongly lowered description, the unimplemented final-value sentinel, the un-discarded value) because the V3d behaviour is absent — fail-red-for-the-intended-reason; the 4 negative-case assertions pass; the other 432 tests stay green. |
| V4a | Complete | 2026-06-30 | `match`, `?`, and `Result` (paired with `V4a-T`): fills the parse/type seam `src/parser/match-result.ts` and the runtime seam `src/runtime/match-result.ts`. `checkQuestionOperand` (ERR-18) returns `loom/parse/question-on-non-result` for a non-`Result<_, QueryError>` operand (a `Result` whose error arm is not `QueryError` renders the `<type>` placeholder as `Result`), `undefined` for `Result<T, QueryError>`. `checkQuestionScope` returns `loom/parse/question-outside-result-fn` for an annotated non-`Result`-compatible scope; inferred and `Result`-compatible annotated scopes pass. `checkMatchArmTypes` reports `loom/parse/match-arm-type-mismatch` when the arms share no common upper bound under type-system.md §"Type compatibility" (delegating to `checkCompatible`), and otherwise resolves the `match` to the least upper bound of its arms — a sink, if present, narrows every arm to and resolves the `match` to the sink. `evaluateMatch` dispatches the scrutinee against the arms in order (first match wins, non-selected bodies never evaluated), recursively matching the six pattern forms (wildcard/identifier/literal via `valuesEqual`/constructor over `Ok`/`Err` Result payloads/object-schema with unlisted-field tolerance/exact-length array) into a fresh per-arm bindings map, and raises `MatchError` (`loom/runtime/match-error`) when no arm matches (loom 1.0 performs no static exhaustiveness check). The `?`/`match` panic bypass and the registered `loom/runtime/match-error` message template are deferred to `V4b`. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 16 `V4a-T` tests green; full suite 428 green, typecheck + lint clean. |
| V4a-T | Tests red | 2026-06-30 | `match`, `?`, and `Result` tests (paired `V4a` impl): the parse/type seam `src/parser/match-result.ts` (`checkQuestionOperand`, `checkQuestionScope`, `checkMatchArmTypes` — inert stubs returning no diagnostic / no LUB) and the runtime seam `src/runtime/match-result.ts` (the `Pattern` model, `MatchArm`, the `MatchError` panic class carrying `loom/runtime/match-error`, and `evaluateMatch` — an inert null-sentinel stub that matches no arm and raises no panic), plus `tests/match-result.test.ts` with 16 tests pinning errors-and-results/error-model.md and its expressions.md / type-system.md closure. `ERR-18`: a `?` on a non-`Result<_, QueryError>` operand fires `loom/parse/question-on-non-result` (type phase). `?` outside a `Result`-compatible scope fires `loom/parse/question-outside-result-fn` (inferred and Result-compatible scopes pass). `loom/parse/match-arm-type-mismatch`: arms sharing no common upper bound fire the diagnostic, and a well-typed `match` resolves to the LUB of its arms (`integer ⊔ number = number`). `loom/runtime/match-error`: a scrutinee matching none of the arms raises `MatchError`, while each of the six pattern forms (wildcard/identifier/literal/constructor/object-schema/array) binds and evaluates the selected arm — the panic's `?`/`match` bypass and registered message template are deferred to `V4b-T`. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 13 tests red on their own primary assertions (absent diagnostic, absent LUB, unthrown `MatchError`, null sentinel) because the V4a behaviour is absent — fail-red-for-the-intended-reason; the 3 negative-case assertions pass; the other 415 tests stay green. |
| V4d | Complete | 2026-06-30 | `QueryError` variant schema (paired with `V4d-T`): fills the four behaviour-bearing helpers in `src/runtime/query-error.ts`. `ERR-14` `orderValidationIssues` returns a stable ascending sort keyed on (path, schema_keyword, message), each field compared by Unicode code point via an iterator-based `compareByCodePoint` (so U+FFFF sorts before astral U+1F600, where a UTF-16 code-unit `<` would invert them); the sort is stabilised by a captured input index. `ERR-15` `loom10QueryErrorKinds()` returns the nine wire tags in union-declaration order (frozen as an `as const` module constant, immutable so the no-globals scan admits it) and `isLoom10QueryErrorKind` is the closed-set membership predicate, while `kind` stays typed `string` at the type level. `ERR-17` `synthesizeForcedRespondIssue` emits the synthesised `ValidationIssue` (path `""`, schema_keyword `"required"`, plain-text vs wrong-tool two-arm message). `ERR-19` `makeToolLoopExhaustedError` builds the `tool_loop_exhausted` error with `rounds == maxRounds`. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 13 `V4d-T` tests green; full suite 412 green, typecheck + lint clean. |
| V4d-T | Tests red | 2026-06-30 | `QueryError` variant schema tests (paired `V4d` impl): the `src/runtime/query-error.ts` seam — the nine-variant `QueryError` union and its `ValidationIssue` / `ValidationError` shapes (`kind` typed `string` on every arm, the ERR-15 type-openness seam), plus four inert stubs (`orderValidationIssues` identity copy; `loom10QueryErrorKinds()`→`[]` / `isLoom10QueryErrorKind`→`false`; `synthesizeForcedRespondIssue`→sentinel issue; `makeToolLoopExhaustedError`→wrong-`kind` / `rounds:-1`) — plus `tests/queryerror-variants.test.ts` with 13 tests pinning queryerror-variants.md. `ERR-14`: `orderValidationIssues` sorts ascending on the tuple (path, schema_keyword, message), `[0]` is the canonically-first issue, the sort is stable for equal-key entries, and comparison is by Unicode code point not UTF-16 code unit (U+FFFF before U+1F600). `ERR-15`: the runtime conformance set is exactly the nine wire tags and `isLoom10QueryErrorKind` rejects a future `"binder"` tag, while the type-level witness `const k: QueryError["kind"] = "binder"` compiles (the open seam). `ERR-17`: the forced-respond non-compliance synthesised `ValidationIssue` (path `""`, schema_keyword `"required"`, plain-text vs wrong-tool two-arm message). `ERR-19`: the `ToolLoopExhaustedError` shape (`kind: "tool_loop_exhausted"`, `rounds == max_rounds`, `last_tool_name` incl. the `null` forward-compat branch, `raw_response` incl. the pure-tool-use `null` branch). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 12 tests red on their own primary assertions (unsorted identity output; empty conformance set; sentinel issue; wrong exhaustion shape) because the V4d behaviour is absent — fail-red-for-the-intended-reason; the one passing test is the static type-openness witness; the other 400 tests stay green. |
| V3h | Complete | 2026-06-30 | Expression stdlib `object` members (paired with `V3h-T`): fills `src/runtime/stdlib-object.ts`. `evaluateObjectMember(receiver, member, args)` dispatches the loom-1.0 `object` stdlib table of expressions.md §"Built-in methods and properties" (the EXPR code-keyed area): `keys()` returns the loom-side field names as `array<string>` via `Object.keys` (the object value's own key order — schema declaration order for named schemas, insertion order otherwise; both reduce to own-key order at runtime), `values()` returns the field values as a heterogeneous `array<T>` via `Object.values` in the same order as `keys()`, and `has(k)` tests own loom-side names only via `Object.prototype.hasOwnProperty.call` (not the JS prototype chain, so `toString` reports absent), returning `false` for an unknown key without panic. Pure dispatch — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call; not wired into the V3a evaluator (no test exercises it, matching the V3f/V3g parity). All 4 `V3h-T` tests green; full suite 399 green, typecheck + lint clean. |
| V3h-T | Tests red | 2026-06-30 | Expression stdlib `object` member tests (paired `V3h` impl): the `src/runtime/stdlib-object.ts` seam (`evaluateObjectMember(receiver, member, args)` → `LoomValue`, inert `null`-sentinel stub) plus `tests/expression-stdlib-object.test.ts` with 4 failing tests pinning expressions.md §"Built-in methods and properties" (the EXPR code-keyed area), `object` table. `keys()` returns the loom-side field names as `array<string>` in schema declaration order for named schemas (`{name,role,experience_years}`→`["name","role","experience_years"]`) and insertion order otherwise (`{z,a,m}`→`["z","a","m"]`), with the empty object yielding `[]`; `values()` returns the field values as a heterogeneous `array<T>` in the same order as `keys()` (`→["Ada","reviewer",7]`), empty object→`[]`; `has(k)` returns `true` for a present loom-side name, `false` for an unknown key without panic, and `false` for an inherited prototype name (`toString`) — the safe-check is over the object's own loom-side names. No new diagnostics-registry codes (the `object` stdlib members assert pure behaviour / return type, no diagnostic assertions). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 4 tests red on their own primary assertions (the `null` sentinel equals no expected value) because the V3h `object` stdlib surface is absent — fail-red-for-the-intended-reason; the other 395 tests stay green. |
| V3g | Complete | 2026-06-30 | Expression stdlib `array<T>` members (paired with `V3g-T`): fills `src/runtime/stdlib-array.ts`. `evaluateArrayMember(receiver, member, args)` dispatches the loom-1.0 `array<T>` stdlib table of expressions.md §"Built-in methods and properties" (the EXPR code-keyed area): `length` is the element count (`receiver.length`), `join(sep)` concatenates the (string) elements (`receiver.join(sep)`; the parse-time precondition guarantees a `string` element type), `includes(x)`/`indexOf(x)` use the V2c `valuesEqual` loom structural equality via `some`/`findIndex` (reference-distinct but structurally-equal object matches; `indexOf` returns the first index or `-1`), and `slice(start, end?)` delegates to `Array.prototype.slice` for JS semantics (negative-index-from-end, exclusive `end`, omitted `end`→length). `checkArrayJoin(elementType, site)` returns `loom/parse/non-string-array-join` (message anchored to code-registry-parse.md, `<element>` rendered via `displayType`) unless the element type is `string` (a `string`-typed literal element is equally admissible). Pure dispatch — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call; not wired into the V3a evaluator (no test exercises it, matching the V3f parity). All 8 `V3g-T` tests green; full suite 395 green, typecheck + lint clean. |
| V3g-T | Tests red | 2026-06-30 | Expression stdlib `array<T>` member tests (paired `V3g` impl): the `src/runtime/stdlib-array.ts` seam (`evaluateArrayMember(receiver, member, args)` → `LoomValue`, inert `null`-sentinel stub; `checkArrayJoin(elementType, site)` → `Diagnostic \| undefined`, inert `undefined` stub) plus `tests/expression-stdlib-array.test.ts` with 8 failing tests pinning expressions.md §"Built-in methods and properties" (the EXPR code-keyed area), `array<T>` table. `length` is the element count (`[1,2,3]`→3, `[]`→0); `join(sep)` concatenates `string` elements (`["a","b","c"].join(", ")`→`"a, b, c"`, single→element, empty→`""`), and `checkArrayJoin` fires `loom/parse/non-string-array-join` (message `array.join requires a string element type; got array<integer>`, anchored to code-registry-parse.md) for a non-`string` element type while a `string` element type raises none; `includes(x)` / `indexOf(x)` use the V2c `valuesEqual` loom structural equality (reference-distinct but structurally-equal object matches), with `indexOf` returning the first index and `-1` when absent; `slice(start, end?)` follows JS semantics — exclusive `end` (`slice(1,3)`→`[1,2]`), negative-index-from-end (`slice(-2)`→`[3,4]`, `slice(1,-1)`→`[1,2,3]`), omitted `end` to length (`slice(2)`→`[2,3,4]`). The `join` accept arm is folded with its firing assertion so the inert stub cannot pass it vacuously. `array<T>.concat`'s LUB element type is owned by `V3f-T` and not re-declared. No new diagnostics-registry codes (`loom/parse/non-string-array-join` already in code-registry-parse.md). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 8 tests red on their own primary assertions (the `null` sentinel equals no expected value; the `undefined` join-check equals no expected diagnostic) because the V3g `array<T>` stdlib surface is absent — fail-red-for-the-intended-reason; the other 387 tests stay green. |
| V3f | Complete | 2026-06-30 | Expression stdlib `string` members (paired with `V3f-T`): fills `src/runtime/stdlib-string.ts`. `evaluateStringMember(receiver, member, args)` dispatches the loom-1.0 `string` stdlib table of expressions.md §"Built-in methods and properties" (the EXPR code-keyed area): `length` is the UTF-16 code-unit count (`receiver.length`; no grapheme / code-point segmentation), `toLowerCase`/`toUpperCase`/`trim` are the locale-independent host transforms (`"ß".toUpperCase()`→`"SS"`, NBSP trimmed both ends), `startsWith`/`endsWith`/`includes` return `boolean` with JS semantics, and `split(sep)` returns `array<string>` (literal-only) with the empty separator decomposing into one string per UTF-16 code unit (`"a💩".split("")`→`["a","\ud83d","\udca9"]`). `replace(from, to)` is implemented by `replaceLiteral` — a single left-to-right, non-overlapping `indexOf` scan inserting `to` literally (it does **not** use host `String.prototype.replaceAll`, whose string form interprets `$&`/`$$`/`$n`), with an empty `from` returning the receiver unchanged; reproduces the five normative reference vectors exactly (`"aXbXc".replace("X","[$&]")`→`"a[$&]b[$&]c"`, `"100".replace("0","$$")`→`"1$$$$"`, `"a-b".replace("-","x$1y")`→`"ax$1yb"`, `"abc".replace("","X")`→`"abc"`, `"aaaaa".replace("aa","x")`→`"xxa"`). `concatElementType(left, right, env)` computes the `array<T>.concat` LUB element type via the V2b `checkCompatible` `⊑` relation: if one element type is `⊑` the other the wider one is the LUB (collapsing identical types and applying `integer ⊑ number` in both call directions), else disjoint types union receiver-first (`integer | string`). Pure functions plus one bounded scan loop — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. All 17 `V3f-T` tests green; full suite 387 green, typecheck + lint clean. |
| V3f-T | Tests red | 2026-06-30 | Expression stdlib `string` member tests (paired `V3f` impl): the `src/runtime/stdlib-string.ts` seam (`evaluateStringMember(receiver, member, args)` → `LoomValue`, inert `null`-sentinel stub; `concatElementType(left, right, env)` → `CompatType`, inert `null`-primitive stub) plus `tests/expression-stdlib-string.test.ts` with 17 failing tests pinning expressions.md §"Built-in methods and properties" (the EXPR code-keyed area). The five normative `String.replace` reference vectors (all-occurrences + literal `$&`, literal `$$`, literal `$n`, empty `from` unchanged, left-to-right non-overlapping scan `"aaaaa".replace("aa","x")`→`"xxa"`); `array<T>.concat` LUB element type (`integer ⊔ number = number` both directions, identical-type collapse, disjoint → `integer | string` receiver-first). The `string` members: `length` is the UTF-16 code-unit count (`"a💩".length`→3, distinguishing it from a code-point count), `toLowerCase`/`toUpperCase`/`trim` are the locale-independent transforms (`"ß".toUpperCase()`→`"SS"`, NBSP-trim both ends), `startsWith`/`endsWith`/`includes` return `boolean`, and `split` returns `array<string>` with the empty separator decomposing into one string per code unit (`"a💩".split("")`→`["a","\ud83d","\udca9"]`). No new diagnostics-registry codes (the EXPR stdlib members assert pure behaviour / return type, no diagnostics). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 17 tests red on their own primary assertions (the `null` sentinel equals no expected value; the `null`-prim sentinel equals no expected type) because the V3f stdlib surface is absent — fail-red-for-the-intended-reason; the other 370 tests stay green. |
| V3c | Complete | 2026-06-30 | Control flow (paired with `V3c-T`): fills `src/parser/control-flow.ts` and `src/runtime/control-flow.ts` per control-flow.md. `checkForIterand` fires `loom/parse/non-array-iterand` (`'for' expects array<T> after 'in'; got <type>`, `<type>` rendered via the V2b `displayType`, type phase) for any iterand whose static type is not `array<T>` (string / number / object) and raises none for an `array<T>` iterand. `checkBreakStatement` fires `loom/parse/break-outside-loop` (`'break' outside of a loop`) for a `break` outside any loop, else `loom/parse/break-with-value` (`'break' takes no value in loom 1.0`) for a `break expr`, accepting a valueless in-loop `break`. `checkContinueStatement` fires `loom/parse/continue-outside-loop` (`'continue' outside of a loop`) for a `continue` outside any loop. `evaluateForLoop` implements CTRL-1: it calls `host.evaluateIterand` exactly once at loop entry, captures the `array<T>` snapshot, then runs the body once per element in order — so the iterand effect commits once even for an empty array (body skipped) and a body-side `let mut` reassignment cannot alter the captured snapshot. Messages anchored to code-registry-parse.md per the *Diagnostic message anchors* rule; no new registry codes (all four asserted codes already present). `displayType` exported from `type-compat.ts` for reuse. Pure functions / a small loop — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. All 12 `V3c-T` tests green; full suite 370 green, typecheck + lint clean. |
| V3c-T | Tests red | 2026-06-30 | Control-flow tests (paired `V3c` impl): the `src/parser/control-flow.ts` seam (`checkForIterand` / `checkBreakStatement` / `checkContinueStatement`, all inert stubs returning `undefined`; the `ControlFlowSite` / `ForIterand` {type: CompatType} / `BreakStatement` {insideLoop, hasValue} / `ContinueStatement` {insideLoop} shapes) and the `src/runtime/control-flow.ts` seam (`evaluateForLoop(host)` inert no-op; the `ForLoopHost` collaborator `evaluateIterand` / `runIteration`) plus `tests/control-flow.test.ts` with 9 failing obligation assertions pinning control-flow.md. CTRL-1 (asserted against `evaluateForLoop`): the `for` iterand is evaluated exactly once at loop entry before the first iteration; the effect commits once even when the array is empty and the body is skipped; a mid-body `let mut` reassignment does not alter the snapshotted sequence. `loom/parse/non-array-iterand` (`'for' expects array<T> after 'in'; got <type>`, type phase) for a string / number / object iterand, while an `array<T>` iterand raises none; `loom/parse/break-outside-loop` (`'break' outside of a loop`, parse phase) for a `break` outside any loop, while a valueless in-loop `break` raises none; `loom/parse/continue-outside-loop` (`'continue' outside of a loop`, parse phase) for a `continue` outside any loop, while an in-loop `continue` raises none; `loom/parse/break-with-value` (`'break' takes no value in loom 1.0`, parse phase) for a `break expr`. Messages anchored to code-registry-parse.md per the *Diagnostic message anchors* rule; each test cites its `CTRL-1` / `loom/parse/...` code inline. No new diagnostics-registry codes (all four asserted codes already in code-registry-parse.md). Inert stub seams: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 9 obligation tests red on their own primary assertions (an absent expected diagnostic; an iterand-evaluation count of `0` / no recorded iteration) because the V3c control-flow checker and loop evaluator are absent — fail-red-for-the-intended-reason; the 3 acceptance cases (`array<T>` iterand / in-loop `break` / in-loop `continue`) pass and the other 361 tests stay green. |
| V3b | Complete | 2026-06-30 | Bindings and mutability (paired with `V3b-T`): fills the five `src/parser/bindings.ts` checkers. `checkLetBinding` fires `loom/parse/let-without-initialiser` (`let binding '<name>' has no initialiser`) for an annotation-only `let x: T` and accepts an initialised `let` (bindings.md §`let` requires an initialiser). `checkReassignment` fires `loom/parse/immutable-rebinding` (`cannot reassign immutable binding '<name>'`) for a non-`mut` target and accepts a `let mut` reassignment, incl. the compound `+=` forms (§`let` vs `let mut`). `checkAssignmentTarget` fires `loom/parse/assignment-to-member-or-index` (`cannot assign to member or index; mutability is binding-level only`) for member/index targets and accepts a plain identifier (§Mutability is binding-level only). `checkMutModifier` fires `loom/parse/mut-on-immutable-context` (`'mut' is not permitted in this binding position`) for `mut` on a function parameter / `for` iteration variable / `match` pattern binding and accepts the `let` position (§Immutable contexts). `checkIncrementDecrement` always fires `loom/parse/increment-decrement` (`'<op>' operator is not supported`, `<op>` verbatim) for `++` / `--` (§Increment / decrement). Messages + hints anchored to code-registry-parse.md per the *Diagnostic message anchors* rule; no new registry codes. Pure functions — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. All 13 `V3b-T` tests green; full suite 358 green, typecheck + lint clean. |
| V3b-T | Tests red | 2026-06-30 | Bindings-and-mutability tests (paired `V3b` impl): the `src/parser/bindings.ts` seam (`checkLetBinding` / `checkReassignment` / `checkAssignmentTarget` / `checkMutModifier` / `checkIncrementDecrement`, all inert stubs returning `undefined`; the `BindingSite` / `LetBindingDecl` / `BindingReassignment` / `AssignmentTarget` (`AssignTargetKind` = `identifier`/`member`/`index`) / `MutModifier` (`BindingPosition` = `let`/`fn-param`/`for-var`/`match-bind`) / `IncrementDecrementOp` shapes) plus `tests/bindings.test.ts` with 9 failing obligation assertions pinning bindings.md. `loom/parse/immutable-rebinding` (`cannot reassign immutable binding '<name>'`) for a non-`mut` reassignment, while a `let mut` reassignment raises none; `loom/parse/assignment-to-member-or-index` (`cannot assign to member or index; mutability is binding-level only`) for `obj.field = …` / `arr[i] = …` targets, while a plain identifier target raises none; `loom/parse/mut-on-immutable-context` (`'mut' is not permitted in this binding position`) for `mut` on a function parameter / `for` iteration variable / `match` pattern binding, while `let mut` raises none; `loom/parse/increment-decrement` (`'<op>' operator is not supported`, `<op>` verbatim) for `++` and `--`; `loom/parse/let-without-initialiser` (`let binding '<name>' has no initialiser`) for an initialiser-less `let`, while an initialised `let` raises none. Messages anchored to code-registry-parse.md per the *Diagnostic message anchors* rule; each test cites its `loom/parse/...` code inline. No new diagnostics-registry codes (all five asserted codes already in code-registry-parse.md). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 9 obligation tests red on their own primary assertions (an absent expected diagnostic) because the V3b binding checker is absent — fail-red-for-the-intended-reason; the 4 acceptance cases (valid `let mut` / identifier target / `let mut` position / initialised `let`) pass and the other 345 tests stay green. |
| V3a | Complete | 2026-06-30 | Expression evaluator (paired with `V3a-T`): fills `src/runtime/expression-evaluator.ts`. A self-contained tokenizer + recursive-descent parser + tree-walking evaluator for the bounded TypeScript subset of expressions.md §"Supported forms" exercised by V3a (number/string/boolean/null literals, identifier reads, `name(args)` calls, unary `!`/`-`, binary `+ - * / %` / `< <= > >=` / `== !=`, logical `&&`/`||`, ternary `?:`, parens). Precedence/associativity follow the §"Operator precedence" table (highest→lowest: primary/call, unary right-assoc, `* / %` left, `+ -` left, ordering non-assoc, equality non-assoc, `&&` left, `||` left, ternary right-assoc). Evaluation is left-to-right with `&&`/`||` short-circuit and ternary branch-selection routed through `EvalHost.callFunction`, so a short-circuited / not-taken operand's call never runs (§"Evaluation order and short-circuiting"). `==`/`!=` route through the V2c `valuesEqual` structural relation (cross-type → `false`/`true`, no panic; `42 == 42.0` → `true`); `+` concatenates two strings else adds; `-`/`*`/`%` numeric; `/` always `number`; div/mod-by-zero use the host's native IEEE-754 (`1/0`→`Infinity`, `0/0`/`5%0`→`NaN`) with no panic; ordering uses the host relational operators (signed numeric / UTF-16 code-unit, `NaN`→`false`). `checkBooleanPosition` routes the operand type through the V2b `⊑` relation against `boolean` and fires `loom/parse/non-boolean-condition` ("condition must be boolean; got <type>", message anchored to code-registry-parse.md) for any non-`boolean`, deferring a statically-unresolvable operand to the runtime safety net. No module-level mutable state (the operator constants are immutable), no broad catch / ambient primitive / Promise combinator / Sync call. All 21 `V3a-T` tests green; full suite 345 green, typecheck + lint clean; H2a/H3a scans green. |
| V3a-T | Tests red | 2026-06-30 | Expression-evaluator tests (paired `V3a` impl): the `src/runtime/expression-evaluator.ts` seam (`evaluateSource(source, host)` → `LoomValue`, inert `null`-sentinel stub; the `EvalHost` collaborator `resolveIdentifier` / `callFunction`; `checkBooleanPosition({position, operandType, site})` → `Diagnostic[]`, inert `[]` stub; `BooleanPosition` = `if`/`while`/`ternary-condition`/`&&`/`||`) plus `tests/expression-evaluator.test.ts` with 21 failing tests pinning expressions.md (the EXPR code-keyed area) + the `loom/parse/non-boolean-condition` diagnostic. §Operator precedence (observed through evaluation): `*` tighter than `+` (`2 + 3 * 4`→14), parens override (`(2+3)*4`→20), `+`/`-` left-assoc (`2-3-4`→-5), unary tighter than `==` (`!true == false`→true), ordering tighter than equality (`2 < 3 == true`→true), `&&` tighter than `||` (`true || false && false`→true), ternary right-assoc (`false ? 1 : true ? 2 : 3`→2), unary `-` tighter than `*` (`-2*3`→-6), and an identifier read resolved through the host. §Evaluation order / short-circuiting: `&&`/`||` evaluate the right operand's call iff the left selects it (must-run + short-circuit asserted in one test so the stub reds on the must-run case), and a ternary evaluates only the taken branch's call. §Equality / §Ordering / §Other arithmetic: cross-type `==` is `false` and `!=` `true` (no panic), `42 == 42.0` is `true`, `integer ⊑ number` mixed `+` value (`1+2.5`→3.5), `/` always `number` and `1/0`→`Infinity`/`-1/0`→`-Infinity`/`0/0`→`NaN`/`5%0`→`NaN` without panic, signed ordering (`-5<3`) and every ordering against `NaN` `false`. §Truthiness: `checkBooleanPosition` fires `loom/parse/non-boolean-condition` (`condition must be boolean; got <type>`, message anchored to code-registry-parse.md) for a non-`boolean` ternary-condition / `&&` / `if` / `while` operand and raises none for a `boolean` operand. Each negative (no-effect / no-diagnostic) assertion is folded into a test whose must-run / firing assertion reds the stub, so no test passes vacuously. Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 21 tests red on their own primary assertions (the `null` sentinel equals no expected value, the short-circuited must-run call is never recorded, and the boolean-position check returns no diagnostic) because the V3a interpreter and boolean-position check are absent — fail-red-for-the-intended-reason; the other 324 tests stay green. |
| V2b | Complete | 2026-06-30 | Type-compatibility engine (`⊑`) (paired with `V2b-T`): fills `src/parser/type-compat.ts`. `checkCompatible(sub, sup, env)` decides the directed relation `T₁ ⊑ T₂` over the `CompatType` model: aliases are unfolded transparently on both sides first (TYPE-11, recursing through nested aliases; an unresolvable `named` stays `named` → `"unknown"` → runtime AJV safety net), then the decision procedure applies TYPE-6 union-distribution on the left, TYPE-5 union-widening on the right, TYPE-7 array covariance, TYPE-8 inline-object field-wise (exact field set, order-irrelevant, no excess-property widening), TYPE-10 nominal name-identity for `named` object schemas (never crosses the inline/named boundary nor relates distinct names), and TYPE-1/TYPE-2/TYPE-3 at the primitive/literal leaves (reflexivity, one-way `integer⊑number`, and the reverse `number⊑integer` classified `integer-narrowing`). The three per-site checkers report TYPE-9 diagnostics: `checkLetRhsCompat` fires `loom/parse/integer-narrowing` (`cannot narrow number to integer`) for the narrowing case and `loom/parse/let-rhs-type-mismatch` otherwise; `checkFnArgCompat` fires `loom/parse/fn-arg-type-mismatch`; `checkCommonType` fires `loom/parse/array-element-type-mismatch` at the first branch failing an in-scope sink and `loom/parse/array-no-common-type` when no sink and the branches share no common type. A statically-unresolvable operand yields no per-site diagnostic (runtime AJV safety net). Messages anchored to code-registry-parse.md; no new registry codes. Pure functions — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. All 29 `V2b-T` tests green; full suite 324 green, typecheck + lint clean. |
| V2b-T | Tests red | 2026-06-30 | Type-compatibility engine (`⊑`) tests (paired `V2b` impl): the `src/parser/type-compat.ts` seam (`checkCompatible(sub, sup, env)` → `Compatibility` = `compatible`/`incompatible`/`integer-narrowing`/`unknown`, inert stub returning the `unknown` sentinel; the per-site diagnostic seams `checkLetRhsCompat` / `checkFnArgCompat` / `checkCommonType`, inert no-ops returning `[]`; the `CompatType` model — `prim`/`literal`/`named`/`array`/`union`/`object` — `NamedDecl` = `object-schema` (nominal, TYPE-10) / `alias` (transparent, TYPE-11), and `TypeEnv`) plus `tests/type-compat.test.ts` with 29 failing tests pinning type-system.md §"Type compatibility" TYPE-1…TYPE-11. TYPE-1 reflexivity (`T ⊑ T` for an identical primitive and an identical named schema); TYPE-2 `integer ⊑ number` one-way + the reverse classified `integer-narrowing` and the `loom/parse/integer-narrowing` site diagnostic (`cannot narrow number to integer`); TYPE-3 literal-to-primitive (`"validation" ⊑ string`, `42 ⊑ integer`/`number`, `true ⊑ boolean`, `null ⊑ null`); TYPE-4 variant-to-union; TYPE-5 union widening `T ⊑ T|U`; TYPE-6 union-distributive (`T₁|T₂ ⊑ T₃` iff each arm); TYPE-7 array covariance `array<T₁> ⊑ array<T₂>` iff `T₁ ⊑ T₂`; TYPE-8 inline-object field-wise with exact field set (extra field never widens); TYPE-9 per-site codes `loom/parse/let-rhs-type-mismatch`, `loom/parse/fn-arg-type-mismatch`, `loom/parse/array-element-type-mismatch` (branch vs sink) and `loom/parse/array-no-common-type` (no sink, no common type); TYPE-10 nominal object schemas (two distinct named schemas, and the named↔inline boundary both ways, are incompatible by shape); TYPE-11 alias transparency (`"low" ⊑ Severity`, `StringOrNumber ⊑ string | number`, and `schema Y = Cat` unfolding to the nominal `Cat`). Messages anchored to code-registry-parse.md per the *Diagnostic message anchors* rule, each test citing its TYPE-id / code inline. Typecheck + lint clean; the 29 tests red on their own primary assertions (the relation seam returns the `unknown` sentinel — equal to no expected outcome — and each per-site seam returns no diagnostic) because the V2b engine is absent — fail-red-for-the-intended-reason; the other 295 tests stay green. |
| V5d | Complete | 2026-06-30 | Schema-subset reject gate (paired with `V5d-T`): fills `src/parser/schema-subset-gate.ts`. `checkSubsetKeyword` checks a candidate JSON-Schema keyword against the fixed loom-defined permitted subset (`type`/`anyOf`/`enum`/`const`/`properties`/`required`/`additionalProperties`/`items`/`$defs`/`$ref`, held as an immutable module-level `ReadonlySet`) and returns `loom/parse/unsupported-feature` (message `unsupported syntactic feature: <keyword>`, anchored to code-registry-parse.md) for every other keyword — an **allowlist / reject-by-default** gate, so enumerated-unsupported keywords (`oneOf`/`pattern`/`nullable`/…) and out-of-subset keywords absent from that enumeration alike (`title`/`$comment`/`readOnly`/…) are both rejected, excluding a denylist build. `checkSubsetKeywords` maps the per-keyword check over a sequence, emitting one diagnostic per rejected keyword in input (array element) order. `checkSchemaFeedingType` delegates to the V2a `parseTypeExpression` at the `schema-feeding` position, which walks the whole type tree (including `array<T>` element types and union arms) and raises `loom/parse/result-in-schema-position` for any reachable `Result` application while accepting a lowerable `string \| null`. No new registry codes (both asserted codes already present). Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 44 `V5d-T` tests green; full suite 295 green, typecheck + lint clean. |
| V5d-T | Tests red | 2026-06-30 | Schema-subset reject-gate tests (paired `V5d` impl): the `src/parser/schema-subset-gate.ts` seam (`checkSubsetKeyword(keyword, site)` / `checkSubsetKeywords(keywords, site)` / `checkSchemaFeedingType(typeSource, site)`, inert accept-all no-op stubs; `SchemaSubsetSite` shape) plus `tests/schema-subset-gate.test.ts` with 44 failing tests pinning schema-subset.md. The gate is an **allowlist / reject-by-default** gate: `checkSubsetKeyword` fires `loom/parse/unsupported-feature` (message anchored to the registry `unsupported syntactic feature: <construct>` template prefix) for each of the 30 enumerated rejected JSON-Schema keywords (`oneOf`/`allOf`/`not`/`if`/`then`/`else` composition + the `pattern`…`nullable` enumeration) and accepts the 10 permitted-subset keywords (`type`/`anyOf`/`enum`/`const`/`properties`/`required`/`additionalProperties`/`items`/`$defs`/`$ref`). Reject-by-default: 9 out-of-subset keywords absent from both the permitted set and the enumerated unsupported list (`title`/`description`/`examples`/`$comment`/`$anchor`/`$dynamicRef`/`readOnly`/`writeOnly`/`deprecated`) are still rejected with `loom/parse/unsupported-feature`, witnessing the allowlist (excludes a denylist build). `checkSchemaFeedingType` fires `loom/parse/result-in-schema-position` (message from code-registry-parse.md) for a bare `Result<T, E>` and for a `Result` reached through an `array<T>` element, and accepts a lowerable `string | null`. `checkSubsetKeywords` returns one diagnostic per rejected keyword in input (array element) order. The accept arms are paired with a reject assertion in the same test so an accept-all stub cannot pass them vacuously. Typecheck + lint clean; the 44 tests red on their own primary assertions (an absent expected rejection / wrong rejection order) because the V5d allowlist gate is absent — fail-red-for-the-intended-reason; the other 251 tests stay green. |
| V5c | Complete | 2026-06-30 | Descriptions (`///`) (paired with `V5c-T`): fills `src/parser/descriptions.ts`. `joinDocComment` joins the `///` `RestOfLine` contents with `\n` and strips the common leading whitespace of the non-blank lines (the query-template dedent alphabet {U+0020, U+0009}; whitespace-only lines are ignored for the common prefix and normalised to an empty line). `extractDescription` collects only the maximal trailing run of `///` lines (`^[ \t]*///(?!/)(.*)$`, the `(?!/)` keeping a four-slash `////` line out of the run), so a regular `//` line is not propagated and a block with no `///` line yields `undefined`. `lowerDescription` writes the joined text byte-for-byte into `description:` for the `schema`/`enum`/`field`/`variant` anchors (pre-existing fragment keys preserved) and returns the fragment unchanged for `fn` (functions have no JSON Schema — the `///` stays AST-only). `checkDocCommentPlacement` fires `loom/parse/doc-comment-misplaced` (`'///' doc comment is not legal above this production`) for any production outside the five eligible anchors and `undefined` for an eligible anchor. Message anchored to code-registry-parse.md per the *Diagnostic message anchors* rule. Pure functions — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. All 4 `V5c-T` tests green; full suite 251 green, typecheck + lint clean. |
| V5c-T | Tests red | 2026-06-30 | Descriptions (`///`) tests (paired `V5c` impl): the `src/parser/descriptions.ts` seam (`joinDocComment` / `extractDescription` / `lowerDescription` / `checkDocCommentPlacement` inert stubs; `DocCommentSite` / `DocAnchorKind` shapes) plus `tests/descriptions.test.ts` with 4 failing tests pinning descriptions.md + grammar.md §`///` placement. §No transformation / §Placement: `lowerDescription` writes the joined text byte-for-byte into `description:` for `schema`/`enum`/`field`/`variant` anchors (base fragment keys preserved), while a `fn` anchor lowers nowhere — the `///` stays AST-only (no `description`). §Placement / grammar.md §`///` placement: `loom/parse/doc-comment-misplaced` (message `'///' doc comment is not legal above this production`) fires above `let`/`import`/`export`/expression/control-flow productions and raises none above the five eligible anchors. §Multi-line: `joinDocComment` joins consecutive `///` `RestOfLine` contents with `\n`, strips the common leading whitespace of non-blank lines (query-template dedent), and turns an empty `///` line into a blank line. §`//` is a regular code comment: `extractDescription` collects only the trailing `///` run, so a `//` line is not propagated. Message anchored to code-registry-parse.md per the *Diagnostic message anchors* rule. Typecheck + lint clean; the 4 tests red on their own primary assertions (absent description / absent diagnostic / empty join) because the V5c lowering is absent — fail-red-for-the-intended-reason; the other 247 tests stay green. |
| V5b | Complete | 2026-06-30 | Discriminated unions, recursion, and cycle detection (paired with `V5b-T`): implements the three V5b seams on `src/parser/schema-declarations.ts`. `checkDiscriminatedUnion` runs implicit detection on the wire name (`wireName ?? name`) — a field present in every variant as a single string literal with unique values qualifies; exactly one qualifier is the discriminator (no diagnostic), two or more fire `loom/parse/ambiguous-discriminator` (candidates in source order), a present-in-all single-string-literal field with duplicate values fires `loom/parse/duplicate-discriminator-value`, a present-in-all single-literal field whose type is non-string fires `loom/parse/non-string-discriminator` (`got <kind>`), and otherwise `loom/parse/missing-discriminator`. The explicit `by <field>` path checks the named field: nested value first (`loom/parse/nested-discriminator`), then non-string literal (`loom/parse/non-string-discriminator`), then duplicate value (`loom/parse/duplicate-discriminator-value`). `checkByClause` fires `loom/parse/by-on-object-schema` for the object form and nothing for the union form. `detectTypeAliasCycles` runs a recursion-stack DFS over the resolved reference graph: a back-edge whose cycle nodes are all aliases fires `loom/parse/type-alias-cycle` (path joined with ` → `, deduped per node-set), while a cycle traversing at least one object-schema hop is legal and raises nothing. Parse-time literal values rendered bare-when-identifier-shaped per placeholder-rendering-b.md. Pure functions — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. All 7 `V5b-T` tests green; full suite 247 green, typecheck + lint clean. |
| V5b-T | Tests red | 2026-06-30 | Discriminated-union / recursion / cycle-detection tests (paired `V5b` impl): three new inert seam stubs on `src/parser/schema-declarations.ts` (`checkDiscriminatedUnion(decl, site)` with `DiscriminatedUnionDecl` / `UnionVariantSchema` / `DiscriminatorCandidateField`; `checkByClause(decl, site)` with `ByClauseDecl` form `object`/`union`; `detectTypeAliasCycles(nodes, site)` with `SchemaGraphNode` kind `alias`/`object`) plus `tests/disc-unions-recursion.test.ts` with 7 failing tests pinning schemas.md §Discriminated unions + §Recursion. §Discriminated unions: `loom/parse/non-string-discriminator` (integer `kind` → `discriminator 'kind' on Animal must be a string-literal type; got integer`), `loom/parse/ambiguous-discriminator` (two qualifying fields → `ambiguous discriminator for Animal; candidates: kind, species. Declare explicitly with 'by <field>'.`), `loom/parse/missing-discriminator` (no shared single-literal field → `Animal is a union of object schemas with no shared single-literal discriminator field. ...`), `loom/parse/duplicate-discriminator-value` (explicit `by kind`, both `kind: "same"` → `duplicate discriminator value 'same' across variants of Animal`), `loom/parse/nested-discriminator` (`kind: { type: "x" }` → `discriminator field 'kind' must be at the top level of each variant of Animal`), and `loom/parse/by-on-object-schema` (`schema X by f { ... }` → `the 'by' clause applies only to discriminated-union schemas (schema X by f = A | B | …)`, while the union form raises none). §Recursion: `loom/parse/type-alias-cycle` (pure-alias cycle `schema X = Y; schema Y = X` → `type-alias cycle: X → Y → X`; a cycle through an object-schema hop `schema Y { f: X }` raises none). Messages anchored to code-registry-parse.md per the *Diagnostic message anchors* rule. Typecheck + lint clean; the 7 tests red on their own primary assertions (an absent expected diagnostic) because the V5b discriminated-union / cycle checker is absent — fail-red-for-the-intended-reason; the other 240 tests stay green. |
| V5a | Complete | 2026-06-30 | Schema declarations (object / alias / enum) (paired with `V5a-T`): fills `src/parser/schema-declarations.ts`. `checkObjectSchema` fires `loom/parse/empty-schema-body` for a no-field schema, `loom/parse/redundant-wire-name` (W) when a field's wire name equals its loom-side name, and `loom/parse/wire-name-collision` when two fields share an effective wire name (`wireName ?? loomName`) or an explicit wire name equals another field's loom-side name (each colliding name reported once, in source order). `checkEnumDeclaration` runs the name-duplication check before the value-duplication check (schemas.md §Enum declarations): `loom/parse/empty-enum-body` for a no-variant enum, `loom/parse/duplicate-enum-variant-name` for a repeated identifier (so `enum X { Low = "a", Low = "b" }` fires the name code, not the value code), `loom/parse/non-string-enum-value` for a non-string explicit value (`got <kind>`), and `loom/parse/duplicate-enum-value` only when ≥2 *distinct* names share one explicit string value. `checkInlineEnumForm` fires `loom/parse/inline-enum` for a leading `enum[...]` form. `checkVariantAccess` fires `loom/parse/unknown-variant` for a reference outside the declared set. Messages anchored to code-registry-parse.md per the *Diagnostic message anchors* rule. Pure functions — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. All 10 `V5a-T` tests green; full suite 240 green, typecheck + lint clean. |
| V5a-T | Tests red | 2026-06-30 | Schema-declaration tests (paired `V5a` impl): the `src/parser/schema-declarations.ts` seam (`checkObjectSchema` / `checkEnumDeclaration` / `checkInlineEnumForm` / `checkVariantAccess`, inert no-op stubs; `ObjectSchemaDecl` / `SchemaFieldDecl` / `EnumDecl` / `EnumVariantDecl` / `EnumValueKind` / `VariantAccess` / `SchemaDeclSite` shapes) plus `tests/schema-declarations.test.ts` with 10 failing tests. schemas.md §Object schema: `loom/parse/empty-schema-body` (`schema X { }` → `'X' has no fields; an empty schema cannot be validated.`, a non-empty schema raises none). §Wire-name renaming: `loom/parse/wire-name-collision` (two fields sharing a wire name, and a wire name equal to another field's loom-side name → `wire name '<name>' collides with another field on schema '<schema>'`), `loom/parse/redundant-wire-name` (W) (`field as "field"` → `redundant 'as' clause: wire name '<name>' equals the loom-side name`, severity `warning`; a genuine rename raises none). §Enum declarations: `loom/parse/empty-enum-body` (`enum X { }` → `'X' has no variants; ...`), `loom/parse/inline-enum` (`enum["a","b"]` → `inline 'enum[...]' is not supported; ...`), `loom/parse/non-string-enum-value` (`Low = 1` → `enum variant value must be a string literal; got integer`), the name-before-value ordering — `enum X { Low = "a", Low = "b" }` fires `loom/parse/duplicate-enum-variant-name` (`duplicate variant name 'Low' on enum 'X'`) and **not** `loom/parse/duplicate-enum-value`, while `enum X { Low = "x", High = "x" }` fires `loom/parse/duplicate-enum-value` (`duplicate enum value 'x' across variants of enum 'X'`). §Variant access: `loom/parse/unknown-variant` (`Severity.Critical` → `unknown variant 'Critical' on enum 'Severity'`, a declared variant raises none). Messages anchored to code-registry-parse.md per the *Diagnostic message anchors* rule. Typecheck + lint clean; the 10 tests red on their own primary assertions (an absent expected diagnostic) because the V5a schema-declaration checker is absent — fail-red-for-the-intended-reason; the other 230 tests stay green. |
| V2d | Complete | 2026-06-30 | Canonical integer/number renderer (paired with `V2d-T`): fills `src/render/canonical-number.ts`. `renderCanonicalNumber(value, kind)` selects BNDR-4 (`integer`) vs BNDR-5 (`number`) from the caller-supplied `kind` discriminator — never the value's runtime integrality — and depends on no value model or schema. A shared `expandToFixedPoint` re-expands `String(n)`'s exponential form (the forbidden `±1e21` large-magnitude and `\|value\| < 1e-7` small-magnitude switches) to pure fixed-point by shifting the decimal point, leaving non-exponential shortest digits untouched; `canonicalDecimal` adds sign handling and the `-0 → 0` pin (`value === 0` covers `+0`/`-0`). BNDR-4: `42`→`42`, `0`→`0`, `-7`→`-7`, `1e21`→`1000000000000000000000` (no exponent/separators), `-0`→`0` (bndr-6p). BNDR-5: `3.14`→`3.14` (bndr-6q), integral `42`→`42` (no `.0`), `1e21`→`1000000000000000000000` (bndr-6r), `1e-8`→`0.00000001` (bndr-6s), interior `5e-8`→`0.00000005`, `-0`→`0` (bndr-6p). Pure functions — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. All 11 `V2d-T` tests green; full suite 230 green, typecheck + lint clean. |
| V2d-T | Tests red | 2026-06-30 | Canonical integer/number renderer tests (paired `V2d` impl): the `src/render/canonical-number.ts` seam (`renderCanonicalNumber(value, kind)` with `NumberKind = "integer" \| "number"`, an inert stub returning `""`) plus `tests/canonical-number-render.test.ts` with 11 failing tests pinning BNDR-4 / BNDR-5 from `binder/defaulting-system-note-echo.md` §"Echo policy". BNDR-4 (`integer` kind): canonical base-10 — `42`→`42`, `0`→`0`, `-7`→`-7`, `1e21`→`1000000000000000000000` (no exponent/separators), `-0`→`0` (bndr-6p). BNDR-5 (`number` kind): shortest round-tripping fixed-point with no scientific notation — `3.14`→`3.14` (non-integral keeps a fractional digit, bndr-6q), `42`→`42` (integral carries no `.0`), the large-magnitude `±1e21` switch expanded (`1e21`→`1000000000000000000000`, bndr-6r), the small-magnitude `\|value\| < 1e-7` switch expanded (`1e-8`→`0.00000001` bndr-6s and the interior vector `5e-8`→`0.00000005`), `-0`→`0` (bndr-6p). The renderer selects BNDR-4 vs BNDR-5 from the caller-supplied kind discriminator, not the value's runtime integrality, and has no dependency on the runtime value model. Typecheck + lint clean; the 11 tests red on their own primary assertions (`expected '' to be …`) because the V2d rendering body is absent — fail-red-for-the-intended-reason; the other 219 tests stay green. |
| V2c | Complete | 2026-06-30 | Runtime value model and equality (paired with `V2c-T`): fills `src/runtime/value.ts`. `makeEnumValue` encodes an enum value as a boxed `String` carrying a **non-enumerable** `__loomEnum` declaring-enum tag, so `JSON.stringify` yields the bare wire string while the tag stays available for cross-enum equality. `valuesEqual` implements `runtime-value-model.md` §Equality structural deep equality: enum values compare declaring-enum tag *and* wire value (`Severity.High == OtherEnum.High` is `false`); `Result` compares the `Ok`/`Err` discriminator and recurses on the payload; arrays compare element-wise at equal length; objects compare loom-side key set and per-key value (order-irrelevant); primitives compare by value with `NaN == NaN` `true` and `+0 == -0` `true`; a cross-type pair (including enum-vs-string and object-vs-primitive) evaluates `false` with no panic and no diagnostic; `42 == 42.0` is `true`. `isWireLowerable` recognises a `Result` value (the `ok`-discriminator shape) as never lowerable, everything else lowerable. No module-level mutable state (`ENUM_TAG` is a constant literal), no broad catch / ambient primitive / Promise combinator / Sync call. All 9 `V2c-T` tests green; full suite 219 green, typecheck + lint clean; H2a/H3a scans green. |
| V2c-T | Tests red | 2026-06-30 | Runtime value-model + equality tests (paired `V2c` impl): the `src/runtime/value.ts` seam (`LoomValue`; the opaque branded `EnumValue`; the `ResultValue` `Ok`/`Err` discriminated union; the `makeEnumValue` / `makeOk` / `makeErr` constructors; the `valuesEqual` structural-equality relation and the `isWireLowerable` predicate, both inert stubs) plus `tests/runtime-value-model.test.ts` with 9 failing tests pinning the `runtime-value-model.md` RVM code-keyed area. Value representation: `JSON.stringify` of an enum value yields the bare wire string (tag never in JSON output); a `Result` value is never lowered to wire (`isWireLowerable(makeOk/makeErr)` is `false`, a primitive `true`). Equality (`#equality`): structural deep equality on arrays/objects/nested values; cross-type compares `false` with no parse diagnostic / no panic (`42 == true`, `null == "x"`, `[1] == 1`, `Severity.Low == "low"`); `NaN == NaN` is `true`; `+0 == -0` is `true`; `42 == 42.0` is `true` (`integer ⊑ number`); enum equality compares the declaring-enum tag and wire value (`Severity.High == OtherEnum.High` is `false`); `Result` equality compares the `Ok`/`Err` discriminator and recurses on the payload. Typecheck + lint clean; the 9 tests red on their own primary assertions (a tagged-object `JSON.stringify`, an every-value-lowerable predicate, an always-unequal relation) because the V2c declaring-enum-tagged representation, structural-equality relation, and `Result`-not-lowerable recognition are absent — fail-red-for-the-intended-reason; the other 210 tests stay green (H2a/H3a scans green — the new `src/runtime/value.ts` holds no module-level mutable state and no ambient primitive). |
| V2a | Complete | 2026-06-30 | Type grammar and loom literal sublanguage (paired with `V2a-T`): fills `src/parser/type-grammar.ts` and `src/parser/literal-sublanguage.ts`. `parseTypeExpression` tokenises and recursive-descent-parses a type expression (primitive / named / generic `array`/`Result` / inline-object / union / literal), then walks it for the position-sensitive checks: `loom/parse/generic-arity-mismatch` (a closed-set constructor — `array` arity 1, `Result` arity 2 — applied with the wrong type-argument count), `loom/parse/void-in-non-return-position` (`void` anywhere but the top-level `return` annotation — nested in a generic arg, inline-object field, or union arm always fires), and `loom/parse/result-in-schema-position` (any `Result` application inside a `schema-feeding` type, the whole tree being lowered-schema reachable). `checkArrayCommonType` returns `undefined` for a real sink (`binding-annotation`/`fn-param`/`constructor-field`/`array-element`) and `loom/parse/array-no-common-type` for an unsunk `for-iterand`/`none` whose elements have no single common type (an `[]` always fires). `checkLiteralSublanguage` parses the expression and pre-order-walks for the first sub-expression outside the literal sublanguage (admitted: primitive / `Enum.Variant` member / unary-`-` numeric / array / bare- and named-object containers; forbidden: identifier refs, calls, index, operators, ternary, `` `…` `` / `${…}` templates, `@`...`` queries), firing `loom/parse/default-not-literal` or `loom/parse/tool-arg-not-literal` naming the offending sub-expression. `checkObjectLiteralFields` emits one `loom/parse/missing-object-field` per declared field absent from the literal, in declared order. Messages anchored to code-registry-parse.md; lookup tables are `Object.freeze`d (no module-level mutable state), parsers are per-call class instances, no broad catch / ambient primitive / Promise combinator / Sync call. All 8 `V2a-T` tests green; full suite 210 green, typecheck + lint clean; H2a/H3a scans green. |
| V2a-T | Tests red | 2026-06-30 | Type-grammar + loom-literal-sublanguage tests (paired `V2a` impl): two seam stubs — `src/parser/type-grammar.ts` (`parseTypeExpression(source, position, site)` + `checkArrayCommonType(context, elementTypes, site)`, inert no-ops; `TypePosition` = `return`/`value`/`schema-feeding`, `ArraySinkContext` = the exhaustive sink set plus `for-iterand`/`none`) and `src/parser/literal-sublanguage.ts` (`checkLiteralSublanguage(source, position, site)` + `checkObjectLiteralFields(schema, presentFields, site)`, inert no-ops; `LiteralPosition` = `default`/`tool-arg`, `ObjectSchemaSpec`) — plus `tests/type-grammar.test.ts` with 8 failing tests. `loom/parse/generic-arity-mismatch` (`array<string, number>` → arity 1 got 2; `Result<string>` → arity 2 got 1), `loom/parse/void-in-non-return-position` (`void` in a value position fires, in return position does not), `loom/parse/result-in-schema-position` (`Result<…>` in `schema-feeding` fires, in `value` does not), `loom/parse/default-not-literal` (a `params:` default RHS operator form), `loom/parse/tool-arg-not-literal` (a Pi-tool-arg function-call form), `loom/parse/missing-object-field` (`Cat { name }` omitting `age` → exact message, a full-field literal raises none), and `loom/parse/array-no-common-type` (`for x in []` iterand is not a sink → fires; a binding-annotation sink resolves). Messages anchored to code-registry-parse.md per the *Diagnostic message anchors* rule. Typecheck + lint clean; the 8 tests red on their own primary assertions (an absent expected diagnostic) because the V2a type-expression parser, is-literal check, and sink-resolution engine are absent — fail-red-for-the-intended-reason; the other 202 tests stay green (H2a/H3a scans green — the new `src/parser/**` seams hold no module-level state and no ambient primitive). |
| V1a | Complete | 2026-06-30 | Lexer core (paired with `V1a-T`): fills `src/lexer/lexer.ts`. `lexLoom` UTF-8-validates the raw pre-normalisation bytes (`firstInvalidUtf8Offset`, reporting the zero-based offset of the first invalid byte; a non-UTF-8 BOM faults on its leading byte → offset 0) and on failure emits `loom/load/invalid-encoding` (file-only) through the `V7d` `emitDiagnosticBatch` seam. On valid input it strips a leading UTF-8 BOM, normalises `\r\n`/`\r`→`\n` before any span is recorded (so CRLF and LF sources tokenise byte-identically), then scans tokens (`//`/`///` line comments skipped, `/* */` → `loom/parse/block-comment`, stray `\` → `loom/parse/stray-backslash`, single-line strings, numbers, identifiers vs the reserved-keyword set, greedy two-char operators) and collapses raw newline runs into significant `stmt-sep` tokens under the closed continuation-trigger rule (open-bracket depth, trailing binary/ternary operator or `=`, leading binary/ternary operator — one decision per newline run, so blank lines never break a continuation). Contextual checks at declarator-name / control-header positions fire `loom/parse/reserved-keyword-as-identifier`, `loom/parse/schema-case-mismatch`, `loom/parse/binding-case-mismatch`, and `loom/parse/single-line-if` (messages anchored to code-registry-{load,parse}.md). All 15 `V1a-T` tests green; full suite 187 green, typecheck + lint clean; H2a/H3a scans green (Sets are function-local, no ambient primitives). |
| V1a-T | Tests red | 2026-06-30 | Lexer-core tests (paired `V1a` impl): a `src/lexer/lexer.ts` seam stub (`lexLoom(source, deps)` — inert no-op returning an empty token stream / no diagnostics; `Token`/`TokenKind`/`LoomSource`/`LexResult` shapes, including the significant `stmt-sep` token that marks a real statement boundary), plus `tests/lexer-core.test.ts` with 15 failing tests. Each lexer-surfaced diagnostic is asserted to fire through the `V7d` producer-facing diagnostic-emission seam (a recording channel double captures the batched `loom-system-note` `details.diagnostics`, never a direct out-of-band `pi.sendMessage`): `loom/load/invalid-encoding` (non-UTF-8 byte fails load at the zero-based offset, message anchored to code-registry-load.md), `loom/parse/reserved-keyword-as-identifier`, `loom/parse/schema-case-mismatch`, `loom/parse/binding-case-mismatch`, `loom/parse/block-comment`, `loom/parse/single-line-if`, `loom/parse/stray-backslash` (messages anchored to code-registry-parse.md). `lexical.md` §Newline normalisation: LF and CRLF inputs tokenise to identical token streams. `grammar.md` §Newline continuation: each of the four closed continuation triggers (unmatched open bracket, trailing binary/ternary operator, trailing comma inside an open bracket, leading binary/ternary operator) tokenises its spanning lines as one statement, and one or more intervening blank lines do not break the continuation for the trailing-trigger and leading-operator forms. Typecheck + lint clean; the 15 tests red on their primary assertions (empty token stream, wrong statement count, no delivered diagnostic) because the V1a tokeniser/validator/continuation engine is absent — fail-red-for-the-intended-reason; the other 172 tests stay green. |
| V1b-T | Tests red | 2026-06-30 | String/number/path-literal tests (paired `V1b` impl): the `src/lexer/literals.ts` seam (`validatePathLiteral` / `checkIntegerNarrowing` inert stubs; `NumericLiteralType` / `PathLiteralKind` / `PathLiteral` / `NarrowingSite` shapes) plus two optional `Token` fields (`value` for the decoded string, `numericType` for the integer/number tag), and `tests/literals-and-paths.test.ts` with 15 failing tests. String + number behaviours are asserted through `lexLoom` and the `V7d` diagnostic seam (recording channel double): `\u{1F600}` and the simple escape table decode to `Token.value`; `loom/parse/illegal-escape` (`\x`), `loom/parse/invalid-unicode-escape` (`>U+10FFFF` and a `D800` surrogate), `loom/parse/integer-literal-out-of-range` (`12345678901234567890`), `loom/parse/number-literal-not-finite` (`1e400`), and `loom/parse/unsupported-feature` (`0xFF`/`0o17`/`0b101`/`1_000`) fire; `3.14`/`42` classify as `number`/`integer`. The path/narrowing rules use the standalone seams: `loom/parse/invalid-path-separator` (backslash), the byte-exact `.LOOM` rejection via `loom/parse/invoke-non-loom-extension`, and `loom/parse/integer-narrowing` (`number`→`integer` fires, `integer`→`number` widening does not). Messages anchored to code-registry-parse.md. Typecheck + lint clean; the 15 tests red on their primary assertions (missing decoded value / numeric type / absent diagnostic) because the V1b escape-decoder, number-range checks, path validator, and narrowing check are absent — fail-red-for-the-intended-reason; the other 187 tests stay green. |
| V1b | Complete | 2026-06-30 | String/number/path literals (paired with `V1b-T`): fills `src/lexer/lexer.ts` string + number scanning and the `src/lexer/literals.ts` seam. The string scanner decodes the escape table (`\"`/`\'`/`\\`/`\n`/`\t`/`\r`/`\u{XXXX}`) into `Token.value` (keeping the verbatim slice in `Token.text`), firing `loom/parse/illegal-escape` (message `illegal escape sequence: \<char>`) for an unrecognised escape and `loom/parse/invalid-unicode-escape` for a `\u{…}` whose 1–6-hex-digit scalar exceeds `U+10FFFF` or names a `U+D800`–`U+DFFF` surrogate. The number scanner classifies `Token.numericType` (`integer` with no fractional/exponent part, else `number`), fires `loom/parse/integer-literal-out-of-range` (`> 2^53-1`, judged per-token) / `loom/parse/number-literal-not-finite` (non-finite double), and reports any digit/letter/underscore abutting the decimal literal — `0x`/`0o`/`0b`/`1_000` — as `loom/parse/unsupported-feature`. `literals.ts` `validatePathLiteral` fires `loom/parse/invalid-path-separator` (any backslash, located at the span) then the byte-exact lowercase final-segment check (`loom/parse/import-non-warp-extension` for `import`, else `loom/parse/invoke-non-loom-extension`), so `.LOOM`/`.WARP` is rejected cross-OS; `checkIntegerNarrowing` returns `loom/parse/integer-narrowing` for `number`→`integer` and `undefined` for the one-way `integer`→`number` widening. Messages anchored to code-registry-parse.md. All 15 `V1b-T` tests green; full suite 202 green, typecheck + lint clean; H2a/H3a scans green. |
| V8b | Complete | 2026-06-30 | `FileSystem` seam (paired with `V8b-T`): implements the `PiFileSystem` production adapter (`src/seams/pi-file-system.ts`) and the in-memory `FakeFileSystem` (`tests/helpers/fake-file-system.ts`) against `PIC-13`. `PiFileSystem` delegates to Node `fs.promises` / `os` — `readText` (utf8), `readBytes` (raw Buffer copied to a standalone `Uint8Array`, preserving invalid UTF-8 per `lexical.md` §Encoding), `writeText`, `exists` (resolves `false` only on `ENOENT` via a Promise rejection handler, not a broad `catch`; rejects every other `.code`), `homedir()` via `os.homedir()` (no `process.env`), `cwd()` captured once at construction via `process.cwd()` carrying the `// allow-ambient: process.cwd — FileSystem` exempt-site comment, `readdir` (entry names, no normalisation), `lstat` (no symlink follow), and `realpath` via promisified `fs.realpath.native` (full-path case canonicalisation satisfying the case-canonicalisation / byte-stable guarantees). `FakeFileSystem` resolves the same `.code` mapping (`ENOENT`/`EACCES`/`EPERM`), transitive symlink resolution with `ELOOP` cycle detection, and case-insensitive canonicalisation against its constructor-injected maps. All 31 `V8b-T` tests green; full suite 172 green, typecheck + lint clean; H3a ambient scan green (the one `process.cwd()` site carries its `allow-ambient` comment). |
| V8b-T | Tests red | 2026-06-30 | `FileSystem` seam tests (paired `V8b` impl): the `PiFileSystem` production-adapter stub (`src/seams/pi-file-system.ts`) and the in-memory `FakeFileSystem` conforming-seam stub (`tests/helpers/fake-file-system.ts`, constructor takes `homedir`/`cwd`/`files`/`dirs`/`symlinks`/`errors`/`caseInsensitive`), plus `tests/filesystem-seam.test.ts` with 31 failing tests pinning `PIC-13` (the eight enumerated members map Node `.code` values — `readText`/`writeText`/`exists` round-trip + `ENOENT`/`EACCES`/`EPERM`; `homedir()`/`cwd()` constructor-injected / capture-once, never `process.env`/`process.cwd`; `readdir` entry-names-only + NFD-unfolded encoding guarantee; `lstat` no-follow; `realpath` transitive-follow + `ELOOP`/`ENOENT`/`EACCES`/`EPERM` + byte-stable + case-canonicalisation) and `lexical.md` §Encoding (`readBytes` returns raw pre-decode `Uint8Array` preserving invalid UTF-8, same `.code` mapping). Tests compile (typecheck clean), lint clean, and red on their primary assertions because the V8b adapters are absent (stubs throw); the H3a ambient scan stays green (no ambient ref in the stub). |
| V8c-T | Tests red | 2026-06-30 | `SchemaValidator` seam tests (paired `V8c` impl): the production `AjvSchemaValidator` stub in `src/seams/schema-validator.ts` (constructor `{ emit, slugOf }` — an injected `Diagnostic` sink for `loom/runtime/validator-cache-collision` and a `SchemaSlugFn` content-addressing seam returning `{ slug, canonicalBytes }`; the stub hands back an inert validator that rejects every value with one sentinel error, caches nothing, emits nothing), plus `tests/schema-validator-seam.test.ts` with 8 failing tests pinning `PIC-11` (host-interfaces-services.md): one-pass multi-error reporting (no fast-fail — both `/a` and `/b` type errors surface), NO coercion (a numeric string fails the `type` check and is left as-is on the input), NO default-filling (an optional `default`-carrying property stays absent on the validated object), in-document `$ref` resolution (a `#/$defs/Name` ref validates/rejects), silent unknown-`format` acceptance, determinism across two compilations, and the slug-cache byte-verify: a byte-identical cache hit serves with no collision diagnostic, while two distinct documents forced (via an injected fixed-slug `slugOf`) to share one slug fire `loom/runtime/validator-cache-collision` (E, message anchored to code-registry-runtime.md — `validator-cache collision on slug <slug>: two distinct schema documents hash alike`, hint carrying both canonical-byte forms) and recompile the new document instead of serving the wrong cached validator. No new diagnostics-registry codes (`loom/runtime/validator-cache-collision` already present). Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 8 tests red on their own primary assertions (sentinel-error rejection, no recorded error paths, absent collision diagnostic) because the V8c validator is absent — fail-red-for-the-intended-reason; the other 501 tests stay green. |
| V8c | Complete | 2026-06-30 | `SchemaValidator` seam (paired with `V8c-T`): fills `src/seams/schema-validator.ts` — `AjvSchemaValidator` (constructor `{ emit, slugOf }`) compiles each lowered schema through a per-instance `Ajv({ strict: false, allErrors: true, logger: false })` with `ajv-formats` registered as an instance-level side effect, satisfying the PIC-11 contract: `allErrors: true` → one-pass multi-error reporting (no fast-fail); no `coerceTypes` → no value conversion (a numeric string fails the `type` check, the input is left as-is); no `useDefaults` → no default-filling (an optional `default`-carrying property stays absent); AJV's default in-document `$ref` resolver → `#/$defs` ref scope; `strict:false` + `ajv-formats` + `logger:false` → silent unknown-`format` acceptance; deterministic per (schema, value). The per-query compiled-validator cache (`Map<slug, {validator, canonicalBytes}>`) is keyed by `slugOf(schema).slug`; on a cache hit it byte-compares the candidate's canonical bytes against the cached document's bytes (a comparison, not a re-serialisation) and serves the cached validator on a match, but on a byte mismatch (a schema-slug collision) emits `loom/runtime/validator-cache-collision` (E, message anchored to code-registry-runtime.md — `validator-cache collision on slug <slug>: two distinct schema documents hash alike`, hint carrying both canonical-byte forms) and recompiles the new document rather than serving the wrong cached validator; `invalidate(slug)` drops the cache entry. Per-instance `Ajv` + cache (no module-level global) — no broad catch / ambient primitive / Promise combinator / Sync call. All 8 `V8c-T` tests green; full suite 509 green, typecheck + lint clean. |
| V8d-T | Tests red | 2026-06-30 | `Clock` / `IdSource` seam tests (paired `V8d` impl): the `WallClock` production-adapter stub (`src/seams/wall-clock.ts`) and `CryptoIdSource` production-adapter stub (`src/seams/crypto-id-source.ts`), plus the `FakeClock` (`tests/helpers/fake-clock.ts`) and sequence-driven `FakeIdSource` (`tests/helpers/fake-id-source.ts`) test-fake stubs — all members throw, none referencing an ambient primitive so the H3a real-tree scan stays green — and `tests/clock-id-seams.test.ts` with 18 failing tests pinning `PIC-12` and `PIC-20` (host-interfaces-services.md). `PIC-12`: `WallClock` `now()` monotonic ms / `wallNow()` epoch ms / `setTimeout` fires + returns handle / `clearTimeout` cancels / per-runtime independence; `FakeClock` `advance(ms)` fires due timers in deadline order, `advance(0)` fires a 0-ms timer (the loop-iter macrotask-yield vehicle), equal deadlines fire in registration order, `clearTimeout` no-op for already-fired / cancels pending, `now()` not implicitly advanced + `wallNow()` returns the injected epoch, two instances have independent timer queues; and the ambient-timing ban architectural test — stripping the same-line `// allow-ambient: <primitive> — Clock` comments re-exposes `performance.now`/`Date.now`/`setTimeout`/`clearTimeout` (delegations exist), the intact scan flags nothing, and no other `src/**` module references a timing primitive (sole site). `PIC-20`: `CryptoIdSource` `newInvocationId()`/`newToolCallId()` each return a canonical lowercase 8-4-4-4-12 hex UUID minted fresh per call; `FakeIdSource` hands out the configured sequence in order across both members with two instances independent; and the ambient `crypto.randomUUID` ban architectural test — stripping the `// allow-ambient: crypto.randomUUID — IdSource` comments re-exposes exactly two `crypto.randomUUID` references (one per member), the intact scan flags nothing, and no other `src/**` module references `crypto.randomUUID` (sole site). No new diagnostics-registry codes. Inert stub seams: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 16 behavioural tests red on the stub `not implemented` throw and the 2 architectural tests red on the delegation-absent strip-scan assertion because the V8d adapters/fakes are absent — fail-red-for-the-intended-reason; the other 516 tests stay green; the H3a ambient scan stays green. |
| V7d | Complete | 2026-06-30 | `loom-system-note` delivery channel (paired with `V7d-T`): fills `src/extension/system-note-channel.ts` — `sendSystemNote` emits one best-effort `pi.sendMessage({customType:"loom-system-note", content, display, details}, {triggerTurn:false})` and, on a synchronous throw, runs the fallback chain (`ctx.ui.notify(content,"error")` → `loom/runtime/system-note-delivery-failed` diagnostic with `message`=content / `hint`=throw message → terminal `console.error`), skipping `ctx.ui.notify` when `display:false` or `content:""`, catching a throwing `ctx.ui.notify`, and never aborting the handler; the terminal `console.error` is `try`/`catch`-wrapped (PIC-54) and its throw silently swallowed. `emitDiagnosticBatch` delivers a V7a-assembled `Diagnostic[]` as exactly one `sendMessage` per `.loom` (content = `renderDiagnosticBatch`, `details.diagnostics` = full batch, no per-error fan-out; re-scan re-emits with no dedup/supersede). `createSystemNoteRenderer` gains the PIC-21 render-time trap: an internal renderer-body throw does not escape — `display:true` falls back to a raw-content `Component`, `display:false` returns `undefined`, no diagnostic emitted. All 8 `V7d-T` tests green; full suite 141 green, typecheck + lint clean. |
| V7d-T | Tests red | 2026-06-30 | `loom-system-note` delivery-channel tests (paired `V7d` impl): the `src/extension/system-note-channel.ts` seam (`sendSystemNote` + `emitDiagnosticBatch`, inert no-op stubs; `SystemNote` / `SystemNoteDetails` / `SystemNoteSender` / `UiNotifier` / `SystemNoteChannelDeps` shapes; `SYSTEM_NOTE_CHANNEL` + `SYSTEM_NOTE_DELIVERY_FAILED_CODE`) plus an injectable `formatLines` seam on `createSystemNoteRenderer` (so the PIC-21 test can drive an internal renderer throw), and `tests/system-note-channel.test.ts` with eight failing tests: multi-error batch delivery (the V7a-assembled `Diagnostic[]` → exactly one `sendMessage` carrying the full batch in `content` + `details.diagnostics`, no per-error fan-out), re-scan re-emits without dedup/supersede, `PIC-21` (an internal renderer throw does not escape — `display:true` falls back to a raw-content `Component`, `display:false` returns `undefined`, no `loom/runtime/*` diagnostic), and the `loom/runtime/system-note-delivery-failed` fallback chain (`ctx.ui.notify(content,"error")` → diagnostic with `message`=content / `hint`=throw message → terminal `console.error`; skips `ctx.ui.notify` on `display:false` or `content:""`; catches a throwing `ctx.ui.notify`; `PIC-54` — the terminal `console.error` is `try`/`catch`-wrapped and its throw is silently swallowed). Tests compile (typecheck clean), lint clean, and red on their primary behavioural assertions (delivery / fallback / PIC-21 hardening absent); other 133 tests stay green. |
| V7c | Complete | 2026-06-30 | Placeholder rendering (paired with `V7c-T`): fills the eight per-category renderers in `src/diagnostics/placeholder.ts` (DIAG-4). Cat 1 `renderType` re-serialises a Loom static type in source-grammar form (lowercase primitives, `\| `-joined unions, `array<T>`, `Result<T, E>`, inline object `{ f: T }`). Cat 2 `renderRuntimeValue` stringifies via the canonical table with the 80→77+`...` code-point truncation (counted by code point) and `Ok(…)`/`Err(…)`. Cat 3 `renderConstruct` emits the caller-supplied closed token-name / verbatim span. Cat 4 `renderInteger` is shortest-decimal with `-0`→`0`. Cat 5 `renderSourceDerived` renders identifiers/paths bare, `<key>` quoted only when not identifier-shaped (`^[A-Za-z_][A-Za-z0-9_]*$`), `<char>` raw-or-escaped (`\xNN`/`\u{NNNN}`), `<descriptor>` as `<kind>:"<value>"`. Cat 6 `renderUnderlyingError` coerces a caught throw (object string `.message`, else `String(v)`, else `<unreadable>`) then first-line-truncates (`\r\n`/`\r`→`\n`, cut at first `\n`, trailing-ws preserved, `<no message>` when empty; `\u2028`/`\u2029` ordinary). Cat 7 `renderCategory7` dispatches the closed sub-rules (identifier/closed-enum verbatim, `<uuid>` lowercased, numeric via cat 4, path/descriptor via cat 5). Cat 8 `renderHostDerivedTail`/`renderHostIncompatible` assemble the `host incompatible (<kind>): observed <observed>, required <required>` template with the host-derived `<observed>` first-line-truncated; host version passed in, never read from `process`. All 10 `V7c-T` tests green; full suite 133 green, typecheck + lint clean. |
| V7c-T | Tests red | 2026-06-30 | Placeholder-rendering tests (paired `V7c` impl): the `src/diagnostics/placeholder.ts` seam (eight per-category render functions — `renderType`, `renderRuntimeValue`, `renderConstruct`, `renderInteger`, `renderSourceDerived`, `renderUnderlyingError`, `renderCategory7`, `renderHostDerivedTail` / `renderHostIncompatible` — inert stubs returning `""`) plus `tests/placeholder-rendering.test.ts` with ten failing tests pinning `DIAG-4`: each of the eight categories renders its normative vector byte-identically (static-type re-serialisation; runtime-value stringification; construct closed-token + verbatim span; integer rule; source-derived verbatim; underlying-error first-line coercion; category-7 closed-enum/uuid/numeric; category-8 host-derived tail), the category-2 80→81 code-point truncation boundary (full at 80, `77 chars + "..."` at 81), and the category-8 `loom/load/host-incompatible` (`node-floor`) row with the host version mocked to `v18.19.0` asserted via the §8 anchored partial-match `^host incompatible \(node-floor\): observed (.*), required >=22\.19\.0$` (the *Message* template sourced from the registry, never a full-*Message* equality). Tests compile (typecheck clean), lint clean, and red on their primary assertions (empty render) because the renderer is absent. |
| H1a | Complete | 2026-06-30 | Project scaffold and toolchain: `package.json` deps/scripts, `tsconfig.json`, Vitest, lint toolchain + loadable `eslint-plugin-loom-local` skeleton, architectural manifest tests. |
| H2a | Complete | 2026-06-30 | Cross-cutting lint + architectural gates over `src/**`: comment-keyed `no-broad-catch`, `no-unguarded-promise-combinator`, `no-blocking-sync` ESLint rules (`eslint.config.js`), and a module-level-mutable-binding architectural scan; wired into `npm test`. |
| H3a | Complete | 2026-06-30 | Dependency-injection seam skeleton: the seven host-seam interfaces (`Checkpoint`, `SchemaValidator`, `Clock`, `FileSystem`, `FileWatcher`, `TokenEstimator`, `IdSource`) and a constructor-injection `RuntimeRoot`; plus an identifier-keyed ambient-primitive scan (`// allow-ambient:` comment-keyed) wired into `npm test` over `src/**`. |
| H4a | Complete | 2026-06-30 | Extension factory shell + end-to-end harness: the never-throw synchronous-arm `src/extension/factory.ts` (per-call `try`/`catch` Pi-SDK-boundary registrations + `loom-system-note` renderer), the thin `extensions/index.ts` entry shim, an in-memory fixture-supply seam, and a reusable in-process session double with a minimal response-emission capability; session-double fidelity-contract self-check (streaming-before-idle, single-turn append, `pi.on` cancel-forward, CNCL-4 reason propagation) wired into `npm test`. |
| H4b | Complete | 2026-06-30 | Response-programming surface: the single input-side scripting API (`tests/harness/response-program.ts`, exposed via `double.responses` / `driveResponses()`) extending H4a's minimal emission into scripted-injection categories (a) assistant turns + fragments, (b) `tool_use` results incl. `isError` + mixed parallel batch, (c) binder response/failure with the V11f ≤3-call per-class retry budget, (d) `tool_loop.max_rounds` exhaustion, (e) abort at pre-call/in-flight/during-retry; deterministic-replay gate + per-category functional-effect self-checks wired into `npm test`. |
| H5a | Complete | 2026-06-30 | REQ-ID / diagnostic-code closing-gate automation: `tools/closing-gate/` reconciles the spec REQ-ID set + diagnostics registry against `coverage-matrix.md` and the asserting/citing tests, returning a structured per-gap findings collection (unmapped-executable-REQ-ID, mapped-REQ-ID-no-citing-test, registry-code-no-asserting-test, asserted-code-not-in-registry, retired/live clash, per-prefix numbering hole; `loom/typecheck/*` excluded). Evaluated against seeded fixtures under `test-fixtures/closing-gate/` (green no-violation + red per owned violation), wired into `npm test`. |
| H5c | Complete | 2026-06-30 | `no-broad-catch` allow-list closing-gate arm: extends `tools/closing-gate/` with a reconciliation that scans the `// allow-broad-catch:` exemption comments across `src/**` and reds out (`broad-catch-allow-list-unresolved`) when an entry's cited token resolves to none of the four admitted arms — a coverage-matrix REQ-ID, an exactly-one `cka-<n>` *Token* cell, a concrete `loom/...` registry code (never a glob/wildcard family), or the structural `pi-sdk-boundary` token. Evaluated against seeded `broad-catch-no-violation` / `broad-catch-unresolved` fixtures under `test-fixtures/closing-gate/`, wired into `npm test`. |
| H5d | Complete | 2026-06-30 | Transitive-completeness closing-gate arm: extends `tools/closing-gate/` (`parseClosingLeafCells` + `parseH5bDeps` + `expandLeafTokens`, plus `h5bDepsText` in `loadCorpus`) to reconcile every `coverage-matrix.md` closing-leaf cell (in the *Numbered REQ-IDs* and *Code-keyed obligation areas* tables) against H5b's expanded `Deps.` membership — tokenising each cell by its backtick spans, expanding contiguous within-group `<group><letter>` ranges on both sides, excluding the literal `<new>` and `*(numbered above)*` cells — and emits `transitive-completeness-unreachable` for a per-cell at-least-one failure (a multi-leaf primary + co-witness cell stays green when one listed leaf is present). Evaluated against seeded `transitive-no-violation` / `transitive-unreachable` fixtures under `test-fixtures/closing-gate/`, wired into `npm test`. |
| H5e | Complete | 2026-06-30 | Un-anchored normative-MUST text-scan closing-gate arm: extends `tools/closing-gate/` (`parsePrefixTablePages` + `parseCkaAreaRows` + `parsePlanLeaves` + `pageHasUnanchoredMust` + `classifyClosingCell` wired into `runClosingGate`, plus `planLeavesText` in `loadCorpus`) with three sub-recognisers over the non-narrative `spec_topics/**`-shaped pages — un-enumerated-MUST (`un-anchored-must-unenumerated`), `<new>`-placeholder-MUST (`un-anchored-must-new-placeholder`, plus `un-anchored-must-unresolved-leaf` for a non-resolving token like `V99z`), and un-rowed-page (`un-rowed-page-residue`, excluding GOV-24 hub stubs whose stem matches a trailing-slash subtree row). A page is in scope iff its prefix-table cell is not the byte-exact `(no IDs — narrative)` literal; an un-anchored MUST (a MUST/MUST-NOT paragraph with no `PREFIX-N` and no `loom/...` code) must be enumerated in the *Code-keyed obligation areas* table with a closing-leaf token resolving to a real plan leaf. The arm runs only when the corpus supplies `planLeavesText`, so the H5a/H5c/H5d fixtures stay dormant. Evaluated against seeded `un-anchored-no-violation` / `un-enumerated-must` / `new-placeholder-must` / `un-rowed-page` fixtures under `test-fixtures/closing-gate/`, wired into `npm test`; live-corpus binding flips at H6a. |
| H5f | Complete | 2026-06-30 | Per-facet citing-test closing-gate arm: extends `tools/closing-gate/` (`deriveFacetPartition` + `parseFacetRows` + `citesTokenInline` wired into `runClosingGate`, plus `perFacetCitingTests` in `loadCorpus`, gated by an `h5f-enabled.md` marker) to reconcile each multi-leaf `coverage-matrix.md` row — across **both** the *Numbered REQ-IDs* and *Code-keyed obligation areas* tables — against its facet leaves' facet-naming citing tests. For a row whose closing-leaf cell lists ≥2 leaves it derives the facet partition from the cell alone (dropping a leaf whose immediately-following parenthetical contains the literal `co-witness` token, keeping every other listed leaf) and emits `per-facet-citing-test-missing` for any remaining facet with no test source citing both the row's subject (the numbered REQ-ID, or the `cka-<n>` token for a code-keyed row) and that facet's closing-leaf-ID inline. Best-effort per-test-source existence scan (per-facet semantic fidelity stays the release-time residue item-7 obligation). Evaluated against seeded `per-facet-no-violation` (including a co-witness-excluded row) / `per-facet-violation` fixtures under `test-fixtures/closing-gate/`, wired into `npm test`; live-corpus binding flips at H6a. |
| M-T | Tests red | 2026-06-30 | Minimal end-to-end `.loom` slash-command tests (SLSH-2 MVP happy path): the `buildMinimalLoom` seam (`src/mvp/minimal-loom.ts`, inert stub) + two failing harness tests — a dispatched prompt-mode loom issues one untyped `@`-query whose streamed assistant response appends as one prompt-mode turn, and the run produces exactly one appended turn and no diagnostic. Session double extended with a `pi.sendMessage` diagnostics recorder. Tests compile (typecheck clean) and red on the absent prompt-mode pipeline. |
| H4c | Complete | 2026-06-30 | Modeled-behaviour surface: extends the H4b scripting contract with the two modelling categories the `V4c`/`V4f`/`V9i`/`V9o` vectors consume — (f) `scriptInvokeChild` (a completed `invoke(...)` child's produced final value surfaces as a completed-invoke-child outcome, ERR-13 no-rollback) and (g) `scriptSubagentCallee` (a private subagent `AgentSession` dispatch whose outcome is read from the terminal `agent_end`, ignoring `willRetry:true` events per PIC-43, surfaced as a subagent-loom outcome rather than a plain turn); determinism gate + per-category functional-effect self-checks wired into `npm test`. |
| V7a-T | Tests red | 2026-06-30 | Diagnostics-primitive tests (paired `V7a` impl): the `src/diagnostics/diagnostic.ts` seam (`Diagnostic` shape + stubbed `renderDiagnosticLine` / `renderDiagnosticBatch` / `assembleDiagnostics`) plus failing tests pinning `DIAG-1` (registry code carried; located / file-only / location-less content-line format; hint + related lines) and multi-error assembly (no fast-fail, no per-error loss, `(file, line, col)` ordering across an entry `.loom` + 2 transitive `.warp` modules). Tests compile (typecheck clean), lint clean, and red on their primary assertions (empty render / empty array) because the primitive is absent. |
| V7b | Complete | 2026-06-30 | Diagnostic code registry (paired with `V7b-T`): fills the `tools/code-registry/` seam. `parseRegistry` parses the four sharded spec registry tables (`loom/parse/*`, `loom/load/*`, `loom/runtime/*`, `loom/host/*`) into one structured `{code, namespace, severity, phase, trigger, message}` row per code (table-escape-aware cell split; `\|`/`` \` `` decoding; runtime trailing-`.` dropped from the backtick span). `registryMessage` is the DIAG-4 single source for normative Message strings. `reconcileClosedSet` enforces DIAG-2 in both directions (`asserted-code-not-in-registry` / `registry-code-no-asserting-test`); `reconcileStableIds` enforces DIAG-3 (`code-renamed` for a pinned-baseline code absent from the current registry). All 5 `V7b-T` tests green; full suite 123 green, typecheck + lint clean. |
| V7b-T | Tests red | 2026-06-30 | Diagnostic code-registry tests (paired `V7b` impl): the `tools/code-registry/` seam (inert `parseRegistry` / `registryMessage` / `reconcileClosedSet` / `reconcileStableIds`) + four failing tests pinning `DIAG-2` (the registry is closed — parseRegistry yields a structured namespace/severity/phase/trigger/message row per code across all four families; an asserted code with no row and a registry code with no asserting test both fail), `DIAG-3` (codes are stable identifiers — a renamed code, i.e. a pinned-baseline code absent from the current registry, fails; renames deferred to loom 2.0), and `DIAG-4` (the Message column is normative — `registryMessage` is the single source asserting tests draw expected strings from). Tests compile (typecheck clean), lint clean, and red on their primary assertions (empty registry / empty findings / undefined message) because the registry seam is absent. |
| V7a | Complete | 2026-06-30 | Diagnostics primitive (paired with `V7a-T`): fills the `src/diagnostics/diagnostic.ts` seam — `renderDiagnosticLine` emits the located `<file>:<line>:<col>: <code>: <message>`, file-only `<file>: <code>: <message>`, and location-less `<code>: <message>` content-line forms plus the two-space-indented `hint:` continuation and code-less `<file>:<line>:<col>: <message>` related-site lines; `renderDiagnosticBatch` joins one block per diagnostic with a single blank line; `assembleDiagnostics` flattens per-file groups with no fast-fail / no per-error loss and sorts by `(file, line, col)`. All 7 `V7a-T` tests green; full suite + typecheck + lint clean. |
| M | Complete | 2026-06-30 | Minimal end-to-end `.loom` slash command (SLSH-2 MVP happy path): `buildMinimalLoom` (`src/mvp/minimal-loom.ts`) parses a `mode:`-only frontmatter and the single untyped `` @`<literal>` `` body query of an in-memory `.loom` source and returns a `LoomFixture` whose `run` drives one prompt-mode turn — `pi.sendUserMessage(<rendered literal>)` then `ctx.waitForIdle()` — leaving exactly one appended user+assistant turn and no diagnostic. Harness-supplied single-source discovery only (no ambient `src/**` filesystem read, no `FileSystem` seam); the two `M-T` SLSH-2 tests go green. |

Open spec-review work lives in [`docs/spec-review.md`](./docs/spec-review.md)
(per-finding fixes processed bottom-up by `/fix-spec-shape-single-findings`).

## Highlights

- **Two file kinds, one grammar.** `.loom` files are invocable as Pi slash
  commands; `.warp` files are import-only library modules.
- **Query-and-await.** `@`-prefixed templates send the conversation so far to
  the model and bind the typed reply to a variable.
- **Structured outputs.** `schema` declarations lower to a JSON-Schema subset
  validated with AJV; typed queries return `Result<T, QueryError>`.
- **Two execution modes.** *Prompt mode* appends to the caller's
  conversation; *subagent mode* runs in an isolated child conversation and
  returns a value.
- **Tool calls and cross-loom invocation.** `<tool>(args)` invokes registered
  Pi tools; `invoke(...)` runs another loom with cycle detection.
- **LLM-driven argument binding.** Slash-command arguments are parsed into
  typed `params` by an LLM binder with deterministic fallbacks.

## Install

Not yet published. Once implemented, the extension will be installed as a Pi
package that exposes its `extensions/` directory via the `pi.extensions`
field in `package.json`.

## Repository layout

| Path | Contents |
|---|---|
| [`docs/spec.md`](./docs/spec.md) | Top-level spec index. |
| [`docs/spec_topics/`](./docs/spec_topics/) | Per-topic normative spec pages. |
| [`docs/plan.md`](./docs/plan.md) | Top-level implementation plan. |
| [`docs/plan_topics/`](./docs/plan_topics/) | Per-phase plan pages and coverage matrix. |
| [`docs/spec-review.md`](./docs/spec-review.md) | Spec review log. |
| `extensions/` | Pi extension entry points (populated during implementation). |
| `package.json` | Package manifest; declares the Pi extension and peer-deps on `@earendil-works/pi-*`. |

## License

Licensed under either of

- Apache License, Version 2.0 ([LICENSE-APACHE](./LICENSE-APACHE) or
  <https://www.apache.org/licenses/LICENSE-2.0>)
- MIT License ([LICENSE-MIT](./LICENSE-MIT) or
  <https://opensource.org/licenses/MIT>)

at your option.

### Contribution

Unless you explicitly state otherwise, any contribution intentionally
submitted for inclusion in this work by you, as defined in the Apache-2.0
license, shall be dual licensed as above, without any additional terms or
conditions.
