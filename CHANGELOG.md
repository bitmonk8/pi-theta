# Changelog

2026-06-30 H1a — Project scaffold and toolchain: package.json deps/scripts/engines/type, tsconfig.json, Vitest runner, lint toolchain (eslint, @typescript-eslint/parser, loadable eslint-plugin-loom-local skeleton), and architectural manifest tests; npm install/build/typecheck/test all green on an empty src tree.
2026-06-30 H2a — Cross-cutting lint + architectural gates: comment-keyed no-broad-catch / no-unguarded-promise-combinator / no-blocking-sync ESLint rules in eslint-plugin-loom-local wired by eslint.config.js over src/**, plus a module-level-mutable-binding architectural scan; lint + architectural assertions run under npm test against fixtures and the real src/** tree.
2026-06-30 H3a — Dependency-injection seam skeleton: the seven host-seam interfaces (Checkpoint/SchemaValidator/Clock/FileSystem/FileWatcher/TokenEstimator/IdSource, PIC-10/11/12/13/14/16/20) under src/seams/, a constructor-injection RuntimeRoot + createRuntimeRoot factory threading them, and an identifier-keyed ambient-primitive scan (process.env/process.cwd/crypto.randomUUID + the PIC-12 timing set, // allow-ambient: comment-keyed) wired into npm test over src/**; two-runtime-root isolation and the direct-reference ambient ban asserted.
2026-06-30 H4a — Extension factory shell + end-to-end harness: never-throw synchronous-arm src/extension/factory.ts (per-call try/catch Pi-SDK-boundary registrations of the --loom flag, the loom-system-note renderer, and the resources_discover/session_start/session_shutdown subscriptions; registerCommand fires from session_start), the thin extensions/index.ts entry shim re-exporting the factory, an in-memory fixture-supply seam (LoomExtensionDeps.fixtures) M/M-T bind against, and a reusable in-process session double + harness with a minimal response-emission capability; session-double fidelity-contract self-check (streaming-before-idle, single-turn append, pi.on cancel-forward subscription, CNCL-4 reason propagation) wired into npm test. tsconfig now includes extensions/.
2026-06-30 H4b — Response-programming surface: a single deterministic input-side scripting API (tests/harness/response-program.ts, reached through H4a's harness via double.responses / driveResponses()) extending H4a's minimal response-emission into categories (a) scripted assistant turns + per-turn streamed fragments, (b) tool_use results incl. isError:true and mixed-success parallel batches (V14b), (c) binder/provider response & failure honouring the V11f per-invocation retry budget (≤3 calls: 1 + 1 transport + 1 malformed, per determinism-cancellation-failure.md), (d) tool_loop.max_rounds round-exhaustion (ERR-19/FRNT-1, V13c), and (e) abort injection at pre-call/in-flight/during-retry (V11f/V17a); determinism gate + per-category functional-effect self-checks wired into npm test.
2026-06-30 H4c — Modeled-behaviour response-programming surface: extends H4b's scripting contract (tests/harness/response-program.ts, reached via double.responses / driveResponses()) with the two modelling categories V4c/V4f/V9i/V9o consume — (f) scriptInvokeChild driving a nested invoke(...) child to completion, surfacing its produced final value as a completed-invoke-child outcome (ERR-13 no-rollback), and (g) scriptSubagentCallee dispatching a private subagent AgentSession per the V9i contract, resolving the subagent-loom outcome from the terminal agent_end event and ignoring willRetry:true events (PIC-43) rather than emitting a plain scripted turn; determinism gate + per-category functional-effect self-checks wired into npm test.
2026-06-30 H5a — REQ-ID / diagnostic-code closing-gate automation: tools/closing-gate/ (runClosingGate + loadCorpus + extractors) reconciles the spec REQ-ID set and the diagnostics registry against coverage-matrix.md and the asserting/citing test corpus, returning one structured finding per gap (kinds: unmapped-executable-req-id, mapped-req-id-no-citing-test, registry-code-no-asserting-test, asserted-code-not-in-registry, retired-live-id-clash, per-prefix-numbering-hole) without throwing or gating the live corpus; loom/typecheck/* excluded from registry reconciliation; citing-test scan is best-effort inline PREFIX-N existence. Evaluated against seeded fixtures under test-fixtures/closing-gate/ (green no-violation, red per owned violation) wired into npm test.
2026-06-30 H5c — no-broad-catch allow-list closing-gate reconciliation arm: extends tools/closing-gate/ (parseCkaTokens + extractBroadCatchEntries + a resolveBroadCatchToken predicate wired into runClosingGate, plus srcSources in loadCorpus) to scan the // allow-broad-catch: exemption comments across src/** as the lint allow-list entries and emit a broad-catch-allow-list-unresolved finding when an entry's cited token resolves to none of the four admitted arms — a coverage-matrix REQ-ID, an exactly-one cka-<n> Token cell, a concrete loom/... registry code (the resolver matches no glob/wildcard family, e.g. loom/host/session-shutdown-*), or the structural pi-sdk-boundary token. Evaluated against seeded broad-catch-no-violation / broad-catch-unresolved fixtures under test-fixtures/closing-gate/ (green no-violation, red unresolved) wired into npm test; live-corpus binding flips at H6a.
2026-06-30 H5d — Transitive-completeness closing-gate arm: extends tools/closing-gate/ (parseClosingLeafCells + parseH5bDeps + expandLeafTokens wired into runClosingGate, plus h5bDepsText in loadCorpus) to reconcile every coverage-matrix.md closing-leaf cell in the Numbered REQ-IDs and Code-keyed obligation areas tables against H5b's expanded Deps. membership — tokenising each cell by its backtick spans, expanding contiguous within-group <group><letter> ranges on both sides, excluding the literal <new> placeholder and *(numbered above)* retirement cells — and emits transitive-completeness-unreachable for a per-cell at-least-one failure (a multi-leaf primary + co-witness cell stays green when one listed leaf is present, so an intentionally-excluded co-witness does not redden the arm). The arm runs only when the corpus supplies h5bDepsText, so the H5a/H5c fixtures stay dormant. Evaluated against seeded transitive-no-violation / transitive-unreachable fixtures under test-fixtures/closing-gate/ (green no-violation, red unreachable) wired into npm test; live-corpus binding flips at H6a via H5b.
2026-06-30 H5e — Un-anchored normative-MUST text-scan closing-gate arm: extends tools/closing-gate/ (parsePrefixTablePages + parseCkaAreaRows + parsePlanLeaves + pageHasUnanchoredMust + classifyClosingCell wired into runClosingGate, plus planLeavesText in loadCorpus) with the three un-anchored-obligation sub-recognisers over the non-narrative spec_topics/**-shaped pages — un-enumerated-MUST (un-anchored-must-unenumerated), <new>-placeholder-MUST (un-anchored-must-new-placeholder; a non-resolving token like V99z reddens as un-anchored-must-unresolved-leaf), and un-rowed-page (un-rowed-page-residue, excluding GOV-24 hub stubs whose filename stem matches a trailing-slash subtree row). A page is in scope iff its prefix-table cell is not the byte-exact (no IDs — narrative) literal; an un-anchored MUST (a MUST/MUST-NOT paragraph carrying no PREFIX-N REQ-ID and no loom/... registry code) must be enumerated in the Code-keyed obligation areas table with a closing-leaf token resolving to a real plan leaf. The arm runs only when the corpus supplies planLeavesText, so the H5a/H5c/H5d fixtures stay dormant. Evaluated against seeded un-anchored-no-violation / un-enumerated-must / new-placeholder-must / un-rowed-page fixtures under test-fixtures/closing-gate/ (green no-violation, red per owned violation) wired into npm test; live-corpus binding flips at H6a.
2026-06-30 H5f — Per-facet citing-test closing-gate arm: extends tools/closing-gate/ (deriveFacetPartition + parseFacetRows + citesTokenInline wired into runClosingGate, plus perFacetCitingTests in loadCorpus, gated by an h5f-enabled.md marker file) to reconcile each multi-leaf coverage-matrix.md row across both the Numbered REQ-IDs and Code-keyed obligation areas tables against its facet leaves' facet-naming citing tests. For a closing-leaf cell listing two or more leaves it derives the facet partition from the cell alone (dropping a leaf whose immediately-following parenthetical contains the literal co-witness token, keeping every other listed leaf as a facet) and emits per-facet-citing-test-missing for any remaining facet with no single test source citing both the row's subject (the numbered REQ-ID, or the cka-<n> token for a code-keyed row) and that facet's closing-leaf-ID inline. Best-effort per-test-source inline existence scan — per-facet semantic fidelity stays the release-time residue item-7 obligation. Evaluated against seeded per-facet-no-violation (every facet covered, including a row whose co-witness-annotated leaf is correctly excluded from the partition) and per-facet-violation (one facet of a code-keyed multi-leaf row uncovered) fixtures under test-fixtures/closing-gate/ wired into npm test; live-corpus binding flips at H6a.
2026-06-30 M-T — Minimal end-to-end .loom slash-command tests (SLSH-2 MVP happy path): the buildMinimalLoom seam (src/mvp/minimal-loom.ts, an inert stub returning a registering-but-non-driving LoomFixture) plus two failing harness tests pinning the narrowest vertical — a single in-memory .loom source discovered + registered as a slash command, dispatched, its mode: prompt frontmatter parsed, its single untyped @-query issued as one prompt-mode turn whose streamed assistant response appends as one appended turn (exactly one appended turn, no diagnostic). Harness session double extended with a pi.sendMessage diagnostics recorder (systemNotes) so the no-diagnostic assertion is observable. Tests compile (typecheck clean), lint clean, and red on their primary assertions (transcript empty) because the prompt-mode pipeline is absent — fail-red-for-the-intended-reason; remaining 109 tests stay green.
2026-06-30 M — Minimal end-to-end .loom slash command (SLSH-2 MVP happy path): buildMinimalLoom (src/mvp/minimal-loom.ts) parses a mode:-only frontmatter block and the single untyped @`<literal>` body query of an in-memory .loom source and returns a LoomFixture whose run drives one prompt-mode turn — pi.sendUserMessage(rendered literal) then ctx.waitForIdle() — leaving exactly one appended user+assistant turn and no diagnostic; harness-supplied single-source discovery only (no ambient src/** filesystem read, no FileSystem seam). The two M-T SLSH-2 tests go green; full suite 111 green, typecheck + lint clean.
2026-06-30 V7a-T — Diagnostics-primitive tests: the src/diagnostics/diagnostic.ts seam (Diagnostic shape: severity/code/file?/range?/message/hint?/related?/masked?/details?, plus stubbed renderDiagnosticLine / renderDiagnosticBatch / assembleDiagnostics) and tests/diagnostics-primitive.test.ts. Tests pin DIAG-1 (every author-visible diagnostic carries a registry code; located `<file>:<line>:<col>: <code>: <message>`, file-only, and location-less content-line forms; hint continuation + code-less related-site lines, messages sourced from the registry Message column) and multi-error assembly (no fast-fail, no per-error loss; single Diagnostic[] ordered by (file, line, col) across an entry .loom + 2 transitively-imported .warp modules supplied out of order). Typecheck + lint clean; 7 tests red on their primary assertions (empty render / empty array) because the primitive is absent — fail-red-for-the-intended-reason; other 111 tests stay green.
2026-06-30 V7a — Diagnostics primitive: implements the three src/diagnostics/diagnostic.ts seam functions the V7a-T tests pin. renderDiagnosticLine emits the DIAG-1 serialised content-line by located-site category — located `<file>:<line>:<col>: <code>: <message>`, file-only `<file>: <code>: <message>`, location-less `<code>: <message>` — appending the two-space-indented `  hint: <hint>` continuation when a hint is present and one code-less `  <file>:<line>:<col>: <message>` line per related site. renderDiagnosticBatch joins one renderDiagnosticLine block per diagnostic with a single blank line in the given order. assembleDiagnostics flatMaps every per-file group into one Diagnostic[] (no fast-fail, no per-error loss) and stable-sorts by (file, line, col) so an entry .loom and >=2 transitively-imported .warp modules supplied out of order land in spec order. All 7 V7a-T tests green; full suite 118 green, typecheck + lint clean.
2026-06-30 V7b-T — Diagnostic code-registry tests: the tools/code-registry/ seam (parseRegistry / registryMessage / reconcileClosedSet / reconcileStableIds, inert stubs returning empty/undefined) plus tests/code-registry.test.ts with four failing tests pinning the paired V7b machine-checkable registry + closed-set/stable-id enforcement the H5a gate consumes. DIAG-2: the registry is closed — parseRegistry yields one structured row (namespace/severity/phase/trigger/message) per code across the parse/load/runtime/host families (read from the four sharded spec registry pages), and reconcileClosedSet fails both directions (an asserted code with no registry row → asserted-code-not-in-registry; a registry code no test asserts → registry-code-no-asserting-test). DIAG-3: codes are stable identifiers — reconcileStableIds reports code-renamed for a pinned-baseline code absent from the current registry (renames deferred to loom 2.0). DIAG-4: the Message column is normative — registryMessage is the single source the asserting tests draw expected strings from (binding-case-mismatch message sourced from the registry, not prose). Typecheck + lint clean; 4 tests red on their primary assertions because the registry seam is absent (fail-red-for-the-intended-reason); other 119 tests stay green.
2026-06-30 V7b — Diagnostic code registry: implements the four tools/code-registry/ seam functions the V7b-T tests pin. parseRegistry parses the four sharded spec registry pages into one structured {code, namespace, severity, phase, trigger, message} row per code, splitting table cells on unescaped pipes (GFM `\|` does not split), decoding `\|`/`` \` `` table escapes, and taking the Message column as the first..last backtick span (so a runtime row's trailing `.` outside the span is dropped). registryMessage returns a code's normative Message string (DIAG-4 single source) or undefined. reconcileClosedSet enforces DIAG-2 in both directions (asserted-code-not-in-registry for an asserted code with no row; registry-code-no-asserting-test for a registry code no test asserts). reconcileStableIds enforces DIAG-3 (code-renamed for a pinned-baseline code absent from the current registry; renames deferred to loom 2.0). All 5 V7b-T tests green; full suite 123 green, typecheck + lint clean.
2026-06-30 V7c-T — Placeholder-rendering tests: the src/diagnostics/placeholder.ts seam (eight per-category render functions — renderType (cat 1), renderRuntimeValue (cat 2), renderConstruct (cat 3), renderInteger (cat 4), renderSourceDerived (cat 5), renderUnderlyingError (cat 6), renderCategory7 (cat 7), renderHostDerivedTail/renderHostIncompatible (cat 8); inert stubs returning "") plus tests/placeholder-rendering.test.ts with ten failing tests pinning DIAG-4. Each of the eight categories asserts its normative vector byte-identically (array<integer | string> / Foo | null; compact JSON {"name":"fluffy"} + integer 42; arrow function + verbatim a + b span; -1/33/-0; bare identifier wibble + settings:"~/work/looms" descriptor + my-key→"my-key"/kind→kind; connection closed first-line + <no message>; closed-enum quit + uuid + integer; node-floor host tail). The category-2 80→81 code-point boundary: full at 80 code points, 77 chars + "..." at 81. The category-8 loom/load/host-incompatible (node-floor) row with the host version mocked to v18.19.0 (passed in, never read from process — the src/** ambient-primitive ban) asserts via the §8 anchored partial-match ^host incompatible \(node-floor\): observed (.*), required >=22\.19\.0$, sourcing the Message template from the registry (registryMessage) per the Diagnostic message anchors rule, never a full-Message equality (non-conformant for category-8 rows). Typecheck + lint clean; 10 tests red on their primary assertions (empty render '') because the renderer is absent — fail-red-for-the-intended-reason; other 123 tests stay green.
2026-06-30 V7c — Placeholder rendering: implements the eight per-category renderers in src/diagnostics/placeholder.ts the V7c-T tests pin (DIAG-4). renderType (cat 1) re-serialises a Loom static type in source-grammar form (lowercase primitives, ` | `-joined unions with no parens, array<T>, Result<T, E>, inline object `{ f: T }` in declaration order). renderRuntimeValue (cat 2) stringifies per the canonical table — compact JSON.stringify for a schema-typed object (schema name does not surface), integer/number shortest-decimal with -0→0, Ok(…)/Err(…) for Result — with the category-2 string truncation: a string over 80 Unicode code points is cut to its first 77 code points + the literal "..." (counted by code point via Array.from, not UTF-16 units). renderConstruct (cat 3) emits the caller-supplied closed token-name (<construct>) or verbatim source span (<expr>). renderInteger (cat 4) is shortest-decimal with -0→"0". renderSourceDerived (cat 5) renders identifiers/paths bare, <key> double-quoted only when not identifier-shaped (^[A-Za-z_][A-Za-z0-9_]*$), <char> raw when printable else \xNN (≤U+00FF) / \u{NNNN}, <descriptor> as <kind>:"<value>". renderUnderlyingError (cat 6) coerces a caught thrown value (object string .message; else String(v); else <unreadable> when String(v) throws) then first-line-truncates (\r\n and bare \r → \n, prefix up to first \n, trailing whitespace preserved, <no message> when empty; \u2028/\u2029 ordinary). renderCategory7 (cat 7) dispatches the closed sub-rules (identifier/closed-enum verbatim, <uuid> canonical lowercase, numeric via cat 4, path/descriptor via cat 5). renderHostDerivedTail / renderHostIncompatible (cat 8) assemble the host-incompatible Message template `host incompatible (<kind>): observed <observed>, required <required>` with the host-derived <observed> first-line-truncated; the host version is passed in, never read from the ambient process (the src/** ambient-primitive ban). All 10 V7c-T tests green; full suite 133 green, typecheck + lint clean.
2026-06-30 V7d-T — loom-system-note delivery-channel tests: the src/extension/system-note-channel.ts seam (sendSystemNote + emitDiagnosticBatch, inert no-op stubs; SystemNote/SystemNoteDetails/SystemNoteSender/UiNotifier/SystemNoteChannelDeps shapes; SYSTEM_NOTE_CHANNEL + SYSTEM_NOTE_DELIVERY_FAILED_CODE constants) plus an injectable formatLines seam added to createSystemNoteRenderer (so the PIC-21 test can drive an internal renderer-body throw without coupling to the dim-styling implementation), and tests/system-note-channel.test.ts with eight failing tests. Multi-error batch delivery: the V7a-assembled Diagnostic[] is delivered as exactly one sendMessage per .loom carrying the full batch serialised into content (renderDiagnosticBatch) and the Diagnostic[] in details.diagnostics, with no per-error fan-out (sent.length === 1 for a 3-diagnostic batch). Re-scan re-emits without dedup/supersede (two emitDiagnosticBatch calls => two sendMessage calls). PIC-21: an internal renderer throw does not escape the MessageRenderer invocation — display:true falls back to a minimal Component rendering raw message.content, display:false returns undefined, and no loom/runtime/* diagnostic is emitted (renderer takes no diagnostics sink by construction). loom/runtime/system-note-delivery-failed fallback chain: on a pi.sendMessage throw the runtime falls back in order — ctx.ui.notify(content,"error"), then a loom/runtime/system-note-delivery-failed diagnostic (message = original content, hint = underlying throw message), then a terminal console.error — skipping ctx.ui.notify when display:false or content:"", catching a throwing ctx.ui.notify and proceeding to the diagnostic step, and never aborting the handler; PIC-54: the terminal console.error is try/catch-wrapped at the definition site and a throw from it is silently swallowed without propagating out of the sendSystemNote fallback chain. Typecheck + lint clean; the 8 tests red on their primary behavioural assertions (delivery / fallback / PIC-21 hardening absent) — fail-red-for-the-intended-reason; other 133 tests stay green.
2026-06-30 V7d — loom-system-note delivery channel: implements src/extension/system-note-channel.ts (paired with V7d-T). sendSystemNote sends one best-effort pi.sendMessage({customType:"loom-system-note", content, display, details}, {triggerTurn:false}) — never awaited, no .catch — and on a synchronous throw runs the fallback chain per runtime-event-channel.md §System notes: step 1 ctx.ui.notify(content,"error") (skipped when display:false or content:"", a throwing notify caught and swallowed), step 2 a loom/runtime/system-note-delivery-failed diagnostic (message=original note content, hint=underlying throw message), step 3 terminal console.error wrapped in try/catch (PIC-54: a console.error throw is silently swallowed and never propagates out of the chain, regardless of reach-path). The chain never aborts the slash handler or spawned subagent session. emitDiagnosticBatch is the producer-facing diagnostic-emission seam: it delivers a V7a-assembled Diagnostic[] as exactly one sendMessage per .loom (content=renderDiagnosticBatch(batch), details.diagnostics=full batch, no per-error fan-out) and re-emits on a re-scan with no dedup/supersede. createSystemNoteRenderer gains the PIC-21 render-time exception trap: an internal renderer-body throw (e.g. injected formatLines) does not escape — display:true returns a minimal Component rendering raw message.content, display:false returns undefined, and no loom/runtime/* diagnostic is emitted. Broad catches carry allow-broad-catch comments (pi-sdk-boundary for the pi.sendMessage / ctx.ui.notify SDK throws, PIC-21 for the renderer trap, PIC-54 for the diagnostic-step and terminal-console.error wraps). All 8 V7d-T tests green; full suite 141 green, typecheck + lint clean.
2026-06-30 V8b-T — FileSystem seam tests (paired V8b impl): a PiFileSystem production-adapter stub (src/seams/pi-file-system.ts, every member throws "not implemented") and an in-memory FakeFileSystem conforming-seam stub (tests/helpers/fake-file-system.ts, constructor takes homedir/cwd/files/dirs/symlinks/errors/caseInsensitive), plus tests/filesystem-seam.test.ts with 31 failing tests. PIC-13: the eight enumerated FileSystem members map Node .code values — readText/writeText/exists round-trip and reject ENOENT (false on exists) / EACCES / EPERM; homedir() and cwd() return constructor-injected values (Fake) and os.homedir() / a once-captured process.cwd() (Pi), never reading process.env / re-reading process.cwd(); readdir returns entry names only and preserves NFD bytes un-normalised (entry-name encoding guarantee); lstat does not follow symlinks; realpath follows symlinks transitively to a canonical absolute path, rejects ELOOP on a cycle / ENOENT on a missing component / EACCES / EPERM, and is byte-stable + case-canonicalising. lexical.md §Encoding: readBytes returns the raw pre-decode bytes as a Uint8Array preserving invalid UTF-8, carrying the same ENOENT/EACCES/EPERM .code mapping as readText. Typecheck + lint clean; the 31 tests red on their primary assertions because the V8b adapters are absent (fail-red-for-the-intended-reason); the H3a ambient scan and the other 141 tests stay green.
2026-06-30 V8b — FileSystem seam (paired with V8b-T): implements the PiFileSystem production adapter (src/seams/pi-file-system.ts) and the in-memory FakeFileSystem (tests/helpers/fake-file-system.ts) against PIC-13. PiFileSystem delegates to Node fs.promises / os — readText (utf8), readBytes (raw Buffer copied into a standalone Uint8Array preserving invalid UTF-8 per lexical.md §Encoding), writeText, exists (resolves false only on ENOENT through a Promise rejection handler rather than a broad catch — Node fs errors carry no narrow subtype — and rejects every other .code), homedir() via os.homedir() (no process.env, no platform branch), cwd() captured once at construction from process.cwd() carrying the `// allow-ambient: process.cwd — FileSystem` exempt-site comment the H3a scan admits, readdir (entry names only, no Unicode normalisation), lstat (does not follow symlinks), and realpath via promisified fs.realpath.native (full-path canonicalisation satisfying PIC-13's case-canonicalisation and byte-stable canonical-output guarantees that bare fs.promises.realpath does not). FakeFileSystem reports its constructor-injected homedir/cwd/files/dirs/symlinks/errors/caseInsensitive maps with the same Node-style .code rejection mapping (ENOENT/EACCES/EPERM), transitive symlink resolution with ELOOP cycle detection, ENOENT on a missing component, and case-insensitive realpath canonicalisation; writeText round-trips through readText. All 31 V8b-T tests green; full suite 172 green, typecheck + lint clean; the H3a ambient-primitive scan stays green (the single process.cwd() reference is allow-ambient-exempt).
2026-06-30 V1a-T — Lexer-core tests (paired V1a impl): a src/lexer/lexer.ts seam stub (lexLoom(source, deps) — inert no-op returning an empty token stream and no diagnostics; Token/TokenKind/LoomSource/LexResult shapes, including the significant stmt-sep token marking a real statement boundary) plus tests/lexer-core.test.ts with 15 failing tests. Every lexer-surfaced diagnostic is asserted to fire through the V7d producer-facing diagnostic-emission seam — a recording channel double captures the batched loom-system-note details.diagnostics the lexer delivers through emitDiagnosticBatch → pi.sendMessage, never a direct out-of-band pi.sendMessage. loom/load/invalid-encoding: a non-UTF-8 byte ([0x68,0x69,0xff]) fails load (result.ok=false) at zero-based byte offset 2, message "invalid UTF-8 encoding at byte offset 2" anchored to code-registry-load.md, delivered as a batched loom-system-note (customType=loom-system-note, details.diagnostics present). lexical.md §Newline normalisation: LF and CRLF inputs tokenise to identical (non-empty) token streams. loom/parse/reserved-keyword-as-identifier (let match = 1), loom/parse/schema-case-mismatch (schema animal = …), loom/parse/binding-case-mismatch (let Foo = 1), loom/parse/block-comment (/* … */), loom/parse/single-line-if (if (x) stmt), loom/parse/stray-backslash (lone backslash) — each fires with its registry-anchored Message from code-registry-parse.md. grammar.md §Newline continuation: each of the four closed continuation triggers (unmatched open bracket, trailing binary/ternary operator, trailing comma inside an open bracket, leading binary/ternary operator) tokenises its spanning lines as one statement (statementGroups length 1), and one or more intervening blank lines do not break the continuation for both the trailing-trigger (let x =\n\n  foo) and leading-operator (let x = a\n\n  + b) forms. Typecheck + lint clean; the 15 tests red on their primary assertions (empty token stream, wrong statement count, no delivered diagnostic) because the V1a tokeniser/validator/continuation engine is absent — fail-red-for-the-intended-reason; the other 172 tests stay green.
2026-06-30 V1a — Lexer core (paired with V1a-T): implements src/lexer/lexer.ts. lexLoom UTF-8-validates the raw pre-normalisation bytes via firstInvalidUtf8Offset (a hand-written UTF-8 state machine returning the zero-based offset of the first invalid byte, rejecting overlong/surrogate continuation ranges; a non-UTF-8 BOM faults on its leading byte → offset 0) and, on failure, emits loom/load/invalid-encoding (file-only, message "invalid UTF-8 encoding at byte offset <offset>" anchored to code-registry-load.md) through the V7d emitDiagnosticBatch producer seam, never a direct pi.sendMessage. On valid input it strips a leading UTF-8 BOM (EF BB BF), normalises \r\n and bare \r → \n before any span is recorded (so CRLF and LF sources tokenise byte-identically — observable, not an implementation detail), then scanTokens produces raw tokens (// and /// line comments consumed silently; /* */ → loom/parse/block-comment and aborts the scan; a backslash outside any literal → loom/parse/stray-backslash; single-line string literals with \-escaped chars; decimal numbers; identifiers vs the reserved-keyword set; greedy two-char operators ==/!=/<=/>=/&&/||). collapseContinuations folds raw newline runs into significant stmt-sep tokens under the closed continuation-trigger rule (grammar.md §Newline continuation): a run is swallowed when bracket depth is open, the prior token is a trailing binary/ternary operator (or the binding =, per the spec's own worked example), or the next token is a leading binary/ternary operator — decided once per run, so one or more blank lines never break a continuation. contextualDiagnostics fires loom/parse/reserved-keyword-as-identifier, loom/parse/schema-case-mismatch, loom/parse/binding-case-mismatch at let / let mut / fn / schema / enum name positions and loom/parse/single-line-if for an if/for/while/fn header whose logical line carries no {. All 15 V1a-T tests green; full suite 187 green, typecheck + lint clean; H2a module-level-mutable + H3a ambient scans green (lookup Sets are function-local; TextDecoder/TextEncoder are not banned ambient primitives).
2026-06-30 V1b-T — String/number/path-literal tests (paired V1b impl): the src/lexer/literals.ts seam (validatePathLiteral / checkIntegerNarrowing inert stubs; NumericLiteralType / PathLiteralKind / PathLiteral / NarrowingSite shapes) plus two optional Token fields (value for the decoded string literal, numericType for the integer/number tag), and tests/literals-and-paths.test.ts with 15 failing tests. String and number behaviours are lexer-surfaced, asserted through lexLoom and the V7d producer-facing diagnostic seam (recording channel double capturing the batched loom-system-note details.diagnostics): the LEX code-keyed area — \u{1F600} decodes to Token.value, and the simple escape table (\n \t \" \) decodes to its characters; loom/parse/illegal-escape (\x), loom/parse/invalid-unicode-escape (a value above U+10FFFF and a D800 surrogate), loom/parse/integer-literal-out-of-range (12345678901234567890 > 2^53-1, judged per-token), loom/parse/number-literal-not-finite (1e400 overflowing to Infinity), and loom/parse/unsupported-feature (0xFF / 0o17 / 0b101 / 1_000) all fire; 3.14/42 classify as number/integer on Token.numericType. The path-literal and narrowing rules need a parse/type context the tokeniser lacks, so they use the standalone literals.ts seams: loom/parse/invalid-path-separator (a backslash separator, located at the offending span), the byte-exact lowercase .LOOM rejection via loom/parse/invoke-non-loom-extension (cross-OS), and loom/parse/integer-narrowing (number→integer fires "cannot narrow number to integer", integer→number widening returns undefined). Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule. Typecheck + lint clean; the 15 tests red on their own primary assertions (a missing decoded value, a missing numeric type, or an absent diagnostic) because the V1b escape-decoder / number-range checks / path validator / narrowing check are absent — fail-red-for-the-intended-reason; the other 187 tests stay green.
2026-06-30 V1b — String/number/path literals (paired with V1b-T): fills src/lexer/lexer.ts string + number scanning and the src/lexer/literals.ts seam. The string scanner decodes the escape table (\" \' \ \n \t \r \u{XXXX}) into Token.value (Token.text keeps the verbatim source slice), firing loom/parse/illegal-escape (message "illegal escape sequence: \<char>") for an unrecognised escape (including a dangling backslash at EOL/EOF) and loom/parse/invalid-unicode-escape ("invalid Unicode escape: value is not a Unicode scalar value") for a \u{...} whose 1–6-hex-digit scalar exceeds U+10FFFF or lies in the D800–DFFF surrogate range (a well-formed scalar decodes via String.fromCodePoint). The number scanner sets Token.numericType (integer when there is no fractional or exponent part, else number), fires loom/parse/integer-literal-out-of-range ("integer literal exceeds the safe-integer range") when an integer literal's per-token magnitude exceeds 2^53-1 and loom/parse/number-literal-not-finite ("number literal is not a finite IEEE-754 double") when a number literal parses to a non-finite double (e.g. 1e400 → Infinity), and treats any digit/letter/underscore abutting the decimal literal — the loom 1.0-deferred 0x/0o/0b radix prefixes and the 1_000 underscore separator — as loom/parse/unsupported-feature (consuming the malformed run). literals.ts validatePathLiteral fires loom/parse/invalid-path-separator ("invalid path separator: backslash in path literal", located at the literal span) for any backslash, then the byte-exact lowercase final-segment check on the literal as written: an import path not ending in .warp → loom/parse/import-non-warp-extension, an invoke/tools path not ending in .loom → loom/parse/invoke-non-loom-extension, so .LOOM/.WARP is rejected identically cross-OS. checkIntegerNarrowing returns loom/parse/integer-narrowing ("cannot narrow number to integer") for a number value reaching an integer position and undefined for the one-way integer→number widening. Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule; all functions pure (no module-level state, no broad catch, no ambient primitive, no Promise combinator or Sync call). All 15 V1b-T tests green; full suite 202 green, typecheck + lint clean.
2026-06-30 V2a-T — Type-grammar + loom-literal-sublanguage tests (paired V2a impl): two inert seam stubs — src/parser/type-grammar.ts (parseTypeExpression(source, position, site) with TypePosition return/value/schema-feeding, and checkArrayCommonType(context, elementTypes, site) with ArraySinkContext over the exhaustive sink set plus for-iterand/none) and src/parser/literal-sublanguage.ts (checkLiteralSublanguage(source, position, site) with LiteralPosition default/tool-arg, and checkObjectLiteralFields(schema, presentFields, site) with ObjectSchemaSpec) — plus tests/type-grammar.test.ts with 8 failing tests. grammar.md §"Type grammar": loom/parse/generic-arity-mismatch (array<string, number> → "generic type 'array' expects 1 type argument(s); got 2"; Result<string> → "...'Result' expects 2...; got 1"), loom/parse/void-in-non-return-position ("'void' is only permitted as a function or loom return type" — fires in value position, not in return position), loom/parse/result-in-schema-position ("'Result' has no lowered-schema form and is not permitted in a schema-feeding position" — fires in schema-feeding, not value). grammar.md §"Loom literal sublanguage": loom/parse/default-not-literal (a params: default operator form `a + b`), loom/parse/tool-arg-not-literal (a Pi-tool-arg function-call form `f(x)`), loom/parse/missing-object-field (Cat{name} omitting age → "missing field 'age' on schema 'Cat'"; a full-field literal raises none). grammar.md §"array<T> literal type-sink rule": loom/parse/array-no-common-type ("array elements have no common type; annotate the binding with array<A | B> or use a single schema" — a `for x in []` iterand is not a sink and fires; a binding-annotation sink resolves). Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule. Typecheck + lint clean; the 8 tests red on their own primary assertions (an absent expected diagnostic) because the V2a type-expression parser, is-literal check, and sink-resolution engine are absent — fail-red-for-the-intended-reason; the other 202 tests stay green (H2a/H3a scans green — the new src/parser/** seams hold no module-level state and no ambient primitive).
2026-06-30 V2a — Type grammar and loom literal sublanguage (paired with V2a-T): fills src/parser/type-grammar.ts and src/parser/literal-sublanguage.ts. parseTypeExpression tokenises and recursive-descent-parses a type expression (primitive / named / generic array (arity 1) / Result (arity 2) / inline-object / union / literal type forms, return-only void), then walks the AST in source order for the three position-sensitive checks: loom/parse/generic-arity-mismatch (a closed-set constructor applied with a type-argument count other than its declared arity — message "generic type '<ctor>' expects <n> type argument(s); got <m>"), loom/parse/void-in-non-return-position ("'void' is only permitted as a function or loom return type" — admitted only as the top-level type in a return position; a void nested in a generic argument, inline-object field, or union arm is never the top-level return type and always fires), and loom/parse/result-in-schema-position ("'Result' has no lowered-schema form and is not permitted in a schema-feeding position" — every Result application within a schema-feeding type fires, since the whole tree including array<T> element types and union arms is lowered-schema reachable; Result stays admitted in value/return positions). checkArrayCommonType implements the exhaustive array<T> literal type-sink rule: binding-annotation / fn-param / constructor-field / array-element are sinks (return undefined); the for-iterand (explicitly NOT a sink) and none contexts leave the literal unsunk, firing loom/parse/array-no-common-type when the elements alone have no single common type (an empty [] always fires, a single shared element type self-resolves). checkLiteralSublanguage tokenises (handling backtick templates, ${...} interpolation, and @`...` query templates as forbidden single tokens) and recursive-descent-parses the expression, then pre-order-walks for the first sub-expression outside the literal sublanguage — admitted forms are primitive literals, Enum.Variant member access (one level, head a bare identifier), unary - on a numeric literal, array literals, and bare/named object literals (recursing into members); identifier references, calls, index access, binary/ternary operators, templates, and queries are forbidden — emitting loom/parse/default-not-literal (params: default RHS) or loom/parse/tool-arg-not-literal (Pi-tool argument) naming the offending sub-expression source text. checkObjectLiteralFields emits one loom/parse/missing-object-field per declared field absent from the literal, in declared order (message "missing field '<field>' on schema '<schema>'"). Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule. The two generic/precedence lookup tables are Object.freeze-wrapped so the H2a module-level-mutable architectural test (which flags an object/array-literal const initializer) stays green; parsers are per-call class instances holding no cross-invocation state; no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 8 V2a-T tests green; full suite 210 green, typecheck + lint clean; H2a/H3a scans green.
2026-06-30 V2c-T — Runtime value-model + equality tests (paired V2c impl): the src/runtime/value.ts seam (LoomValue; the opaque branded EnumValue; the ResultValue Ok/Err discriminated union; the makeEnumValue / makeOk / makeErr constructors; valuesEqual — the structural-equality relation — and isWireLowerable, both inert stubs: valuesEqual reports every pair unequal, isWireLowerable reports every value lowerable, and makeEnumValue returns a plain tagged object) plus tests/runtime-value-model.test.ts with 9 failing tests pinning the runtime-value-model.md RVM code-keyed area. Value representation (value-representation table): JSON.stringify of an enum value yields the bare wire string "high" (the interpreter-private declaring-enum tag never appears in JSON output); a Result value is never lowered to wire — isWireLowerable(makeOk(1)) and isWireLowerable(makeErr("boom")) are false, isWireLowerable(42) is true. Equality (runtime-value-model.md #equality): structural deep equality on arrays (element-wise, equal length), objects (loom-side key set, order-irrelevant), and nested values; a cross-type pair compares false with neither a parse diagnostic nor a panic (42 == true, null == "x", [1] == 1, Severity.Low == "low"); NaN == NaN is true; +0 == -0 is true; 42 == 42.0 is true (integer ⊑ number routes to per-shape value comparison); enum equality compares the declaring-enum tag and wire value (Severity.High == Severity.High true, Severity.High == OtherEnum.High false even when wire values match, Severity.High == Severity.Low false); Result equality compares the Ok/Err discriminator and recurses on the payload (Ok([1,2]) == Ok([1,2]) true, Ok("x") == Err("x") false, Ok(1) == Ok(2) false). Typecheck + lint clean; the 9 tests red on their own primary assertions (a tagged-object JSON.stringify, an every-value-lowerable predicate, an always-unequal relation) because the V2c declaring-enum-tagged representation, structural-equality relation, and Result-not-lowerable recognition are absent — fail-red-for-the-intended-reason; the other 210 tests stay green (H2a/H3a scans green — the new src/runtime/value.ts holds no module-level mutable state and no ambient primitive).
2026-06-30 V2c — Runtime value model and equality (paired with V2c-T): fills src/runtime/value.ts. makeEnumValue encodes an enum value as a boxed String with a non-enumerable __loomEnum declaring-enum tag (reference encoding), so JSON.stringify of the value yields the bare wire string ("high") and the tag never appears in JSON output, while still carrying the tag for cross-enum equality. valuesEqual implements runtime-value-model.md §Equality structural deep equality: enum values compare the declaring-enum tag AND the wire value (Severity.High == Severity.High true, Severity.High == OtherEnum.High false even when wire values match, Severity.High == Severity.Low false); Result compares the Ok/Err discriminator and recurses on the payload (Ok([1,2]) == Ok([1,2]) true, Ok("x") == Err("x") false, Ok(1) == Ok(2) false); arrays compare element-wise at equal length; objects compare the loom-side key set and per-key value with declaration order irrelevant ({a:1,b:2} == {b:2,a:1} true); primitives compare by value with the two fixed refinements NaN == NaN true and +0 == -0 true; a cross-type pair — including enum-vs-string (Severity.Low == "low") and object-vs-primitive ([1] == 1) — evaluates false with neither a parse diagnostic nor a panic; 42 == 42.0 is true (integer ⊑ number routes to per-shape value comparison). isWireLowerable recognises a Result value (the ok-discriminator shape) as never lowerable and reports primitives/arrays/objects/enums lowerable. ENUM_TAG is a constant string literal (no module-level mutable state); the enum-tag and Result recognisers are pure helpers; no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 9 V2c-T tests green; full suite 219 green, typecheck + lint clean; H2a/H3a scans green.
2026-06-30 V2d-T — Canonical integer/number renderer tests (paired V2d impl): the src/render/canonical-number.ts seam (renderCanonicalNumber(value, kind) with NumberKind = "integer" | "number", an inert stub returning "") plus tests/canonical-number-render.test.ts with 11 failing tests pinning BNDR-4 / BNDR-5 from binder/defaulting-system-note-echo.md §"Echo policy". BNDR-4 (integer kind): canonical base-10 — 42→42, 0→0, -7→-7, 1e21→1000000000000000000000 (no exponent/separators), -0→0 (bndr-6p). BNDR-5 (number kind): shortest round-tripping fixed-point with no scientific notation — 3.14→3.14 (non-integral keeps a fractional digit, bndr-6q), 42→42 (integral carries no .0), the large-magnitude ±1e21 switch expanded (1e21→1000000000000000000000, bndr-6r), the small-magnitude |value| < 1e-7 switch expanded (1e-8→0.00000001 bndr-6s and the interior vector 5e-8→0.00000005), -0→0 (bndr-6p). The renderer selects BNDR-4 vs BNDR-5 from the caller-supplied kind discriminator, not the value's runtime integrality, and has no dependency on the runtime value model. Typecheck + lint clean; the 11 tests red on their own primary assertions (expected '' to be …) because the V2d rendering body is absent — fail-red-for-the-intended-reason; the other 219 tests stay green.
2026-06-30 V2d — Canonical integer/number renderer (paired with V2d-T): fills src/render/canonical-number.ts. renderCanonicalNumber(value, kind) selects BNDR-4 (integer) vs BNDR-5 (number) from the caller-supplied kind discriminator — never the value's runtime integrality — and has no dependency on the runtime value model or schema declarations. A shared expandToFixedPoint helper re-expands String(n)'s exponential form (the BNDR-5-forbidden large-magnitude ±1e21 switch and small-magnitude |value| < 1e-7 switch) to pure fixed-point decimal by shifting the decimal point (right for a positive exponent, left for a negative one, zero-padding as needed), while a non-exponential shortest-digit string from String(n) is returned untouched; canonicalDecimal adds sign handling and the -0 → 0 pin (value === 0 covers both +0 and -0, bndr-6p). BNDR-4 (integer kind): canonical base-10 — 42→42, 0→0, -7→-7, 1e21→1000000000000000000000 (no exponent, no separators). BNDR-5 (number kind): shortest round-tripping fixed-point with no scientific notation — 3.14→3.14 (bndr-6q, non-integral keeps a fractional digit), integral 42→42 (no trailing .0), the large-magnitude switch 1e21→1000000000000000000000 (bndr-6r), the small-magnitude switch 1e-8→0.00000001 (bndr-6s) and the required interior vector 5e-8→0.00000005 (strictly inside 1e-8 < |5e-8| < 1e-7), -0→0 (bndr-6p). Messages/anchors per BNDR-4/BNDR-5 in binder/defaulting-system-note-echo.md §"Echo policy". The discriminator is the sole selector; both rules reduce to one exponent-free fixed-point computation for conforming inputs. Pure functions — no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 11 V2d-T tests green; full suite 230 green, typecheck + lint clean.
2026-06-30 V5a-T — Schema-declaration tests (paired V5a impl): the src/parser/schema-declarations.ts seam (checkObjectSchema / checkEnumDeclaration / checkInlineEnumForm / checkVariantAccess inert no-op stubs; ObjectSchemaDecl / SchemaFieldDecl / EnumDecl / EnumVariantDecl / EnumValueKind / VariantAccess / SchemaDeclSite shapes) plus tests/schema-declarations.test.ts with 10 failing tests pinning schemas.md schema/alias/enum declarations. §Object schema: loom/parse/empty-schema-body (schema X { } → "'X' has no fields; an empty schema cannot be validated.", a non-empty schema raises none). §Wire-name renaming: loom/parse/wire-name-collision (two fields sharing a wire name, and a wire name equal to another field's loom-side name → "wire name '<name>' collides with another field on schema '<schema>'"), loom/parse/redundant-wire-name (W) (field as "field" → "redundant 'as' clause: wire name '<name>' equals the loom-side name", severity warning; a genuine rename raises none). §Enum declarations: loom/parse/empty-enum-body (enum X { } → "'X' has no variants; an empty enum cannot be validated."), loom/parse/inline-enum (enum["a","b"] → "inline 'enum[...]' is not supported; use a top-level 'enum' declaration or a literal-union"), loom/parse/non-string-enum-value (Low = 1 → "enum variant value must be a string literal; got integer"; a string value raises none), and the name-before-value ordering per §Enum declarations — enum X { Low = "a", Low = "b" } (distinct explicit values, shared name) fires loom/parse/duplicate-enum-variant-name ("duplicate variant name 'Low' on enum 'X'") and NOT loom/parse/duplicate-enum-value, while enum X { Low = "x", High = "x" } (distinct names, shared value) fires loom/parse/duplicate-enum-value ("duplicate enum value 'x' across variants of enum 'X'"). §Variant access: loom/parse/unknown-variant (Severity.Critical → "unknown variant 'Critical' on enum 'Severity'"; a declared variant raises none). Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule. Typecheck + lint clean; the 10 tests red on their own primary assertions (an absent expected diagnostic) because the V5a schema-declaration checker is absent — fail-red-for-the-intended-reason; the other 230 tests stay green.
2026-06-30 V5a — Schema declarations (object / alias / enum) (paired with V5a-T): fills src/parser/schema-declarations.ts. checkObjectSchema fires loom/parse/empty-schema-body for a no-field schema (returns immediately); loom/parse/redundant-wire-name (W) when a field's wire name equals its loom-side name; and loom/parse/wire-name-collision when two fields share an effective wire name (wireName ?? loomName) or an explicit wire name equals another field's loom-side name, each colliding name reported once in source order. checkEnumDeclaration enforces the schemas.md §Enum declarations name-before-value ordering: loom/parse/empty-enum-body for a no-variant enum; loom/parse/duplicate-enum-variant-name for a repeated identifier (so enum X { Low = "a", Low = "b" } fires the name code, not the value code); loom/parse/non-string-enum-value for an explicit value whose kind is not string ("got <kind>"); loom/parse/duplicate-enum-value only when two or more DISTINCT variant names share one explicit string value (enum X { Low = "x", High = "x" }), grouped via a value→distinct-name-set map so a same-name same-value pair does not also fire the value code. checkInlineEnumForm matches a leading enum[ form and fires loom/parse/inline-enum. checkVariantAccess fires loom/parse/unknown-variant when the variant is absent from knownVariants. Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule. Pure functions — no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 10 V5a-T tests green; full suite 240 green, typecheck + lint clean.
2026-06-30 V5b-T — Discriminated-union / recursion / cycle-detection tests (paired V5b impl): three inert seam stubs on src/parser/schema-declarations.ts (checkDiscriminatedUnion(decl, site) with DiscriminatedUnionDecl / UnionVariantSchema / DiscriminatorCandidateField carrying per-variant literal kind+text and a nested flag and an optional explicit `by` field; checkByClause(decl, site) with ByClauseDecl form object/union; detectTypeAliasCycles(nodes, site) with SchemaGraphNode kind alias/object and references) plus tests/disc-unions-recursion.test.ts with 7 failing tests pinning schemas.md §Discriminated unions + §Recursion. §Discriminated unions: loom/parse/non-string-discriminator (integer kind → "discriminator 'kind' on Animal must be a string-literal type; got integer"), loom/parse/ambiguous-discriminator (kind+species both qualify → "ambiguous discriminator for Animal; candidates: kind, species. Declare explicitly with 'by <field>'."), loom/parse/missing-discriminator (no shared single-literal field → "Animal is a union of object schemas with no shared single-literal discriminator field. Add a 'kind' (or similar) field to each variant, or declare explicitly with 'by <field>'."), loom/parse/duplicate-discriminator-value (explicit by kind, both kind: "same" → "duplicate discriminator value 'same' across variants of Animal"), loom/parse/nested-discriminator (kind: { type: "x" } → "discriminator field 'kind' must be at the top level of each variant of Animal"), loom/parse/by-on-object-schema (schema X by f { ... } → "the 'by' clause applies only to discriminated-union schemas (schema X by f = A | B | …)"; the union form raises none). §Recursion: loom/parse/type-alias-cycle (pure-alias cycle schema X = Y; schema Y = X → "type-alias cycle: X → Y → X"; a cycle through an object-schema hop schema Y { f: X } raises none). Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule. Typecheck + lint clean; the 7 tests red on their own primary assertions (an absent expected diagnostic) because the V5b discriminated-union / cycle checker is absent — fail-red-for-the-intended-reason; the other 240 tests stay green.
2026-06-30 V5b — Discriminated unions, recursion, and cycle detection (paired with V5b-T): implements the three V5b seams on src/parser/schema-declarations.ts. checkDiscriminatedUnion dispatches on the explicit `by` field; implicit detection runs on the wire name (wireName ?? name) across the variants — evaluateField classifies each candidate wire-name as present-in-all, all-literal, all-string, value-unique. A present-in-all single-string-literal unique-value field qualifies; exactly one qualifier is the discriminator (no diagnostic); two or more fire loom/parse/ambiguous-discriminator (qualifying names in first-seen source order, comma-space joined); zero qualifiers resolve in precedence order — a string-shaped field with duplicate values fires loom/parse/duplicate-discriminator-value, else a present-in-all single-literal non-string field fires loom/parse/non-string-discriminator ("got <kind>"), else loom/parse/missing-discriminator. The explicit by-field path checks nested-value first (loom/parse/nested-discriminator, since a nested value's type/value cannot be read), then non-string literal (loom/parse/non-string-discriminator, the constraint applying equally to the explicit form), then duplicate value (loom/parse/duplicate-discriminator-value). checkByClause fires loom/parse/by-on-object-schema for the object body and undefined for the union form. detectTypeAliasCycles runs a recursion-stack DFS over the resolved reference graph (after schema-name resolution, before lowering): a back-edge to an on-stack node whose cycle slice is all-alias fires loom/parse/type-alias-cycle with the path joined by " → " and deduped per sorted node-set; a cycle traversing at least one object-schema hop crosses a $ref against $defs and raises nothing; dangling references are ignored. Parse-time literal values rendered bare-when-identifier-shaped else double-quoted per placeholder-rendering-b.md category 5. Pure functions — no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 7 V5b-T tests green; full suite 247 green, typecheck + lint clean.
2026-06-30 V5c-T — Descriptions (`///`) tests (paired V5c impl): the src/parser/descriptions.ts seam (joinDocComment(docLines) / extractDescription(commentLines) / lowerDescription(text, anchor, fragment) / checkDocCommentPlacement(production, site), inert stubs; DocCommentSite / DocAnchorKind = "schema"|"enum"|"field"|"variant"|"fn" shapes) plus tests/descriptions.test.ts with 4 failing tests pinning descriptions.md + grammar.md §`///` placement. §No transformation / §Placement: lowerDescription writes the joined text byte-for-byte into description: for the schema/enum/field/variant anchors (pre-existing fragment keys preserved), while a fn anchor lowers nowhere — the `///` is preserved AST-only and the lowered fragment carries no description. §Placement / grammar.md §`///` placement: loom/parse/doc-comment-misplaced ("'///' doc comment is not legal above this production") fires above let/import/export/expression/control-flow productions and raises none above the five eligible anchors (schema/enum/field/variant/fn). §Multi-line: joinDocComment joins consecutive `///` RestOfLine contents with \n, strips the common leading whitespace of non-blank lines (same algorithm as query-template dedent), and turns an empty `///` line into a blank line ("   first line", "", "   second line" → "first line\n\nsecond line"; single line strips its one-space prefix). §`//` is a regular code comment: extractDescription collects only the trailing `///` run, so a leading `//` line is not propagated (["// noise","/// line one","/// line two"] → "line one\nline two"; a block with no `///` line → undefined). Message anchored to code-registry-parse.md per the Diagnostic message anchors rule. Typecheck + lint clean; the 4 tests red on their own primary assertions (absent description / absent diagnostic / empty join) because the V5c lowering is absent — fail-red-for-the-intended-reason; the other 247 tests stay green.
2026-06-30 V5c — Descriptions (`///`) (paired with V5c-T): fills src/parser/descriptions.ts. joinDocComment joins the `///` RestOfLine contents with \n and strips the common leading whitespace of the non-blank lines (same algorithm as the query-template dedent QRY-7: common prefix drawn from {U+0020 space, U+0009 tab}; whitespace-only lines ignored for the common-prefix walk and normalised to an empty line). extractDescription walks the comment lines from the end, collecting the maximal trailing run of `///` lines via ^[ \t]*///(?!/)(.*)$ (the negative lookahead keeps a four-slash //// line out of the doc-comment run), so a regular // line terminates the run and is not propagated and a block with no `///` line yields undefined; the captured RestOfLine contents are passed to joinDocComment. lowerDescription writes the joined text byte-for-byte into description: for the schema/enum/field/variant anchors (pre-existing fragment keys preserved via spread) and returns the fragment unchanged for fn (functions have no JSON Schema — the description stays AST-only). checkDocCommentPlacement fires loom/parse/doc-comment-misplaced ("'///' doc comment is not legal above this production", severity error, located at the site) for any production outside the eligible set {schema, enum, field, variant, fn} and undefined for an eligible anchor. Message anchored to code-registry-parse.md per the Diagnostic message anchors rule. Pure functions — no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 4 V5c-T tests green; full suite 251 green, typecheck + lint clean.
2026-06-30 V5d-T — Schema-subset reject-gate tests (paired V5d impl): the src/parser/schema-subset-gate.ts seam (checkSubsetKeyword(keyword, site) → Diagnostic | undefined; checkSubsetKeywords(keywords, site) → Diagnostic[]; checkSchemaFeedingType(typeSource, site) → Diagnostic[]; all inert accept-all no-op stubs; SchemaSubsetSite shape) plus tests/schema-subset-gate.test.ts with 44 failing tests pinning schema-subset.md as an ALLOWLIST / reject-by-default gate. Per-keyword rejection: checkSubsetKeyword fires loom/parse/unsupported-feature (message anchored to the registry "unsupported syntactic feature: <construct>" template prefix, keyword as the construct) for each of the 30 enumerated rejected JSON-Schema keywords — composition oneOf/allOf/not/if/then/else plus the "Explicitly not supported … rejected at parse time" enumeration  — and accepts (returns undefined) the 10 permitted-subset keywords type/anyOf/enum/const/properties/required/additionalProperties/items/$defs/$ref. Reject-by-default: 9 out-of-subset keywords absent from BOTH the permitted set and the enumerated unsupported list (title/description/examples/$comment/$anchor/$dynamicRef/readOnly/writeOnly/deprecated) are still rejected with loom/parse/unsupported-feature, witnessing the allowlist and excluding a denylist build. Result-in-schema: checkSchemaFeedingType fires loom/parse/result-in-schema-position (message "'Result' has no lowered-schema form and is not permitted in a schema-feeding position" from code-registry-parse.md) for a bare Result<string, string> and for a Result reached through an array<T> element (transitive reachability + array element order preserved), and accepts a lowerable string | null. Array element order: checkSubsetKeywords returns one loom/parse/unsupported-feature per rejected keyword in input (array element) order across a mixed permitted/rejected sequence. Each accept arm is paired with a reject assertion in the same test so an accept-all stub cannot pass it vacuously. No new diagnostics registry codes (both asserted codes already in code-registry-parse.md). Typecheck + lint clean; the 44 tests red on their own primary assertions (an absent expected rejection / +0 ≠ 3 rejection count) — fail-red-for-the-intended-reason; the other 251 tests stay green.
2026-06-30 V5d — Schema-subset reject gate (paired with V5d-T): fills src/parser/schema-subset-gate.ts. checkSubsetKeyword(keyword, site) checks a candidate JSON-Schema keyword against the fixed loom-defined permitted subset (type/anyOf/enum/const/properties/required/additionalProperties/items/$defs/$ref, held as an immutable module-level ReadonlySet) and returns loom/parse/unsupported-feature (message "unsupported syntactic feature: <keyword>" anchored to code-registry-parse.md) for every keyword not in it — an allowlist / reject-by-default gate. This rejects both the enumerated-unsupported keywords (oneOf/allOf/not/if/then/else and pattern…nullable) and out-of-subset keywords absent from that enumeration (title/description/examples/$comment/$anchor/$dynamicRef/readOnly/writeOnly/deprecated), excluding a denylist build. checkSubsetKeywords(keywords, site) maps checkSubsetKeyword over a sequence, collecting one diagnostic per rejected keyword in input (array element) order (array-element-order preservation). checkSchemaFeedingType(typeSource, site) delegates to the V2a parseTypeExpression at the schema-feeding position, which walks the whole type tree (including array<T> element types and union arms) and raises loom/parse/result-in-schema-position ("'Result' has no lowered-schema form and is not permitted in a schema-feeding position", with the registry hint) for any reachable Result application, while a lowerable string | null raises none. The V5f lowering pass and canonical-hash recipe over the accepted subset are out of scope. No new diagnostics-registry codes (both asserted codes already in code-registry-parse.md). Pure functions — no module-level mutable state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 44 V5d-T tests green; full suite 295 green, typecheck + lint clean.
2026-06-30 V2b-T — Type-compatibility engine (`⊑`) tests (paired V2b impl): the src/parser/type-compat.ts seam (checkCompatible(sub, sup, env) → Compatibility = compatible/incompatible/integer-narrowing/unknown, inert stub returning the `unknown` sentinel; the per-site diagnostic seams checkLetRhsCompat / checkFnArgCompat / checkCommonType, inert no-ops returning []; the CompatType model — prim/literal/named/array/union/object — NamedDecl = object-schema (nominal, TYPE-10) / alias (transparent, TYPE-11), and TypeEnv) plus tests/type-compat.test.ts with 29 failing tests pinning type-system.md §"Type compatibility" TYPE-1…TYPE-11. TYPE-1 reflexivity (identical primitive + identical named schema); TYPE-2 integer⊑number one-way and the reverse classified integer-narrowing, plus the loom/parse/integer-narrowing site diagnostic ("cannot narrow number to integer"); TYPE-3 literal-to-primitive ("validation"⊑string, 42⊑integer/number, true⊑boolean, null⊑null); TYPE-4 variant-to-union; TYPE-5 union widening T⊑T|U; TYPE-6 union-distributive (each arm ⊑ T₃); TYPE-7 array covariance array<T₁>⊑array<T₂> iff T₁⊑T₂; TYPE-8 inline-object field-wise with exact field set (extra field never widens, additionalProperties:false); TYPE-9 per-site codes loom/parse/let-rhs-type-mismatch, loom/parse/fn-arg-type-mismatch, loom/parse/array-element-type-mismatch (branch vs in-scope sink) and loom/parse/array-no-common-type (no sink + no common type); TYPE-10 nominal object schemas (distinct named schemas, and named↔inline both directions, incompatible by shape); TYPE-11 alias transparency ("low"⊑Severity for the literal-union alias, StringOrNumber⊑string|number, and schema Y = Cat unfolding to the nominal Cat — incompatible with an inline object of the same shape, compatible reflexively against Cat). Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule; each test cites its TYPE-id / code inline. No new diagnostics-registry codes (all asserted codes — integer-narrowing, let-rhs-type-mismatch, fn-arg-type-mismatch, array-element-type-mismatch, array-no-common-type — already in code-registry-parse.md). Inert stub seam: no module-level mutable state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. Typecheck + lint clean; the 29 tests red on their own primary assertions (the `unknown` sentinel equals no expected outcome; each per-site seam returns no diagnostic) because the V2b engine is absent — fail-red-for-the-intended-reason; the other 295 tests stay green.
2026-06-30 V2b — Type-compatibility engine (`⊑`) (paired with V2b-T): fills src/parser/type-compat.ts. checkCompatible(sub, sup, env) decides the directed relation T₁ ⊑ T₂ over the CompatType model. unfoldAlias transparently replaces a named type whose declaration is a type-alias schema (schema X = R) with its RHS, recursing through nested aliases until a non-alias form (TYPE-11); a named resolving to an object schema stays named (nominal, TYPE-10) and an unresolvable named stays named so the relation reports "unknown" and the runtime AJV safety net applies (type-system.md §"Unresolvable operands"). The decide procedure over alias-unfolded operands: TYPE-6 union-distributive on the left (T₁|T₂ ⊑ T₃ iff each arm); TYPE-5 union-widening on the right (T ⊑ T|U iff T ⊑ some arm — an integer-narrowing arm does NOT make widening hold); TYPE-7 array covariance (array<T₁> ⊑ array<T₂> iff T₁ ⊑ T₂); TYPE-8 inline-object field-wise with exact field set (field counts must match, every super field found by name, each field-wise ⊑, order irrelevant, no excess-property widening); TYPE-10 nominal named object schemas (⊑ only the same name by identity, never an inline object nor a distinct named schema); and at the leaves decidePrimitive — TYPE-1 reflexivity, TYPE-2 one-way integer⊑number and the reverse number⊑integer classified integer-narrowing — with a literal typing as its typesAs primitive (TYPE-3). The three per-site checkers report TYPE-9 diagnostics: checkLetRhsCompat fires loom/parse/integer-narrowing ("cannot narrow number to integer") for the narrowing outcome and loom/parse/let-rhs-type-mismatch ("let binding '<name>' initialiser type mismatch: expected <expected>, got <actual>") for an incompatible RHS; checkFnArgCompat fires loom/parse/fn-arg-type-mismatch for any non-compatible argument; checkCommonType fires loom/parse/array-element-type-mismatch at the first branch failing an in-scope sink and loom/parse/array-no-common-type when no sink and no branch is a common type the others are all ⊑. A statically-unresolvable operand ("unknown") yields no per-site diagnostic, deferring to the runtime AJV safety net (Ships when). displayType renders the <expected>/<actual> interpolations. Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule; no new diagnostics-registry codes (all five asserted codes already present). Pure functions — no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 29 V2b-T tests green; full suite 324 green, typecheck + lint clean.
2026-06-30 V3a-T — Expression-evaluator tests (paired V3a impl): the src/runtime/expression-evaluator.ts seam (evaluateSource(source, host) → LoomValue, inert null-sentinel stub; the EvalHost collaborator resolveIdentifier/callFunction; checkBooleanPosition({position, operandType, site}) → Diagnostic[], inert [] stub; BooleanPosition = if/while/ternary-condition/&&/||) plus tests/expression-evaluator.test.ts with 21 failing tests pinning expressions.md (the EXPR code-keyed area) and the loom/parse/non-boolean-condition diagnostic. §Operator precedence (observed through evaluation): * tighter than + (2+3*4→14), parens override ((2+3)*4→20), +/- left-associative (2-3-4→-5), unary tighter than == (!true == false→true), ordering tighter than equality (2 < 3 == true→true), && tighter than || (true || false && false→true), ternary right-associative (false ? 1 : true ? 2 : 3→2), unary - tighter than * (-2*3→-6), and an identifier read resolved through the host in precedence context. §Evaluation order / short-circuiting: && / || evaluate the right operand's call iff the left selects it — the must-run and short-circuit cases are asserted together in one test so the inert stub (which never calls the host) reds on the must-run assertion — and a ternary evaluates only the taken branch's call. §Equality / §Ordering comparisons / §Other arithmetic: a cross-type == is false and != is true (no panic, no diagnostic), 42 == 42.0 is true (integer ⊑ number routes to value comparison), the mixed-+ widening value 1+2.5→3.5, / always produces number with 1/0→Infinity / -1/0→-Infinity / 0/0→NaN / 5%0→NaN and no panic, signed IEEE-754 ordering (-5<3) and every ordering operator against NaN evaluating false. §Truthiness: checkBooleanPosition fires loom/parse/non-boolean-condition (message "condition must be boolean; got <type>" anchored to code-registry-parse.md) for a non-boolean ternary-condition / && / if / while operand and raises none for a boolean operand. Each negative (no-effect / no-diagnostic) assertion is folded into a test whose must-run or firing assertion reds the stub, so no test passes vacuously on the inert stub. No new diagnostics-registry codes (loom/parse/non-boolean-condition already in code-registry-parse.md). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 21 tests red on their own primary assertions (the null sentinel equals no expected value; the short-circuited must-run call is never recorded; the boolean-position check returns no diagnostic) because the V3a interpreter and boolean-position check are absent — fail-red-for-the-intended-reason; the other 324 tests stay green.
2026-06-30 V3a — Expression evaluator (paired with V3a-T): fills src/runtime/expression-evaluator.ts. evaluateSource tokenizes (a self-contained scanner for number/string/boolean/null literals, identifiers, the call/paren punctuators, and the unary/binary/logical/ternary operators), parses by recursive descent over the expressions.md §"Operator precedence" table (highest→lowest: primary/call level 1, unary !/- level 2 right-assoc, * / % level 3 left, + - level 4 left, ordering < <= > >= level 5 non-assoc, equality == != level 6 non-assoc, && level 7 left, || level 8 left, ternary ?: level 9 right-assoc), and tree-walks with EvalHost — resolveIdentifier for identifier reads and callFunction for f(args). Evaluation is left-to-right with && / || short-circuit (the right operand evaluates only when the left selects it) and ternary branch-selection, so a short-circuited / not-taken operand's call never runs (§"Evaluation order and short-circuiting"). == / != route through the V2c valuesEqual structural relation (a cross-type pair is false / true with no panic; 42 == 42.0 is true via integer ⊑ number); + concatenates two strings else adds (integer ⊑ number implicit in the JS double); - * % numeric; / always produces number; div/mod-by-zero use the host's native IEEE-754 so 1/0→Infinity, -1/0→-Infinity, 0/0→NaN, 5%0→NaN with no panic (§"Other arithmetic"); ordering uses the host relational operators (signed numeric / UTF-16 code-unit, every ordering against NaN false) (§"Ordering comparisons"). checkBooleanPosition routes the operand type through the V2b ⊑ relation against boolean and fires loom/parse/non-boolean-condition ("condition must be boolean; got <type>", message anchored to code-registry-parse.md) for any non-boolean if/while/ternary-condition/&&/|| operand, returning none for a boolean operand and deferring a statically-unresolvable operand to the runtime safety net. Message anchored to code-registry-parse.md per the Diagnostic message anchors rule; no new diagnostics-registry codes. No module-level mutable state (the operator-set constants are immutable), no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 21 V3a-T tests green; full suite 345 green, typecheck + lint clean.
2026-06-30 V3b-T — Bindings-and-mutability tests (paired V3b impl): the src/parser/bindings.ts seam (checkLetBinding(decl, site) / checkReassignment(reassign, site) / checkAssignmentTarget(target, site) / checkMutModifier(mod, site) / checkIncrementDecrement(op, site), all inert stubs returning undefined; the BindingSite / LetBindingDecl {name, mutable, hasInitialiser} / BindingReassignment {name, mutable} / AssignmentTarget {kind} (AssignTargetKind = identifier/member/index) / MutModifier {position} (BindingPosition = let/fn-param/for-var/match-bind) / IncrementDecrementOp {op: "++"|"--"} shapes) plus tests/bindings.test.ts with 9 failing obligation assertions pinning bindings.md. loom/parse/immutable-rebinding ("cannot reassign immutable binding '<name>'") for a non-mut reassignment, while a let mut reassignment raises none (the valid-reassignment acceptance case of the V3b Ships-when gate); loom/parse/assignment-to-member-or-index ("cannot assign to member or index; mutability is binding-level only") for member (obj.field = …) and index (arr[i] = …) targets, while a plain identifier target raises none; loom/parse/mut-on-immutable-context ("'mut' is not permitted in this binding position") for a mut modifier on a function parameter, a for iteration variable, and a match pattern binding, while a let position raises none; loom/parse/increment-decrement ("'<op>' operator is not supported", <op> the source token verbatim) for ++ and --; loom/parse/let-without-initialiser ("let binding '<name>' has no initialiser") for an annotation-only initialiser-less let, while an initialised let raises none. Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule; each test cites its loom/parse/... code inline. No new diagnostics-registry codes (all five asserted codes already in code-registry-parse.md). Inert stub seam: no module-level mutable state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. Typecheck + lint clean; the 9 obligation tests red on their own primary assertions (an absent expected diagnostic; the seams return undefined) because the V3b binding checker is absent — fail-red-for-the-intended-reason; the 4 acceptance cases pass and the other 345 tests stay green.
2026-06-30 V3b — Bindings and mutability (paired with V3b-T): fills the five src/parser/bindings.ts checkers per bindings.md. checkLetBinding fires loom/parse/let-without-initialiser ("let binding '<name>' has no initialiser") for an annotation-only let x: T and accepts an initialised let (§`let` requires an initialiser). checkReassignment fires loom/parse/immutable-rebinding ("cannot reassign immutable binding '<name>'") for a non-mut target and accepts a let mut reassignment incl. the compound += forms (§`let` vs `let mut`). checkAssignmentTarget fires loom/parse/assignment-to-member-or-index ("cannot assign to member or index; mutability is binding-level only") for member (obj.field = …) and index (arr[i] = …) targets and accepts a plain identifier (§Mutability is binding-level only). checkMutModifier fires loom/parse/mut-on-immutable-context ("'mut' is not permitted in this binding position") for a mut modifier on a function parameter / for iteration variable / match pattern binding and accepts the let position (§Immutable contexts). checkIncrementDecrement always fires loom/parse/increment-decrement ("'<op>' operator is not supported", <op> the source token verbatim) for ++ and -- (§Increment / decrement). Messages and hints anchored to code-registry-parse.md per the Diagnostic message anchors rule; no new diagnostics-registry codes (all five asserted codes already present). Pure functions — no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 13 V3b-T tests green; full suite 358 green, typecheck + lint clean.
2026-06-30 V3c-T — Control-flow tests (paired V3c impl): the src/parser/control-flow.ts seam (checkForIterand(iterand, site) / checkBreakStatement(brk, site) / checkContinueStatement(cont, site), all inert stubs returning undefined; the ControlFlowSite / ForIterand {type: CompatType} / BreakStatement {insideLoop, hasValue} / ContinueStatement {insideLoop} shapes) and the src/runtime/control-flow.ts seam (evaluateForLoop(host) inert no-op; the ForLoopHost collaborator evaluateIterand() → readonly LoomValue[] / runIteration(element, index)) plus tests/control-flow.test.ts with 9 failing obligation assertions pinning control-flow.md. CTRL-1 (asserted against evaluateForLoop): the for iterand is evaluated exactly once at loop entry before the first iteration (iterand-eval counter must be 1, body runs once per snapshot element in order); the effect commits once even when the array is empty and the body is skipped (counter 1, zero iterations); a mid-body let mut reassignment does not alter the already-snapshotted sequence (the iterated sequence is the entry-time value). loom/parse/non-array-iterand ("'for' expects array<T> after 'in'; got <type>", type phase) for a string / number / object iterand, while an array<T> iterand raises none; loom/parse/break-outside-loop ("'break' outside of a loop", parse phase) for a break outside any loop, while a valueless in-loop break raises none; loom/parse/continue-outside-loop ("'continue' outside of a loop", parse phase) for a continue outside any loop, while an in-loop continue raises none; loom/parse/break-with-value ("'break' takes no value in loom 1.0", parse phase) for a break expr. Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule; each test cites its CTRL-1 / loom/parse/... code inline. No new diagnostics-registry codes (all four asserted codes already in code-registry-parse.md). Inert stub seams: no module-level mutable state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. Typecheck + lint clean; the 9 obligation tests red on their own primary assertions (an absent expected diagnostic; an iterand-evaluation count of 0 / no recorded iteration) because the V3c control-flow checker and loop evaluator are absent — fail-red-for-the-intended-reason; the 3 acceptance cases (array<T> iterand / in-loop break / in-loop continue) pass and the other 361 tests stay green.
2026-06-30 V3c — Control flow (paired with V3c-T): fills src/parser/control-flow.ts and src/runtime/control-flow.ts per control-flow.md. checkForIterand fires loom/parse/non-array-iterand ("'for' expects array<T> after 'in'; got <type>", <type> rendered via the now-exported V2b displayType, type phase) for any iterand whose static type is not array<T> (iterating a string / number / object) and raises none for an array<T> iterand. checkBreakStatement fires loom/parse/break-outside-loop ("'break' outside of a loop") for a break outside any loop, otherwise loom/parse/break-with-value ("'break' takes no value in loom 1.0") for a break expr, accepting a valueless in-loop break. checkContinueStatement fires loom/parse/continue-outside-loop ("'continue' outside of a loop") for a continue outside any loop, accepting an in-loop continue. evaluateForLoop implements CTRL-1: call host.evaluateIterand exactly once at loop entry, capture the array<T> snapshot before iterating, then call host.runIteration once per snapshot element in order — so the iterand effect commits exactly once even when the array is empty (the body never runs) and a body-side let mut reassignment of the iterand source cannot reach the captured snapshot reference. Messages anchored to code-registry-parse.md per the Diagnostic message anchors rule; no new diagnostics-registry codes (all four asserted codes already present). displayType exported from type-compat.ts for reuse by the iterand-type message. Pure functions plus a small bounded loop — no module-level mutable state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 12 V3c-T tests green; full suite 370 green, typecheck + lint clean.
2026-06-30 V3f-T — Expression stdlib `string` member tests (paired V3f impl): the src/runtime/stdlib-string.ts seam (evaluateStringMember(receiver, member, args) → LoomValue, inert null-sentinel stub; concatElementType(left, right, env) → CompatType, inert null-primitive stub) plus tests/expression-stdlib-string.test.ts with 17 failing tests pinning expressions.md §"Built-in methods and properties" (the EXPR code-keyed area). The five normative String.replace reference vectors reproduce exactly — all-occurrences + literal $& ("aXbXc".replace("X","[$&]")→"a[$&]b[$&]c"), literal $$ ("100".replace("0","$$")→"1$$$$"), literal $n ("a-b".replace("-","x$1y")→"ax$1yb"), empty from returns receiver unchanged ("abc".replace("","X")→"abc"), and the left-to-right non-overlapping scan ("aaaaa".replace("aa","x")→"xxa"). array<T>.concat LUB element type: integer ⊔ number = number in both call directions, identical element types collapse (TYPE-1), disjoint element types union receiver-first (integer | string). The string members reproduce their normative behaviour and return type: length is the UTF-16 code-unit count ("a💩".length→3, distinguishing it from a code-point count of 2), toLowerCase/toUpperCase/trim are the locale-independent transforms (full-case "ß".toUpperCase()→"SS"; NBSP stripped from both ends by trim), startsWith/endsWith/includes return boolean (typeof asserted), and split returns array<string> with the empty separator decomposing into one string per UTF-16 code unit ("a💩".split("")→["a","\ud83d","\udca9"], 3 elements). No new diagnostics-registry codes (the EXPR stdlib members assert pure behaviour / return type, no diagnostic assertions). Inert stub seam: no module-level mutable state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. Typecheck + lint clean; the 17 tests red on their own primary assertions (the null sentinel equals no expected value; the null-prim sentinel equals no expected type) because the V3f stdlib surface is absent — fail-red-for-the-intended-reason; the other 370 tests stay green.
2026-06-30 V3f — Expression stdlib `string` members (paired with V3f-T): fills src/runtime/stdlib-string.ts. evaluateStringMember(receiver, member, args) dispatches the loom-1.0 string stdlib table of expressions.md §"Built-in methods and properties" (the EXPR code-keyed area): length is the UTF-16 code-unit count (receiver.length; no grapheme/code-point segmentation), toLowerCase/toUpperCase/trim are the locale-independent host transforms ("ß".toUpperCase()→"SS", NBSP trimmed both ends), startsWith/endsWith/includes return boolean with JS semantics, and split(sep) returns array<string> (literal-only) with the empty separator decomposing into one string per UTF-16 code unit ("a💩".split("")→["a","\ud83d","\udca9"]). replace(from,to) is implemented by replaceLiteral — a single left-to-right, non-overlapping indexOf scan inserting `to` literally (NOT host String.prototype.replaceAll, whose string form interprets $&/$$/$n), empty from returns the receiver unchanged; reproduces the five normative reference vectors exactly ("aXbXc".replace("X","[$&]")→"a[$&]b[$&]c", "100".replace("0","$$")→"1$$$$", "a-b".replace("-","x$1y")→"ax$1yb", "abc".replace("","X")→"abc", "aaaaa".replace("aa","x")→"xxa"). concatElementType(left,right,env) computes the array<T>.concat LUB element type via the V2b checkCompatible ⊑ relation: if one element type is ⊑ the other the wider one is the LUB (collapsing identical types and applying integer ⊑ number in both directions), else disjoint types union receiver-first (integer | string). Pure functions plus one bounded scan loop — no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. All 17 V3f-T tests green; full suite 387 green, typecheck + lint clean.
2026-06-30 V3g-T — Expression stdlib array<T> member tests (paired V3g impl): the src/runtime/stdlib-array.ts seam (evaluateArrayMember(receiver, member, args) → LoomValue, inert null-sentinel stub; checkArrayJoin(elementType, site) → Diagnostic | undefined, inert undefined stub) plus tests/expression-stdlib-array.test.ts with 8 failing tests pinning expressions.md §"Built-in methods and properties" (the EXPR code-keyed area), array<T> table. length is the element count ([1,2,3]→3, []→0); join(sep) concatenates string elements (["a","b","c"].join(", ")→"a, b, c", single→element, empty→""), and checkArrayJoin fires loom/parse/non-string-array-join (message "array.join requires a string element type; got array<integer>", anchored to code-registry-parse.md) for a non-string element type while a string element type raises none; includes(x)/indexOf(x) use the V2c valuesEqual loom structural equality (a reference-distinct but structurally-equal object matches), with indexOf returning the first index and -1 when absent; slice(start, end?) follows JS semantics — exclusive end (slice(1,3)→[1,2]), negative-index-from-end (slice(-2)→[3,4], slice(1,-1)→[1,2,3]), omitted end slices to length (slice(2)→[2,3,4]). The join accept arm is folded with its firing assertion so the inert undefined stub cannot pass it vacuously. array<T>.concat's LUB element type is owned by V3f-T and not re-declared here. No new diagnostics-registry codes (loom/parse/non-string-array-join already in code-registry-parse.md). Inert stub seam: no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call. Typecheck + lint clean; the 8 tests red on their own primary assertions (the null sentinel equals no expected value; the undefined join-check equals no expected diagnostic) because the V3g array<T> stdlib surface is absent — fail-red-for-the-intended-reason; the other 387 tests stay green.
2026-06-30 V3g — Expression stdlib array<T> members (paired with V3g-T): fills src/runtime/stdlib-array.ts. evaluateArrayMember(receiver, member, args) dispatches the loom-1.0 array<T> stdlib table of expressions.md §"Built-in methods and properties" (the EXPR code-keyed area): length is the element count (receiver.length), join(sep) concatenates the (string) elements (receiver.join(sep); the parse-time precondition guarantees a string element type), includes(x)/indexOf(x) use the V2c valuesEqual loom structural equality via some/findIndex (a reference-distinct but structurally-equal object matches; indexOf returns the first index or -1), and slice(start, end?) delegates to Array.prototype.slice for JS semantics (negative-index-from-end, exclusive end, omitted end→length). checkArrayJoin(elementType, site) returns loom/parse/non-string-array-join (message anchored to code-registry-parse.md, <element> rendered via displayType) unless the element type is string (a string-typed literal element is equally admissible). Pure dispatch — no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call; not wired into the V3a evaluator (no test exercises it, matching the V3f parity). All 8 V3g-T tests green; full suite 395 green, typecheck + lint clean.
2026-06-30 V3h-T — Expression stdlib `object` member tests (paired V3h impl): the src/runtime/stdlib-object.ts seam (evaluateObjectMember(receiver, member, args) → LoomValue, inert null-sentinel stub) plus tests/expression-stdlib-object.test.ts with 4 failing tests pinning expressions.md §"Built-in methods and properties" (the EXPR code-keyed area), object table. keys() returns loom-side field names as array<string> in schema declaration order for named schemas ({name,role,experience_years}→["name","role","experience_years"]) and insertion order otherwise ({z,a,m}→["z","a","m"]), empty object→[]; values() returns the field values as a heterogeneous array<T> in the same order as keys() (→["Ada","reviewer",7]), empty object→[]; has(k) returns true for a present loom-side name, false for an unknown key without panic, and false for an inherited prototype name (toString) — the safe-check is over the object's own loom-side names. No new diagnostics-registry codes (the object stdlib members assert pure behaviour / return type, no diagnostic assertions). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 4 tests red on their own primary assertions (the null sentinel equals no expected value) because the V3h object stdlib surface is absent — fail-red-for-the-intended-reason; the other 395 tests stay green.
2026-06-30 V3h — Expression stdlib object members (paired with V3h-T): fills src/runtime/stdlib-object.ts. evaluateObjectMember(receiver, member, args) dispatches the loom-1.0 object stdlib table of expressions.md §"Built-in methods and properties" (the EXPR code-keyed area): keys() returns the loom-side field names as array<string> via Object.keys (the object value's own key order — schema declaration order for named schemas, insertion order otherwise; both reduce to own-key order at runtime), values() returns the field values as a heterogeneous array<T> via Object.values in the same order as keys(), and has(k) tests own loom-side names only via Object.prototype.hasOwnProperty.call (not the JS prototype chain, so toString reports absent), returning false for an unknown key without panic. Pure dispatch — no module-level state, no broad catch, no ambient primitive, no Promise combinator, no Sync call; not wired into the V3a evaluator (no test exercises it, matching the V3f/V3g parity). All 4 V3h-T tests green; full suite 399 green, typecheck + lint clean.
2026-06-30 V4d-T — QueryError variant schema tests (paired V4d impl): the src/runtime/query-error.ts seam — the nine-variant QueryError union and its ValidationIssue / ValidationError shapes (kind typed string on every arm, the ERR-15 type-openness seam), plus four inert stubs (orderValidationIssues identity copy; loom10QueryErrorKinds()→[] / isLoom10QueryErrorKind→false; synthesizeForcedRespondIssue→sentinel issue; makeToolLoopExhaustedError→wrong-kind / rounds:-1) — plus tests/queryerror-variants.test.ts with 13 tests pinning queryerror-variants.md. ERR-14: orderValidationIssues sorts ascending on the tuple (path, schema_keyword, message), [0] is the canonically-first issue, the sort is stable for equal-key entries, and comparison is by Unicode code point not UTF-16 code unit (U+FFFF before U+1F600). ERR-15: the runtime conformance set is exactly the nine wire tags and isLoom10QueryErrorKind rejects a future "binder" tag, while the type-level witness const k: QueryError["kind"] = "binder" compiles (the open seam). ERR-17: the forced-respond non-compliance synthesised ValidationIssue (path "", schema_keyword "required", plain-text vs wrong-tool two-arm message). ERR-19: the ToolLoopExhaustedError shape (kind "tool_loop_exhausted", rounds == max_rounds, last_tool_name incl. the null forward-compat branch, raw_response incl. the pure-tool-use null branch). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 12 tests red on their own primary assertions (unsorted identity output; empty conformance set; sentinel issue; wrong exhaustion shape) because the V4d behaviour is absent — fail-red-for-the-intended-reason; the one passing test is the static type-openness witness; the other 400 tests stay green.
2026-06-30 V4d — QueryError variant schema (paired with V4d-T): fills the four behaviour-bearing helpers in src/runtime/query-error.ts. ERR-14 orderValidationIssues returns a stable ascending sort keyed on (path, schema_keyword, message), each field compared by Unicode code point via an iterator-based compareByCodePoint (U+FFFF before astral U+1F600, where a UTF-16 code-unit < would invert), stabilised by a captured input index. ERR-15 loom10QueryErrorKinds() returns the nine wire tags in union-declaration order (an `as const` immutable module constant the no-globals scan admits) and isLoom10QueryErrorKind is the closed-set membership predicate, while kind stays typed string at the type level. ERR-17 synthesizeForcedRespondIssue emits the synthesised ValidationIssue (path "", schema_keyword "required", plain-text vs wrong-tool two-arm message). ERR-19 makeToolLoopExhaustedError builds the tool_loop_exhausted error with rounds == maxRounds. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 13 V4d-T tests green; full suite 412 green, typecheck + lint clean.
2026-06-30 V4a-T — `match`, `?`, and `Result` tests (paired V4a impl): the parse/type seam src/parser/match-result.ts (checkQuestionOperand / checkQuestionScope / checkMatchArmTypes — inert stubs returning no diagnostic / no LUB) and the runtime seam src/runtime/match-result.ts (the Pattern model, MatchArm, the MatchError panic class carrying loom/runtime/match-error, and evaluateMatch — an inert null-sentinel stub that matches no arm and raises no panic), plus tests/match-result.test.ts with 16 tests pinning errors-and-results/error-model.md and its expressions.md / type-system.md closure. ERR-18: a ? on a non-Result<_, QueryError> operand fires loom/parse/question-on-non-result (type phase). ? outside a Result-compatible scope fires loom/parse/question-outside-result-fn (inferred and Result-compatible scopes pass). loom/parse/match-arm-type-mismatch: arms sharing no common upper bound fire the diagnostic, and a well-typed match resolves to the LUB of its arms (integer ⊔ number = number). loom/runtime/match-error: a scrutinee matching none of the arms raises MatchError, while each of the six pattern forms (wildcard/identifier/literal/constructor/object-schema/array) binds and evaluates the selected arm — the panic's ?/match bypass and registered message template are deferred to V4b-T. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 13 tests red on their own primary assertions (absent diagnostic, absent LUB, unthrown MatchError, null sentinel) because the V4a behaviour is absent — fail-red-for-the-intended-reason; the 3 negative-case assertions pass; the other 415 tests stay green.
2026-06-30 V4a — `match`, `?`, and `Result` (paired with V4a-T): fills src/parser/match-result.ts and src/runtime/match-result.ts. checkQuestionOperand (ERR-18) returns loom/parse/question-on-non-result for a non-Result<_, QueryError> operand (a Result whose error arm is not QueryError renders the <type> placeholder as Result), undefined for Result<T, QueryError>. checkQuestionScope returns loom/parse/question-outside-result-fn for an annotated non-Result-compatible scope; inferred and Result-compatible annotated scopes pass. checkMatchArmTypes reports loom/parse/match-arm-type-mismatch when the arms share no common upper bound under type-system.md §"Type compatibility" (via checkCompatible), otherwise resolves the match to the least upper bound of its arms (integer ⊔ number = number); a sink narrows every arm to and resolves the match to the sink. evaluateMatch dispatches the scrutinee against arms in order (first match wins, non-selected bodies never evaluated), recursively matching the six pattern forms (wildcard/identifier/literal via valuesEqual/constructor over Ok·Err Result payloads/object-schema with unlisted-field tolerance/exact-length array) into a fresh per-arm bindings map, and raises MatchError (loom/runtime/match-error) when no arm matches (loom 1.0 performs no static exhaustiveness check). The ?/match panic bypass and the registered loom/runtime/match-error message template are deferred to V4b. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 16 V4a-T tests green; full suite 428 green, typecheck + lint clean.
2026-06-30 V3d-T — Functions and return tests (paired V3d impl): the parse/type seam src/parser/functions.ts (checkFnPlacement / checkFunctionReference / resolveFnCall→"unchecked" / buildFnDeclaration dropping the doc / lowerFnDescription wrongly carrying it / resolveReturnType→"unchecked" / checkBareReturn / checkUnreachableCode — inert stubs) and the runtime seam src/runtime/function-result.ts (functionResult→unimplemented sentinel for every outcome; discardForVoid not discarding), plus tests/functions-and-return.test.ts with 24 tests pinning functions.md and return.md. FN-1: a nested fn fires loom/parse/nested-fn, a function-as-value fires loom/parse/function-as-value, hoisted mutual recursion resolves. FN-2: the /// doc is preserved on the AST and does not lower into the provider payload (functions have no JSON Schema). FN-3/RET-1: an annotation-less body infers the LUB of its tail/return operands (integer ⊔ number = number), wraps in Result<T, QueryError> on a ?/Result contribution, fires loom/parse/return-no-common-type for operands sharing no common upper bound, and an explicit return annotation type-checks each operand against the annotation instead of inferring. FN-4: an empty-tail body infers null, a ?-bearing empty-tail body infers Result<null, QueryError>, and a void function discards its tail value (produces null). RET-2/RET-3: a bare return fires loom/parse/bare-return-in-non-void outside a void function (including at the top level); code after a return warns loom/parse/unreachable-code (warning severity). FN-5: the function-result seam exposes the body's produced value on the success path and reports it absent on fail/cancel. No new diagnostics-registry codes (all five cited parse codes already in code-registry-parse.md). Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 20 tests red on their own primary assertions (absent diagnostic, the "unchecked" sentinel, a missing AST doc, a wrongly lowered description, the unimplemented final-value sentinel, the un-discarded value) because the V3d behaviour is absent — fail-red-for-the-intended-reason; the 4 negative-case assertions pass; the other 432 tests stay green.
2026-06-30 V3d — Functions and return (paired with V3d-T): fills the parse/type seam src/parser/functions.ts and the runtime seam src/runtime/function-result.ts. FN-1: checkFnPlacement returns loom/parse/nested-fn for a nested fn, checkFunctionReference returns loom/parse/function-as-value for a function used outside call position, and resolveFnCall resolves a name against the hoisted top-level fn set (resolved/unresolved), so hoisted mutual recursion resolves regardless of order. FN-2: buildFnDeclaration preserves a /// doc comment on the AST node and lowerFnDescription returns the empty fragment (functions have no JSON Schema, so the doc never lowers into a provider payload). FN-3/RET-1: resolveReturnType infers an annotation-less body's return type as the LUB (under ⊑, via checkCompatible) of its tail/return operand types (integer ⊔ number = number), wraps in Result<T, QueryError> when the body bears ? or any Result-typed contribution (reconciling success payloads), fires loom/parse/return-no-common-type when operands share no common upper bound, and type-checks each operand against an explicit annotation instead of inferring. FN-4: an empty-tail body infers the null literal type, a ?-bearing empty-tail body infers Result<null, QueryError>, and discardForVoid discards a void function's tail value (produces null). RET-2/RET-3: checkBareReturn returns loom/parse/bare-return-in-non-void outside a void function (incl. top-level), checkUnreachableCode warns loom/parse/unreachable-code (warning severity) for code after a return. FN-5: functionResult exposes the body's produced value on success and reports it absent on fail/cancel. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 24 V3d-T tests green; full suite 452 green, typecheck + lint clean.
2026-06-30 V4b-T — Runtime-panics tests (paired V4b impl): the new src/runtime/runtime-panics.ts seam — the LoomPanic base + five panic classes (IndexOutOfBoundsPanic/MissingObjectKeyPanic/NullIndexAccessPanic/NullMemberAccessPanic/InvokeDepthExceededPanic), the accessor seams evaluateIndexAccess/evaluateMemberAccess/enterInvokeFrame (inert: raise nothing), the ?-propagation seam evaluateQuestion (inert: returns a value sentinel without invoking the operand), the HostFatal NOCEIL-3 marker, and surfaceUnexpectedThrow (inert: returns a wrong-code sentinel diagnostic for every input) — plus tests/runtime-panics.test.ts with 23 tests pinning errors-and-results/error-model.md §"Runtime panics" (closure into invocation.md §"Invocation depth bound" and hard-ceilings/ceiling-invariants-and-audit.md §"No additional ceilings" for NOCEIL-3). The six closed panic sources (loom/runtime/index-out-of-bounds/missing-object-key/null-index-access/null-member-access/match-error/invoke-depth-exceeded) each emit their registered message template (sourced from the diagnostics registry via registryMessage, placeholders filled — e.g. index out of bounds: 5 not in 0..3, MatchError: no arm matched 5, invoke chain depth exceeded: 33 > 32) and bypass ? (raised in a ? operand, propagates past evaluateQuestion) and match (raised in a match arm body, escapes evaluateMatch; match-error's own bypass-of-match is intrinsic and V4a-covered, so it is exercised for ?-bypass only). An unexpected thrown value (TypeError, the catchable RangeError allocation family) surfaces as loom/runtime/internal-error (internal error: <error.message>, hint = error.stack); an already-classified LoomPanic is not reclassified; the NOCEIL-3 uncatchable host-fatal (modelled by HostFatal) emits no diagnostic at all. No new diagnostics-registry codes (all seven cited codes already in code-registry-runtime.md). Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 23 tests red on their own primary assertions because the V4b panic surface is absent — fail-red-for-the-intended-reason; the other 452 tests stay green.
2026-06-30 V4b — Runtime panics (paired with V4b-T): fills the behaviour-bearing seams of src/runtime/runtime-panics.ts and closes the match-error message template in src/runtime/match-result.ts. The six closed panic sources each raise their panic carrying the registered message template (diagnostics/code-registry-runtime.md): evaluateIndexAccess raises IndexOutOfBoundsPanic (index out of bounds: <i> not in 0..<length>) on an out-of-range array index, MissingObjectKeyPanic (missing object key: <key>) on an absent object key, NullIndexAccessPanic (null index access: [<i>]) on a null target; evaluateMemberAccess raises NullMemberAccessPanic (null member access: .<field>) on a null target; enterInvokeFrame raises InvokeDepthExceededPanic (invoke chain depth exceeded: <depth> > 32) past the depth-32 cap; evaluateMatch raises MatchError (MatchError: no arm matched <scrutinee summary>) — MatchError now extends LoomPanic so the runtime-defect surface recognises it as a closed panic source, and the scrutinee summary renders per the category-2 runtime-value rule (decimal numbers, truncated strings, Ok/Err, compact JSON objects/arrays, bare enum wire). Panics bypass ? (evaluateQuestion invokes the operand outside any catch, so a thrown panic propagates) and match (a panic is thrown, never an arm value); evaluateQuestion applies ? propagation Ok(v)->value / Err(e)->propagate. surfaceUnexpectedThrow routes an unexpected throw to loom/runtime/internal-error (internal error: <error.message>, hint=error.stack or "<no stack available>"), passes a LoomPanic through unreclassified (undefined), and emits no diagnostic for a NOCEIL-3 HostFatal. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 23 V4b-T tests green; full suite 475 green, typecheck + lint clean.
2026-06-30 V5f-T — Schema-lowering and canonical-hash tests (paired V5f impl): the new src/parser/schema-lowering.ts seam — canonicalForm/canonicalHash/schemaSlug over the typed LoweredJsonValue domain (inert: return ""), lowerUnion (inert: { anyOf: [] }), buildSidecar (inert: empty maps), dedupInlineSchemas (inert: dedups nothing, raises no diagnostic), plus SlugKeyedFragment/SidecarFieldInput/SchemaSidecar/LoweredUnionArm shapes — plus tests/schema-lowering-hash.test.ts with 12 tests pinning schema-subset.md. Canonical schema hash: the canonical form sorts object keys by code point, emits no whitespace, binder-number-renders numeric const/enum literals (1e21→1000000000000000000000, 42.0→42); the slug is the first 16 lowercase hex of SHA-256 over the canonical-form bytes (digest computed in-test via node:crypto). SUBS-1: an all-primitive union (null counted as a primitive) lowers to { type: [...] } in source order with "null" last; any non-primitive arm forces { anyOf: [...] }. Lowering Algorithm step 5: the sidecar captures a wire-name translation map (renamed fields only) and a named-enum-position map keyed by JSON Pointer (present iff the source type was a named enum; anonymous string-literal-union positions absent). loom/load/schema-slug-collision (message anchored to code-registry-load.md): two non-byte-identical fragments sharing a slug fire; byte-identical ones dedup silently to one $defs entry. Each accept/silent arm is paired so it cannot pass vacuously against the inert stub. No new diagnostics-registry codes (loom/load/schema-slug-collision already present). Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 12 tests red on their own primary assertions because the V5f lowering/hash bodies are absent — fail-red-for-the-intended-reason; the other 475 tests stay green.
2026-06-30 V5f — Schema lowering and canonical hash (paired with V5f-T): fills the behaviour-bearing bodies of src/parser/schema-lowering.ts. Canonical schema hash: canonicalForm serialises a LoweredJsonValue to the deterministic UTF-8 JSON form — object keys sorted by Unicode code-point (a dedicated code-point comparator, not UTF-16-unit <), no insignificant whitespace, numeric const/enum literals delegated to the V2d renderCanonicalNumber keyed off the declared integer/number kind (never runtime integrality), array elements left in lowering order, strings RFC 8259 minimal-escaped via JSON.stringify; canonicalHash is SHA-256 (node:crypto createHash) over those bytes; schemaSlug is the first 16 lowercase hex. SUBS-1: lowerUnion lowers an all-primitive union (with null counted a primitive) to { type: [...] } in source order with "null" last, and any non-primitive arm to { anyOf: [...] } in source order (each primitive arm to { type: <name> }, each non-primitive arm carrying its lowered fragment). Lowering Algorithm step 5: buildSidecar builds the wire-name translation map (renamed fields only) and the named-enum-position map keyed by JSON Pointer (present iff the source type was a named enum; anonymous string-literal-union positions absent). dedupInlineSchemas groups by slug, byte-compares canonical-form bytes on a slug match, dedups byte-identical fragments silently to one $defs entry, and raises loom/load/schema-slug-collision (message anchored to code-registry-load.md) refusing to merge on a byte-mismatch. No new diagnostics-registry codes. Pure functions — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 12 V5f-T tests green; full suite 487 green, typecheck + lint clean.
2026-06-30 V2e-T — Wire-name translation boundary tests (paired V2e impl): the new src/runtime/wire-translation.ts seam — translateInbound / translateOutbound over InboundTranslationInput / OutboundTranslationInput (validated/value + per-$defs SchemaSidecar map + root $defs name), both inert identity-passthrough stubs — plus tests/wire-name-translation.test.ts with 6 tests pinning runtime-value-model.md §"Wire-name translation" (RVM code-keyed area) and schemas.md §"Wire-name renaming". Inbound (model output → loom value, post-AJV): rebuilds loom-side names from the V5f wire-name sidecar so loom code never sees wire names (top-level and one level nested via $ref recursion through the per-$defs sidecar map); reattaches the declaring-enum tag at a named-enum position so the result compares equal to a V2c makeEnumValue variant while JSON.stringify still yields the bare wire string; a named-enum position is tagged while an anonymous string-literal-union position (absent from the sidecar) stays a plain string (Severity.Low == "low" remains false). Defaults bypass inbound translation: a frontmatter default authored as Severity.High arrives already branded and loom-side-named, equal to the inbound-translated model output without passing through translateInbound. Outbound (loom value → JSON): rewrites loom-side names to wire names before AJV validation. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 6 tests red on their own primary assertions (wire key survives identity inbound, enum tag not reattached, named/anonymous positions not discriminated, default not equal to untranslated stub output, loom key survives identity outbound) because the V2e translation pass is absent — fail-red-for-the-intended-reason; the other 487 tests stay green.
2026-06-30 V2e — Wire-name translation boundary (paired with V2e-T): fills src/runtime/wire-translation.ts. Inbound translateInbound/rebuildInbound walks AJV-validated JSON, renames keys wire→loom from the V5f SchemaSidecar.wireNames map (loom code never sees wire names at any depth), reattaches the V2c declaring-enum tag at each named-enum position (namedEnumPositions /properties/<wire> pointers map to the data field) so the result compares equal to a locally constructed makeEnumValue variant while JSON.stringify yields the bare wire string, and leaves anonymous string-literal-union positions (absent from the sidecar) plain. Outbound translateOutbound/lowerOutbound renames loom→wire and collapses an enum boxed-String to its bare wire string. Both recurse through arrays and nested objects. Divergence (logged in notes.md/decisions.jsonl): the V5f sidecar carries no per-field $ref target, so nested-object recursion resolves the nested $defs sidecar by matching the field's wire name against a $defs key — faithful for the fixtures, not for the general field: NamedSchema case. Defaults bypass inbound translation. No new diagnostics-registry codes. Pure functions — no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. All 6 V2e-T tests green; full suite 493 green, typecheck + lint clean.
2026-06-30 V6a-T — Frontmatter field-contract tests (paired V6a impl): the new src/parser/frontmatter.ts seam — parseFrontmatter(source, { file, modelMatcher }) (inert stub returning { registered: false, diagnostics: [] }), the model-reference-matcher injection seam V6a defines (ModelReferenceMatcher.resolve(reference) → "resolved" | "no-match" | "ambiguous", the interface the parser's model: resolution hook calls; the concrete matcher binds V11a's exact-match resolver over ctx.modelRegistry.getAvailable() and is injected by V9b), plus the ParsedFrontmatter/FrontmatterParseResult/ParseFrontmatterOptions/LoomMode/ModelMatchOutcome shapes — plus tests/frontmatter-contract.test.ts with 8 tests pinning frontmatter/frontmatter-fields-a.md §Field contract. loom/load/missing-mode: frontmatter omitting mode: fires the load error (message anchored to code-registry-load.md) and the loom is not registered, a valid mode: subagent resolves and registers. loom/load/unknown-frontmatter-field (W): an unknown top-level key warns and the loom still loads (tolerated). loom/parse/timeout-field-rejected (NOCEIL-1 seam): a timeout: field is rejected (message anchored to code-registry-parse.md). loom/load/model-unresolved: a present model: resolving to no available model (non-string scalar, provider/modelId no-match driven through the injection seam asserting the hook hands the matcher the raw reference, provider-ambiguous bare modelId) fails the load and the loom is not registered, while a resolving reference registers; the no-match message anchored to code-registry-load.md with the unquoted-YAML-scalar <value> rendering. No new diagnostics-registry codes. Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 8 tests red on their own primary assertions (absent diagnostic, un-registered loom, undefined parsed field, un-consulted matcher) because the V6a frontmatter parser is absent — fail-red-for-the-intended-reason; the other 493 tests stay green.
2026-06-30 V6a — Frontmatter field contract (paired with V6a-T): fills src/parser/frontmatter.ts. parseFrontmatter(source, { file, modelMatcher }) extracts the leading ---fenced YAML block and parses it via the yaml dependency (parseDocument + LineCounter for best-effort located diagnostics), recognises the loom 1.0 field vocabulary (description/argument-hint/mode/model/bind_model/bind_context/bind_echo/tools/system/respond_repair/tool_loop/params), and applies the field contract from frontmatter-fields-a.md §Field contract: required mode: → loom/load/missing-mode (E, message anchored to code-registry-load.md), loom not registered; any unknown top-level key → loom/load/unknown-frontmatter-field (W, one per key), loom still registers (forward-compat seam); timeout: → loom/parse/timeout-field-rejected (E, NOCEIL-1 seam, message anchored to code-registry-parse.md); a present model: resolved through the injected ModelReferenceMatcher.resolve(raw) seam V6a defines → loom/load/model-unresolved (E) on no-match/ambiguous with the <value> placeholder rendered as the unquoted YAML scalar (placeholder-rendering-b.md category 5), loom not registered; a resolving reference registers and surfaces the model string. Registration = no error-severity diagnostic; ParsedFrontmatter (mode, optional model) returned only when registered. No new diagnostics-registry codes (all four cited codes already present). Module-level vocabulary set is immutable readonly — no mutable cross-invocation state, no broad catch / ambient primitive / Promise combinator / Sync call. All 8 V6a-T tests green; full suite 501 green, typecheck + lint clean.
2026-06-30 V8c-T — SchemaValidator seam tests (paired V8c impl): the production AjvSchemaValidator stub in src/seams/schema-validator.ts (constructor { emit, slugOf } — an injected Diagnostic sink for loom/runtime/validator-cache-collision and a SchemaSlugFn content-addressing seam returning { slug, canonicalBytes }; the stub hands back an inert validator rejecting every value with one sentinel error, caching nothing and emitting nothing) plus tests/schema-validator-seam.test.ts with 8 tests pinning PIC-11 (host-interfaces-services.md). One-pass multi-error reporting (no fast-fail — both /a and /b type errors surface); NO coercion (a numeric string fails the type check and is left unchanged on the input); NO default-filling (an optional default-carrying property stays absent on the validated object); in-document $ref resolution (a #/$defs/Name ref validates/rejects); silent unknown-format acceptance; determinism across two compilations; and the slug-cache byte-verify: a byte-identical cache hit serves with no collision diagnostic, while two distinct documents forced (via an injected fixed-slug slugOf) to share one slug fire loom/runtime/validator-cache-collision (E, message anchored to code-registry-runtime.md: validator-cache collision on slug <slug>: two distinct schema documents hash alike, hint carrying both canonical-byte forms) and recompile the new document instead of serving the wrong cached validator. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 8 tests red on their own primary assertions (sentinel-error rejection, no recorded error paths, absent collision diagnostic) because the V8c validator is absent — fail-red-for-the-intended-reason; the other 501 tests stay green.
2026-06-30 V8c — SchemaValidator seam (paired with V8c-T): fills src/seams/schema-validator.ts. AjvSchemaValidator (constructor { emit, slugOf }) compiles each lowered schema through a per-instance Ajv({ strict: false, allErrors: true, logger: false }) with ajv-formats registered as an instance-level side effect, satisfying PIC-11: allErrors:true gives one-pass multi-error reporting (no fast-fail); the absence of coerceTypes gives no value conversion (a numeric string fails the type check, input left as-is); the absence of useDefaults gives no default-filling (an optional default-carrying property stays absent); AJV's default in-document $ref resolver gives the #/$defs ref scope; strict:false + ajv-formats + logger:false give silent unknown-format acceptance; deterministic per (schema, value). The per-query compiled-validator cache (Map keyed by slugOf(schema).slug, storing the compiled validator alongside its canonical bytes) byte-compares a candidate's canonical bytes against the cached document's bytes on a cache hit (a comparison, not a re-serialisation) and serves the cached validator on a match; on a byte mismatch (a schema-slug collision) it emits loom/runtime/validator-cache-collision (E, message anchored to code-registry-runtime.md, hint carrying both canonical-byte forms) and recompiles the new document rather than serving the wrong cached validator. invalidate(slug) drops the entry. Per-instance Ajv + cache (no module-level global) — no broad catch / ambient primitive / Promise combinator / Sync call. All 8 V8c-T tests green; full suite 509 green, typecheck + lint clean.
2026-06-30 V6b-T — params and defaults tests (paired V6b impl): the new src/parser/params.ts seam — parseParams(fields, bodyTypes, site) (inert stub → { diagnostics: [] }) plus the ParamFieldInput (name/typeSource/optional defaultSource/range), BodyTypeDeclaration (a whole-file schema/enum/imported .warp named type carrying its lowered $defs fragment), ParamsParseSite, and ParamsParseResult (diagnostics + optional loweredSchema) shapes — plus tests/params-defaults.test.ts with 7 tests pinning frontmatter/frontmatter-fields-a.md §params and §Defaults. loom/parse/non-trailing-default (E): a non-defaulted param after a defaulted one fires naming the first offender (message anchored to code-registry-parse.md), while all-trailing defaults raise none. loom/parse/default-not-literal (E): a function-call default RHS fires naming the offending sub-expression, while a primitive-literal default raises none. params lower to an AJV-validatable schema document the real V8c AjvSchemaValidator accepts a well-typed argument set against and rejects a mistyped one. A params RHS forward-referencing a body schema resolves whole-file (no loom/parse/unresolved-named-type) and validates a correct value while rejecting one missing a required field. loom/parse/unresolved-named-type (E): a named type with no body declaration fires naming the type. No new diagnostics-registry codes. Inert stub seam: no module-level state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 5 obligation tests red on their own primary assertions (absent diagnostic, undefined lowered schema) because the V6b params: contract is absent — fail-red-for-the-intended-reason; the 2 negative-case assertions pass; the other 511 tests stay green.
2026-06-30 V6b — params and defaults (paired with V6b-T): fills src/parser/params.ts. parseParams(fields, bodyTypes, site) implements the params: field contract of frontmatter/frontmatter-fields-a.md §params and §Defaults. Whole-file NamedType resolution: each field RHS type lowers against a bodyTypeMap built from every BodyTypeDeclaration regardless of source order, so a frontmatter-to-body forward reference (author: Author with schema Author declared later) resolves and lowers to an in-document { "$ref": "#/$defs/Author" } registered under $defs; an identifier-shaped named type with no body declaration fires loom/parse/unresolved-named-type (E, message anchored to code-registry-parse.md, naming the type). Ordering: a non-defaulted field after a defaulted one fires loom/parse/non-trailing-default (E) once, naming the first offender. Defaults: each = <rhs> default is run through the V2a checkLiteralSublanguage is-literal check, surfacing loom/parse/default-not-literal (E) naming the offending sub-expression. Type lowering (primitive → {type}, array<T> → {type:array,items}, union → SUBS-1 via lowerUnion, named → $ref) produces an AJV-validatable per-loom object document (type:object, non-defaulted fields required, additionalProperties:false, resolved named types under $defs) the V8c AjvSchemaValidator compiles; the document is returned only when the block lowers cleanly (no unresolved/ordering/non-literal error). No new diagnostics-registry codes. Pure functions, immutable module-level constants only — no mutable cross-invocation state, no broad catch / ambient primitive / Promise combinator / Sync call. All 7 V6b-T tests green; full suite 516 green, typecheck + lint clean.
2026-06-30 V8d-T — Clock / IdSource seam tests (paired V8d impl): the WallClock production-adapter stub (src/seams/wall-clock.ts) and CryptoIdSource production-adapter stub (src/seams/crypto-id-source.ts), plus FakeClock (tests/helpers/fake-clock.ts) and sequence-driven FakeIdSource (tests/helpers/fake-id-source.ts) test-fake stubs — every member throws, none referencing an ambient primitive so the H3a real-tree scan stays green — and tests/clock-id-seams.test.ts with 18 tests pinning PIC-12 and PIC-20 (host-interfaces-services.md). PIC-12: WallClock now() monotonic ms / wallNow() epoch ms / setTimeout fires+returns handle / clearTimeout cancels / per-runtime independence; FakeClock advance(ms) fires due timers in deadline order, advance(0) fires a 0-ms timer (loop-iter macrotask-yield vehicle), equal deadlines fire in registration order, clearTimeout no-op for already-fired + cancels pending, now() not implicitly advanced + wallNow() returns injected epoch, two instances have independent timer queues; ambient-timing ban architectural test — stripping the same-line // allow-ambient: <primitive> — Clock comments re-exposes performance.now/Date.now/setTimeout/clearTimeout (delegations exist), intact scan flags nothing, no other src/** module references a timing primitive (sole site). PIC-20: CryptoIdSource newInvocationId()/newToolCallId() each return a canonical lowercase 8-4-4-4-12 hex UUID minted fresh per call; FakeIdSource hands out the configured sequence in order across both members, two instances independent; ambient crypto.randomUUID ban architectural test — stripping the // allow-ambient: crypto.randomUUID — IdSource comments re-exposes exactly two crypto.randomUUID references (one per member), intact scan flags nothing, no other src/** module references crypto.randomUUID (sole site). No new diagnostics-registry codes. Inert stub seams: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 16 behavioural tests red on the stub not-implemented throw and the 2 architectural tests red on the delegation-absent strip-scan assertion because the V8d adapters/fakes are absent — fail-red-for-the-intended-reason; the other 516 tests stay green; the H3a ambient scan stays green.
2026-06-30 V8d — Clock / IdSource seam adapters (paired with V8d-T): fills the production adapters src/seams/wall-clock.ts and src/seams/crypto-id-source.ts and the test fakes tests/helpers/fake-clock.ts / tests/helpers/fake-id-source.ts. PIC-12: WallClock is the sole exempt src/** timing site — now()→performance.now() (monotonic ms), wallNow()→Date.now() (epoch ms), setTimeout/clearTimeout delegate to the globals, each direct timing reference carrying its own same-line // allow-ambient: <primitive> — Clock comment (the comment is itself the H3a allow-list entry); FakeClock drives deterministic timing — advance(ms) fires due timers in deadline order (equal deadlines in registration order; a 0-ms timer fires under advance(0), the loop-iter macrotask-yield vehicle), clearTimeout is a no-op for an already-fired handle and cancels a pending one, now()/wallNow() are not implicitly advanced (now() accumulates only via advance, wallNow() returns the injected epoch), parallel instances hold independent timer queues. PIC-20: CryptoIdSource is the sole direct crypto.randomUUID site — both newInvocationId() and newToolCallId() delegate to crypto.randomUUID() returning the canonical lowercase 8-4-4-4-12 hex form, each call carrying its own // allow-ambient: crypto.randomUUID — IdSource comment; FakeIdSource hands out a constructor-injected sequence across both members (throwing loudly on exhaustion rather than returning undefined), parallel instances draw independent sequences. No new diagnostics-registry codes. Stateless production adapters / instance-only fake state — no module-level mutable state, no broad catch / ambient primitive outside the exempt sites / Promise combinator / Sync call. All 18 V8d-T tests green; full suite 534 green, typecheck + lint + H3a ambient scan clean.
2026-06-30 V8a-T — Checkpoint seam tests (paired V8a impl): the ProductionCheckpoint production-wiring stub (src/seams/production-checkpoint.ts, constructor takes the injected Clock; before(kind, site) throws) plus tests/checkpoint-seam.test.ts with 13 tests pinning PIC-10 (host-interfaces-services.md). loop-iter stays pending across a microtask drain and resolves only after clock.advance(0) fires the 0-ms timer the seam schedules through the injected Clock.setTimeout(fn, 0) — a RecordingClock wrapper asserts exactly one 0-ms schedule, never a bare global timer; every non-loop-iter kind (query/tool-call/invoke/binder-call) resolves on the microtask queue with no clock.advance and schedules no timer; each kind's resolution matches its declared yield kind; and the CheckpointKind union is exactly the five enumerated cancel sites — pinned structurally by an exhaustive Record<CheckpointKind, …> so adding/removing a member fails type-check (adds no extra sites); per-invocation independence of two checkpoints from the same clock. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 12 behavioural tests red on the stub not-implemented throw because the V8a ProductionCheckpoint is absent — fail-red-for-the-intended-reason; the structural union-exhaustiveness assertion is green; the other 534 tests stay green.
2026-06-30 V8a — Checkpoint seam (paired with V8a-T): fills src/seams/production-checkpoint.ts. ProductionCheckpoint (constructor takes the injected Clock), one instance per loomAbort (per invocation). before(kind, site) implements PIC-10 (host-interfaces-services.md): for loop-iter it returns a promise resolved by clock.setTimeout(resolve, 0) — one macrotask turn scheduled through the injected Clock seam's PIC-12 timer surface, never a bare global timer — so a Pi-dispatched abort can land before the next loop-iteration signal-check; for every other kind (query/tool-call/invoke/binder-call) it returns Promise.resolve(), resolving on the microtask queue because those checkpoints precede real async I/O that already yields. No before(...) sites beyond the five enumerated CheckpointKinds (structurally pinned by the paired tests' exhaustive Record). No new diagnostics-registry codes. Stateless adapter (only the injected Clock held) — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 13 V8a-T tests green; full suite 547 green, typecheck + lint clean.
2026-06-30 V8e-T — FileWatcher / TokenEstimator seam tests (paired V8e impl): extends the H3a FileWatcher seam (src/seams/file-watcher.ts) with the enumerated terminal-signal channel — a WatchTermination payload + OnWatchTerminate callback added as an optional third watch(roots, handler, onTerminate?) parameter (GOV-18 arm (a)-permitted, illustrative shape) so the stopped-delivering observation stays structurally distinct from the three change kinds — re-exported via src/seams/index.ts; adds the PiFileWatcher chokidar production-adapter stub (src/seams/pi-file-watcher.ts) and PiTokenEstimator production-adapter stub (src/seams/pi-token-estimator.ts), plus the FakeFileWatcher (tests/helpers/fake-file-watcher.ts, emit/terminate injection points) and FakeTokenEstimator (tests/helpers/fake-token-estimator.ts, per-message-count map) test-fake stubs — every member throws, none referencing an ambient primitive so the H3a real-tree scan stays green — and tests/watch-token-seams.test.ts with 12 tests pinning PIC-14 and PIC-16 (host-interfaces-services.md). PIC-14: FakeFileWatcher watch() returns an idempotent Unsubscribe, the handler observes the three change kinds add/change/unlink (delivery contract), the terminal-signal channel conveys a stopped-delivering observation distinct from the three change kinds (onTerminate fires, the change handler does not), terminate() with no channel attached is a no-op, and two instances dispatch independently (per-runtime); PiFileWatcher watch() returns an idempotent-teardown Unsubscribe over a real temp dir and two instances are independent. PIC-16: PiTokenEstimator estimate(message) delegates to the estimateTokens named import (estimate === estimateTokens for the same message), is a deterministic pure function of its argument, and is per-runtime; FakeTokenEstimator returns the configured per-message integer (not a content-derived count) and two instances report independent counts. No new diagnostics-registry codes. Inert stub seams: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 12 tests red on the stub not-implemented throw because the V8e adapters/fakes are absent — fail-red-for-the-intended-reason; the other 547 tests stay green; the H3a ambient scan stays green.
2026-06-30 V8e — FileWatcher / TokenEstimator seams (paired with V8e-T): fills the PiFileWatcher chokidar production adapter (src/seams/pi-file-watcher.ts), the PiTokenEstimator production adapter (src/seams/pi-token-estimator.ts), and the test fakes tests/helpers/fake-file-watcher.ts / tests/helpers/fake-token-estimator.ts against PIC-14 and PIC-16. PIC-14: PiFileWatcher.watch(roots, handler, onTerminate?) wires a chokidar watcher with ignoreInitial, routing only the three load-bearing change kinds (add/change/unlink) to the handler (chokidar addDir/unlinkDir/ready/raw never wired, filtered out by construction) and conveying a chokidar error over the terminal-signal channel (onTerminate({roots})) — a stopped-delivering observation distinct from the three change kinds; the returned Unsubscribe closes the watcher and is idempotent (void watcher.close() guarded by an active flag, not awaited because Unsubscribe is sync). FakeFileWatcher is the conformance vehicle: watch records the handler/onTerminate and returns an idempotent Unsubscribe that drops them, emit(event) synchronously delivers one change-kind event, terminate(termination) drives the terminal channel (reaching onTerminate, never the change handler — a no-op with no channel attached); per-instance state only, parallel instances dispatch independently. PIC-16: PiTokenEstimator.estimate(message) delegates to the estimateTokens named import, returning its result unchanged (redefining none of Pi's algorithm), relied on as a deterministic pure function of message at the fixed Pi-SDK pin; FakeTokenEstimator returns the constructor-injected per-message count (keyed by message identity), throwing loudly on an unconfigured message rather than a silent zero. No new diagnostics-registry codes. Stateless production adapters / instance-only fake state — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 12 V8e-T tests green; full suite 559 green, typecheck + lint + H3a ambient scan clean.
2026-06-30 V9a-T — Capability-probe (Step 0) tests (paired V9a impl): the src/extension/capability-probe.ts seam — runCapabilityProbe(host) as a pure function over an injected ProbeHost snapshot (no ambient process.versions / global AbortSignal read), the closed HostIncompatibleKind discriminator union, ProbeFailureDetails / ProbeOutcome shapes, and the FACTORY_PROBABLE_CAPABILITIES constant (an Object.freeze([]) stub kept off the H2a module-level-mutable scan; V9a fills [1,2,3,4,6], inventory items 1/2/3/4/6) — plus tests/capability-probe.test.ts with 21 failing tests pinning PIC-3/PIC-4/PIC-5/PIC-6 and the seven-kind loom/load/host-incompatible discriminator (capability-probe.md, host-prerequisites.md). PIC-3: a host whose AbortSignal aborted/reason accessor getters throw on read still passes (the probe uses `in`, not a value read), also witnessed against the real WHATWG global. PIC-4: a constructor that throws on new still passes (no construction) and zero-arity function members satisfy the typeof checks (no arity sniffing). PIC-5: a recording host proves exactly the five enumerated check categories (a/b/c/d/e) are consulted and no sixth, and FACTORY_PROBABLE_CAPABILITIES enumerates exactly inventory items 1/2/3/4/6. PIC-6: a hostile nodeVersion / peer-version read routes to probe-failed (step / package / cause) rather than escaping. Per-kind coverage: node-floor (sub-floor observed/required >=22.19.0; qualifying prerelease passes; non-SemVer routes to probe-failed), abortsignal-shape (typeof-checked member observed undefined/required function; absent prototype-property observed absent/required present), sdk-capability-missing (member names the first-failure path under the listed order), peer-dep-out-of-range, peer-dep-malformed-version (raw string for condition 4, <unresolvable> for conditions 1-3), typebox-shape, probe-failed (<unreadable> sentinels), and the (a)->(b) short-circuit (first failure wins, no aggregation). Observed/required substrings sourced from diagnostics/placeholder-rendering-b.md's per-kind enumeration. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 21 tests red on their primary behavioural assertions (the probe returns an off-union sentinel refusal / FACTORY_PROBABLE_CAPABILITIES is empty) because the V9a probe body is absent — fail-red-for-the-intended-reason; the other 558 tests stay green.
2026-06-30 V9a — Capability probe (Step 0) (paired with V9a-T): fills runCapabilityProbe(host) in src/extension/capability-probe.ts as a pure function over the injected ProbeHost snapshot (no ambient process.versions / global AbortSignal read). Runs the five checks in the fixed short-circuit order (a) Node floor -> (b) AbortSignal/AbortController shape -> (c) nine factory-probable SDK members -> (d) four-package peer-dep lock-step -> (e) typebox Type.Unsafe, stopping at the first failure and returning one loom/load/host-incompatible refusal or a pass. (a) uses semver.satisfies with includePrerelease and semver.valid routing a non-SemVer version to probe-failed; (b) is typeof-only for the seven typeof-checked members and in-only for the aborted/reason accessor properties (PIC-3 never reads the checked member, PIC-4 never constructs); (c) names the first-failure member under the normative listed order; (d) iterates the four @earendil-works/* peers in order, tilde-testing each installed version against ~0.75.5 (condition-4 raw string / conditions-1-3 <unresolvable> for peer-dep-malformed-version, out-of-range otherwise); (e) is typeof Type.Unsafe === function. Every step is try/catch-wrapped (PIC-6) routing any throw to probe-failed with step/package/coerced cause and <unreadable> observed/required sentinels. Exports FACTORY_PROBABLE_CAPABILITIES = [1,2,3,4,6] (the V18a/V18c partition-target symbol) and SHUTDOWN_AWAIT_CAP_MS = 2000 (cancellation-runtime constant owned by V17a, imported by V9g/V9i/V17a). Observed/required substrings sourced from diagnostics/placeholder-rendering-b.md per-kind enumeration. No factory wiring or new diagnostics-registry code added here. No module-level mutable state; four catch (e: unknown) sites carry // allow-broad-catch: PIC-6 (category-2 self-failure traps); no ambient primitive / Promise combinator / Sync call. All 21 V9a-T tests green; full suite 580 green, typecheck + lint + H3a ambient scan clean.
2026-06-30 V9d-T — Runtime-event channel and `masked` co-fire tests (paired V9d impl): the src/runtime/runtime-event-channel.ts seam — the RuntimeEvent payload type (declared as a type alias so its implicit index signature keeps it assignable to the channel's { event: Record<string, unknown> } detail arm, no V7d edit), the MASKED_CEILING_IDS / GROUP_A_KINDS / NON_ALWAYS_LOG_KINDS closed sets, the computeMasked V1 reachable predicate (MaskedPredicateInput), dedupKey / cascadeReemit, isAlwaysLogKind / alwaysLogGroup routing, the five details-variant builders (buildRuntimeEventNote / buildDiagnosticsBatchNote / buildPanicNote / buildStructuralNote / buildRecoveryNote), emitRuntimeEvent / emitPanic, and successSideNote — plus tests/runtime-event-channel.test.ts with 14 tests pinning PIC-1 clauses (a)–(g) and the §"Runtime event channel" display/content matrix (runtime-event-channel.md). PIC-1: the only loom-1.0 non-empty reachable mask is ["ceiling#2"] on a validation event at the typed-query response boundary on a forced-respond turn whose post-increment tool_loop slot count == max_rounds (closed id set per clause a); masked omitted (undefined, never []) on every other surface (clause b); the cascade twin copies masked verbatim (clause f) while the (kind, query_site, message, occurred_at) dedup key excludes masked (clause g); the pure-read property (clause e) witnessed as exactly one emission carrying the right masked. Always-log: each group-A kind (transport/model_tool/code_tool/tool_loop_exhausted/invoke_infra) emits exactly once, the four deliberately-excluded kinds (validation/context_overflow/cancelled/invoke_callee) are non-members, success side honours the null-policy (Ok(v) yields no note). Matrix: one fixture per details variant pins the (display, content) pairing — event cascade display:true/template vs author-handled display:false/"" , diagnostics batch + runtime panic, structural, and recovery all display:true with carried content. Group-A/B single-shape routing: a group-A failure emits one event-shape note and zero diagnostics-shape; a panic the reverse — no fan-out. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 14 tests red on their primary assertions (computeMasked returns a sentinel id, emit helpers are no-ops, builders set wrong display/content, dedupKey includes masked, cascadeReemit strips masked, successSideNote returns non-null) because the V9d implementation is absent — fail-red-for-the-intended-reason; the other 580 tests stay green.
2026-06-30 V9d — Runtime-event channel and `masked` co-fire (paired with V9d-T): fills src/runtime/runtime-event-channel.ts. computeMasked implements the PIC-1(c)/(d) V1 reachable predicate as a pure two-scalar read — returns ["ceiling#2"] iff a validation event (cause schema_validation) is raised at the typed-query response AJV boundary on a forced_respond turn whose post-increment tool_loop slot count == max_rounds (and max_rounds > 0), undefined (never [], clause b) on every other surface. dedupKey keys on (kind, query_site, message, occurred_at) only, excluding masked (clause g); cascadeReemit spreads the originating instance verbatim including masked/occurred_at (clause f). isAlwaysLogKind admits the five group-A kinds; alwaysLogGroup routes a loom/runtime/* panic code to "B" and any kind to "A" (no fan-out). The five details builders pin the display/content matrix: buildRuntimeEventNote sets display=cascade-flag with content=template (cascade) / "" (author-handled); the diagnostics batch, panic, structural, and recovery builders are display:true with carried content. emitRuntimeEvent / emitPanic emit exactly once via the V7d sendSystemNote seam; successSideNote returns null (Ok(v) null-policy). Pure functions + DI via deps — no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 14 V9d-T tests green; full suite 594 green, typecheck + lint clean.
2026-06-30 V9f-T — Tool-registration lifetime and visibility tests (paired V9f impl): the new src/runtime/tool-registration.ts seam — deriveToolLabel(ToolLabelInput) (inert stub → ""), withActiveSetGate(deps, body) (inert stub → runs body with no snapshot/swap/restore and emits nothing), and the prompt-mode registration cache createRegistrationCache / registerToolInCache(cache, entry, deps) (inert stub → always mints the base content-addressed name and re-registers without a byte-equality check or collision diagnostic), plus the ActiveSetPi / ActiveSetGateDeps / RegistrationEntry / RegistrationCache(Record) / RegistrationCacheDeps shapes — plus tests/tool-registration-lifetime.test.ts with 11 tests pinning PIC-8 / PIC-19 / PIC-17 / PIC-44 (pi-integration-contract/tool-registration-lifetime.md) and the ToolDefinition.label derivation (extension-bootstrap-and-per-loom.md §Per-loom registration, GOV-22 un-anchored residue). PIC-17: the step-2 install vector is exactly [] for an empty callable set on an untyped turn and [respondToolName] on a forced-respond turn — never unioning in a step-1 snapshot member (acceptance criteria (a)/(b)) — and the step-4 finally restores the snapshot so visibility tracks the invocation. PIC-8: a step-4 restore throw triggers exactly one re-attempt (two restore attempts, no third try), emits loom/runtime/active-set-restore-failed (E) with the snapshot tool names in hint and a display:true loom-system-note carrying the verbatim template (only /<name> substituted), and propagates the original error unmasked. PIC-19: a step-1 snapshot throw routes to internal-error with no setActiveTools ever called (no restore owed), and a step-2 swap-install throw routes to internal-error with no restore attempt — neither fires active-set-restore-failed. PIC-44: a byte-equal cache hit reuses the registration (registerTool once, no collision); a byte-mismatch on the same slug fires loom/runtime/registration-cache-collision (E), refuses to dedup, and registers under the disambiguated per-slug-counter name __loom_callee_<slug>_2__<post> / __loom_respond_<slug>_2 (n starts at 2). Label: code-review.loom → "Code-review" (interior hyphen preserved, leading character capitalised); typed-query one-shot → literal "Loom typed-query response". Diagnostic codes/templates sourced from diagnostics/code-registry-runtime.md; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 11 tests red on their own primary assertions (absent install vector, absent restore-failure diagnostic/note, absent internal-error routing, absent collision diagnostic, wrong label) because the V9f implementation is absent — fail-red-for-the-intended-reason; the other 594 tests stay green.
2026-06-30 V9f — Tool-registration lifetime and visibility (paired with V9f-T): fills src/runtime/tool-registration.ts. deriveToolLabel derives the materialised ToolDefinition.label — a loom-file basename with interior hyphens preserved and only the leading character capitalised (code-review → "Code-review"), and the synthesised typed-query one-shot label as the literal "Loom typed-query response" (extension-bootstrap-and-per-loom.md §Per-loom registration, GOV-22 un-anchored residue). withActiveSetGate(deps, body) runs the PIC-17 active-set window: step-1 snapshot, step-2 swap-install the exact install vector (snapshot not unioned in), step-3 body, step-4 restore in a finally. PIC-8: a step-4 restore throw re-attempts the restore exactly once with the same snapshot, then emits loom/runtime/active-set-restore-failed (E) — message = failed to restore tool active-set after /<name>: <error> (first-line-truncated underlying error), hint = the snapshot tool names — plus a display:true loom-system-note carrying the verbatim template (only /<name> substituted), and the original body error propagates unmasked (restore failure swallowed in the finally). PIC-19: a step-1 snapshot or step-2 swap-install throw routes to loom/runtime/internal-error via routeInternalError with no restore owed. PIC-44: registerToolInCache registers a unique slug once under the content-addressed name storing the canonical-form bytes; a cache hit byte-compares — byte-equal reuses the registration, byte-mismatch fires loom/runtime/registration-cache-collision (E), refuses to dedup, and registers under the disambiguated per-slug-counter name (__loom_callee_<slug>_<n>__<post> / __loom_respond_<slug>_<n>, n from 2). Diagnostic codes/templates sourced from diagnostics/code-registry-runtime.md; no new diagnostics-registry codes. Cache state is the injected Map, no module-level mutable state; the four catch (e: unknown) snapshot/restore sites carry // allow-broad-catch: pi-sdk-boundary; no ambient primitive / Promise combinator / Sync call. All 11 V9f-T tests green; full suite 605 green, typecheck + lint clean.
2026-06-30 V10c-T — Settings reads-and-merge tests (paired V10c impl): the new src/discovery/settings.ts seam — mergeSettings(global, project) (inert stub → {}) and async loadSettings(fs) (inert stub → { settings: {}, diagnostics: [] }), plus the JsonObject / LoomsSettings / LoomSettings / SettingsLoadResult shapes — plus tests/settings-merge.test.ts with 13 failing tests pinning DISC-7 and the four loom/load/settings-* codes (discovery/package-and-settings.md). DISC-7: nested objects deep-merge (project keys override, global-only nested keys retained), arrays/scalars replace wholesale (project over global), a key present in only one file kept. settings-invalid-json (W): present-but-unparseable file fires one diagnostic. settings-unreadable (W): a missing or unreadable file is treated as {}, the other file's loaded value unaffected, one diagnostic fires. settings-value-out-of-range (E): top-level shape — non-array loomPaths / non-object looms treated as absent with one diagnostic per malformed top-level key (malformed looms does not cascade per nested key), non-object JSON root fires one (root) diagnostic with no per-key cascade; scalar keys — each of the four looms.* keys (binderModel non-empty string, scanPackages JSON true/false, scanPackagesMaxFiles/scanPackagesTimeoutMs integers ≥ 1; null out of range) failing its type/range treated as absent with one diagnostic per offending key per file, valid sibling surviving and a malformed value in one file not suppressing a valid value of the same key in the other. settings-invalid-entry (E): a non-string loomPaths entry rejected per-entry while other entries still process. Byte-exact message assertions sourced from diagnostics/code-registry-load.md Message column (parsed-scalar <observed> carve-out). Resolved paths POSIX-joined: project <cwd>/.pi/settings.json, global <homedir>/.pi/agent/settings.json. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 13 tests red on their own primary assertions because the V10c body is absent — fail-red-for-the-intended-reason; the other 605 tests stay green.
2026-06-30 V10c — Settings reads-and-merge (paired with V10c-T): fills src/discovery/settings.ts. mergeSettings(global, project) deep-merges nested objects key-by-key with project precedence and replaces arrays/scalars wholesale (DISC-7). loadSettings(fs) POSIX-joins and reads <cwd>/.pi/settings.json and <homedir>/.pi/agent/settings.json sequentially through the FileSystem seam: a read rejection (missing/unreadable) → loom/load/settings-unreadable (W) with the file treated as {} and the other file unaffected; an invalid-UTF-8 decode (fatal TextDecoder) or JSON parse failure → loom/load/settings-invalid-json (W); both discriminated via Promise rejection handlers, not catch clauses. Per-file validation: top-level shape — a non-array loomPaths, non-object looms, or non-object JSON root is treated as absent and fires one loom/load/settings-value-out-of-range (E) per malformed top-level key ((root) once for a non-object root, observed via the parsed-scalar carve-out using render/canonical-number; a malformed looms does not cascade per nested key); the four looms.* scalar keys (binderModel non-empty string, scanPackages boolean, scanPackagesMaxFiles/scanPackagesTimeoutMs integers ≥ 1, null out of range) each fire one out-of-range diagnostic per offending key per file and are treated as absent, valid siblings surviving and a malformed value in one file not suppressing a valid value of the same key in the other; non-string loomPaths entries → loom/load/settings-invalid-entry (E) per entry while other entries process. The two cleaned objects are then merged. No module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 13 V10c-T tests green; full suite 618 green, typecheck + lint clean.
2026-06-30 V10a-T — Discovery-walk tests (paired V10a impl): the new src/discovery/discovery-walk.ts seam — async discoverLooms(input) (inert stub → { looms: [], diagnostics: [] }) plus the DiscoverySource / PiOwnedCommand / DiscoveryInput / DiscoveredLoom / DiscoveryResult shapes — plus tests/discovery-walk.test.ts with 14 failing tests pinning DISC-1…DISC-4 (discovery/discovery-sources.md). DISC-1: a bare ~/ prefix expands via the FileSystem.homedir() seam (path pinned to the homedir-joined absolute) and the ~user form is taken literally (no env/platform branch). DISC-2: a missing conventional root is silent while a missing settings/CLI entry is loom/load/missing-source E; an unreadable conventional root is loom/load/unreadable-source W while an unreadable CLI path is E; a conventional root resolving to a regular file is loom/load/wrong-type-source W; the clean-leaf-ENOENT ancestor walk separates missing (all ancestors lstat ok) from unreadable (an ancestor lstats EACCES); a discovered .loom that is itself unreadable is loom/load/unreadable W and skipped while siblings still register. DISC-3: two case-variant *.loom entries in one source fire loom/load/case-collision W naming both paths; a valid stem with a non-canonical extension case fires loom/load/non-canonical-extension W (invalid-stem files stay silent); a stem failing ^[a-z0-9][a-z0-9_-]*$ fires loom/load/invalid-slash-name E and does not register. DISC-4: two same-priority looms deriving one slash name fire loom/load/cross-format-collision E and none register; a loom colliding with a Pi-owned command fires the same code and the loom loses asymmetrically. Settings plumbing (cross-leaf, no REQ-ID): a loomPaths entry from V10c's merged settings contributes its .loom via the settings source. --loom supplied pre-split (cliPaths) for platform-independence. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 14 tests red on their own primary assertions because the V10a body is absent — fail-red-for-the-intended-reason; the other 618 tests stay green.
2026-06-30 V10a — Discovery walk, sources, and collisions (paired with V10a-T): fills src/discovery/discovery-walk.ts discoverLooms(input). Walks the CLI (priority 1), Settings (2), Project (3), and Global (5) discovery sources in priority order through the injected FileSystem seam (Package source is V10b's, not yet plumbed into DiscoveryInput). DISC-1: a bare ~ / ~/… prefix expands via FileSystem.homedir() only — expandHome honours ~ alone or ~/ and takes every other form (~user) literally, with no env or platform branch. DISC-2: per-source failure modes via classifyPath — conventional global/project roots are silent-on-missing and warning on unreadable/wrong-type; settings entries are missing/wrong-type E and unreadable W; CLI paths are all E — emitted as loom/load/{missing,unreadable,wrong-type}-source with the source descriptor in the message; the clean-leaf-ENOENT ancestor walk (ancestorsClean lstats each proper ancestor root-first) classifies an ENOENT candidate as missing iff every ancestor lstats ok as a directory, else unreadable; a discovered .loom that is itself unreadable (readBytes rejects) fires loom/load/unreadable W and is skipped while siblings still register. DISC-3: resolveCaseCollisions detects intra-source paths differing only in case (loom/load/case-collision W, the byte-first path winning); enumerateDirectory emits loom/load/non-canonical-extension W for a case-variant .loom/.warp extension on a valid stem (realpath-deduped against byte-exact canonical siblings, so case-insensitive one-entry-two-spellings does not warn); validateAndRead rejects a stem failing ^[a-z0-9][a-z0-9_-]*$ with loom/load/invalid-slash-name E (the file does not register). DISC-4: resolveSlashNames groups by derived name — a Pi-owned command with the same name drops every colliding loom (loom/load/cross-format-collision E, Pi-owned survives, the loom loses asymmetrically); >1 candidate at the highest priority tier is a same-priority loom-vs-loom cross-format-collision E (all drop); a single top-tier winner shadows lower-priority candidates with loom/load/cross-source-shadow W. --loom arrives pre-split as cliPaths; the merged loomPaths from V10c reaches the walk via the settings source (source: "settings"). All read/lstat/realpath rejections discriminated via Promise rejection handlers (no catch clauses, broad-catch ban honoured); constant tables (PRIORITY, *_MODES) declared as const so no module-level mutable binding; no ambient primitive / Promise combinator / Sync call. Diagnostic messages sourced from diagnostics/code-registry-load.md; no new registry codes. All 14 V10a-T tests green; full suite 632 green, typecheck + lint + H2a/H3a gates clean.
2026-07-01 V9b-T — Registration steps and reload-wiring seam tests (paired V9b impl): the new src/extension/reload-wiring.ts seam — LoomRegistry (Map<slashName, parsedLoom> with get/snapshot/publish), rebuildAndSwap(changedPath, build, deps) (inert stub → false, no publish/diagnostic), the test-only ReloadFailureInjector seam + createReloadFailureInjector(deps) (inert no-op; registry-swap / loom-warp-reparse arms, settings-remerge deferred to V10d), dropCollidingLooms(pending, snapshot) (inert stub → all pending survive), structuralChangeNote(added, removed) (inert stub → placeholder note for every input), createModelReferenceMatcher(registry) (inert stub → always no-match), and loadPassParse(files, deps) (inert stub → fresh matcher per file) plus the ParsedLoom/AvailableModel/ModelRegistrySurface/RegistrySwapDeps/LoadPassDeps shapes and the REGISTRY_SWAP_FAILED_CODE constant — plus tests/registration-reload-wiring.test.ts with 9 failing tests pinning PIC-36/37/38/39 and the model-reference-matcher production wiring (registration-steps.md, host-interfaces-core.md#model-registry-pin). PIC-36: a successful rebuild publishes the staged map (build-aside then publish); a failed rebuild discards the staging set, keeps the prior snapshot live, and fires loom/runtime/registry-swap-failed (E, message "registry swap failed: <path>", hint = underlying error); the ReloadFailureInjector routes a synthetic registry-swap / re-parse failure onto that same surfacing path. PIC-39: the collision pass drops a name colliding with a prompt-source command while leaving the pi.getCommands() snapshot (an Object.freeze'd array whose mutators throw) unmutated in length and order (read-only-by-convention). PIC-37: an empty debounce window (added.length + removed.length === 0) emits no note. PIC-38: a same-window rename of P (removed P then added P, summing to 2) emits the note with the fixed "loom watcher: 2 file(s) added or removed; run /reload to refresh the slash command list" content and display:true. Model wiring: the matcher is an exact-match resolver over getAvailable() matching .id / .provider (never the api-shaped .api), reports a bare modelId ambiguous across providers as ambiguous, and the load pass constructs it once and injects that single instance into every V6a parse call (single-source-of-construction, instance identity). Diagnostic message sourced from diagnostics/code-registry-runtime.md; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 9 tests red on their own primary assertions because the V9b body is absent — fail-red-for-the-intended-reason; the other 632 tests stay green.
2026-07-01 V9b — Registration steps and reload-wiring seams (paired with V9b-T): fills src/extension/reload-wiring.ts. PIC-36: rebuildAndSwap builds the staged Map<slashName, parsedLoom> aside and publishes it via a single synchronous LoomRegistry.publish only after every staged step succeeds; a throw out of build() discards the staging set, keeps the prior snapshot live, and emits exactly one loom/runtime/registry-swap-failed (E, message "registry swap failed: <path>", hint = underlying error message), the rebuild-failure trap being the one broad catch (// allow-broad-catch: loom/runtime/registry-swap-failed). The test-only ReloadFailureInjector (createReloadFailureInjector) routes the registry-swap and loom-warp-reparse arms onto that same surfacing path; settings-remerge is left to V10d. PIC-39: dropCollidingLooms is a single read-only forward pass dropping pending looms whose slash name collides with an existing command whose source is in {prompt, extension, skill}, never mutating the snapshot array. PIC-37: structuralChangeNote returns undefined when added.length + removed.length === 0 (empty-window suppression). PIC-38: otherwise it returns the fixed "loom watcher: <N> file(s) added or removed; run /reload to refresh the slash command list" note with display:true and details.structural carrying the two arrays (N base-10, arrays not deduplicated, a same-window rename counting twice). Model-reference-matcher production wiring: createModelReferenceMatcher is loom's exact-match resolver over getAvailable() matching a bare modelId against .id (ambiguous across providers) and provider/modelId against .provider (short id, never the api-shaped .api) plus .id; loadPassParse constructs the matcher once and injects that single instance into every V6a parse call (single-source-of-construction, instance identity). No module-level mutable state (only a const collision-source Set), no ambient primitive / Promise combinator / Sync call. No new diagnostics-registry codes. All 9 V9b-T tests green; full suite 641 green, typecheck + lint + H2a/H3a gates clean. Divergence: the PIC-36 broad catch is a spec-mandated sixth broad-catch site not literally enumerated among conventions.md's five exempt categories (token resolves; recorded for release-time residue inspection).
2026-07-01 V9e-T — ActiveInvocationRegistry tests (paired V9e impl): the new src/runtime/active-invocation-registry.ts seam — the ActiveInvocationRegistry class (add/remove/size/snapshot, inert stubs storing nothing → size 0 / snapshot []), async dispatchSiteSetup(request) (inert stub → a sentinel defect with a wrong loom/runtime/__v9e-unimplemented__ code and cause: "load_failure"), and async runPerInvocationFinally(request) (inert no-op: no dispose, no barrier settle, no removal) plus the ActiveInvocationEntry (five-field) / InvocationMode / AbortSourceLike / AgentSessionLike / DispatchSetupRequest / DispatchSetupOk / DispatchSetupDefect / PerInvocationFinallyRequest shapes — plus tests/active-invocation-registry.test.ts with 7 failing tests pinning active-invocation-registry.md (PIC area). Insertion-order iteration: registered entries are tracked via the size probe and walked in insertion order, removal shrinking the live set (teardown reaches every in-flight entry). disposeBarrier: a single entry's barrier settles after that entry's AgentSession.dispose() returns (subagent) / immediately (prompt), settling only that one entry's barrier (a concurrent sibling's stays pending), not the aggregate settle-all. invocationId allocation: minted only through IdSource.newInvocationId() (newToolCallId() throws if touched). Dispatch-site setup wrap failure path: a setup-sequence throw (newInvocationId throws, before the registry Set.add) and a setup-sequence rejection (createAgentSession rejects) each route through the runtime-defect surface (loom/runtime/internal-error / InvokeInfraError { cause: "internal_error" }); the throw-before-Set.add leaks no entry (the entry-count probe stays empty); a catch-arm cleanup loomAbort.abort() throw is dropped without masking the original setup throw (the surfaced message reflects the original, not the cleanup throw). All assertions are on observable side effects (probe counts, ordered snapshot, disposeBarrier settlement, the emitted diagnostic/error surface), per the registry-name-is-internal testing posture. Diagnostic code anchored to the diagnostics registry; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 7 tests red on their own primary assertions (empty probe, the sentinel defect code) because the V9e body is absent — fail-red-for-the-intended-reason; the other 641 tests stay green.
2026-07-01 V9e — ActiveInvocationRegistry (paired with V9e-T): fills src/runtime/active-invocation-registry.ts. The ActiveInvocationRegistry class is Set-backed (private #entries, insertion-order iteration matching the V8 Set invariant the session_shutdown teardown sub-steps 2/3 rely on); add/remove maintain the live set and size()/snapshot() are the entry-count / ordered-snapshot probe seams (the registry name itself is internal). dispatchSiteSetup(request) is the shared Dispatch-site setup wrap for the three insertion sites: creates loomAbort (makeAbortController test seam, default new AbortController()), mints invocationId only through IdSource.newInvocationId() before any awaitable work, builds the disposeBarrier Promise<void> with a closure-held resolver (manual Promise.withResolvers() — ES2022 lib), Set.adds the five-field entry, attaches the inbound-signal forwarding listener, and (subagent mode) awaits createAgentSession() then wires the one-shot AgentSession.abort(). On any setup throw/rejection it routes through the runtime-defect surface (loom/runtime/internal-error via surfaceUnexpectedThrow / InvokeInfraError { cause: "internal_error" }); a throw before Set.add leaks no entry (probe stays empty); a throw after Set.add removes the entry and settles its barrier; the catch-arm cleanup loomAbort.abort() is wrapped in a nested try/catch and its throw dropped without masking the original. runPerInvocationFinally(request) settles a single entry's barrier after AgentSession.dispose() returns (subagent) / immediately (prompt) then removes the entry — never the aggregate settle-all. Two broad catch (e: unknown) carry // allow-broad-catch: pi-sdk-boundary (the wrap surrounds the createAgentSession Pi-SDK await). No module-level mutable state, no ambient primitive / Promise combinator / Sync call. No new diagnostics-registry codes. All 7 V9e-T tests green; full suite 648 green, typecheck + lint + H2a/H3a gates clean. Divergence: the setup-wrap catch removes-and-settles a post-Set.add entry itself (spec assigns removal to the per-invocation finally; the defect return leaves no caller handle), and the two broad catches reuse the structural pi-sdk-boundary token — both recorded for the release-time residue inspection.
2026-07-01 V9k-T — Extension-bootstrap SDK-failure abort tests (paired V9k impl): the src/extension/factory.ts seam grows the EXTENSION_BOOTSTRAP_FAILED_CODE constant ("loom/load/extension-bootstrap-failed") and an optional emitDiagnostic dependency on LoomExtensionDeps — plus tests/extension-bootstrap-failures.test.ts with 5 failing tests pinning the extension-bootstrap-failed rule (extension-bootstrap-and-per-loom.md §"Extension-bootstrap SDK failures", PIC area, no numbered REQ-IDs). pi.registerFlag failure: a factory-time registerFlag throw is fatal to the whole extension — steps 2–5 do not execute (none of the three factory-time pi.on subscriptions install) — and emits exactly one diagnostic with details.capability="pi.registerFlag", details.error = the caught throw's underlying-error string (placeholder-rendering-b.md#underlying-error-coercion), and a message anchored on the byte-identical registry-template prefix "extension bootstrap failed: pi.registerFlag threw ". pi.on(...) failure (parametrized over resources_discover / session_start / session_shutdown): a factory-time subscription throw is fatal — no subsequent pi.register*/pi.on call runs after the failing subscription (asserted via the recorded call log + the installed-subscription prefix) — and emits exactly one diagnostic with details.capability="pi.on" and details.event naming the subscribed Pi event. The fatal-truncation and single-diagnostic facets are folded per pi.on event into one test so the last subscription (session_shutdown, whose truncation facet is trivially true) still reds on the absent diagnostic. Diagnostic code/message anchored to diagnostics/code-registry-load.md; no new diagnostics-registry codes. Tests-task seam adds no behaviour (the factory's per-call try/catch still swallows and continues); typecheck + lint clean; the 5 tests red on their own primary assertions because the V9k body is absent — fail-red-for-the-intended-reason; the other 648 tests stay green.
2026-07-01 V9k — Extension-bootstrap SDK-failure abort surfaces (paired with V9k-T): fills the two whole-extension abort branches in src/extension/factory.ts. A factory-time pi.registerFlag throw is fatal — the factory emits one loom/load/extension-bootstrap-failed (E, load) with details.capability="pi.registerFlag" and returns, so steps 2–5 (the renderer and all three pi.on subscriptions) do not execute. Each of the three factory-time pi.on subscriptions (resources_discover / session_start / session_shutdown) is wrapped so a throw is fatal: the factory emits one diagnostic with details.capability="pi.on" and details.event naming the subscribed event, then returns — no subsequent pi.register*/pi.on call runs after the failing subscription. Both diagnostics carry details.error = the caught throw's underlying-error string (via renderUnderlyingError, placeholder-rendering-b.md#underlying-error-coercion) and render the byte-identical registry-template Message "extension bootstrap failed: <capability> threw <error>" (code-registry-load.md). The renderer-failure surface stays a non-abort local swallow (degrade + diagnostic owned by V9p); the per-loom registerCommand drop and session_start-time getCommands read-failure drop are likewise out of V9k scope. New helper bootstrapFailedDiagnostic(capability, caught, event?) constructs the diagnostic; the three pi.on subscriptions are installed inline (literal-event overloads keep each handler on its typed signature). The two abort catch sites reuse the // allow-broad-catch: pi-sdk-boundary token. No new diagnostics-registry codes. No module-level mutable state, no ambient primitive / Promise combinator / Sync call. All 5 V9k-T tests green; full suite 653 green, typecheck + lint + H2a/H3a/broad-catch gates clean.
2026-07-01 V9m-T — LoomRegistry drain-state contract tests (paired V9m impl): extends src/extension/reload-wiring.ts LoomRegistry with the closed four-method drain-state surface (backing fields #drained/#drainStateTag; drain()/initDrainStateTag()/markRuntimeDegraded() inert no-op stubs, readDrainState() the real field-read) plus the DrainStateTag/DrainStateSnapshot types, and adds src/extension/drain-state.ts — routeDrainStateArm (inert stub → always arm (a)), shouldShortCircuitShutdown (inert stub → always false), routeSlashDispatchWithReadFailover / evalShutdownShortCircuitWithReadFailover (inert stubs ignoring the read), resolveSlashDispatch (inert stub → always dispatch), and the fixed shuttingDownNote/degradedNote/supersededNote templates — plus tests/drain-state-contract.test.ts with 14 tests pinning drain-state-contract.md (PIC-29/30/31/32) and registration-steps.md#superseded-entry-dispatch. PIC-29: each of the six (drained, tag) tuples routes to its closed three-arm enumeration value (mutually exclusive, exhaustive), and the short-circuit predicate drained === true || tag !== undefined fires on every tuple except steady-state (false, undefined). PIC-30: routing the full six-tuple space yields exactly three arms (no fourth arm), the readDrainState snapshot exposes exactly drained/tag (no third boolean field), and the post-failed-handler (false, undefined) tuple still routes to arm (a) (no arm-specific gate). PIC-31: a slash-site read-failure fails safe onto arm (c), a handler-entry read-failure fails safe to steady-state (predicate not fired → full teardown). PIC-32: LoomRegistry.drain() flips drained to true and the short-circuit predicate is idempotent. Superseded-entry dispatch: a steady-state dispatch with a dropped entry reaches arm (a), the entry-table lookup misses, and the fixed "loom /<name>: superseded; /reload to refresh" note is returned. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 10 of the 14 tests red on their own primary assertions because the V9m body is absent — fail-red-for-the-intended-reason; the other 657 tests stay green.
2026-07-01 V9m — LoomRegistry drain-state contract (paired with V9m-T): fills the three drain-state writers in src/extension/reload-wiring.ts (drain() sets drained=true per PIC-32; initDrainStateTag() sets drainStateTag="shutting-down" iff currently undefined; markRuntimeDegraded() sets drainStateTag="degraded-needs-reload" unconditionally) and the routing/dispatch surface in src/extension/drain-state.ts. routeDrainStateArm maps the six (drained, tag) tuples onto the closed three arms (PIC-29): tag "degraded-needs-reload" → arm (c); tag "shutting-down" or drained → arm (b); steady-state (false, undefined) → arm (a); no fourth arm (PIC-30). shouldShortCircuitShutdown is the disjunction drained === true || tag !== undefined (idempotent, fires on every non-steady tuple). routeSlashDispatchWithReadFailover wraps the slash-site readDrainState in a broad try/catch and routes a read throw to arm (c) (PIC-31); evalShutdownShortCircuitWithReadFailover wraps the handler-entry read and treats a throw as steady-state (predicate not fired → full teardown). resolveSlashDispatch routes arm (a) to the entry-table lookup, returning the dispatched loom on a hit and the fixed "loom /<name>: superseded; /reload to refresh" note on a miss. The two read-failover catch (e: unknown) sites carry // allow-broad-catch: PIC-31. No module-level mutable state, no ambient primitive / Promise combinator / Sync call. No new diagnostics-registry codes. All 14 V9m-T tests green; full suite 667 green, typecheck + lint + H2a/H3a/broad-catch gates clean.
2026-07-01 V9p-T — Extension-bootstrap SDK-failure non-abort surface tests (paired V9p impl): src/extension/system-note-channel.ts grows the RendererGate class (available()/degrade(), instance-private #rendererAvailable) and an optional rendererGate field on SystemNoteChannelDeps; src/extension/factory.ts LoomExtensionDeps grows optional rendererGate / registry fields — plus tests/extension-bootstrap-nonabort.test.ts with 6 failing tests pinning the three non-abort arms of loom/load/extension-bootstrap-failed (extension-bootstrap-and-per-loom.md §"Extension-bootstrap SDK failures" + #getcommands-read-failure, PIC area; no numbered REQ-IDs). pi.registerMessageRenderer failure: a factory-time renderer throw drops the renderer, the factory completes the remaining steps (all three pi.on subscriptions install), emits one diagnostic with details.capability="pi.registerMessageRenderer" + details.error, permanently degrades the RendererGate (available()→false), and a degraded System-notes channel skips the pi.sendMessage arm and routes every note through ctx.ui.notify. pi.registerCommand failure: a session_start-time per-loom throw drops only the failing loom, siblings still register, and one diagnostic per failing loom carries details.capability="pi.registerCommand" + details.loom = the slash name (two failures → two diagnostics). pi.getCommands() read failure: a session_start-time read throw drops the pending-registration list (no pi.registerCommand call issues), the handler swallows the throw, MUST NOT set drain state (the injected LoomRegistry stays at steady-state (drained:false, tag:undefined)), and emits one diagnostic with details.capability="pi.getCommands" (asserted distinctly from the write-side surface). Diagnostic code/message anchored to diagnostics/code-registry-load.md; no new diagnostics-registry codes. Tests-task seam adds no behaviour (the factory still swallows renderer/per-loom failures locally and never reads pi.getCommands()); typecheck + lint clean; the 6 tests red on their own primary assertions because the V9p body is absent — fail-red-for-the-intended-reason; the other 667 tests stay green.
2026-07-01 V9p — Extension-bootstrap SDK-failure NON-ABORT surfaces (paired with V9p-T): fills the three per-surface non-abort branches in src/extension/factory.ts and the degraded-channel branch in src/extension/system-note-channel.ts. The bootstrapFailedDiagnostic helper is widened to bootstrapFailedDiagnostic(capability, caught, { event?, loom? }) and the BootstrapCapability union grows the three V9p capabilities. pi.registerMessageRenderer failure: a factory-time renderer throw now degrades the injected RendererGate (degrade() → available() false) and emits one loom/load/extension-bootstrap-failed (E, load) with details.capability="pi.registerMessageRenderer" + details.error, while the factory completes the remaining steps (all three pi.on subscriptions still install). sendSystemNote grows a degraded-instance branch: when the RendererGate is degraded it skips the pi.sendMessage transcript arm and routes through ctx.ui.notify (a throwing toast falls to the terminal console.error, PIC-54); the renderer failure already emitted one bootstrap-failed diagnostic so no per-note delivery-failed diagnostic fires for the expected degraded route. pi.registerCommand failure: the session_start handler emits one diagnostic per failing loom with details.capability="pi.registerCommand" + details.loom = the slash name; only the failing loom drops, siblings register. pi.getCommands() read failure: the session_start handler now reads pi.getCommands() first (the step-3 collision pass) inside a per-call try/catch; a throw drops the pending-registration list (no pi.registerCommand issues), swallows the throw, emits one diagnostic with details.capability="pi.getCommands", and leaves the injected LoomRegistry drain state untouched (V9m owns drain state). No new diagnostics-registry codes; the four per-surface catch sites carry // allow-broad-catch: pi-sdk-boundary / PIC-54. No module-level mutable state, no ambient primitive / Promise combinator / Sync call. All 6 V9p-T tests green; full suite 673 green, typecheck + lint + H2a/H3a/broad-catch gates clean.
2026-07-01 V9q-T — Watcher post-error terminal recovery posture tests (paired V9q impl): adds src/extension/watcher-recovery.ts — the armWatcherWithTerminalRecovery(deps) seam (inert onTerminate-stub: arms the change-delivery contract but performs no recovery), the WATCHER_TERMINATED_CODE / WATCHER_TERMINATED_MESSAGE constants, and the watcherTerminatedDiagnostic() builder — plus tests/watcher-terminated-recovery.test.ts with 2 failing tests pinning PIC-55 and the loom/runtime/watcher-terminated registry code (registration-steps.md#pic-55, host-interfaces-services.md PIC-14, code-registry-runtime.md). PIC-55: driving the V8e FakeFileWatcher.terminate() terminal-signal injection point (the stopped-delivering — terminal case) emits exactly one persistent loom/runtime/watcher-terminated loom-system-note through the loom-system-note channel as its primary sink (never ctx.ui.notify), leaves the watcher torn down rather than re-armed (single watch call; a subsequent change no longer reaches the handler), keeps LoomRegistry live and dispatchable through arm (a) of readDrainState (steady-state tuple, /greet still dispatches), and writes no drain-state tag. loom/runtime/watcher-terminated: the emitted note's message is sourced from the registry Message column via registryMessage ("loom watcher terminated; hot-reload halted until /reload") and the note carries the diagnostic in details.diagnostics. Diagnostic message anchored to diagnostics/code-registry-runtime.md; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 2 tests red on their own primary assertion (no persistent note emitted) because the V9q recovery body is absent — fail-red-for-the-intended-reason; the other 673 tests stay green.
2026-07-01 V9q — Watcher post-error terminal recovery posture (paired with V9q-T): fills the inert onTerminate recovery body in src/extension/watcher-recovery.ts. On the V8e FileWatcher terminal-signal channel (the stopped-delivering — terminal case, PIC-55) the callback (1) tears the watcher down via the seam's idempotent unsub() — left torn down, never re-armed; (2) emits exactly one persistent loom/runtime/watcher-terminated loom-system-note through the V7d channel via sendSystemNote as its primary sink (never ctx.ui.notify on the steady-state route), carrying details.diagnostics=[watcherTerminatedDiagnostic()] and content rendered from the registry-sourced location-less diagnostic line (loom watcher terminated; hot-reload halted until /reload); (3) leaves the injected LoomRegistry untouched so it stays live and dispatchable through arm (a) of readDrainState and writes no drain-state tag. Diagnostic message anchored to diagnostics/code-registry-runtime.md; no new diagnostics-registry codes. No broad catch / global-static-singleton / ambient primitive / Promise combinator / Sync call. All 2 V9q-T tests green; full suite 675 green, typecheck + lint + H2a/H3a/broad-catch gates clean.
2026-07-01 V10b-T — Package-discovery bounded-walk tests (paired V10b impl): adds src/discovery/package-discovery.ts — the discoverPackageLooms(input) seam (inert stub → { looms: [], diagnostics: [] }), the PackageDiscoveryInput (fs / clock / merged settings) / PackageDiscoveredLoom / PackageDiscoveryResult shapes, and DEFAULT_SCAN_PACKAGES* bound constants — plus tests/package-discovery.test.ts with 10 failing tests pinning DISC-5 / DISC-6 (discovery/package-and-settings.md), with codes/messages from diagnostics/code-registry-load.md. DISC-5: a non-string[] pi.looms fires loom/load/manifest-invalid E (package contributes nothing, siblings process); minimatch globs resolve !/+/- in the fixed order with - taking final precedence over a + re-admission; file/dir/other match rules hold (a .loom match registers directly, a directory match is scanned non-recursively for *.loom, any other file type is filtered silently); an entry resolving outside the package root fires loom/load/manifest-escapes-package W per entry (in-root entries still process). DISC-6: the walk trips loom/load/discovery-slow W at the operator looms.scanPackagesMaxFiles value and at the looms.scanPackagesTimeoutMs value (both distinct from the 2000 default so a hardcoded-2000 impl fails), driven deterministically through the FakeClock seam by hung package.json reads advancing fake time via the per-read deadline timers; on a simultaneous cap-check tie the file-count predicate is consulted before time (cap = scanPackagesMaxFiles); looms.scanPackages: false skips the walk wholesale (zero candidate package.json reads via an InstrumentedFileSystem counter, verified against an otherwise-identical enabled control); a per-read timeout fires loom/load/package-read-timeout W carrying details.kind and continues to the next candidate, its rendered <deadline> being max(200, floor(scanPackagesTimeoutMs / 10)) — 200ms at the default and 500ms at a 5000 override (reddening the hardcoded-200 misread). Merged bounds flow into the walk from V10c's settings view (defaults applied only when a key is absent). Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call; test-only InstrumentedFileSystem decorator counts and hangs package.json reads. Typecheck + lint clean; the 10 tests red on their own primary assertions (no loom / no diagnostic / no read) because the V10b body is absent — fail-red-for-the-intended-reason; the other 675 tests stay green.
2026-07-01 V10b — Package discovery (bounded walk) (paired with V10b-T): fills src/discovery/package-discovery.ts discoverPackageLooms(input). Enumerates the five installed-package roots (project .pi/npm / .pi/git, project-local node_modules, global ~/.pi/agent/npm / ~/.pi/agent/git) through the injected FileSystem seam — npm-style roots take each immediate child (and @scope children) as a candidate package; git-style roots descend to package.json-bearing directories. DISC-5: a pi.looms that is not a string[] fires loom/load/manifest-invalid E and the package contributes nothing while siblings process; a valid array resolves via minimatch (nocase off; matched against the package-root-relative path, basename, and absolute path) with the fixed !/+/- override order (plain includes select the starting set — every path when none present; ! drops; + exact operands re-admit; - exact operands remove with final precedence over +), each surviving match contributing per the file/dir/other rule (a .loom file registers directly, a directory is scanned non-recursively for *.loom, any other file type is filtered silently); an entry resolving outside the package root fires loom/load/manifest-escapes-package W per entry; a package with no pi.looms field falls back to the conventional looms/ directory. DISC-6: the walk is bounded by looms.scanPackagesMaxFiles (default 2000) reads and looms.scanPackagesTimeoutMs (default 2000) ms, both from V10c's merged settings (defaults applied only when absent), the file-count predicate consulted before elapsed-time at a tie so loom/load/discovery-slow W names looms.scanPackagesMaxFiles; each candidate read is bounded by a per-read deadline max(200, floor(scanPackagesTimeoutMs/10)) armed through Clock.setTimeout and awaited via a hand-rolled first-settle race (no Promise.race), a timeout firing one loom/load/package-read-timeout W (details.kind) and continuing; looms.scanPackages: false skips the walk wholesale. The walk clock starts at the first cap-check so candidate enumeration is not charged to the read budget. fs rejections discriminated via Promise rejection handlers (no catch); no module-level mutable state, no ambient primitive / Promise combinator / Sync call. No new diagnostics-registry codes; not yet plumbed into discoverLooms (no production caller wires discovery yet). All 10 V10b-T tests green; full suite 685 green, typecheck + lint + H2a/H3a gates clean. Divergences: npm-root candidates not lstat-pre-filtered, no cross-root package-identity dedup, git-root enumeration descends to package.json-bearing dirs.
2026-07-01 V10d-T — Reload-debounce and cross-window rebuild-serialization tests (paired V10d impl): adds src/extension/reload-debounce.ts — the ReloadDebouncer seam (onWatcherEvent() no-op stub), the ReloadDebouncerDeps / RebuildOutcome shapes, and RELOAD_DEBOUNCE_WINDOW_MS (250) — plus tests/reload-debounce.test.ts with 6 failing tests pinning the reload-debounce code-keyed area cka-36 (discovery/package-and-settings.md §Caching and reload) and PIC-49 (registration-steps.md#pic-49), all driven through the injected Clock seam via the V8d FakeClock (Clock.setTimeout / Clock.clearTimeout, FakeClock.advance). cka-36: a burst of N watcher events 50 ms apart within one 250 ms window (each clearing the pending handle and rescheduling) coalesces to exactly one reload (per-event firing would produce N); a further event after the window boundary produces a second reload; a reload fires only once the window closes. PIC-49: a debounce timer firing while a prior window's rebuild is still in flight (a caller-controlled unsettled rebuild() promise) defers rather than starting a concurrent rebuild, the in-flight guard releasing on the in-flight rebuild's single synchronous publish or its loom/runtime/registry-swap-failed discard, and multiple windows closing during one in-flight rebuild collapse to a single deferred rebuild. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 6 tests red on their own primary toHaveBeenCalledTimes assertions (no reload ever fires) because the V10d body is absent — fail-red-for-the-intended-reason; the other 685 tests stay green.
2026-07-01 V10d — Reload debounce and cross-window rebuild serialization (paired with V10d-T): fills src/extension/reload-debounce.ts ReloadDebouncer.onWatcherEvent(). cka-36 (package-and-settings.md §Caching and reload): each watcher event clears the pending Clock.setTimeout handle and reschedules the 250 ms window (drop-and-reschedule, most-recent handle only), so a burst coalesces into exactly one reload firing 250 ms after the last event, and a post-boundary event starts a fresh cycle producing a second reload. PIC-49 (registration-steps.md#pic-49): a debounce timer firing while a prior window's rebuild is still awaiting its async steps defers via a flag rather than starting a concurrent rebuild against the live LoomRegistry / validator cache / registration cache; the in-flight guard releases on the rebuild promise settling (the PIC-36 single synchronous publish or the loom/runtime/registry-swap-failed discard V9b owns), after which one deferred rebuild runs — multiple windows collapse to a single deferred rebuild. Wires V10d's settings-remerge arm into V9b's ReloadFailureInjector (SETTINGS_REMERGE_FAILED_CODE = loom/load/settings-invalid-json), the re-parse/re-merge diagnostic arm V4g binds against, distinct from the swap arm. Clock-seam timing only; .then guard-release (no catch / Promise combinator / Sync call); no module-level mutable state. No new diagnostics-registry codes. All 6 V10d-T tests green; full suite 691 green, typecheck + lint + H2a/H3a gates clean. Divergence: settings-remerge re-produced code chosen as loom/load/settings-invalid-json (spec names the loom/load/settings-* family without pinning one).
2026-07-01 V4g-T — Pre-evaluation reload-failure integration tests (paired V4g impl): adds src/extension/reload-pre-eval.ts — the createReloadFailurePreEvalRouter(deps) seam (a stub returning V9b's ReloadFailureInjector wired to a no-op diagnostic sink so no loom-system-note fires) and the ReloadPreEvalDeps shape (registry + the V7d SystemNoteChannelDeps) — plus tests/pre-evaluation-reload-failure.test.ts with 4 failing tests pinning ERR-7 (errors-and-results/error-model.md#err-7, discovery/package-and-settings.md §"Watcher-time reload failures"). ERR-7: a synthetic watcher-time reload failure injected through V9b's single ReloadFailureInjector.injectReloadFailure interface — the registry-swap and loom-warp-reparse arms V9b wires (both surfacing loom/runtime/registry-swap-failed) plus V10d's settings-remerge arm (surfacing the loom/load/settings-invalid-json re-parse/re-merge diagnostic) — routes pre-eval onto the loom-system-note channel (customType = loom-system-note) with the fixed triggerTurn:false option, so no arm fires a turn or becomes an evaluation outcome; no live V10d/V9b watcher is stood up. Exercises both the re-parse/re-merge diagnostic arm and the loom/runtime/registry-swap-failed registry-swap arm through the one injection interface. Diagnostic codes anchored via REGISTRY_SWAP_FAILED_CODE / SETTINGS_REMERGE_FAILED_CODE; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 4 tests red on their own primary sendMessage-called assertion (the routed note never reaches the channel's pi.sendMessage) because the V4g pre-eval routing is absent — fail-red-for-the-intended-reason; the other 691 tests stay green.
2026-07-01 V11a-T — Binder-model resolution and strict-capability probe tests (paired V11a impl): adds src/binder/binder-model.ts — the resolveBinderModel(input) seam (inert stub → { resolved: false, diagnostics: [] }), computeBinderModelRecoveryNote(previouslyUnresolved) (inert stub → null), renderBinderModelRecoveryContent(looms) (the verbatim hot-reload recovery-note template renderer), reconcileBinderModelSettingsEdit(loaded, deps) (inert stub → []), loadPassResolveBinderModels(files, deps) (inert stub → []), the BINDER_MODEL_UNRESOLVED / _NOT_STRICT_CAPABLE / _STRICT_CAPABILITY_UNKNOWN code + message constants, and the StrictCapableProbe / BinderModelResolutionInput / BinderModelResolution / PreviouslyUnresolvedLoom / LoadedLoom / BinderModelLoadPass* shapes — plus tests/binder-model-resolution.test.ts with 10 failing tests. loom/load/binder-model-unresolved: fires when no reference matches — a bare modelId matched against Model<Api>.id and a provider/modelId reference matched against the short Model<Api>.provider + .id (never the api-shaped .api; worked example anthropic/claude-haiku, binder-model-parse-rule); a bypass-eligible loom skips both checks. strictCapable probe (three-valued, strict-capability-requirement): false → loom/load/binder-model-not-strict-capable (E, refused); undefined → loom/load/binder-model-strict-capability-unknown (W, registered); true → resolves. Two-step chain (§Binder model): with frontmatter bind_model: omitted, the merged looms.binderModel (V10c) resolves the binder model with no unresolved diagnostic — asserting the value reaches the binder from merged settings, not a hardcoded model. Hot-reload recovery note (binder-model-hot-reload; informational loom-system-note, no loom/load/* code): a reload recovering a previously-unresolved model emits the consolidated note (details.recovery.looms, display:true, verbatim content). BNDR-11: a looms.binderModel-only settings edit does not re-run binder-model resolution or the strictCapable probe for any already-loaded loom — each retains its previously-resolved handle until its next load. Single-matcher cross-resolution reconciliation (host-interfaces-core.md#model-registry-pin): the one load-pass matcher instance threads into both V6a's loom/load/model-unresolved resolution and this leaf's binder-model resolution — observed by single-source-of-construction (instance identity), not equivalence-of-outcome. Diagnostic Message strings sourced from diagnostics/code-registry-load.md per the Diagnostic message anchors rule; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 10 tests red on their own primary assertions (no diagnostic fires, no resolution, no recovery note, no handle retention, matcher never threaded) because the V11a body is absent — fail-red-for-the-intended-reason; the other 691 tests stay green (the 4 pre-existing V4g-T reds are unaffected).
2026-07-01 V11a — Binder-model resolution and strict-capability probe (paired with V11a-T): fills src/binder/binder-model.ts. resolveBinderModel(input) resolves the two-step chain (bind_model: frontmatter → merged looms.binderModel) through the shared exact-match matcher (binder-model-and-context.md §Binder model, binder-model-parse-rule); an empty chain, a no-match, or a cross-provider-ambiguous reference → loom/load/binder-model-unresolved E, and resolution short-circuits the strict-capability probe. On a resolved model the duck-typed three-valued strictCapable probe runs (strict-capability-requirement): false → loom/load/binder-model-not-strict-capable E (refused), undefined → loom/load/binder-model-strict-capability-unknown W (registered — the pinned production branch), true → admit. Bypass-eligible looms skip both checks. computeBinderModelRecoveryNote(previouslyUnresolved) re-runs the matcher-only resolution and, when ≥1 loom re-resolves to a model under the changed setting, emits the consolidated informational loom-system-note (recovery.looms in ascending Unicode code-point order, display:true, verbatim content via renderBinderModelRecoveryContent; no loom/load/* code, binder-model-hot-reload); returns null on empty recovery. BNDR-11: reconcileBinderModelSettingsEdit(loaded, deps) returns the loaded looms untouched WITHOUT invoking the resolver — a looms.binderModel-only edit re-runs neither resolution nor the probe for any already-loaded loom; each retains its previously-resolved handle until its next load. loadPassResolveBinderModels(files, deps) constructs the matcher once via createModelReferenceMatcher(modelRegistry) and threads that one instance into both V6a's parse({ modelMatcher }) and each resolveBinderModel({ matcher }), so the model: and binder-model paths bind the same instance (single-source-of-construction, instance identity, host-interfaces-core.md#model-registry-pin). Diagnostic Message strings anchored to diagnostics/code-registry-load.md; no new diagnostics-registry codes. No module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 10 V11a-T tests green; typecheck + lint clean. Divergence (see notes.md): recovery-note membership keys on the matcher-only "re-resolves to a model" step (not the strict-capability probe), per the spec's "makes no claim that a listed loom's next /reload succeeds". The 4 pre-existing V4g-T reds (unimplemented paired impl leaf) are unrelated and unaffected.
2026-07-01 V11c-T — Binder bypass and envelope-schema tests (paired V11c impl): adds src/binder/binder-envelope.ts — the buildBinderEnvelopeSchema(input) seam (inert stub → { anyOf: [] }), classifyBinderBypass(fields) (inert stub → { kind: "binder" }), applyBinderBypass(input) (inert stub → { bypassed: false, args: {} }), trimSlashArgumentWhitespace(raw) (inert stub → unchanged), binderFailureRowPrefix(kind) / renderBinderFailureRow(name, kind, message) (inert stubs → ""), plus BINDER_ENVELOPE_KINDS / BINDER_ENVELOPE_MESSAGE_MAX_LENGTH and the BypassParamsField / BinderBypassDecision / BinderEnvelopeSchema shapes — plus tests/binder-bypass-envelope.test.ts with 14 failing tests pinning binder/binder-bypass-and-envelope.md. BNDR-1: the envelope is a three-arm anyOf discriminated on kind (ok | needs_info | ambiguous), no arm folded away. BNDR-2: the ambiguous arm keeps candidates typed array<string> | null (type:["array","null"], string items) and required, so AJV accepts candidates:null. Relaxed copy: each defaulted field removed from the ok arm's args.required (types unchanged, additionalProperties:false kept; all-defaulted → required:[]); message and each candidates[i] carry the maxLength:500 model budget (not a user cap). Bypass (§Binder bypass): params absent/{} → no-params-bypass; exactly one required plain-string field → single-string-bypass (defaulted / non-string / optional / nullable / multi-field near-misses route to the binder); no-params check runs before single-string; applyBinderBypass yields the trimmed single-string value / empty no-params object without a binder call; slash-argument trimming strips ASCII whitespace only, preserving U+00A0. BNDR-3: the needs_info (argument binding needs more info) and ambiguous (ambiguous arguments) failure-mode template row prefixes stay distinct (rows sourced from determinism-cancellation-failure.md#failure-mode-templates-normative). No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 14 tests red on their own primary assertions (empty envelope, always-binder classification, not-bypassed args, empty prefixes) because the V11c body is absent — fail-red-for-the-intended-reason; the other tests stay green (the pre-existing V4g-T reds are unaffected).
2026-07-01 V11c — Binder bypass and dynamic envelope schema (paired with V11c-T): fills src/binder/binder-envelope.ts. buildBinderEnvelopeSchema(input) builds the three-arm anyOf discriminated on kind (ok | needs_info | ambiguous, BNDR-1), no arm folded; the ambiguous arm keeps candidates typed array<string> | null (type:["array","null"], string items) and required so AJV accepts candidates:null (BNDR-2); the ok arm's args is the relaxed lowered-params copy — each defaulted field removed from required (types unchanged, additionalProperties:false preserved, all-defaulted → required:[]); message and each candidates[i] carry the maxLength:500 model budget (not a user cap). classifyBinderBypass(fields) runs the no-params check (params absent/{}) before the single-string check (exactly one required plain-string field; defaulted / non-string / optional / nullable / multi-field near-misses route to the binder). applyBinderBypass yields the trimmed single-string value / empty no-params object without any binder or LLM call. trimSlashArgumentWhitespace strips ASCII slash-argument whitespace only, preserving U+00A0. BNDR-3: binderFailureRowPrefix / renderBinderFailureRow keep the distinct argument binding needs more info / ambiguous arguments failure-mode template row prefixes (rows sourced from determinism-cancellation-failure.md#failure-mode-templates-normative). No new diagnostics-registry codes. No module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 14 V11c-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds (unimplemented paired impl leaf) are unrelated and unaffected.
2026-07-01 V9j-T — Binder inference call and provider-error-mapping tests (paired V9j impl): adds src/binder/provider-error-mapping.ts — classifyProviderResponse(input) (inert sentinel stub → CancelledError), checkTypedQueryProviderSupport(input) (inert sentinel diagnostic stub), synthesizeUnsupportedProviderTransportError(provider) (wrong-sentinel stub), the TYPED_QUERY_UNSUPPORTED_PROVIDER_CODE / typedQueryUnsupportedProviderMessage(provider,model) / TYPED_QUERY_SUPPORTED_PROVIDER_APIS constants, and the ProviderClassifierInput / TypedQueryProviderCheckInput shapes — plus src/binder/binder-inference.ts — buildBinderCompleteCall(input) (inert triple stub), binderToolName(slug), BINDER_MESSAGE_CONTENT / BINDER_TOOL_DESCRIPTION — plus tests/binder-inference-provider-mapping.test.ts with 33 failing tests. Closes cka-34 (binder-inference.md §Binder inference call) and cka-35 (provider-error-mapping.md §Provider error mapping / §Provider seed-field mapping), and supplies the asserting test for the registry code loom/load/typed-query-unsupported-provider. cka-35 (classifier): per-provider overflow signatures (anthropic HTTP 400 /prompt is too long|.../, openai HTTP 400 or HTTP 200 stopReason error context_length_exceeded, mistral 400 /context.*length/, bedrock ValidationException even with no HTTP response) and HTTP 200 stopReason length → ContextOverflowError; deterministic two-numeric-run token extraction (larger→tokens_used, smaller→tokens_limit; zero/one/three+ runs → null; adjacent stray separators 1,,234 split into runs 1 and 234; mistral/bedrock counts always null); overflow-signature precedence over HTTP status / network-level. Every other classifier-reaching response → TransportError with retryable by transport class (true for 5xx / 429 / network-level http_status:null, false for non-429 4xx / HTTP 200 body-envelope error / non-200 2xx / surfaced 3xx / unrecognised stop reason), provider = api-shaped Model<Api>.api. cka-34 (complete() envelope): fixed single user message literal "Bind the slash-command arguments now.", one forced structured-output tool (__loom_bind_<slug>, fixed description, Type.Unsafe-wrapped envelope schema), options.toolChoice {type:"tool",name}, temperature:0, signal = loomAbort.signal, onResponse capture callback, and the per-provider seed field (openai-completions→seed, mistral→random_seed, anthropic-messages/amazon-bedrock omitted). loom/load/typed-query-unsupported-provider (W): surfaced for a typed query on an unsupported provider (message sourced from diagnostics/code-registry-load.md), null for a supported provider or a loom with no typed query; runtime guard synthesises TransportError { kind:"transport", message:"<provider> does not support forced tool-use; typed queries unavailable", http_status:null, provider, retryable:false }. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint + H2a/H3a gates clean; the 33 tests red on their own primary assertions because the V9j body is absent — fail-red-for-the-intended-reason; the other tests stay green (4 pre-existing V4g-T reds unaffected). Divergences (see notes.md): (1) the spec seed-field/provider-error tables key on api-shaped mistral / amazon-bedrock but pi-ai's Api union exposes mistral-conversations / bedrock-converse-stream (mistral / amazon-bedrock are Provider values); tests encode the spec's four api-strings verbatim. (2) The spec pins a binder-tool label "Loom binder envelope" ToolDefinition field, but pi-ai's Context.tools element (Tool) is {name,description,parameters} with no label; the complete() envelope omits label.
2026-07-01 V9j — Binder inference call and provider-error mapping (paired with V9j-T): fills src/binder/provider-error-mapping.ts and src/binder/binder-inference.ts. cka-35 classifier (classifyProviderResponse): per-provider overflow signatures (anthropic HTTP 400 /prompt is too long|exceeds .* context window|maximum context length/i; openai HTTP 400 or HTTP 200 stopReason:"error" /maximum context length|context_length_exceeded/i; mistral HTTP 400 /context.*length/i; bedrock /(input is too long|context window)/i at any captured status including network-level http_status:null) and HTTP 200 stopReason:"length" → ContextOverflowError; deterministic two-numeric-run token extraction (larger→tokens_used, smaller→tokens_limit; zero/one/three-or-more runs → null; adjacent stray separators 1,,234 split into runs 1 and 234; mistral/bedrock counts always null); overflow-signature precedence over HTTP status and the network-level class; every other classifier-reaching response → TransportError with retryable populated by transport class (true for 5xx / 429 / network-level http_status:null, false for non-429 4xx / HTTP 200 body-envelope error / non-200 2xx / surfaced 3xx / unrecognised stop reason), provider = api-shaped Model<Api>.api. cka-34 envelope (buildBinderCompleteCall): fixed single user message literal "Bind the slash-command arguments now." (fixed timestamp:0), exactly one forced structured-output tool (__loom_bind_<slug>, fixed description, Type.Unsafe-wrapped envelope schema), options.toolChoice {type:"tool",name}, temperature:0, signal = loomAbort.signal, onResponse capture callback, and the per-provider seed field (openai-completions→seed, mistral→random_seed, anthropic-messages/amazon-bedrock omitted). checkTypedQueryProviderSupport emits loom/load/typed-query-unsupported-provider (W) only for a typed query on a provider outside the supported set (message anchored to diagnostics/code-registry-load.md), null for a supported provider or a loom with no typed query; synthesizeUnsupportedProviderTransportError returns TransportError { kind:"transport", message:"<provider> does not support forced tool-use; typed queries unavailable", http_status:null, provider, retryable:false }. Lookup tables Object.frozen (no module-level mutable state, H2a clean); no broad catch / ambient primitive / Promise combinator / Sync call. All 33 V9j-T tests green; typecheck + lint clean. Divergence (see notes.md): the binder user-message timestamp is fixed at 0 — pi-ai's UserMessage type requires it, the spec is silent, and a fixed constant keeps the deterministic complete() call identical per run without a WallClock read. The 4 pre-existing V4g-T reds (unimplemented paired impl leaf) are unrelated and unaffected.
2026-07-01 V11d-T — Binder system-prompt builder tests (paired V11d impl): adds src/binder/binder-system-prompt.ts — buildBinderSystemPrompt(input) (inert stub → ""), renderBinderParamLine(field) (inert stub → ""), and the BuildBinderSystemPromptInput / SystemPromptParamField / ParamRequirement / SystemPromptSessionContext shapes — plus tests/binder-system-prompt.test.ts with 34 failing tests. Closes the code-keyed obligation area cka-45 (binder/binder-bypass-and-envelope.md §"System-prompt structure (normative)"). Covers the eight structural items: (1) exactly one Loom: /<name> identity line; (2) conditional Description: <d> line (present + absent + empty halves); (3) conditional Argument hint: <v> line (present + absent + empty); (4) Parameters: header + per-field lines in declaration order, exactly-two-U+0020 indent, whole-block omission when params empty; (5) User arguments: <raw> with ASCII slash-argument trim (U+00A0 preserved) and the empty-args User arguments:<space> trailing-space form; (6) Session-context block framing — opening line begins with the literal token Recent session context and ends with :, then the compact-transcript body, then a terminating blank line so the block ends \n\n (omitted when no session context); (7) all three envelope kind tokens ok / needs_info / ambiguous; (8) the no-invent-defaults line (literal defaulted plus one of Do not / omit / skip). Plus the Type display reference renderings (8-row surface-type table incl. string | null and Cat | Dog), the Default-literal rendering forms (Severity.High, "hello", [1, 2, 3], []), and the byte-exact Parameter-line reference renderings (three with-description rows + the description-omitted "  language (string) required" ending after required with no trailing space and no em-dash). Type surface strings, default-literal surface strings, and the compact-transcript body are builder inputs (surface rendering delegated to V2a; transcript body to V11b / walk to V11i), so V11d's job is the structural composition and the per-field-line format. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint + H2a/H3a gates clean; all 34 tests red on their own primary assertions because the V11d body is absent — the conditional-rule negative-half tests carry a positive Loom-identity-line baseline so they red against the empty stub rather than passing vacuously — fail-red-for-the-intended-reason; the other 748 tests stay green (4 pre-existing V4g-T reds unaffected).
2026-07-01 V11d — Binder system-prompt builder (paired with V11d-T): fills src/binder/binder-system-prompt.ts. renderBinderParamLine(field) renders one per-field line — exactly two U+0020 SPACE indent, then <wire-name> (<type>) <requirement>, where <requirement> is the literal required or default=<literal> (literal emitted verbatim after default=, the Default-literal rendering forms Severity.High / "hello" / [1, 2, 3] / []), and the — <description> segment (separator exactly U+0020 U+2014 U+0020) is appended iff description is present and non-empty and omitted entirely otherwise so the line ends immediately after the requirement with no trailing whitespace/em-dash; the Type display surface strings (string, integer, boolean, Severity, Author, array<integer>, string | null, Cat | Dog) and the four byte-exact Parameter-line reference renderings (three with-description rows plus the description-omitted "  language (string) required") reproduce verbatim. buildBinderSystemPrompt(input) assembles the eight structural items of cka-45 (binder/binder-bypass-and-envelope.md §"System-prompt structure (normative)"): (1) exactly one Loom: /<name> identity line; (2) conditional Description: <d> (emitted iff non-empty); (3) conditional Argument hint: <v> (iff non-empty); (4) Parameters: header (unindented) + per-field lines in declaration order, whole block omitted when params absent/empty; (5) User arguments: <raw> always present, with leading/trailing ASCII slash-argument whitespace stripped via the shared trimSlashArgumentWhitespace (U+00A0 preserved) and the empty-args User arguments:<space> trailing-space form; (6) Session-context block emitted iff input carries a transcript body — opening line begins with the literal token Recent session context and ends with :, then the compact-transcript body, then a terminating blank line so the block ends \n\n (omitted when no session context); (7) all three envelope kind tokens ok / needs_info / ambiguous listed; (8) a single no-invent-defaults line containing the literal defaulted plus one of Do not / omit / skip. Wording of the intro / envelope / no-invent lines is non-normative and follows the spec's illustrative example. Type surface strings, default-literal surface strings, and the compact-transcript body are builder inputs (surface rendering delegated to V2a; transcript body to V11b; truncation walk to V11i), so V11d owns only the structural composition and the per-field-line format. No module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. All 34 V11d-T tests green; typecheck + lint clean; no new diagnostics-registry codes. The 4 pre-existing reds in an unrelated subsystem (fail on HEAD without this change) are unaffected.
2026-07-01 V11g-T — Fill-if-absent defaulting and post-default-merge AJV validation tests (paired V11g impl): adds src/binder/defaulting.ts — fillDefaultsAndRevalidate(input) (inert stub → { args: {}, defaultedWireNames: [], validation: { ok: true } }) plus the DefaultedField / FillDefaultsInput / PostMergeValidation / FillDefaultsResult shapes — plus tests/defaulting-revalidation.test.ts with 3 failing tests. Closes the code-keyed obligation area cka-40 (binder/defaulting-system-note-echo.md §Defaulting, anchor #post-default-merge-ajv-validation). Covers: (1) an absent wire name takes its declared default in the merged args and is reported default-supplied; (2) a present wire name is preserved unchanged (no default applied) and is not reported default-supplied — fill-if-absent keyed on the field's wire name; (3) the compiled SchemaValidator re-validates the MERGED args (default filled in), called exactly once with the merged object, and its verdict is surfaced. The post-merge validation is witnessed with a spy CompiledValidator recording the value handed to validate(). Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 3 tests red on their own primary assertions (empty merged args ⇒ absent default not filled and present value not preserved; validator never invoked ⇒ zero recorded calls) because the V11g body is absent — fail-red-for-the-intended-reason; the other tests stay green (4 pre-existing V4g-T reds unaffected).
2026-07-01 V11g — Fill-if-absent defaulting and post-default-merge AJV validation (paired with V11g-T): fills src/binder/defaulting.ts. fillDefaultsAndRevalidate(input) merges the binder-returned args with the loom's defaulted params: fields by fill-if-absent keyed on each field's wire name — a present wire name is preserved unchanged and not reported default-supplied (even for a defaulted field); an absent wire name takes its declared default and is recorded in defaultedWireNames (driving the echo's (default) tagging) — then runs the post-default-merge AJV validation: the compiled SchemaValidator's validate() re-validates the merged args (defaults filled in) against the lowered params schema and the verdict is surfaced on validation. Absence keyed with Object.prototype.hasOwnProperty.call so an explicit undefined-valued wire name is treated present. Closes cka-40 (binder/defaulting-system-note-echo.md §Defaulting, anchor #post-default-merge-ajv-validation). All 3 V11g-T tests green; typecheck + lint clean; no new diagnostics-registry codes; no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. The 4 pre-existing reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V11h-T — Argument-echo formatter tests (paired V11h impl): adds src/render/argument-echo.ts — the renderEchoValue(value, type) per-value seam and the renderArgumentEcho(input) whole-line seam (both inert stubs → a sentinel that equals no expected rendering, so the BNDR-6a "" row still reds rather than passing vacuously), plus the EchoType static-type descriptor and the EchoField / EchoParam / ArgumentEchoInput shapes — plus tests/argument-echo.test.ts with 26 failing tests pinning binder/defaulting-system-note-echo.md §"Echo policy". BNDR-6: the 24 reference renderings bndr-6a … bndr-6x reproduce exactly — string quote predicate (empty → "", unquoted [A-Za-z0-9_.-], space/`=`/non-ASCII-letter force quoting, only " → \" and \ → \ escaped); object {first-field-value, …} (schema first field; discriminated-union discriminator-first {cat, …}; single-field fixed , … marker; inline anonymous object leftmost field; array-first {[a, "b c"], …}; nested-object {{Whiskers, …}, …}); array [] and [a, "b c"] with per-element quoting; integer/number via the composed V2d renderCanonicalNumber (42, 3.14, 1e21 → 1000000000000000000000, 1e-8 → 0.00000001, -0 → 0); true/false/null lowercase literals; enum underlying-wire string (High, "needs review") through the quote predicate. Echo (default) annotation (§Defaulting fill-if-absent): a default-supplied field is tagged focus_areas=[] (default); a binder-supplied value for a defaulted field is rendered untagged (asserted as an exact-line equality so the untagged facet reds on the stub rather than passing vacuously). The integer-vs-number kind and object field order are carried on the EchoType descriptor (a builder input per V2d's caller-derives-the-kind contract), not inferred from runtime shape. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 26 tests red on their own primary assertions (the sentinel != each pinned observable byte output) because the V11h body is absent — fail-red-for-the-intended-reason; the other 785 tests stay green (4 pre-existing V4g-T reds unaffected).
2026-07-01 V11h — Argument echo formatter (paired with V11h-T): fills src/render/argument-echo.ts. renderEchoValue(value, type) is the data-driven per-value renderer of binder/defaulting-system-note-echo.md §"Echo policy" — string quote predicate (unquoted iff non-empty and every code point matches [A-Za-z0-9_.-], else quoted with only " → \" and \ → \ escaped, empty → ""); integer/number via the composed V2d renderCanonicalNumber selecting BNDR-4 vs BNDR-5 from the EchoType descriptor's static kind (never runtime integrality); boolean/null lowercase literals; enum underlying-wire string through the quote predicate; array rule ([], [a, b, c] for ≤3, [a, b, c, …+N more] for ≥4); object rule {first-field-value, …} (first field from the descriptor's declaration order, fixed , … marker, rendered recursively). renderArgumentEcho(input) composes Running /<name>: <fields> — each field name=<value>, comma-space separated in declaration order, with (default) appended only for a field that took its declared default (fill-if-absent per §Defaulting), untagged for a binder-supplied value. Reproduces the BNDR-6 reference renderings bndr-6a … bndr-6x exactly. All 26 V11h-T tests green; typecheck + lint clean; no new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. The 4 pre-existing reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V11e-T — Binder system-note rendering + determinism tests (paired V11e impl): adds src/binder/system-note.ts — the shared line-discipline seam (sanitizeSystemNoteSubstring rule 1, capSystemNote + SYSTEM_NOTE_CODEPOINT_CAP rule 2, renderFailureNote rule-3 grammar, classifyModelContent rule 4, renderAmbiguousSuffix rule 5; all inert UNIMPLEMENTED-sentinel / always-present stubs) — and src/binder/binder-seed.ts — deriveBinderSeed(name) (inert -1 sentinel) — plus tests/binder-system-note-determinism.test.ts with 6 failing tests pinning binder/defaulting-system-note-echo.md §"System-note rendering" and binder/determinism-cancellation-failure.md §Determinism. Closes the code-keyed obligation areas cka-41 (five line-discipline rules: rule-1 tab+spaces→one U+0020 with U+00A0 preserved against the normative reference note; rule-2 120-code-point cap at scalar boundaries with trailing … incl. astral run, no … when ≤120; rule-3 loom /<name>: <fixed-phrase> — <suffix> failure grammar + Running /<name>: <args> echo boundary; rule-4 whitespace-only message / all-empty candidates → empty-malformed; rule-5 ambiguous renders only message, never candidates) and cka-42 (FNV-1a reference vectors code-review→0x7ba86b63, hello→0x4f9f2cab, a→0xe40c292c; determinism; temperature:0 on the V9j-built call). Inert stub seams: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 6 tests red on their own primary assertions (sentinel != each pinned output; -1 != each seed vector) because the V11e body is absent — fail-red-for-the-intended-reason; the other tests stay green (4 pre-existing V4g-T reds unaffected).
2026-07-01 V11e — Binder system-note rendering + determinism (paired with V11e-T): fills src/binder/system-note.ts and src/binder/binder-seed.ts. system-note.ts closes cka-41 (binder/defaulting-system-note-echo.md §"System-note rendering"): sanitizeSystemNoteSubstring (rule 1) collapses runs of the enumerated ASCII-whitespace set {U+0009,000A,000B,000C,000D,0020} to one U+0020 and trims only U+0020 at the edges (not JS String.prototype.trim, which would also strip U+00A0), so a tab+three-spaces run collapses to one space while U+00A0 is preserved verbatim; capSystemNote (rule 2) caps at SYSTEM_NOTE_CODEPOINT_CAP=120 code points via Array.from scalar iteration (never UTF-16 code units, so surrogate pairs are not split), keeping the first 119 scalars + a trailing … (counts toward the cap) for a >120 note and returning a ≤120 note unchanged; renderFailureNote (rule 3) composes loom /<name>: <fixed-phrase> — <sanitised-suffix> (spaced U+2014 boundary), sanitising the suffix and capping the whole note; classifyModelContent (rule 4) returns empty-malformed when the message is empty after rule-1 stripping or a non-empty candidates array is all-empty after stripping (an absent or [] candidates array is not itself a trigger); renderAmbiguousSuffix (rule 5) surfaces only the sanitised message, never candidates. binder-seed.ts closes cka-42 (binder/determinism-cancellation-failure.md §Determinism): deriveBinderSeed(name) is 32-bit FNV-1a (offset basis 0x811c9dc5, prime 0x01000193 via Math.imul) over the UTF-8 bytes of the bare command name, masked >>> 0 — reference vectors code-review→0x7ba86b63, hello→0x4f9f2cab, a→0xe40c292c reproduced exactly; temperature:0 and the provider-mapped seed on the constructed call are owned by the V9j buildBinderCompleteCall (unchanged here). A module-level stateless TextEncoder const is a pure helper (no cross-invocation mutable state, H2a clean); no broad catch / ambient primitive / Promise combinator / Sync call. All 6 V11e-T tests green; typecheck + lint clean; no new diagnostics-registry codes. The 4 pre-existing reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V13a-T — Query render / escapes / stringification tests (paired V13a impl): adds src/render/query-render.ts — lexQueryTemplate(source) (inert stub → single raw text part, no diagnostics, terminated:true), renderTemplateText(text) (identity stub), stringifyInterpolatedValue(value, type) (inert stub → { ok:true, text:"" }), emptyTemplateWarning(staticBody) / renderEmptyShortCircuit(renderedText) (inert no-op stubs), the ILLEGAL_TEMPLATE_ESCAPE_CODE / UNTERMINATED_TEMPLATE_CODE / EMPTY_TEMPLATE_CODE / INTERPOLATED_RESULT_CODE codes + their registry-anchored message strings (illegalTemplateEscapeMessage / UNTERMINATED_TEMPLATE_MESSAGE / EMPTY_TEMPLATE_MESSAGE / INTERPOLATED_RESULT_MESSAGE / EMPTY_TEMPLATE_RENDER_MESSAGE), and the QueryTemplatePart / QueryTemplateLexResult / InterpolationType / StringifyResult shapes — plus tests/query-render.test.ts with 26 tests pinning query/query-forms.md (QRY-6, QRY-7) and query/query-escapes-stringification.md (QRY-17, QRY-18). QRY-7: the full normative newline-trim→dedent vector table (10 vectors — CR/CRLF→LF first, LF-only line splitting, {U+0020,U+0009} dedent alphabet, whitespace-only-line normalisation, newline-trim-first ordering). QRY-17: recognised-escape resolution (\` / \$ / \ / \n), ${…} interpolation splitting, loom/parse/illegal-template-escape on a backslash+unrecognised-char pair, loom/parse/unterminated-template at EOF inside a body. QRY-18: per-Loom-static-type stringification (string verbatim, integer/number canonical decimal, boolean/null literals, enum bare wire value unquoted, array/object compact JSON.stringify with outbound wire-name translation) and the static loom/parse/interpolated-result rejection of a Result-valued interpolation. QRY-6: the parse-time loom/parse/empty-template warning over the static body (escapes NOT applied; explicit \n literal suppression hatch) and the runtime short-circuit to ValidationError{kind:"validation", cause:"empty_template", message:"rendered query template is empty", attempts:0, validation_errors:[], raw_response:null} without a provider round-trip and never the respond-repair path (ASCII whitespace set {U+0009,000A,000B,000C,000D,0020} only; a U+00A0-only render issues a turn). Diagnostic Message strings sourced from diagnostics/code-registry-parse.md per the Diagnostic message anchors rule; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 25 of 26 tests red on their own primary assertions (identity render pipeline, no escape/termination diagnostics, empty stringification, no-op degenerate defences) because the V13a body is absent — fail-red-for-the-intended-reason; the one green test is the single-line normative vector 5 "  hi"→"  hi" that coincides with the identity stub. Full suite 818 green + 29 red (25 new + 4 pre-existing V4g-T reds unaffected). Divergence (see notes.md): the leaf prose says "eight normative vectors" but query-forms.md QRY-7 now pins ten; tested all ten (the binding is the spec table).
2026-07-01 V13a — Query render / escapes / stringification (paired with V13a-T): fills src/render/query-render.ts. lexQueryTemplate(source) (QRY-17) resolves the recognised escapes (\` / \$ / \ / \n / \t / \r), splits unescaped ${…} interpolations with brace-depth tracking (a nested } does not close early), coalesces literal runs into single text parts, and emits loom/parse/illegal-template-escape (E) on a backslash+unrecognised-char pair and loom/parse/unterminated-template (E) at EOF inside a body. renderTemplateText(text) (QRY-7) applies newline-trim then dedent in that fixed order: CR/CRLF→LF normalisation first; a no-LF single-line input is returned verbatim (dedent no-op — vector 5 "  hi"→"  hi"); otherwise a single leading and a single trailing LF are trimmed and the longest common leading {U+0020,U+0009} prefix of the non-blank lines is stripped, whitespace-only lines normalised to empty lines and excluded from the common-prefix walk — reproduces all ten normative vectors incl. mixed tab/space no-common-prefix (4), CRLF→LF (9), and non-ASCII-leading-ws-is-content (10). stringifyInterpolatedValue(value, type) (QRY-18) renders by the Loom static type: string verbatim, integer/number via the composed V2d renderCanonicalNumber (BNDR-4/BNDR-5), boolean/null lowercase literals, enum bare wire value unquoted, array/object compact JSON.stringify (outbound wire-name translation applied when sidecars+rootDef supplied, else direct), and a static loom/parse/interpolated-result (E) rejection of a Result-typed expr. emptyTemplateWarning(staticBody) (QRY-6) warns loom/parse/empty-template (W) over the notionally newline-trim+dedent-normalised static body with escapes NOT applied (so a literal two-char \n sequence suppresses the warning); renderEmptyShortCircuit(renderedText) short-circuits to ValidationError{kind:"validation", cause:"empty_template", message:"rendered query template is empty", attempts:0, validation_errors:[], raw_response:null} when the fully-rendered text is empty or drawn solely from the ASCII whitespace set {U+0009,000A,000B,000C,000D,0020} (a U+00A0-only render issues a turn), never the respond-repair path. All 26 V13a-T tests green; typecheck + lint clean; no new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. The 4 pre-existing reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V6d-T — `system:` template-interpolation tests (paired V6d impl): adds src/parser/system-interpolation.ts — checkSystemInterpolation(input) (inert stub → { diagnostics: [] }, no template), renderSystemPrompt(input) (inert stub → empty text), the SystemParamType (no Result arm) / SystemTemplate / SystemTemplatePart shapes, and the five diagnostic-code + registry-message anchors (SYSTEM_ON_PROMPT_MODE_*, SYSTEM_INTERP_{NOT_PATH,UNKNOWN_PARAM,BAD_FIELD,UNTERMINATED}_*) — plus tests/system-interpolation.test.ts with 24 failing tests pinning frontmatter/frontmatter-fields-b-and-templates.md §"system Interpolation" and query/query-escapes-stringification.md (QRY-18). Covers the four loom/parse/system-interp-* codes (not-path on ${arr[0]}/${a+b}/${f(x)}/${a?.b}/${"x"}; unknown-param on an undeclared head Ident; bad-field on an unreachable object-field step and on descent into an array or un-narrowed discriminated union; unterminated on an unclosed ${); \${ suppresses interpolation and renders literal ${…} with no diagnostic; a Path-only ${param}/${param.field} resolves against the validated params object; loom/parse/system-on-prompt-mode (registry message asserted) on a mode: prompt loom; the resolve-then-stringify path feeds each distinct param-resolvable static type (string; integer; number finite + NaN/Infinity/-Infinity; boolean; null → literal null; enum wire value; array<T>; schema-typed object with wire-name translation) into the shared stringifyInterpolatedValue renderer (each vector asserts parity with the shared renderer plus the concrete canonical string); and that Result<T, E> never arises from this surface. Diagnostic Message strings anchored to diagnostics/code-registry-parse.md; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint + H2a/H3a gates clean; all 24 tests red on their own primary assertions (absent diagnostic, undefined template, empty rendered text) because the V6d body is absent — fail-red-for-the-intended-reason; the other 843 tests stay green (4 pre-existing V4g-T reds unaffected).
2026-07-01 V6d — `system:` template interpolation (paired with V6d-T): fills src/parser/system-interpolation.ts. checkSystemInterpolation(input) rejects system: on a mode: prompt loom with loom/parse/system-on-prompt-mode (registry message, no template); otherwise scans the value honouring the \${ escape (only that exact sequence suppresses interpolation → literal ${; \ is verbatim elsewhere) and applies four per-body checks — loom/parse/system-interp-unterminated (unclosed ${), loom/parse/system-interp-not-path (body not Ident ('.' Ident)* — ${arr[0]}/${a+b}/${f(x)}/${a?.b}/${"x"}), loom/parse/system-interp-unknown-param (undeclared head Ident), loom/parse/system-interp-bad-field (a .Ident step naming no reachable object field, or descent into an array / un-narrowed discriminated union). A validated path captures its terminal SystemParamType mapped to the canonical-table InterpolationType; a template is returned only when no error-severity diagnostic fired. renderSystemPrompt(input) resolves each path against the validated params object and feeds the value through the shared stringifyInterpolatedValue renderer (QRY-18), concatenating literal runs — string/integer/number(finite+NaN/±Infinity)/boolean/null→null/enum-wire/array<T>/schema-typed-object(compact JSON + outbound wire-name translation) render identically to the @`...` surface; Result<T,E> never arises (params: types exclude it). Path bodies are tokenised into segments (not a ${param} regex), preserving the deferred richer-expression seam. No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 24 V6d-T tests green; typecheck + lint clean. The 4 pre-existing reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V13b-T — Query schema-inference tests (paired V13b impl): adds src/parser/query-schema-inference.ts — inferQuerySchema({frames, explicit}) (inert stub → an unimplemented sentinel schema so both the sink and the untyped expectations red), checkExplicitSchemaMismatch({ascription, annotation, env, site}) (inert stub → a sentinel diagnostic so both the firing and the no-warning vectors red), and prunePerQueryDefs(doc) (identity stub → prunes nothing so an unreachable $def reds), plus the InferredSchema / SchemaSinkFrame / QueryDefsDocument shapes and the EXPLICIT_SCHEMA_MISMATCH_CODE / EXPLICIT_SCHEMA_MISMATCH_MESSAGE anchors — plus tests/query-schema-inference.test.ts with 17 failing tests pinning query/query-forms.md (QRY-2 typed form, QRY-3 override rule, the §"Schema inference algorithm" shallow outward walk, QRY-4 explicit form) and schema-subset.md §"Lowering Algorithm" step 4 (SUBS code-keyed area). Inference walk — innermost sink wins: binding-annotation sink across the postfix ? and let RHS; the nearest call-site parameter type over an outer binding annotation; an inner call boundary hiding the outer parameter; an array-literal element inheriting the array<T> element type. Crossed/stopped rules: a binary operator and the match scrutinee are opaque (untyped even under an outer sink); a .loom tail supplies no sink; a no-sink query is untyped. QRY-3/QRY-4: an explicit @<Schema> overrides the walk regardless of position (including an opaque match scrutinee). loom/parse/explicit-schema-mismatch: the four normative vectors (integer⊑number no-warning; number⋢integer fires with the registry code+message; reflexive no-warning; variant-to-union no-warning) plus the one-directional skip when an operand is past the parser's static view. SUBS Lowering step 4: the per-query request document's $defs pruned to the transitive reachability closure from the response-schema root (unreachable defs dropped; recursive references terminate). Diagnostic Message anchored to diagnostics/code-registry-parse.md; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 17 tests red on their own primary assertions because the V13b body is absent — fail-red-for-the-intended-reason; the other 866 tests stay green (5 pre-existing reds in unrelated subsystems — 1 H2a gate + 4 V4g-T — red on HEAD without this change, are unaffected).
2026-07-01 V13b — Query schema inference (paired with V13b-T): fills src/parser/query-schema-inference.ts. inferQuerySchema({frames, explicit}) (QRY-2/QRY-3) returns an explicit @<Schema> ascription directly (override, any position); otherwise the §"Schema inference algorithm" shallow walk searches frames innermost-first for the nearest type sink — crossing paren/propagate/ternary (transparent) and each array-literal (unwrapping one array<T> level off the resolved sink), taking a let binding annotation / typed call-arg parameter / declared fn-return type as the sink, stopping with no sink at a stop (opaque operator, match scrutinee, if/while condition) or an untyped call-arg (call boundary), continuing past a return-type-less fn-return (a .loom supplies no sink), and returning undefined (untyped) on an exhausted or stopped walk. checkExplicitSchemaMismatch({ascription, annotation, env, site}) (QRY-4 §"Explicit form") emits loom/parse/explicit-schema-mismatch (W) iff ascription ⋢ annotation via the composed V2b checkCompatible — one-directional (safe widening silent) and skipped when an operand is past the parser's static view ("unknown"); the four normative vectors pass (integer⊑number no-warning; number⋢integer fires with the registry code+message; reflexive no-warning; variant-to-union no-warning). prunePerQueryDefs(doc) (schema-subset.md §"Lowering Algorithm" step 4) BFS-walks the response-schema root's $ref reachability graph (a visited set terminates recursive references) and keeps only the reachable $defs, pruning the unreachable ones. Registry Message string sourced from diagnostics/code-registry-parse.md per the Diagnostic message anchors rule; no new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 17 V13b-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V13g-T — Discarded-query result discipline + discard observability tests (paired V13g impl): adds src/runtime/query-discard.ts — checkDiscardedQueryResult(stmt) (inert stub → undefined, never fires), buildDiscardEvent(error, input) (inert stub → a sentinel RuntimeEvent preserving neither kind/message nor discard_site), and emitDiscardObservability(input, deps) (inert stub → unconditionally emits one sentinel display:true event regardless of Ok/Err), plus the DISCARDED_QUERY_RESULT_CODE/_MESSAGE/_HINT anchors and the QueryStatement / QueryStatementDisposition / DiscardForm / DiscardedOutcome / DiscardEmitInput shapes — plus tests/query-discard.test.ts with 5 failing tests pinning query/query-escapes-stringification.md (QRY-19, QRY-20). QRY-19: a bare @`...` expression-statement fires loom/parse/discarded-query-result (E) at its location while the ?-propagate, let _ =-discard, and let x = ...?-bind forms (and a non-query bare statement) do not. QRY-20: a discarded query settling to Err emits exactly one operator-facing loom-system-note (group-A details.event, display:false, empty companion content) preserving the discarded Err's kind/message and stamping discard_site with the discarding let _ = location (or the tail @`...` expression location for the void-tail form); a discarded Ok emits nothing for either form; buildDiscardEvent copies kind/message and stamps discard_site. Diagnostic Message anchored to diagnostics/code-registry-parse.md per the Diagnostic message anchors rule; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 5 tests red on their own primary assertions because the V13g body is absent — fail-red-for-the-intended-reason; the other 884 tests stay green (4 pre-existing V4g-T reds unaffected). Divergence (see notes.md): tested the registry Message string, not the leaf/spec-prose phrasing of the discarded-query message.
2026-07-01 V13g — Discarded-query result discipline + discard observability (paired with V13g-T): fills src/runtime/query-discard.ts. checkDiscardedQueryResult(stmt) (QRY-19) returns loom/parse/discarded-query-result (E) iff a must-use @`...` query result sits in bare expression-statement position (stmt.isQuery && disposition === "bare-expr-statement"); the ?-propagate, let _ =-discard, and let x = ...?-bind forms (and any non-query statement) return undefined. Diagnostic Message/Hint sourced from diagnostics/code-registry-parse.md per the Diagnostic message anchors rule. buildDiscardEvent(error, input) (QRY-20) copies the discarded Err's kind/message, stamps discard_site with the discard location, carries query_site when known, and preserves attempts (validation) / tokens_used (context_overflow) where the variant defines them. emitDiscardObservability(input, deps) (QRY-20) returns without emitting on a discarded Ok; on a discarded Err it builds the event and routes it through the always-log loom-system-note channel via emitRuntimeEvent with topLevelCascade: false, so the note carries display: false and content: "". No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 5 V13g-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15a-T — Invocation-core tests (paired V15a impl): adds src/runtime/invocation.ts — checkInvokePathContainment(deps, resolvedPath, activeRoots) (inert stub → {canonicalPath:"", within:false}), the load-time surface checkInvokePathAtLoad(input) and the invocation-time re-check recheckInvokePathAtRuntime(input) (both inert stubs → a "within" verdict with "" canonical path, emitting neither the escape diagnostic nor the InvokeInfraError), and runStaticResolutionPass(deps, entryPath) (inert stub → empty cache, never calls parseAndLower), plus the INVOKE_PATH_ESCAPE_CODE/invokePathEscapeMessage/INVOKE_PATH_ESCAPE_HINT anchors and the InvokePathCheckDeps/InvokePathContainment/InvokePathSurfaceInput/LoadTimeInvokePathResult/RuntimeInvokePathResult/ParsedCallee/StaticResolutionDeps/StaticResolutionResult shapes — plus tests/invocation-core.test.ts with 10 failing tests pinning invocation.md (INV-1 §Resolution, §Static resolution) and implementation-notes.md "Static-resolution load pass". INV-1: the shared realpath+segment-boundary containment check decides on the byte-exact realpath output (in-root within; sibling-prefix escape rejected — trailing-separator requirement load-bearing; symlink-farm resolving outside every root rejected on its realpath output; union-of-roots cross-root composition legal); the load-time check and the invocation-time re-check share identical semantics; a load-time escape emits loom/load/invoke-path-escape (E, registry Message with the literal <path>), an invocation-time escape surfaces on BOTH channels (diagnostic + Err(InvokeInfraError{cause:"load_failure", callee_path})), and the re-check reads the currently-active roots so a hot-reload that removed the relied-upon root fails closed. Static-resolution: the per-pass parse cache walks transitively from the entry loom across literal invoke paths and .loom tools: entries (diamond graph), parsing/lowering each visited file exactly once. Diagnostic Message anchored to diagnostics/code-registry-load.md per the Diagnostic message anchors rule; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 10 tests red on their own primary assertions ("" != each expected realpath output; "within" != "escape"; empty cache / zero parseAndLower calls) because the V15a body is absent — fail-red-for-the-intended-reason; the other tests stay green (4 pre-existing V4g-T reds in an unrelated subsystem, red on HEAD without this change, are unaffected).
2026-07-01 V15a — Invocation core (paired with V15a-T): fills src/runtime/invocation.ts. checkInvokePathContainment(deps, resolvedPath, activeRoots) (INV-1) realpath-normalises the callee and each active root (forward-slash-normalised, no independent case-folding) and decides segment-boundary containment on the byte-exact realpath output — within iff the callee equals a root or begins with it followed by a single separator (trailing-separator requirement rejects sibling-prefix escape; union of active roots keeps cross-root composition legal). checkInvokePathAtLoad(input) returns within or an escape carrying the loom/load/invoke-path-escape diagnostic; recheckInvokePathAtRuntime(input) shares the identical containment check and on escape surfaces on BOTH channels — the diagnostic AND Err(InvokeInfraError{cause:"load_failure", callee_path}) — reading the currently-active roots so a hot-reload that dropped the relied-upon root fails closed. runStaticResolutionPass(deps, entryPath) canonicalises the entry then walks transitively across literal invoke paths and .loom tools: entries into a per-pass parse cache keyed on realpath output, calling the injected parseAndLower exactly once per canonical path (diamond dedup). Diagnostic Message/Hint sourced from diagnostics/code-registry-load.md per the Diagnostic message anchors rule; no new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 10 V15a-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V6c-T — `tools:` callable-set + resolution-snapshot tests (paired V6c impl): adds src/parser/callable-set.ts — resolveCallableSet(input) (inert stub → registers an empty, unfrozen snapshot, raises no diagnostic), plus the ToolsField (absent / comma-scalar / list spellings), CallableSetDeps (resolvePiTool / resolveLoomCallee / reservedNames), ResolvedPiTool / ResolvedLoomCallee / ResolvedCallable, CallableSetSnapshot, and CallableSetResult shapes — plus tests/callable-set.test.ts with 14 failing tests pinning frontmatter/frontmatter-fields-a.md (§tools) and frontmatter/frontmatter-fields-b-and-templates.md (§YAML-shape, §Resolution snapshot). Covers the five load-time rejections — loom/load/unknown-tool, loom/load/unresolvable-loom-path, loom/load/prompt-mode-callable, loom/load/invalid-tool-rename, loom/load/tool-name-collision (both the two-entry and the top-level fn/import arm) — each refusing registration; plus the positive arms (as rename disambiguates a collision and binds the renamed name; default .loom name maps hyphens → underscores), the frozen resolution snapshot with no ambient inheritance (only listed callables appear; absent/empty tools: → empty set), and both YAML spellings parsing to the same set. Diagnostic Message strings anchored to diagnostics/code-registry-load.md per the Diagnostic message anchors rule; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 14 tests red on their own primary assertions (absent expected diagnostic; missing resolved entry; unfrozen snapshot) because the V6c body is absent — fail-red-for-the-intended-reason; the other tests stay green (4 pre-existing V4g-T reds in an unrelated subsystem, red on HEAD without this change, are unaffected).
2026-07-01 V6c — `tools:` callable set and resolution snapshot (paired with V6c-T): fills src/parser/callable-set.ts. resolveCallableSet(input) splits both YAML spellings (comma short form / list) through one per-entry grammar (<spec> ('as' <name>)?), classifies each entry as a Pi-tool name (bare identifier [A-Za-z_][A-Za-z0-9_]* → deps.resolvePiTool) or a .loom path (else → deps.resolveLoomCallee), and applies the five load-time rejections: loom/load/invalid-tool-rename (as target failing the lowercase-first identifier rule [a-z_][A-Za-z0-9_]*, checked before resolution), loom/load/unknown-tool (Pi tool absent from registry), loom/load/unresolvable-loom-path (.loom path resolving to no file), loom/load/prompt-mode-callable (resolved callee mode === "prompt"), loom/load/tool-name-collision (post-rename name already bound by an earlier entry, or in deps.reservedNames — the top-level fn/import arm). Default name = Pi-tool name verbatim, or the .loom basename minus .loom with hyphens → underscores (./code-review.loom → code_review); as overrides it. Registers iff no error-severity diagnostic fired; a registered loom carries an Object.freeze'd CallableSetSnapshot (no ambient inheritance — only listed callables appear; absent/empty tools: → frozen empty set), and no snapshot is returned on rejection. Diagnostic Message strings anchored to diagnostics/code-registry-load.md per the Diagnostic message anchors rule; no new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 14 V6c-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V14a-T — Tool calls (code-side) + `CodeToolError` tests (paired V14a impl): adds src/runtime/tool-call.ts — checkToolCallArguments(input) (inert stub → no diagnostics), codeToolErrorCauses() (inert stub → []), codeToolErrorKind()/modelToolErrorKind() (inert stubs → ""), lowerAcceptedPiToolReturn(finalOutput)/lowerAcceptedLoomCallableReturn(payload) (inert stubs → Err(null)), and surfaceLoomCallableInputValidationFailure(...)/surfaceLoomCallableCalleeFailure(...) (inert stubs → kind:"" values), plus the ToolCallCalleeKind/ToolCallStaticResolution/ToolCallArgCheckInput shapes — plus tests/tool-calls.test.ts with 10 failing tests pinning tool-calls.md (§Argument shape, §Return type, §Failures, CodeToolError TOOL code-keyed area) and pi-integration-contract/host-interfaces-core.md §"Tool execution from loom code". Argument checks: loom/parse/tool-arg-arity (multi-arg Pi-tool call), loom/parse/tool-arg-not-literal (non-literal single arg), loom/parse/tool-arg-type-mismatch (statically-resolvable .loom-callable mismatch), and the arity-before-type ordering (a call that both over-supplies args and type-mismatches fires only the arity code). CodeToolError: the closed cause enum (validation/execution/cancelled/unknown_tool) and its distinctness from ModelToolError ("code_tool" vs "model_tool" wire tags). Accepted-path return lowering: a Pi-tool return → Ok(string); a subagent-mode .loom return → Ok(T). .loom-callable failure: input-validation → InvokeInfraError{cause:"validation"} (never CodeToolError); a callee-returned failure → InvokeCalleeError carrying the inner error. Diagnostic Message strings anchored to diagnostics/code-registry-parse.md per the Diagnostic message anchors rule; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 10 tests red on their own primary assertions because the V14a body is absent — fail-red-for-the-intended-reason; the other tests stay green (4 pre-existing V4g-T reds in an unrelated subsystem, red on HEAD without this change, are unaffected).
2026-07-01 V14a — Tool calls (code-side) and CodeToolError (paired with V14a-T): fills src/runtime/tool-call.ts. checkToolCallArguments(input) runs the parse-time argument checks in arity-before-type order — loom/parse/tool-arg-arity short-circuits on an over-supplied positional count (positionalCount > 1), then a Pi-tool single argument runs the shared V2a is-literal check (checkLiteralSublanguage(..., "tool-arg") → loom/parse/tool-arg-not-literal), then a statically-resolvable .loom-callable argument mismatch fires loom/parse/tool-arg-type-mismatch; a call that both over-supplies args and type-mismatches fires only the arity code. codeToolErrorCauses() returns the closed cause enum (validation/execution/cancelled/unknown_tool); codeToolErrorKind()/modelToolErrorKind() return the distinct "code_tool"/"model_tool" wire tags. Accepted-path lowering: lowerAcceptedPiToolReturn(finalOutput) → Ok(string); lowerAcceptedLoomCallableReturn(payload) → Ok(T). .loom-callable failure: surfaceLoomCallableInputValidationFailure(...) → InvokeInfraError{cause:"validation"} (never CodeToolError); surfaceLoomCallableCalleeFailure(...) → InvokeCalleeError carrying the inner error. Diagnostic Message strings anchored to diagnostics/code-registry-parse.md per the Diagnostic message anchors rule; no new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Divergence: the loom-callable arity check emits loom/parse/tool-arg-arity because the ToolCallArgCheckInput seam carries no declared param count for the invoke-arity too-few/too-many split (no V14a test exercises it). All 10 V14a-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15c-T — Imports (`.warp` library files) tests (paired V15c impl): adds src/parser/imports.ts — checkWarpTopLevelForm(form, site) (inert stub → undefined), checkImportExtension(pathLiteral, site) (inert stub → undefined), checkImportedSymbols(input) (inert stub → []), detectImportCycle(entry, graph, site) (inert stub → undefined), RelativeWarpResolver.resolve(spec, fromFile) (inert stub → "", never throwing/resolving), and loadWarpImport(resolver, spec, fromFile, site) (inert stub → { registered: true, diagnostics: [] }), plus the UnresolvableWarpPathError class, the Resolver / WarpDirectoryProbe seams, and the ImportSite / ImportSpecifier / ImportCheckInput / WarpImportGraph / WarpImportLoad shapes with the six code + registry-message anchors — plus tests/imports.test.ts with 16 tests pinning imports.md (.warp file rules, Resolver interface, IMP-1 resolver failure contract, cycles / unknown-symbol / name-collision). IMP-1: the Resolver signals unresolvable by throwing (non-relative spec; no byte-exact final-segment entry — a case-variant Personas.warp does not match personas.warp on any host; byte-exact-but-unreadable entry), and a throw from resolve emits loom/load/unresolvable-warp-path and does not register the importing file. loom/parse/warp-top-level-statement fires on a top-level statement / let / query (import/export/schema/enum/fn do not). loom/parse/import-non-warp-extension fires on a .loom path and a non-lowercase .WARP variant (byte-exact lowercase, every host), not a byte-exact .warp path. loom/load/import-cycle fires on a two-node .warp cycle with its path printed (import cycle: a.warp → b.warp → a.warp). loom/parse/import-unknown-symbol names the source symbol not the alias and renders <path> as written; loom/parse/import-name-collision fires on two imports sharing a local name and on an import colliding with a same-file top-level declaration. Resolver success path: a resolvable relative .warp resolves (/proj/shared/personas.warp), registers, and binds a known symbol with no diagnostic. Diagnostic Message strings anchored to diagnostics/code-registry-parse.md and diagnostics/code-registry-load.md per the Diagnostic message anchors rule; no new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 13 of 16 tests red on their own primary assertions (missing throw; undefined != the expected diagnostic; "" != the resolved path) because the V15c body is absent — fail-red-for-the-intended-reason; the 3 green are negative-control guards (permitted top-level forms, byte-exact .warp, acyclic graph → no diagnostic). The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15c — Imports (`.warp` library files) (paired with V15c-T): fills src/parser/imports.ts. checkWarpTopLevelForm(form, site) returns loom/parse/warp-top-level-statement for a non-permitted top-level form (statement/let/query) and undefined for the five permitted forms (import/export/schema/enum/fn). checkImportExtension(pathLiteral, site) fires loom/parse/import-non-warp-extension unless the literal ends in byte-exact lowercase .warp (a .loom path and a non-lowercase .WARP both reject on every host). RelativeWarpResolver.resolve(spec, fromFile) rejects a non-relative spec (@scope/pkg, /looms/...) and a non-.warp spec by throwing UnresolvableWarpPathError, else joins spec against posix.dirname(fromFile) and enumerates the resolved parent once via the injected WarpDirectoryProbe, matching the final segment byte-for-byte (a case-variant Personas.warp does not match personas.warp) and requiring the entry be readable — throwing otherwise (IMP-1). loadWarpImport(resolver, spec, fromFile, site) treats any throw from resolve as a resolution failure per IMP-1, emitting loom/load/unresolvable-warp-path (rendering <path> as written) and registered:false; on success it registers the file and carries the resolved path. checkImportedSymbols(input) collects (no fast-fail) loom/parse/import-unknown-symbol for a specifier whose source symbol is neither a top-level declaration nor a re-export of the resolved file (message names the source, not the alias) and loom/parse/import-name-collision for a local name shared by two imports or colliding with a same-file top-level declaration. detectImportCycle(entry, graph, site) walks the static .warp import graph (DFS) and fires loom/load/import-cycle with the cycle path printed (import cycle: a.warp → b.warp → a.warp). Diagnostic Message strings anchored to diagnostics/code-registry-parse.md and diagnostics/code-registry-load.md; no new diagnostics-registry codes; no module-level mutable state, ambient primitive, Promise combinator, or Sync call. Divergence: loadWarpImport uses one // allow-broad-catch: loom/load/unresolvable-warp-path broad catch that does NOT rethrow — IMP-1 mandates treating any throw from resolve as a resolution failure, and conventions.md forbids the rethrow-on-mismatch pattern the V15c-T note anticipated; this adds a broad-catch site outside the five enumerated exempt categories (release-time residue-inspection item-6 consideration). All 16 V15c-T tests green; typecheck + lint clean; arch checks clean. The 4 pre-existing reload reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15e-T — Hot-reload static-resolution cache eviction tests (paired V15e impl): adds src/extension/cache-eviction.ts — evictStaleParseCacheEntries(deps, input) (inert stub → { evicted: [] }, never mutates input.cache, never calls input.rebuildImportGraph), plus the WarpImportEdgeGraph / CacheEvictionDeps / CacheEvictionInput / CacheEvictionResult seams — plus tests/hot-reload-cache-eviction.test.ts with 3 tests pinning implementation-notes.md "Static-resolution load pass" and invocation.md §Resolution. (1) The changed file and every transitive .warp importer are dropped from the per-pass parse cache (V15a) as part of the LoomRegistry swap (V9b), reverse-walking the importer→imported edge graph (V15c), while an unrelated file survives. (2) A symlink-delivered non-canonical changed path canonicalises (FileSystem.realpath then forward-slash) to the cache key form so the stale entry drops rather than surviving. (3) Negative-direction under-eviction / ordering pin: on a reload that newly establishes an x → target .warp edge (absent from the pre-swap graph), the eviction walk runs over the rebuilt post-swap graph from rebuildImportGraph() — sequenced after the rebuild — so x is identified as a transitive importer and dropped; the test records the rebuild call and reds if the walk read the pre-swap graph. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 3 tests red on their own primary assertions (cache still holds every entry; rebuild thunk never called) because the V15e body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15e — Hot-reload static-resolution cache eviction (paired with V15e-T): fills src/extension/cache-eviction.ts. evictStaleParseCacheEntries(deps, input) canonicalises the watcher-delivered changed-file path (V9b) via the single realpath-then-forward-slash form — now exported as canonicalizePath(fs, path) from src/runtime/invocation.ts (reused not restated; checkInvokePathContainment refactored onto it) — then calls input.rebuildImportGraph() to get the post-swap .warp import-edge graph (V15c) and only afterward reverse-walks the importer→imported edges to a fixed point, so the eviction set is the changed file plus every transitive .warp importer. Each matching entry is dropped in place from the per-pass parse cache (V15a) and reported in evicted (only paths present-and-removed). Rebuild-before-walk ordering is intrinsic (the walk reads the thunk's result), so a reload that newly establishes an x → target edge identifies x over the rebuilt graph rather than serving it stale; a non-canonical changed path (symlink/case-variant/./..-segments/relative) canonicalises to the cache key form so the drop hits. No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 3 V15e-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15f-T — Invoke parse/load diagnostics tests (paired `V15f` impl): the src/parser/invoke-diagnostics.ts seam (five inert-stub checkers checkInvokeArgTypes/checkInvokeReturnType/checkInvokeArity/checkInvokeExtension/checkCalleeHasErrors + the arity-before-type orchestrator checkInvokeCall, each returning a single stub/v15f-unimplemented sentinel diagnostic; six registry-anchored code constants + message/hint builders; the input shapes, reusing the V2b CompatType/TypeEnv/checkCompatible/displayType) plus tests/invoke-diagnostics.test.ts with 17 tests pinning invocation.md (invoke-arg-type-mismatch fire/skip on static resolvability; invoke-return-type-mismatch Cat⊑Animal accepted/incompatible-fires/skip; invoke-arity-too-few fire-when-resolvable-else-runtime-net, invoke-arity-too-many always-fires, arity-checked-before-per-argument-type; invoke-non-loom-extension on .warp/.LOOM/tools: entries; callee-has-errors error-for-tools/warning-for-invoke with related sites). Messages/hints anchored to code-registry-parse.md and code-registry-load.md; no new registry codes. Typecheck + lint clean; all 17 tests red on their own primary assertions (wrong sentinel code / unexpected length 1) because the V15f body is absent — fail-red-for-the-intended-reason; 942 unrelated tests green (4 pre-existing V4g-T/reload reds in an unrelated subsystem unaffected).
2026-07-01 V15f — Invoke parse/load diagnostics (paired with V15f-T): fills the five checkers in src/parser/invoke-diagnostics.ts. checkInvokeArgTypes skips when the callee is not statically resolvable (runtime AJV net), else fires loom/parse/invoke-arg-type-mismatch for each positional slot whose argType is not ⊑ the declared paramType (V2b checkCompatible; "unknown" operands deferred). checkInvokeReturnType skips when the callee is not resolvable, else checks T_calleeReturn ⊑ Schema by compatibility not equality — Cat⊑Animal accepted, an incompatible pair fires loom/parse/invoke-return-type-mismatch. checkInvokeArity: provided>total always fires loom/parse/invoke-arity-too-many (even when not statically resolvable); provided<required fires loom/parse/invoke-arity-too-few only when statically resolvable (else the runtime validation net). checkInvokeCall runs arity before per-argument type, returning arity diagnostics without running the type check when arity fails. checkInvokeExtension fires loom/parse/invoke-non-loom-extension unless the literal path ends byte-exact-lowercase .loom (.warp/.LOOM reject; same code for invoke(...) and tools: surfaces). checkCalleeHasErrors emits loom/load/callee-has-errors at the referencing site when the callee failed static resolution — error for a tools: entry, warning for an invoke(...) literal — listing underlying sites via related. Messages/hints anchored to code-registry-parse.md and code-registry-load.md; no new registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 17 V15f-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15g-T — Invoke invocation-record provenance seam tests (paired `V15g` impl): the src/runtime/invoke-provenance.ts seam (the inert-stub producer recordInvocationProvenance(deps, input) → { parentPath: "", callSiteLine: 0 }; the InvokeCallSite two-arm descriptor — literal_invoke carrying the invoke( invokeToken position, loom_callable_bare carrying the callee-name calleeNameToken position; the InvocationRecord parentPath/callSiteLine shape; the InvocationProvenanceDeps/InvocationProvenanceInput shapes, reusing the diagnostics Position and the FileSystem seam) plus tests/invoke-provenance.test.ts with 6 tests pinning slash-invocation.md §SLSH-5 (chain attribution, consumed by V12b) and invocation.md §Resolution. The record exposes the parent loom's post-realpath path (symlink-delivered parent resolves to its target not the literal link; already-canonical path unchanged) and the call-site token's 1-indexed source line (literal invoke(...) → invoke( token line; .loom-callable bare call → callee-name identifier line; multi-line call of each form pins the recorded line to the call-site token, not a receiving binding's earlier line). No new registry codes. Typecheck + lint clean; all 6 tests red on their own primary assertions ("" != expected realpath output; 0 != expected 1-indexed call-site line) because the V15g body is absent — fail-red-for-the-intended-reason; 4 pre-existing V4g-T reds in an unrelated subsystem unaffected.
2026-07-01 V15g — Invoke invocation-record provenance seam (paired with V15g-T): fills src/runtime/invoke-provenance.ts. recordInvocationProvenance(deps, input) produces the per-frame invocation record for one executed invoke hop (slash-invocation.md §SLSH-5, consumed by V12b): it realpath-normalises the parent path through the injected FileSystem.realpath seam so parentPath is the byte-exact post-realpath form V15a's discovery-root containment uses (symlink-delivered parent resolves to its target; already-canonical path unchanged), and records callSiteLine as the call-site token's 1-indexed line — the invoke( token for a literal invoke(...) call (literal_invoke arm) or the callee-name identifier for a .loom-callable bare-identifier call (loom_callable_bare arm) — never a receiving binding's earlier line (multi-line calls of each form pinned). No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 6 V15g-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T/reload reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15h-T — Invoke-child execution-Promise swallowing-handler per-site routing tests (paired `V15h` impl): adds src/runtime/invoke-swallowing-handler.ts — the two seams of the invoke-child swallowing handler (cancellation.md §"Race semantics — swallowing-handler attachment on every abandonable Promise", coverage-matrix cka-33 / V15h facet): guardInvokeExecutionPromise(promise, guard, channels) (construction-site attachment; inert stub returns the Promise WITHOUT attaching a handler so a late rejection reaches Node's unhandledRejection channel) and routeInvokeExecutionLateSettlement(settlement, guard, channels) (the discard decision; inert stub bypasses the substrate — emits a sentinel second RuntimeEvent and a loom/runtime/internal-error diagnostic and returns "surfaced"), plus the InvokeExecutionSettlement / InvokeCancellationGuard / InvokeExecutionSideChannels / InvokeLateSettlementDisposition shapes — plus tests/invoke-swallowing-handler.test.ts with 4 tests citing both cka-33 and the V15h facet leaf-ID inline (H5f per-facet citing-test gate). The late settlement is landed via the Checkpoint seam (V8a, PIC-10): after the invoke checkpoint surfaces cause:"cancelled" the abandoned child execution Promise settles late and MUST be suppressed on all three side channels — no Node unhandledRejection, no second RuntimeEvent on the always-log channel, no diagnostic of any severity (a diagnostic-worthy OOM-style late rejection is still discarded, no promotion to loom/runtime/internal-error). No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 4 tests red on their own primary assertions (a real unhandledRejection is captured because the construction-site handler is absent; the stub route emits a sentinel event/diagnostic) because the V15h body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15h — Invoke-child execution-Promise swallowing-handler per-site routing (paired with V15h-T): fills src/runtime/invoke-swallowing-handler.ts. guardInvokeExecutionPromise(promise, guard, channels) attaches the swallowing handler synchronously at the child execution Promise's construction site (a .then(onResolve, onReject) before the first microtask boundary; a lazily-attached .catch would miss a rejection already queued for unhandledRejection) and returns the same Promise; every settlement is routed through routeInvokeExecutionLateSettlement(settlement, guard, channels), which once guard.cancellationSurfaced is true (the invoke checkpoint surfaced cause:"cancelled") discards the late settlement on all three side channels — no Node unhandledRejection (absorbed at the construction-site handler), no second RuntimeEvent, no diagnostic of any severity (a diagnostic-worthy OOM-style late rejection still discarded, no promotion to loom/runtime/internal-error); the discriminator is whether cancellation surfaced, not the late-settle kind (late resolved and late rejected discarded identically). Pre-cancellation the settlement reports "surfaced" to the normal invoke Surfacing path (this site emits nothing itself). Deterministic-test substrate is the Checkpoint seam (V8a, PIC-10). No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 4 V15h-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T/reload reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15i-T — Imports export visibility and re-exports tests (paired `V15i` impl): adds two visibility seams to src/parser/imports.ts — computeWarpExports(forms) (downstream-visible name set = auto-exported declarations + `export … from` re-export aliases; inert stub returns the WRONG set — the plain-import locals, which are NOT visible — omitting declarations and re-exports) and warpLocalBindings(forms) (the module's local bindings, excluding re-export sources; inert stub returns the re-export SOURCE names, which a re-export does not bind), plus the WarpDeclaration / WarpDeclarationKind / ReExportSpecifier / WarpModuleForms shapes — plus tests/export-visibility.test.ts with 7 tests citing coverage-matrix code-keyed-area token cka-48 inline (imports.md §Visibility + §Re-exports; un-anchored GOV-22 residue, no numbered REQ-ID). Auto-export: a bare top-level schema/enum/fn is each exported with no `export` keyword and resolves from an importer with no import-unknown-symbol. Aliased re-export: `export { A as B } from "./x.warp"` is visible downstream as B not A, creates no local binding for A or B, and a downstream import { B } binds. Negative half: a plain import { A } is not re-exported — A is absent from the re-importing file's exports and a further downstream import { A } is loom/parse/import-unknown-symbol. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 7 tests red on their own primary assertions (missing export; wrongly-present export/local-binding; missing/unexpected downstream diagnostic) because the V15i body is absent — fail-red-for-the-intended-reason; the 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15i — Imports export visibility and re-exports (paired with V15i-T): fills the two visibility bodies in src/parser/imports.ts. computeWarpExports(forms) returns the downstream-visible name set — every top-level schema/enum/fn declaration name (auto-exported, no export keyword, no privacy modifier) plus every export … from re-export's downstream name (exported = the as alias or source when unaliased); a plain import local is excluded (a plain import is not re-exported). warpLocalBindings(forms) returns the module's local bindings — top-level declaration names plus plain-import locals — and excludes export … from re-export sources (a re-export creates no local binding). These feed checkImportedSymbols's resolvedExports so a downstream importer binds an auto-exported symbol and an aliased re-export (by its alias B, not the source A) with no loom/parse/import-unknown-symbol, while a plain-imported symbol is invisible downstream and reds as unknown. imports.md §Visibility + §Re-exports; coverage-matrix code-keyed-area token cka-48 (no numbered REQ-ID, GOV-22 residue). No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call (pure functions). All 7 V15i-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15k-T — Invoke runtime arg/options and final-value propagation tests (paired `V15k` impl): adds src/runtime/invoke-args-options.ts — the three facets split out of V15a per §smallest-shippable-leaf. INV-2: buildInvokeArgList(args) (inert stub → wrong style "named") and invokeArgListStyleDefinedInLoom10(style) (inert stub → inverted verdict), plus the InvokeArgListStyle ("positional"|"named") / InvokeArg / InvokeArgList shapes. INV-3: readInvokeOptions(options) (inert stub → drops the present known-field values) over the open-struct InvokeOptions (three known slots cancellation/calleeHandle/returnSchema plus an index signature admitting additive future fields) and ResolvedInvokeOptions. FN-5: invokeFinalValueFromCalleeResult(result) (inert stub → fixed propagated sentinel) projecting V3d's FunctionResult seam onto the InvokeFinalValueObservation (propagated/absent). Plus tests/invoke-args-options.test.ts with 7 tests citing INV-2/INV-3/FN-5 inline (invocation.md INV-2/INV-3/§Final-value propagation, return.md, functions.md §Final value). INV-2: the arg list is built positional (loom 1.0's only surface) and only positional is defined. INV-3: the three known slots project verbatim and an additive perCallTimeoutMs field is tolerated with no type error and no disturbance to the known slots. FN-5: the callee's final value propagates on success and is absent on fail/cancel, driven off V3d's functionResult. No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 7 tests red on their own primary assertions (wrong style; inverted predicate; dropped option values; sentinel value / wrong kind) because the V15k body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15k — Invoke runtime arg/options and final-value propagation (paired with V15k-T): fills the three facets in src/runtime/invoke-args-options.ts (split out of V15a per §smallest-shippable-leaf). INV-2: buildInvokeArgList(args) builds the AST arg list with style "positional" (loom 1.0's only invocation surface) and invokeArgListStyleDefinedInLoom10(style) returns true only for "positional" — the "named" arm is the reserved deferred-surface seam with no loom 1.0 behaviour. INV-3: readInvokeOptions(options) projects the three known slots (cancellation/calleeHandle/returnSchema) verbatim off the open-struct InvokeOptions, tolerating additive unknown fields (e.g. a future perCallTimeoutMs) with no effect on the known-slot projection. FN-5: invokeFinalValueFromCalleeResult(result) projects V3d's FunctionResult seam — a present (success) result propagates its value ({kind:"propagated"}); a fail/cancel result yields {kind:"absent"} (no final value flows). invocation.md INV-2/INV-3/§Final-value propagation, return.md, functions.md §Final value. No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call (pure functions). All 7 V15k-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15l-T — Invoke fresh-vs-attach cross-mode matrix tests (paired V15l impl): adds src/runtime/invoke-cross-mode.ts — the cross-mode matrix selection split out of V15a per §smallest-shippable-leaf. selectCalleeContext(calleeMode) selects fresh-vs-attach by the callee mode alone (inert stub → inverted mapping: subagent→attach, prompt→fresh); composeCalleeSession(input) composes the callee session state (inert stub → inverted context, caller prior messages regardless of context, PARENT inference config), plus the LoomMode / CalleeContext / InferenceConfig / ConversationMessage / CrossModeCell / CallerConversation / CalleeDefinition / CrossModeInput / CalleeSessionComposition shapes. Plus tests/invoke-cross-mode.test.ts with 9 tests citing invocation.md §Cross-mode semantics / §Tools and model inline (un-anchored INV-area obligation, no numbered PREFIX-N REQ-ID; canonical enumeration in subagent.md "Subagent state-isolation matrix"). Scope: the three in-scope cells (prompt→subagent, subagent→subagent, subagent→prompt); prompt→prompt parent-suspend + setActiveTools snapshot/restore is owned by V15d (cka-15) and out of scope. A subagent-mode callee gets a fresh isolated conversation and a prompt-mode callee attaches to the caller's current conversation (caller mode irrelevant); a fresh-context callee starts with no prior conversation messages; every callee uses the child's own model/tools/system not the parent's across all three cells. No new registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 9 tests red on their own primary assertions (inverted context; parent config where child's required) because the V15l body is absent — fail-red-for-the-intended-reason; the 4 pre-existing V4g-T reds in an unrelated subsystem unaffected.
2026-07-01 V15l — Invoke fresh-vs-attach cross-mode matrix (paired with V15l-T): fills the two behaviour-bearing functions in src/runtime/invoke-cross-mode.ts (split out of V15a per §smallest-shippable-leaf). selectCalleeContext(calleeMode) selects fresh-vs-attach by the callee mode alone — subagent→"fresh" (prompt→subagent, subagent→subagent), prompt→"attach" (subagent→prompt); the caller mode is irrelevant (invocation.md §Cross-mode semantics). composeCalleeSession(input) composes the callee session state: a fresh-context callee starts with no prior conversation messages (priorMessages []); an attach-context callee carries the caller's current conversation messages (the caller subagent's own private conversation for subagent→prompt); and the inference config is always the child's own (input.callee.config), never the parent's (invocation.md §Tools and model). Scope: the three in-scope cells; the prompt→prompt parent-suspend + setActiveTools snapshot/restore cell is owned by V15d (cka-15) and out of scope. Un-anchored INV-area obligation (no numbered PREFIX-N REQ-ID; canonical enumeration in subagent.md "Subagent state-isolation matrix"). No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call (pure functions). All 9 V15l-T tests green; typecheck + lint clean. The 4 pre-existing reds in tests/pre-evaluation-reload-failure.test.ts (red on HEAD without this change) are unaffected.
2026-07-01 V15d-T — Prompt→prompt parent-suspend and setActiveTools snapshot/restore tests (paired `V15d` impl): adds src/runtime/invoke-prompt-suspend.ts — the prompt→prompt cell peeled off V15a/V15l. runPromptSuspendInvoke(input) runs one prompt→prompt invoke hop under the parent-suspend + active-set snapshot/restore window (inert stub → runs the child body directly with NO snapshot / install / restore and reports engaged:false), plus the PromptSuspendPi / PromptSuspendInput / PromptSuspendOutcome shapes. Plus tests/invoke-prompt-suspend.test.ts with 5 tests: 2 citing invocation.md §Cross-mode semantics inline (un-anchored INV-area prompt→prompt suspend + snapshot-before-child obligation, no numbered PREFIX-N REQ-ID) and 3 citing PIC-17 inline (tool-registration-lifetime.md step-4 finally restore, generalised to the child's whole body). INV: on the prompt→prompt path the parent snapshots its ambient active set and installs the child's callable set before the child runs (window engaged), and is suspended until the child settles. PIC-17: after a child that throws, cancels, or fails (returns Err) inside the window — leaving a foreign mid-window mutation the restore must overwrite — the pre-invoke snapshot is restored (pi.getActiveTools() returns the pre-invoke set) with the inner failure surfaced unmasked, across both the throw and cancel sub-cases. Distinct from PIC-8/PIC-19 (restore-call/setup-side failure, V9f). No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 5 tests red on their own primary assertions (window not engaged; no snapshot/install before the child; the pre-invoke set never restored over the mid-window mutation) because the V15d body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T/reload reds in an unrelated subsystem (red on HEAD without this change) are unaffected.
2026-07-01 V15d — Prompt→prompt parent-suspend and setActiveTools snapshot/restore (paired with V15d-T): fills src/runtime/invoke-prompt-suspend.ts. runPromptSuspendInvoke(input) runs one prompt→prompt invoke hop under the parent-suspend + active-set snapshot/restore window, engaged only for the prompt→prompt cell (callerMode==="prompt" && calleeMode==="prompt"); every other cell passes the child body through untouched (engaged:false, owned by V15l). For the prompt→prompt cell: step-1 snapshot pi.getActiveTools(), step-2 install pi.setActiveTools([...childCallableSet]) (snapshot NOT unioned in — ambient tools not inherited), suspend the parent by awaiting the child body, and step-4 restore the pre-invoke snapshot in a finally on every settle path (success, returned Err, cancel, throw), overwriting any mid-window mutation with no diagnostic (invocation.md §Cross-mode semantics; PIC-17 step-4 finally restore generalised to the child's whole body); the inner failure propagates past the finally unmasked. PIC-8/PIC-19 restore/setup-failure protocols and the recovery-mutex fallback are behavioural preconditions owned by V9f, not re-implemented here (the restore call succeeds on every V15d path). No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 5 V15d-T tests green; typecheck + lint clean. The 4 pre-existing reds in tests/pre-evaluation-reload-failure.test.ts (red on HEAD without this change) are unaffected.
2026-07-01 V16a-T — Hard-ceiling interaction order and `masked` co-fire tests (paired `V16a` impl): adds src/runtime/ceiling-arbitration.ts — the pure, stateless Class-2 cross-ceiling arbitration seam. arbitrate(candidate) maps a ceiling-candidate { site, satisfied } (a check site plus the co-fire set of satisfied ceiling classes) to { surfaced, masked? }: surfaced is the ceiling whose first-enforcement point is that site (invoke-entry→#1, round-boundary→#2, slash-load-binder→#3, ajv-boundary→#4), masked enumerates the co-fired siblings from the closed MaskedCeilingId set (reused from V9d), omitted (never []) when no co-fire occurred (inert stub → deranges the site→ceiling map and never enumerates masked). Plus tests/ceiling-arbitration.test.ts with 6 tests citing CIO-1 … CIO-6 inline (hard-ceilings/ceilings-3-and-4.md §"Interaction between ceilings" + §`masked` field). CIO-1: a candidate co-presenting #3 with a runtime-class ceiling surfaces #3 (the precedence decision) masking the runtime-class ceiling; CIO-2/3/4: an invoke-entry / AJV-boundary / round-boundary candidate resolves #1 / #4 / #2; CIO-5: #3 co-present with #1/#2/#4 surfaces a single ceiling not an interleave; CIO-6: at most one ceiling surfaces and masked enumerates co-fired siblings with the omit-when-empty rule. The temporal cross-site relations (CIO-1 slash-load-before-runtime placement, CIO-5 never-interleaves) are witnessed downstream (V4e/V11f/V5e/V13c/V15b) and by H7a, not at this stateless seam. No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 6 tests red on their own primary assertions (wrong surfaced identifier; absent masked enumeration) because the V16a body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V16a — Hard-ceiling interaction order and masked co-fire (paired with V16a-T): fills arbitrate in src/runtime/ceiling-arbitration.ts — the pure, stateless Class-2 cross-ceiling arbitration seam. arbitrate({ site, satisfied }) maps a ceiling-candidate to { surfaced, masked? }: surfaced is the ceiling whose first-enforcement point IS the candidate's check site via the fixed site→ceiling map (invoke-entry→#1 CIO-2, round-boundary→#2 CIO-4, slash-load-binder→#3 CIO-1, ajv-boundary→#4 CIO-3); masked filters the closed MASKED_CEILING_IDS set (from V9d) to the satisfied siblings other than surfaced, in canonical order, omitting the key (never []) when empty. CIO-1's #3-over-runtime-class precedence is realised because a co-fire whose surfacing site is the slash-load-binder site surfaces #3 and masks the co-present runtime-class ceiling (CIO-5 the seam-local single-surface form; CIO-6 at-most-one surfaces per event). The seam runs no breach check, receives no events, owns no per-ceiling surface — each ceiling's detection stays distributed across V5e/V11f/V13c/V15b/V4e, which consult this seam. The temporal cross-site relations (CIO-1 slash-load-before-runtime, CIO-5 never-interleaves) are witnessed downstream and by H7a, not here. No new diagnostics-registry codes; SITE_CEILING is an as-const frozen literal (no module-level mutable state); no broad catch / ambient primitive / Promise combinator / Sync call (pure function). All 6 V16a-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V5e-T — JSON document depth enforcement (hard ceiling #4) tests (paired `V5e` impl): adds src/runtime/depth-walk.ts — two pure Class-2 seams mirroring V16a's seam pattern (exercised in isolation; live AJV-boundary sites built downstream at V13c/V14e/V15j/V4e). depthWalk(value) fast-fails the first node whose depth exceeds MAX_JSON_DEPTH (5), producing the canonical depth-violation issue (schema_keyword "maxDepth", message "JSON document depth exceeds 5", cause "schema_validation"); jsonDepth(value) exposes the counting algorithm; routeDepthBoundary(site) decides the per-boundary destination class. Inert stubs → jsonDepth returns 0, depthWalk never fires, routeDepthBoundary deranges the site→destination map. Plus tests/depth-enforcement.test.ts with 7 tests citing cka-10 inline (schema-subset.md §Depth Enforcement; the depth ceiling carries no numbered PREFIX-N REQ-ID) plus the ceiling-#4 per-boundary table (#ceiling-4-table) and CIO-3. Group 1: a depth-6 value fires the canonical maxDepth issue with the canonical message and cause schema_validation. Group 2 (routing decision only, wrapping asserted at the site owners): typed-query response → ValidationError (V13c), code-driven tool args → CodeToolError (V14e), params-via-invoke and invoke<T> return → InvokeInfraError (V15j); the two non-Err rows are decision-only — model-driven tool args → model feedback, slash-load params → ceiling-#3 cross-route (V4e). Group 3 (counting algorithm): depth-5 passes / depth-6 trips, scalar and empty values are depth 1, a non-empty value is 1 + max child, and anyOf arms add no level (depth measured against the materialised value). No new diagnostics-registry codes; inert stub seam — no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 7 tests red on their own primary assertions (walk never fires; wrong depth constant; deranged routing) because the V5e body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V5e — JSON document depth enforcement (hard ceiling #4) (paired with V5e-T): fills the three seams in src/runtime/depth-walk.ts. jsonDepth(value) implements the §Counting algorithm — scalar / null / {} / [] are depth 1, a non-empty object or array is 1 + max(depth(child)), and anyOf arms add no level because depth is measured against the single materialised value. depthWalk(value) is a recursive descent (root at level 1, member/element descent increments) that short-circuits on the first node whose level exceeds MAX_JSON_DEPTH (5) and returns the canonical depth-violation issue (schema_keyword "maxDepth", message "JSON document depth exceeds 5", cause "schema_validation") with the RFC-6901 JSON Pointer to that node (~→~0 / /→~1 escaping). routeDepthBoundary(site) maps ceiling #4's five enforcement points to their destination surface class per the per-boundary table (#ceiling-4-table): typed-query response → ValidationError, code-driven tool args → CodeToolError, params-via-invoke and invoke<T> return → InvokeInfraError, model-driven tool args → model-feedback, slash-load params → ceiling-3-cross-route. Pure functions — no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 7 V5e-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V11f-T — Binder per-class retry budget and failure taxonomy (hard ceiling #3) tests (paired `V11f` impl): adds src/binder/retry-taxonomy.ts — the runtime seam that drives the per-slash-invocation retry budget and renders the six failure-mode templates. runBinderWithRetries({attempt}) classifies each attempt independently and enforces the per-class budget (1 transport retry HC3-a, 1 malformed retry HC3-b, AJV-on-args terminal HC3-c) with MAX_BINDER_LLM_CALLS=3 worst case and most-recent-failure surfacing (HC3-d/HC3-e); renderBinderSystemNote(name, surface) renders the six failure-mode templates; renderAjvSummary / renderDepthWalkAjvSummary render the <ajv-summary> (joined "; " form and single-issue depth-walk form); classifyBinderArgs({depth, ajvIssues}) is the CIO-1/CIO-3 params-boundary cross-ceiling classification. Inert stubs → runBinderWithRetries returns a zero-call sentinel without issuing any attempt, the renderers return the UNIMPLEMENTED sentinel, classifyBinderArgs always reports ok. Plus tests/binder-retry-taxonomy.test.ts with 10 tests: HC3-a … HC3-e cited inline, the six verbatim failure-mode templates (<provider>=Model.api, <ajv-summary>=<path> <message> joined "; "; ContextOverflow folds into transport), and the depth-walk fast-fail sub-case (a depth-tripping ok-envelope args classified into AJV-on-args with "<JSON-Pointer> JSON document depth exceeds 5" synthesised from the depth-walk issue schema_keyword:"maxDepth", not from errorsText over the empty AJV errors array — renderAjvSummary([]) is ""). No new diagnostics-registry codes (binder failures surface as loom-system-notes, not loom/... codes); inert stub seam — no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 10 tests red on their own primary assertions (callCount 0≠expected; sentinel≠each pinned template; ok≠ajv_args) because the V11f body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V11f — Binder per-class retry budget and failure taxonomy (hard ceiling #3) (paired with V11f-T): fills the five seams in src/binder/retry-taxonomy.ts. runBinderWithRetries({attempt}) drives the per-slash-invocation retry budget — two single per-class budgets (transport HC3-a, malformed HC3-b) that interleave (a transport failure on the retry of a malformed envelope consumes the transport budget, and symmetrically), ok/needs_info/ambiguous/ajv_args terminal (HC3-c: AJV-on-args carries no retry budget), bounding issued calls at MAX_BINDER_LLM_CALLS=3 (HC3-d) and returning the most-recent attempt's outcome so the surfaced row matches the most recent failure (HC3-e). renderBinderSystemNote(name, surface) renders the six verbatim failure-mode templates through the V11e line discipline: needs_info/ambiguous/malformed/ajv_args via renderFailureNote (rule-3 em-dash grammar), transport via its own (<provider>: <message>) parenthetical (<provider>=Model.api verbatim) + capSystemNote, cancelled fixed-suffix + capSystemNote; ContextOverflow folds into the transport class (no row of its own). renderAjvSummary(issues) joins <path> <message> with "; " (empty list → ""); renderDepthWalkAjvSummary(issue) is the single-issue form (no separator). classifyBinderArgs({depth, ajvIssues}) is the CIO-1/CIO-3 params-boundary cross-ceiling classification (depth-walk before AJV; a depth breach cross-routes ceiling #4 → #3 into AJV-on-args with the depth-walk-synthesised summary, AJV errors not consulted). No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 10 V11f-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V4e-T — Load-time pre-evaluation failure routing tests (paired V4e impl): adds src/extension/load-pre-eval.ts — the createLoadFailurePreEvalRouter(deps) seam whose routePreEvalFailure(cause, note) is stubbed to drop the assembled loom-system-note (no pi.sendMessage), plus the crossRouteSlashLoadParams(loomName, value) ERR-16 helper that consults V5e's depthWalk (depth-6 breach detection) and V16a's arbitrate (CIO-1 slash-load candidate surfacing ceiling #3, masking ceiling #4) and renders the ceiling-#3 ajv_args no-retry note via V11f's renderBinderSystemNote/renderDepthWalkAjvSummary, then routes it (dropped by the stub). Plus tests/pre-evaluation-failures.test.ts with 8 failing tests pinning ERR-1…ERR-6 and ERR-16 inline (errors-and-results/error-model.md pre-evaluation failure list; hard-ceilings/ceilings-3-and-4.md CIO-1). Each load-time cause (ERR-1 host-incompatible loom/load/host-incompatible; ERR-2 lex/parse/type loom/parse/*; ERR-3 frontmatter loom/load/missing-mode; ERR-4 binder-model loom/load/binder-model-unresolved; ERR-5 binder arg-binding ceiling #3; ERR-6 tools: loom/load/unknown-tool; ERR-16 slash-load params cross-route) MUST route onto the loom-system-note channel (customType = loom-system-note) with the fixed triggerTurn:false option, never firing a turn or becoming an evaluation outcome. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 8 tests red on their own primary sendMessage-called assertion (the routed note never reaches the channel's pi.sendMessage) because the V4e routing is absent — fail-red-for-the-intended-reason; the ERR-16 CIO-1 arbitration secondary assertions (surfaced ceiling#3 / masked ceiling#4) are exercised through V16a/V5e. The 4 pre-existing V4g-T reds in an unrelated subsystem are unaffected.
2026-07-01 V4e — Load-time pre-evaluation failure routing (paired with V4e-T): wires routePreEvalFailure(cause, note) in src/extension/load-pre-eval.ts to deliver the assembled loom-system-note over the V7d sendSystemNote channel, which applies the fixed triggerTurn:false option — so all seven load-time pre-evaluation failure causes route onto the loom-system-note channel (customType = loom-system-note) without firing a turn and never become an evaluation outcome. ERR-1 host-incompatible (loom/load/host-incompatible, V9a capability probe); ERR-2 lex/parse/type batches; ERR-3 frontmatter (V6a); ERR-4 binder-model resolution (V11a); ERR-5 binder argument-binding ceiling #3 (V11f); ERR-6 tools: resolution (V10a/V6a); ERR-16 slash-load params arm of ceiling #4 cross-routed via CIO-1 through ceiling #3's no-retry classification (V5e live depth walk detects the depth-6 breach, V16a arbitration surfaces ceiling #3 and masks ceiling #4, V11f renders the ceiling-#3 ajv_args no-retry note) — the crossRouteSlashLoadParams helper routes through the same single delivery surface. All seven causes share one delivery path (sendSystemNote); the cause discriminant is carried for reload-integration reuse (ERR-7/V4g) but drives no per-cause branching. No new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 8 V4e-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V14e-T — Ceiling-#4 depth-6 code-driven-tool-args routing (live carrier) tests (paired V14e impl): adds the enforceCodeToolArgDepth(toolName, argValue) seam to src/runtime/tool-call.ts (inert stub → never fires, returns undefined) plus the CodeToolArgDepthBreach shape (result wrapped Err, error the CodeToolError carrier, issue the V5e DepthViolationIssue), and tests/tool-calls-depth-ceiling.test.ts with 2 failing tests pinning hard-ceilings/ceilings-3-and-4.md §Per-boundary destination/surface table (#ceiling-4-table, code-driven-tool-args row) and CIO-3 (#cio-3, depth-walk-before-AJV), plus tool-calls.md §Failures (CodeToolError{cause:"validation"} carrier). Delegated live-carrier witness for V5e's code-driven-tool-args routing row: a depth-6 code-driven <name>(args) argument trips the loom-owned depth walk (V5e) before AJV and surfaces wrapped as Err(CodeToolError{cause:"validation"}) carrying schema_keyword:"maxDepth" (message "JSON document depth exceeds 5"), building on V14a's CodeToolError carrier; a within-cap (depth-5) argument produces no breach and defers to the downstream AJV boundary. Message anchored to schema-subset.md §Error shape via V5e's DepthViolationIssue; no new diagnostics-registry codes; no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; both tests red on their own primary assertion (a depth-6 argument must trip ceiling #4 — the stub returns undefined) because the V14e body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V14e — Ceiling-#4 depth-6 code-driven-tool-args routing (live carrier) (paired with V14e-T): fills enforceCodeToolArgDepth(toolName, argValue) in src/runtime/tool-call.ts. Runs V5e's loom-owned depthWalk over the materialised code-driven <name>(args) argument before AJV (CIO-3, #cio-3): a within-cap value returns undefined (defer to the downstream AJV boundary), a depth-6+ breach is wrapped into the V14a CodeToolError carrier (kind "code_tool", cause "validation", tool_name the post-rename callable-set name, message the canonical "JSON document depth exceeds 5") and surfaced as Err(CodeToolError) to loom code per the code-driven row of the ceiling-#4 per-boundary table (hard-ceilings/ceilings-3-and-4.md #ceiling-4-table); the returned CodeToolArgDepthBreach also carries the depth walk's issue (schema_keyword "maxDepth", canonical message from schema-subset.md §Error shape via V5e's DepthViolationIssue), keeping the V14a-owned CodeToolError schema unchanged (no validation_errors field added). Pure function — no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Both V14e-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V15b-T — Invoke depth bound (INV-4) and invocation-cycle detection tests (paired V15b impl): adds src/runtime/invoke-depth-cycle.ts — the per-chain invoke-depth counter, the .warp fn residence classifier, the two-mode depth-overflow panic router, and the parse-time invocation-cycle detector (reusing V4b's InvokeDepthExceededPanic / INVOKE_DEPTH_CAP). pushCountableFrame(chain, kind) (inert stub → chain unchanged, never increments/enters the cap guard), warpFnFrameKind(call) (inert stub → fixed wrong sentinel), crossSubagentBoundary(chain) (inert stub → resets to 0), surfaceDepthOverflow(panic, ctx) (inert stub → wrong-code top-level sentinel), detectInvocationCycle(entry, graph) (inert stub → undefined), plus the CountableFrameKind/WarpFnCall/InvokeChain/DepthOverflowSurface/InvokeGraph shapes and invocationCycleMessage. Plus tests/invoke-depth-cycle.test.ts with 9 tests citing INV-4 and the loom/... codes inline. INV-4: a single chain mixing all three countable frame classes (direct invoke(...), .loom-via-tools:, cross-file .warp fn) increments the one shared per-chain counter (depths 1..32) and fires loom/runtime/invoke-depth-exceeded (invoke chain depth exceeded: 33 > 32, registry-anchored) at the 33rd frame; the counter crosses the subagent boundary unchanged and sibling chains are independent; a cross-file .warp fn is a countable warp-fn-cross-file frame while an intra-file one is not; a top-level overflow surfaces as a Pi system note carrying the diagnostic and a nested overflow as Err(InvokeInfraError { cause: "panic", ... }). loom/load/invocation-cycle (location-less head, registry-anchored invocation cycle: A → B → A): a static A → B → A cycle fires at parse time, while a cycle routed through an unresolvable (leaf) callee is not detected. No new diagnostics-registry codes (reuses the two existing rows). Inert stub seam: no module-level mutable state, no broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; the 8 behavioural tests red on their own primary assertions (counter never increments; wrong residence sentinel; boundary resets; wrong-code/wrong-mode surface; cycle undefined) because the V15b body is absent — fail-red-for-the-intended-reason (the 9th is the cap === 32 constant guard, already provided by V4b). The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V15b — Invoke depth bound (INV-4) and invocation-cycle detection (paired with V15b-T): fills the five behaviour-bearing seams in src/runtime/invoke-depth-cycle.ts. pushCountableFrame(chain, kind) increments the one shared per-chain depth counter (+1 per countable frame class) and enters V4b's enterInvokeFrame cap guard before the child begins — a chain mixing a direct invoke(...), a .loom-via-tools:, and a cross-file .warp fn frame reaches depths 1..32 and fires InvokeDepthExceededPanic (loom/runtime/invoke-depth-exceeded, invoke chain depth exceeded: 33 > 32) on the 33rd frame; the immutable InvokeChain value makes sibling chains independent by construction. warpFnFrameKind(call) classifies a .warp fn call by comparing callerFile against the resolved calleeResidence (cross-file → countable warp-fn-cross-file, intra-file → not countable). crossSubagentBoundary(chain) passes the counter through unchanged. surfaceDepthOverflow(panic, ctx) routes a top-level overflow to the loom/runtime/invoke-depth-exceeded diagnostic (Pi system note) and a nested overflow to Err(InvokeInfraError { kind: "invoke_infra", cause: "panic", callee_path }). detectInvocationCycle(entry, graph) walks the static-resolution graph depth-first, firing the location-less loom/load/invocation-cycle (invocation cycle: A → B → A) on a back-edge and treating an unresolvable callee as a leaf (cycle through it undetected). No new diagnostics-registry codes (reuses the two existing rows); pure functions — no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 9 V15b-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V15j-T — Invoke ceiling-#4 depth-6 params / invoke<T>-return routing (live carrier) tests (paired V15j impl): adds src/runtime/invoke-ceiling-depth.ts — the two invoke-boundary live-carrier seams enforceInvokeParamsDepth(calleePath, paramsValue) (runtime invoke(...) params argument boundary, cause "validation") and enforceInvokeReturnDepth(calleePath, returnValue) (invoke<T> return-value boundary, cause "return_validation"), both inert stubs → never fire, return undefined; plus the InvokeDepthBreach shape (result wrapped Err, error the InvokeInfraError carrier, issue the V5e DepthViolationIssue). Plus tests/invoke-ceiling-depth.test.ts with 5 failing tests pinning hard-ceilings/ceilings-3-and-4.md §Per-boundary destination/surface table (#ceiling-4-table, the params/invoke(...) and invoke<T>-return rows), CIO-3 (#cio-3, depth-walk-before-AJV) and CIO-1 (#cio-1, slash-load cross-route), and invocation.md §Failures (InvokeInfraError { cause: "validation" | "return_validation" } carrier), citing cka-10 inline (the depth ceiling carries no numbered PREFIX-N REQ-ID; V15j is its params/invoke<T>-return co-witness closing leaf, closed by V5e). Delegated live-carrier witness for V5e's params/invoke<T>-return routing rows: a depth-6 runtime invoke(...) params argument trips the loom-owned depth walk (V5e) before AJV and surfaces wrapped as Err(InvokeInfraError{cause:"validation"}), and a depth-6 invoke<T> return value as Err(InvokeInfraError{cause:"return_validation"}); both carry schema_keyword:"maxDepth" (message "JSON document depth exceeds 5"), building on the V15a invoke core. A within-cap (depth-5) value produces no breach and defers to the downstream AJV boundary. Per CIO-1 the params vector targets the runtime invoke boundary, not the binder slash-load params boundary (which cross-routes to ceiling #3, witnessed at V11f/V4e). Message anchored to schema-subset.md §Error shape via V5e's DepthViolationIssue; no new diagnostics-registry codes; the V4d-owned InvokeInfraError schema is unchanged. Inert stub seam: no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 5 tests red on their own primary assertion (a depth-6 value must trip ceiling #4 — the stub returns undefined) because the V15j body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V15j — Invoke ceiling-#4 depth-6 params / invoke<T>-return routing (live carrier) (paired with V15j-T): fills the two invoke-boundary live-carrier seams in src/runtime/invoke-ceiling-depth.ts via a shared enforceInvokeDepth(calleePath, value, cause) helper. Both sites run V5e's loom-owned depthWalk over the materialised value before AJV (CIO-3, #cio-3): a within-cap (depth-5) value returns undefined (defer to the downstream AJV boundary); a depth-6+ breach is wrapped into an InvokeInfraError carrier (kind "invoke_infra", canonical message "JSON document depth exceeds 5", callee_path) and surfaced as Err(InvokeInfraError) per the ceiling-#4 per-boundary table (hard-ceilings/ceilings-3-and-4.md #ceiling-4-table). enforceInvokeParamsDepth carries cause "validation" (runtime invoke(...) params argument boundary); enforceInvokeReturnDepth carries cause "return_validation" (invoke<T> return-value boundary). Both route identically per V5e's routeDepthBoundary (params-invoke/invoke-return -> InvokeInfraError), differing only in cause. Per CIO-1 (#cio-1) the params vector targets the runtime invoke boundary, not the binder slash-load params boundary (which cross-routes to ceiling #3, witnessed at V11f/V4e). The InvokeDepthBreach also carries the depth walk's issue (schema_keyword "maxDepth", canonical message from schema-subset.md §Error shape via V5e's DepthViolationIssue), keeping the V4d-owned InvokeInfraError schema unchanged. No new diagnostics-registry codes; cka-10 co-witness closing leaf (closed by V5e). Pure functions — no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 5 V15j-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V17a-T — Cancellation core tests (paired V17a impl): adds src/runtime/cancellation-core.ts — the loomAbort controller and cancellation contract seams, all inert stubs (createLoomAbort returns a fresh AbortController; forwardSlashCommandCancel/forwardToolExposedCancel/abortForAgentEnd are no-ops so loomAbort never fires; deriveChildLoomAbort returns a fresh UNLINKED controller; routeToolCallLateSettlement wrongly rebinds + re-emits Err/RuntimeEvent after cancel and returns "rebind"; the Checkpoint-seam substrate attachSwallowingHandler attaches NO handler and routeAbandonableSettlement re-surfaces; runCancellableSequence synthesises a top-level cancelled and retains no bindings), plus AGENT_END_CANCEL_MESSAGE and the ToolCallSettlement/ToolCallSideChannels/AbandonableSettlement/SubstrateSideChannels/OperationResult/CancellableStatement/CancellableSequenceOutcome shapes and makeCancelledError. Plus tests/cancellation-core.test.ts with 19 tests citing CNCL-1…CNCL-6 and both the cka-33 row token and this facet's V17a leaf-ID inline (per the coverage-matrix Multi-leaf-row per-facet citing tests convention for the H5f gate). Forwarding (cancellation.md §Signal source / §Forwarding into loomAbort): a cancel forwards through loomAbort.signal (a distinct signal from ctx.signal) across the slash-command, tool-exposed, and invoke-parent entry points, tolerating ctx.signal===undefined. CNCL-4: reason identity (loomAbort.signal.reason === source.reason) landed via the Checkpoint seam across all three paths, first-source-wins under the one-shot guard, and the reason-less agent_end trigger synthesises Error.message byte-exact "loom cancelled by agent_end". Propagation (§Propagation): downward-only (parent→child derived controller; never child→parent; a parent already aborted at spawn yields an already-aborted child). CNCL-1/2/3: a late tool-call settlement after cancellation surfaced is discarded on all three coupled channels (no rebind, no second Err, no second RuntimeEvent). cka-33 / V17a: three-side-channel swallowing-handler suppression at the Checkpoint-seam substrate (no Node unhandledRejection, no second RuntimeEvent, no diagnostic of any severity). CNCL-5: a completed Ok(v) is retained, not rewritten to Err({kind:"cancelled"}). CNCL-6: a tail abort after the final cancellable operation produces no synthesised top-level cancelled. No new diagnostics-registry codes (surfacing kinds reuse the V4d QueryError union). Inert stub seam: no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; 17 of 19 tests red on their own primary assertions because the V17a body is absent — fail-red-for-the-intended-reason; the 2 green tests assert negative invariants (undefined-tolerance, no upward propagation) the inert stub satisfies trivially. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V17a — Cancellation core (paired with V17a-T): fills the behaviour-bearing seams in src/runtime/cancellation-core.ts. A shared forwardSignalReason(loomAbort, source) forwards each source signal into loomAbort carrying the source's reason — synchronous if already aborted, else a one-shot abort listener — so forwardSlashCommandCancel (tolerating ctx.signal===undefined), forwardToolExposedCancel, and deriveChildLoomAbort all route through it; AbortController idempotency makes the first source's reason win under the one-shot guard (CNCL-4 reason identity across all three forwarding paths). abortForAgentEnd aborts with new Error(AGENT_END_CANCEL_MESSAGE) (byte-exact "loom cancelled by agent_end"). deriveChildLoomAbort returns an independent child controller linked parent→child only (downward-only §Propagation; never child→parent; a parent already aborted at spawn yields an already-aborted child carrying its reason). routeToolCallLateSettlement discards on all three coupled channels once cancellationSurfaced (CNCL-1: no rebind; CNCL-2: no second Err; CNCL-3: no second RuntimeEvent), else rebinds the timely resolved value. attachSwallowingHandler attaches .then(onResolve, onReject) at construction (before the first microtask boundary) and returns the original Promise; routeAbandonableSettlement discards silently once cancellation surfaced — no Node unhandledRejection, no second RuntimeEvent, no diagnostic of any severity (cka-33 / V17a Checkpoint-seam substrate suppression); a diagnostic-worthy OOM-style late rejection is still discarded (no promotion to loom/runtime/internal-error). runCancellableSequence awaits checkpoint.before(kind, site) and reads the signal before each run(): an abort observed at a checkpoint surfaces Err({kind:"cancelled"}) at that position while retaining every already-completed binding verbatim (CNCL-5 — no retroactive Ok→Err rewrite), and a run-to-completion (abort only in the pure tail, no further checkpoint) keeps the produced value with synthesizedTopLevelCancelled:false (CNCL-6 — no top-level synthesis on tail abort). No new diagnostics-registry codes (surfacing kinds reuse the V4d QueryError union). Pure functions — no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. All 19 V17a-T tests green; typecheck + lint clean. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
2026-07-01 V4c-T — Terminal outcomes / partial-append non-mutation tests (paired V4c impl): adds src/runtime/terminal-outcomes.ts — the runtime side of the partial-append contract (errors-and-results/error-model.md §Partial-append contract, ERR-8 … ERR-12; cancellation.md §Surfacing). Declares the committed-surface model (CommittedSurface/CommittedSurfaceKind), the CommittedConversationMutator the runtime holds against Pi's committed conversation (truncate/rewrite/replace/remove/injectCompensatingTurn), the handlePartialTerminalOutcome(outcome, mutator) entry (over PartialTerminalPath = cancelled | question-propagation and DrivenConversationMode = prompt | subagent), and the classifyNonMutationWindow(events) ERR-11 scope helper over WindowTimelineEvent. Non-compliant stub bodies so the tests red on their non-mutation primary assertions: handlePartialTerminalOutcome truncates + rewrites the first committed surface (ERR-8) and injects a compensating turn (ERR-9) on every path/mode; classifyNonMutationWindow mis-scopes (closes at the LAST driver send, collects every append). Plus tests/terminal-outcomes.test.ts with 6 tests citing ERR-8 … ERR-12 inline. ERR-8: no truncate/rewrite/replace/remove of a committed surface after a mid-stream cancellation. ERR-9: no compensating-turn injection. ERR-10: ERR-8/ERR-9 hold symmetrically across the cancellation and ?-propagation paths. ERR-11: the non-mutation window opens at the cancelled streaming turn and closes at the NEXT driver send, excluding respond-repair appends after that send. ERR-12: ERR-8 holds inside a subagent loom, exercised via the H4a/H4c harness modelling a subagent-mode callee (scriptSubagentCallee → subagent-loom outcome), not the live V9i surface. No new diagnostics-registry codes. Inert stub seam: no module-level mutable state / broad catch / ambient primitive / Promise combinator / Sync call. Typecheck + lint clean; all 6 tests red on their own primary assertions (the stub mutates / injects / mis-scopes) because the V4c body is absent — fail-red-for-the-intended-reason. The 4 pre-existing V4g-T reds in an unrelated subsystem (tests/pre-evaluation-reload-failure.test.ts, red on HEAD without this change) are unaffected.
