# Coverage matrix (fixture)

FRNT-1 (runtime) is mapped; FRNT-2 (terminology, non-executable) is deliberately
absent — the gate must NOT flag it as an unmapped executable REQ-ID.

| REQ-ID | Closing leaf(s) |
|---|---|
| FRNT-1 | `V6e` |
