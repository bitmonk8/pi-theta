// Seeded fixture test corpus. Cites the runtime REQ-ID FRNT-1 inline so its
// mapped-REQ-ID citing-test obligation is satisfied. FRNT-2 is a terminology
// (non-executable) REQ-ID and carries no citing-test obligation.
import { it } from "vitest";
it("FRNT-1 malformed-frontmatter rejection", () => {});
