# Placeholder (fixture spec page — PLAC)

The interpreter MUST honour this obligation. It carries no numbered REQ-ID and
no diagnostics-registry code, and its only coverage-matrix entry is a `<new>`
placeholder row that names no real closing leaf — so the gate treats it
identically to an un-enumerated MUST and reddens.
