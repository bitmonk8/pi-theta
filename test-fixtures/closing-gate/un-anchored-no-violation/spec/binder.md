# Binder (fixture table-of-contents hub stub)

This is a table-of-contents hub stub for the `binder/` subtree. It carries no
REQ-ID anchors and no prefix-table row of its own; its obligations live in the
files beneath the subtree, which a reader MUST consult.

Even though it carries a MUST token, it is excluded from the un-rowed-page
residue defect because its filename stem matches the `binder/` subtree row.
