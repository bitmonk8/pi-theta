# Plan leaf-ID universe (H5e fixture stand-in)

The real plan leaf-ID universe the H5e closing-leaf resolver matches each
Code-keyed closing-leaf token against. A token in this set resolves to a real
plan leaf; `<new>` and any token absent here do not.

- `V8a`
- `V8b`
- `H7a`
