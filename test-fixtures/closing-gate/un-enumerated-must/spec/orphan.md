# Orphan (fixture spec page — ORPH)

This page carries a normative obligation: the loader MUST reject a malformed
header. The obligation carries no numbered REQ-ID and no diagnostics-registry
code, and no Code-keyed obligation-areas row enumerates it — so the gate reddens
as an un-enumerated un-anchored MUST.
