# Stray (fixture spec page absent from the prefix table)

This spec-shaped page has no prefix-table row and is not a table-of-contents hub
stub — there is no `stray/` subtree row binding it — so the gate reddens it as
an un-rowed-page residue defect.
