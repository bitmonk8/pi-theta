# Triage log — rejected candidates (append-only)

Every rejected candidate finding becomes one row here and its file is deleted
(git history keeps the full text). Reviewers and triage pre-read this log so the
same false positive is not re-filed by a later wave.

| date | candidate | verdict | reason |
|---|---|---|---|
| 2026-09-08 | qw20260907130901-d2-03-seams-barrel-entries-unimported.md | false-positive | verdict: false-positive — the barrel is an exact rule-governed mirror of each seam module's interface surface (checkpoint 3/3, clock 2/2, file-system 2/2, file-watcher 6/6, token-estimator 1/1, id-source 1/1, schema-validator 4/4 interface-side; only the below-divider Ajv implementation types are omitted), and all six "unconsumed" names are declared constituents of interfaces its importers do import — `Checkpoint.before(kind: CheckpointKind, site: CheckpointSite)`, `CompiledValidator.validate() -> readonly ValidationError[]`, `FileWatchEvent.kind: FileWatchEventKind`, `FileWatcher.watch(..., onTerminate?: OnWatchTerminate): Unsubscribe` with `OnWatchTerminate = (t: WatchTermination) => void` — so the header ("single source of each interface's shape") matches the entry list and the claimed silent mismatch does not reproduce (triage: claude-opus-5) |
| 2026-09-08 | qw20260907183353-d2-02-doc-comment-lines-field-write-only.md | false-positive | verdict: false-positive — the no-reader greps reproduce, but the field is spec-mandated, not cruft: spec_topics/grammar.md:204 and descriptions.md §Placement require fn/variant `///` text be "preserved on the AST for tooling that wants to render it", and since `FnDecl` has no `description` slot and `attachDocDescriptions` never keys fn/variant lines (:2242 "survives only as the floating `DocComment` sibling"), `lines` is that text's ONLY carrier — refuting the claim that "the attachment channel carries the content" and the suggestion to drop it (triage: claude-opus-5) |
| 2026-09-08 | qw20260907183353-d2-03-cancellable-sequence-generality-unused.md | false-positive | verdict: false-positive — every "unused" channel (N-statement loop, named `binding`, `bindings` map, `synthesizedTopLevelCancelled`) has deliberate witness-test consumers discharging CNCL-5/CNCL-6, which coverage-matrix.md:102 assigns to this exact function (`V17a`), and CNCL-5 is unstatable on a single-statement runner (tests/cancellation-core.test.ts:415-453 needs two distinct bindings and reads `bindings.get("x")`) — tests-only consumers are not deadness in this repo, so the generality is spec-mandated, not speculative (triage: claude-opus-5) |
| 2026-09-08 | qw20260907183353-d2-03-lower-query-schema-two-pins-count-stale.md | false-positive | verdict: false-positive — "the two shipped seam-contract pins" names a specific pair, not a census of two-argument callers: the "(d) TOTALITY PINS — the seam contract the fix deliberately does NOT change" section of tests/unresolved-annotation-lowering.test.ts holds exactly two two-argument calls (:1040 `("NotDeclaredAnywhere", [])`, :1050 `("", [])`), still exactly two today, and the candidate's own regex requires an identifier second argument so it structurally cannot match `, [])` — it counted a disjoint set; the drift story is further refuted because the sentence was written in bug-0028 commit 1bd9361a (which added `enums`) when ~17 two-argument callers already existed (triage: claude-opus-5) |
| 2026-09-08 | qw20260907183353-d2-04-executebodydeps-optional-rationale-stale.md | false-positive | verdict: false-positive — the doc says "existing constructors", never "src constructors"; all 25 typed test constructors omit `invokeChain` and 10+ omit `emitDiagnostic`, and tsconfig includes `tests` under `strict`, so "a required field would flip every one of them" reproduces as presently TRUE (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-02-line-carries-reserved-key-passthrough.md | false-positive | verdict: false-positive — the doc says "its existing call sites", not "its existing src/ call sites", and those call sites exist: `git show 470071d8^:tests/subagent-envelope.test.ts` already carried the import plus four boolean cells at lines 26/281/283/285/287, byte-identical to HEAD, and `git show 470071d8 -- tests/subagent-envelope.test.ts` is empty for this identifier, so the commit deliberately left them as the pin and said so in its own new witness test ("`lineCarriesReservedKey` stays a `boolean` wrapper ... the existing boolean cells in `tests/subagent-envelope.test.ts` remain the pin on that surface") — the sentence was true when it landed, refuting the "false on the day it landed / names an empty set" anchor; the residual pass-through complaint is a filing against test-only-reachable production code, which this repository's convention (and the lens brief's "file nothing against test-only-reachable production code") treats as alive. (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-03-runtime-exported-types-unimported.md | false-positive | verdict: false-positive — all nine annotate exported symbols consumed cross-module (`evaluateQuestion`/`verifyChildCallableHashes` by production statement-executor.ts:67, production-theta-producer.ts:275, production-composition.ts:56/1503; `dispatchNoParamsTheta`/`rendersTranscriptCard`/`SlashPromptDriveDeps` by tests/slash-dispatch.test.ts:28-32 and b0401:83), so the exports are live public-signature surface, not dead code (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-03-toInterpolationType-arms-unreachable.md | false-positive | verdict: false-positive — deleting both arms fails typecheck with TS2366 at :501 (exported total fn over the 9-kind SystemParamType union, so they are compiler-mandated, not removable dead code), and the comment's "renderSystemPrompt overrides this" flow demonstrably exists at renderSystemPrompt:629-635 + interpolationTypeOfValue:645-650 and is bug 0408's documented fix design (docs/bugs/0408:158) (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-04-stdlibparamkind-importer-roster-stale.md | false-positive | verdict: false-positive — git log -S over all history shows StdlibParamKind never appeared in stdlib-array.ts or stdlib-object.ts (type + doc landed together in 52712fb3), so nothing is vestigial; both modules do import the shape from stdlib-string via StdlibMemberSignature and spell StdlibParamKind literals in ARRAY_/OBJECT_MEMBER_SIGNATURES, so the doc points readers at the right files and only the verb "imported" is loose (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-05-body-type-lowering-consumer-roster-stale.md | false-positive | verdict: false-positive — every excerpt byte-matches at the cited lines and the 4-production-importer census reproduces (theta-document.ts:105-110, query-schema-lowering.ts:135-141, import-static-checks.ts:91, type-layer-checks.ts:86), but the asserted contradiction does not: the sentence's subject is the lowering it introduces ("the single canonical place that lowers a theta body's `schema`/`enum` declarations"), and `buildBodyTypeSchemas` has exactly the two production callers the two bullets name — theta-document.ts:1980 inside `collectBodyTypes` (:1931) and query-schema-lowering.ts:168 inside `lowerQueryResponseSchema`, whose own header at :21 calls it "the two-pass whole-file `buildBodyTypeSchemas`" — while the two unlisted importers touch only `collectUnresolvedNamedTypes` on ONE type source with an EMPTY declared set, i.e. neither whole-file nor resolving: import-static-checks.ts:233 (`referencedNamedTypes`, whose own doc at :222-231 says handing it "an EMPTY declared set turns that refusal list into a plain reference walk") and type-layer-checks.ts:1319 (`NO_DECLARED_TYPE_NAMES`, reading only the `unspellable` out-parameter), so no third "whole-file `NamedType` resolver" exists to make the numeral wrong; the finding's load-bearing premise that the header is "the module's only statement of who depends on it" is refuted by the second importer roster at body-type-lowering.ts:26-34 (and by the per-function rosters at :50 and :604), and its "exhaustive" census is not — its two grep patterns structurally cannot match the 13 test files importing `../src/parser/body-type-lowering` (e.g. tests/discriminator-field-classifier-brace-group.test.ts:6, tests/e2e-s3-schema-lowering-conformance.test.ts:25), contradicting its own "no test file imports the module through a different path" check; git dates check out (header 369facac 2026-07-11, consumers 9eb1290d/d03f7398) but a later importer of a different export does not falsify a claim scoped to the lowering, leaving only a taste-level wish for a fuller roster (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-05-envelope-scan-unparseable-lines-speculative.md | false-positive | verdict: false-positive — every cited fact reproduces (doc 423-434, producer 442-457, `scanStreamForEnvelope` has zero src/extensions/tools callers, `unparseableLines` read only at tests/subagent-wire-parse-failed-classifier.test.ts:112-113/124-125, no string-keyed access, no `export *`, 470071d8 added it), but that makes this a filing against test-only-reachable production code, which the lens brief forbids ("file nothing against test-only-reachable production code") and this repository's convention treats as alive: the field's user count is not zero, it is one deliberate witness caller — the same test whose header states its purpose is asserting "that `scanStreamForEnvelope` ... surfaces the unparseable lines it skipped instead of merging the classes a second time", the named discharge of bug 0086 §Actual behaviour item 5 — so the "speculative generality, zero users" anchor is the disallowed deadness complaint applied one level in (the field is exactly as alive as its producing function), and the sibling filing on this same module and commit (d2-02) was rejected on the same ground. (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-05-pi-token-estimator-body-comment-restates-header.md | out-of-scope | verdict: out-of-scope — both excerpts byte-match at :3-7/:18-20 and the restatement is real (the body's 3 clauses are a strict subset of the header, all still true: named import at :12, direct return at :21, file is 23 lines, no tool reads the text), but the sole anchor is duplication between live, factually-current prose copies, which the D2 brief lists as do-not-file and README assigns to the D4 lens; blame refutes any residue reading — the header dates to stub commit 7eb788a5 while the body comment was authored fresh in implementation commit a1b553bb (the same commit that deleted the stub-status note), unlike the confirmed doc-duplicate siblings whose blame proved excision residue; same ruling as qw20260907202646-d2-04-params-defaults-paragraph-restated (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-07-errnote-event-forward-hook-comment-stale.md | false-positive | verdict: false-positive — comment conditions on an origin-site *emission*, not on the param having a caller: 0383 §Residuals 1 defines that as "no `topLevelCascade: true` caller of `emitRuntimeEvent`", still true at HEAD (its lone production caller, query-discard.ts:219, passes `false`), and bug 0399 §Non-goals re-affirms "no origin emission exists" — 0399 fed the hook an origin-*constructed* event, and reconstruction remains the norm (validation-only), so the cited premise is inverted (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-08-filesystem-activeroots-consumer-roster-stale.md | false-positive | verdict: false-positive — the INV-1-scoped block asserts nothing false and never claims exclusivity; the "unstated" fileSystem-absence consequences are documented at 843-851 (with an explicit cross-reference to #recheckCalleeContainment's same skip condition), 868 and 1700, and activeRoots is supplied alongside the spawn seams at the single production composition site (production-composition.ts:859) so `thetaDirs: []` is unreachable in production (triage: claude-opus-5) |
| 2026-09-08 | qw20260908115521-d2-04-pre-eval-failure-count-eight-stale.md | false-positive | verdict: false-positive — the "eight" restates error-model.md:13's pre-evaluation failure list ("the eight below", ERR-1…ERR-7 + ERR-16), which the same sentence quotes; every "seven" in src/ is the narrower load-time subset that load-pre-eval.ts:49-52 explicitly defines as the eight minus ERR-7 ("split out to `V4g` and is not a member here"), so 7 load-time + 1 watcher-time = 8 and no count disagrees (triage: claude-opus-5) |
| 2026-09-10 | qw20260907183353-d2-01-toolsfield-scalar-absent-arms-unconstructed.md | false-positive | verdict: false-positive — every excerpt and search reproduces (production sites now at production-composition.ts:2369/:3180 after ~118-line drift, both `kind: "list"`; ToolsField `scalar`/`absent` constructions only at tests/callable-set.test.ts:73/:272, subagent-fn.test.ts:588, tools-entry-closed-grammar.test.ts:539), but the anchor fails on two mechanical grounds: (1) the "vestigial" narrative ("production wiring stopped exercising [the arms] when the frontmatter layer took over") asserts a history that never happened — `extractToolsList` landed in H8b (4c0a4c20, 2026-07-03) BEFORE the first production `resolveCallableSet` call in V20a (44faae57, 2026-07-04), and that first call already passed `{ kind: "list", items: toolsList }`, so no production construction of the other arms ever existed to become residue; (2) what remains is a filing against test-only-reachable production code, which this repository treats as alive — the arms are held by four deliberate spec-anchored witness constructions (the V6c-T seam contract's "both YAML spellings parse to the same callable set" and "absent / empty `tools:` yields the empty callable set" cells), and the layer split is bug-mandated rather than incidental: frontmatter.ts:1948-1970 must dispatch on the raw scalar-vs-sequence arm to refuse a zero-entry scalar under `theta/load/malformed-tools-field` (bugs 0104/0206) while keeping `tools: []` silent, so the header's contract claim (callable-set.ts:8-9) stays true of the module and frontmatter.ts:600-605 documents its own collapse; same ruling as the d2-02 and d2-05 envelope siblings (triage: claude-opus-5) |
| 2026-09-10 | qw20260907202646-d2-02-generic-argument-wire-bound-claim-restated.md | out-of-scope | verdict: out-of-scope — all 12 excerpts and the grep (13 hits, exact lines) reproduce and the repeated claim is TRUE (`lowerTypeExpr`, params.ts:743-982, has no inline-object arm: a `{…}` generic argument falls to the `unspellable` atom sink at :980-981 and lowers to `{}`), so nothing here is stale; blame refutes any residue reading — every site was authored or rewritten in one commit, 6184e7c30 (bug 0233), which removed `insideGenericArgument` from `walkType` and deliberately EXPANDED the narration from 6 to 9 `never divid` statements while doing so (leftovers do not grow) — which makes the sole anchor duplication between live, factually-current prose copies, the D2 brief's do-not-file and README's D4 lens, same ruling as qw20260907202646-d2-04-params-defaults-paragraph-restated and d2-05-pi-token-estimator-body-comment-restates-header; the candidate's tallies are also loose (only 9 of 12 sites carry the lowering/wire ground — :62-66 is a pure cross-reference to the duplicate rule, refuting "none is a cross-reference", and :89-92 / :1534-1536 state only the answers-alike consequence and the unchanged `rules`/`position` recursion; the 13 hits fall in 9 not 8 passages; 4 not 3 sites name `lowerTypeExpr`, 3 not 2 name code-registry-parse.md) and no git-history check was run (triage: claude-opus-5) |
| 2026-09-10 | qw20260907202646-d2-04-params-defaults-paragraph-restated.md | out-of-scope | verdict: out-of-scope — independently re-verified: all three excerpts byte-match at wire-translation.ts:35-43 / inbound-boundary.ts:138-148 / runtime-value-model.md:37 and every stated search reproduces (2 hits for "states the same mechanism", no doc tooling in package.json, no test reads either module's text, bindParamsInbound:150 → decodeInboundValue:77 → translateInbound:84), but the sole anchor is duplication between live, factually-current prose copies, which lens-d2-cruft.theta:45 lists under "Out of scope (do not file)" and README's lens roster assigns to D4; blame refutes the only D2-eligible reading (excision residue, the ground on which PTQ-0034/0105/0111 were confirmed): both first sentences were authored together in e73c1acae and both closing sentences together in 5a92a36c9 (bug 0186), where they deliberately REPLACED a spec-divergence tracking note ("rvm:37 still states that defaults 'bypass'…") with a spec-agreement pointer in the same commit that rewrote rvm:37 — fresh parallel authoring, not a leftover; the side-claim that the wire-translation.ts copy is "not about anything this module declares" is a placement opinion (also do-not-file) and inaccurate, since "this pass"/"here" is the translation pass this module owns (triage: claude-opus-5) |
| 2026-09-10 | qw20260907202646-d2-04-type-alias-exports-unimported.md | false-positive | verdict: false-positive — every mechanical claim reproduces (declarations byte-exact at type-compat.ts:498/:759 and type-grammar.ts:213; 48 import statements from the two modules name none of the three; no namespace/dynamic import, `export *`, barrel or package `exports`; the theta-document.ts excerpt drifted 9688→10003 content-identical; a tsc 5.9 fixture under this tsconfig's `declaration: true` confirms dropping `export` compiles), but the anchor does not hold: each alias annotates the signature of an exported function consumed cross-module — `classifyIndexReceiver` by expression-evaluator.ts:625, stdlib-object.ts:72 and tests/typeenv-prototype-names.test.ts:1010-1055; `commonType` by static-type-inference.ts:607 and tests/match-fn-return-lub-dominating-discipline.test.ts:682; `parseTypeExpression` by params.ts:225, schema-subset-gate.ts:123, theta-document.ts ×9 (the `rules` value selected at :10007) and 7 test files — which is exactly the ground on which the same-wave, same-reporter sibling d2-03-runtime-exported-types-unimported was rejected (TRIAGE_LOG row 15: "live public-signature surface, not dead code"); the fixture's emitted .d.ts shows the alias lands in the module's public declaration whether or not it is exported, so the keyword widens nothing and only makes an already-public type nameable; a repo-wide census finds 260 of 727 `export type`/`export interface` names in src/ referenced in no other file, so this is the codebase's convention rather than PTQ-0150's proven outlier (there, every other constant of the kind reached a further file); and the export was deliberate, recorded intent (docs/bugs/0081:240 "new exported `CompatRelation`"; type-compat.ts:750-758 documents the injectable-relation seam; PTQ-0025's triage lists `TypeCheckRules` among type-grammar's intended surface) (triage: claude-opus-5) |
| 2026-09-10 | qw20260907202646-d2-05-bypass-params-field-feeds-both-roster-stale.md | false-positive | verdict: false-positive — every excerpt reproduces (doc now binder-envelope.ts:152-156; `params(\?)?\.fields` grep now 18 hits, the extra being new prose at type-layer-checks.ts:317; the 11 production readers are real) but the stale-roster anchor does not: the "Feeds BOTH" sentence was not written "when the binder seam introduced it" (66db8874, 2026-07-01, read "for the load-time bypass classification") but in b027a524 (bug-0011, 2026-07-28), the same hunk that added `defaultSource`, and `git grep` at b027a524 shows five of the eleven "unnamed" readers already live then (production-composition.ts:1246 arity, production-theta-producer.ts:864 `bind_echo` / :3103 subagent names, theta-document.ts:3942 roots / :4449 rootLocals), so the sentence never was a consumer census that later adoptions could outgrow — it states the record's two binder-side roles (why prompt-line data rides a `Bypass…` record) and its literal claim is true, with no "only"/"exactly"/"two consumers" wording, unlike PTQ-0086/PTQ-0137 where git proved decay; frontmatter.ts:124 is not a filed location; what remains is a wording preference, same ruling as the d2-05 body-type-lowering roster rejection (triage: claude-opus-5) |
| 2026-09-10 | qw20260907130901-d2-08-macrotask-ms-param-constant.md | false-positive | verdict: false-positive — independently re-verified and the filed claims do not reproduce: HEAD has exactly ONE `macrotask(` call site (:5947, `#pollWhile`); the filed "call site 2" (`driveStreamedUserTurn`'s local `pollWhile`, filed at :7223) is not line-drift, it is gone — 4f2db584 deleted the whole function on 2026-09-08, the day after this 2026-09-07 filing — so "at both of its call sites"/"exactly two"/`sites: 3` fail reality-check, not just staleness; the filing's own False-positive-check git claim ("no commit introducing a call that passes anything other than POLL_INTERVAL_MS") is independently refuted, confirmed by rerunning both forms myself: `git log -S macrotask -- <file>` (no `--follow`) stops dead at the rename 2bc69157 exactly as the filer ran it, while `git log --follow -S macrotask` surfaces fed12acd (2026-07-03), which called `macrotask(this.#clock, SUBAGENT_MID_STREAM_CANCEL_MS)` (250 ≠ POLL_INTERVAL_MS's 10) until 20540abc removed it hours later — so the helper's generality was designed and exercised, not merely asserted, which sinks "configurability nothing uses" as a live counter-fact rather than a taste quibble; with the premised second call site gone the filing's own "why" (cross-call-site consistency burden) has no second site left to be a burden across (triage: claude-opus-5) |
| 2026-09-11 | qw20260907183353-d2-03-stdlib-anchor-exports-unimported.md | human-convention | human ruling (thomasa, 2026-09-11): an export keyword with no external importer is not cruft in this repo (12/19 _HINT anchors, the invoke-diagnostics.ts mirror, and ~16 sibling sites share the shape); diagnostic anchors are published as citable/greppable surface for the code registry docs; de-exporting one module creates inconsistency, not less cruft. Precedent: file only when the declaration itself is dead |
| 2026-09-11 | qw20260907202646-d2-01-ajvschemavalidatordeps-dead-export.md | human-convention | human ruling (thomasa, 2026-09-11): constructor-injection *Deps interfaces are exported so composition roots may annotate their deps object (19/40 *Deps in src/ share the condition); the interface types the live constructor and #deps field, only the export keyword is unreached; house style, ruled wholesale. Precedent: an export with no external importer is not cruft when the declaration is alive |
| 2026-09-11 | qw20260908115521-d2-03-host-incompatible-kind-export-unimported.md | human-convention | human ruling (thomasa, 2026-09-11): 228/699 exported types in src/ have no out-of-file referencer, including structurally identical kind discriminators (AssignTargetKind, CommittedSideEffectKind); the alias is alive as the single spelling of clause (ii)'s discriminator; house export style, ruled wholesale with the two sibling filings |
| 2026-09-11 | qw20260907130901-d2-02-swallow-route-unread-inputs.md | human-witness-seam | human ruling (thomasa, 2026-09-11): the channels/settlement inputs are the deliberate red/green observation seam for cka-33 channels 2-3 (git f53245b3/0d55d094) - a threaded-but-unread handle whose only reader is a MUST-NOT witness test is this repo's spec-obligation posture, not cruft; the type system proves the handle unread today, the witness is the tripwire against a future mutating call. Noted as the most flippable of the three (production fabricates noopSwallowChannels() at 3 sites) - revisit only if the seam posture itself is revised |
| 2026-09-11 | qw20260907130901-d2-03-watcher-recovery-registry-unread.md | human-witness-seam | human ruling (thomasa, 2026-09-11): the field is the subject of PIC-55's MUST-NOT witness (watcher-terminated-recovery.test.ts:163-167 asserts no drain tag lands on the registry handed in); removing it makes the witness tautological and touches 8 call sites for zero behaviour; deliberate seam, do not file. Same ruling as the swallow-route and ExecuteBodyDeps.mutator siblings |
| 2026-09-11 | qw20260907202646-d2-01-executebodydeps-mutator-mode-discarded.md | human-witness-seam | human ruling (thomasa, 2026-09-11): the empty handlePartialTerminalOutcome body IS the landed ERR-8/ERR-9 contract and statement-executor.test.ts:585-638 threads a RecordingMutator through these exact fields to witness it (red under the non-compliant V4c-T stub, green now); 29 test construction sites of churn for zero behaviour; deliberate witness seam, do not file |
| 2026-09-11 | qw20260907130901-d2-02-rung1-probe-constant-false.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): capability-probe.md:84 / subagent.md:168 state the two-conjunct rung-1 formula verbatim ('Both conjuncts are required'); bug-0001's fix (b8d4fd2c) deliberately replaced the plain false the candidate proposes restoring, and the surface-present/no-dispatcher host shape has a dedicated e2e regression (extension-tool-unreachable-load-refusal-e2e.test.ts:491-545); one typeof per compose pass against divergence from the spec's stated formula. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907130901-d2-03-followup-surfacing-free-phase-arm-unproduced.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): the arm mirrors the spec-named two-valued scalar it feeds (MaskedPredicateInput.turnKind, PIC-1 (d)) whose free_phase IS exercised one hop downstream and whose consumer would honour the value; narrowing it spells one spec scalar two ways; already recorded as non-blocking residual R2 in docs/bugs/0355. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907130901-d2-04-discarded-query-check-constant-inputs.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): the only unused surface is QRY-19's accepted-form arms, which tests/query-discard.test.ts:119-138 deliberately witnesses; the function states the spec's accepted forms as data and the parser happens to route one shape today; cross-module churn for zero behaviour. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907130901-d2-04-sidecar-anonymous-union-kind-undiscriminated.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): the variant was born unread (68bf05b1) as a deliberate mirror of schema-subset.md step 5(2)'s named category and documents WHY the value is absent from the named-enum map; collapsing it moves a spec-anchored fact into a comment. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907202646-d2-05-number-kind-discriminator-inert.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): BNDR-4 and BNDR-5 are separately normative and the two arms were born byte-identical with today's rationale doc present (e78482b9) - no removed feature, no scaffolding; the header pins kind-from-static-type as the caller's obligation; collapsing touches 5 production callers + 21 test sites to merge two spec-anchored describes. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907202646-d2-07-thetalib-form-let-query-indistinguishable.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): the union mirrors imports.md:13's three-way enumeration of non-permitted forms and tests/imports.test.ts:165-174 witnesses each named category; the arms landed with the feature, not as scaffolding. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907183353-d2-03-evaluate-question-thunk-eager.md | human-by-design | human ruling (thomasa, 2026-09-11): the thunk arrived test-first (10d797bd) with the V4b-T ?-bypass witness (6 passing tests drive throwing thunks through it); the cost of keeping it is two () => wrappers, the cost of removing it is dismantling a live witness of a panic-bypass obligation. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907183353-d2-03-probe-readable-cache-constant-true.md | human-by-design | human ruling (thomasa, 2026-09-11): nothing is dead (the write drives all resolution; the boolean backs the live ThetaLibDirectoryProbe.entryReadable contract) and docs/bugs/0428 records probe-side readable=false as the un-implemented Fix Option 2 and 'the coherent end state' - a Map->Set flip that documented end state would flip straight back. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907183353-d2-04-annotation-inferred-brace-guard-subsumed.md | human-by-design | human ruling (thomasa, 2026-09-11): guard and general gate entered in the same commit (72272a20), so no supersession or vestige; the redundancy reproduces (fuzzed 4129 inputs, zero divergence) but docs/bugs/0130 Fix (f) already names both and records a held disposition, and bugs 0093/0220/0222 cite the guard by name - deleting it orphans four bug-doc citations for zero behaviour. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907183353-d2-05-inference-checkcompatible-knob-constant.md | human-by-design | human ruling (thomasa, 2026-09-11): explicit in-repo rationale rejects the direct import (type-compat.ts:749-758 names and rejects 'importing checkCompatible into the inference pass'; static-type-inference.ts:616-624 'rather than reach for a copy that would silently bypass it'); the deps interface, constructor and per-site literal all survive for enumNames, so the fix buys one field against documented intent. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907183353-d2-10-parseparams-inline-fragments-inert.md | human-by-design | human ruling (thomasa, 2026-09-11): inert-by-design from birth and stated so in code at params.ts:190-192; docs/bugs/0039 Fix wires the same sink triple at all three mint sites for uniform posture (the finding miscounted them as two, omitting query-schema-lowering.ts:192). Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907130901-d2-03-execute-for-evaluateforloop-passthrough.md | human-by-design | human ruling (thomasa, 2026-09-11): the drive is behaviour-free as claimed, but the seam is the module header's documented design and coverage-matrix V3c's 'executor iteration authority'; the proposed inlining would demote evaluateForLoop/ForLoopHost to test-only production code - creating the dead-seam shape a cruft lens should abhor - and the honest alternatives (delete the seam + its matrix row, or route evalParFor through it too) are architecture calls outside D2. Residual noted: the executeFor/evalParFor asymmetry. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260911104855-d2-03-binder-model-doc-inline-duplicate.md | false-positive | verdict: false-positive — the exact "bypass-eligible…never call(s) the binder" rule recurs well beyond the cited pair: resolveBinderModel's own doc (binder-model.ts:201-204, "Bypass-eligible thetas skip both checks") is a third, immediately-adjacent restatement the candidate quoted in its own false-positive check but didn't count, plus independent restatements in extension/reload-wiring.ts:110-120 and extension/production-composition.ts:1228-1237; bindModelUnresolvable's rule, with the identical "(bug 0297)"/"(the ABSENT-field behaviour)" phrasing, is independently restated a third time in src/parser/frontmatter.ts:139-147 — this is a deliberate cross-file documentation convention for these two bug-linked invariants, not the isolated two-site duplication the finding's narrower-scoped convention check claims (triage: claude-opus-5) |
| 2026-09-11 | qw20260911104855-d7-01-parsedeps-reimplemented-not-imported.md | false-positive | verdict: false-positive — the file's own header comment (:144, :215-216) states this harness is copied "verbatim" from tests/non-object-receiver-gate.test.ts's "group-(e) pattern" (added one day earlier), and the identical local parseDeps/parseOnly shape recurs in 79 other test files (several, e.g. array-sink-unresolvable-deferral.test.ts, explicitly citing this very file's range as "the pattern"), so this is a documented, widely-repeated sibling convention, not an isolated oversight that skipped the e2e-s1.ts import demonstrated by one comparison file (triage: claude-opus-5) |
| 2026-09-11 | qw20260911104855-d7-03-dispatch-harness-scaffolding-duplicated.md | false-positive | verdict: false-positive — core claim "no comparable module exists ... which is why each site re-derives it in place" is false: tests/helpers/call-with-clause-harness.ts already exports rootDouble()/noopPi()/driveCtx(cwd), and 107 test files roll their own local rootDouble()-shaped RuntimeRoot double while 186 files use the identical "(mirrors <sibling>)" comment for intentionally per-file-duplicated harnesses — this is the suite's pervasive, established convention, not a gap isolated to the 3 cited files (triage: claude-opus-5) |
| 2026-09-12 | qw20260912091742-d7-02-b0328-discoveryhost-harness-duplicated.md | false-positive | verdict: false-positive — b0328's own cited comment mirrors the older tests/subagent-child-hash-refusal-e2e.test.ts (added 2026-07-24, continued by b0343 on 2026-08-31), not tests/helpers/production-load-harness.ts, which git log shows was added 2026-09-11 — two weeks after b0328 (2026-08-29) — for a differently-shaped runProductionLoad/diagnosticLines family used by only 1 of the 21 files sharing that name; b0328 continues an independent, already-repeated (3x) sibling convention this finding never accounts for, not a skipped import of a pre-existing helper (triage: claude-opus-5) |
