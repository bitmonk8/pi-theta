# Triage log — rejected candidates (append-only)

Every rejected candidate finding becomes one row here and its file is deleted
(git history keeps the full text). Reviewers and triage pre-read this log so the
same false positive is not re-filed by a later wave.

| date | candidate | verdict | reason |
|---|---|---|---|
| 2026-09-08 | qw20260907130901-d2-03-seams-barrel-entries-unimported.md | false-positive | verdict: false-positive — the barrel is an exact rule-governed mirror of each seam module's interface surface (checkpoint 3/3, clock 2/2, file-system 2/2, file-watcher 6/6, token-estimator 1/1, id-source 1/1, schema-validator 4/4 interface-side; only the below-divider Ajv implementation types are omitted), and all six "unconsumed" names are declared constituents of interfaces its importers do import — `Checkpoint.before(kind: CheckpointKind, site: CheckpointSite)`, `CompiledValidator.validate() -> readonly ValidationError[]`, `FileWatchEvent.kind: FileWatchEventKind`, `FileWatcher.watch(..., onTerminate?: OnWatchTerminate): Unsubscribe` with `OnWatchTerminate = (t: WatchTermination) => void` — so the header ("single source of each interface's shape") matches the entry list and the claimed silent mismatch does not reproduce (triage: claude-opus-5) |
| 2026-09-08 | qw20260907183353-d2-02-doc-comment-lines-field-write-only.md | false-positive | verdict: false-positive — the no-reader greps reproduce, but the field is spec-mandated, not cruft: spec_topics/grammar.md:204 and descriptions.md §Placement require fn/variant `///` text be "preserved on the AST for tooling that wants to render it", and since `FnDecl` has no `description` slot and `attachDocDescriptions` never keys fn/variant lines (:2242 "survives only as the floating `DocComment` sibling"), `lines` is that text's ONLY carrier — refuting the claim that "the attachment channel carries the content" and the suggestion to drop it (triage: claude-opus-5) |
| 2026-09-08 | qw20260907183353-d2-03-cancellable-sequence-generality-unused.md | false-positive | verdict: false-positive — every "unused" channel (N-statement loop, named `binding`, `bindings` map, `synthesizedTopLevelCancelled`) has deliberate witness-test consumers discharging CNCL-5/CNCL-6, which coverage-matrix.md:102 assigns to this exact function (`V17a`), and CNCL-5 is unstatable on a single-statement runner (tests/cancellation-core.test.ts:415-453 needs two distinct bindings and reads `bindings.get("x")`) — tests-only consumers are not deadness in this repo, so the generality is spec-mandated, not speculative (triage: claude-opus-5) |
| 2026-09-08 | qw20260907183353-d2-03-lower-query-schema-two-pins-count-stale.md | false-positive | verdict: false-positive — "the two shipped seam-contract pins" names a specific pair, not a census of two-argument callers: the "(d) TOTALITY PINS — the seam contract the fix deliberately does NOT change" section of tests/unresolved-annotation-lowering.test.ts holds exactly two two-argument calls (:1040 `("NotDeclaredAnywhere", [])`, :1050 `("", [])`), still exactly two today, and the candidate's own regex requires an identifier second argument so it structurally cannot match `, [])` — it counted a disjoint set; the drift story is further refuted because the sentence was written in bug-0028 commit 1bd9361a (which added `enums`) when ~17 two-argument callers already existed (triage: claude-opus-5) |
| 2026-09-08 | qw20260907183353-d2-04-executebodydeps-optional-rationale-stale.md | false-positive | verdict: false-positive — the doc says "existing constructors", never "src constructors"; all 25 typed test constructors omit `invokeChain` and 10+ omit `emitDiagnostic`, and tsconfig includes `tests` under `strict`, so "a required field would flip every one of them" reproduces as presently TRUE (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-02-line-carries-reserved-key-passthrough.md | false-positive | verdict: false-positive — the doc says "its existing call sites", not "its existing src/ call sites", and those call sites exist: `git show 470071d8^:tests/subagent-envelope.test.ts` already carried the import plus four boolean cells at lines 26/281/283/285/287, byte-identical to HEAD, and `git show 470071d8 -- tests/subagent-envelope.test.ts` is empty for this identifier, so the commit deliberately left them as the pin and said so in its own new witness test ("`lineCarriesReservedKey` stays a `boolean` wrapper ... the existing boolean cells in `tests/subagent-envelope.test.ts` remain the pin on that surface") — the sentence was true when it landed, refuting the "false on the day it landed / names an empty set" anchor; the residual pass-through complaint is a filing against test-only-reachable production code, which this repository's convention (and the lens brief's "file nothing against test-only-reachable production code") treats as alive. (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-03-runtime-exported-types-unimported.md | false-positive | verdict: false-positive — all nine annotate exported symbols consumed cross-module (`evaluateQuestion`/`verifyChildCallableHashes` by production statement-executor.ts:67, production-theta-producer.ts:275, production-composition.ts:56/1503; `dispatchNoParamsTheta`/`rendersTranscriptCard`/`SlashPromptDriveDeps` by tests/slash-dispatch.test.ts:28-32 and b0401:83), so the exports are live public-signature surface, not dead code (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-03-toInterpolationType-arms-unreachable.md | false-positive | verdict: false-positive — deleting both arms fails typecheck with TS2366 at :501 (exported total fn over the 9-kind SystemParamType union, so they are compiler-mandated, not removable dead code), and the comment's "renderSystemPrompt overrides this" flow demonstrably exists at renderSystemPrompt:629-635 + interpolationTypeOfValue:645-650 and is bug 0408's documented fix design (docs/bugs/0408:158) (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-04-stdlibparamkind-importer-roster-stale.md | false-positive | verdict: false-positive — git log -S over all history shows StdlibParamKind never appeared in stdlib-array.ts or stdlib-object.ts (type + doc landed together in 52712fb3), so nothing is vestigial; both modules do import the shape from stdlib-string via StdlibMemberSignature and spell StdlibParamKind literals in ARRAY_/OBJECT_MEMBER_SIGNATURES, so the doc points readers at the right files and only the verb "imported" is loose (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-05-body-type-lowering-consumer-roster-stale.md | false-positive | verdict: false-positive — every excerpt byte-matches at the cited lines and the 4-production-importer census reproduces (theta-document.ts:105-110, query-schema-lowering.ts:135-141, import-static-checks.ts:91, type-layer-checks.ts:86), but the asserted contradiction does not: the sentence's subject is the lowering it introduces ("the single canonical place that lowers a theta body's `schema`/`enum` declarations"), and `buildBodyTypeSchemas` has exactly the two production callers the two bullets name — theta-document.ts:1980 inside `collectBodyTypes` (:1931) and query-schema-lowering.ts:168 inside `lowerQueryResponseSchema`, whose own header at :21 calls it "the two-pass whole-file `buildBodyTypeSchemas`" — while the two unlisted importers touch only `collectUnresolvedNamedTypes` on ONE type source with an EMPTY declared set, i.e. neither whole-file nor resolving: import-static-checks.ts:233 (`referencedNamedTypes`, whose own doc at :222-231 says handing it "an EMPTY declared set turns that refusal list into a plain reference walk") and type-layer-checks.ts:1319 (`NO_DECLARED_TYPE_NAMES`, reading only the `unspellable` out-parameter), so no third "whole-file `NamedType` resolver" exists to make the numeral wrong; the finding's load-bearing premise that the header is "the module's only statement of who depends on it" is refuted by the second importer roster at body-type-lowering.ts:26-34 (and by the per-function rosters at :50 and :604), and its "exhaustive" census is not — its two grep patterns structurally cannot match the 13 test files importing `../src/parser/body-type-lowering` (e.g. tests/discriminator-field-classifier-brace-group.test.ts:6, tests/e2e-s3-schema-lowering-conformance.test.ts:25), contradicting its own "no test file imports the module through a different path" check; git dates check out (header 369facac 2026-07-11, consumers 9eb1290d/d03f7398) but a later importer of a different export does not falsify a claim scoped to the lowering, leaving only a taste-level wish for a fuller roster (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-05-envelope-scan-unparseable-lines-speculative.md | false-positive | verdict: false-positive — every cited fact reproduces (doc 423-434, producer 442-457, `scanStreamForEnvelope` has zero src/extensions/tools callers, `unparseableLines` read only at tests/subagent-wire-parse-failed-classifier.test.ts:112-113/124-125, no string-keyed access, no `export *`, 470071d8 added it), but that makes this a filing against test-only-reachable production code, which the lens brief forbids ("file nothing against test-only-reachable production code") and this repository's convention treats as alive: the field's user count is not zero, it is one deliberate witness caller — the same test whose header states its purpose is asserting "that `scanStreamForEnvelope` ... surfaces the unparseable lines it skipped instead of merging the classes a second time", the named discharge of bug 0086 §Actual behaviour item 5 — so the "speculative generality, zero users" anchor is the disallowed deadness complaint applied one level in (the field is exactly as alive as its producing function), and the sibling filing on this same module and commit (d2-02) was rejected on the same ground. (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-05-pi-token-estimator-body-comment-restates-header.md | out-of-scope | verdict: out-of-scope — both excerpts byte-match at :3-7/:18-20 and the restatement is real (the body's 3 clauses are a strict subset of the header, all still true: named import at :12, direct return at :21, file is 23 lines, no tool reads the text), but the sole anchor is duplication between live, factually-current prose copies, which the D2 brief lists as do-not-file and README assigns to the D4 lens; blame refutes any residue reading — the header dates to stub commit 7eb788a5 while the body comment was authored fresh in implementation commit a1b553bb (the same commit that deleted the stub-status note), unlike the confirmed doc-duplicate siblings whose blame proved excision residue; same ruling as qw20260907202646-d2-04-params-defaults-paragraph-restated (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-07-errnote-event-forward-hook-comment-stale.md | false-positive | verdict: false-positive — comment conditions on an origin-site *emission*, not on the param having a caller: 0383 §Residuals 1 defines that as "no `topLevelCascade: true` caller of `emitRuntimeEvent`", still true at HEAD (its lone production caller, query-discard.ts:219, passes `false`), and bug 0399 §Non-goals re-affirms "no origin emission exists" — 0399 fed the hook an origin-*constructed* event, and reconstruction remains the norm (validation-only), so the cited premise is inverted (triage: claude-opus-5) |
| 2026-09-08 | qw20260907202646-d2-08-filesystem-activeroots-consumer-roster-stale.md | false-positive | verdict: false-positive — the INV-1-scoped block asserts nothing false and never claims exclusivity; the "unstated" fileSystem-absence consequences are documented at 843-851 (with an explicit cross-reference to #recheckCalleeContainment's same skip condition), 868 and 1700, and activeRoots is supplied alongside the spawn seams at the single production composition site (production-composition.ts:859) so `thetaDirs: []` is unreachable in production (triage: claude-opus-5) |
| 2026-09-08 | qw20260908115521-d2-04-pre-eval-failure-count-eight-stale.md | false-positive | verdict: false-positive — the "eight" restates error-model.md:13's pre-evaluation failure list ("the eight below", ERR-1…ERR-7 + ERR-16), which the same sentence quotes; every "seven" in src/ is the narrower load-time subset that load-pre-eval.ts:49-52 explicitly defines as the eight minus ERR-7 ("split out to `V4g` and is not a member here"), so 7 load-time + 1 watcher-time = 8 and no count disagrees (triage: claude-opus-5) |
| 2026-09-10 | qw20260907183353-d2-01-toolsfield-scalar-absent-arms-unconstructed.md | false-positive | verdict: false-positive — every excerpt and search reproduces (production sites now at production-composition.ts:2369/:3180 after ~118-line drift, both `kind: "list"`; ToolsField `scalar`/`absent` constructions only at tests/callable-set.test.ts:73/:272, subagent-fn.test.ts:588, tools-entry-closed-grammar.test.ts:539), but the anchor fails on two mechanical grounds: (1) the "vestigial" narrative ("production wiring stopped exercising [the arms] when the frontmatter layer took over") asserts a history that never happened — `extractToolsList` landed in H8b (4c0a4c20, 2026-07-03) BEFORE the first production `resolveCallableSet` call in V20a (44faae57, 2026-07-04), and that first call already passed `{ kind: "list", items: toolsList }`, so no production construction of the other arms ever existed to become residue; (2) what remains is a filing against test-only-reachable production code, which this repository treats as alive — the arms are held by four deliberate spec-anchored witness constructions (the V6c-T seam contract's "both YAML spellings parse to the same callable set" and "absent / empty `tools:` yields the empty callable set" cells), and the layer split is bug-mandated rather than incidental: frontmatter.ts:1948-1970 must dispatch on the raw scalar-vs-sequence arm to refuse a zero-entry scalar under `theta/load/malformed-tools-field` (bugs 0104/0206) while keeping `tools: []` silent, so the header's contract claim (callable-set.ts:8-9) stays true of the module and frontmatter.ts:600-605 documents its own collapse; same ruling as the d2-02 and d2-05 envelope siblings (triage: claude-opus-5) |
| 2026-09-10 | qw20260907202646-d2-02-generic-argument-wire-bound-claim-restated.md | out-of-scope | verdict: out-of-scope — all 12 excerpts and the grep (13 hits, exact lines) reproduce and the repeated claim is TRUE (`lowerTypeExpr`, params.ts:743-982, has no inline-object arm: a `{…}` generic argument falls to the `unspellable` atom sink at :980-981 and lowers to `{}`), so nothing here is stale; blame refutes any residue reading — every site was authored or rewritten in one commit, 6184e7c30 (bug 0233), which removed `insideGenericArgument` from `walkType` and deliberately EXPANDED the narration from 6 to 9 `never divid` statements while doing so (leftovers do not grow) — which makes the sole anchor duplication between live, factually-current prose copies, the D2 brief's do-not-file and README's D4 lens, same ruling as qw20260907202646-d2-04-params-defaults-paragraph-restated and d2-05-pi-token-estimator-body-comment-restates-header; the candidate's tallies are also loose (only 9 of 12 sites carry the lowering/wire ground — :62-66 is a pure cross-reference to the duplicate rule, refuting "none is a cross-reference", and :89-92 / :1534-1536 state only the answers-alike consequence and the unchanged `rules`/`position` recursion; the 13 hits fall in 9 not 8 passages; 4 not 3 sites name `lowerTypeExpr`, 3 not 2 name code-registry-parse.md) and no git-history check was run (triage: claude-opus-5) |
| 2026-09-10 | qw20260907202646-d2-04-params-defaults-paragraph-restated.md | out-of-scope | verdict: out-of-scope — independently re-verified: all three excerpts byte-match at wire-translation.ts:35-43 / inbound-boundary.ts:138-148 / runtime-value-model.md:37 and every stated search reproduces (2 hits for "states the same mechanism", no doc tooling in package.json, no test reads either module's text, bindParamsInbound:150 → decodeInboundValue:77 → translateInbound:84), but the sole anchor is duplication between live, factually-current prose copies, which lens-d2-cruft.theta:45 lists under "Out of scope (do not file)" and README's lens roster assigns to D4; blame refutes the only D2-eligible reading (excision residue, the ground on which PTQ-0034/0105/0111 were confirmed): both first sentences were authored together in e73c1acae and both closing sentences together in 5a92a36c9 (bug 0186), where they deliberately REPLACED a spec-divergence tracking note ("rvm:37 still states that defaults 'bypass'…") with a spec-agreement pointer in the same commit that rewrote rvm:37 — fresh parallel authoring, not a leftover; the side-claim that the wire-translation.ts copy is "not about anything this module declares" is a placement opinion (also do-not-file) and inaccurate, since "this pass"/"here" is the translation pass this module owns (triage: claude-opus-5) |
| 2026-09-10 | qw20260907202646-d2-04-type-alias-exports-unimported.md | false-positive | verdict: false-positive — every mechanical claim reproduces (declarations byte-exact at type-compat.ts:498/:759 and type-grammar.ts:213; 48 import statements from the two modules name none of the three; no namespace/dynamic import, `export *`, barrel or package `exports`; the theta-document.ts excerpt drifted 9688→10003 content-identical; a tsc 5.9 fixture under this tsconfig's `declaration: true` confirms dropping `export` compiles), but the anchor does not hold: each alias annotates the signature of an exported function consumed cross-module — `classifyIndexReceiver` by expression-evaluator.ts:625, stdlib-object.ts:72 and tests/typeenv-prototype-names.test.ts:1010-1055; `commonType` by static-type-inference.ts:607 and tests/match-fn-return-lub-dominating-discipline.test.ts:682; `parseTypeExpression` by params.ts:225, schema-subset-gate.ts:123, theta-document.ts ×9 (the `rules` value selected at :10007) and 7 test files — which is exactly the ground on which the same-wave, same-reporter sibling d2-03-runtime-exported-types-unimported was rejected (TRIAGE_LOG row 15: "live public-signature surface, not dead code"); the fixture's emitted .d.ts shows the alias lands in the module's public declaration whether or not it is exported, so the keyword widens nothing and only makes an already-public type nameable; a repo-wide census finds 260 of 727 `export type`/`export interface` names in src/ referenced in no other file, so this is the codebase's convention rather than PTQ-0150's proven outlier (there, every other constant of the kind reached a further file); and the export was deliberate, recorded intent (docs/bugs/0081:240 "new exported `CompatRelation`"; type-compat.ts:750-758 documents the injectable-relation seam; PTQ-0025's triage lists `TypeCheckRules` among type-grammar's intended surface) (triage: claude-opus-5) |
| 2026-09-10 | qw20260907202646-d2-05-bypass-params-field-feeds-both-roster-stale.md | false-positive | verdict: false-positive — every excerpt reproduces (doc now binder-envelope.ts:152-156; `params(\?)?\.fields` grep now 18 hits, the extra being new prose at type-layer-checks.ts:317; the 11 production readers are real) but the stale-roster anchor does not: the "Feeds BOTH" sentence was not written "when the binder seam introduced it" (66db8874, 2026-07-01, read "for the load-time bypass classification") but in b027a524 (bug-0011, 2026-07-28), the same hunk that added `defaultSource`, and `git grep` at b027a524 shows five of the eleven "unnamed" readers already live then (production-composition.ts:1246 arity, production-theta-producer.ts:864 `bind_echo` / :3103 subagent names, theta-document.ts:3942 roots / :4449 rootLocals), so the sentence never was a consumer census that later adoptions could outgrow — it states the record's two binder-side roles (why prompt-line data rides a `Bypass…` record) and its literal claim is true, with no "only"/"exactly"/"two consumers" wording, unlike PTQ-0086/PTQ-0137 where git proved decay; frontmatter.ts:124 is not a filed location; what remains is a wording preference, same ruling as the d2-05 body-type-lowering roster rejection (triage: claude-opus-5) |
| 2026-09-10 | qw20260907130901-d2-08-macrotask-ms-param-constant.md | false-positive | verdict: false-positive — independently re-verified and the filed claims do not reproduce: HEAD has exactly ONE `macrotask(` call site (:5947, `#pollWhile`); the filed "call site 2" (`driveStreamedUserTurn`'s local `pollWhile`, filed at :7223) is not line-drift, it is gone — 4f2db584 deleted the whole function on 2026-09-08, the day after this 2026-09-07 filing — so "at both of its call sites"/"exactly two"/`sites: 3` fail reality-check, not just staleness; the filing's own False-positive-check git claim ("no commit introducing a call that passes anything other than POLL_INTERVAL_MS") is independently refuted, confirmed by rerunning both forms myself: `git log -S macrotask -- <file>` (no `--follow`) stops dead at the rename 2bc69157 exactly as the filer ran it, while `git log --follow -S macrotask` surfaces fed12acd (2026-07-03), which called `macrotask(this.#clock, SUBAGENT_MID_STREAM_CANCEL_MS)` (250 ≠ POLL_INTERVAL_MS's 10) until 20540abc removed it hours later — so the helper's generality was designed and exercised, not merely asserted, which sinks "configurability nothing uses" as a live counter-fact rather than a taste quibble; with the premised second call site gone the filing's own "why" (cross-call-site consistency burden) has no second site left to be a burden across (triage: claude-opus-5) |
| 2026-09-11 | qw20260907183353-d2-03-stdlib-anchor-exports-unimported.md | human-convention | human ruling (thomasa, 2026-09-11): an export keyword with no external importer is not cruft in this repo (12/19 _HINT anchors, the invoke-diagnostics.ts mirror, and ~16 sibling sites share the shape); diagnostic anchors are published as citable/greppable surface for the code registry docs; de-exporting one module creates inconsistency, not less cruft. Precedent: file only when the declaration itself is dead |
| 2026-09-11 | qw20260907202646-d2-01-ajvschemavalidatordeps-dead-export.md | human-convention | human ruling (thomasa, 2026-09-11): constructor-injection *Deps interfaces are exported so composition roots may annotate their deps object (19/40 *Deps in src/ share the condition); the interface types the live constructor and #deps field, only the export keyword is unreached; house style, ruled wholesale. Precedent: an export with no external importer is not cruft when the declaration is alive |
| 2026-09-11 | qw20260908115521-d2-03-host-incompatible-kind-export-unimported.md | human-convention | human ruling (thomasa, 2026-09-11): 228/699 exported types in src/ have no out-of-file referencer, including structurally identical kind discriminators (AssignTargetKind, CommittedSideEffectKind); the alias is alive as the single spelling of clause (ii)'s discriminator; house export style, ruled wholesale with the two sibling filings |
| 2026-09-11 | qw20260907130901-d2-02-swallow-route-unread-inputs.md | human-witness-seam | human ruling (thomasa, 2026-09-11): the channels/settlement inputs are the deliberate red/green observation seam for cka-33 channels 2-3 (git f53245b3/0d55d094) - a threaded-but-unread handle whose only reader is a MUST-NOT witness test is this repo's spec-obligation posture, not cruft; the type system proves the handle unread today, the witness is the tripwire against a future mutating call. Noted as the most flippable of the three (production fabricates noopSwallowChannels() at 3 sites) - revisit only if the seam posture itself is revised |
| 2026-09-11 | qw20260907130901-d2-03-watcher-recovery-registry-unread.md | human-witness-seam | human ruling (thomasa, 2026-09-11): the field is the subject of PIC-55's MUST-NOT witness (watcher-terminated-recovery.test.ts:163-167 asserts no drain tag lands on the registry handed in); removing it makes the witness tautological and touches 8 call sites for zero behaviour; deliberate seam, do not file. Same ruling as the swallow-route and ExecuteBodyDeps.mutator siblings |
| 2026-09-11 | qw20260907202646-d2-01-executebodydeps-mutator-mode-discarded.md | human-witness-seam | human ruling (thomasa, 2026-09-11): the empty handlePartialTerminalOutcome body IS the landed ERR-8/ERR-9 contract and statement-executor.test.ts:585-638 threads a RecordingMutator through these exact fields to witness it (red under the non-compliant V4c-T stub, green now); 29 test construction sites of churn for zero behaviour; deliberate witness seam, do not file |
| 2026-09-11 | qw20260907130901-d2-02-rung1-probe-constant-false.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): capability-probe.md:84 / subagent.md:168 state the two-conjunct rung-1 formula verbatim ('Both conjuncts are required'); bug-0001's fix (b8d4fd2c) deliberately replaced the plain false the candidate proposes restoring, and the surface-present/no-dispatcher host shape has a dedicated e2e regression (extension-tool-unreachable-load-refusal-e2e.test.ts:491-545); one typeof per compose pass against divergence from the spec's stated formula. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907130901-d2-03-followup-surfacing-free-phase-arm-unproduced.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): the arm mirrors the spec-named two-valued scalar it feeds (MaskedPredicateInput.turnKind, PIC-1 (d)) whose free_phase IS exercised one hop downstream and whose consumer would honour the value; narrowing it spells one spec scalar two ways; already recorded as non-blocking residual R2 in docs/bugs/0355. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907130901-d2-04-discarded-query-check-constant-inputs.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): the only unused surface is QRY-19's accepted-form arms, which tests/query-discard.test.ts:119-138 deliberately witnesses; the function states the spec's accepted forms as data and the parser happens to route one shape today; cross-module churn for zero behaviour. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907130901-d2-04-sidecar-anonymous-union-kind-undiscriminated.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): the variant was born unread (68bf05b1) as a deliberate mirror of schema-subset.md step 5(2)'s named category and documents WHY the value is absent from the named-enum map; collapsing it moves a spec-anchored fact into a comment. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907202646-d2-05-number-kind-discriminator-inert.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): BNDR-4 and BNDR-5 are separately normative and the two arms were born byte-identical with today's rationale doc present (e78482b9) - no removed feature, no scaffolding; the header pins kind-from-static-type as the caller's obligation; collapsing touches 5 production callers + 21 test sites to merge two spec-anchored describes. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907202646-d2-07-thetalib-form-let-query-indistinguishable.md | human-spec-mirror | human ruling (thomasa, 2026-09-11): the union mirrors imports.md:13's three-way enumeration of non-permitted forms and tests/imports.test.ts:165-174 witnesses each named category; the arms landed with the feature, not as scaffolding. Precedent: a discriminant, arm, or conjunct that mirrors a spec-named enumeration or formula is not vestigial even if only one value is produced today |
| 2026-09-11 | qw20260907183353-d2-03-evaluate-question-thunk-eager.md | human-by-design | human ruling (thomasa, 2026-09-11): the thunk arrived test-first (10d797bd) with the V4b-T ?-bypass witness (6 passing tests drive throwing thunks through it); the cost of keeping it is two () => wrappers, the cost of removing it is dismantling a live witness of a panic-bypass obligation. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907183353-d2-03-probe-readable-cache-constant-true.md | human-by-design | human ruling (thomasa, 2026-09-11): nothing is dead (the write drives all resolution; the boolean backs the live ThetaLibDirectoryProbe.entryReadable contract) and docs/bugs/0428 records probe-side readable=false as the un-implemented Fix Option 2 and 'the coherent end state' - a Map->Set flip that documented end state would flip straight back. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907183353-d2-04-annotation-inferred-brace-guard-subsumed.md | human-by-design | human ruling (thomasa, 2026-09-11): guard and general gate entered in the same commit (72272a20), so no supersession or vestige; the redundancy reproduces (fuzzed 4129 inputs, zero divergence) but docs/bugs/0130 Fix (f) already names both and records a held disposition, and bugs 0093/0220/0222 cite the guard by name - deleting it orphans four bug-doc citations for zero behaviour. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907183353-d2-05-inference-checkcompatible-knob-constant.md | human-by-design | human ruling (thomasa, 2026-09-11): explicit in-repo rationale rejects the direct import (type-compat.ts:749-758 names and rejects 'importing checkCompatible into the inference pass'; static-type-inference.ts:616-624 'rather than reach for a copy that would silently bypass it'); the deps interface, constructor and per-site literal all survive for enumNames, so the fix buys one field against documented intent. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907183353-d2-10-parseparams-inline-fragments-inert.md | human-by-design | human ruling (thomasa, 2026-09-11): inert-by-design from birth and stated so in code at params.ts:190-192; docs/bugs/0039 Fix wires the same sink triple at all three mint sites for uniform posture (the finding miscounted them as two, omitting query-schema-lowering.ts:192). Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260907130901-d2-03-execute-for-evaluateforloop-passthrough.md | human-by-design | human ruling (thomasa, 2026-09-11): the drive is behaviour-free as claimed, but the seam is the module header's documented design and coverage-matrix V3c's 'executor iteration authority'; the proposed inlining would demote evaluateForLoop/ForLoopHost to test-only production code - creating the dead-seam shape a cruft lens should abhor - and the honest alternatives (delete the seam + its matrix row, or route evalParFor through it too) are architecture calls outside D2. Residual noted: the executeFor/evalParFor asymmetry. Precedent: a knob/belt/thread whose rationale is stated in code or a bug doc is a design decision, not cruft - file only when the rationale is demonstrably false |
| 2026-09-11 | qw20260911104855-d2-03-binder-model-doc-inline-duplicate.md | false-positive | verdict: false-positive — the exact "bypass-eligible…never call(s) the binder" rule recurs well beyond the cited pair: resolveBinderModel's own doc (binder-model.ts:201-204, "Bypass-eligible thetas skip both checks") is a third, immediately-adjacent restatement the candidate quoted in its own false-positive check but didn't count, plus independent restatements in extension/reload-wiring.ts:110-120 and extension/production-composition.ts:1228-1237; bindModelUnresolvable's rule, with the identical "(bug 0297)"/"(the ABSENT-field behaviour)" phrasing, is independently restated a third time in src/parser/frontmatter.ts:139-147 — this is a deliberate cross-file documentation convention for these two bug-linked invariants, not the isolated two-site duplication the finding's narrower-scoped convention check claims (triage: claude-opus-5) |
| 2026-09-11 | qw20260911104855-d7-01-parsedeps-reimplemented-not-imported.md | false-positive | verdict: false-positive — the file's own header comment (:144, :215-216) states this harness is copied "verbatim" from tests/non-object-receiver-gate.test.ts's "group-(e) pattern" (added one day earlier), and the identical local parseDeps/parseOnly shape recurs in 79 other test files (several, e.g. array-sink-unresolvable-deferral.test.ts, explicitly citing this very file's range as "the pattern"), so this is a documented, widely-repeated sibling convention, not an isolated oversight that skipped the e2e-s1.ts import demonstrated by one comparison file (triage: claude-opus-5) |
| 2026-09-11 | qw20260911104855-d7-03-dispatch-harness-scaffolding-duplicated.md | false-positive | verdict: false-positive — core claim "no comparable module exists ... which is why each site re-derives it in place" is false: tests/helpers/call-with-clause-harness.ts already exports rootDouble()/noopPi()/driveCtx(cwd), and 107 test files roll their own local rootDouble()-shaped RuntimeRoot double while 186 files use the identical "(mirrors <sibling>)" comment for intentionally per-file-duplicated harnesses — this is the suite's pervasive, established convention, not a gap isolated to the 3 cited files (triage: claude-opus-5) |
| 2026-09-12 | qw20260912091742-d7-02-b0328-discoveryhost-harness-duplicated.md | false-positive | verdict: false-positive — b0328's own cited comment mirrors the older tests/subagent-child-hash-refusal-e2e.test.ts (added 2026-07-24, continued by b0343 on 2026-08-31), not tests/helpers/production-load-harness.ts, which git log shows was added 2026-09-11 — two weeks after b0328 (2026-08-29) — for a differently-shaped runProductionLoad/diagnosticLines family used by only 1 of the 21 files sharing that name; b0328 continues an independent, already-repeated (3x) sibling convention this finding never accounts for, not a skipped import of a pre-existing helper (triage: claude-opus-5) |
| 2026-09-12 | qw20260912112713-d7-01-binder-model-recovery-note-content-tautology.md | out-of-scope | verdict: out-of-scope — claim reproduces (test:299's `expect` calls the identical `renderBinderModelRecoveryContent(recovered)` the impl already calls at binder-model.ts:345, pinned equal by the preceding `details` assertion, a genuine D7 "assertion that cannot fail"), but the candidate's own `locations:` names `src/binder/binder-model.ts:326-347`, a site outside tests/, which D7's "test sources under tests/ ONLY" rule excludes ("a location outside tests/ = out-of-scope"); every prior D7 filing needing equivalent src-side justification, e.g. PTQ-0231, confines it to Evidence prose and never lists it under `locations:` (triage: claude-opus-5) |
| 2026-09-12 | qw20260912112713-d7-01-degraded-witness-mark-guard-dead.md | false-positive | verdict: false-positive — the dead guard is the file's own documented dual-fork witness design, not a misleading accident: the file header ("WHY these assertions are shaped for both directions") and Cell 3's own comment ("Fork: ... reds [pre-fix] ... Post-fix: ... green") — both in the same single commit as the fix — state exactly this red-before/green-after mechanics, which the candidate quotes adjacent lines to but never engages with (triage: claude-opus-5) |
| 2026-09-12 | qw20260912161041-d9-02-param-line-breaks-combined-scans.md | human-keep-whole | zone band (63 LOC); the two rules answer adjacent spec sentences and the file's own sibling rationale explains why they stay side by side; a split adds indirection and removes no duplication. Re-file only on ≥25% growth or a third distinct rule. |
| 2026-09-13 | qw20260913174959-d8-03-posix-path-helpers-reimplement-node-path.md | false-positive | verdict: false-positive — the filing's central verification claim does not reproduce: re-running its own described check (strip drive, `posix.normalize` the remainder, reattach drive) against a trailing-slash input gives `posix.normalize("/pkg/scripts/")` = `"/pkg/scripts/"` (Node v24.16.0 documented behaviour: trailing separators are preserved) vs the hand-rolled loop's `"/pkg/scripts"` — a real divergence for exactly the "trailing slash" case the filing claims was tested and found byte-identical across all 12 cases; `joinPosix`, cited as one of the five fully-verified sites, gets no feature-for-feature check at all in the Evidence and independently diverges from `posix.join` on dot-segment tails (`joinPosix("/a","../b")` keeps the literal `"/a/../b"`, `posix.join` collapses it to `"/b"`) — a missed load-bearing need for 2 of the 5 claimed reimplementations (triage: claude-opus-5) |
| 2026-09-13 | qw20260913174959-d8-04-linebreak-collapse-hand-scanned-not-regex.md | false-positive | verdict: false-positive — re-ran the current implementation against the filing's own proposed three-`replace` equivalent: for break-free text with leading/trailing U+0020 (e.g. `"  hello world  "`, no \r or \n anywhere), the current short-circuit returns it byte-identical per the function's own documented contract, but the candidate's regex chain unconditionally strips the edges (`.replace(/^ +/,"")`/`.replace(/ +$/,"")` run regardless of break presence) and returns `"hello world"` — a missed load-bearing need (byte identity for break-free text) the filing's 14-case comparison never exercised (triage: claude-opus-5) |
| 2026-09-13 | qw20260913183958-d8-01-host-loop-bridge-provider-against-grain.md | false-positive | verdict: false-positive — every counted concept (nonce-named provider, two-state streamSimple, active-flag teardown, agent_settled arm-before-send, active-tool/model snapshot-restore, dispatch serialisation) maps 1:1 onto subagent.md PIC-64's own (a)-(f) enumerated wiring, quoted near-verbatim there ("two-state streamSimple", "dummy literal apiKey", "per-dispatch unique name") with its own load-bearing rationale per element, and the code's comments restate the same rationale; this is D8's "spec-mirroring arms / rationale-stated knobs" not-finding precedent (quality/README.md D8 section), and the filing's own suggested direction concedes no simpler shape exists today (triage: claude-opus-5) |
| 2026-09-13 | qw20260913183958-d8-02-adaptchild-hand-rolled-listener-bookkeeping.md | false-positive | verdict: false-positive — the Site 2 claim ("a settled Promise already provides" onExit's behaviour) misses a load-bearing need: live checks this session (`node -e`) confirm the current exitListeners/exitInfo mechanism delivers SYNCHRONOUSLY, in the same call stack as the `'close'` handler, to a listener already registered before exit, while a settled Promise's `.then()` always defers via microtask even to a pre-attached handler — a real divergence in exactly the always-exercised production shape (`driveSubagentChild` registers its `onExit` listener synchronously at drive start, provably before any exit can fire), which the filing's own "verified against a live Node process" check never tested — it verified only the already-settled/late-subscriber replay half of the "mirrors event-emitter ordering" comment it quotes, not the early-subscriber half the interface actually exercises in production. (triage: claude-opus-5) |
| 2026-09-14 | qw20260913183958-d9-02-package-discovery-six-concerns-bundled.md | human-keep-whole | human 2026-09-13: zone band (710 LOC, no presumption of breakdown) and one cohesive feature (package discovery); the six concerns are 28/27/55/69/209/137 LOC, so a split yields 30-70-LOC modules for no band change. Keep whole. The function-level resolvePiThetas (105 LOC, justify) stays separately fileable under its own #host key. |
| 2026-09-14 | qw20260914060226-d9-03-runcomposepass-ten-registration-gates.md | human-defer | human 2026-09-14: deferred, not refused - same host as the ratified Seam 0 (d9-02 -> production-discovered-theta.ts) and the store co-lanes same-lens same-host issues; one seam per host per wave. Re-file with fresh line numbers once the file changes (runComposePass 885 LOC remains a breakdown candidate). |
| 2026-09-14 | qw20260914060226-d9-04-composeextensioninstance-four-phases.md | human-defer | human 2026-09-14: deferred, not refused - same host as the ratified Seam 0 on production-composition.ts; one seam per host per wave. Re-file once the file changes (composeExtensionInstance 284 LOC remains a breakdown candidate). |
| 2026-09-14 | qw20260914091051-d8-01-echo-arm-matching-reimplements-ajv.md | human-keep-whole | human 2026-09-14: keep. echoTypeFromValue and its helpers are, by their own stated rationale, a pure value -> descriptor approximation for the binder echo note (a cosmetic system note), not a validation boundary; bug 0381 scoped the remaining gap as exotic / non-normative. Compiling and running per-arm AJV validators on every successful bind to name a type in a note is heavier than the note warrants, and would put the validator on a pure render path. Re-file only if the echo note's type descriptor becomes normative or the descriptor is observed wrong on a real theta. |
| 2026-09-14 | qw20260914091051-d8-01-enumerate-directory-redundant-readdir.md | human-keep-whole | human 2026-09-14: keep. One additional fs.readdir per directory candidate at discovery/reload time is not a cost worth a mechanism: carrying names inside PathClass and giving enumerateDirectory an optional pre-fetched-names parameter adds surface and removes the independent accepted-then-unreadable classification (the two functions' distinct failure duties the filing itself names). Heavier-than-scale cuts the other way here. Re-file only with a measured discovery-time cost. |
| 2026-09-14 | qw20260914091051-d9-01-invoke-static-checks-seven-subsystems-bundled.md | human-defer | human 2026-09-14: deferred for sequencing, not refused. Its Seam A moves exactly the four checkImported* functions that two ruled D8 issues (PTQ-0319 collectCallSites-once and the collectLocalBinderNames-once sibling) edit next wave; let those land first, then re-file. Intended next seam: Seam A -> src/extension/invoke-imported-checks.ts (cycle-free: invoke-static-checks.ts itself never calls the four; import-static-checks.ts re-points its import; collectCallSites / collectProvableArgTypes / dedupeArgType get exported). |
| 2026-09-14 | qw20260914130212-d8-03-thetalib-probe-redundant-readable-cache.md | false-positive | verdict: false-positive — docs/bugs/0428 documents the probe-side EACCES/broken-symlink readable=false refinement (Fix Option 2) as the un-implemented half of "the coherent end state" this Map shape exists for, matching human ruling qw20260907183353-d2-03-probe-readable-cache-constant-true.md (2026-09-11, by-design) which pre-rejected this same "Map→Set flip" on identical grounds, and this wave's own D8 shard-01 review already declined to file it as "rationale-stated placeholder, not overbuilt." (triage: claude-opus-5) |
| 2026-09-14 | qw20260914130212-d8-04-progress-params-hand-duplicated-schema-type.md | false-positive | verdict: false-positive — the named facility (typebox's Type.Object/Type.String/Optional/Integer builder) is not actually available in this codebase: the inventory-closure audit restricts every `Type.<member>` access across the audited tree (`src/**/*.ts`) to `Type.Unsafe` only (`TYPEBOX_MEMBER_ACCESS_ALLOW_LIST = ["Unsafe"]` in tests/inventory-closure-audit-gate.test.ts, currently green with 0 violations), a load-bearing MUST documented at docs/spec_topics/pi-integration-contract/host-prerequisites.md ("only Type.Unsafe"), capability-probe.md Step 0(e), and audit-failures.md, which names `Type.Object` verbatim as the canonical violating example; live-running the actual `runInventoryClosureAudit` (transpiled from src/extension/inventory-closure-audit.ts) against progress-tool.ts with `Type.Unsafe<ThetaProgressParams>(` swapped for `Type.Object(` reproduces exactly one `audit/violation/peer-import/off-inventory` record for `typebox#Type.Object` — a missed load-bearing need the filing's own reference search (which reports only Type.Unsafe hit sites and never checks for this audit or for Type.Object usage, which is repo-wide zero) did not account for, and no `challenges_spec` is set (triage: claude-opus-5) |
| 2026-09-14 | PTQ-0328-b0319-scriptedlivesession-harness-duplicated.md | parked | skipped by the fixer in 2 waves — PTQ-0328: Re-verified against current code — the cited duplication is already gone (tests/helpers/scripted-live-session-harness.ts exists and b0288/b0319/b0414 already import ANTHROPIC_MODEL/SessionEntryDouble/TurnState/appendUserEntry/appendAssistantEntry/parse/ajv from it instead of redeclaring them, per an earlier commit already present in this tree). No longer reproduces; no changes made, skipped. / PTQ-0345: Re-verified — cell (E) in tests/b0319-prompt-bidirectional-ctx-abort-witness.test.ts reproduced exactly (never read ctxAbortCalls). Added expect(ctxAbortCalls, ...).toBeGreaterThanOrEqual(1) right after hook.assertFired(), matching the observable sibling cells (A)/(B)/(C)/(G) already assert, so the cell now discriminates "the throw fired and was trapped" from "the reverse listener doesn't exist." No assertions weakened, no tests deleted. / PTQ-0343: Re-verified — all 5 cited files (b0328, b0329, b0331, b0343, subagent-child-hash-refusal-e2e.test.ts) reproduced the byte-identical savedEnv/setEnv/restore-loop. Extended the existing tests/helpers/ambient-control-plane-scrub.ts (per the D7 "extend an existing helper" rule, and it's the exact module the issue's own suggested direction names) with createEnvSandbox()/EnvSandbox, and switched all 5 files to `const { setEnv, restoreEnv } = createEnvSandbox();` plus `restoreEnv()` in afterEach, leaving every setEnv(key, value) call site textually unchanged. subagent-child-hash-refusal-e2e.test.ts's setEnv gained the first-capture guard the other four already had; verified no test in that file calls setEnv twice for the same key within one it(), so this is behavior-preserving for every existing test. Updated two prose comments in b0331 that named the now-relocated savedEnv identifier. No production src/ touched, no tests deleted. / PTQ-0344: Re-verified — tests/b0347-subagent-leg-propagated-mintable-wrapped-unit.test.ts still hand-rolled SEAM_NOOP_CHECKPOINT/SEAM_NOOP_SINK/SEAM_NOOP_MUTATOR/span()/RecordedHop, byte-identical to tests/helpers/invoke-seam-scaffold.ts. Deleted the local declarations and imported them the same way in-scope sibling b0349 already does; dropped the local type-only imports (Checkpoint, ToolLoweringSink, SourceRange, CommittedConversationMutator, CommittedSurface) left unused by the deletion, keeping DrivenConversationMode (still used elsewhere). No test cells changed or deleted. / Verification: `npx tsc --noEmit && npm test` green (662/662 test files, 11181/11181 tests). Two intermediate gate attempts showed unrelated timeouts (production-tools-load-resolution.test.ts, extension-tool-unreachable-load-refusal-e2e.test.ts, shared-subtree-judged-once-per-pass-not-once-per-path.test.ts, tools-entry-grammar-derivations-lockstep.test.ts, etc. — none touched by this change) coinciding with a measured system load average over 100-150 on this 32-core shared machine, matching the sibling-lane-contention pattern this cluster's own prior fix-attempt log documents (bug 0474-adjacent). I A/B-tested tests/b0331-root-winner-preempt.test.ts (its cell (G) appeared in the flaky set) by temporarily swapping in the untouched original file under the same load and saw the identical borderline timing, confirming the sensitivity predates and is independent of my edit. After load subsided (confirmed via /proc/loadavg), a full re-run was completely green with no retries needed on any file this change touches. \| review unconfirmed: PTQ-0328-b0319-scriptedlivesession-harness-duplicated.md — shed by this round's fixer (zero lines in the current uncommitted diff touch b0288, b0414, b0319's harness/import block, or tests/helpers/scripted-live-session-harness.ts); independently reverified anyway and the described problem is in fact already gone via an earlier, already-committed change (the helper exports ANTHROPIC_MODEL/SessionEntryDouble/TurnState/appendUserEntry/appendAssistantEntry/parseDeps/parse/ajv and all three of b0288/b0319/b0414 import them with no local redeclaration remaining; `^class ScriptedLiveSession` survives only as each file's own divergent behaviour class, per the issue's own suggested boundary) — nothing left to fix in code; only the tracker entry (still status: open in quality/issues/, fix_skips: 1) needs to catch up administratively. |
| 2026-09-15 | qw20260915044704-d9-01-resolvereexportclosure-four-phases-bundled.md | human-defer | human 2026-09-15: deferred, not refused — same host file (import-static-checks.ts) as the ratified Seam C on checkThetaImports, and one seam per host per wave. Re-file once Seam C lands; the likely ratification is this filing's Seam A (hoist resolveDeclaringSite + diagnoseReExportCollisions, 104 LOC, two explicit parameters, no other host cross-reference). |
| 2026-09-16 | qw20260916045442-d4-04-arithmetic-exactness-gate-asymmetry.md | false-positive | verdict: false-positive — independently re-verified past the git --stat check the candidate stopped at: the bodies of docs/bugs/0142-division-result-type-not-number.md and 0152-modulo-zero-result-type-not-number.md (both shipped by the cited commits) pin this exact asymmetry as deliberate — 0142 §Fix(b) witnesses "`"a" / "b"` stays withheld" as a row that must NOT move, its Residuals #3 names this very type-layer-checks.ts:2841-2846 comment as stale and states "§Non-goals keeps that file unedited, so this is recorded rather than repaired," and its §Provenance traces the design to bug 0050 review round 4, which already tried trusting the operator-fixed result unconditionally at provableArgType's site and found "nine false-`E` routes including `g("6" / "2")`" — the same fix the candidate's own Suggested direction proposes; 0152 §Fix(b)/(c) reaffirms "`/` is untouched" and scopes the MIRROR to collectProvableArgTypes alone. This refutes the finding's load-bearing claim that "neither adjudicated bug filing discusses the inversion" (triage: claude-opus-5) |
| 2026-09-16 | qw20260916045442-d8-01-globmatches-recompiles-minimatch-pattern.md | false-positive | verdict: false-positive — none of the four cited discovery-walk.ts ranges reproduce against current HEAD (622 lines): globMatches is actually at 205-217 (cited 438-450, which is discoverThetas's opening), listTree's doc/signature at 135-147 (cited 369-380), addGlob at 384-393 (cited 617-627), and the `!`-loop at 401-409 (cited 637-645, past current EOF); the same-wave D9 fix (commit 46a063e0, landed after this candidate's b96ca994 filing) extracted ~277 lines to discovery-source-enumerate.ts and shifted everything ~230 lines, so the filing's own "re-read all four ranges immediately before filing; content matches verbatim" claim is false against current code, even though the quoted excerpts are otherwise byte-identical at their corrected locations (triage: claude-opus-5) |
| 2026-09-17 | qw20260916144930-d9-01-resolvereexportclosure-four-phases-bundled.md | false-positive | verdict: false-positive — none of this filing's citations reproduce against current HEAD: resolveReExportClosure is actually at 651-930 (size-scan confirms 280 LOC/band strong, so the total survives, only every position is wrong), not the cited 623-902 — closeOverReExports's const sits at line 691, not 663-667 (663-667 today is the tail of ReExportEdge's own doc comment plus its interface body, unrelated content); fixReExportedNames at 775 not 747-751; diagnoseReExports at 802 not 774-789; resolveDeclaringSite/diagnoseReExportCollisions at 830/872 not 802-893; the orchestration tail at 923-930 not 895-902; the call-site comment at 1728-1738 not 1688-1698; the call itself at 1741 not 1701; even the supporting cross-reference to collectImportedSpecifierFacts is stale (966-1316 per today's map, not the cited 924-1274). Root cause verified by git: same-wave commit 82efa599 ("quality: qw20260916144930 fix src/extension__p2", 2026-09-16 19:08:57) landed AFTER this finding's own filing commit f67b6530 (2026-09-16 18:07:09) and inserted ~28 lines (ThetaLibDeclarationStmt/isThetaLibDeclarationStmt/MATERIALIZE_SYMBOL_DECLARATION_KINDS, verified in its diff) earlier in the file — refuting both the filing's explicit "re-read verbatim at the cited lines immediately before filing" claim and its own stated reasoning that nothing before checkThetaImports could have moved the function; this is the same failure mode already rejected in qw20260916045442-d8-01 (content byte-identical at corrected locations, but the filing as cited does not reproduce). Independently, the earlier triage note's own "exemptions.json ... one hit ... enumerateDirectory" framing undersells it further: grepping exemptions.json for import-static-checks.ts returns zero hits outright (confirmed), not a near-miss. Supersedes the prior triage note on this file (triage: claude-opus-5) |
| 2026-09-17 | qw20260917045205-d8-01-discovery-path-classify-reimplements-posix.md | false-positive | verdict: false-positive — joinPosix has far more real call sites (~24 across settings.ts/package-discovery.ts/discovery-walk.ts/discovery-source-enumerate.ts) than the "4 src call sites" the Evidence cites, and one uncounted site — discovery-walk.ts:263's resolveSettingsOperand, joining `thetaPathsBaseDir` against an operator-authored relative `thetaPaths` entry that settings.ts validates only as "is a string" (no '..'-rejection, per package-and-settings.md's entry schema) — diverges from `path.posix.join` on a dot-segment tail (`joinPosix("/proj/.pi","../shared/x")` stays literal `"/proj/.pi/../shared/x"`; `path.posix.join` collapses it to `"/proj/shared/x"`, verified live via `node -e`), a shape the filing's Feature-for-feature check never tests (scoped only to fs.cwd()/globalAgentDir()/homedir()-join and readdir-name shapes) and normalizePath does not collapse either (backslash-only rewrite) — the identical missed-load-bearing-need class (joinPosix vs posix.join on dot-segment tails) that already sank the near-identical qw20260913174959-d8-03-posix-path-helpers-reimplement-node-path.md filing on this same function, rejected 2026-09-13 (triage: claude-opus-5) |
| 2026-09-17 | PTQ-0374-callable-surface-argument-loop-cloned.md | parked | skipped by the fixer in 2 waves — PTQ-0364-with-clause-mode-gate-duplicated-surfaces.md: Duplication was already unified by a prior attempt into one withClausePromptModeRefusal helper, but it lived inside the over-band host src/extension/invoke-static-checks.ts (1815 LOC > 1000 D4 justify band), exactly what the prior review flagged. Extracted withClausePromptModeRefusal verbatim (doc comment included, one stale "above" cross-reference to checkClauseCwdType corrected since it's no longer in the same file) into a new sibling module src/extension/with-clause-prompt-mode-gate.ts (61 LOC, header comment stating its role); all of its real dependencies (CallWithClause, ThetaMode, Diagnostic/SourceRange, withClausePromptModeCalleeMessage + its code/hint constants) already lived outside invoke-static-checks.ts, so the sibling imports nothing from the oversized host (zero cycle). invoke-static-checks.ts (now 1777 LOC) imports the helper back like any other caller; dropped 3 now-unused imports from its ../parser/invoke-diagnostics block. Both call sites and their emitted diagnostics are byte-identical; no test changes needed (no test imports the helper by name). Gate green: npx tsc --noEmit clean, npm test -- --minWorkers=1 --maxWorkers=3 -> 680 test files / 11497 tests passed. Only invoke-static-checks.ts (modified) and with-clause-prompt-mode-gate.ts (new) touched. / PTQ-0374-callable-surface-argument-loop-cloned.md: Skipped. Re-verified the clone still reproduces verbatim at current lines invoke-static-checks.ts:1054-1085 / :1167-1191 (node tools/quality/clone-scan.mjs groups -> G008, 110 tokens, renamed-only(6)), both copies still inside the same 1777-LOC host, over the D4 HOME BAND bar. The loop's only host-local dependencies, collectProvableArgTypes and renderCollectedTypes, are each called at several other sites inside that file, and collectProvableArgTypes is additionally imported directly by a third file (invoke-imported-checks.ts) outside the two cited copies -- the rule's sanctioned escape (move the runtime value with the helper, old host imports it back) would ripple into an unrelated file/edit set disproportionate to a ~30-line loop, and no cycle-free sibling is otherwise available. Skipped per the D4 rule's own "else SKIP with a note (a runtime import cycle is not an acceptable outcome)" clause; no code touched. / PTQ-0375-fn-param-junk-guard-cross-module-clone.md: Skipped. Re-verified the clone still reproduces verbatim at current lines invoke-imported-checks.ts:226-241 / type-layer-checks.ts:2672-2690 (clone-scan -> G055, 65 tokens, renamed-only(6)). The copies straddle extension/ and parser/; their shared dependency (annotationToCompatType, annotationSourceIsNotTypeExpression) lives only in type-layer-checks.ts (4006 LOC, over the band). Every existing module that already imports those two predicates (type-layer-checks.ts itself, theta-document.ts, query-schema-resolve.ts) does so FROM type-layer-checks.ts, so any sibling hosting the shared guard that type-layer-checks.ts calls back into would form a module cycle, and the compliant alternative (relocating the two predicates out of type-layer-checks.ts) would touch importers outside the two cited copies. Skipped per the same rule clause; no code touched. \| review unconfirmed: PTQ-0374-callable-surface-argument-loop-cloned.md — untouched (shed by the fixer): the per-argument type-mismatch loop is still hand-duplicated between checkThetaCallableCallSurface (src/extension/invoke-static-checks.ts:1054-1085) and checkRuntimeToolCallSurface (src/extension/invoke-static-checks.ts:1167-1191); clone-scan.mjs groups still reports it as G008 (110 tokens, renamed-only(6)); no shared helper was extracted between the two surfaces' shared checkInvokeArity/checkToolCallArguments calls, and this diff touches neither span. PTQ-0375-fn-param-junk-guard-cross-module-clone.md — untouched (shed by the fixer): the parameter-annotation-junk guard is still duplicated between checkImportedFnCallArgs (src/extension/invoke-imported-checks.ts:226-241) and checkFnCallArgs (src/parser/type-layer-checks.ts:2672-2690); clone-scan.mjs groups still reports it as G055 (65 tokens, renamed-only(6)); no shared predicate was added beside annotationToCompatType / annotationSourceIsNotTypeExpression in parser/, and this diff touches neither file. |
| 2026-09-17 | PTQ-0375-fn-param-junk-guard-cross-module-clone.md | parked | skipped by the fixer in 2 waves — PTQ-0364-with-clause-mode-gate-duplicated-surfaces.md: Duplication was already unified by a prior attempt into one withClausePromptModeRefusal helper, but it lived inside the over-band host src/extension/invoke-static-checks.ts (1815 LOC > 1000 D4 justify band), exactly what the prior review flagged. Extracted withClausePromptModeRefusal verbatim (doc comment included, one stale "above" cross-reference to checkClauseCwdType corrected since it's no longer in the same file) into a new sibling module src/extension/with-clause-prompt-mode-gate.ts (61 LOC, header comment stating its role); all of its real dependencies (CallWithClause, ThetaMode, Diagnostic/SourceRange, withClausePromptModeCalleeMessage + its code/hint constants) already lived outside invoke-static-checks.ts, so the sibling imports nothing from the oversized host (zero cycle). invoke-static-checks.ts (now 1777 LOC) imports the helper back like any other caller; dropped 3 now-unused imports from its ../parser/invoke-diagnostics block. Both call sites and their emitted diagnostics are byte-identical; no test changes needed (no test imports the helper by name). Gate green: npx tsc --noEmit clean, npm test -- --minWorkers=1 --maxWorkers=3 -> 680 test files / 11497 tests passed. Only invoke-static-checks.ts (modified) and with-clause-prompt-mode-gate.ts (new) touched. / PTQ-0374-callable-surface-argument-loop-cloned.md: Skipped. Re-verified the clone still reproduces verbatim at current lines invoke-static-checks.ts:1054-1085 / :1167-1191 (node tools/quality/clone-scan.mjs groups -> G008, 110 tokens, renamed-only(6)), both copies still inside the same 1777-LOC host, over the D4 HOME BAND bar. The loop's only host-local dependencies, collectProvableArgTypes and renderCollectedTypes, are each called at several other sites inside that file, and collectProvableArgTypes is additionally imported directly by a third file (invoke-imported-checks.ts) outside the two cited copies -- the rule's sanctioned escape (move the runtime value with the helper, old host imports it back) would ripple into an unrelated file/edit set disproportionate to a ~30-line loop, and no cycle-free sibling is otherwise available. Skipped per the D4 rule's own "else SKIP with a note (a runtime import cycle is not an acceptable outcome)" clause; no code touched. / PTQ-0375-fn-param-junk-guard-cross-module-clone.md: Skipped. Re-verified the clone still reproduces verbatim at current lines invoke-imported-checks.ts:226-241 / type-layer-checks.ts:2672-2690 (clone-scan -> G055, 65 tokens, renamed-only(6)). The copies straddle extension/ and parser/; their shared dependency (annotationToCompatType, annotationSourceIsNotTypeExpression) lives only in type-layer-checks.ts (4006 LOC, over the band). Every existing module that already imports those two predicates (type-layer-checks.ts itself, theta-document.ts, query-schema-resolve.ts) does so FROM type-layer-checks.ts, so any sibling hosting the shared guard that type-layer-checks.ts calls back into would form a module cycle, and the compliant alternative (relocating the two predicates out of type-layer-checks.ts) would touch importers outside the two cited copies. Skipped per the same rule clause; no code touched. \| review unconfirmed: PTQ-0374-callable-surface-argument-loop-cloned.md — untouched (shed by the fixer): the per-argument type-mismatch loop is still hand-duplicated between checkThetaCallableCallSurface (src/extension/invoke-static-checks.ts:1054-1085) and checkRuntimeToolCallSurface (src/extension/invoke-static-checks.ts:1167-1191); clone-scan.mjs groups still reports it as G008 (110 tokens, renamed-only(6)); no shared helper was extracted between the two surfaces' shared checkInvokeArity/checkToolCallArguments calls, and this diff touches neither span. PTQ-0375-fn-param-junk-guard-cross-module-clone.md — untouched (shed by the fixer): the parameter-annotation-junk guard is still duplicated between checkImportedFnCallArgs (src/extension/invoke-imported-checks.ts:226-241) and checkFnCallArgs (src/parser/type-layer-checks.ts:2672-2690); clone-scan.mjs groups still reports it as G055 (65 tokens, renamed-only(6)); no shared predicate was added beside annotationToCompatType / annotationSourceIsNotTypeExpression in parser/, and this diff touches neither file. |
| 2026-09-17 | qw20260917095931-d2-01-fill-defaults-validation-field-unread.md | false-positive | verdict: false-positive — re-verified: facts reproduce (sole production caller now at production-theta-producer.ts:1732 drops `validation`; no src/ `.validation` reads, string-keyed access, or re-exports), but code whose only callers are tests is not dead under D2: `result.validation` has deliberate value-asserting witnesses (defaulting-revalidation.test.ts:78; defaulting-post-merge-classification.test.ts:233,292,333,366; proto-named-schema-validator-enforcement.test.ts:734 bug-0212 cell E PRIMARY; inbound-boundary-binder-args.test.ts:285; inbound-union-arm-dispatch.test.ts:1223), the module header + spec anchor defaulting-system-note-echo.md#post-default-merge-ajv-validation require the verdict be surfaced, the field doc states why it stays distinct from `classification` (breach arm `{ ok: false, errors: [] }` = AJV-not-asked), and 94e81974 (bug 0066) added `classification` alongside it, not in place of it (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917095931-d2-02-session-context-walk-metrics-unread.md | false-positive | verdict: false-positive — re-verified independently: the caller (now production-theta-producer.ts:1589-1601) does read only `includedMessages`, but `applies`/`includedTurnCount`/`includedTokenTotal` have live readers asserting concrete spec-boundary values in tests/session-context-truncation.test.ts (:108-109,113,135-138,156-157,174,179,203-204,239-240,249-250 — inclusive 8000/20 caps, BNDR-10 skip) and tests/b0478-augmented-agentmessage-variants-excluded-before-walk.test.ts (:276-279,317-318,335-336,350-351), and D2 rules test-only callers NOT dead; every confirmed field-unread peer (PTQ-0006/0008/0115/0143) established zero test readers first, which the candidate's own FP-check concedes fails here; not a duplicate of PTQ-0158 (the redundant `!walk.applies` guard, already fixed) (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917095931-d2-03-max-binder-llm-calls-enforcement-claim-stale.md | false-positive | verdict: false-positive — re-verified independently: "enforced by runBinderCallWithCancellation" is a true behavioural claim (tests/binder-retry-taxonomy.test.ts:141-155 HC3-d drives runBinderCallWithCancellation with a never-aborting signal and asserts s.calls() === MAX_BINDER_LLM_CALLS === 3; grep of MAX_BINDER_LLM_CALLS across src/extensions/tools/tests confirms definition + comment + test only), not a claim of a code-level import; the header line was authored in PTQ-0290's fix commit 7ef57e4c under the human ruling (quality/resolved/PTQ-0290) that kept the constant exported, kept HC3-d as the coupling witness, and made naming the two budget literals "permitted, not required" — the missing import is ratified design, not stale or miscounted narration (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917095931-d4-01-binder-provider-api-tables-parallel.md | false-positive | verdict: false-positive — accounting inaccurate: BINDER_TEMPERATURE_TABLE has 12 row keys at binder-temperature.ts:97-116 (its own excerpt lists 12), not the 10 stated twice; and the "no file imports another's table / only coupling is developer discipline" anchor is refuted — tests/version-bump-gates.test.ts:194-241 imports BINDER_TEMPERATURE_TABLE, FORCED_TOOL_CHOICE_API_KEYS and FORCED_TOOL_CHOICE_UNMEASURED_APIS and asserts via apiCoverageFailures that every member of the pinned 10-entry api-coverage snapshot (sdk-inventory.ts:428-439) is a temperature row key and falls in exactly one disjoint forced-tool bucket, so a new KnownApi reds the suite rather than silently taking the default arm (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917095931-d4-02-binder-retry-budget-parallel.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce (binder-cancellation.ts:93-104 verbatim; the constant drifted to retry-taxonomy.ts:42-51, still `= 3`) and MAX_BINDER_LLM_CALLS greps to definition + one comment (binder-cancellation.ts:102) + tests/binder-retry-taxonomy.test.ts only, but resolved PTQ-0290-retry-budget-reimplemented-uncoupled.md (same two files, same d4_class: parallel) already cited that comment-only reference and the independently hardcoded `= 1` budgets as its "no shared source of truth" evidence, and the human ratification (fixed in 7ef57e4c) kept the constant exported with HC3-d (tests/binder-retry-taxonomy.test.ts:141-155, `s.calls() === MAX_BINDER_LLM_CALLS` driven through runBinderCallWithCancellation) as the coupling witness and ruled deriving/naming the budgets beside the constant "permitted, not required" — the candidate's "enforced only by comments and code review" anchor is also refuted by that pinning test (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917095931-d4-02-discoverysource-tables-parallel.md | false-positive | verdict: false-positive — re-verified: counts 5/4/5/5 over the 5-member union are accurate and excerpts match at cited lines, but membership drift is compile-blocked (tsconfig strict: Record<DiscoverySource> rejects added/missing keys; string-returning switches fail TS2366 when non-exhaustive) and the four values are separately spec-normative (priority table; DISC-2 severity table; DISC-2 rule-2 prose labels; discovery-sources.md#descriptor-kinds, which explicitly states the kinds are distinct from the prose labels) — D4 spec-table carve-out; same double-switch already dispositioned incidental in shards qw20260914060226/qw20260914130212 (triage: claude-fable-5-1) |
| 2026-09-17 | PTQ-0375-fn-param-junk-guard-cross-module-clone.md | human-defer | human-defer (2026-09-17): the clone is real (G055) but the only compliant dedupe relocates annotationToCompatType/annotationSourceIsNotTypeExpression out of type-layer-checks.ts (4006 LOC, strong band, 7 importers) - a D9-scale move for a two-branch guard. Wait for the inevitable D9 breakdown ruling on type-layer-checks.ts and fold the predicate relocation into it; re-file or re-accept then. |
| 2026-09-17 | qw20260917154546-d7-01-systemnotecontents-duplicated-four-in-scope-cells.md | duplicate | verdict: duplicate — independently re-verified: all four excerpts reproduce (params-unterminated-literal:89-116 and quoted-inline-field-name:102-129 diff byte-identical to harness.ts:814-840 collectSystemNotes incl. the PIC-72 comment; reserved-keyword-misfire-faces:173-203 and reserved-keyword-remaining-positions:152-182 are brace-reformatted with identical branch logic), all four copies are live (each called on handle.sessionManager.getEntries()), and collectSystemNotes greps as unexported (callers only at harness.ts:553/676/732) — but the root cause (harness.ts's unexported reader forcing per-file restatements, ~33 copies repo-wide) is already tracked by same-wave sibling quality/intake/qw20260917154546-d7-01-live-cell-note-channel-extraction-duplicated.md, triaged confirmed as the earliest-filed canonical filing that siblings dedupe against; exporting collectSystemNotes there fixes these four sites too (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-01-systemnotecontents-duplicated-schema-cells.md | duplicate | verdict: duplicate — independently re-verified: the three named `systemNoteContents` bodies (schema-field-discard-recovery:103-130, unterminated-template-registration:120-147, withheld-binder-provenance:128-155) md5 to the same 380e0a2d20e473fb1ab80d1ae69bac17 as tests/live/harness.ts:814-841 `collectSystemNotes` after a name-only substitution, the inlined copy at schema-body-unclosed-at-eof:176-197 differs only by indentation and iterating `handle.sessionManager.getEntries()` directly, all four files already import from ./harness, and `collectSystemNotes` greps as unexported (callers only harness.ts:553/676/732) — but the candidate omits that harness twin and its root cause (the unexported harness reader forcing per-file restatements, ~35 copies in tests/live) is already tracked by the earliest-filed, confirmed same-wave canonical quality/intake/qw20260917154546-d7-01-live-cell-note-channel-extraction-duplicated.md, against which sibling d7-01-systemnotecontents-duplicated-four-in-scope-cells was likewise ruled duplicate; exporting collectSystemNotes there fixes these four sites too (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-02-b0333-b0334-b0335-normative-message-helper-duplicated.md | false-positive | verdict: false-positive — excerpts reproduce (byte-identical normativeMessage/line at b0333:83-108, b0334:86-104, b0335:84-102) but both stated pattern searches are refuted: `^function normativeMessage` hits 27 test files not 3 (11 with this exact signature, incl. a byte-identical 4th copy at tests/reexport-chain-resolution.test.ts:223-231 and a near-copy at b0304:73-83) and `^function line(code: string, message: string)` hits 10 not 3; and the anchor that PTQ-0222/0215's dedupe "stopped one function short" is refuted by tests/helpers/registry-oracle.ts:7-10, authored in PTQ-0215's accepted fix (2594cd44), which states the module "centralises that read only" and each file's registryMessageOf-shaped reader "stays local" because assertion wording varies per file (verified: b0268 throws, import-*/subagent-root carry three other messages) — PTQ-0215's title named those readers, so the per-file reader is ratified scope, not an overlooked remainder, and the suggested export contradicts the helper's documented contract (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-02-b0463-bycode-redeclared.md | false-positive | verdict: false-positive — the anchor's "call sites read only `.code`" claim is refuted: b0463:163-164 read `invalid[0]!.file` (and :150/:152/:153/:186/:187 read `.severity`/`.message`), and the harness's `CapturedNote` (`{code, message, severity}`, package-merge-e2e-harness.ts:45-49) is a recording-double projection with no `file` member — a scratch copy of the file with the proposed `import { byCode, ... }` substitution and the local helper deleted fails `tsc --strict` with `TS2339: Property 'file' does not exist on type 'CapturedNote'` at both `.file` reads (unmodified control: 0× TS2339), so the local `byCode` over `readonly Diagnostic[]` (faces 2/3 filter real `discoverThetas` diagnostics, not harness notes) is a differently-typed, non-interchangeable helper, not boilerplate duplication of the export (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-02-bindings-span-reimplements-invoke-seam-scaffold.md | false-positive | verdict: false-positive — excerpts reproduce, but the anchor is refuted: invoke-seam-scaffold.ts's header scopes "a file that needs them" to the ExecuteBodyDeps scaffold for `executeBody`-driving invoke/code-call bug-witness files (0294/0295/0347/0349), and bindings.test.ts is a parser-level V3b test (added 23c62ee7 2026-06-30, predating the helper's f593d10e 2026-09-12) with zero executeBody/InvokeChild/ExecuteBodyDeps references; the helper is not canonical — three tests/helpers modules export the identical literal (call-with-clause-harness.ts:61 R(), invoke-seam-scaffold.ts:54 span(), tool-call-dispatch-harness.ts:51 span()), each for its own family — and a local span() recurs in 41 files under tests/, the suite-wide single-ingredient idiom that PTQ-0244's and PTQ-0278's triage already ruled independent of any harness and a false-positive shape when claimed alone (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-02-executable-probe-host-double-reimplemented.md | duplicate | verdict: duplicate — independently re-verified (all four excerpts match at cited lines; `function host(overrides` → exactly the 3 cited files; overridableExecutableHost is imported by no test file, only by fakeSubagentLaunchRequest in its own helper; placement-seam's copy landed in c7a47d17 on 2026-09-15, three days after the helper in 4d1c9e6a, and its `cli.js`/`endsWith("node")` deltas are not load-bearing — `cli.js` is asserted nowhere and both host() calls at :72/:280 resolve identically under the helper's regex), but the root cause — local `host()` reimplementing overridableExecutableHost — is already tracked as confirmed in same-wave sibling quality/intake/qw20260917154546-d7-01-host-launchrequest-reimplement-canonical-helper.md, which covers the two byte-identical sites (child-launch:61-69, executable-probe:27-34); fold the one new site, tests/subagent-placement-seam.test.ts:45-53, into that record at acceptance; note the candidate's docs/bugs grep claim of 0 hits is inaccurate (bugs 0168/0323 mention isGenericRuntime, 0168 names subagent-child-launch.test.ts as witness) but no merge/rename/delete is proposed so the witness carve-out is unaffected (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-02-inline-object-empty-parse-harness-duplicated.md | duplicate | verdict: duplicate — excerpts reproduce at both cited ranges, but the filing's accounting does not: `^function expectGroup(cells: readonly Cell\[\]` across tests/*.test.ts → 13 hits (not 2) and the literal `const FM = "---\nmode: subagent\n---\n";` → 8 hits (not 2), so this is one harness spread over 13 sibling files rather than a 2-site localized clone; the `diagLines` half is already exported verbatim from tests/helpers/e2e-s1.ts:100 (PTQ-0205's fix, a module both files already import `parseDoc` from), refuting "no such module yet exists" for that symbol; the same Cell/expectGroup root cause is filed with the correct 13-file inventory — explicitly naming both of these files at its lines 104-108 — by same-wave sibling quality/intake/qw20260917154546-d7-03-generic-argument-expectgroup-harness-duplicated.md, which should carry it (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-02-schema-alias-union-decl-ajv-emitted-helper-duplicated.md | duplicate | verdict: duplicate — independently re-verified the observation (excerpts reproduce at tests/schema-alias-union-decl.test.ts:624-635 and tests/helpers/scripted-live-session-harness.ts:112-119; the stated search returns exactly the same 12 tests/*.test.ts files; `diff` of the 625-635 body against params-inline-object-lowering.test.ts:445-455 is empty; the copy is live at 1044/1086/2310 and no resolved PTQ covers the capturing shape) but the root cause — the one byte-identical emitted-capturing `ajv()` builder retyped across 12 test files — is already tracked by same-wave intake filing quality/intake/qw20260917154546-d7-01-ajv-emitted-helper-duplicated-in-scope.md, triaged confirmed with a note naming this file for merge at acceptance; a per-shard re-filing of the same 12-copy clone from a different primary location is not a distinct finding (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-02-systemnotecontents-duplicated-inline-rename-let-annotation-pair.md | duplicate | verdict: duplicate — independently re-verified: both `systemNoteContents` bodies (inline-object-wire-name-rename:101-128, let-annotation-query-double-emission:115-142) diff byte-identical to tests/live/harness.ts:814-841 `collectSystemNotes` after a name-only substitution incl. the PIC-72 comment, both copies are live (called at :253 and :200/:208 on handle.sessionManager.getEntries()), and `collectSystemNotes` greps as unexported with no importer (callers only harness.ts:553/676/732), no gate/recording-double/coverage-matrix carve-out applies — but the root cause (the unexported harness reader forcing per-file restatements, 32 named `function systemNoteContents` copies plus inlined variants across tests/live) is already tracked by the earliest-filed, confirmed same-wave canonical quality/intake/qw20260917154546-d7-01-live-cell-note-channel-extraction-duplicated.md, against which siblings d7-01-systemnotecontents-duplicated-four-in-scope-cells and d7-01-systemnotecontents-duplicated-schema-cells were likewise ruled duplicate; exporting collectSystemNotes there fixes these two sites too (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-03-callable-set-runtime-span-strexpr-reimplemented.md | false-positive | verdict: false-positive — excerpts reproduce at 44-50 / 60-68, but the "canonical helper" anchor is refuted: the identical 1-char range literal is exported by three tests/helpers modules under three names (call-with-clause-harness.ts:61 `R`, invoke-seam-scaffold.ts:54 `span`, tool-call-dispatch-harness.ts:51 `span`) and a local `function span()` with this body is the suite idiom in 39 test files (53 files carry the literal), so the batch-scoped "defined twice" search does not reproduce at suite scope; callable-set-runtime-enforcement.test.ts is a QTL-2 callable-set test importing nothing from tests/helpers, and call-with-clause-harness.ts's own header scopes it to the RFC 0009 `with { cwd }` RED tests — the four "siblings" are that harness's own with-clause tests, not this file's kin; resolved PTQ-0278 (confirmed, fixed) already ruled the wider-suite span() idiom "independent of this harness" and stood only on the in-file fact that failure-arms already imported nine names from the module and triple-duplicated the value, neither of which holds here (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-03-console-error-spy-gate-duplicated.md | duplicate | verdict: duplicate — independently re-verified: all four excerpts reproduce at the cited lines (`let consoleErrorSpy` + `beforeEach` byte-identical; the escaped-quote `afterEach` differs only in string-literal wrapping, same runtime message), the class is D7 boilerplate duplication in tests/ with no binding carve-out (no *gate* file; bug docs 0129/0131/0156/0229 cite these files only as witnesses and the finding proposes no it()/describe() change; docs/bugs/0030 pins the spy gate at live-production-acceptance.test.ts:233-252, not here), and `^let consoleErrorSpy` greps to 27 tests/live files repo-wide, not 4 — but the identical root cause (the bug-0030 console.error spy setup/teardown scaffold redeclared per live cell instead of one shared helper beside `thetaOwnedStderrLines`) is already tracked by same-wave sibling quality/intake/qw20260917154546-d7-02-console-error-spy-gate-duplicated-schema-cells.md, triaged confirmed with a consolidation note naming this file; fold these four sites into that PTQ's location list at acceptance (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-03-live-cell-tools-chain-fixture-and-summands-duplicated.md | false-positive | verdict: false-positive — the fixture-literal excerpts do reproduce at the cited lines, but the finding's anchor does not: none of the three files carries the claimed header comment choosing (263, 514) "different from" bugs 0262/0274/0277/0278 "so no cell's discriminator can satisfy another's assertion" (grep of those bug numbers and that phrase returns 0 hits in all three; the wording lives in b0274live:102, b0277live:108, b0278live:88, b0281live:112, b0282live:129, b0284live:117), so the "stated intent is itself violated" claim is fabricated; and the (263, 514) pair / "prior step produced the number … plus 100" template are not a three-file copy but the repo-wide bug-0243 sentinel-discipline convention — used in ~20 tests/live cells and as the default `addends = [263, 514]` of promptTheta at tests/live/live-production-acceptance.test.ts:78 (template also in 5 further live cells) — leaving only an unanchored likeness of three 11-line planted-bytes literals whose bytes each cell's header documents as the object under observation (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-03-pi-cli-entry-requirepath-duplicated.md | duplicate | verdict: duplicate — independently re-verified (excerpt matches b0342:86-112 verbatim, sibling excerpts anchor at real-spawn:43 and inbound-boundary:100, `const PI_CLI_ENTRY = fileURLToPath` hits the same 12 tests/*.test.ts plus an exported 13th copy in tests/live/acceptance/harness.ts:49-66 the `tests/*.test.ts` glob missed, tests/helpers/ has 0 hits; minor over-claim: subagent-theta-roots-forwarding.test.ts:214 has `CHILD_MODEL_PROVIDER = "fable"`, not `"anthropic"`), but the same root cause — no tests/helpers/ home for the PI_CLI_ENTRY/EXTENSION_ENTRY/requirePath real-spawn bundle across these exact 12 files — is already tracked and triaged confirmed in same-wave quality/intake/qw20260917154546-d7-03-b0337-subagent-spawn-launch-watchdog-harness-duplicated.md, whose 12-file list names b0342-forwarded-enum-subagent-chain.test.ts and whose direction exports precisely these constants + requirePath (this filing is a strict subset of that bundle; its "0 hits in quality/intake" overlap check is refuted by that file) (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-03-specreservedkeywords-reimplements-exported-reservedkeywords.md | false-positive | verdict: false-positive — excerpts reproduce and `reservedKeywords` is indeed exported (lexer.ts:156; the "module-private" clauses at :167 and :395 are stale narration, both authored in 61806a3a — the same commit that flipped the function to `export`), but the anchor that `specReservedKeywords()` is a redundant re-derivation of that export is refuted: the helper reads docs/spec_topics/lexical.md:20 (the sentence exists) as an oracle deliberately independent of the implementation under test — c3's own name ("in spec order"), comment ("read out of lexical.md:20 rather than restated here on trust … added to or removed from the SPEC reds this cell") and the file header ("SPEC ANCHORS (the contract, not the current code)") state that design, matching the same file's DIAG-4 registry-read-from-spec oracle — so substituting `[...reservedKeywords()]` would change c3 from a spec-vs-matrix check into a code-vs-matrix check that no longer reds on a spec-only edit, not a mechanical dedupe; the sole verified defect is two stale doc-comment clauses in a test, which is none of the five D7 classes (a comment, not a name) and outside D2's src/ scope (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-04-composition-producer-conversation-drive-message-builders-duplicated.md | duplicate | verdict: duplicate — independently re-verified (both excerpts reproduce at the cited lines; `userMessage` and the api/provider/model/usage/cost/stopReason/timestamp envelope block are byte-identical; 4-file / 0-helper greps reproduce, and the only helpers emitting `role: "assistant"` build untyped session-entry doubles with model "m1" and no `usage`, not pi-ai `AssistantMessage` values), but the same root cause is already filed as the superset in same-wave intake sibling quality/intake/qw20260917154546-d7-14-prompt-transport-mapping-message-builders-duplicated.md (sites: 4, cross-module), which cites these two files' `userMessage` at 98-100 / 112-114 alongside the byte-for-byte prompt-transport-mapping ↔ b0413 pair and carries the stronger anchor (b0413's own comment stating it is "mirroring tests/prompt-transport-mapping.test.ts's pi-ai Message[] builders") — this 2-of-4 subset would be fixed by any fix of d7-14; note also the FP-check's docs/bugs "0 hits" and coverage-matrix "hits exist" claims both fail to reproduce (many hits / 0 hits respectively), immaterial to the duplication class (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-05-recording-ui-double-duplicated-rfc0010-cells.md | duplicate | verdict: duplicate — same root cause (the copy-pasted `RecordedCall`/`createRecordingUi` ExtensionUIContext recording double) is already filed in same-wave sibling quality/intake/qw20260917154546-d7-05-createrecordingui-extensionuicontext-double-duplicated.md (shard-101), which cites a superset of sites — both RFC-0010 cells (parent:70-117 / wire-child:82-129, re-diffed: zero-line, exit 0) PLUS the third near-identical copy at tests/live/execution-status-parfor-ui-live-cell.test.ts:73-133 that this filing omits; this filing's own "zero-line diff, exit code 0" claim over 82-139 / 70-127 also does not reproduce (exit 1: parent carries a `/** Whether any recorded call's text/lines carries … */` doc comment at :118 before `anyCallCarries`, and 70-127 truncates the function's closing `);`), so the sibling is the accurate survivor (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-10-e2e-s2-frontmatter-parse-harness-duplicated.md | duplicate | verdict: duplicate — independently re-verified: all five excerpts reproduce at the cited lines (resolvingMatcher and withCode byte-identical in all five; parse() variants as described), the exact `^const resolvingMatcher` search returns exactly these 5 files, none is a gate/kin file, coverage-matrix.md 0 hits, and docs/bugs 0300/0398/0478 cite these files for behavioural cells not this harness — but the same root cause (the resolvingMatcher/parse-over-parseFrontmatter/withCode harness retyped per file) is already tracked by this wave's confirmed qw20260917154546-d7-01-frontmatter-contract-bind-context-transcript-harness-duplicated.md, whose triage note already records tests/e2e-s2-advisory-diagnostics.test.ts:28-37, tests/e2e-s2-frontmatter-fields.test.ts:27-36 and tests/frontmatter-tool-loop-respond-repair.test.ts:40-55 as the three further copies a shared helper must absorb; this filing is the 5-site superset of that finding, not a distinct one (PTQ-0227 is a different doc()/expectRow harness over parseDoc) (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-118-03-ajv-emitted-builder-duplicated.md | duplicate | verdict: duplicate — independently re-verified the observation (both excerpts reproduce at tests/params-inline-object-lowering.test.ts:444-455 and tests/helpers/scripted-live-session-harness.ts:112-119; the stated search returns exactly the same 12 tests/*.test.ts files and md5 of all 12 extracted `ajv()` bodies is a single hash, so byte-identity holds for every copy not just the two spot-checked; the copy is live at 979/1013 and the file does not import the harness) but the root cause — the one byte-identical emitted-capturing `ajv()` builder retyped across 12 test files, with the harness exporting only the non-capturing `emit: () => {}` variant — is already tracked by same-wave intake filing quality/intake/qw20260917154546-d7-01-ajv-emitted-helper-duplicated-in-scope.md, triaged confirmed with a note naming this file (d7-118-03) for merge at acceptance; the sibling per-shard re-filing d7-02-schema-alias-union-decl was already dispositioned duplicate on the same ground, and citing the harness's non-capturing `ajv()` as "closer canonical source" adds no distinct root cause (the confirmed filing's triage note already records that export) (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-125-02-noop-checkpoint-ajv-rootdouble-reimplemented.md | duplicate | verdict: duplicate — observation re-verified (all four excerpts reproduce verbatim at the cited lines; the reviewed file imports only fake-json-child and e2e-s1, never tool-call-dispatch-harness; docs/bugs and coverage-matrix greps are 0 hits) but the root cause is already tracked by resolved PTQ-0209-rootdouble-producer-harness-duplicated.md, the confirmed pattern-wide finding on the hand-rolled NOOP_CHECKPOINT/rootDouble/producer trio whose triage counted 95 NOOP_CHECKPOINT and 89 rootDouble sites — tests/quality-loop-empty-tail-return-validation.test.ts (created c83b5c9e 2026-09-10) already held these exact declarations at lines 249/256/269 at that triage commit acac5cc3 and so was among the counted sites; the store's ratified posture since (REVIEW_LOG rows 111, 146, 148, 214) is that per-file recurrences of this trio are not re-filed, and the candidate's only distinguishing detail — naming tool-call-dispatch-harness (itself minted by PTQ-0238) as the AJV-carrying canonical rather than call-with-clause-harness — is a choice of import target, not a different root cause (the ajv() body is also exported by tests/helpers/scripted-live-session-harness.ts:113) (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-140-03-subagent-child-env-scrub-rootdouble-bindinput-duplicated.md | false-positive | verdict: false-positive — core claim does not reproduce: tests/helpers/parent-producer-harness.ts:45 declares `function rootDouble()` with NO `export` (exports are queryBody/makeParentDeps/parentBindInput/carrierRaw/marshalledHashes only), so the lead-named builder cannot be imported; its only exported path, makeParentDeps(), hardcodes `subagentParentEnv: {}` (line 97) and omits the getAllTools/getAvailable members the scrub drive needs, so the local createProductionProducerDeps call is load-bearing; the rootDouble recurrence is the 100+-file pattern already resolved under PTQ-0209 and logged do-not-refile in quality/REVIEW_LOG.md rows 57/111/146/148/214; the reviewed file (ce3ca3f2, 2026-09-11) predates the helper (f6832d60, 2026-09-17, PTQ-0384 fix scoped to b0328/b0343), so no existing helper was bypassed; the lone accurate residue — bindInput() as a 7-line copy of exported parentBindInput(theta) — is a shape in 37 test files (`thetaAbort: new AbortController()` grep) that the filing neither cites nor counts (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-149-03-visible-regime-producer-deps-harness-duplicated.md | duplicate | verdict: duplicate — the observation reproduces (noopPi 71-73/60-62 and subagentTheta 100-108/64-72 byte-identical; childCtx/rootDouble differ only by the `shutdown` param / optional `checkpoint` + `now` field; no tests/helpers module exports a `getAllTools`-shaped noopPi or a `parseExpressionSource(tail)` subagentTheta; no gate/coverage-matrix/docs-bugs carve-out), but the same root cause over the same two files and same four functions is already tracked and confirmed in the earlier same-wave filing quality/intake/qw20260917154546-d7-01-noop-pi-subagenttheta-childctx-quartet-duplicated.md (shard-01, filed 20:12 vs this file's 20:15), which the candidate's own overlap grep missed (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-63-noop-checkpoint-idsource-reimplemented-across-helpers.md | duplicate | verdict: duplicate — same root cause as resolved PTQ-0209-rootdouble-producer-harness-duplicated.md (no-op Checkpoint + fixed inv-1/tc-1 idSource RuntimeRoot double retyped instead of imported from tests/helpers; confirmed pattern-wide at 89/95 files, fixed by designating call-with-clause-harness.rootDouble() as the home, and logged as do-not-refile for residual instances in REVIEW_LOG.md lines 57/111/146/148/214) — parent-producer-harness.ts:45-55 is byte-identical to that exported rootDouble(); the candidate's own evidence is also refuted: the stated grep hits 7 tests/helpers files not 5, the two omitted (tool-call-dispatch-harness.ts:81-104 exported NOOP_CHECKPOINT+rootDouble, call-with-clause-harness.ts:152-167 exported rootDouble) disprove "a single canonical home", SEAM_NOOP_CHECKPOINT's header scopes it to the executeBody seam-scaffold family (exactly its 4 importers), and FakeIdSource is a PIC-20 conformance double with a shared finite throw-on-exhaustion sequence (0 of ~115 fixed-pair test files use it), not a canonical export of this shape (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-73-collectsystemnotes-reimplemented-two-more-live-cells.md | duplicate | verdict: duplicate — independently re-verified: alias-sink-array-element-check:117-141 `systemNoteContents` body diffs byte-identical to tests/live/harness.ts:815-839 `collectSystemNotes` (incl. the PIC-72 comment), b0046live:225-249 inlined loop is identical after dedent + `handle.sessionManager.getEntries()`→`entries`, both copies are live (called at :272 / consumed at :250-273), `function collectSystemNotes` at harness.ts:814 carries no `export` and greps to only its own callers at :553/:676/:732, and `customType === "theta-system-note"` hits exactly once per cited file — but the root cause (the unexported harness reader forcing per-file restatements, ~59 tests/live files) is already tracked by the earliest-filed, confirmed same-wave canonical quality/intake/qw20260917154546-d7-01-live-cell-note-channel-extraction-duplicated.md, against which siblings d7-01-systemnotecontents-duplicated-four-in-scope-cells, d7-01-systemnotecontents-duplicated-schema-cells and d7-02-inline-rename-let-annotation-pair were likewise ruled duplicate; exporting collectSystemNotes there fixes these two sites too (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-97-01-systemnotecontents-duplicated-five-in-scope-cells.md | duplicate | verdict: duplicate — independently re-verified: all five `systemNoteContents` bodies (index-sentinel-typeenv-case-fence:136-162, inline-field-name-not-identifier:108-134, inline-object-empty-field-type-truncation:195-221, inline-object-field-name-case:100-126, inline-object-malformed-entry-resync:121-147) diff byte-identical to each other and to tests/live/harness.ts:814-841 `collectSystemNotes` after a name-only substitution incl. the PIC-72 comment, all five copies are live (called on handle.sessionManager.getEntries() at :279/:262/:333/:245+:386/:242), all five files import from ./harness, and `collectSystemNotes` greps as unexported with no importer (callers only harness.ts:553/676/732), no gate/recording-double/coverage-matrix carve-out applies — but the root cause (the unexported harness reader forcing per-file restatements, 32 named `function systemNoteContents` copies across tests/live) is already tracked by the earliest-filed, confirmed same-wave canonical quality/intake/qw20260917154546-d7-01-live-cell-note-channel-extraction-duplicated.md, against which siblings d7-01-four-in-scope-cells, d7-01-schema-cells, d7-02-inline-rename-let-annotation-pair and d7-73-two-more-live-cells were likewise ruled duplicate; exporting collectSystemNotes there fixes these five sites too (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-97-02-console-error-spy-gate-duplicated-in-scope.md | duplicate | verdict: duplicate — independently re-verified: all five excerpts reproduce at the cited lines (`let consoleErrorSpy` + `beforeEach` byte-identical in all five; the `afterEach` bodies differ only in string-literal wrapping with the same runtime message — the filing's "byte-identical to malformed-entry-resync" diff claim is slightly off, resync uses the other wrapping, immaterial to the clone), the class is D7 boilerplate duplication in tests/ with no binding carve-out (no *gate* file; 0 hits in docs/reference/coverage-matrix.md; docs/bugs/0135/0154/0237/etc. cite these files only as witnesses and the finding proposes no it()/describe() change; docs/bugs/0030 pins the spy gate at live-production-acceptance.test.ts:233-252, not here), and `let consoleErrorSpy` greps to 28 tests/live files repo-wide, not 5 — but the identical root cause (the bug-0030 console.error spy setup/teardown scaffold redeclared per live cell instead of one shared helper beside `thetaOwnedStderrLines`) is already tracked by same-wave sibling quality/intake/qw20260917154546-d7-02-console-error-spy-gate-duplicated-schema-cells.md, triaged confirmed with a consolidation note naming this file, and sibling d7-03 was already ruled duplicate of it on the same grounds; fold these five sites into that PTQ's location list at acceptance (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-99-02-systemnotecontents-redeclares-harness-collectsystemnotes.md | duplicate | verdict: duplicate — independently re-verified: live-production-acceptance.test.ts:955-981 `systemNoteContents` sed-extracted and diffed against tests/live/harness.ts:814-840 `collectSystemNotes` after a name-only substitution → zero diff (PIC-72 comment included); `function collectSystemNotes` carries no `export` and greps only to its internal callers at harness.ts:553/676/732; the local copy is live (47 `systemNoteContents(handle.sessionManager.getEntries())` call sites), both locations under tests/, D7 boilerplate-duplication class, stated searches reproduce (docs/bugs `systemNoteContents` → 0; coverage-matrix file cite → 0; docs/bugs 0048/0287 cite harness's own reader, not this copy), no gate/recording-double/red-test carve-out — but the root cause (unexported harness reader forcing per-file restatements across tests/live) is already tracked by the earliest-filed confirmed same-wave canonical quality/intake/qw20260917154546-d7-01-live-cell-note-channel-extraction-duplicated.md, against which d7-73, d7-01-four-in-scope-cells, d7-01-schema-cells, d7-02 and d7-97-01 were likewise ruled duplicate on disjoint file sets; exporting collectSystemNotes there fixes this site too (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917154546-d7-70-fm-tail-body-fixture-duplicated.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce at tests/let-annotation-inline-object-compat.test.ts:213-219 and tests/let-annotation-query-double-emission.test.ts:96-101 (FM/TAIL/body byte-identical bar the doc-comment), both under tests/, D7 copy-paste-fixture class, no gate/recording-double carve-out; the filing's "no hits" claim for docs/bugs/ is wrong (39 hits, bugs 0093/0130/0118 cite both files as witnesses) but immaterial since no merge/rename/delete is proposed — however the accounting is a two-file slice of a repo-wide pattern (grep -F 'const TAIL = "let a = 1\na\n"' tests/ → 14 files, 13 with the identical `${FM}${stmt}\n${TAIL}` body(); bare FM literal in 101 files) and the root cause (no tests/helpers module exports the `mode: prompt` FM/TAIL/body() fixture triad, forcing per-file restatement) is already tracked by accepted quality/issues/PTQ-0751-inline-object-fixture-harness-duplicated.md, whose nine-helper inventory names FM/TAIL/body() verbatim at tests/inline-empty-object-type.test.ts:221-227 and inline-object-duplicate-field-name.test.ts:301-; the shared helper PTQ-0751's fix mints serves these two sites by import — fold them into PTQ-0751's location list at fix time (triage: claude-fable-5-1) |
| 2026-09-17 | qw20260917204232-d7-02-b30-emitdiagnostic-never-wired-assertion-cannot-fail.md | duplicate | verdict: duplicate — independently re-verified: the excerpt reproduces verbatim at tests/execution-status-child-tap.test.ts:157-183, `attachChildActivityTap(child, publish)` at :163 passes no third argument and the SUT's only optional param is `opts?: ChildTapOptions` (`{ clock?: Clock }`, src/extension/execution-status/child-tap.ts:66-70) with no diagnostic hook, `emitDiagnostic` greps to only its declaration (:160-161) and the `toBeDefined()` at :181 so the assertion is invariant under implementation behaviour (D7: assertion that cannot fail); not a gate file, 0 coverage-matrix hits, and although the filing's `grep -rln emitDiagnostic docs/bugs/` claim of "no hits" is wrong (48 files match an unrelated production identifier) none of them cite this test or B30, so no carve-out applies — but the identical root cause (same file, same it(), same never-wired local const, same `toBeDefined()` line) is already tracked as quality/issues/PTQ-0612-b30-emitdiagnostic-tautology.md (open, confirmed, prior wave qw20260917154546); nothing new here (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-01-b0314-assertloudthrow-duplicated.md | duplicate | verdict: duplicate — independently re-verified: all four `assertLoudThrow` excerpts reproduce at tests/b0314:141-167, b0332:179-205, b0370:214-236 (param renamed `leak`, messages compressed, same five expects in order) and b0392:198-224; `grep -rn "^function assertLoudThrow" tests/*.test.ts` → the stated 8 hits and b0369:199-212 does delegate to `assertFramesToInternalError`; tests/helpers/runtime-belt-probe-harness.ts exports only Probe/InterpProbe/InvokeProbe/rootDouble/producer/render/assertValue/makeBeltProbes (no throw-framing helper), docs/bugs `assertLoudThrow` → 0, coverage-matrix → 0 for all four files, bug 0314 fixed (0.293.0), no gate/recording-double carve-out, all sites under tests/ (D7 boilerplate duplication) — but the root cause (the belt-probe harness centralises `assertValue` yet stops short of the five-step surfaceUnexpectedThrow/INTERNAL_ERROR_CODE framing half, so each belt-probe file restates it) is already tracked verbatim as the "Why" and direction of open, confirmed quality/issues/PTQ-0584-b0365-b0366-b0367-internal-error-framing-block-duplicated.md; the shared helper that PTQ-0584's fix mints serves these four sites by import, per the store's precedent for same-cause/disjoint-file-set filings (systemNoteContents, FM/TAIL/body) — fold b0314/b0332/b0370/b0392 into PTQ-0584's location list at fix time (same-wave siblings d7-02-b0370 and d7-02-b0392 cite the same sites) (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-01-b0351-ast-executor-harness-duplicated.md | duplicate | verdict: duplicate — independently re-verified: all excerpts match at the cited lines with zero drift; `diff <(sed -n 160,193p …b0351) <(sed -n 175,208p …b0387)` (ScriptedHost + deps()) is exactly empty and the aligned builder span (b0351:72-159 vs b0387:75-174) differs only by b0387's own `blockExpr` (the candidate's literal 75-161 range also shows a 13-line truncation tail that is range misalignment, not doc-comment drift — non-blocking); the 17-declaration grep returns 17/17 in both files; both under tests/, D7 boilerplate-duplication class, not gate files, docs/bugs/0351:204 and 0387:174,182 cite the files but no merge/rename/delete is proposed; one FP-check claim corrected on record: tests/helpers already exports byte-identical `span()` (invoke-seam-scaffold.ts:55, tool-call-dispatch-harness.ts:51), `strExpr` (:59) and `NOOP_CHECKPOINT` (:81, scripted-typed-query-harness.ts:36), the same correction recorded on PTQ-0529/0536 — but the root cause (the statement-executor AST/executor harness bundle has no shared tests/helpers home, forcing per-witness-file restatement) is already tracked by the open, confirmed quality/issues/PTQ-0529-b0307-ast-harness-duplicated.md (canonical) and PTQ-0536-b0387-ast-executor-harness-duplicated.md, and the candidate itself states it "adds b0351 as the fourth site rather than proposing a new home" — the shared helper that fix mints serves b0351 by import, so fold tests/b0351-value-position-query-success-binds-ok.test.ts:72-159 into PTQ-0529's location list at fix time (precedent: systemNoteContents siblings and FM/TAIL vs PTQ-0751 ruled duplicate on disjoint file sets); the one component PTQ-0529/0536 do not inventory (ScriptedHost + deps(), b0351:160-193) is filed more completely by same-wave sibling quality/intake/qw20260918050411-d7-03-scriptedhost-deps-harness-triplicated.md, whose three-site list already includes b0351:152-203 (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-01-theta-line-joiner-duplicated-tools-entry-trio.md | duplicate | verdict: duplicate — independently re-verified: all three excerpts reproduce at the cited lines (derived-name-shape:219-221, closed-grammar:189-191, containment:133-135), sed-extracted bodies diff byte-identical, all three copies are live (9/10/16 `theta(` call sites), `grep -rn "function theta\b" tests/helpers/*.ts` → 0 and production-load-harness.ts exports only LoadOutcome/runProductionLoad/plantThetaWorkspace/disposeWorkspace/composedRunnableCount, none is a *gate* test, 0 docs/bugs and 0 coverage-matrix hits, no it()/describe() change proposed, PTQ-0517 cites the disjoint :298-330/:286-318/:190-232 scaffold and never mentions theta — a genuine D7 boilerplate-duplication observation, but the root cause (no `theta(...lines)` export in tests/helpers/production-load-harness.ts forcing per-file restatement across the discoverAndComposeFixtures production-load family; `grep -rl '^function theta(\.\.\.lines' tests/` → 25 files) is already tracked by open/confirmed quality/issues/PTQ-0606-theta-fixture-builder-quartet-duplicated.md, which names the identical home module and whose minted shared `theta()` serves these three same-family sites by import (PTQ-0782 was ruled distinct only because tests/live has its own harness module, which does not apply here) — fold these three sites into PTQ-0606's location list at fix time (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-02-b0370-assertloudthrow-fifth-copy.md | duplicate | verdict: duplicate — independently re-verified: the excerpt reproduces verbatim at tests/b0370-reassign-target-scope.test.ts:214-234 and its thrown arm is structurally identical to PTQ-0584's b0366:171-189 copy (same five expect calls — isThetaPanic→false, surfaceUnexpectedThrow(x, SITE)→defined, .code→INTERNAL_ERROR_CODE, .message→/^internal error: / — in the same order; only message-string literals differ); `grep -rn "surfaceUnexpectedThrow\\|INTERNAL_ERROR_CODE" tests/helpers/` → 0, coverage-matrix → 0, bug 0370 Status fixed (0.370.0), file 33/33 green, no gate/recording-double/red-test carve-out, D7 boilerplate-duplication class under tests/ — but the root cause (no shared internal-error-framing assertion in tests/helpers/runtime-belt-probe-harness.ts) and the suggested direction are those of open, confirmed quality/issues/PTQ-0584-b0365-b0366-b0367-internal-error-framing-block-duplicated.md verbatim, and the candidate's "fifth site" is an undercount: the same five-step block lives in 13 test files (b0314, b0332, b0338, b0365, b0366, b0367, b0368, b0369, b0370, b0392, b0394, b0402, b0439 — `function assertLoudThrow` declared in 8 of them), so the helper PTQ-0584's fix mints serves this site by import; fold tests/b0370-reassign-target-scope.test.ts:214-234 into PTQ-0584's location list at fix time (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-02-b0392-assertloudthrow-duplicates-framing-check.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce verbatim at tests/b0392-unary-minus-operand-discipline.test.ts:198-224 and 239-255 with the same four expects (isThetaPanic→false, surfaceUnexpectedThrow(x, SITE)→defined, .code→INTERNAL_ERROR_CODE, .message→/^internal error: /) in the same order, both helpers live (callers at :293 and :319), file 20/20 green, 0 coverage-matrix hits, bug doc 0392 cites the whole file only, not a gate file, no recording-double/red-test carve-out, tests/helpers/ has 0 INTERNAL_ERROR_CODE/surfaceUnexpectedThrow hits so no shared framing helper exists — but the root cause (the belt-probe harness centralises `assertValue` and stops short of the surfaceUnexpectedThrow/INTERNAL_ERROR_CODE framing half, so every belt-probe file restates it; `function assertLoudThrow` in 8 tests/ files, `function assertFramesToInternalError` in 7) is already tracked by open, confirmed quality/issues/PTQ-0584-b0365-b0366-b0367-internal-error-framing-block-duplicated.md, whose shared-helper fix replaces BOTH of this file's copies by import and thereby dissolves the intra-file pair; same-wave sibling d7-01-b0314 citing this very site (b0392:198-224) was already ruled duplicate of PTQ-0584 on the same grounds, and resolved PTQ-0586 (b0392 harness bundle) explicitly left these two helpers out — fold b0392:198-224 and :239-255 into PTQ-0584's location list at fix time (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-02-capturednote-producerwithcapture-duplicated.md | duplicate | verdict: duplicate — independently re-verified: all four excerpts reproduce at the cited lines, `diff` of the two `producerWithCapture` bodies (b0478:406-422 vs binder-forced:338-354) shows exactly the claimed signature-default/`[model]` differences, both files green (35/35), both under tests/, D7 boilerplate-duplication class, no gate/recording-double/coverage-matrix carve-out — but the root cause (no shared tests/helpers export of the e2e-s5-origin CapturedNote/rootDouble/producerWithCapture note-capture rig) is already tracked by the confirmed PTQ-0535 family: quality/issues/PTQ-0535-b0381-ajv-producer-harness-duplicated.md's own census and triage note name tests/b0478-augmented-agentmessage-variants-excluded-before-walk.test.ts by file as a member of the 13-file cluster ("the disjoint b0398/b0478 pair", "binder-forced/post-merge pairs are further clusters", "the fix should land one shared helper"), and quality/issues/PTQ-0628-binder-forced-tool-dispatch-harness-duplicated.md already covers binder-forced-tool-dispatch.test.ts's CapturedNote (:163-167) and its SYSTEM_NOTE_CHANNEL filter; the candidate itself concedes its sites "should fold into [PTQ-0628's] location list rather than stand as an unrelated cause"; its stated searches also misreport the census (`function producerWithCapture` → 13 test files, not 2+1; tests/helpers/package-merge-e2e-harness.ts:42 already `export interface CapturedNote`, not 0 helper hits; local `const SYSTEM_NOTE_CHANNEL` → 18 test files; docs/bugs → 1 hit, 0437, citing neither in-scope file) — fold b0478's two sites into PTQ-0535/PTQ-0628's location lists at fix time (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-02-frontmatter-parse-harness-duplicated.md | duplicate | verdict: duplicate — independently re-verified: all three excerpts reproduce at the cited lines; sed-extracted matcher/parse/diagCodes/diagLines blocks (shape-refusal:295-310 vs zero-entry:218-233) diff to the single `"bug0104.theta"`/`"bug0206.theta"` literal and the BODY_NO_CALL/theta pair (225-226+235-238 vs 235-241) differs only by one doc-comment line; every piece is live (parse 7/5, diagCodes 2/3, diagLines 5/4, theta 18/12, BODY_NO_CALL 14/12 hits), both stated greps return exactly these two files, neither is a gate/kin test, no recording double, 0 coverage-matrix hits, no it()/describe() change proposed; both files import only ./helpers/registry-oracle and no tests/helpers module exports a FrontmatterParseResult-typed harness (e2e-s1.ts:124-131 diagLines/diagCodes take a ThetaDocument) — but the root cause (no shared helper for the parseFrontmatter-driving resolving-matcher `{ resolve: () => "resolved" }` + `parse(source, { file, modelMatcher })` wrapper + `---`-fence `theta` joiner, forcing per-file restatement) is already tracked by open/confirmed quality/issues/PTQ-0469-frontmatter-contract-bind-context-transcript-harness-duplica.md, whose triage note already directs the shared helper to absorb the same harness in e2e-s2-advisory-diagnostics, e2e-s2-frontmatter-fields and frontmatter-tool-loop-respond-repair; `grep -l 'resolve: () => "resolved"' tests/*.test.ts \| xargs grep -l 'parseFrontmatter('` → exactly 7 files = PTQ-0469's 2 + its note's 3 + these 2, so this filing is a two-file slice of that tracked pattern (the d7-70/PTQ-0751 precedent) and the candidate's dedupe check never consulted PTQ-0469; the file-label-parameterised parse/theta helper PTQ-0469's fix mints serves these sites by import, with the FrontmatterParseResult-typed diagCodes/diagLines reducers riding along — fold both files into PTQ-0469's location list at fix time (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-02-params-brace-union-ajv-double-reimplements-capturingajv.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce (harness :135-145 `export function capturingAjv()`, test :539-549 `function ajv()`) and `diff` of the sed-extracted blocks after a name-only substitution is empty; the local copy is live (called at :1120/:1166/:1168 with `emitted` read as positive values), `grep helpers/scripted-live-session-harness` on the file → 0, `capturingAjv` is imported only by the two files the now-resolved PTQ-0425 migrated while 10 `^function ajv(): { readonly validator…` restatements remain repo-wide, stated docs/bugs-0097 and coverage-matrix searches → 0, both locations under tests/, D7 copy-paste-double class, no gate/recording-double/red-test carve-out — but this identical root cause (capturingAjv exported, sibling restatements never migrated) is already tracked by the earlier-filed, confirmed same-wave canonical quality/intake/qw20260918050411-d7-01-params-inline-object-ajv-double-reimplements-capturingajv.md, whose triage note names this d7-02 filing for fold-in; add tests/params-brace-union-rhs-lowering.test.ts:539-549 to that canonical's location list at acceptance (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-02-pkg-roots-buildpackages-third-site.md | duplicate | verdict: duplicate — independently re-verified: `PKG_ROOTS` at tests/discovery-glob-universe-enumeration-failure.test.ts:253-259 and `buildPackages` at :279-286 reproduce verbatim and sed-extract/diff byte-identical to the copies in discovery-root-enumeration-failure.test.ts (now :226-232 / :252-259 — the candidate's :258-268/:288-295 cites drifted 36 lines after commit cc0a8fe7, content unchanged) and discovery-tree-walk-lstat-failure.test.ts (:166-172 / :188-195), the only divergence being the doc comment's `packageRoots` line cite (`226-246` vs `232-242`; production now at src/discovery/package-discovery.ts:214, so all three are stale); `grep -rn "const PKG_ROOTS" tests` and `"^function buildPackages"` each return the same 5 files as stated, no `tests/helpers/` export exists, the copy is live (6 `buildPackages(` calls in the glob file), not a gate file, 0 coverage-matrix hits, bug 0113/0078/0207/0461 witness cites unaffected since no cell merge/rename/delete is proposed — but the root cause (no shared helper export for the five-root empty fixture + its FakeFileSystem wrapper, forcing per-file restatement) is exactly what open, confirmed quality/issues/PTQ-0487-pkg-roots-buildpackages-duplicated.md already tracks, and the candidate itself names PTQ-0487 as the anchor; the shared helper PTQ-0487's fix mints serves this site by import — fold tests/discovery-glob-universe-enumeration-failure.test.ts:249-259 and :279-285 into PTQ-0487's location list at fix time (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-02-plantedtheta-observed-withcode-duplicated-sibling-pair.md | duplicate | verdict: duplicate — independently re-verified: all six excerpts reproduce verbatim at the cited lines (derived-name-shape :214-217/:339-345/:467-470; closed-grammar :184-187/:327-333/:508-511), all copies live (PlantedTheta types `THETAS` at :231/:201; `observed()` ×13/×20; `withCode(` ×10/×7), both under tests/, not gate files, 0 docs/bugs and 0 coverage-matrix hits, no it()/describe() touched — but two of the three helpers are already tracked and the filing's accounting for both is wrong: (1) `PlantedTheta {stem,text}` is the fixture-record for the inline plant loop that open/confirmed PTQ-0517 (same two files, :298-330/:286-318) migrates to `plantThetaWorkspace`, whose exported `PlantedThetaFile {stem; text; ext?}` at tests/helpers/production-load-harness.ts:109 is the shared shape the filing's "no shared module exports any of the three" (`grep "interface PlantedTheta" tests/helpers/*.ts` → 1 hit, not 0) says does not exist — the PTQ-0600/0669 triage notes already treat local `PlantedTheta` as absorbed by that migration; (2) `withCode` in tools-derived-name-shape.test.ts is a listed site of open/confirmed PTQ-0740 (resolveCallableSet harness quintuple `withCode/piTool/thetaCallee/deps/resolveList`, then :479-525), refuting "none of [the prior withCode filings] cites either of these two files", and closed-grammar's `withCode` heads the same group-C `withCode/deps/resolveScalar/resolveList` resolveCallableSet harness that 0740's shared export targets; (3) `observed()` alone is untracked (store grep → only this candidate) but the "sibling pair" framing is a two-file slice of a 9-file pattern (`grep -rlF ' Registered: ${JSON.stringify(outcome.registered)}' tests/` → 9 files; 4 byte-identical zero-arg copies over module `outcome`, plus `observed(o: LoadOutcome)`/`observed(stem)` variants in the rest) and, closing over module state, has no seam in either 0517 or 0740 — fold the two PlantedTheta sites into PTQ-0517 and closed-grammar's withCode into PTQ-0740 at fix time, and re-file `observed()` as its own single-root-cause candidate over the full 9-file set (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-02-thetasrc-fixture-builder-triplicated.md | duplicate | verdict: duplicate — independently re-verified: all three excerpts reproduce byte-for-byte at tests/discriminator-field-classifier-brace-group.test.ts:564-566, empty-object-discriminator-field-withhold.test.ts:175-177 and non-literal-by-field-refusal.test.ts:224-226; `grep -rn "^function thetaSrc" tests/` → exactly those 3 hits and no src/extensions/tools reference; docs/bugs `thetaSrc` → 0, coverage-matrix cite of the three files → 0; all three already import parseDoc from tests/helpers/e2e-s1; not gate files, pure string template so no recording-double carve-out; D7 copy-paste-fixture class in tests/ only — one accounting correction: tests/helpers/prompt-value-harness.ts:41 already exports the `FM = "---\nmode: prompt\n---\n"` prefix half (the wrapper function itself is indeed unexported) — but `thetaSrc(decls)` is `${FM}${decls}\n` + `let a = 1\na`, the same `mode: prompt` + `let a = 1\na`-tail body() wrapper (differing only in the trailing newline and the identifier) whose root cause — no tests/helpers module exports the FM/TAIL/body() fixture wrapper, forcing per-file restatement (14 tests/*.test.ts carry the TAIL const, 22 the FM literal) — is already tracked by accepted quality/issues/PTQ-0751-inline-object-fixture-harness-duplicated.md, against which same-shape sibling qw20260917154546-d7-70 was likewise ruled duplicate on a disjoint file set; the shared wrapper PTQ-0751's fix mints serves these three sites by import — fold them into PTQ-0751's location list at fix time (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-03-shippedharness-third-copy-glob-universe.md | duplicate | verdict: duplicate — independently re-verified: all excerpts reproduce at tests/discovery-glob-universe-enumeration-failure.test.ts:1004-1095 and tests/load-warning-delivery.test.ts:257-347; the two `makeShippedHarness` bodies prettier-normalised and diffed under $TEMP differ only in the sibling's added `availableModels` parameter and its `[...availableModels]` substitution for `[]` (plus a comment re-wrap), so this is a live (cell E1 exercises it; vitest 19/19 green), under-tests/ copy-paste double in D7's boilerplate-duplication class; stated searches reproduce (`makeShippedHarness\|ShippedHarness` in docs/bugs → 0; coverage-matrix file cite → 0; not a gate file; bug 0113 witness citation unaffected since no it()/describe() is touched) — but the root cause (this exact RecordedNote/pi-double/ctx-double/`createThetaExtension`-via-`composeExtensionInstance`/`fireSessionStart` harness redeclared per file with no tests/helpers export) is already tracked as open, confirmed quality/issues/PTQ-0632-01-shipped-harness-duplicated-load-phase-pre-eval.md, whose fix (a shared helper module) serves this site by import exactly as the filing itself concedes; fold tests/discovery-glob-universe-enumeration-failure.test.ts:1004-1095 into PTQ-0632's location list at fix time (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-04-ajv-capturing-double-duplicated-nested-lowering.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce (harness :135-145 `export function capturingAjv()`, test :588-598 `function ajv()`) and `diff` of the sed-extracted blocks after a name-only substitution is empty; the local copy is live (called at :978/:1013/:1179/:1249/:1305/:1684/:1717, `emitted` read at :978), `grep helpers/scripted-live-session-harness` on the file → 0, the exact `^function ajv(): { readonly validator…` search reproduces at the same 10 files, `capturingAjv` is imported only by the two files the now-resolved PTQ-0425 migrated, stated docs/bugs-0039 and coverage-matrix searches → 0, both locations under tests/, D7 copy-paste-double class, no gate/recording-double/red-test carve-out and no merge/rename/delete proposed — but this identical root cause (capturingAjv exported, sibling restatements never migrated) is already tracked by the earlier-filed, confirmed same-wave canonical quality/intake/qw20260918050411-d7-01-params-inline-object-ajv-double-reimplements-capturingajv.md, against which d7-02 (params-brace-union) was likewise ruled duplicate on a disjoint file; the candidate's "capturingAjv → 0 hits in quality/" overlap claim missed those siblings; add tests/inline-object-nested-lowering.test.ts:588-598 to that canonical's location list at acceptance (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-07-session-shutdown-real-teardown-boot-duplicated.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce verbatim at e2e-s6-session-shutdown-real-teardown.test.ts:60-86 and session-shutdown-wiring.test.ts:48-71, both `boot()` copies are live (4 `await boot(` callers each) and share the byte-identical `deps: ThetaExtensionDeps = { fixtures: [], composeInstance: async (): Promise<ExtensionInstanceWiring> => ({ …, clock: new FakeClock(), installHotReload: () => ({ detach }) }) }` literal plus the `createThetaExtension(deps)(harness.pi); await harness.fireSessionStart();` drive pair, differing only in which seeds are inlined vs parameterised; no tests/helpers module offers a fixed-wiring composeInstance-stub boot (the three helpers mentioning composeInstance all call real composeExtensionInstance; watch-arming-harness's bootWatchArming included); stated searches reproduce (docs/bugs → 0073/0074/0216 by filename only, coverage-matrix → 0, both files green 11/11), both sites under tests/, D7 boilerplate-duplication class, no gate/recording-double/merge carve-out; PTQ-0576 (fixed) really was scoped to the Harness/makeHarness/makeTheta triple beneath this layer — but the root cause (no shared composeInstance-stub `bootFactory`-shaped helper, forcing each factory-wiring test to inline the same ThetaExtensionDeps/ExtensionInstanceWiring/createThetaExtension/fireSessionStart drive) is already tracked by the earlier-numbered, confirmed same-wave canonical quality/intake/qw20260918050411-d7-01-bootfactory-scaffolding-duplicated.md, whose triage note explicitly names session-shutdown-wiring.test.ts `boot()`/`bootWithDiagnosticSink()` and e2e-s6 `boot()` as the other slice of the same pattern and directs the fixer to mint one shared boot helper serving all four files; the candidate's own suggested direction ("a shared `bootFactory`-shaped helper") is that fix; fold these two sites (plus the uncounted third copy `bootWithDiagnosticSink` at session-shutdown-wiring.test.ts:198-219) into d7-01's location list at acceptance (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-47-01-rootdouble-realajvvalidator-duplicated.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce (enum-access-merge `realAjvValidator` :371-379 / `rootDouble` :386-401; trailing-residue :902-910 / :919-934), awk-extracted `realAjvValidator` bodies diff to zero and `rootDouble` bodies differ only in the `src`/`source` binding, `grep -rl "function realAjvValidator" tests/*.test.ts` → 16 files reproduces, neither is a gate file, the real AJV validator and rejecting fs are not recording doubles, 0 coverage-matrix hits, no it()/describe() merge/rename/delete proposed (the filing's docs/bugs "0 hits" is off by one — bug 0172:1255 names another test's rootDouble — immaterial); but the trailing-residue site :896-934 lies wholly inside open confirmed quality/issues/PTQ-0661-03-runbinder-drive-harness-duplicated.md's cited range :856-1073, whose Observation and triage note name `realAjvValidator()`/`rootDouble()` explicitly and whose suggested direction (a shared helper holding `realAjvValidator` and `rootDouble` parameterised by the fixture-source map) is exactly this candidate's; the sole new fact is a third consumer at tests/params-default-enum-access-merge.test.ts:364-401 that imports the same helper PTQ-0661's fix mints — per the d7-70/d7-99-02 precedent fold that site into PTQ-0661's location list at fix time (the PTQ-0655 "each a different file" precedent does not apply when one cited site is the same file and lines as an open issue) (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-57-02-ajv-reimplements-capturingajv.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce (harness :135-145 `export function capturingAjv()`, test :295-305 `function ajv()` — the filing's :294 is the doc-comment line, trivial drift) and `diff` of the sed-extracted blocks after a name-only substitution is empty; the local copy is live (destructured `{ validator, emitted }` at :817/:852/:884 with `emitted` read as positive values), `grep helpers/scripted-live-session-harness` on the file → 0, the exact `^function ajv(): { readonly validator…` search reproduces at the same 10 tests/*.test.ts files, `capturingAjv` is imported only by the two files the now-resolved PTQ-0425 migrated, stated docs/bugs `capturingAjv` and coverage-matrix searches → 0 (the filing undercounts bug-doc citations of the file — 15 docs, not just 0044 — but immaterial since no merge/rename/delete is proposed), both locations under tests/, D7 copy-paste-double class, no gate/recording-double/red-test carve-out — but this identical root cause (capturingAjv exported, sibling restatements never migrated) is already tracked by the earlier-filed, confirmed same-wave canonical quality/intake/qw20260918050411-d7-01-params-inline-object-ajv-double-reimplements-capturingajv.md, whose triage note names this d7-57-02 filing for fold-in and against which d7-02 and d7-04 were likewise ruled duplicate on disjoint files; add tests/reserved-keyword-type-position.test.ts:295-305 to that canonical's location list at acceptance (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918050411-d7-57-03-loadparams-harness-duplicated.md | false-positive | verdict: false-positive — both excerpts reproduce verbatim at tests/reserved-keyword-type-position.test.ts:308-347 and tests/schema-alias-union-decl.test.ts:616-660 and the duplication between them is real, but the filing's root cause and its stated search do not reproduce: `grep -rl "loadParams\\|LoadedParams" tests/helpers/*.ts` → 1 hit, not 0 — tests/helpers/e2e-s1.ts:177 `export interface LoadedParams` and :188 `export function loadCleanly(label, source, path)`, the PTQ-0212 (fixed, commit 2594cd44 2026-09-11) canonical whose three throw clauses are the common origin of both local copies (sed-extracted diff: the `frontmatter === null` and `params === undefined` clauses byte-identical in canonical and both copies, the `loweredSchema === undefined` clause differing only by the dropped "for the argument boundary" phrase; both files import only `parseDoc`/`codes` from e2e-s1, never `loadCleanly`), so the "why" ("no shared tests/helpers/ module backing either copy", "no canonical helper currently exists") is refuted and the suggested direction — minting a new tests/helpers reader — would stand a second canonical beside `loadCleanly`; the schema-alias-union-decl site is already tracked by the confirmed same-wave qw20260918050411-d7-03-schema-alias-union-decl-loadparams-reimplements-loadcleanly, and the reserved-keyword-type-position copy (region last changed 61806a3a 2026-08-02, pre-export) is a genuine unmigrated PTQ-0212 residual that should be refiled on its own against `loadCleanly` per the store's per-file convention, not as this pairwise finding (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918075903-d7-01-package-discovery-flush-reimplements-canonical.md | duplicate | verdict: duplicate — independently re-verified: all five excerpts reproduce at the cited lines, sed-extracted diff of each local body against tests/helpers/fake-clock.ts:94-98 (after stripping `export` / renaming `flushMicrotasks`) is empty for all four copies, every copy is live (2/2/22/3 `await flush*(` call sites), all four files already import `FakeClock` from the same module, the `async function flush(times = 8)\|flushMicrotasks` search reproduces at exactly the 4 cited files, docs/bugs and coverage-matrix searches → 0, all locations under tests/, D7 copy-paste-helper class, no gate/recording-double/red-test carve-out (the filing's "no test file imports the exported flush" is wrong — tests/reload-teardown-quiesce.test.ts:47 and tests/b0376-teardown-call-label-set-underenumerates.test.ts:32 do via `import { FakeClock, flush }` — but that only strengthens the fit of the canonical import) — however this identical root cause (fake-clock.ts exports `flush`, sibling FakeClock importers retype it) is already tracked by the open, confirmed quality/issues/PTQ-0831-reload-debounce-flush-redeclared.md, which cites the very same tests/reload-debounce.test.ts:22-27 site and whose triage note already names tests/hot-reload-stale-quiesce-arms.test.ts:31 and tests/package-discovery.test.ts for fold-in at fix time; per the d7-47-01/d7-57-02 precedent (same file and lines as an open issue → duplicate, not a per-file refile) the sole new fact — tests/execution-status-checkpoint-decorator.test.ts:33-37 `flushMicrotasks` — should be added to PTQ-0831's location list alongside the other two uncited copies at acceptance (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918075903-d7-02-execution-status-bus-stub-duplicated.md | duplicate | verdict: duplicate — independently re-verified: all three excerpts reproduce (tool :50-76 `fakeBus`, tool :292-314 inline, wire :164-181 inline — trivial drift from the cited :48/:289/:163), `grep ExecutionStatusBus tests/helpers/` → 0, both files import only `ARGS, fakeEntry, fakeHostApi` from the helper, the `L3-B12\|L3-B21` coverage-matrix/bug-doc search → 0, all under tests/ and D7 copy-paste-double class; but the same root cause is already tracked by open, confirmed quality/issues/PTQ-0852-executionstatusbus-noop-double-triplicated.md — the wire file contains exactly one `invocationStarted: (): void => {}` literal, so this candidate's wire site IS PTQ-0852's third location (its :192-207 drifted to :166-181), and PTQ-0852's triage note already names both execution-status-progress-tool.test.ts sites (`fakeBus` and the inline copy) for fold-in (sites: 5); nothing here is new beyond that fold-in (title's "12-method" also miscounts — the interface at src/extension/execution-status/types.ts:185-217 has 14 members) (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918075903-d7-03-bootstrap-sink-liveness-makehost-reimplements-canonical.md | false-positive | verdict: false-positive — independently re-verified: every excerpt reproduces at the cited lines and the 11 no-op `pi` stubs + 3 `ctx` scalars are byte-identical, but the headline root cause ("reimplements the canonical makeHost/RecordedNote/HostDouble") does not hold — mktemp `diff` of harness :66-107 vs test :187-251 shows 34 differing lines: the signature (`makeHost(options: HostOptions = {})` vs `makeHost(cwd: string)`), BOTH recording surfaces (`sendMessage` counts attempts, throws `staleError`/`sendThrows`, captures `triggerTurn`; `ui.notify` counts attempts and throws when invalidated), and the return shape (`{pi, ctx, notes, notified, handlers, sendAttempts, notifyAttempts}` vs the canonical's `{pi, ctx, notes, notified}` at :105 which NEVER exposes `handlers`); every diverged member is live (`host.handlers` :350 via requireHandler, `triggerTurn` :270, `notifyAttempts()` :275/:292, `sendAttempts()` :309/:764, `invalidated` :364/:423, `sendThrows` :296) and `RecordedNote.triggerTurn`/`HostDouble.sendAttempts\|notifyAttempts\|handlers` have no canonical counterpart, so the suggested compose-over direction ("override only sendMessage and ui.notify") cannot supply `handlers` without also overriding `on`, leaving 10 trivial no-op one-liners as the whole reusable residue — this is a diverged options double born as such (e3d2418c 2026-07-29, `-S'function makeHost(cwd: string)'` → 0 commits; the harness landed 2594cd44 2026-09-11), exactly as already ruled on record in fixed PTQ-0477's triage note ("NOT a copy … a diverged options double … that bug 0023's tier tests require"); the one genuinely byte-identical export, `noteDiagnostics` :114-121, is already tracked by open PTQ-0744 (whose triage note names the harness :202 export), so it is not a new root cause here; stated docs/bugs 0023 (:214 witness count only) and coverage-matrix (0 hits) searches reproduce (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918075903-d7-04-bootstrap-console-stderr-capture-duplicated.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce at tests/extension-bootstrap-sink-liveness.test.ts:125-148 and tests/extension-bootstrap-production-wiring.test.ts:246-269, `diff` of the mktemp-extracted 24-line spans is empty, both copies are live (7 / 8 call sites), the `function captureConsoleError\\|function captureStderr` search reproduces at exactly these two files across src/ extensions/ tools/ tests/, no tests/helpers module exports either, coverage-matrix/docs/bugs grep → 0, both sites under tests/, D7 boilerplate-duplication class with no gate/recording-double/red-test carve-out — but this identical root cause (captureConsoleError/captureStderr byte-identical between these same two files) is already tracked by the open, confirmed quality/issues/PTQ-0744-qw20260917154546-d7-52b-bootstrap-spy-capture-narrowing-dupl.md, whose locations list production-wiring:246-265 and sink-liveness:124-143 (the sink-liveness range has drifted by one line since); the only material this candidate adds is the three-line `afterEach(() => { vi.restoreAllMocks(); })` immediately following the pair, a trivial appendix to the same block rather than a distinct root cause — fold the afterEach hook into PTQ-0744's fix at acceptance (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918092852-d7-01-parse-registry-msg-wrapper-duplicated.md | duplicate | verdict: duplicate — independently re-verified: all seven excerpts reproduce at the cited lines and a mktemp `diff` of the 4-line `REGISTRY`/`msg()` blocks (case :119,121-123 vs reserved :221,228-230) is empty, both stated greps return exactly 1 hit per file, and neither file imports `PARSE_REGISTRY`; but this exact block in these exact two files is the root cause already tracked and FIXED as quality/resolved/PTQ-0567-registry-load-and-msg-duplicated-canonical-unused.md (locations fn-param-name-case:115-151, fn-param-name-reserved-keyword:214-255) — `git show cc0a8fe7` (2026-09-18, wave qw20260917204232, 153 test files) shows the current lines ARE that fix's output: the hand `parseRegistry(readFileSync(...))` read became `readRegistry(["parse"])` and the 15-line local `msg()` collapsed to a one-line shim over `registryMessageOf`, the shape the ratified registry-oracle header rules for ("each file's own registryMessageOf-shaped reader … stays local, parameterised by the REGISTRY this module exports", echoed in PTQ-0501's triage note) and the shape registry-oracle.ts itself uses at :52-56 (`LOAD_REGISTRY = readRegistry(["load"])` + `loadRowMessage`); the candidate's "sites: 2" also undercounts — the identical shim line is in 14 tests/ files (12 with the same `readRegistry(["parse"])` const), so it is the post-fix repo idiom, not a two-file copy, and its "bypasses PARSE_REGISTRY" framing is a preference for load-row-harness's b02xx read over the registry-oracle read the PTQ-0567 fix chose, i.e. a fix-design re-litigation of a resolved row rather than a new root cause (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918092852-d7-02-two-param-theta-fixture-duplicated.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce verbatim at tests/e2e-s5-binder-echo-emission.test.ts:63-91 and tests/echo-value-rule1-sanitisation.test.ts:434-458, the awk-extracted `TWO_PARAM_THETA` blocks diff to zero and both copies are live (5 / 1 `twoParamTheta()` call sites), but the root cause — the `TWO_PARAM_THETA` fixture retyped per file with no `tests/helpers` export — is already tracked by the OPEN, confirmed quality/issues/PTQ-0628-binder-forced-tool-dispatch-harness-duplicated.md, whose title, observation, evidence (:117-131) and fix direction (:137) name `TWO_PARAM_THETA` as one of the sextet to hoist into a shared helper, so its fix necessarily mints the canonical export this file's copy would import (the candidate's "different file pair" argument confuses the pairing with the root cause); the candidate's own stated searches also do not reproduce — `grep -n "^const TWO_PARAM_THETA = \[" tests/*.test.ts` and `grep -n "^function twoParamTheta" tests/*.test.ts` each return 6 hits, not 2 (b0397-binder-failure-note-runtime-event:146/189, b0401-informational-notes-omit-details:231/243, b0417-responses-binder-toolchoice-gate:178/209, binder-forced-tool-dispatch:173/372 all carry a byte-identical fixture, every copy diffing to zero against e2e-s5) — fold echo-value-rule1-sanitisation:437-458, b0397:146-198, b0401:231-252 and b0417:178-218 into PTQ-0628's fix at acceptance (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918092852-d7-10-proto-named-binder-jsonslug-range-not-migrated.md | duplicate | verdict: duplicate — independently re-verified: the local `const jsonSlug` :103-106 and `function range` :114-116 sit at the cited lines, the file's sole helper import is :18 `{ hasOwn, prototypeReport }`, the helper's exported `jsonSlug` (:7-10) is byte-identical and its `range` (:33-35) differs only in the `end.column` sentinel (2 vs 10) that no caller reads (`grep '\.end'` in the binder file → 0 hits; the sole call is `range(index + 1)` at :137 feeding `parseParams`), both siblings already import the names (record-write-sites:27, schema-validator-enforcement:11), coverage-matrix → 0 hits, D7 boilerplate-duplication class with no gate/recording-double/red-test carve-out — but this identical root cause (binder file's jsonSlug/range left un-migrated onto the proto-named-harness import) is already filed in this same wave as quality/intake/qw20260918092852-d7-01-jsonslug-range-not-migrated-proto-named-binder.md, triaged confirmed and already naming this file for fold-in; the candidate's stated `grep -rl "function range(line" quality/...` → "0 hits" does not reproduce (it hits d7-01 and resolved PTQ-0671), so this adds nothing beyond d7-01 — fold into that PTQ at acceptance (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918092852-d7-10-single-child-drive-orchestration-duplicated.md | false-positive | verdict: false-positive — independently re-verified: the excerpts reproduce at the cited lines and the two `finally` blocks are byte-identical, but the filing's stated exact search (`grep -n "await reapSubagentChildren(\[{ kill: () => child.kill" tests/*.ts` → "exactly these two files, one hit each") does NOT reproduce — it returns 10 hits in 10 files (b0337-theta-enum-identity-invoke:700, b0342-forwarded-enum-subagent-chain:312, inbound-boundary-theta-callable:347, inbound-union-arm-dispatch:1438, invoke-prompt-cell-enum-return:551, subagent-child-real-spawn:142, subagent-root-binder-model-exempt:416, subagent-theta-roots-forwarding:352 plus the two cited), and the same `expect(launch.ok…)`/`childExit`/`driveWatchedSubagentChild`/`killedByWatchdog` guard shape is present in ≥ 6 of them, so `sites: 2` and the two-file root cause misdescribe the pattern by ≥ 3×; further, the "residue left behind after PTQ-0583's fix" is that fix's documented seam, not an oversight — tests/helpers/real-subagent-spawn.ts:1-2 (landed b2555372) states "Fixtures, assertions, control-plane overrides, and watchdog bounds stay with each caller", which is exactly the per-scenario guard messages and 90_000 bound the candidate proposes lifting; mktemp `diff` of :149-208 vs :267-328 shows 36 differing lines (fixture writes, slug literals, both watchdog messages, four comment wraps), and what remains identical is one-line calls into the helper (`realExecutableHost()`, `childExit(child)`, `reapSubagentChildren(...)`) plus `if (!launch.ok) return` / `if (!result.ok) return` early-exits that cannot move into a helper without changing the test's control flow — a fresh, correctly-counted filing across all ≥ 6 sites arguing against the helper header's stated seam would be a different candidate; coverage-matrix (0 hits), docs/bugs 0067/0180 (0 hits) and quality/ dedupe (0 prior hits on the new helper's exports) searches reproduce (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918092852-d7-11-refusal-message-composer-duplicated.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce at tests/subagent-invoke-nonfinite-return-refusal.test.ts:134,160-182 and tests/subagent-return-depth-refusal.test.ts:349,359-385, both copies are live (3 / 2 call sites), no tests/helpers module exports the composer, docs/bugs and coverage-matrix greps → 0, D7 boilerplate-duplication inside tests/ with no gate/recording-double carve-out — but this identical root cause (the `<value>`-split `cut`/`head`/`tail`/`subject`/`location` composer retyped per file) is already tracked by open, confirmed quality/issues/PTQ-0868-registry-pointer-message-composer-triplicated.md, whose triage note explicitly names tests/subagent-return-depth-refusal.test.ts's `nonRepresentableMessage` as the byte-identical fourth copy to fold into its location list at acceptance (sites: 4); two of the candidate's stated searches do not reproduce — `grep -rn "template.indexOf(VALUE_PLACEHOLDER)" tests/*.ts` returns FOUR files (also subagent-envelope-nonfinite-ok-refusal.test.ts:268 and subagent-envelope-result-carriage.test.ts:373), not two, and the case-insensitive `grep -rli "expectedrefusalmessage\\|nonrepresentablemessage" quality/issues …` hits PTQ-0868 rather than 0 (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918075903-d7-06-depth-5-6-value-fixture-duplicated.md | human-reject | human-reject (2026-09-18): the literal is the spec's normative depth vector (schema-subset.md:37) cited to its clause at both sites and locally witnessed by each file's own jsonDepth/breach assertions - a mistyped copy fails where it lives. The same vector deliberately recurs under local names in 5 more files. A shared fixture would couple 7 witness files for zero drift risk; the local duplication is the point. |
| 2026-09-18 | qw20260918092852-d7-09-expectdeclared-duplicated-load-row-harness.md | human-reject | human-reject (2026-09-18): the fold is not mechanical - the harness projection filters schema\|\|enum while this file feeds a schema-only projection, and fixture A5 carries an enum in its prelude, so importing the harness helper changes a witness's assertion set. Re-file only if load-row-harness gains a projection-parameterised variant. |
| 2026-09-18 | qw20260918131151-d7-02-config-builder-duplicated-tool-loop-pair.md | false-positive | verdict: false-positive — both excerpts reproduce verbatim at tests/query-tool-loop.test.ts:40-48 and tests/query-tool-loop-noncompliance.test.ts:57-65 and both copies are live (10 / 2 `config(n)` call sites), but the two-file root cause does not reproduce: `grep -rn "function config(maxRounds" tests/` returns SIX files (also b0308-snk-h-null-last-tool:121, b0327-untyped-exhaustion-raw-response:47, b0355-repair-terminal-masked-followup-slot:85, b0399-boundary-event-attempts-tokens-masked:302), so `sites: 2` undercounts the pattern by 3× (the candidate's "no other in-scope file" search was scoped to the wave pair, not the repo); and what is identical across the copies is only the `QueryToolLoopConfig`-typed field shape plus a `maxRounds` passthrough — the fixture values vary in 2–5 of 5 fields across the six (b0355/b0399 differ in querySite, invocationId AND occurredAt), so the proposed shared builder collapses to `{ maxRounds, ...fixture }` (cf. the existing `forcedRespondConfig` at tests/helpers/typed-query-harness.ts:34-38, which is exactly that with `0`), each file would still keep its local fixture constant and wrapper, and a mistyped copy already fails typecheck where it lives — incidental type-enforced similarity with no drift surface; docs/bugs (0), coverage-matrix (0) and quality dedupe searches reproduce (PTQ-0905 / PTQ-0855 / intake d7-01 track the b0399 driver quartet and `liveSignal`, not `config()`); a correctly-counted six-site filing arguing a non-trivial shared payload would be a different candidate (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918131151-d7-02-max-2-control-cell-triplicated.md | false-positive | verdict: false-positive — the three excerpts reproduce at the cited lines and a mktemp diff confirms the b0324/b0325 bodies are identical (trailing comma only) with b0326 differing in the code constant and two messages, but the filing's stated docs/bugs search does NOT reproduce: `grep -n "max 2\\|CONTROL"` over docs/bugs/0324/0325/0326 returns 10 hits, not 0, and they are material — docs/bugs/0326-max-zero-negative-silently-raised-to-one.md:189 lists "C `max 2`" by name in that bug's 8-cell witness roster, and docs/bugs/0324-max-non-integer-silently-unthrottled.md:46,:119 record "control `max 2` → peak 2" as the reproduction's measured control, refuting the claim that no bug doc names this control cell or its fixture; each copy is therefore its bug's own documented, locally-witnessed control (the shape human-rejected for qw20260918075903-d7-06: a shared fixture couples witness files for zero drift risk), not free boilerplate; also `sites: 3` is understated — tests/b0438-par-max-fractional-width-silent-floor.test.ts:166-193 (CONTROL A4) carries the same fixture literal and the identical first assertion message uncited; coverage-matrix grep → 0 reproduces, all 4 files green (25/25), and the shape is disjoint from resolved PTQ-0929 (drive sequence, now `driveGated`) and PTQ-0483/0629/0692 (the ParForHost class) (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918155535-d7-01-errdetail-triplicated.md | duplicate | verdict: duplicate — all three excerpts reproduce byte-identical at :287-290 / :334-337 / :443-446, `grep -rn "function errDetail" tests/` → exactly 3 declarations, 0 hits in tests/helpers, src, extensions, tools, docs/bugs, coverage-matrix, and every copy is live (5 / 8 / 12 uses), but the root cause is already tracked: in each file `errDetail` is a contiguous member of the same copy-pasted attach-cell scaffold block — `driveTypedInvoke` (same name in all three: :226 / :269 / :377) carrying both verbatim PIC-17 and "Bug 0293" comments, then a byte-identical `boundaryResult` with identical doc comment, then `errDetail` immediately after — which quality/issues/PTQ-0687-02-prompt-attach-driver-harness-triplicated.md (open, confirmed) tracks under its driver + `boundaryResult` + Result-renderer (`promptOutcome`) root cause, and whose triage note already names tests/invoke-depth-wire-form-metric.test.ts:281-340 and tests/invoke-return-enum-carrier-projection.test.ts:269-330 as uncounted copies for the fixer to fold in; the candidate's dedupe grep searched only for the identifier `errDetail`, which PTQ-0687 does not name, so it missed the tracking issue — a separate ticket for a one-line renderer inside that already-tracked block would fragment one fold into two; PTQ-0687's fixer should carry `errDetail` into the shared helper alongside `boundaryResult` (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918155535-d7-01-positionrows-twelve-position-table-duplicated.md | false-positive | verdict: false-positive — both excerpts reproduce at tests/inline-object-stranded-entry-refusal.test.ts:404-421 and tests/inline-object-empty-entry-slot-refusal.test.ts:509-526, a mktemp diff/md5 confirms byte identity, both copies are live (2 / 1 call sites, 42/42 green) and no tests/helpers/ export exists, but the anchor is refuted: the "why" asserts the table "was not independently derived from each bug's own reproduction section", yet each bug doc carries its OWN §Reproduction (b) table with rows b1–b12 whose `position` column matches the test rows one-for-one (docs/bugs/0256-generic-argument-stranded-entry-registers-permissive.md:217-228; docs/bugs/0257-empty-inline-object-entry-slot-silently-tolerated.md:258-269), each doc's acceptance criteria pin that roster ("Every cell of … (b) rows b1–b12" 0256:325; "holds at all twelve positions of §(b)" 0257:385), and both docstrings cite "§Reproduction (b)" at the site — so each copy is its bug's own documented, locally-witnessed cell roster (a mistyped row fails against its own bug doc where it lives), the shape human-rejected for qw20260918075903-d7-06 ("a shared fixture would couple witness files for zero drift risk; the local duplication is the point"), with the same vector recurring under local row sets in 9 more sibling files exactly because each bug doc pins its own (8/10/12 rows); additionally `sites: 2` is understated — tests/inline-object-keyless-entry-refusal.test.ts:426-450 is a renamed-only third copy (`interior` for `type`, same 12 labels/templates, matching docs/bugs/0244:232-243) the filing knowingly excluded — and `paramsSrc` is a per-file local wrapper (stranded:249, empty-slot:325), not an imported e2e-s1 alias as stated; coverage-matrix (0) and quality dedupe (0, PTQ-0596 is Cell/expectGroup) reproduce (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918155535-d7-01-scripted-complete-mock-scaffold-duplicated.md | duplicate | verdict: duplicate — all five excerpts reproduce at the cited lines (repair :75/:217/:245/:305/:369-376 ↔ live :77/:336/:366/:383/:443-450) and both copies are live, but the root cause is already tracked by quality/issues/PTQ-0736-02-off-session-scripted-complete-harness-duplicated.md (open, confirmed): it lists BOTH in-scope files among its 8 sites (typed-repair-two-phase :77-109 and typed-two-phase-live :65-97, small line drift since) for the identical `vi.hoisted` `{queue,calls}` holder, the `vi.mock("@earendil-works/pi-ai/compat")` sticky-last/throw-on-empty `complete()` body and the `assistantReply` builder, and names the same missing `tests/helpers/` home beside scripted-live-session-harness.ts as the fix direction; the `beforeEach`/`afterEach` reset of the holder is inseparable from that scaffold (`grep -l 'scripted.queue = \[\];' tests/*.test.ts` → 7 files, all PTQ-0736 sites); the candidate's prior-finding check searched only the resolved PTQ-0519/PTQ-0728 and missed the open row. The only residual not in PTQ-0736 — `RespondFixture`/`respondFixture` (+ `qry15Body`) — is ALSO declared in tests/typed-query-provider-gate.test.ts:251 (so `sites: 2` undercounts it) and is already inventoried by open PTQ-0729's declaration grep/direction as part of the gate↔live fold; a fixer working PTQ-0736/PTQ-0729 should carry it along, or a correctly-counted three-file `respondFixture`/`qry15Body` filing would be a separate candidate (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918155535-d7-01-seededentry-fixture-reimplements-fakeentry.md | duplicate | verdict: duplicate — both excerpts reproduce verbatim at tests/e2e-s6-session-shutdown-real-teardown.test.ts:35-42 and tests/helpers/execution-status-progress.ts:51-59, `seededEntry` has exactly two live call sites (:58-59), the file imports nothing from the helper, and coverage-matrix → 0 reproduces, but the root cause is already tracked: quality/issues/PTQ-0965-activeinvocationentry-fixture-reimplemented-b0484.md (open, confirmed, same lens) is "a local ActiveInvocationEntry builder reimplements fakeEntry" and its triage note explicitly names `seededEntry` at tests/e2e-s6-session-shutdown-real-teardown.test.ts as an uncounted site "which should be folded into the location list at acceptance" alongside `makeEntry` (tests/active-invocation-registry.test.ts:22-30) and the inline literal in tests/double-session-start-supersession.test.ts:161-167 — the fix for PTQ-0965 is the fix for this file; one stated search does not reproduce (`grep -rln "seededEntry\\|thetaAbort: new AbortController" docs/bugs/` → docs/bugs/0295-child-internal-cancel-wrap-arm-unreachable.md, not 0, though that doc is unrelated to this test and immaterial) (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918155535-d7-02-drivetheta-checklines-harness-duplicated-sibling-bugs.md | false-positive | verdict: false-positive — all ten excerpts reproduce verbatim at the cited lines, both files are live and green (vitest 2 files / 174 passed), both under tests/, not gate files, no recording double, docs/bugs signature grep → 0 and coverage-matrix → 0 reproduce; but the filing's two-file root cause does not reproduce for two of its five functions and bundles two distinct root causes: (1) the throw-based `registryMessageOf` is not a pair — awk-extracted bodies hash identical (a86325c5) across FOUR params-default files (trailing-residue, unary-minus, type-compat, unresolvable-enum-variant) and the exact throw text recurs in 15 tests/*.test.ts, and `registryRowOf` is declared in 11 files (pair-identical f73cbe12, 9 other wordings), so `sites: 10` undercounts the reader halves ~5×; more importantly that idiom carries a recorded fix decision — tests/helpers/registry-oracle.ts:7-10 (PTQ-0215) states each file's `registryMessageOf`/`registryRowOf`-shaped reader "stays local" — and PTQ-0951's human ratification (2026-09-18) explicitly refuted the repo-wide "byte-identical" claim and narrowed the throw-based reader to two inline-object files, so re-filing the other copies as a pair-local scaffold sidesteps that ruling rather than accounting for the 4/15/11-site pattern; (2) `notLiteralMessage` is 3 files, not 2 (also tests/params-default-unresolvable-enum-variant.test.ts:364); only `driveTheta`/`DriveRow`/`checkLines` are genuinely pair-local (grep → exactly these 2 files), and even those close over per-file `DRIVE_BODY` (`schema S { a: integer }` vs `schema Count = number`) and `LITERAL_SITE` (`bug0175.theta` vs `bug0166.theta`), so the shared payload is a parameterised frontmatter join plus a 3-line map — a correctly-scoped filing on that pair-local trio alone, distinct from the registry-reader idiom, would be a different candidate; not a duplicate (PTQ-0661/0660/0803 cover disjoint helpers in these files) (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918155535-d7-03-ajv-capturing-builder-not-migrated-nested-lowering.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce verbatim (harness :125-131 `export function capturingAjv()` built on proto-named-harness.ts:8-11 `jsonSlug`; test :582-592 `function ajv()` with the field-identical inlined `slugOf`), the local copy is live (7 further `ajv()` calls at :973/:1008/:1174/:1244/:1300/:1679/:1712, `emitted` read at :973), the file imports neither helper module, `capturingAjv` is imported only by the two files PTQ-0425 migrated, and the capturing-shape `function ajv(): { readonly validator…; readonly emitted…}` search still returns the same 10 unmigrated tests/*.test.ts files — but the candidate's overlap grep does NOT reproduce as stated: `grep -rl "function ajv()\\|capturingAjv" quality/issues/*.md quality/resolved/*.md` returns 19 files, not just PTQ-0425, and among them quality/issues/PTQ-0824-params-inline-object-ajv-double-reimplements-capturingajv.md (status: open, confirmed) already tracks this identical root cause (capturingAjv exported, sibling restatements never migrated); this very file's copy was already filed as qw20260918050411-d7-04-ajv-capturing-double-duplicated-nested-lowering.md and ruled duplicate of that canonical (TRIAGE_LOG:122) with the instruction to add tests/inline-object-nested-lowering.test.ts to PTQ-0824's location list — a per-file re-filing does not change that; fold this site into PTQ-0824 rather than mint a new row (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918155535-d7-03-scriptedhost-deps-triplicated.md | duplicate | verdict: duplicate — same root cause as quality/issues/PTQ-0885-scriptedhost-deps-harness-triplicated.md (open, confirmed): the `class ScriptedHost implements StatementEvalHost` + `function deps(host)` pair triplicated across the same three files b0307/b0351/b0387; the observation itself still reproduces (grep → exactly b0307:89/122, b0351:160/193, b0387:124/157; mktemp sed-range diff of b0307:89-131 vs b0387:124-166 is empty; tests/helpers/ exports no `ScriptedHost`, only par-for-harness mentions `StatementEvalHost`) — the candidate's lower line numbers reflect drift since PTQ-0885 was filed (b0307:133→89, b0387:175→124), not a distinct finding; its only new angle (naming invoke-seam-scaffold.ts as the home) is a fix-direction detail PTQ-0885's fixer already owns (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-01-inline-field-key-msg-reimplements-registrymessageof.md | duplicate | verdict: duplicate — same root cause as quality/issues/PTQ-0808-generic-argument-msg-reimplements-registrymessageof.md (open, confirmed): the observation itself reproduces (imports at :1/:4, `msg` at :193-208 live via :269, `registryMessageOf` at load-row-harness.ts:59-79; mktemp sed-range diff of the two bodies after normalising only `REGISTRY`→`registry` and the path literal is empty), but PTQ-0808's triage note already names this exact site — "the byte-identical sibling msg at tests/generic-argument-inline-field-key-rules.test.ts:193-207 is uncited — fold it into the location list at fix time" — so the candidate's "not covered by that finding's sites: 1" claim is refuted by the row's own record; the fixer migrating 0808 owns both sibling generic-argument files, and a per-file re-filing does not mint a new root cause (same disposition as the ajv/PTQ-0824 fold ruling): add this file to PTQ-0808's location list rather than open a new row (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-01-systemnotecontents-reimplements-live-transcript-collectsystemnotes.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce at the cited lines and the two bodies compare identical after name substitution and whitespace/brace normalisation (mktemp cmp), the local copy is live (:315), the file imports nothing from ../helpers/live-transcript, and `collectSystemNotes` is exported there and imported by 30+ tests/live cells — but the root cause (per-file `systemNoteContents` redeclaring the harness's `collectSystemNotes`) is the very one quality/issues/PTQ-0760-systemnotecontents-note-reader-quintupled.md (open, confirmed) still tracks and PTQ-0515/0516/0769 already fixed for other shards; five prior same-cause shard re-filings (TRIAGE_LOG:83/90/104/105/107) were each ruled duplicate with the instruction to consolidate, and the candidate itself concedes it is "the same broad root cause"; two non-refuting inaccuracies noted — the overlap grep does not reproduce (hits are PTQ-0817 and resolved PTQ-0618, not PTQ-0516) and `function collectSystemNotes` greps to 4 declarations in tests/ (live-transcript, recording-system-note-channel, probe-harness, double-session-start), not 1; fold this file (and the other ~19 remaining `function systemNoteContents` sites) into PTQ-0760's location list rather than mint a new row (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-01-systemnotecontents-reimplements-now-exported-collectsystemnotes.md | duplicate | verdict: duplicate — independently re-verified: all three excerpts reproduce at the cited lines (test :948-982 `systemNoteContents` incl. PIC-72 comment; harness.ts:40-41 import+re-export; recording-system-note-channel.ts:70-121 `collectSystemNoteEntries`/`collectSystemNotes`), the local copy is live (`grep -n systemNoteContents` → 1 decl + 30+ `systemNoteContents(handle.sessionManager.getEntries())` call sites), the file imports six names from `./harness` but not `collectSystemNotes`, and the two readers are output-equivalent (same per-entry text/text-part/`data.content` extraction in entry order via flatMap — "byte-identical" in the title overstates: the canonical is now a two-function shape, but that is immaterial); the stale "unexported" doc comment is real (export landed in e3546327) — BUT the root cause (per-file `systemNoteContents` restatements of the harness's `collectSystemNotes` across tests/live; `grep -rn "function systemNoteContents" tests/` → 20 files still) is already tracked by open, confirmed quality/issues/PTQ-0760-systemnotecontents-note-reader-quintupled.md (all five of whose sites also still carry their copies), whose own triage note directs consolidation of every such site under one row; this very site was previously filed as qw20260917154546-d7-99-02 and ruled duplicate (TRIAGE_LOG:107) on the premise that PTQ-0479's export fix would migrate it — that migration did not happen (PTQ-0479/0515/0516 fixed only their own location lists), so fold tests/live/live-production-acceptance.test.ts:948-982 into PTQ-0760's location list rather than mint a new row; not a distinct root cause: the stale comment vanishes with the migration (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-01-theta-body-fixture-redeclared-b0463.md | false-positive | verdict: false-positive — the excerpts reproduce (helper :35 `export const THETA_BODY = "mode: prompt\n---\n"`; b0463 :49 local, consumed :132-133, no discovery-scratch-harness import) but the filing's stated search does NOT: `grep -lF 'const THETA_BODY = "mode: prompt\n---\n";' tests/*.test.ts` returns 9 files (b0459, b0463, discovery-cli-entry-override-prefix, discovery-glob-universe-enumeration-failure, discovery-root-enumeration-failure, discovery-symlinked-root-classification, discovery-tree-walk-lstat-failure, host-config-dir, settings-glob-disc5-matcher), not "exactly one local redeclaration", and the helper has 4 importers (b0363, b0364, b0440, b0461), not 2 — so "b0463 adds a fourth independent copy" and `sites: 1` undercount the identical-literal family ~9×, and the direction as filed (import in b0463 alone) would leave 8 byte-identical siblings in place; not a duplicate (no open PTQ tracks THETA_BODY; resolved PTQ-0255/PTQ-0899 covered b0363/b0364 and b0440/b0461 only) — a correctly-scoped filing citing all 9 redeclaring files as one root cause would be a different candidate (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-02-msg-wrapper-duplicated-registrymessageof.md | false-positive | verdict: false-positive — the 27-hit grep and the three excerpts reproduce at the cited lines, but the finding re-files the ratified residue of prior fixes as a fresh smell: `git blame` puts 25 of the 27 `msg` one-liners in quality fix commits (cc0a8fe7 qw20260917204232 for most, 8bad24ba / 2594cd44 for b0046 / b0272) that closed PTQ-0495/0501/0502/0508/0539/0567/0569/0616/0808…, where PTQ-0616's direction explicitly left "whether the local `msg` wrapper name is kept as a one-line delegator" to the fix stage and PTQ-0501's triage recorded that hoisting `msg()` into the helper "remains a fixer/human call" under PTQ-0215's ratified registry-oracle header ("readers … stay local, using the shared registry read") — the fixer chose the delegator, the tickets are status: fixed, and re-filing that shape sidesteps the ruling (same basis as this wave's registryRowOf rejection); the reviewer's own REVIEW_LOG:434 entry treats the identical `expectGroup` one-liner residue in the same file (PTQ-0596) as "that fix's accepted residue, not a fresh smell"; the bound arguments are not boilerplate but per-file data the load-row-harness header says is "threaded through explicitly rather than assumed" (22 files bind a parse-shard `readRegistry(["parse"])`/`PARSE_REGISTRY`, 5 bind the four-shard `REGISTRY` from registry-oracle, across 5 distinct path strings), leaving only a signature and a closing brace as shared text; and the "exactly this one-line-body shape" claim is false for b0429:70-77 / b0430:80-87, whose `msg` carries an 8-line body with a `REGISTRY.find`+`expect(...).toBeDefined()` preamble ahead of the delegation (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-02-plant-boot-dispose-scaffold-nonuplicated.md | false-positive | verdict: false-positive — all 18 excerpts reproduce at the cited lines and the docs/bugs (0) / coverage-matrix (0) / quality-overlap (only sibling d7-09) searches re-run as stated, but the five lines are not a hand-rolled scaffold: they are direct calls into the harness's two exported primitives (`plantThetaWorkspace` → `bootShippedExtension({ workspace, provider })` → `handle.dispose()` / `workspace.dispose()`), every byte of lifecycle behaviour already lives in tests/live/harness.ts and nothing is reimplemented; the shape is the suite-wide convention, not nine copies — `grep -l 'const workspace = plantThetaWorkspace' tests/live/*.test.ts` → 93 files (all 93 use the identical `({ workspace, provider })` form), 142 plant→boot adjacent pairs and 189 `await handle.dispose();`→`workspace.dispose();` pairs repo-wide, so `sites: 9` understates the footprint ~15× and a nine-file fix would split one suite into two idioms; and the plant/boot split is load-bearing — 5 offender/control cells boot two handles against two workspaces per test, 58 plant sites have fixture/comment material between plant and boot, and live-session-control disposes in `afterAll` — so a `withLiveWorkspace`-style callback wrapper is a new abstraction design rather than a mechanical dedupe; the only cross-site knowledge (handle-before-workspace order) is a taste anchor with no drift shown at any site (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-03-console-error-spy-gate-triplicated.md | duplicate | verdict: duplicate — independently re-verified: all three excerpts reproduce at the cited lines (mktemp sed-range diff: empty-object-discriminator:118-139 and fn-call-arity:119-140 byte-identical; escaped-quote:133-153 differs only at the string-literal wrap on lines 14-16, same runtime message), `let consoleErrorSpy` still greps to exactly 25 tests/live/*.test.ts files, none of the three files imports `tests/helpers/theta-stderr-gate`, no carve-out binds (no *gate* file; 0 coverage-matrix hits; docs/bugs/0129/0131/0229 cite these files only as witnesses with no mention of the spy block; docs/bugs/0030 pins the gate at live-production-acceptance.test.ts:233-252, not here) — but the identical root cause (the bug-0030 console.error spy scaffold redeclared per live cell instead of the canonical `assertThetaStderrCleanForEach` that PTQ-0546's fix commit e3546327 created in tests/helpers/theta-stderr-gate.ts, imported by only 4 files) is already tracked by same-wave sibling quality/intake/qw20260918202006-d7-01-console-error-spy-gate-triplicated-live-cells.md, triaged confirmed with a consolidation note naming this file; PTQ-0546 itself is resolved with a disjoint trio and cannot absorb these sites. Acceptance MUST actually fold these three locations into d7-01's row this time — the prior wave's identical fold instruction (qw20260917154546-d7-03, which cited these same three files, TRIAGE_LOG:92) was never applied, so PTQ-0546 fixed only its own three sites and left these unmigrated; the candidate's suggested "new helper" home is also stale since the export already exists — the fix is a migration, not an extraction (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-05-exp-render-renderall-trio-duplicated-inline-object-pair.md | duplicate | verdict: duplicate — observation independently re-verified (both excerpt pairs match at stray-close:227-232/279-285 and type-source-capture:195-200/226-232; mktemp sed-range diffs of the `Exp` block and the `render`/`renderAll` block are both empty; `grep -rl "^function renderAll" tests/*.test.ts` → exactly the 13 named files and `^interface Exp\b` → 13; no tests/helpers/* exports `Exp`/`renderAll`, only e2e-s1.ts:311's `expectGroup` parameter; both files pass their local `renderAll` into `expectGroupShared` at :332/:648) but the root cause is one 13-file pattern, not a per-pair one: the four copies cited here and in same-wave sibling quality/intake/qw20260918202006-d7-04-exp-render-renderall-trio-duplicated.md (generic-argument pair, itself verified to reproduce at :249-253/:313-319 and :222-226/:268-274) are byte-identical to each other, and per the PTQ-0596 precedent (its d7-02 sibling deferred to the carrier) and the PTQ-0824 fold ruling, one carrier row per root cause — d7-04 is the lower-numbered carrier; fold this file pair's four locations into d7-04's location list rather than mint a new row; fixer notes for the carrier: e2e-s1.ts:150 already exports an unrelated `render(doc: ThetaDocument)` so the shared trio cannot land there under that name, and each local `render` closes over the per-file `msg()` tracked by PTQ-0811/0827/0997, so a shared renderer must take the message builder as a parameter or follow those migrations (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-10-b0259-reimplements-live-diagnostic-oracle-fixtures.md | duplicate | verdict: duplicate — same root cause as same-wave sibling quality/intake/qw20260918202006-d7-02-minimal-prompttheta-casecode-precondition-duplicated.md (already triaged confirmed): live cells redeclaring the minimal `promptTheta` + `CASE_CODE` + `promptTheta(["let P = 1", "@\`hi\`"])` precondition fixture instead of importing `tests/helpers/live-diagnostic-oracle.ts`; the observation itself reproduces (b0259live:108 `CASE_CODE`, :111-113 minimal `promptTheta` — mktemp sed-extract + diff against fn-param-list-unclosed:89-91 is empty, :156-160 inlined fixture; file imports only `./harness`; sibling import sits at b0262live:102, line drift only; docs/bugs 0259/0139 and coverage-matrix greps → 0 hits), and `grep -rl 'const CASE_CODE = "theta/parse/binding-case-mismatch"' tests/live/` returns exactly the six unswept files d7-02's triage note enumerated (b0046live, b0259live, fn-param-annotation-optional, fn-param-list-unclosed, fn-param-not-identifier, schema-body-unclosed-at-eof), b0259live included by name — a per-file re-filing of one of those six does not mint a second row; not PTQ-0615 (fixed, disjoint `"description: d"` b02XXlive set) nor PTQ-0988 (tool-call-dispatch `(thetaBody, tools?)` variant); fold this site into whichever row d7-02 mints (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-12-emptyschemabodyfragment-reimplements-registryfragment.md | false-positive | verdict: false-positive — all four excerpts reproduce (second sibling drifted to :177-178 and its EMPTY_SCHEMA_BODY_CODE hit at :179 is `{ X: "S" }`, not `{}`), both searches reproduce, and a `registryFragment(EMPTY_SCHEMA_BODY_CODE, { X: "{}" })` swap would be result-identical (the row's only placeholder is `<X>`); but the candidate's claim that nothing documents a rationale for keeping this reader local is refuted by tests/helpers/registry-oracle.ts:8-10 — "Readers whose assertion style and wording vary per file stay local, using the shared registry read" — a header rewritten in commit e3546327, the SAME commit that created `registryFragment` and fixed PTQ-0500 in this very file (migrating the `REGISTRY` line three lines above `emptySchemaBodyFragment` to `readRegistry(["parse"])` while deliberately leaving the cell-worded reader in place); `emptySchemaBodyFragment` matches that carve-out exactly (shared read, cell-specific failure wording, `[a-zA-Z]` regex) and is one of ≥10 tests/live cells retaining a per-file `*Fragment()` reader post-fix, so this is ratified fix scope, not an overlooked remainder (same ruling as TRIAGE_LOG:84 on the identical header decision); not a duplicate — PTQ-0500 covered only this file's read, PTQ-0562/0602 covered byte-identical `registryFragment` copies in other files (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-13-unterminatedtemplatefragment-reimplements-registryfragment.md | false-positive | verdict: false-positive — the excerpts reproduce verbatim (test :90-104, registry-oracle.ts :88/:121-135; `registryFragment` grep → 0 in the file; registry row :84 carries no `<placeholder>`; bugs/coverage-matrix greps → 0), but the "why" is refuted by a recorded fix decision the candidate does not account for: PTQ-0602 (fixed) already cited THIS file at :91-98 under a title naming the duplicated `registryFragment` reader, and its fix commit e3546327 (human-authored, 2026-09-18) edited this very file — swapped `REGISTRY` to `readRegistry(["parse"])`, hoisted only the byte-identical `registryFragment` copies (now imported by 8 live cells), deliberately left `unterminatedTemplateFragment` local — and wrote the rule into tests/helpers/registry-oracle.ts:7-10: "centralises … the identical live-cell fragment assertions. Readers whose assertion style and wording vary per file stay local, using the shared registry read"; this reader's DIAG-2 wording ("precondition unmet: … the code this cell counts is unregistered") differs from `registryFragment`'s ("… this cell asserts is not registered"), so it is exactly the class that ruling keeps local, and swapping it changes the failure text; further, `sites: 1` undercounts a 16-reader/9-file idiom (awk-extracted `^function *Fragment()` bodies across tests/live, 6 distinct DIAG-2 wordings, incl. a near-identical twin `emptyTemplateFragment` in empty-template-warning-registration-live-cell.test.ts:87) — a filing that challenges the header's "stay local" decision must cite that whole pattern, which would be a different candidate, not this per-file re-filing (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-14-systemnotecontents-reimplements-exported-collectsystemnotes.md | duplicate | verdict: duplicate — independently re-verified: the local `systemNoteContents` at tests/live/inline-object-wire-name-rename-live-cell.test.ts:93-118 (decl at :93, doc comment :88-92 — one-line drift from the cited 89) is live (called at :245 on `handle.sessionManager.getEntries()`), its PIC-72 comment and all branch predicates diff identical to `collectSystemNoteEntries` in tests/helpers/recording-system-note-channel.ts (which `collectSystemNotes` :119-121 flatMaps to the same string list), harness.ts:40-41 does import+re-export `collectSystemNotes`, and the file imports four other symbols from ./harness but never `collectSystemNotes` — but the root cause (per-file restatement of the harness note-channel reader, now 20 remaining `function systemNoteContents` copies under tests/live per `grep -rln`) is already tracked by the open, confirmed quality/issues/PTQ-0760-systemnotecontents-note-reader-quintupled.md (its own sites discovery-cli-override-prefix / discovery-entry-lstat-failure are likewise still unmigrated), alongside open PTQ-0761 and PTQ-0774 on disjoint file sets, all carrying the triage instruction to consolidate under one export-collectSystemNotes row; this very file's copy was already ruled duplicate once (TRIAGE_LOG:90, qw20260917154546-d7-02) against the canonical that became PTQ-0479, whose fix exported the function but did not migrate this site — fold tests/live/inline-object-wire-name-rename-live-cell.test.ts into PTQ-0760's location list rather than mint a new row; the candidate's overlap check only consulted resolved PTQ-0515/0516 and missed the three open rows, and its docs/bugs grep claim of 0 hits does not reproduce (2 hits: 0048, 0287, both citing the harness reader as available, non-refuting) (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-17-console-error-spy-gate-duplicated-fn-param-inline-object.md | duplicate | verdict: duplicate — independently re-verified: both excerpts reproduce at the cited lines (mktemp sed-range diff of fn-param-sink-array-literal:137-156 vs inline-object-wire-name-rename:161-180 differs only at the string-literal wrap on lines 14-16, same runtime message), `let consoleErrorSpy: MockInstance` still greps to exactly 25 tests/live/*.test.ts files, both files import only `thetaOwnedStderrLines` from ./theta-stderr-prefixes and neither imports tests/helpers/theta-stderr-gate, no carve-out binds (no *gate* file; 0 coverage-matrix hits; bugs 0018/0030 both fixed and docs/bugs/0030 pins the spy gate at live-production-acceptance.test.ts, not here) — but the identical root cause (the bug-0030 console.error spy scaffold redeclared per live cell instead of the canonical `assertThetaStderrCleanForEach` that PTQ-0546's fix commit e3546327 created in tests/helpers/theta-stderr-gate.ts, imported by only the 3 PTQ-0546 files) is already tracked by same-wave sibling quality/intake/qw20260918202006-d7-01-console-error-spy-gate-triplicated-live-cells.md, triaged confirmed with a consolidation note that names this file (d7-17) for folding, exactly as d7-03 was ruled duplicate of it; PTQ-0546 itself is resolved with a disjoint trio and cannot absorb these sites. Acceptance must fold these two locations into d7-01's row; the candidate's suggested home (a new export from theta-stderr-prefixes.ts) is stale since the helper already exists — the fix is a migration, not an extraction (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918202006-d7-19-exp-render-renderall-trio-unterminated-literal-params.md | duplicate | verdict: duplicate — observation independently re-verified (both excerpts reproduce verbatim at :156-161 and :186-193; `grep -rl "^function renderAll" tests/*.test.ts` → exactly the 13 named files; awk-extracted `interface Exp` block hashes identical (9f6d5de5) in all 13; the file imports only `expectGroup as expectGroupShared`/`DiagnosticCell`/`parseDoc` from e2e-s1 and passes its local `renderAll` into it; no tests/helpers/* exports `Exp`/`renderAll` — e2e-s1.ts:311 only takes `renderAll` as a parameter and its :150 `render(doc: ThetaDocument)` is unrelated; docs/bugs/0232 + coverage-matrix greps → 0; not a gate file, not a recording double) but the root cause is the one 13-file pattern already carried by same-wave sibling quality/intake/qw20260918202006-d7-04-exp-render-renderall-trio-duplicated.md (ruled confirmed as the cohort carrier, with d7-05 already folded into it as duplicate under the PTQ-0596/PTQ-0824 one-carrier-per-root-cause precedent), and the candidate itself concedes this is "the same already-recognised class"; fold this file's two locations (:156-161, :186-193) into d7-04's location list rather than mint a new row; accounting correction for the carrier: the claim that "the other 12 each carry a byte-identical copy" is refuted for 3 of them — `render` hashes identical (42a63730) in 10/13, while inline-object-{empty-entry-slot,keyless-entry,stranded-entry}-refusal inline the fill loop into an 18-line `render` calling `registryMessageOf` directly (hash 9bbe8b2b) — immaterial to this file's own copy, which is in the identical-10 set (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918220713-d7-01-registryhint-cell-reader-duplicated.md | duplicate | verdict: duplicate — observation independently re-verified (all three excerpts reproduce at :131-161 / :126-157 / :1160-1204; `grep -rn "registryHint\\|HINT_CELL_INDEX" tests/ src/ tools/` → declarations in exactly these three files, none in tests/helpers/; tests/helpers/registry-oracle.ts has zero "hint" hits and RegistryRow carries no hint field; docs/bugs 0 hits, coverage-matrix 0 hits; no gate/recording-double carve-out; every copy live at :219/:374, :224/:521, :1337) but the identical root cause — the same three `registryHint`/`HINT_CELL_INDEX` markdown-cell readers in the same three files with the same registry-oracle home direction — is already tracked by open, confirmed quality/issues/PTQ-0829-registryhint-cell-parser-triplicated.md (wave qw20260918050411), which REVIEW_LOG:366/396/422 already cite as the carrier for this shape; the candidate's dedupe check consulted only quality/intake/ and missed the issues store; accounting note carried over from PTQ-0829: "byte-for-byte" overstates (error strings differ, third copy adds a link/backtick strip) — non-blocking, deltas are stated in the Observation; nothing to fold since PTQ-0829 already lists all three locations (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918220713-d7-02-theta-expectdiagnostics-trio-duplicated.md | duplicate | verdict: duplicate — observation independently re-verified (mktemp awk-extracted bodies md5-identical across all five files for `theta` (55119785) and `existing` (faa81bc6); `expectDiagnostics` identical (31c0610b) in four with pattern-field-literal's copy factored through `expectDiagnosticsOf` exactly as the filing states; `grep -rl "function expectDiagnostics" tests/*.test.ts` → the same 6 files with typeenv-prototype-names' diagLines/parse body correctly excluded; every copy is live (theta 2-10, existing 3-13, expectDiagnostics 4-42 calls per file); no tests/helpers module exports a body→ThetaDocument `theta`/`existing`/`expectDiagnostics` — the three helper `theta(...lines)` exports return strings — though `FM` is already canonical at tests/helpers/prompt-value-harness.ts:88; coverage-matrix 0 hits; not a gate/recording-double/failLoudly case; resolved PTQ-0645/PTQ-0646 cite disjoint scaffolds) but the root cause — the pattern-refusal parse/assertion layer (FM/theta/existing/expectDiagnostics plus patternRange/wrong-arm) redeclared per file over the already-shared load-row-harness primitives — is already carried by same-wave sibling quality/intake/qw20260918220713-d7-01-pattern-head-parse-scaffold-duplicated.md, ruled confirmed with a consolidation note that names this d7-02 for folding at the identical ranges (field-set :383-391, unresolved :222-230); under the one-carrier-per-root-cause precedent (PTQ-0596/PTQ-0824, d7-17→d7-01 this cycle) acceptance must fold this row's three additional files (reserved-keyword :124-131/:140-159, pattern-field-literal :280-287/:351-378, capitalised-bare :210-218) into d7-01's location list rather than mint a second PTQ for one extraction; accounting correction, non-refuting: the docs/bugs grep returns 6 files (0118, 0219, 0221, 0226, 0234, 0246), not only 0219/0234 — all witness citations, none licensing the duplication (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918220713-d7-03-diaglines-diagcodes-shadow-e2e-s1-exports.md | duplicate | verdict: duplicate — observation independently re-verified (local `diagLines`/`diagCodes` at tests/params-block-mapping-rhs-refusal.test.ts:301-309 byte-identical in body to the exports at tests/helpers/e2e-s1.ts:290-292/:330-332; the file's sole e2e-s1 import at :13 names only `parseDoc, fieldOf`; stated greps reproduce; not a gate/kin file, no recording double, coverage-matrix 0 hits; full docs/bugs sweep finds `diagCodes` in 0060/0100/0102 only as cell-expectation prose, non-refuting) but the identical root cause at this exact site is already carried by OPEN issue quality/issues/PTQ-0798-diaglines-diagcodes-reimplemented-three-in-scope-files.md (status open, verdict confirmed), whose location list names `tests/params-block-mapping-rhs-refusal.test.ts:311-318` — the same pair, drifted 10 lines upward by the later `93ed4e00` touch — and whose sibling d7-02 triage note in this wave already flagged the pair as PTQ-0798's; the candidate's overlap check grepped `quality/store`, a directory that does not exist (open rows live in `quality/issues`), so it consulted only resolved rows and missed the open carrier — nothing to fold, PTQ-0798 already lists this file (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918220713-d7-05-scripttoolcallenvelope-reimplements-scriptenvelope.md | duplicate | verdict: duplicate — observation independently re-verified (both excerpts reproduce verbatim at :780-806 and helpers :250-268; the file's `scripted` holder :228-231 is typed `replyFor: undefined \| ((context: unknown) => unknown)`, exactly the export's parameter shape; `toolCallReply`'s only caller is :804 so the pair collapses to one `scriptEnvelope(scripted, envelope, msg)` call; :1 imports `EM_DASH`/`ajvArgsNote` from the same module; docs/bugs grep for both names → 0, coverage-matrix grep → 0, not a gate file, a positive scripted reply not a MUST-NOT recording double; mktemp sed-range diff shows this :780-806 block is byte-IDENTICAL to binder-post-merge-ajv-enforcement:385-411 and params-default-enum-access-merge:427-453) but the root cause — the canonical `scriptEnvelope` export redeclared per binder-dispatch test — is already carried by same-wave sibling quality/intake/qw20260918220713-d7-01-scriptenvelope-reimplemented-three-files.md, ruled confirmed with a consolidation note that names this very candidate (d7-05, :780-806) and d7-10 for folding under the one-carrier-per-root-cause precedent (PTQ-0596/PTQ-0824), exactly as d7-03/d7-17 and d7-05/d7-19 were folded in the prior wave; the candidate's overlap check saw d7-01 but misread its "outside this shard's scope" remark as licence for a separate row — scope shards do not split one root cause into multiple tickets; fold tests/params-default-unresolvable-enum-variant.test.ts:780-806 into d7-01's location list rather than mint a new row (triage: claude-fable-5-1) |
| 2026-09-18 | qw20260918220713-d7-10-toolcallreply-scripttoolcallenvelope-reimplements-scriptenvelope.md | duplicate | verdict: duplicate — observation independently re-verified (all three excerpts reproduce verbatim: enum-access-merge :427-453, unresolvable-enum-variant :780-806, helpers :250-268; both files' `scripted` holders (:121-122 / :228-229) are typed `replyFor: undefined \| ((context: unknown) => unknown)`, exactly the export's parameter shape; `toolCallReply`'s only caller in each file is the `scriptToolCallEnvelope` body (:451 / :804) and `scriptToolCallEnvelope` has exactly one caller each (:510 / :890), so each pair collapses to one `scriptEnvelope(scripted, envelope, msg)` call; :1 of each file imports 2 names from the same module; the stated 6-file grep, docs/bugs → 0, coverage-matrix → 0 all reproduce; not a gate file, a positive scripted reply not a MUST-NOT recording double; canonical export is live via 6 importers; chronology caveat not refuting: both local copies (e73c1aca 2026-08-17, ce9c1090 2026-08-18) predate the export (6ebe890d 2026-09-18), so the accurate class is copy-paste double left unmigrated when the shared home landed) but the root cause — the canonical `scriptEnvelope` export redeclared per binder-dispatch test — is already carried by same-wave sibling quality/intake/qw20260918220713-d7-01-scriptenvelope-reimplemented-three-files.md, ruled confirmed with a consolidation note that names this very candidate (d7-10, :427-453) for folding under the one-carrier-per-root-cause precedent (PTQ-0596/PTQ-0824), exactly as d7-05 (:780-806, the second location cited here) was already ruled duplicate of it; the candidate's overlap check saw d7-01 but treated its "outside the reviewed set" remark as licence for a separate row, and its claim that the overlap grep returned "only" d7-01 does not reproduce (it also hits d7-05 and PTQ-1022, the latter a disjoint `ajv`/`rootDouble`/`producerWithCapture` trio); fold tests/params-default-enum-access-merge.test.ts:427-453 into d7-01's location list rather than mint a new row (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0731-separator-test-local-parse-reimplements-parsedoc.md | parked | skipped by the fixer in 2 waves — [PTQ-0779-02-statement-executor-invoke-seam-scaffold-not-migrated.md] PTQ-0779: Already migrated to shared span and no-op checkpoint imports; no changes needed. / PTQ-0787: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0788: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0789: Both files now import the shared model and entry type and delegate appends to the newly exported existing helper; no tests or assertions removed. Required verification command passed: TypeScript and all 11,585 tests across 689 files. \|\| [PTQ-0793-canonical-honesty-sorted-closure-duplicated.md] PTQ-0793: Reused assertKeysSorted; retained fragment equality, array-order, and whitespace checks. No tests deleted or renamed. / PTQ-0794: Reused inlineDefName; removed the inline hash computation and unused crypto import. No tests deleted or renamed. / PTQ-0795: Shared holder-bound accessors in scripted-live-session-harness.ts; preserved errors, casts, mocks, and assertions. No tests deleted or renamed. / PTQ-0796: Shared AST capture in e2e-s1.ts; preserved fixture paths and all three assertions. No tests deleted or renamed. Required gate passed for all four issues: TypeScript and 11,585 tests across 689 files; no production or quality-store changes. \|\| [PTQ-0797-ctor-proto-named-field-livesessiondouble-not-migrated.md] PTQ-0797: Reused shared model, entry type, and append helpers; imported parseDeps from its current home, e2e-s1.ts. Recording behavior and assertions unchanged; no tests deleted. / PTQ-0798: Replaced all five duplicated diagnostic helpers across three files with existing exports; assertions unchanged and no tests deleted. / PTQ-0799: Replaced both local diagLines helpers with imports and removed unused types; assertions unchanged and no tests deleted. / PTQ-0800: Imported shared diagLines, preserving the lines wrapper and all assertions; removed unused types; no tests deleted. Exact required verification gate passed for all four issues: TypeScript clean, 689 test files and 11,585 tests passed. No production or quality-store edits. \|\| [PTQ-0801-diaglines-reimplemented-inline-object-empty-pair.md] PTQ-0801: Replaced both local diagLines implementations with the existing shared helper; no tests removed. / PTQ-0804: Reused PARSE_REGISTRY and removed the duplicate registry interface/read; assertions unchanged and no tests removed. / PTQ-0806: Shared the diagnostic/fn harness across all five triaged sites, preserving the annotation-specific registration predicate; no tests renamed or removed. / PTQ-0808: Both triaged message readers now delegate to registryMessageOf, preserving registry scope and placeholder checks; no tests removed. Required gate passed for all fixes: tsc and 11,585 tests across 689 files. \|\| |
| 2026-09-19 | PTQ-0732-01-diaglines-shadows-e2e-s1-export-pair.md | parked | skipped by the fixer in 2 waves — [PTQ-0779-02-statement-executor-invoke-seam-scaffold-not-migrated.md] PTQ-0779: Already migrated to shared span and no-op checkpoint imports; no changes needed. / PTQ-0787: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0788: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0789: Both files now import the shared model and entry type and delegate appends to the newly exported existing helper; no tests or assertions removed. Required verification command passed: TypeScript and all 11,585 tests across 689 files. \|\| [PTQ-0793-canonical-honesty-sorted-closure-duplicated.md] PTQ-0793: Reused assertKeysSorted; retained fragment equality, array-order, and whitespace checks. No tests deleted or renamed. / PTQ-0794: Reused inlineDefName; removed the inline hash computation and unused crypto import. No tests deleted or renamed. / PTQ-0795: Shared holder-bound accessors in scripted-live-session-harness.ts; preserved errors, casts, mocks, and assertions. No tests deleted or renamed. / PTQ-0796: Shared AST capture in e2e-s1.ts; preserved fixture paths and all three assertions. No tests deleted or renamed. Required gate passed for all four issues: TypeScript and 11,585 tests across 689 files; no production or quality-store changes. \|\| [PTQ-0797-ctor-proto-named-field-livesessiondouble-not-migrated.md] PTQ-0797: Reused shared model, entry type, and append helpers; imported parseDeps from its current home, e2e-s1.ts. Recording behavior and assertions unchanged; no tests deleted. / PTQ-0798: Replaced all five duplicated diagnostic helpers across three files with existing exports; assertions unchanged and no tests deleted. / PTQ-0799: Replaced both local diagLines helpers with imports and removed unused types; assertions unchanged and no tests deleted. / PTQ-0800: Imported shared diagLines, preserving the lines wrapper and all assertions; removed unused types; no tests deleted. Exact required verification gate passed for all four issues: TypeScript clean, 689 test files and 11,585 tests passed. No production or quality-store edits. \|\| [PTQ-0801-diaglines-reimplemented-inline-object-empty-pair.md] PTQ-0801: Replaced both local diagLines implementations with the existing shared helper; no tests removed. / PTQ-0804: Reused PARSE_REGISTRY and removed the duplicate registry interface/read; assertions unchanged and no tests removed. / PTQ-0806: Shared the diagnostic/fn harness across all five triaged sites, preserving the annotation-specific registration predicate; no tests renamed or removed. / PTQ-0808: Both triaged message readers now delegate to registryMessageOf, preserving registry scope and placeholder checks; no tests removed. Required gate passed for all fixes: tsc and 11,585 tests across 689 files. \|\| |
| 2026-09-19 | PTQ-0733-01-diaglines-reimplemented-both-in-scope-files.md | parked | skipped by the fixer in 2 waves — [PTQ-0779-02-statement-executor-invoke-seam-scaffold-not-migrated.md] PTQ-0779: Already migrated to shared span and no-op checkpoint imports; no changes needed. / PTQ-0787: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0788: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0789: Both files now import the shared model and entry type and delegate appends to the newly exported existing helper; no tests or assertions removed. Required verification command passed: TypeScript and all 11,585 tests across 689 files. \|\| [PTQ-0793-canonical-honesty-sorted-closure-duplicated.md] PTQ-0793: Reused assertKeysSorted; retained fragment equality, array-order, and whitespace checks. No tests deleted or renamed. / PTQ-0794: Reused inlineDefName; removed the inline hash computation and unused crypto import. No tests deleted or renamed. / PTQ-0795: Shared holder-bound accessors in scripted-live-session-harness.ts; preserved errors, casts, mocks, and assertions. No tests deleted or renamed. / PTQ-0796: Shared AST capture in e2e-s1.ts; preserved fixture paths and all three assertions. No tests deleted or renamed. Required gate passed for all four issues: TypeScript and 11,585 tests across 689 files; no production or quality-store changes. \|\| [PTQ-0797-ctor-proto-named-field-livesessiondouble-not-migrated.md] PTQ-0797: Reused shared model, entry type, and append helpers; imported parseDeps from its current home, e2e-s1.ts. Recording behavior and assertions unchanged; no tests deleted. / PTQ-0798: Replaced all five duplicated diagnostic helpers across three files with existing exports; assertions unchanged and no tests deleted. / PTQ-0799: Replaced both local diagLines helpers with imports and removed unused types; assertions unchanged and no tests deleted. / PTQ-0800: Imported shared diagLines, preserving the lines wrapper and all assertions; removed unused types; no tests deleted. Exact required verification gate passed for all four issues: TypeScript clean, 689 test files and 11,585 tests passed. No production or quality-store edits. \|\| [PTQ-0801-diaglines-reimplemented-inline-object-empty-pair.md] PTQ-0801: Replaced both local diagLines implementations with the existing shared helper; no tests removed. / PTQ-0804: Reused PARSE_REGISTRY and removed the duplicate registry interface/read; assertions unchanged and no tests removed. / PTQ-0806: Shared the diagnostic/fn harness across all five triaged sites, preserving the annotation-specific registration predicate; no tests renamed or removed. / PTQ-0808: Both triaged message readers now delegate to registryMessageOf, preserving registry scope and placeholder checks; no tests removed. Required gate passed for all fixes: tsc and 11,585 tests across 689 files. \|\| |
| 2026-09-19 | PTQ-0737-02-retag-makedeps-reimplements-e2e-s1.md | parked | skipped by the fixer in 2 waves — [PTQ-0779-02-statement-executor-invoke-seam-scaffold-not-migrated.md] PTQ-0779: Already migrated to shared span and no-op checkpoint imports; no changes needed. / PTQ-0787: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0788: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0789: Both files now import the shared model and entry type and delegate appends to the newly exported existing helper; no tests or assertions removed. Required verification command passed: TypeScript and all 11,585 tests across 689 files. \|\| [PTQ-0793-canonical-honesty-sorted-closure-duplicated.md] PTQ-0793: Reused assertKeysSorted; retained fragment equality, array-order, and whitespace checks. No tests deleted or renamed. / PTQ-0794: Reused inlineDefName; removed the inline hash computation and unused crypto import. No tests deleted or renamed. / PTQ-0795: Shared holder-bound accessors in scripted-live-session-harness.ts; preserved errors, casts, mocks, and assertions. No tests deleted or renamed. / PTQ-0796: Shared AST capture in e2e-s1.ts; preserved fixture paths and all three assertions. No tests deleted or renamed. Required gate passed for all four issues: TypeScript and 11,585 tests across 689 files; no production or quality-store changes. \|\| [PTQ-0797-ctor-proto-named-field-livesessiondouble-not-migrated.md] PTQ-0797: Reused shared model, entry type, and append helpers; imported parseDeps from its current home, e2e-s1.ts. Recording behavior and assertions unchanged; no tests deleted. / PTQ-0798: Replaced all five duplicated diagnostic helpers across three files with existing exports; assertions unchanged and no tests deleted. / PTQ-0799: Replaced both local diagLines helpers with imports and removed unused types; assertions unchanged and no tests deleted. / PTQ-0800: Imported shared diagLines, preserving the lines wrapper and all assertions; removed unused types; no tests deleted. Exact required verification gate passed for all four issues: TypeScript clean, 689 test files and 11,585 tests passed. No production or quality-store edits. \|\| [PTQ-0801-diaglines-reimplemented-inline-object-empty-pair.md] PTQ-0801: Replaced both local diagLines implementations with the existing shared helper; no tests removed. / PTQ-0804: Reused PARSE_REGISTRY and removed the duplicate registry interface/read; assertions unchanged and no tests removed. / PTQ-0806: Shared the diagnostic/fn harness across all five triaged sites, preserving the annotation-specific registration predicate; no tests renamed or removed. / PTQ-0808: Both triaged message readers now delegate to registryMessageOf, preserving registry scope and placeholder checks; no tests removed. Required gate passed for all fixes: tsc and 11,585 tests across 689 files. \|\| |
| 2026-09-19 | PTQ-0750-registry-oracle-reimplemented-inline-object-pair.md | parked | skipped by the fixer in 2 waves — [PTQ-0779-02-statement-executor-invoke-seam-scaffold-not-migrated.md] PTQ-0779: Already migrated to shared span and no-op checkpoint imports; no changes needed. / PTQ-0787: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0788: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0789: Both files now import the shared model and entry type and delegate appends to the newly exported existing helper; no tests or assertions removed. Required verification command passed: TypeScript and all 11,585 tests across 689 files. \|\| [PTQ-0793-canonical-honesty-sorted-closure-duplicated.md] PTQ-0793: Reused assertKeysSorted; retained fragment equality, array-order, and whitespace checks. No tests deleted or renamed. / PTQ-0794: Reused inlineDefName; removed the inline hash computation and unused crypto import. No tests deleted or renamed. / PTQ-0795: Shared holder-bound accessors in scripted-live-session-harness.ts; preserved errors, casts, mocks, and assertions. No tests deleted or renamed. / PTQ-0796: Shared AST capture in e2e-s1.ts; preserved fixture paths and all three assertions. No tests deleted or renamed. Required gate passed for all four issues: TypeScript and 11,585 tests across 689 files; no production or quality-store changes. \|\| [PTQ-0797-ctor-proto-named-field-livesessiondouble-not-migrated.md] PTQ-0797: Reused shared model, entry type, and append helpers; imported parseDeps from its current home, e2e-s1.ts. Recording behavior and assertions unchanged; no tests deleted. / PTQ-0798: Replaced all five duplicated diagnostic helpers across three files with existing exports; assertions unchanged and no tests deleted. / PTQ-0799: Replaced both local diagLines helpers with imports and removed unused types; assertions unchanged and no tests deleted. / PTQ-0800: Imported shared diagLines, preserving the lines wrapper and all assertions; removed unused types; no tests deleted. Exact required verification gate passed for all four issues: TypeScript clean, 689 test files and 11,585 tests passed. No production or quality-store edits. \|\| [PTQ-0801-diaglines-reimplemented-inline-object-empty-pair.md] PTQ-0801: Replaced both local diagLines implementations with the existing shared helper; no tests removed. / PTQ-0804: Reused PARSE_REGISTRY and removed the duplicate registry interface/read; assertions unchanged and no tests removed. / PTQ-0806: Shared the diagnostic/fn harness across all five triaged sites, preserving the annotation-specific registration predicate; no tests renamed or removed. / PTQ-0808: Both triaged message readers now delegate to registryMessageOf, preserving registry scope and placeholder checks; no tests removed. Required gate passed for all fixes: tsc and 11,585 tests across 689 files. \|\| |
| 2026-09-19 | PTQ-0753-malformed-entry-resync-registers-reimplements-isloadparseerr.md | parked | skipped by the fixer in 2 waves — [PTQ-0779-02-statement-executor-invoke-seam-scaffold-not-migrated.md] PTQ-0779: Already migrated to shared span and no-op checkpoint imports; no changes needed. / PTQ-0787: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0788: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0789: Both files now import the shared model and entry type and delegate appends to the newly exported existing helper; no tests or assertions removed. Required verification command passed: TypeScript and all 11,585 tests across 689 files. \|\| [PTQ-0793-canonical-honesty-sorted-closure-duplicated.md] PTQ-0793: Reused assertKeysSorted; retained fragment equality, array-order, and whitespace checks. No tests deleted or renamed. / PTQ-0794: Reused inlineDefName; removed the inline hash computation and unused crypto import. No tests deleted or renamed. / PTQ-0795: Shared holder-bound accessors in scripted-live-session-harness.ts; preserved errors, casts, mocks, and assertions. No tests deleted or renamed. / PTQ-0796: Shared AST capture in e2e-s1.ts; preserved fixture paths and all three assertions. No tests deleted or renamed. Required gate passed for all four issues: TypeScript and 11,585 tests across 689 files; no production or quality-store changes. \|\| [PTQ-0797-ctor-proto-named-field-livesessiondouble-not-migrated.md] PTQ-0797: Reused shared model, entry type, and append helpers; imported parseDeps from its current home, e2e-s1.ts. Recording behavior and assertions unchanged; no tests deleted. / PTQ-0798: Replaced all five duplicated diagnostic helpers across three files with existing exports; assertions unchanged and no tests deleted. / PTQ-0799: Replaced both local diagLines helpers with imports and removed unused types; assertions unchanged and no tests deleted. / PTQ-0800: Imported shared diagLines, preserving the lines wrapper and all assertions; removed unused types; no tests deleted. Exact required verification gate passed for all four issues: TypeScript clean, 689 test files and 11,585 tests passed. No production or quality-store edits. \|\| [PTQ-0801-diaglines-reimplemented-inline-object-empty-pair.md] PTQ-0801: Replaced both local diagLines implementations with the existing shared helper; no tests removed. / PTQ-0804: Reused PARSE_REGISTRY and removed the duplicate registry interface/read; assertions unchanged and no tests removed. / PTQ-0806: Shared the diagnostic/fn harness across all five triaged sites, preserving the annotation-specific registration predicate; no tests renamed or removed. / PTQ-0808: Both triaged message readers now delegate to registryMessageOf, preserving registry scope and placeholder checks; no tests removed. Required gate passed for all fixes: tsc and 11,585 tests across 689 files. \|\| |
| 2026-09-19 | PTQ-0754-interpolation-parse-diagnostics-anthropic-model-not-migrated.md | parked | skipped by the fixer in 2 waves — [PTQ-0779-02-statement-executor-invoke-seam-scaffold-not-migrated.md] PTQ-0779: Already migrated to shared span and no-op checkpoint imports; no changes needed. / PTQ-0787: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0788: Replaced local parseDeps() with the existing helper import; all tests and assertions preserved. / PTQ-0789: Both files now import the shared model and entry type and delegate appends to the newly exported existing helper; no tests or assertions removed. Required verification command passed: TypeScript and all 11,585 tests across 689 files. \|\| [PTQ-0793-canonical-honesty-sorted-closure-duplicated.md] PTQ-0793: Reused assertKeysSorted; retained fragment equality, array-order, and whitespace checks. No tests deleted or renamed. / PTQ-0794: Reused inlineDefName; removed the inline hash computation and unused crypto import. No tests deleted or renamed. / PTQ-0795: Shared holder-bound accessors in scripted-live-session-harness.ts; preserved errors, casts, mocks, and assertions. No tests deleted or renamed. / PTQ-0796: Shared AST capture in e2e-s1.ts; preserved fixture paths and all three assertions. No tests deleted or renamed. Required gate passed for all four issues: TypeScript and 11,585 tests across 689 files; no production or quality-store changes. \|\| [PTQ-0797-ctor-proto-named-field-livesessiondouble-not-migrated.md] PTQ-0797: Reused shared model, entry type, and append helpers; imported parseDeps from its current home, e2e-s1.ts. Recording behavior and assertions unchanged; no tests deleted. / PTQ-0798: Replaced all five duplicated diagnostic helpers across three files with existing exports; assertions unchanged and no tests deleted. / PTQ-0799: Replaced both local diagLines helpers with imports and removed unused types; assertions unchanged and no tests deleted. / PTQ-0800: Imported shared diagLines, preserving the lines wrapper and all assertions; removed unused types; no tests deleted. Exact required verification gate passed for all four issues: TypeScript clean, 689 test files and 11,585 tests passed. No production or quality-store edits. \|\| [PTQ-0801-diaglines-reimplemented-inline-object-empty-pair.md] PTQ-0801: Replaced both local diagLines implementations with the existing shared helper; no tests removed. / PTQ-0804: Reused PARSE_REGISTRY and removed the duplicate registry interface/read; assertions unchanged and no tests removed. / PTQ-0806: Shared the diagnostic/fn harness across all five triaged sites, preserving the annotation-specific registration predicate; no tests renamed or removed. / PTQ-0808: Both triaged message readers now delegate to registryMessageOf, preserving registry scope and placeholder checks; no tests removed. Required gate passed for all fixes: tsc and 11,585 tests across 689 files. \|\| |
| 2026-09-19 | PTQ-0731-separator-test-local-parse-reimplements-parsedoc.md | false-positive | verdict: false-positive — does not reproduce at HEAD: the cited local `parse()` (:233-235) and the `parseThetaDocument`/`FileSystem` imports were deleted in commit cc0a8fe7 (quality: qw20260917204232 fix tests__p1, 2026-09-18), which changed :12 to `import { parseDeps, parseDoc as parse } from "./helpers/e2e-s1"`; `grep -n "^function parse(source" tests/import-specifier-separator-production-required.test.ts` → 0 hits, `git status` clean for the file, and the two callers (parseLib :243, parseApp :254) already go through the shared helper — nothing remains to fix, close rather than re-fix (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0732-01-diaglines-shadows-e2e-s1-export-pair.md | false-positive | verdict: false-positive — does not reproduce at HEAD: both cited local `function diagLines` declarations are gone (grep `^function diagLines` over both files → 0 hits, working tree clean) and each file now imports `diagLines` from ./helpers/e2e-s1 (typeenv-prototype-names.test.ts:22 `import { parseDoc, diagLines }`, union-arm-literal-const-lowering.test.ts:20 `import { yamlQuoted, parseDoc, diagLines }`); the migration landed in commit cc0a8fe7 (2026-09-18, qw20260917204232 fix tests__p1), whose diff removes `-function diagLines(doc: ThetaDocument): string[]` from both files, so nothing remains to fix and the 2 fixer skips were correct (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0733-01-diaglines-reimplemented-both-in-scope-files.md | false-positive | verdict: false-positive — does not reproduce at HEAD: both cited local declarations are gone; tests/union-generic-arm-lowering.test.ts:15 now reads `import { yamlQuoted, parseDoc, diagLines } from "./helpers/e2e-s1"` and tests/unresolvable-operand-structural-target-adjudication.test.ts:22 reads `import { parseDoc, diagLines as lines } from "./helpers/e2e-s1"`; the candidate's own decl grep (`^function diagLines\\|^function lines(doc`) returns 0 hits and no `${d.severity} ${d.code}` render expression remains in either file; `git log -S` shows both copies removed by commit cc0a8fe7 (quality: qw20260917204232 fix tests__p1, 2026-09-18), whose diff is exactly the suggested migration (import added, local function deleted) though unattributed to this record; working tree clean for both files; not a duplicate — same-slug resolved PTQ-0799 cites brace-and-angle-annotation-junk-refusal / brace-rooted-union-arm-capture, not this pair; close as already resolved (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0737-02-retag-makedeps-reimplements-e2e-s1.md | false-positive | verdict: false-positive — does not reproduce at HEAD: commit cc0a8fe7 (qw20260917204232, 2026-09-18) removed makeDeps() and its SystemNoteChannelDeps/ModelReferenceMatcher imports from tests/wire-translation-inbound-retag.test.ts; line 1 now imports parseDoc from ./helpers/e2e-s1 and parse() (lines 39-40) delegates to parseDoc(src, path) keeping the "retag.theta" default; grep for SystemNoteChannelDeps\|ModelReferenceMatcher\|parseThetaDocument in the file → 0 hits, working tree clean — the cited duplication is already gone, so this needs closure not a fix (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0750-registry-oracle-reimplemented-inline-object-pair.md | false-positive | verdict: false-positive — does not reproduce at HEAD: tests/inline-empty-object-type.test.ts:1 and tests/inline-object-duplicate-field-name.test.ts:1 both `import { REGISTRY } from "./helpers/registry-oracle"`; `git grep -n 'parseRegistry\\|interface RegistryRow' HEAD` on both files = 0 hits and the working tree is clean; the cited 20-line local read was removed in committed cc0a8fe7 (2026-09-18, qw20260917204232 fix tests__p1, -48/+4 across the two files, part of a 15-file inline-object-family migration) — the finding was accurate at filing but is already resolved with no attributable fix record of its own; not a duplicate (PTQ-0473 cites empty-entry-slot/empty-field-type-truncation, PTQ-0751 cites these files' fixture builder only), so reject for closure rather than re-fix (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0753-malformed-entry-resync-registers-reimplements-isloadparseerr.md | false-positive | verdict: false-positive — does not reproduce at HEAD: the cited inline re-derivation is gone; tests/inline-object-malformed-entry-resync.test.ts:294-295 now reads `return !doc.diagnostics.some(isLoadParseError);` with `isLoadParseError` imported from ./helpers/e2e-s1 at line 8, and `grep 'startsWith("theta/load/")'` in that file returns 0 hits; the dedupe landed in commit cc0a8fe7 (qw20260917204232 fix tests__p1, 2026-09-18) — `git show cc0a8fe7` on this file shows `-(d.code.startsWith("theta/load/") \|\| ...)` / `+return !doc.diagnostics.some(isLoadParseError);` — so the two later fixer "skips" were already-resolved no-ops, not refusals; working tree clean for the file; close as already fixed rather than re-fixing (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0754-interpolation-parse-diagnostics-anthropic-model-not-migrated.md | false-positive | verdict: false-positive — does not reproduce at HEAD: the local `const ANTHROPIC_MODEL` at tests/interpolation-parse-diagnostics.test.ts:1017-1023 was deleted and replaced by `import { ANTHROPIC_MODEL } from "./helpers/scripted-live-session-harness"` (line 1) in commit cc0a8fe7 (quality: qw20260917204232 fix tests__p1); `grep -n "const ANTHROPIC_MODEL"` on the file returns no hit, both use sites (:1068 getAvailable, :1075 ctx.model) consume the import, and the file passes 41/41 — the root cause was real when filed but has already been fixed by a prior committed wave, so there is nothing left to fix and the row should close (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0757-registry-oracle-reread-let-annotation-pair.md | parked | skipped by the fixer in 2 waves — [PTQ-0757-registry-oracle-reread-let-annotation-pair.md] PTQ-0757: Already resolved at HEAD; both files use registry-oracle. No edits. / PTQ-0848: Shared constructor drive plumbing/readers through the existing helper, including the triage-added interpolation copy; preserved guards and assertions. No tests deleted. / PTQ-1013: Renamed the ternary test to describe its observed result; assertions unchanged. No citation updates needed. / PTQ-1015: Replaced all eight triage-identified double copies with one shared factory; preserved recordings, responses, and assertions. No tests deleted. Required gate passed: TypeScript and all 11,585 tests across 689 files. \| review unconfirmed: PTQ-0757-registry-oracle-reread-let-annotation-pair.md — shed by the fixer (no working-tree change attributable to it), and correctly so: the described problem no longer exists at HEAD — tests/let-annotation-inline-object-compat.test.ts:97 imports REGISTRY from ./helpers/registry-oracle and tests/let-annotation-query-double-emission.test.ts:52,68 imports and calls readRegistry(["parse"]); neither file reimplements the shard read anymore. The issue file is stale and can be closed on re-verification with no code change. \|\| |
| 2026-09-19 | PTQ-0778-01-static-type-inference-invoke-seam-scaffold-not-migrated.md | parked | skipped by the fixer in 2 waves — [PTQ-0778-01-static-type-inference-invoke-seam-scaffold-not-migrated.md] PTQ-0778: Already imports all three shared scaffold exports; no duplication remains and no changes made. / PTQ-0809: Reused parseTheta at all three sites, retained the frontmatter guard, and moved typed-query validation to fixture creation. No tests removed or renamed. / PTQ-0811: Both msg wrappers now delegate to registryMessageOf, preserving registry scope and placeholder checks. No tests removed or renamed. / PTQ-0812: Replaced duplicate registry loading and row type with readRegistry(["parse"]); assertions unchanged and no tests removed. Required verification command passed for all edits: TypeScript and 11,585 tests across 689 files. \| review unconfirmed: PTQ-0778-01-static-type-inference-invoke-seam-scaffold-not-migrated.md — shed by the fixer (no working-tree change attributable), but the described problem is already absent at HEAD: tests/static-type-inference.test.ts line 1 imports span/SEAM_NOOP_CHECKPOINT/SEAM_NOOP_MUTATOR from ./helpers/invoke-seam-scaffold and the local span()/NOOP_CHECKPOINT/NoopMutator declarations cited at :52-54/:167-180 no longer exist (grep: only the import and a NOOP_CHECKPOINT usage at :193 remain). It was migrated by a prior wave's commit; the issue should be closed as already-resolved rather than re-queued. \|\| [PTQ-0813-isregistrationerror-reimplements-isloadparseerror.md] PTQ-0813: Replaced all three predicate copies identified by triage with the existing isLoadParseError helper; assertions unchanged and no tests removed. / PTQ-0814: Centralized all four diagnostic-reader pairs in production-load-harness, preserving lazy outcome access and exact filtering; no tests removed. / PTQ-0815: Reused appendUserEntry/appendAssistantEntry and removed private append plumbing; recording and turn behavior unchanged, with no tests removed. / PTQ-0816: Reused rootDouble/noopPi and removed redundant doubles and imports; no tests removed. Required cluster gate passed: TypeScript succeeded and all 689 test files / 11,585 tests passed. \|\| [PTQ-0817-misfire-faces-registry-msg-reimplements-load-row-harness.md] PTQ-0817: Reused the shared parse registry, row type, and registryMessageOf; existing assertions retained. / PTQ-0818: Replaced all seven redundant definedness checks with not.toThrow() preconditions; helper checks remain intact. / PTQ-0819: Both files now use registryMessageOf; removed the redundant row lookup and message-rendering boilerplate. / PTQ-0820: Reused runProductionLoad and LoadOutcome, extending options to preserve UI mode and system-note recording. No tests deleted or renamed across the cluster; required gate passed: TypeScript compilation and 689 files / 11,585 tests. \|\| [PTQ-0822-off-surface-live-wiring-noop-checkpoint-sink-reimplemented.md] PTQ-0822: Re-verified and replaced local no-op checkpoint and sink with aliased shared imports; no tests or assertions removed or changed. Required gate passed. / PTQ-0823: Re-verified and replaced duplicate registry read with shared REGISTRY; local lookup wrapper, tests, and assertions unchanged. Required gate passed. / PTQ-0824: Re-verified and replaced local AJV factory with shared capturingAjv; tests and diagnostic-capture assertions unchanged. Required gate passed. / PTQ-0826: Re-verified and imported shared REGISTRY and RegistryRow; local readers, tests, and assertions unchanged. Required gate passed: TypeScript and all 11,585 tests across 689 files. No production or quality files changed. \|\| [PTQ-0827-quoted-stray-msg-reimplements-registrymessageof.md] PTQ-0827: Both msg wrappers delegate to registryMessageOf; existing assertions retained. No tests removed or renamed. / PTQ-0829: Shared registryHintOf replaces all three parsers; failure wording and markup stripping preserved. All 386 equivalence comparisons passed. No tests removed or renamed. / PTQ-0830: Reused shared REGISTRY; removed the redundant interface, four-page read, and unused imports. No tests removed or renamed. / PTQ-0832: All three message-reader sites use registryMessageOf; template, severity, and phase assertions retained. No tests removed or renamed. Required cluster gate passed: TypeScript and 689 files / 11,585 tests. \|\| [PTQ-0834-schema-alias-union-decl-msg-reimplements-registrymessageof.md] PTQ-0834: Replaced duplicate message rendering with registryMessageOf; all tests retained. / PTQ-0835: Reused registryMessageOf, preserving the pre-substitution non-empty guard through an opt-in option; added guard coverage and retained all existing tests. / PTQ-0836: Reused shared parse helpers across all four files, retaining fixture paths and error rejection; dispatch now uses the shared fixture-error wording. No tests renamed or deleted. / PTQ-0839: Moved the identical regex helper into registry-oracle.ts and replaced all four copies with imports; assertions unchanged and all tests retained. / Required verification command passed: TypeScript clean; 689 test files and 11,586 tests passed. No src/ or quality/ files changed. \|\| |
| 2026-09-19 | PTQ-0757-registry-oracle-reread-let-annotation-pair.md | false-positive | verdict: false-positive — does not reproduce at HEAD 4a9d1d5e: neither file imports parseRegistry or reads docs/spec_topics/diagnostics/code-registry-*.md (grep of both files → zero parseRegistry hits); tests/let-annotation-inline-object-compat.test.ts:97 imports REGISTRY from ./helpers/registry-oracle and tests/let-annotation-query-double-emission.test.ts:52,68 imports and calls readRegistry(["parse"]); the migration landed in commit cc0a8fe7 (wave qw20260917204232, -36/+5 across the pair, removing the parseRegistry import and both local REGISTRY reads); the compat file's surviving readFileSync/fileURLToPath imports serve a live fixture read at :585-590 (acc-typed-inline.theta), not a registry read — issue is stale, close with no code change (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0778-01-static-type-inference-invoke-seam-scaffold-not-migrated.md | false-positive | verdict: false-positive — does not reproduce at HEAD: tests/static-type-inference.test.ts:1 already imports span/SEAM_NOOP_CHECKPOINT (aliased NOOP_CHECKPOINT)/SEAM_NOOP_MUTATOR from ./helpers/invoke-seam-scaffold and feeds them to execDeps at :193/:195; the cited local span()/NOOP_CHECKPOINT/NoopMutator declarations (:52-54/:167-180) no longer exist (grep 0 hits; working tree clean) — removed by committed cc0a8fe7 (2026-09-18, qw20260917204232, -27/+2 on this file), so the finding is already-resolved and both fixer skips were correct (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0815-livesessiondouble-append-reimplements-appenduserentry.md | parked | skipped by the fixer in 2 waves — [PTQ-0815-livesessiondouble-append-reimplements-appenduserentry.md] PTQ-0815: Already resolved at current HEAD; shared append helpers are used and private #append is absent. No edits. / PTQ-0840: Replaced both local template readers with loadRowMessage; substitutions and behavior assertions preserved. No tests removed or renamed. / PTQ-0841: Reused shared parseDeps and parseDoc, preserving fixture paths and corpus parsing. No tests removed or renamed. / PTQ-0842: Reused shared REGISTRY and RegistryRow; verified all four oracle rows match the original read. Assertions unchanged; no tests removed. Required cluster verification passed: TypeScript and all 11,586 tests across 689 files. \| review unconfirmed: PTQ-0815-livesessiondouble-append-reimplements-appenduserentry.md — shed by the fixer (no working-tree change attributable to it), but the described problem is already absent at HEAD: tests/prompt-provider-field-derivation.test.ts:55 imports appendUserEntry/appendAssistantEntry from ./helpers/scripted-live-session-harness and calls them at :173/:192, with the private #append plumbing gone — resolved by prior committed wave qw20260919114228 (commit 4e83337e). Close as already-resolved rather than re-queue. \|\| [PTQ-0844-b0316-noop-mutator-sink-reimplemented.md] PTQ-0844: Replaced local no-op sink and misleading RecordingMutator with shared exports; all assertions retained. / PTQ-0846: Imported shared FM and producer; preserved parse-refusal handling and pure-host execution. / PTQ-0849: Imported shared snapshot as snapshotOf; call sites and assertions unchanged. / PTQ-0850: Reused workspace/load/disposal helpers with hasUI: true; strengthened the resolution check to assert control-fixture registration. No tests deleted or renamed for any issue. Required gate passed: TypeScript and 11,586 tests across 689 files. \|\| [PTQ-0855-livesignal-helper-reimplemented-two-in-scope-files.md] PTQ-0855: Both files now import the existing liveSignal helper; all tests retained. / PTQ-0858: Shared range-rendering helpers replace all three copies named by triage; diagnostic assertions unchanged. / PTQ-0859: Reused registryMessageOf, preserving replace-all behavior and the unfilled-placeholder assertion; no tests deleted. / PTQ-0861: Migrated the remaining params copy to registryErrorLine; both siblings already used it. No tests deleted. Required gate passed for all fixes: TypeScript and 11,586 tests across 689 files. \|\| [PTQ-0864-readdiagnosticspage-duplicated.md] PTQ-0864: Replaced both local page readers with existing readRepoFile; retained both assertions. / PTQ-0865: Removed fullOf and migrated all 12 calls to existing diagLines; assertions unchanged. / PTQ-0867: Imported shared REGISTRY; removed the duplicate loader, unused interface and imports; assertions unchanged. / PTQ-0868: Shared pointer-message composition across all four triaged sites, preserving per-file failure handling; 1,056 before/after comparisons passed. No tests removed for any issue. Required gate green: tsc and all 11,586 tests across 689 files passed. \|\| [PTQ-0869-schema-alias-union-decl-diaglines-reimplemented.md] PTQ-0869: Replaced local diagLines with the existing helper import; all assertions retained. / PTQ-0870: Reused schemaDeclsOf/enumDeclsOf in both boundary files and all three additional triage-named files; no tests or assertions removed. / PTQ-0873: Replaced the local checkpoint/root/producer trio with shared helpers, preserving the no-clock root setup and all tests. / PTQ-0874: Replaced only the A_INT_INLINE formula with inlineDefName; retained the distinct slug routine and all assertions. Required verification command passed for the cluster: TypeScript clean, 689 test files and 11,586 tests passed. No production or quality-store files changed. \|\| [PTQ-0876-collectcalls-ast-walker-duplicated-drifted.md] PTQ-0876: Replaced all three recursive walkers with a shared collector using collectByKind; retained invoke handling and cardinality assertions. No tests deleted. / PTQ-0877: Replaced both local diagLines definitions with the existing shared export; assertions unchanged. No tests deleted. / PTQ-0878: Shared the unchanged envelope builder through tests/helpers/e2e-s1.ts; byte-comparison assertions unchanged. No tests deleted. / PTQ-0879: Delegated to shared loadCleanly while retaining bug0039.theta and the properties-object guard. No tests deleted. Required cluster gate passed: tsc and all 11,586 tests in 689 files. \|\| |
| 2026-09-19 | PTQ-0815-livesessiondouble-append-reimplements-appenduserentry.md | false-positive | verdict: false-positive — does not reproduce at HEAD: the cited `#append`/inline user+assistant message shapes at tests/prompt-provider-field-derivation.test.ts:167-211 no longer exist (grep `#append\|entries.length + 1` → 0 hits); the file now imports `appendUserEntry`/`appendAssistantEntry` from ./helpers/scripted-live-session-harness at :55 and calls them at :173/:192, exactly the suggested direction; `git show 4e83337e --stat` (wave qw20260919114228, 3 insertions/22 deletions on this file) removed the `#append` body, `role: "user"`/`role: "assistant"` literals and `parentId` derivation and added the two imports+calls — the fixer's own note for that wave reports "PTQ-0815: Reused appendUserEntry/appendAssistantEntry and removed private append plumbing" even though the store recorded the item as skipped; working tree clean, file passes 5/5 — the finding was real when filed but is already resolved by a committed wave, so close as resolved rather than re-queue (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0880-params-default-empty-literal-registrymessageof-duplicated.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0881-registry-oracle-reimplemented-binder-param-line.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0883-rootdouble-producer-reimplemented-instead-of-imported.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0884-schema-alias-union-decl-loadparams-reimplements-loadcleanly.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0892-description-registration-composedrunnablecount-not-migrated.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0895-01-params-default-unresolvable-enum-variant-seam-scaffold-no.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0896-01-registry-single-page-reimplements-readregistry.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0898-b0399-invoke-seam-scaffold-reimplemented.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0901-expectcallerrefused-reimplements-canonical.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0902-normativemessagepattern-reimplemented-b0268.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0905-b0399-typed-query-loop-harness-duplicated.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0906-dispatchpass-rundispatchpass-duplicated.md | parked | skipped by the fixer in 2 waves — [PTQ-0908-registry-oracle-reimplemented-b0268.md] PTQ-0908: Already uses readRegistry(["parse"]); cited duplication no longer reproduces. No edits. / PTQ-0909: Already uses soleByFragment at all four cited call sites; local soleCollision is absent. No edits. / PTQ-0910: compose() already delegates to runProductionLoad; duplicated host doubles are absent. No edits. / PTQ-0927: Replaced local parseDeps with the shared import and removed unused types. All tests and assertions retained. Required gate passed: TypeScript clean, 689 test files and 11,586 tests passed. \|\| [PTQ-0928-detach-throw-harness-reimplements-supersessionharness.md] PTQ-0928: Reused shared supersession/base harnesses; preserved quiesce pass attribution and note recording. All assertions retained. / PTQ-0933: Migrated b0282 and triage-listed b0277 fixture builders to loadRowFromBody/loadRowFromParam; fixture paths and assertions unchanged. / PTQ-0934: Imported shared render in b0368 and triage-listed b0369; removed identical local copies without changing assertions. / PTQ-0937: Imported shared reportOf at both sites; preserved exact failure wording through an optional fixture label. No tests deleted. Exact verification gate passed: TypeScript and all 689 test files / 11,586 tests. \|\| [PTQ-0938-requirepath-precondition-pair-duplicated.md] PTQ-0938: Centralized both path checks across all 12 callers, preserving check order and failure wording; no tests deleted. / PTQ-0939: Migrated all three triaged message readers to canonical registry helpers, retaining expected messages and adding placeholder-presence checks; no tests deleted. / PTQ-0942: Replaced the local reportOf with the canonical import, preserving narrowing and failure wording; no tests deleted. / PTQ-0945: Replaced the duplicate four-page registry read with shared REGISTRY and removed unused imports and type; no tests deleted. Required gate passed for all fixes: tsc and 11,586 tests across 689 files; git diff --check clean. \|\| |
| 2026-09-19 | PTQ-0880-params-default-empty-literal-registrymessageof-duplicated.md | false-positive | verdict: false-positive — does not reproduce: none of the five cited excerpts exists in current code; commit b955d65d ("qw20260919135027 fix tests__p1 batch PTQ-0880-…") already replaced the local `registryMessage`→`expect(...).toBeDefined()`→cast body in params-default-empty-literal-refusal:139-145, params-default-string-literal-raw-newline:199-205, params-scalar-nontype-text-refusal:225-231 and schema-body-nontype-text-refusal:189-195 with a one-call delegate to `registryMessageOf as readRegistryMessage` from tests/helpers/load-row-harness.ts:62-80, and params-inline-enum-position-refusal.test.ts has no local `registryMessageOf` at all since 70f3263e (PTQ-0861 fix; it now imports `registryErrorLine as line` from ./helpers/registry-oracle); awk-scan of the 16 remaining `^function registryMessageOf` test files finds the expect-based local body only in b0449-reexport-chain-enum-unknown-variant and b0450-imported-enum-system-param, neither cited here — the fix landed but the store never recorded it, hence the spurious skips (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0881-registry-oracle-reimplemented-binder-param-line.md | false-positive | verdict: false-positive — the cited duplication no longer reproduces: commit b955d65d (qw20260919135027, PTQ-0880 batch) deleted the local `interface RegistryRow`, the local four-page `const REGISTRY = parseRegistry(...)` and the `parseRegistry` import from tests/binder-param-line-newline-normalisation.test.ts and added `import { REGISTRY } from "./helpers/registry-oracle"` at :1; today `grep -nE "RegistryRow\|parseRegistry\|code-registry-"` in the file → 0 hits and the sole `REGISTRY` use (`registryMessage(REGISTRY, code)` at :210) reads the shared helper export, so the fix already landed and nothing remains to import-swap (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0883-rootdouble-producer-reimplemented-instead-of-imported.md | false-positive | verdict: false-positive — does not reproduce at HEAD: tests/member-access-declared-field-type.test.ts:17-18 now imports `rootDouble` from ./helpers/call-with-clause-harness and `producer` from ./helpers/runtime-belt-probe-harness and `run()` calls `producer(rootDouble())` (:1189); grep for NOOP_CHECKPOINT\|function rootDouble\|function producer\|createProductionProducerDeps\|Checkpoint\|ModelRegistry\|ExtensionAPI in the file → only the :79 prose comment; git show b955d65d (qw20260919135027, PTQ-0880 fix batch) deleted the cited :1170-1195 local trio and their five type/factory imports and added exactly the two helper imports this filing proposes — the fixer already applied the direction as a same-batch side fix, so nothing remains to rule on (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0884-schema-alias-union-decl-loadparams-reimplements-loadcleanly.md | false-positive | verdict: false-positive — does not reproduce against HEAD: commit b955d65d (qw20260919135027, batch PTQ-0880) already migrated this site — tests/schema-alias-union-decl.test.ts:11 now imports `loadCleanly` from ./helpers/e2e-s1, `loadParams` (:608-622) is a thin wrapper that calls `expectNoResidue` then `loadCleanly(label, doc)` (canonical widened to accept `string \| ThetaDocument`, e2e-s1.ts:675) and layers only the local `properties` read, and grep for `frontmatter === null\|params === undefined\|lowered === undefined` in the test file → 0 hits, so the three-throw duplicate body the filing cites is gone; the 10 `loadParams(` call sites remain live but the shared portion is already sourced from the canonical export (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0892-description-registration-composedrunnablecount-not-migrated.md | false-positive | verdict: false-positive — does not reproduce at HEAD: tests/e2e-s6-description-registration.test.ts:5,42,48 now imports and drives `plantThetaWorkspace`/`disposeWorkspace`/`runProductionLoad` from ./helpers/production-load-harness; grep for `getFlag\|registerFlag\|discoverAndComposeFixtures(` in the file → 0 hits (only the describe title mentions the name); the cited :40-73 inline `pi`/`ctx` double and mkdtemp/mkdir/writeFileSync/rmSync plant were removed by commit 005e520b (qw20260919135027, self-labelled as the PTQ-0892 fix, −31/+4), which predates both "skipped" waves (qw20260919162231, qw20260919170939) — the skips were nothing-left-to-do, not a refusal; file still 2/2 vitest green; this is a resolved finding whose store state was never closed, not an open duplication (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0895-01-params-default-unresolvable-enum-variant-seam-scaffold-no.md | false-positive | verdict: false-positive — cited duplication no longer reproduces: `grep -n "function parseDeps\\|const NOOP_CHECKPOINT\\|const NOOP_SINK\\|class InertMutator" tests/params-default-unresolvable-enum-variant.test.ts` → 0 hits; the file now imports `SEAM_NOOP_CHECKPOINT as NOOP_CHECKPOINT`/`SEAM_NOOP_SINK as NOOP_SINK`/`SEAM_NOOP_MUTATOR` from ./helpers/invoke-seam-scaffold (:1-5) and `parseDeps` from ./helpers/e2e-s1 (:6), used live at :612, :720, :722, :740, :742; commit 005e520b (qw20260919135027 fix tests__p1 batch, 8+/39-) removed exactly the four local declarations and added these imports, so the suggested direction has already landed and nothing remains to fix (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0896-01-registry-single-page-reimplements-readregistry.md | false-positive | verdict: false-positive — does not reproduce at HEAD: commit 005e520b (qw20260919135027 fix batch) already removed the local `RegistryRow` interface and the `readFileSync`/`fileURLToPath`/`parseRegistry` inline read (-18/+2 lines); the file now has `import { readRegistry, type RegistryRow } from "./helpers/registry-oracle"` at :2 and `const REGISTRY = readRegistry(["parse"])` at :190, with `grep -n "parseRegistry(\\|readFileSync\\|fileURLToPath\\|interface RegistryRow"` → 0 code hits (only a :159 comment mention); the two fixer skips were correct because nothing remained to migrate (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0898-b0399-invoke-seam-scaffold-reimplemented.md | false-positive | verdict: false-positive — no longer reproduces: already fixed by commit 2aab985f (2026-09-19, `qw20260919135027 fix tests__p1 batch PTQ-0898`, −121/+22) which deleted the local `NOOP_SINK`/`NoopMutator`/`span` triad and imports `SEAM_NOOP_SINK, SEAM_NOOP_MUTATOR, span` from `./helpers/invoke-seam-scaffold` at :59 (used at :378/:394/:353); the stated `^const NOOP_SINK\\|^class NoopMutator\\|^function span` search now returns 0 and `grep invoke-seam-scaffold` returns 1, working tree clean vs HEAD, test file 6/6 green; both recorded "skipped" waves postdate the fix, so the store parked an already-resolved issue — reject to close (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0901-expectcallerrefused-reimplements-canonical.md | false-positive | verdict: false-positive — no longer reproduces: the cited local bodies (allDiagnostics filter + toBeGreaterThanOrEqual(1) + toMatch) are gone; all three `expectCallerRefused` declarers (callee-post-parse-errors :279, callee-tools-missing-theta-path :300, grandchild-callee-drop :368) are now six-arg one-call wrappers delegating to `expectCallerRefusedWithCalleeHasErrors`, which each file imports at :16/:11/:9; git shows commit 2aab985f (2026-09-19, qw20260919135027 PTQ-0898 batch, cherry-pick of a7d68295) removed the duplicated bodies, and `git status` is clean for those files so this is the committed state — the residual name-only wrapper is a per-file constant binding, not a copy of the check (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0902-normativemessagepattern-reimplemented-b0268.md | false-positive | verdict: false-positive — the cited duplication no longer reproduces at HEAD: commit 2aab985f (qw20260919135027, PTQ-0898 batch, 2026-09-19) already migrated b0268 — its import statement now carries `normativeMessagePattern as normativeMessagePatternCore` (:13) and the local `normativeMessagePattern` (:123-131) is a delegating wrapper `return normativeMessagePatternCore(REGISTRY, code, () => { throw … })` using the harness's `onMissing` hook (compose-workspace-harness.ts:464-479), exactly the b0275 shape the filing asked for; `registryMessage` is no longer imported by the file; `grep -ln 'replace(/<\[a-z-\]+>/g, ".+")' tests/*.test.ts` finds only thetalib-reparse-walk (scoped out by the filing, tracked by PTQ-0716); vitest 2 passed (2) — nothing left for a fixer to do, which is why two waves skipped it (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0905-b0399-typed-query-loop-harness-duplicated.md | false-positive | verdict: false-positive — re-verified against HEAD: the cited duplication no longer reproduces; commit 2aab985f (2026-09-19, PTQ-0898 fix batch) deleted all eight local declarations from b0399 (`-const NOOP_CHECKPOINT`, `-function liveSignal`, `-function schemaDeclsOf`, `-function ajv`, `-const toolUse/textTurn/respond`, `-class ScriptedParent`, 143-line diff), hoisted `toolUse`/`textTurn`/`respond`/`ScriptedParent` as exports of tests/helpers/scripted-typed-query-harness.ts (:61-95), and b0399 now imports the seven from that helper (:60-68) plus `schemaDeclsOf` from typed-query-harness (:69); grep for any of the eight as a local decl in b0399 → 0 hits, `class ScriptedParent` exists repo-wide only in the helper, b0355 now imports the driver quartet from the helper (:43), both files green (2 files / 11 tests); note for a fresh D7 filing if wanted: b0355 :72-111 still carries its own `NOOP_CHECKPOINT`/`liveSignal`/`schemaDeclsOf`/`ajv` copies, which is a different host and not this finding's root cause (triage: claude-fable-5-1) |
| 2026-09-19 | PTQ-0906-dispatchpass-rundispatchpass-duplicated.md | false-positive | verdict: false-positive — does not reproduce against HEAD: the cited local `DispatchPass`/`runDispatchPass` declarations are absent from both files (b0267 :250-310 and b0270 :255-320 now hold `requireCalleeDropRoute`/`expectCallerRefused`; `grep -rn "DispatchPass\\|runDispatchPass" src extensions tools tests` hits only `tests/helpers/fixture-dispatch-harness.ts:98/:111` plus the two files' `import { runDispatchPass } from "./helpers/fixture-dispatch-harness"` at b0267 :25 and b0270 :20, and `const before = host.notes.length` survives solely in the helper) — commit f2963075 ("fix tests__p1 batch PTQ-0906…", cherry-pick of 37f51d6d, wave qw20260919135027) removed 53+55 lines from the pair and added the single exported builder (which already calls `dispatchCtx(workspace.cwd)` per the earlier triage nuance); the two later "skipped" waves were skipping an already-fixed item, so the store should close this as fixed rather than re-dispatch it (triage: claude-fable-5-1) |
