# Diagnostics

Loom emits structured diagnostics through two delivery channels, both owned by the loom extension. Pi's own `LoadExtensionsResult.errors` field is **not** used: that field belongs to Pi's extension loader and is only populated while Pi is `import()`-ing the loom extension's entry point. A failure there is a bootstrap failure the user sees on Pi startup, orthogonal to the diagnostics defined here, which all fire after the extension is already live (during scan, watcher reload, or slash-command execution).

**Persistent diagnostics (default).** All `loom/parse/*`, `loom/load/*`, and `loom/runtime/*` diagnostics are delivered via the channel below, with three carved-out exceptions — all emitted via `console.error` from the `session_shutdown` handler because Pi's `ExtensionRuntime.invalidate(...)` may already have invalidated `pi.sendMessage` by the time the handler runs (see [Pi Integration Contract — Extension entry point](./pi-integration-contract.md)): `loom/runtime/reload-teardown-timeout` (the bounded-await timeout in sub-step 3), `loom/host/session-shutdown-reason-unknown` (the unknown-reason validation defined in PIC's **Unknown-reason rule**), and `loom/host/session-shutdown-teardown-step-failed` (the per-step isolation rule defined in PIC's **Per-step isolation** paragraph). The structured `Diagnostic` shape below still applies; only the delivery sink differs.

```
pi.sendMessage(
  {
    customType: "loom-system-note",
    content:    <serialised batch>,            // see "Serialised content format" below
    display:    true,
    details:    { diagnostics: <Diagnostic[]> } // single-element array for runtime/single-error cases
  },
  { triggerTurn: false }
)
```


The `pi.registerMessageRenderer("loom-system-note", …)` registered by the loom extension formats these into transcript-persistent, dim-styled notes; see [Pi Integration Contract — System notes](./pi-integration-contract.md) for the renderer registration rules and the best-effort fallback (`ctx.ui.notify` then `loom/runtime/system-note-delivery-failed` then `console.error`) that applies when `pi.sendMessage` itself throws (`pi.sendMessage` returns `void`; only synchronous throws are observable). The renderer MUST be registered synchronously inside the extension factory **before** the first discovery scan kicks off, so the first batch of scan diagnostics renders through the loom-specific renderer rather than as raw fallback text. This is the only diagnostic sink for loom-author-facing parse, type, load, and runtime errors.

The `loom-system-note` channel also carries operator-facing runtime failure events (`details: { event: RuntimeEvent }`) for non-panic `QueryError` failures in the always-log set; the two `details` payload shapes are disjoint by key and are specified under "System notes" and "Runtime event channel" in [Pi Integration Contract](./pi-integration-contract.md). Renderers MUST switch on which key is present and MUST NOT assume both.

**Transient toasts (auxiliary).** Failures internal to the loom extension's own bookkeeping that the user must see immediately but that do **not** belong in the transcript — the chokidar watcher itself throwing, an unrecoverable settings I/O exception that no `loom/load/settings-*` code covers, an internal extension invariant violation — use `ctx.ui.notify(message, "error")` directly. This is a narrow secondary surface; loom-author-facing diagnostics (anything with a `loom/parse/*`, `loom/load/*`, or `loom/runtime/*` code) MUST go through the persistent channel above and MUST NOT be routed through `ctx.ui.notify` as their primary sink.

**Re-scan deduplication.** A watcher-triggered reload re-emits the persistent diagnostic for any file whose contents are still broken. The runtime does **not** attempt to clear or supersede prior `loom-system-note` messages from the transcript — Pi's `pi.sendMessage` API has no documented "remove" or "replace by metadata" counterpart, and `display: true` system notes are part of the immutable session log. Authors will therefore see the same diagnostic line recur after each reload until the underlying file is fixed; this is the V1 contract and the renderer MUST NOT attempt to suppress duplicates.

Internal diagnostic shape:

```
{
  severity: "error" | "warning",
  code:     string,                          // e.g., "loom/parse/binding-case-mismatch"
  file:     string,                          // absolute path
  range:    { start: { line, column }, end: { line, column } },  // 1-indexed; end exclusive
  message:  string,                          // single-line summary
  hint?:    string,                          // optional suggested fix
  related?: array<{ file, range, message }>, // related sites (e.g., the colliding declaration)
  masked?:  array<string>,                   // hard-ceiling co-fire enumeration; see below
}
```

**Code namespaces:**

- `loom/parse/*` — diagnostics produced by the parse pipeline: lexical errors, syntactic errors, and static type-system checks. The `Phase` column in the registry table distinguishes `lex` / `parse` / `type` within this namespace.
- `loom/load/*` — file-load and registration errors (unreadable file, missing or wrong-type discovery source, name collision, invalid frontmatter, unresolvable `tools:` entry).
- `loom/runtime/*` — runtime-phase events: execution panics (`MatchError`, index out of bounds, etc.), the runtime-defect surface (`loom/runtime/internal-error`), and delivery / lifecycle / teardown failures. Routing varies per code — see the [`loom/runtime/*` registry section](#loom-runtime-namespace) and [Errors and Results — Runtime panics](./errors-and-results.md) for per-code channels (most route through `loom-system-note`; `loom/runtime/reload-teardown-timeout` routes through `console.error`).
- `loom/host/*` — anomalies the loom runtime observes coming *from* the Pi host that the spec's host-prerequisites contract does not enumerate (e.g., a `session_shutdown` `event.reason` outside the closed five-arm union, or a throw from a `session_shutdown` teardown sub-step). Emitted via `console.error` since the typical observation site is mid-teardown when the persistent-diagnostic channel may already be invalidated.

**Serialised content format.** This rule fixes the **input** to the renderer (the `content` field on the `CustomMessage` argument), not the renderer's return value. The renderer itself returns a `pi-tui` `Component | undefined`, per the **Renderer registration** block in [Pi Integration Contract — System notes](./pi-integration-contract.md). The `content` string of each `loom-system-note` follows the line format `"<file>:<line>:<col>: <code>: <message>"`, optionally followed by `"\n  hint: <hint>"` when a hint is present. Related sites are appended as additional indented lines. When a single message carries a multi-error batch (the scan-time case), each `Diagnostic` becomes one such line block and successive blocks are separated by a single blank line; the corresponding structured `Diagnostic[]` is carried in `details.diagnostics` for the renderer and for downstream consumers (LSP integrations, test harnesses) that want the typed shape rather than the rendered string.

**Multi-error reporting.** Every parse / type pass collects all errors from the full file (and from transitive `.warp` imports) before failing. The loom is rejected with the complete list in **one `pi.sendMessage` call per `.loom` file** — `content` carries the full batch in the format above; `details.diagnostics` carries the same as a structured array — rather than fast-failing on the first error or fanning out one message per error. Authors get every problem in the file at once, in a single transcript entry.

## Code registry rules (normative)

Three rules govern the diagnostic-code surface:

1. **Every author-visible diagnostic emitted by the runtime MUST carry a code from the registry below.** Emitting an unregistered code is a defect; tests are entitled to assert on the specific code at every documented diagnostic site.
2. **The registry is closed.** Adding a new code, removing a code, or changing a code's namespace, severity, or trigger are all spec changes — not implementation changes. New diagnostic sites added by future spec work MUST land their codes in this table at the same time.
3. **Codes are stable identifiers.** Renaming a registered code is a breaking change to the public diagnostics contract (tests, LSP integrations, system-note formatters, doc cross-links). Treat each code as part of the language surface.
4. **The Message column is normative.** Every row in the registry below carries a *Message* (the rendered author-facing string with `<…>`-style placeholders for interpolated content); renderers MUST emit it character-for-character with placeholders interpolated. Tests asserting a diagnostic's rendered message MUST source the string from this column rather than copy-pasting prose from the spec rule's home page. Wording changes are spec-versioned breaking changes.

Naming convention for codes is `<namespace>/<kebab-case-rule-name>`. The rule-name component derives from the spec rule's short name; it is not generated, so the registry below is the source of truth.

**`masked` field (hard-ceiling co-fire).** The optional `masked` field on the `Diagnostic` shape above carries the hard-ceiling co-fire enumeration defined in [`spec.md` — Hard ceilings — Interaction between ceilings](../spec.md). It is populated only on diagnostics emitted from a hard-ceiling check site whose precondition fired while at least one *sibling* hard ceiling's precondition was simultaneously satisfied at the same site — in V1 this is the `loom/runtime/invoke-depth-exceeded` row below (the only diagnostic-shape hard-ceiling surface; the other co-fire-eligible surfaces are runtime events, not diagnostics, and carry the same enumeration as `event.masked` per [Pi Integration Contract — Runtime event channel](./pi-integration-contract.md)). Each entry is one of `"ceiling#1"`, `"ceiling#2"`, `"ceiling#3"`, `"ceiling#4"`. The field is **omitted** when no co-fire occurred; implementations MUST NOT emit `masked: []`. The per-site reachable mask domain is fixed by the interaction paragraph in `spec.md`.

## Placeholder rendering (normative)

The registry's *Message* column carries `<…>` placeholders that the renderer interpolates at the diagnostic site (rule 4 above). This subsection groups every placeholder used by V1 messages into eight categories and fixes one rendering rule per category, so two conformant implementations produce byte-identical strings (or byte-identical surround around an implementation-defined-tail interpolation, for category 8) for the same source defect. Each rule below carries normative test vectors; conformance tests asserting on a rendered message MUST match these vectors and SHOULD include the boundary cases called out in *Edge cases* at the end of this subsection. The closed token-name table in category 3 and the closed value tables in category 7 carry the same GOV-7 / GOV-8 governance posture as the category-to-placeholder map itself.

**Closure.** The eight categories below form the closed V1.0 placeholder-rendering surface. Every `<…>` token appearing in the *Message* column of any row in [Code registry](#code-registry) is either (a) a placeholder enumerated in categories 1–7 (rendered byte-identically per its category's rule), (b) a §8 placeholder (rendered as a byte-identical prefix and suffix around an implementation-defined-tail interpolation per §8's rule), or (c) the bespoke `<list>` placeholder in `loom/runtime/reload-teardown-timeout` (whose row carries an inline rendering rule that decomposes into the §7 sub-rules for `<slash-name>`, `<invocation-id>`, and the comma-space join). No other placeholders are admitted; the V18s closing CI gate enforces this closure (see [V18s — Coverage-matrix closing CI gate](../plan_topics/v18-cancellation.md#v18s-coverage-matrix-closing-ci-gate)). Introducing a new placeholder, retiring one, or moving a placeholder between categories is a spec-versioned breaking change governed by **GOV-7** and **GOV-8** in [`spec.md` — Governance](../spec.md), exactly as the registry rows themselves are.

### 1. Static-type placeholders

**Placeholders.** `<type>`, `<expected>`, `<actual>`, `<left>`, `<right>`, `<element>`.

**Rule.** Render the Loom static type by re-serialising it in the source-grammar form defined in [Type System](./type-system.md):

- Primitive type names lowercase: `string`, `integer`, `number`, `boolean`, `null`.
- Literal types as their literal source: `"foo"`, `42`, `true`.
- Unions joined by ` | ` (space-pipe-space) with no surrounding parentheses.
- Arrays as `array<T>` (the angle-bracket form; never `T[]`, never `[T]`).
- Named schemas, enums, and type aliases by their loom-side identifier (no wire-name translation; the identifier shape is fixed by [Lexical — Identifiers](./lexical.md)).
- `Result<T, E>` rendered as written, with the inner types recursing this rule.
- Inline anonymous object types as `{ f₁: T₁, f₂: T₂ }`, fields in declaration order, single space after each `:` and after each `,`.

**Test vectors.**

- A binding typed `array<integer | string>` renders as `array<integer | string>`.
- A binding typed `Foo | null` renders as `Foo | null`.

### 2. Runtime-value placeholders

**Placeholders.** `<scrutinee summary>`, `<value>`.

**Rule.** Render via the canonical interpolation-stringification table in [Query — Stringification of interpolated values](./query.md), with one extension and one supplementary case:

- **String truncation.** A string whose length exceeds 80 Unicode code points is truncated to the first 77 code points followed by the literal three-character ellipsis `...`. The 80-code-point cap, the 77-code-point prefix, and the literal ellipsis are normative; counting is by Unicode code point, not by UTF-16 code unit and not by grapheme cluster.
- **`Result<T, E>` values.** A scrutinee whose static type is `Result<T, E>` (the case the query-stringification table reserves for a static parse error) renders as `Ok(<inner>)` or `Err(<inner>)`, with `<inner>` recursing this rule. Panics may legitimately fire on `Result` values (e.g. a `match` whose arms collectively miss an `Err` variant), so this case is reachable here even though it is unreachable in interpolated query templates.

**Test vectors.**

- A `match` panic on a scrutinee `Cat { name: "fluffy" }` renders `MatchError: no arm matched Cat { name: "fluffy" }`.
- A `match` panic on the integer `42` renders `MatchError: no arm matched 42`.
- A `match` panic on a 100-character ASCII string `s` renders `MatchError: no arm matched ` followed by the first 77 code points of `s` followed by the literal `...` (a single trailing three-character ellipsis, no surrounding quotes — the `string` row of the stringification table renders strings verbatim without quoting).

### 3. Syntactic-construct placeholder

**Placeholders.** `<construct>` in `loom/parse/unsupported-feature`; `<expr>` in `loom/parse/default-not-literal` and `loom/parse/tool-arg-not-literal`.

**Rule.**

- For `<expr>` in the two literal-sublanguage codes, render the offending source span verbatim, copied byte-for-byte from the source file between the offending sub-expression's start and end token positions (post-newline-normalisation per [Lexical — Encoding](./lexical.md)), with internal whitespace preserved.
- For `<construct>` in `loom/parse/unsupported-feature`, the offending site is a whole node category with no single source-span anchor (e.g. `=>` lambdas span the entire arrow form, including the body). Use the closed token-name table below.

| Construct | Token name |
|---|---|
| arrow function (`=>`) | `arrow function` |
| spread / rest (`...`) | `spread` |
| optional chaining (`?.`) | `optional chaining` |
| nullish coalescing (`??`) | `nullish coalescing` |
| strict equality (`===` / `!==`) | `strict equality` |
| bitwise op (`&`, `\|`, `^`, `~`, `<<`, `>>`, `>>>`) | `bitwise <op>` (where `<op>` is the source token verbatim) |
| comma operator (expression-position `,`) | `comma operator` |
| nested template literal | `nested template` |
| `new` expression | `new` |
| `typeof` operator | `typeof` |
| `instanceof` operator | `instanceof` |
| `delete` operator | `delete` |
| `void` operator | `void` |
| `yield` expression | `yield` |
| `await` expression | `await` |

**Test vectors.**

- A loom containing `let f = (x) => x + 1` renders `unsupported syntactic feature: arrow function`.
- A `params:` default whose RHS is `a + b` renders `params default RHS must be a literal-sublanguage form; offending sub-expression: a + b`.

### 4. Numeric placeholders

**Placeholders.** `<i>`, `<length>`, `<depth>`, `<offset>`, `<count>`, `<index>`, `<required>`, `<provided>`, `<max>`.

**Rule.** Render as the shortest decimal representation per the `integer` row of the canonical stringification table in [Query — Stringification of interpolated values](./query.md): no scientific notation, no leading zeros, leading `-` for negatives, `0` for the value `-0` (signed zero is normalised at the rendering boundary). `Infinity` and `NaN` are unreachable for these placeholders by construction (every emitting site is bounded — array length is non-negative, invoke depth is bounded by 32, etc.); a renderer that nonetheless encounters one MUST surface it through `loom/runtime/internal-error` rather than emitting `Infinity` or `NaN` into a panic message.

**Test vectors.**

- A negative-index OOB on a 3-element array renders `index out of bounds: -1 not in 0..3`.
- A 33-deep `invoke` chain renders `invoke chain depth exceeded: 33 > 32`.

### 5. Source-derived placeholders

**Placeholders.** `<path>`, `<file>`, `<descriptor>`, `<name>`, `<field>`, `<param>`, `<variant>`, `<keyword>`, `<key>`, `<char>`.

**Rule.** Render verbatim from the source as the offending text would appear there:

- **`<path>`, `<file>`** — the literal text inside the path-literal quotes, with no realpath normalisation, no symlink resolution, no scheme prefixing. This generalises the convention already pinned at `loom/parse/invoke-non-loom-extension`.
- **`<name>`, `<field>`, `<param>`, `<variant>`, `<keyword>`** — identifier-shaped per [Lexical — Identifiers](./lexical.md); rendered unquoted. Any quoting in the rendered message comes from the surrounding registry template (e.g. the literal `'<name>'` in `cannot reassign immutable binding '<name>'`), not from the placeholder.
- **`<key>`** — quoted with double quotes only when the key string is *not* identifier-shaped per [Lexical — Identifiers](./lexical.md) (i.e. would not match `[A-Za-z_][A-Za-z0-9_]*`); otherwise rendered bare. The identifier-shape predicate is a runtime check on the key string, not a parse-time grammar production.
- **`<char>`** — the raw character itself when printable (any code point outside the Unicode general categories `Cc` and `Cn`); otherwise the standard `\xNN` (for code points ≤ U+00FF) or `\u{NNNN}` escape per the escape forms accepted in [Lexical — String literals](./lexical.md).
- **`<descriptor>`** — the discovery-source descriptor as a `kind:value` pair, rendered as `<kind>:"<value>"` (the kind unquoted, the value double-quoted). Kinds are drawn from the closed set defined in [Discovery](./discovery.md) (`settings`, `cli-flag`, `package`); the value is the descriptor's source text verbatim (the settings entry, the CLI flag string, the npm package name). The descriptor format is normative.

**Test vectors.**

- An unknown frontmatter field `wibble` renders `unknown field 'wibble'` (the registry template supplies the surrounding single quotes; the placeholder itself contributes the bare identifier).
- A non-existent settings-sourced loom directory `~/work/looms` renders `discovery source path does not exist: settings:"~/work/looms"`.
- A `match` on `obj["my-key"]` against a missing key renders `missing object key: "my-key"` (key is not identifier-shaped, so quoted).
- A member access `obj["kind"]` on a missing key renders `missing object key: kind` (key is identifier-shaped, so bare).

### 6. Underlying-error placeholders

**Placeholders.** `<error.message>`, `<original content first line>`, `<dispose error first line>`.

**Rule.** Render as the underlying string truncated to its first line:

1. Newline-normalise the underlying string per [Lexical — Encoding](./lexical.md) (`\r\n` and bare `\r` collapse to `\n`).
2. Take the prefix up to (but not including) the first `\n`.
3. Preserve trailing whitespace on the resulting line — no rstrip.
4. When the underlying string is empty, or contains nothing before its first `\n`, render the literal text `<no message>`.

Only `\n` (post-normalisation) is a line break for this rule. The Unicode line separators `\u2028` and `\u2029` are *not* recognised by the lexical newline rule and are *not* recognised here either: a host throwing an `Error` whose message embeds `\u2028` produces a single first-line cut at the first `\n` (or at end-of-string if there is none), with `\u2028` rendered as an ordinary character.

**Test vectors.**

- A `pi.sendMessage` synchronous throw whose underlying error message is `file:5:3: loom/parse/binding-case-mismatch: ...` renders `system-note delivery failed: file:5:3: loom/parse/binding-case-mismatch: ...`.
- A `pi.sendMessage` synchronous throw whose underlying error was constructed without a message (`new Error()`) renders `system-note delivery failed: <no message>`.
- An `AgentSession.dispose()` rejection whose error message is `connection closed\nstack trace ...\n` renders the prefix up to the first `\n` — the literal text `connection closed` — preserving any trailing space on that line.

### 7. Identifier-, descriptor-, and closed-enum placeholders

**Placeholders.** `<callee>`, `<enum>`, `<schema>`, `<method>`, `<model>`, `<provider>`, `<slash-name>`, `<slug>`, `<invocation-id>`, `<uuid>`, `<name1>`, `<name2>`, `<X>`, `<A>`, `<B>`, `<root>`, `<paths>`, `<path-a>`, `<path-b>`, `<source>`, `<higher>`, `<lower>`, `<fields>`, `<ms>`, `<deadline>`, `<cap>`, `<capability>`, `<dotted-key>`, `<reason>`, `<kind>`, `<op>`, `<value>` (in the parse-time literal-value position; the runtime-value usage of `<value>` belongs to category 2 and is distinguished by emitting site).

**Rule.** Each placeholder renders by the closed sub-rule below; sub-rules compose with categories 1–6.

- **Identifier-shaped** (`<callee>`, `<enum>`, `<schema>`, `<method>`, `<model>`, `<provider>`, `<slash-name>`, `<slug>`, `<invocation-id>`, `<uuid>`, `<name1>`, `<name2>`, `<X>`) — rendered unquoted in identifier form per [Lexical — Identifiers](./lexical.md). Any surrounding quoting comes from the registry template, not from the placeholder. The leading `/` of `<slash-name>` is contributed by the surrounding template (e.g. `/<slash-name>`), not by the placeholder. `<uuid>` and `<invocation-id>` render in canonical lowercase 8-4-4-4-12 hex form per the convention pinned in V14a. `<callee>` renders as the registered slash name without leading `/` when one exists; otherwise it falls through to category 5's `<path>` rule (verbatim path-literal source text).
- **Path-shaped** (`<root>`, `<paths>`, `<path-a>`, `<path-b>`, `<source>`, `<A>`, `<B>` — the latter two as the file-path stems used in `loom/load/import-cycle` and `loom/load/invocation-cycle`) — rendered via category 5's `<path>` rule (the literal text inside the path-literal quotes, with no realpath normalisation, no symlink resolution, no scheme prefixing). List-valued forms (`<paths>`) are joined with `, ` (comma-space) in declaration order; for `loom/load/cross-format-collision` the order is: registered candidates first, then the slash-name-deriving `.md` siblings, both internally ordered by discovery-source priority then by absolute path.
- **Descriptor-shaped** (`<higher>`, `<lower>`) — rendered via category 5's `<descriptor>` rule (`<kind>:"<value>"`).
- **Joined-identifier list** (`<fields>`) — identifier-shaped names joined with `, ` (comma-space) in declaration order.
- **Numeric** (`<ms>`, `<deadline>`) — rendered via category 4's integer rule.
- **Closed-enum** — each placeholder draws from a closed value table:
  - `<cap>` ∈ {`looms.scanPackagesMaxFiles`, `looms.scanPackagesTimeoutMs`} (the two scan-package caps in [Discovery — Package discovery](./discovery.md)).
  - `<capability>` ∈ {`pi.registerMessageRenderer`, `pi.registerCommand`, `pi.registerFlag`} (per [Pi Integration Contract — Extension-bootstrap SDK failures](./pi-integration-contract.md)).
  - `<dotted-key>` ∈ {`tool_loop.max_rounds`, `respond_repair.attempts`} (the two non-negative-integer frontmatter scalars in [Frontmatter — Field contract](./frontmatter.md); other dotted keys join the closed set only as new frontmatter scalars are added under GOV-7).
  - `<reason>` ∈ {`quit`, `reload`, `new`, `resume`, `fork`} for the closed-arm path; for the unknown-reason path (per PIC's **Unknown-reason rule**), the verbatim observed value coerced to string via `String(event.reason)`, or the literal `<unreadable>` when the property access threw.
  - `<kind>` is a closed token-name table per emitting site:
    - In `loom/parse/non-string-enum-value` and `loom/parse/non-string-discriminator`: one of `string`, `integer`, `number`, `boolean`, `null` (the type-kind of the offending literal).
    - In `loom/load/settings-invalid-entry` and `loom/load/manifest-invalid`: the JSON kind, one of `string`, `number`, `boolean`, `null`, `array`, `object`.
    - In `loom/load/host-incompatible`: one of `node-floor`, `abortsignal-shape`, `sdk-capability-missing`, `peer-dep-out-of-range`, `peer-dep-malformed-version`, `probe-failed` (the closed Step 0 capability-probe kinds).
  - `<op>` is the source token verbatim, drawn from the closed table in category 3, extended to `++` and `--` for `loom/parse/increment-decrement`.
- **Parse-time literal-value** (`<value>`) — when used in a parse-time row (`loom/load/unknown-mode-value`, `loom/parse/duplicate-enum-value`, `loom/parse/duplicate-discriminator-value`), rendered as the literal source text verbatim per category 5's `<key>` (bare-when-identifier-shaped, double-quoted otherwise) — except that YAML scalars with no enclosing source quoting (e.g. `mode:` values) render unquoted regardless of identifier shape.

**Test vectors.**

- A `loom/runtime/reload-teardown-timeout` with two in-flight invocations against slash names `plan` and `review`, UUIDs `aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa` and `bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb`, and elapsed wall time 2003 ms renders `reload teardown timed out after 2003ms; 2 invocation(s) still in flight: /plan:aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa, /review:bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb`.
- A `loom/load/discovery-slow` aborted at root `~/work/looms` because the `looms.scanPackagesMaxFiles` cap fired renders `package-discovery walk aborted at ~/work/looms: looms.scanPackagesMaxFiles cap reached`.
- A `loom/load/cross-source-shadow` where the same slash name `plan` resolves from a higher-priority `--loom` flag pointing at `/proj/a/plan.loom` and a lower-priority settings entry pointing at `~/work/plan.loom` renders `slash name 'plan' shadowed across discovery sources: 'cli-flag:"--loom /proj/a/plan.loom"' wins over 'settings:"~/work/plan.loom"'`.

### 8. Host-derived freeform-tail placeholders

**Placeholders.** `<error>`, `<message>`, `<observed>`.

**Rule.** Render the underlying host-supplied string by category 6's first-line truncation rule (newline-normalise per [Lexical — Encoding](./lexical.md), take the prefix up to but not including the first `\n`, preserve trailing whitespace, render the literal `<no message>` when empty). Byte-identicality of the surrounding template still holds: every byte of the *Message* template before the §8 placeholder, and every byte after, is byte-identical across implementations. Only the §8 placeholder's interpolation is implementation-defined-tail at the byte level (the first-line truncation rule still applies to bound the rendering). Tests asserting on a *Message* template containing a §8 placeholder MUST construct partial-match patterns anchored on the byte-identical prefix and suffix; conformance to category 8 is therefore both decidable and testable.

**Test vectors.**

- A `loom/load/host-incompatible` whose `<observed>` is `v18.19.0` and `<required>` is `>=20.6.0` renders the byte-identical surround `host incompatible (node-floor): observed ` followed by the host's reported version (truncated to the first line per the rule above) followed by the byte-identical suffix `, required >=20.6.0`. A test asserting this row matches `^host incompatible \(node-floor\): observed (.*), required >=20\.6\.0$`.
- A `loom/runtime/active-set-restore-failed` for a slash command `/plan` whose underlying restore error message is `connection closed\nstack ...` renders `failed to restore tool active-set after /plan: connection closed`.

### Edge cases

- **Category 2 boundary.** A test that constructs an exactly-80-code-point string scrutinee MUST observe the full string (no truncation); a test at 81 code points MUST observe the truncated form (77-code-point prefix + literal `...`). Boundary-condition test vectors are mandatory for the V18s closing CI gate.
- **Category 5 identifier predicate.** The identifier-shape check for `<key>` is a *runtime* string predicate against the regular expression `^[A-Za-z_][A-Za-z0-9_]*$`; a key like `kind` renders bare, `my-key` renders quoted. Keys whose string value happens to match a reserved keyword from [Lexical — Reserved keywords](./lexical.md) are still identifier-shaped for this rule (the reserved-keyword rule fires at parse time on identifier *positions* in source, not at runtime on string values).
- **Category 6 line-separator scope.** Restating for emphasis: `\u2028` and `\u2029` are ordinary characters for this rule. Implementations MUST NOT split on them, MUST NOT strip them, and MUST NOT promote them into `\n` — the runtime is byte-stable in their presence even though authors cannot introduce them through a regular string literal (see [Lexical — String literals](./lexical.md)).
- **Category 7 `<callee>` fallback.** When `<callee>` falls through to the path form (the parent has no slash-name binding for the callee — e.g. a sibling `.warp` `fn`), it renders verbatim per the path-literal rule rather than as an absolute path. This matters for the `loom/parse/invoke-arity-too-few` test vectors: a `.warp` `fn` callee renders as the source-text path the `import` declared, not a realpath.
- **Category 7 closed-enum closure.** The closed value tables for `<cap>`, `<capability>`, `<dotted-key>`, `<reason>` (closed-arm path), and `<kind>` are themselves spec-versioned per **GOV-7** and **GOV-8**: extending any table is a breaking change to the rendering surface even when the underlying registry row is unchanged.
- **Category 8 prefix/suffix anchoring.** A test that asserts equality against the full *Message* string of a category-8 row is non-conformant under §8's rule: the placeholder is implementation-defined-tail at the byte level and only the surround is byte-identical. The V18s closing CI gate flags such tests by enumerating registry rows whose Message templates contain a §8 placeholder and asserting that no test in the suite asserts strict equality against those rows' full Message strings.

## Code registry

The table enumerates every diagnostic the V1 spec defines. *Severity* is `error` (`E`) or `warning` (`W`); a few discovery-source codes carry severity `E/W`, meaning the severity is decided per-source by the table in [Discovery — Failure modes](./discovery.md) rather than fixed at the code level. *Phase* identifies which pipeline stage emits the diagnostic: `lex` (lexing / encoding), `parse` (parsing / static checks performed by the parser), `type` (type-system checks), `load` (file-load, registration, discovery), or `runtime` (panic during execution). *Trigger* is the canonical condition; *Spec rule* points to the topic page where the rule is stated; *Hint* gives the normative author-facing hint when the spec mandates one; *Message* is the rendered author-facing string template (one line; `<…>` placeholders are interpolated by the renderer).

### `loom/parse/*` — lexical, parse, and type-system errors

| Code | Sev | Phase | Trigger | Spec rule | Hint | Message |
|---|---|---|---|---|---|---|
| `loom/parse/illegal-escape` | E | lex | Backslash followed by an unrecognised character inside a regular string literal. | [Lexical — String literals](./lexical.md) | — | `illegal escape sequence: \\<char>` |
| `loom/parse/literal-newline-in-string` | E | lex | Literal newline inside a regular (single-line) string literal. | [Lexical — String literals](./lexical.md) | Use a `@`...`` query template, or split with `+ "\n" +`. | `literal newline in string literal` |
| `loom/parse/unterminated-string` | E | lex | EOF reached while scanning a string literal. | [Lexical — String literals](./lexical.md) | — | `unterminated string literal` |
| `loom/parse/invalid-path-separator` | E | lex | Backslash inside a path literal (`import`, `invoke`, `tools:` `.loom` entry). | [Lexical — Path literals](./lexical.md) | Use `/` separators only. | `invalid path separator: backslash in path literal` |
| `loom/parse/invoke-non-loom-extension` | E | parse | An `invoke(...)` literal or a `tools:` `.loom` entry whose path string does not end in `.loom`. The check runs *after* `loom/parse/invalid-path-separator`, on the path literal as written (no realpath normalisation), and is byte-exact lowercase per [Lexical — Path literals](./lexical.md). | [Invocation — Resolution](./invocation.md) | invoke and `tools:` paths must end in `.loom`; use `import` for `.warp` library code. | `invoke path '<path>' does not end in .loom` |
| `loom/parse/import-non-warp-extension` | E | parse | An `import` path literal that does not end in `.warp` (including a `.loom` path). The check runs *after* `loom/parse/invalid-path-separator`, on the path literal as written (no realpath normalisation), and is byte-exact lowercase per [Lexical — Path literals](./lexical.md). | [Imports — Path resolution](./imports.md) | import paths must end in `.warp`; `.loom` files are not importable — use `invoke(...)` instead. | `import path '<path>' does not end in .warp` |
| `loom/parse/binding-case-mismatch` | E | parse | Identifier in a binding / parameter / fn-name / field-name position does not start with a lowercase letter or `_`. | [Lexical — Identifiers](./lexical.md) | — | `binding name must start with a lowercase letter or _` |
| `loom/parse/schema-case-mismatch` | E | parse | Identifier in a schema / enum / variant / type-alias position does not start with an uppercase letter. | [Lexical — Identifiers](./lexical.md) | — | `schema name must start with an uppercase letter` |
| `loom/parse/reserved-keyword-as-identifier` | E | parse | Reserved keyword used in an identifier position. | [Lexical — Reserved keywords](./lexical.md) | — | `reserved keyword '<keyword>' cannot be used as an identifier` |
| `loom/parse/single-line-if` | E | parse | `if` / `for` / `while` / `fn` body is not a braced block (e.g. `if (x) stmt`). | [Lexical — Statement terminators](./lexical.md) | Wrap the body in `{ ... }`. | `single-line body not permitted; wrap in { ... }` |
| `loom/parse/block-comment` | E | lex | `/* ... */` block comment used. | [Lexical — Comments](./lexical.md) | Use `//` or `///` line comments. | `block comments are not supported` |
| `loom/parse/integer-narrowing` | E | type | `number` value used where `integer` is expected (the `integer → number` widening is one-way). | [Lexical — Number literals](./lexical.md) | — | `cannot narrow number to integer` |
| `loom/parse/unsupported-feature` | E | parse | A V1-deferred or non-Loom syntactic construct (arrow function, spread, optional chaining, `===`, bitwise op, comma op, nested template, etc.) appears in source. | [Expressions — Not supported](./expressions.md) | — | `unsupported syntactic feature: <construct>` |
| `loom/parse/immutable-rebinding` | E | parse | Reassignment of a `let` (non-`mut`) binding. | [Bindings — `let` vs `let mut`](./bindings.md) | Use `let mut` if mutation is intentional. | `cannot reassign immutable binding '<name>'` |
| `loom/parse/assignment-as-expression` | E | parse | Assignment used in expression position (e.g. `if (x = 1)`). | [Bindings — Reassignment is a statement](./bindings.md) | — | `assignment is not an expression` |
| `loom/parse/assignment-to-member-or-index` | E | parse | `obj.field = ...` or `arr[i] = ...`. | [Bindings — Mutability is binding-level only](./bindings.md) | Rebind the whole value with `let mut`. | `cannot assign to member or index; mutability is binding-level only` |
| `loom/parse/mut-on-immutable-context` | E | parse | `mut` modifier on a function parameter, `for` iteration variable, or `match` pattern binding. | [Bindings — Immutable contexts](./bindings.md) | — | `'mut' is not permitted in this binding position` |
| `loom/parse/mut-on-discard` | E | parse | `let mut _ = ...`. | [Bindings — Immutable contexts](./bindings.md) | — | `'mut' is not permitted on discard binding '_'` |
| `loom/parse/increment-decrement` | E | parse | `++` or `--` operator used. | [Bindings — Increment / decrement](./bindings.md) | Use `count += 1` / `count -= 1`. | `'<op>' operator is not supported` |
| `loom/parse/non-boolean-condition` | E | type | Non-`boolean` value used in `if` / `while` / ternary condition or as `&&` / `\|\|` operand. | [Expressions — Truthiness](./expressions.md) | Write `if (x != "")`, `if (xs.length > 0)`, etc. | `condition must be boolean; got <type>` |
| `loom/parse/comparison-chaining` | E | parse | Chained comparison such as `a < b < c`. | [Expressions — Operator precedence](./expressions.md) | Comparison operators do not chain; use `&&`. | `comparison operators do not chain; use &&` |
| `loom/parse/mixed-plus-operands` | E | type | `+` applied to a `number`/`integer` and a `string` (or any other mixed-type pair). | [Expressions — `+` operator](./expressions.md) | Convert explicitly or interpolate inside a string. | `'+' has mixed operand types: <left> and <right>` |
| `loom/parse/array-element-type-mismatch` | E | type | Array literal element does not type-check against the surrounding sink's element type. | [Expressions — Array construction](./expressions.md) | — | `array element type mismatch at index <i>: expected <expected>, got <actual>` |
| `loom/parse/array-no-common-type` | E | type | Array literal whose elements have no common type and no sink to narrow against. | [Expressions — Array construction](./expressions.md) | Annotate the binding with `array<A \| B>` or use a single schema. | `array elements have no common type; annotate the binding with array<A \| B> or use a single schema` |
| `loom/parse/non-string-array-join` | E | type | `arr.join(...)` invoked on an array whose element type is not `string`. | [Expressions — `array<T>` stdlib](./expressions.md) | Map elements to strings first; no implicit type conversion in V1. | `array.join requires a string element type; got array<<element>>` |
| `loom/parse/extra-object-field` | E | parse | Schema constructor lists a field not declared by the schema. | [Expressions — Object construction](./expressions.md) | — | `extra field '<field>' on schema '<schema>'` |
| `loom/parse/missing-object-field` | E | parse | Schema constructor omits a declared (required) field. | [Expressions — Object construction](./expressions.md) | — | `missing field '<field>' on schema '<schema>'` |
| `loom/parse/bare-object-literal` | E | parse | Bare `{ field: expr }` (no schema name) used in expression position outside the two documented carve-outs (`params:` defaults; single argument of a Pi-tool call). | [Expressions — Object construction](./expressions.md) | Name the schema: `Schema { field: expr }`. | `bare object literal not permitted in this position; name the schema (Schema { ... })` |
| `loom/parse/default-not-literal` | E | parse | A `params:` default RHS contains a form outside the [Loom literal sublanguage](./grammar.md#loom-literal-sublanguage) (operator, function call, identifier reference other than `Enum.Variant`, `${...}` interpolation, or `@`...`` template). | [Frontmatter — Defaults](./frontmatter.md) | Replace the offending sub-expression with a literal value, or move the computation into loom body code. | `params default RHS must be a literal-sublanguage form; offending sub-expression: <expr>` |
| `loom/parse/tool-arg-not-literal` | E | parse | The single-argument bare-object literal of a Pi-tool call contains a form outside the [Loom literal sublanguage](./grammar.md#loom-literal-sublanguage). | [Tool Calls — Argument shape](./tool-calls.md) | Bind the computed value via `let` first, then pass through a typed `params:` callee or `invoke(...)`. | `Pi-tool argument must be a literal-sublanguage form; offending sub-expression: <expr>` |
| `loom/parse/tool-arg-arity` | E | parse | A Pi-tool call site uses more than one positional argument (e.g. `read({...}, {...})`). | [Tool Calls — Argument shape](./tool-calls.md) | Pi tools take exactly one input object; merge the arguments. | `Pi tool '<name>' takes a single object argument; got <count>` |
| `loom/parse/let-without-initialiser` | E | parse | `let x: T` (annotation, no initialiser). Loom has no `undefined` value and no definite-assignment analysis. | [Grammar Appendix — `let` form](./grammar.md#let-form) | Initialise the binding at declaration, or restructure to bind once at the point a value is available. | `let binding '<name>' has no initialiser` |
| `loom/parse/statement-in-arm-body` | E | parse | A `match` arm body is a bare `if` / `for` / `while` / `let` / assignment statement. Arm bodies are expressions; the ternary is the expression form of conditional. | [Grammar Appendix — `match` arm body](./grammar.md#match-arm-body) | Wrap the statements in a block expression `{ ... }` whose tail expression is the arm's value. | `match arm body must be an expression; wrap statements in a block expression { ... }` |
| `loom/parse/by-on-object-schema` | E | parse | `schema X by f { ... }` declaration with an object body. The `by` clause applies only to discriminated-union schemas. | [Grammar Appendix — `schema X by <field>`](./grammar.md#schema-x-by-field) | Use `schema X by f = A \| B \| …`, or drop the `by` clause if `X` is an object schema. | `the 'by' clause applies only to discriminated-union schemas (schema X by f = A \| B \| …)` |
| `loom/parse/doc-comment-misplaced` | E | parse | A `///` doc comment appears above a production other than `schema`, `enum`, schema field, enum variant, or `fn`. | [Grammar Appendix — `///` placement](./grammar.md#-placement) | Move the description to a legal anchor, or replace with a regular `//` comment. | `'///' doc comment is not legal above this production` |
| `loom/parse/unknown-identifier` | E | parse | Bare identifier in call or value position resolves to nothing in scope. | [Expressions — Identifier resolution](./expressions.md) | — | `unknown identifier '<name>'` |
| `loom/parse/unknown-method` | E | parse | Method or property accessed on a built-in type that the V1 stdlib does not expose. | [Expressions — Built-in methods](./expressions.md) | — | `unknown method '<method>' on type <type>` |
| `loom/parse/non-array-iterand` | E | type | `for x in expr` where `expr` is not `array<T>`. | [Control Flow — `for`/`in`](./control-flow.md) | Use `obj.keys()` for objects, `s.split(...)` for strings. | `'for' expects array<T> after 'in'; got <type>` |
| `loom/parse/break-outside-loop` | E | parse | `break` outside the body of a `for` / `while` loop. | [Control Flow — `break`/`continue`](./control-flow.md) | — | `'break' outside of a loop` |
| `loom/parse/continue-outside-loop` | E | parse | `continue` outside the body of a `for` / `while` loop. | [Control Flow — `break`/`continue`](./control-flow.md) | — | `'continue' outside of a loop` |
| `loom/parse/break-with-value` | E | parse | `break expr` (V1 `break` carries no value). | [Control Flow — `break`/`continue`](./control-flow.md) | — | `'break' takes no value in V1` |
| `loom/parse/illegal-template-escape` | E | lex | Backslash followed by an unrecognised character inside a `@`...`` query template body. | [Query — Escapes](./query.md) | — | `illegal escape sequence in @\`...\` template: \\<char>` |
| `loom/parse/unterminated-template` | E | lex | EOF reached while scanning a `@`...`` query template. | [Query — Multi-line templates](./query.md) | — | `unterminated @\`...\` query template` |
| `loom/parse/discarded-query-result` | E | parse | Bare `@`...`` expression-statement (the `Result` is dropped without `?` or `let _ =`). | [Query — Discarded query results](./query.md) | Use `?` to propagate failure or `let _ = @\`...\`` to discard explicitly. | `query result discarded; use ? to propagate failure or 'let _ = ...' to discard explicitly` |
| `loom/parse/empty-template` | W | parse | `@`...`` template's static body is empty or whitespace-only after newline-trim and dedent. The loom still loads; the runtime short-circuits the query if the fully-rendered text is also empty. | [Query — Degenerate rendered templates](./query.md) | Add literal text or use `\\n` to keep an intentionally-blank prompt. | `query template body is empty after newline-trim and dedent` |
| `loom/parse/interpolated-result` | E | type | `${expr}` interpolation whose `expr` has Loom static type `Result<T, E>` (the runtime renderer raises the same code as a panic when the type is statically unresolvable). | [Query — Stringification of interpolated values](./query.md) | Unwrap with `?` or `match` before interpolating. | `Result value cannot be interpolated; unwrap with ? or match first` |
| `loom/parse/explicit-schema-mismatch` | W | parse | Both a binding annotation and an explicit `@<Schema>` ascription are present, and the ascription is not compatible with the annotation under [Type System — Type compatibility](./type-system.md#type-compatibility) (i.e. `ascription ⋢ annotation`). Safe widenings (e.g. `@<integer>` under a `number` annotation) do not warn. | [Query — Explicit form](./query.md) | — | `explicit @<Schema> ascription is not compatible with binding annotation` |
| `loom/parse/match-arm-type-mismatch` | E | type | A `match` arm's body type is not assignable to the common type of the other arms. | [Errors and Results — Arm syntax](./errors-and-results.md) | — | `match arm body type does not match the common type of the other arms` |
| `loom/parse/question-outside-result-fn` | E | type | `?` used in a function or top-level loom whose return type is not `Result<_, QueryError>` (and cannot be inferred to one). | [Errors and Results — `?` operator](./errors-and-results.md) | Declare the enclosing scope's return type as `Result<_, QueryError>`. | `'?' used in a scope whose return type is not Result<_, QueryError>` |
| `loom/parse/match-guard-not-supported` | E | parse | Guarded `match` arm (`Pattern if cond => ...`). | [Errors and Results — Pattern grammar](./errors-and-results.md) | Deferred to a future release. | `match guards are not supported in V1` |
| `loom/parse/rest-pattern-not-supported` | E | parse | Rest pattern (`[first, ...rest]`, `{ kind, ...other }`). | [Errors and Results — Pattern grammar](./errors-and-results.md) | Deferred to a future release. | `rest patterns are not supported in V1` |
| `loom/parse/bare-return-in-non-void` | E | type | Bare `return` (no argument) in a function or loom whose return type is not `void`. | [Return Statement](./return.md) | — | `missing return value` |
| `loom/parse/unreachable-code` | W | parse | Statement appears after a `return` in the same block. | [Return Statement](./return.md) | — | `unreachable code after return` |
| `loom/parse/nested-fn` | E | parse | `fn` declaration nested inside another `fn` body or a block. | [Function Definitions — Placement](./functions.md) | Top-level `fn` only in V1. | `nested 'fn' declarations are not supported in V1` |
| `loom/parse/function-as-value` | E | parse | Function name used outside call position (bound to `let`, passed as argument, etc.). | [Function Definitions — Placement](./functions.md) | — | `function '<name>' used outside call position; functions are not first-class in V1` |
| `loom/parse/redundant-wire-name` | W | parse | `field as "field"` rename whose wire name equals the loom-side name. | [Schemas — Wire-name renaming](./schemas.md) | Drop the `as` clause. | `redundant 'as' clause: wire name '<name>' equals the loom-side name` |
| `loom/parse/wire-name-collision` | E | parse | Two fields in the same schema share a wire name, or a wire name collides with another field's loom-side name. | [Schemas — Wire-name renaming](./schemas.md) | — | `wire name '<name>' collides with another field on schema '<schema>'` |
| `loom/parse/empty-schema-body` | E | parse | `schema X { }` declaration with no fields. | [Schemas — Object schemas](./schemas.md) | — | `'<X>' has no fields; an empty schema cannot be validated.` |
| `loom/parse/unknown-variant` | E | parse | `Enum.Variant` reference where `Variant` is not a declared variant of `Enum`. | [Schemas — Variant access](./schemas.md) | — | `unknown variant '<variant>' on enum '<enum>'` |
| `loom/parse/duplicate-enum-value` | E | parse | Two `enum` variants share an explicit string value. | [Schemas — Enum declarations](./schemas.md) | — | `duplicate enum value '<value>' across variants of enum '<enum>'` |
| `loom/parse/non-string-enum-value` | E | parse | `enum` variant explicit value is not a single string literal. | [Schemas — Enum declarations](./schemas.md) | V1 enums carry string values only. | `enum variant value must be a string literal; got <kind>` |
| `loom/parse/inline-enum` | E | parse | `enum["a", "b"]` or other inline-enum form. | [Schemas — Enum declarations](./schemas.md) | Use a literal-union (`"a" \| "b"`) or a top-level `enum` declaration. | `inline 'enum[...]' is not supported; use a top-level 'enum' declaration or a literal-union` |
| `loom/parse/ambiguous-discriminator` | E | parse | Discriminated-union detection finds more than one candidate field. | [Schemas — Discriminated unions](./schemas.md) | Declare explicitly with `by <field>`. | `ambiguous discriminator for <X>; candidates: <fields>. Declare explicitly with 'by <field>'.` |
| `loom/parse/missing-discriminator` | E | parse | Discriminated-union detection finds no candidate field. | [Schemas — Discriminated unions](./schemas.md) | Add a `kind` (or similar) field to each variant, or declare explicitly with `by <field>`. | `<X> is a union of object schemas with no shared single-literal discriminator field. Add a 'kind' (or similar) field to each variant, or declare explicitly with 'by <field>'.` |
| `loom/parse/duplicate-discriminator-value` | E | parse | Two variants of a discriminated union share the same discriminator value. | [Schemas — Discriminated unions](./schemas.md) | — | `duplicate discriminator value '<value>' across variants of <X>` |
| `loom/parse/nested-discriminator` | E | parse | Discriminator field is not at the top level of each variant (e.g. `kind: { type: "x" }`). | [Schemas — Discriminated unions](./schemas.md) | — | `discriminator field '<field>' must be at the top level of each variant of <X>` |
| `loom/parse/non-string-discriminator` | E | parse | Discriminator field's per-variant literal type is not `string` — i.e. a numeric or boolean literal `const`. Applies equally to implicit detection and to the explicit `by <field>` form. | [Schemas — Discriminated unions](./schemas.md) | Wrap the tag as a string (e.g. `kind: "v1"` rather than `kind: 1`). | `discriminator '<field>' on <X> must be a string-literal type; got <kind>` |
| `loom/parse/type-alias-cycle` | E | parse | A `schema X = ...` whose right-hand side reduces to `X` through type aliases only — directly (`schema X = X`) or transitively (`schema X = Y; schema Y = X`). Cycles that traverse at least one object-schema hop remain legal. Detection runs after schema-name resolution but before lowering. | [Schemas — Recursion](./schemas.md) | — | `type-alias cycle: <path>` |
| `loom/parse/system-on-prompt-mode` | E | parse | `system:` frontmatter field declared on a `mode: prompt` loom. | [Frontmatter — `system`](./frontmatter.md) | Either switch to `mode: subagent` or remove `system:`. | `'system:' is not permitted on a mode: prompt loom` |
| `loom/parse/system-interp-not-path` | E | parse | Body of a `${...}` interpolation in `system:` is not a bare identifier path (e.g. `${arr[0]}`, `${a + b}`, `${f(x)}`, `${a?.b}`, `${"x"}`). | [Frontmatter — `system` Interpolation](./frontmatter.md) | The `system:` slot accepts only bare identifier paths; see [future-considerations.md](./future-considerations.md) for richer expressions. | `'system:' interpolation body must be a bare identifier path` |
| `loom/parse/system-interp-unknown-param` | E | parse | Head identifier of a `system:` `${...}` path does not name a declared `params` field. | [Frontmatter — `system` Interpolation](./frontmatter.md) | — | `'system:' interpolation references unknown param '<name>'` |
| `loom/parse/system-interp-bad-field` | E | parse | A `.Ident` step in a `system:` `${...}` path does not name a reachable object field, or descends into an array or un-narrowed discriminated union. | [Frontmatter — `system` Interpolation](./frontmatter.md) | — | `'system:' interpolation '.<field>' does not name a reachable object field on <path>` |
| `loom/parse/system-interp-unterminated` | E | parse | `${` in a `system:` value is not closed by a matching `}` before the YAML scalar ends. | [Frontmatter — `system` Interpolation](./frontmatter.md) | — | `'system:' interpolation '${' is not closed by a matching '}'` |
| `loom/parse/timeout-field-rejected` | E | parse | `timeout:` field declared on a query, tool call, or invoke (V1 has no per-call timeout). | [Cancellation](./cancellation.md) | Per-call timeouts are deferred. | `'timeout:' field is not supported in V1` |
| `loom/parse/bind-context-session-on-subagent` | W | parse | `bind_context: session` declared on a `mode: subagent` loom. | [Binder — Binder context](./binder.md) | Subagent-mode looms have no caller-session context. | `'bind_context: session' has no effect on a mode: subagent loom` |
| `loom/parse/bind-echo-on-bypass` | W | parse | `bind_echo: true` declared on a binder-bypass-eligible loom (single no-default `string` param). | [Binder — Echo policy](./binder.md) | The bypass auto-suppresses echo regardless. | `'bind_echo: true' has no effect on a binder-bypass-eligible loom` |
| `loom/parse/warp-top-level-statement` | E | parse | `.warp` file contains a top-level statement, `let`, or query. | [Imports — `.warp` file rules](./imports.md) | Move the code into a `fn` body. | `top-level statement not permitted in .warp file; move into a fn body` |
| `loom/parse/import-name-collision` | E | parse | Two imports bring in the same symbol name, or an imported symbol collides with a top-level declaration in the same file. | [Imports — Name collisions](./imports.md) | Resolve with `as`-aliasing. | `imported symbol '<name>' collides with another import or top-level declaration` |
| `loom/parse/invoke-arg-type-mismatch` | E | type | `invoke(...)` argument does not type-check against the callee's declared `params` schema (when the callee is statically resolvable). | [Invocation — Argument binding](./invocation.md) | — | `invoke argument <i> ('<param>') type mismatch: expected <expected>, got <actual>` |
| `loom/parse/tool-arg-type-mismatch` | E | type | `<name>(args)` tool-call argument does not match the tool's input schema or the callee loom's `params` (when statically resolvable). | [Tool Calls — Argument shape](./tool-calls.md) | — | `tool '<name>' argument type mismatch: expected <expected>, got <actual>` |
| `loom/parse/invoke-return-type-mismatch` | E | type | `invoke<Schema>(...)` annotation is structurally incompatible with the callee's inferred return type, where both annotation and callee are statically resolvable. | [Invocation — Typed return](./invocation.md) | Widen the annotation, narrow the callee, or drop the annotation and let the runtime AJV check decide. | `invoke<Schema> annotation incompatible with callee '<callee>' return type <actual>` |
| `loom/parse/invoke-arity-too-few` | E | parse | `invoke(...)` or `.loom` callable call passes fewer arguments than the callee's count of non-defaulted `params:`, where the callee is statically resolvable. | [Invocation — Argument arity](./invocation.md#argument-binding) | Provide the missing argument(s) or default the corresponding `params:` field on the callee. | `invoke '<callee>' passes too few arguments: expected <required> non-defaulted, got <provided>` |
| `loom/parse/invoke-arity-too-many` | E | parse | `invoke(...)` or `.loom` callable call passes more arguments than the callee's total `params:` count. Always parse-time — no runtime safety net is possible. | [Invocation — Argument arity](./invocation.md#argument-binding) | Drop the extra argument(s); positional binding has no destination for them. | `invoke '<callee>' passes too many arguments: expected at most <max>, got <provided>` |

### `loom/load/*` — file-load and registration errors

| Code | Sev | Phase | Trigger | Spec rule | Hint | Message |
|---|---|---|---|---|---|---|
| `loom/load/extension-bootstrap-failed` | E | load | A factory-time or `session_start`-time SDK registration call (`pi.registerMessageRenderer`, `pi.registerCommand`, or `pi.registerFlag`) threw or rejected after the step-0 capability probe had already verified the capability is present. The `details: { capability, error }` payload names the failing call (`"pi.registerMessageRenderer" \| "pi.registerCommand" \| "pi.registerFlag"`) and carries the underlying error message; for `pi.registerCommand` failures the payload also carries `loom: <slash-name>`. Granularity per [Pi Integration Contract — Extension-bootstrap SDK failures](./pi-integration-contract.md): a renderer failure degrades system notes to `ctx.ui.notify` for that extension instance but the factory completes; a per-loom `pi.registerCommand` failure drops only that loom and emits one diagnostic per failing loom; a `pi.registerFlag` failure is fatal to the whole extension (steps 2–5 do not execute). Routed through the **System notes** fallback chain (`sendSystemNote` → `ctx.ui.notify` → `console.error`) because the renderer itself may be the failing capability. | [Pi Integration Contract — Extension-bootstrap SDK failures](./pi-integration-contract.md) | Inspect the `error` text in the diagnostic payload; the underlying cause is host-side (Pi internal failure or extension-load coordination conflict). A `/reload` is the recovery path for a transient failure. | `extension bootstrap failed: <capability> threw <error>` |
| `loom/load/host-incompatible` | E | load | The extension-factory capability probe (per [Pi Integration Contract — Step 0 (Capability probe)](./pi-integration-contract.md#entry-capability-probe)) observed a host-side precondition violation. The `details: { kind, observed, required, ...optional }` payload's `kind` is one of: `"node-floor"` (the running Node version is below the pinned floor `>=20.6.0` under SemVer ordering with prerelease tags eligible — the recommended recipe is a `semver.satisfies(…, ">=20.6.0", { includePrerelease: true })` call, but the contract is the comparison, not the library); `"abortsignal-shape"` (a required `AbortSignal` / `AbortController` constructor or named member is missing per the per-member kind table in Step 0 — `typeof === "function"` for constructors and methods, `"<name>" in AbortSignal.prototype` for `aborted` / `reason`); `"sdk-capability-missing"` (one of the seven factory-probable `@mariozechner/pi-coding-agent` function members is missing or non-function); `"peer-dep-out-of-range"` (one of the four lock-step `@mariozechner/*` peer-dep packages — `pi-coding-agent`, `pi-agent-core`, `pi-ai`, `pi-tui`, iterated in that fixed order — has an installed version, read from its `package.json`, that parses as SemVer and is outside the pinned `~0.72.1` tilde range; `details.package` carries the offending scoped name and the iteration short-circuits on the first failure); `"peer-dep-malformed-version"` (for one of the same four packages, the installed `version` field is missing or is not a syntactically valid SemVer string, the `package.json` is unresolvable via `MODULE_NOT_FOUND`, or another foreseeable shape failure of the read — `details.observed` carries the raw string or `"<unresolvable>"` and `details.package` carries the offending scoped name); or `"probe-failed"` (any check threw an unforeseen error — `details.step ∈ { "node-floor", "abortsignal-shape", "sdk-capability-missing", "peer-dep-out-of-range" }` names which check threw, `details.package` additionally carries the lock-step package name when `details.step = "peer-dep-out-of-range"`, and `details.cause` carries the underlying `Error.message`). On any of the six kinds the factory refuses every subsequent factory-time host-binding call (the four `pi.register*` calls **and `pi.on`** — no slash commands, tools, renderers, flags, or event subscriptions register) and emits this single diagnostic. The fixed short-circuit ordering is `(a)` Node floor, `(b)` `AbortSignal` shape, `(c)+(d)` capability list and peer-dep version; a simultaneous-failure host emits the first failing check's `kind`. The diagnostic routes through the **System notes** fallback chain (`sendSystemNote` → `ctx.ui.notify` → `console.error`) because the message renderer itself may be one of the missing capabilities. | [`spec.md` — Orientation — Prerequisites](../spec.md#orientation), Host runtime obligations 1–3; [Pi Integration Contract — Step 0 (Capability probe)](./pi-integration-contract.md#entry-capability-probe) | Upgrade Node to the pinned floor, or upgrade the `@mariozechner/*` package named in `details.package` (and the other three lock-step peers in the same minor bump, per the lock-step rule in **Host prerequisites — Pi SDK pin**) to a version within the pinned range. For `"peer-dep-malformed-version"`, the host's Pi installation is broken for the package named in `details.package` (workspace symlink, git-pinned dev install, corrupt `package.json`); for `"probe-failed"`, inspect `details.cause` to identify the unreadable input. | `host incompatible (<kind>): observed <observed>, required <required>` |
| `loom/load/invalid-encoding` | E | lex | Source file is not valid UTF-8, or carries a non-UTF-8 BOM. | [Lexical — Encoding](./lexical.md) | Re-save the file as UTF-8 (no BOM, or UTF-8 BOM). | `invalid UTF-8 encoding at byte offset <offset>` |
| `loom/load/unknown-frontmatter-field` | W | load | Frontmatter contains a field not in the V1 vocabulary. | [Frontmatter](./frontmatter.md) | — | `unknown frontmatter field '<field>'` |
| `loom/load/deferred-frontmatter-field` | W | load | Frontmatter contains a field reserved for a deferred V1 feature. | [Frontmatter](./frontmatter.md) | — | `frontmatter field '<field>' is reserved for a deferred V1 feature` |
| `loom/load/missing-mode` | E | load | Frontmatter omits the required `mode:` field. | [Frontmatter — Field contract](./frontmatter.md) | Add `mode: prompt` or `mode: subagent`; `mode:` is the only required frontmatter field. | `frontmatter is missing required field 'mode:'` |
| `loom/load/params-null` | E | load | Frontmatter declares `params: null`. Absent `params:` and `params: {}` are the two equivalent ways to declare a no-params loom. | [Frontmatter — Field contract](./frontmatter.md) | Remove the `params:` line entirely, or write `params: {}`. | `'params: null' is not permitted; omit 'params:' or use 'params: {}'` |
| `loom/load/frontmatter-value-out-of-range` | E | load | A frontmatter scalar covered by the non-negative-integer rule (currently `tool_loop.max_rounds` and `respond_repair.attempts`) is present but is not a non-negative integer: a negative integer, a non-integer number (e.g. `25.5`), a non-number scalar (e.g. `"25"`), or `null`. Numeric integer-ness is judged on the parsed value, not the YAML lexical form (`25` and `25.0` are both accepted). | [Frontmatter — Field contract](./frontmatter.md) | Use a non-negative integer; `0` is the disable form for `max_rounds` and is equivalent to `attempts: 0` for `respond_repair`. | `frontmatter field '<dotted-key>' must be a non-negative integer; got <observed>` |
| `loom/load/bind-echo-without-params` | W | load | `bind_echo: true` declared on a no-params loom (`params:` absent or `params: {}`). The binder does not run, so no echo is produced. | [Binder — Binder bypass](./binder.md) | Remove `bind_echo:` or accept that the field has no effect. | `'bind_echo: true' has no effect on a no-params loom` |
| `loom/load/unknown-mode-value` | E | load | `mode:` is present but its value is neither `prompt` nor `subagent`. | [Frontmatter — Field contract](./frontmatter.md) | V1 recognises `prompt` and `subagent` only. | `unknown 'mode:' value '<value>'; expected 'prompt' or 'subagent'` |
| `loom/load/unknown-tool` | E | load | `tools:` entry names a Pi tool not in the registry. | [Frontmatter — `tools`](./frontmatter.md) | — | `unknown Pi tool '<name>'` |
| `loom/load/unresolvable-loom-path` | E | load | `tools:` `.loom` entry resolves to a path that does not exist or is not readable. | [Frontmatter — `tools`](./frontmatter.md) | — | `cannot resolve .loom path '<path>'` |
| `loom/load/prompt-mode-callable` | E | load | `tools:` `.loom` entry points at a prompt-mode loom file. | [Frontmatter — `tools`](./frontmatter.md) | Subagent-mode looms only inside `tools:`; use `invoke(...)` for prompt-mode callees. | `'tools:' entry '<path>' points at a prompt-mode loom; only subagent-mode looms are permitted` |
| `loom/load/tool-name-collision` | E | load | Two `tools:` entries (after `as` rename) resolve to the same name; or a `tools:` name collides with a top-level `fn` or import. | [Frontmatter — `tools`](./frontmatter.md) | Disambiguate with `as`. | `tool name '<name>' collides with another 'tools:' entry, top-level fn, or import` |
| `loom/load/invalid-tool-rename` | E | load | `as <name>` target is not loom-identifier-shaped (e.g. `as MyTool` instead of `as my_tool`). | [Frontmatter — `tools`](./frontmatter.md) | Use a lowercase-first identifier. | `'as <name>' rename target must be lowercase-first; got '<name>'` |
| `loom/load/invocation-cycle` | E | load | Static walk of `invoke` paths discovers a cycle. | [Invocation — Cycle detection](./invocation.md) | — | `invocation cycle: <A> → <B> → <A>` |
| `loom/load/invoke-path-escape` | E | load, runtime | An `invoke(...)` literal or a `tools:` `.loom` entry resolves (post-realpath) to a path that lies outside every active discovery root. Also fires from the runtime open re-check defined in [Invocation — Resolution](./invocation.md): a symlink that resolves outside every active root at invocation time, even if it was inside at load time. | [Invocation — Resolution](./invocation.md) | Move the callee under one of the active discovery roots (see [Directory Convention — Discovery roots](./discovery.md#discovery-roots)), or add the callee's directory as a settings `looms` entry. | `invoke path '<path>' resolves outside every active discovery root` |
| `loom/load/binder-model-unresolved` | E | load | A non-bypass loom is in scope but neither `bind_model:` frontmatter nor the `looms.binderModel` setting resolves to a model identifier. | [Binder — Binder model](./binder.md) | Set `bind_model:` in the loom's frontmatter, or set `looms.binderModel` in `~/.pi/agent/settings.json` or `.pi/settings.json`. | `binder model unresolved: set 'bind_model:' in frontmatter or 'looms.binderModel' in settings` |
| `loom/load/binder-model-not-strict-capable` | E | load | The runtime probed `Model<Api>.strictCapable` (per [Binder — Strict-capability requirement](./binder.md)) and observed `false`, indicating the resolved binder model is explicitly not strict-structured-output / strict-tool-input capable. Under `pi-coding-agent ~0.72.1` the field is absent (`undefined`) and this diagnostic does not fire in production; the W-level `loom/load/binder-model-strict-capability-unknown` fires instead. | [Binder — Strict-capability requirement](./binder.md) | Choose a strict-capable model (Claude Haiku, GPT-4o-mini, Gemini Flash all qualify on supported providers). | `binder model '<model>' is not flagged as strict-structured-output capable` |
| `loom/load/binder-model-strict-capability-unknown` | W | load | Pi's `Model<Api>` for the resolved binder model exposes no strict-capability indicator, so the load-time check degrades to best-effort. Universal under `pi-coding-agent ~0.72.1`. | [Binder — Binder model](./binder.md) | Verify empirically that the chosen binder model supports strict structured-output / strict tool-input on its provider; upgrade `pi-coding-agent` once a minor exposes per-model strict capability. | `binder model '<model>' strict-capability flag unavailable; load-time check degraded to best-effort` |
| `loom/load/argument-hint-not-displayed` | W | load | A loom declares `argument-hint:` but no `description:`. The binder still receives the hint, but Pi's autocomplete dropdown will show only an empty entry (Pi extension-registered commands have no `argumentHint` slot). | [Frontmatter — Field contract](./frontmatter.md) | Add a `description:` so the autocomplete entry is not empty, or accept the empty entry. | `'argument-hint:' declared without 'description:'; Pi's autocomplete entry will be empty` |
| `loom/load/callee-has-errors` | E/W | load | A `.loom` callee referenced by an `invoke(...)` literal or a `tools:` `.loom` entry failed to parse, lower, or pass its own structural checks during the parent's per-load-pass static-resolution walk (per [Invocation — Static resolution](./invocation.md#static-resolution)). The diagnostic carries the callee's own diagnostic codes via `related`. Severity is per surface: `tools:` `.loom` entries are `E` (the callable cannot be created and the parent does not register); `invoke(...)` literals are `W` (the parent registers; static checks against the unresolvable callee are skipped; the runtime AJV check is the safety net). | [Invocation — Static resolution](./invocation.md#static-resolution) | Open the callee and fix the listed errors. | `callee '<path>' has errors; see related diagnostics` |
| `loom/load/import-cycle` | E | load | Static walk of `.warp` `import` graph discovers a cycle. | [Imports — Cycles](./imports.md) | — | `import cycle: <A>.warp → <B>.warp → <A>.warp` |
| `loom/load/case-collision` | W | load | Two `*.loom` files within one discovery source differ only in case. | [Discovery — Case-insensitive collisions](./discovery.md) | Rename one file. | `case-insensitive filename collision in <source>: '<path-a>' and '<path-b>'` |
| `loom/load/cross-source-shadow` | W | load | The same slash name resolves from multiple discovery sources at different priorities. | [Discovery — Source priority](./discovery.md) | Remove the lower-priority entry. | `slash name '<name>' shadowed across discovery sources: '<higher>' wins over '<lower>'` |
| `loom/load/cross-format-collision` | E | load | Two or more candidates at the same priority derive the same slash name. Covers both the same-format case (two `.loom` files at the same priority — e.g. two packages, two settings entries, two `--loom` components) and the cross-format case (`.loom` vs Pi-owned `.md` prompt / `.md` skill / another extension's command). The diagnostic lists every colliding path; for cross-format, the Pi-owned entry survives and only the loom(s) drop; for same-format, none of the colliding looms register. | [Discovery — Slash-name collisions at the same priority](./discovery.md) | Rename one of the colliding files so the slash names diverge, or remove the duplicate configuration entry. | `slash name '<name>' collides at the same priority: <paths>` |
| `loom/load/invalid-slash-name` | E | load | `*.loom` filename stem does not match `^[a-z0-9][a-z0-9_-]*$`. | [Discovery — Filename validity](./discovery.md) | Slash names must be lowercase kebab/snake; rename the file (e.g. `code-review.loom`). | `slash names must be lowercase kebab/snake; rename the file (e.g. \`code-review.loom\`)` |
| `loom/load/missing-source` | E/W | load | A discovery source's path does not exist. Severity is per the failure-modes table in [Discovery — Failure modes](./discovery.md): error for explicit references (`pi.looms` entries, settings `looms` entries, `--loom` flags), warning never — conventional locations (global, project, package `looms/`) emit no diagnostic when missing. | [Discovery — Failure modes](./discovery.md) | Check the source descriptor in the message and either create the path or remove the configuration entry. | `discovery source path does not exist: <descriptor>` |
| `loom/load/unreadable-source` | E/W | load | A discovery source's path exists but cannot be read (permission denied, ACL, symlink loop at the root, transient I/O error). Also fires (warning, never error) when a candidate `package.json` read during the package-discovery walk exceeds the per-read deadline derived from `looms.scanPackagesTimeoutMs` (see [Discovery — Package discovery](./discovery.md)); the diagnostic carries `details.kind = "package-read-timeout"` and the message form `package '<name>' package.json read exceeded <deadline>ms during package discovery`. Severity is per the failure-modes table: error only for `--loom` flags, warning for every other source (including the per-read-timeout cause). | [Discovery — Failure modes](./discovery.md) | — | `discovery source is unreadable: <descriptor>` |
| `loom/load/wrong-type-source` | E/W | load | A discovery source's path exists but is neither a `.loom` file nor a directory containing them. Severity is per the failure-modes table: error for `pi.looms` entries, settings `looms` entries, and `--loom` flags; warning for the conventional-location roots when the root path resolves to something other than a directory. | [Discovery — Failure modes](./discovery.md) | Point the entry at a `.loom` file or a directory of them. | `discovery source <descriptor> is neither a .loom file nor a directory of them` |
| `loom/load/unreadable` | W | load | A `*.loom` file discovered under any source is itself unreadable (broken symlink, EACCES on the file, transient I/O error). The loom is not registered; the rest of the scan continues. | [Discovery — Failure modes](./discovery.md) | — | `.loom file is unreadable: '<path>'` |
| `loom/load/settings-unreadable` | W | load | `~/.pi/agent/settings.json` or `.pi/settings.json` exists but is unreadable. | [Discovery — Settings file reads](./discovery.md) | — | `settings file '<path>' is unreadable` |
| `loom/load/settings-invalid-json` | W | load | A settings file is present but not valid UTF-8 JSON. | [Discovery — Settings file reads](./discovery.md) | — | `settings file '<path>' is not valid UTF-8 JSON` |
| `loom/load/settings-invalid-entry` | E | load | A `looms` array entry is not a string (object-form entries are not accepted in V1). | [Discovery — `looms` entry schema](./discovery.md) | Use a string path; object-form entries are reserved. | `settings 'looms[<index>]' must be a string path; got <kind>` |
| `loom/load/invalid-extension` | E | load | A settings `looms` file entry (or a glob match) resolves to a file whose name does not end in `.loom`. The extension comparison is byte-exact lowercase per [Lexical — Extension matching](./lexical.md#extension-matching). | [Discovery — `looms` entry schema](./discovery.md) | Point the entry at a `.loom` file or narrow the glob. | `'looms[<index>]' resolves to '<path>' which does not end in .loom` |
| `loom/load/non-canonical-extension` | W | load | While walking a discovery-source directory, the loader encountered a file whose stem matches the slash-name regex `^[a-z0-9][a-z0-9_-]*$` but whose extension is a case-variant of `.loom` or `.warp` other than the lowercase canonical form (e.g. `Plan.LOOM`, `personas.WARP`). The file does not register — by [Lexical — Extension matching](./lexical.md#extension-matching) it is not a `.loom`/`.warp` file at all; the warning's purpose is to surface an otherwise-silent authoring mistake. Per-source; deduped against same-inode `.loom`/`.warp` siblings via `realpath`. | [Discovery — Non-canonical extension case](./discovery.md) | Rename the file so the extension is lowercase `.loom` or `.warp`. | `file '<path>' has non-canonical extension case; rename to lowercase '.loom' or '.warp'` |
| `loom/load/manifest-invalid` | E | load | A package's `pi.looms` field is not a `string[]` (a string, object, `null`, or an array containing non-string entries). The package contributes no looms; other packages still process. | [Discovery — Package discovery](./discovery.md#package-discovery) | Set `pi.looms` to an array of path strings, mirroring `pi.skills` / `pi.prompts`. | `package '<name>' has invalid 'pi.looms': expected string[], got <kind>` |
| `loom/load/manifest-escapes-package` | W | load | A `pi.looms` entry's resolved path lies outside the package root (via `..` segments or an absolute path). The entry is skipped; other entries in the same array still process. | [Discovery — Package discovery](./discovery.md#package-discovery) | Use a path relative to the package root. | `package '<name>' 'pi.looms' entry '<path>' resolves outside the package root` |
| `loom/load/discovery-slow` | W | load | The package-discovery walk hit its file-count cap (`looms.scanPackagesMaxFiles`) or wall-clock cap (`looms.scanPackagesTimeoutMs`) before completing. Packages encountered after the cap fires contribute no looms in this session. | [Discovery — Package discovery](./discovery.md#package-discovery) | Set `looms.scanPackages: false`, raise the cap, or use explicit settings `looms` entries to register the wanted looms. | `package-discovery walk aborted at <root>: <cap> cap reached` |
| `loom/load/typed-query-unsupported-provider` | W | load | A loom contains at least one typed-query expression and its resolved `model:` routes through a provider outside the V1 typed-query support set (`anthropic-messages`, `openai-completions`, `mistral`, `amazon-bedrock`). The loom still loads; typed queries return `Err(QueryError { kind: "transport", retryable: false, ... })` at runtime. | [Pi Integration Contract — Provider compatibility for typed queries](./pi-integration-contract.md) | Switch to a supported provider, drop the typed-query expressions, or wait for the deferred JSON-mode fallback in [Future Considerations](./future-considerations.md). | `provider '<provider>' (model '<model>') is outside the V1 typed-query supported set; typed queries will fail at runtime` |

<a id="loom-runtime-namespace"></a>
### `loom/runtime/*` — runtime panics, runtime-defect surface, and delivery failures

V1 has exactly six **panic sources** (the rows below tagged as panics). Each surfaces through the channels defined in [Errors and Results — Runtime panics](./errors-and-results.md) (slash-command system note for top-level looms; `Err(QueryError { kind: "invoke_failure", cause: "panic", ... })` to an `invoke` parent). One further code — `loom/runtime/internal-error` — covers the **runtime-defect surface** also defined in [Errors and Results — Runtime panics](./errors-and-results.md): an unanticipated interpreter or adapter throw outside the closed panic-source list, routed through the same channels as panics but not itself a panic source. The code is registered and stable; only its trigger condition is intentionally open-ended, which does not weaken the closed-registry rule (rule 2 above) — that rule constrains *codes*, not triggers. Six further codes — `system-note-delivery-failed`, `subagent-dispose-failure`, `registry-swap-failed`, `registration-cache-collision`, `reload-teardown-timeout`, and `active-set-restore-failed` — are delivery-, rebuild-, registration-, teardown-, or active-set-restore-failure diagnostics emitted, respectively, by the system-note fallback path, the subagent session lifecycle teardown, the watcher's build-aside-then-publish swap, the prompt-mode tool-registration cache's collision check, the `session_shutdown` handler's bounded await on in-flight subagent disposal, and the prompt-mode (and prompt → prompt cross-mode `invoke`) tool-active-set restore-failure protocol, all defined in [Pi Integration Contract](./pi-integration-contract.md). `reload-teardown-timeout` differs from the other five in delivery channel: because Pi's `ExtensionRuntime.invalidate(...)` may already have invalidated the `pi.*` surface by the time the teardown handler runs, the timeout diagnostic is emitted via `console.error` rather than through the standard persistent-diagnostic channel.

| Code | Sev | Phase | Trigger | Spec rule | Message template |
|---|---|---|---|---|---|
| `loom/runtime/match-error` | E | runtime | A `match` whose arms collectively fail to cover the scrutinee at runtime. | [Errors and Results — Runtime panics](./errors-and-results.md) | `MatchError: no arm matched <scrutinee summary>`. |
| `loom/runtime/index-out-of-bounds` | E | runtime | `arr[i]` with `i < 0` or `i >= arr.length`. | [Errors and Results — Runtime panics](./errors-and-results.md) | `index out of bounds: <i> not in 0..<length>`. |
| `loom/runtime/null-member-access` | E | runtime | `expr.field` where `expr` evaluated to `null`. | [Errors and Results — Runtime panics](./errors-and-results.md) | `null member access: .<field>`. |
| `loom/runtime/null-index-access` | E | runtime | `expr[i]` where `expr` evaluated to `null`. | [Errors and Results — Runtime panics](./errors-and-results.md) | `null index access: [<i>]`. |
| `loom/runtime/missing-object-key` | E | runtime | `obj[k]` where `k` is not a present loom-side name on `obj`. | [Errors and Results — Runtime panics](./errors-and-results.md) | `missing object key: <key>`. |
| `loom/runtime/invoke-depth-exceeded` | E | runtime | `invoke` chain (counting direct `invoke(...)`, `.loom` callable calls, and `.warp` `fn` invokes) exceeds depth 32 on a single chain. | [Invocation — Invocation depth bound](./invocation.md) | `invoke chain depth exceeded: <depth> > 32`. |
| `loom/runtime/system-note-delivery-failed` | E | runtime | `pi.sendMessage` for a `loom-system-note` threw synchronously (the surface returns `void`, so there is no rejection path); emitted by the fallback path after `ctx.ui.notify` has been attempted. `message` carries the original note's `content`; `hint` carries the underlying `sendMessage` error's message. | [Pi Integration Contract — System notes](./pi-integration-contract.md) | `system-note delivery failed: <original content first line>`. |
| `loom/runtime/registry-swap-failed` | E | runtime | The watcher's build-aside-then-publish rebuild of a changed `.loom`/`.warp` file or one of its transitive `.warp` importers threw mid-rebuild; the staging set was discarded and the prior `LoomRegistry` snapshot and AJV validator cache remain live. `message` names the failing path; `hint` carries the underlying error's message. | [Pi Integration Contract — Extension entry point](./pi-integration-contract.md) | `registry swap failed: <path>`. |
| `loom/runtime/internal-error` | E | runtime | The interpreter or an adapter it called threw an exception outside the closed V1 panic-source list (a host-function `TypeError`, an internal invariant violation, an unanticipated SDK reject — e.g. `createAgentSession` throwing or rejecting, or returning a handle whose `dispose` member is not a function, per [Pi Integration Contract — Conversation drive — subagent mode](./pi-integration-contract.md); also **post-probe SDK-shape drift** — a previously-probed Pi member that throws synchronously, returns a Promise that rejects, or returns a value of the wrong shape on a post-factory call (the snapshot-side `pi.setActiveTools`, the per-invocation `pi.sendUserMessage`, the per-call `ctx.modelRegistry.find` re-resolution of the binder model handle, etc.) per [Pi Integration Contract — Post-probe SDK-shape drift](./pi-integration-contract.md#post-probe-sdk-shape-drift), except where the four dedicated codes (`loom/runtime/active-set-restore-failed`, `loom/runtime/system-note-delivery-failed`, `loom/runtime/subagent-dispose-failure`, `loom/runtime/registry-swap-failed`) already apply; also catchable host allocation failures — `RangeError: Invalid string length`, `RangeError: Invalid array length`, `RangeError: Maximum call stack size exceeded`; also a non-conforming Pi tool return envelope observed by the lowering wrapper in [Pi Integration Contract — Tool execution from loom code](./pi-integration-contract.md#tool-execution-from-loom-code) — the resolved value is not an object, `content` is not iterable, an entry is missing `type` / `text`, etc. — in which case the diagnostic carries `details.kind = "tool-return-shape"`, `details.tool_name`, and a discriminator describing the offending shape (e.g. `typeof resolved`, `Array.isArray(content)`). *Uncatchable* host fatals — V8 heap-OOM via the `OOMErrorCallback` / `abort()` path — terminate the host process and emit no diagnostic; see [`spec.md` — Hard ceilings, NOCEIL-3](../spec.md#no-additional-ceilings) and [Errors and Results — Runtime panics](./errors-and-results.md). `message` is the underlying `error.message`; `hint` carries the underlying `error.stack` for operator triage (rendered as `"<no stack available>"` when `error.stack` is falsy). Routed alongside panics: a slash-command surface emits `"loom /<name> aborted with internal error: <message>"` as a system note; an `invoke` parent observes `Err(InvokeInfraError { kind: "invoke_failure", cause: "internal_error", ... })`. User session is not torn down. | [Errors and Results — Runtime panics](./errors-and-results.md) | `internal error: <error.message>`. |
| `loom/runtime/subagent-dispose-failure` | E | runtime | `AgentSession.dispose()` threw while the runtime was tearing down a subagent-mode invocation in its `finally` block. Emitted regardless of whether teardown was triggered by normal return, `Err`, panic, cancellation, or an unexpected exception. The original error that triggered teardown is **not** masked — the parent still observes it. `hint` carries the underlying `dispose` error's message. | [Pi Integration Contract — Subagent session lifecycle](./pi-integration-contract.md) | `subagent dispose failed: <dispose error first line>`. |
| `loom/runtime/registration-cache-collision` | E | runtime | Two distinct lowered tool `parameters` schemas produced the same schema slug; the runtime refused to dedup the registration. Emitted at `pi.registerTool` time with both synthesised names, both lowered-schema canonical-form bytes (truncated for the message; full bytes in `hint`), and the colliding slug. The runtime registers the second schema under a disambiguated name (see [Pi Integration Contract — Tool-registration lifetime and visibility](./pi-integration-contract.md)) and continues. | [Pi Integration Contract — Tool-registration lifetime and visibility](./pi-integration-contract.md) | `tool-registration cache collision on slug <slug>: <name1> vs <name2>`. |
| `loom/runtime/active-set-restore-failed` | E | runtime | The `pi.setActiveTools(snapshot)` call inside the prompt-mode (or prompt → prompt cross-mode `invoke`) snapshot/restore `finally` threw or rejected, and the runtime's mandated single re-attempt with the same snapshot also failed. The original exception (or terminal `Err`) the `finally` was protecting is propagated unmasked. `message` carries the underlying restore error's message; `hint` carries the snapshot tool names so an operator can manually restore via Pi's `/tools` interface. The runtime additionally emits a `display: true` `loom-system-note` whose `content` is the verbatim template defined in [Pi Integration Contract — Tool-registration lifetime and visibility](./pi-integration-contract.md). The retry MUST NOT chain back into `pi.setActiveTools` beyond the single re-attempt. Subagent-mode invocations cannot trigger this code; their tools are scoped to the spawned `AgentSession`. | [Pi Integration Contract — Tool-registration lifetime and visibility](./pi-integration-contract.md) | `failed to restore tool active-set after /<name>: <error>`. |
| `loom/runtime/cancelled-by-session-shutdown` | E | runtime | A `session_shutdown` handler call to `loomAbort.abort()` (per [Pi Integration Contract — Session-swap behaviour for in-flight invocations](./pi-integration-contract.md#session-shutdown-semantics)) cancelled this invocation, and the invocation's `disposeBarrier` settled cleanly inside the `SHUTDOWN_AWAIT_CAP_MS` window. Emitted from the invocation's own `finally` block (not from the teardown handler), exactly once per cleanly-cancelled invocation. `details.event.reason` carries Pi's `event.reason` typed as `"quit" \| "reload" \| "new" \| "resume" \| "fork" \| string` — the wider `string` arm is reserved for the unknown-reason path defined in PIC's **Unknown-reason rule** and carries the observed value verbatim (or the literal `"<unreadable>"` when the `event.reason` property access threw); the closed five-arm union covers every other case. `details.event.loom` carries the invocation's registered slash name without the leading `/`. `display: false` on the companion `loom-system-note` (operator-visible only via the structured payload, no inline transcript content). Routes through the standard `sendSystemNote` fallback chain (degrading to `ctx.ui.notify` and ultimately `console.error` per `loom/runtime/system-note-delivery-failed` if `ExtensionRuntime.invalidate(...)` has already run by emission time). An invocation either emits this code (clean-cancel path) or contributes to the single `loom/runtime/reload-teardown-timeout` `<list>` (still-in-flight path), never both. **Not** in the always-log set; the exclusion mirrors `cancelled` (operator-initiated, self-explanatory in context). | [Pi Integration Contract — Session-swap behaviour for in-flight invocations](./pi-integration-contract.md#session-shutdown-semantics) | `loom /<name> cancelled by session shutdown (<reason>)`. |
| `loom/runtime/reload-teardown-timeout` | E | runtime | The `session_shutdown` handler's bounded await on in-flight subagent disposal exhausted its `SHUTDOWN_AWAIT_CAP_MS (2000ms)` budget before every `disposeBarrier` settled. The handler proceeded to close watchers and detach forwarding listeners. `<ms>` in the message renders the *elapsed* wall time at timeout (`Clock.now() - start`), not the cap. `<N>` is the count of still-in-flight invocations. `<list>` is the comma-and-space-joined (`, `) sequence of `ActiveInvocationRegistry` entries in insertion order, each rendered as `/<slash-name>:<invocation-id>` where `<slash-name>` is the registered slash command name (without its leading slash) immediately following the literal `/` prefix, and `<invocation-id>` is the per-invocation UUID the runtime allocates at handler entry (canonical lowercase 8-4-4-4-12 hex form per V14a's `loom-direct:<uuid>` convention; uniform across slash, tool-execute, and child-`invoke` entry points). The diagnostic only fires when at least one `disposeBarrier` remains unsettled, so `<N> >= 1` and `<list>` is never empty by construction. `hint` carries the elapsed wall time. Emitted at most once per `session_shutdown` event. Routed via `console.error` rather than the persistent-diagnostic channel because Pi's `ExtensionRuntime.invalidate(...)` may already have invalidated the `pi.*` surface (see [Pi Integration Contract — Extension entry point](./pi-integration-contract.md)). | [Pi Integration Contract — Extension entry point](./pi-integration-contract.md) | `reload teardown timed out after <ms>ms; <N> invocation(s) still in flight: <list>`. |

Division by zero, integer overflow, and explicit author-driven panics are deliberately not in V1's panic catalogue — division yields IEEE-754 `Infinity` / `NaN` per the `number` rules in [Expressions](./expressions.md), and there is no `panic(...)` builtin. Adding any of those is a registry-level change per rule 2 above.

### `loom/host/*` — host-observed anomalies

Diagnostics in this namespace are emitted when the loom runtime observes Pi behaviour the spec's host-prerequisites contract does not enumerate. They route via `console.error` because the typical observation site is the `session_shutdown` teardown handler, where Pi's `ExtensionRuntime.invalidate(...)` may already have invalidated the persistent-diagnostic channel — the same delivery-sink carve-out as `loom/runtime/reload-teardown-timeout`.

| Code | Severity | Phase | Trigger | Anchor | Message |
|------|----------|-------|---------|--------|---------|
| `loom/host/session-shutdown-reason-unknown` | W | runtime | The `session_shutdown` handler observed an `event.reason` outside the closed set `{"quit", "reload", "new", "resume", "fork"}`, or the read of `event.reason` itself threw. The handler routes the unknown reason through the full five-sub-step teardown sequence per PIC's **Unknown-reason rule**; this diagnostic surfaces the drift so an operator (or a Pi-version-bump regression test) can observe it. Emitted exactly once per `session_shutdown` event, *before* sub-step 1 runs (so it survives a later throw from the handler). `details: { observed: <String(event.reason)> }` carries the offending value coerced to string — symbols, `undefined`, and objects coerce without throwing; on a throwing property access (where the value never materialised) `details.observed` is the literal string `"<unreadable>"`. Routed via `console.error` rather than the persistent-diagnostic channel because Pi's `ExtensionRuntime.invalidate(...)` may already have invalidated the `pi.*` surface (see [Pi Integration Contract — Extension entry point](./pi-integration-contract.md)). | [Pi Integration Contract — Extension entry point](./pi-integration-contract.md) | `session_shutdown event.reason outside closed set: <observed>`. |
| `loom/host/session-shutdown-teardown-step-failed` | W | runtime | A teardown sub-step inside the `session_shutdown` handler threw, was caught by the per-step `try`/`catch` mandated by PIC's **Per-step isolation** paragraph, and did not prevent the remaining sub-steps from running. Covers sub-step 1 (`LoomRegistry` drain), sub-step 4 (`discoveryWatcher.close()`, `settingsWatcher.close()`, the debounce-handle `Clock.clearTimeout(...)` call — three independent call sites, one potential emission each), and sub-step 5 (each enumerated listener-detach call — one potential emission per failing call). Sub-steps 2 and 3 retain their own per-entry isolation rules and do **not** emit this code. `details: { step: 1 \| 4 \| 5, call: <static call-site label>, error: <error.message> }` — the `call` label is a fixed string per call site (e.g. `"discoveryWatcher.close"`, `"settingsWatcher.close"`, `"Clock.clearTimeout(debounce)"`, `"ctx.signal.removeEventListener"`) so deduplication on `(code, details.step, details.call)` is meaningful across runs. When the captured throw is not an `Error` instance, `details.error` is `String(error)`, falling back to the literal string `"<unreadable>"` if that coercion itself throws (symmetric with `session-shutdown-reason-unknown`'s `details.observed` convention). Routed via `console.error` rather than the persistent-diagnostic channel because Pi's `ExtensionRuntime.invalidate(...)` may already have invalidated the `pi.*` surface (see [Pi Integration Contract — Extension entry point](./pi-integration-contract.md)). | [Pi Integration Contract — Extension entry point](./pi-integration-contract.md) | `session_shutdown teardown step <step> failed at <call>: <error>`. |
