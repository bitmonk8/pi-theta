---
list: parked
corpus: plan
order: bottom-up-by-location
entries:
  - { id: F-0197, tier: medium, file: docs/plan_topics/coverage-matrix.md, anchor: "\"`CIO` / `HC3` / `NOCEIL` / `CEIL` closure rationale\" and the Numbered REQ-IDs table", title: "HC3 and NOCEIL closing-leaf mappings live only in rationale prose, not in the coverage-matrix tables", note: "The fast fix-then-floor pass could not resolve it. Notes: Fast fix resolved the finding's navigability concern but introduced 2 at-or-above-floor (medium) regressions that are doc-vs-doc normative contradictions, not mechanically auto-fixable. The recommended fix (surface HC3-a..HC3-e and NOCEIL-1..4 as coverage-matrix table rows) directly contradicts conventions.md §REQ-ID discipline / §Leaf format, which state GOV-16 inline-label families \"add no coverage-matrix row of its own\" and classify NOCEIL as a \"no-single-leaf family\" closed by distributed seams. Resolution requires a human decision — either keep the mappings out of the table (the finding's premise is then wrong) or amend conventions.md to bless inline-label rows (a separate normative change). Routed back per the urgent reviewer's recommendation. Plan edits rolled back; no contradiction committed." }
  - { id: F-0205, tier: medium, file: docs/plan_topics/conventions.md, anchor: "*Sequential by default* enforcement posture; release-time residue-inspection item 3; *Per-phase TDD ritual* self-review", title: "`V8*` FileSystem seam-adapter exclusion names no module-path boundary", note: "The fast fix-then-floor pass could not resolve it. Notes: RESHAPE_NEEDED — shape:multiple/state:shaped finding could not be collapsed by the recommendation-applier: its ### Recommendation is conditional/ambiguous (applies Option C if the FileSystem adapter's synchronous-call surface is small/empty, Option B if it needs many sync call sites), deferring the choice to an unverified fact. Needs human resolution of the options before a fast fix can apply." }
  - { id: F-0298, tier: high, file: docs/plan_topics/V17a-cancellation-core.md, anchor: "full leaf", title: "V17a bundles four independent cancellation sub-mechanisms in one leaf", note: "The fast fix-then-floor pass could not resolve it. Notes: A1 recommendation-applier returned requires-human (recommendation does not unambiguously select one option); finding remains shape:multiple/state:shaped, so the fast loop cannot fix it. Picker emitted RESHAPE_NEEDED. Needs human reshape or a clear recommendation before it is fixable." }
  - { id: F-0218, tier: medium, file: docs/plan_topics/H5d-transitive-completeness-gate.md, anchor: Adds, title: "Closing-leaf cell tokenisation is never formally stated", note: "The fast fix-then-floor pass could not resolve it. Notes: Fast fix only partially resolved (FindingResolved: partial). The tokenisation step was declared and Tests/Ships-when fixtures added, but the stated rule \"take exactly the backtick-delimited code spans the cell contains\" does not actually ignore annotation prose — the PIC-17,18 closing-leaf cell's parenthetical contains backtick spans (e.g. `invoke`, `PIC-17`, `finally`), so the rule as written resolves a superset contradicting its own claimed {V9c, V15d}. The parse contract must restrict capture to leaf-ID-shaped tokens or exclude backticks inside parentheses. Needs human judgment; re-review should re-derive." }
  - { id: F-0209, tier: medium, file: docs/plan_topics/H4a-factory-shell-and-harness.md, anchor: Adds, title: "Manual real-host smoke governance protocol is oversized and misplaced in H4a's Adds", note: "The fast fix-then-floor pass could not resolve it. Notes: Fast fix only partially resolved the finding (FindingResolved: partial). The protocol was relocated to docs/plan_topics/real-host-smoke-gate.md and H4a/H6a/V18d inbound refs repointed, but H7a-integration-acceptance.md (~L10) still attributes \"the H4a manual real-host smoke run's pass criterion (e)\" to H4a, which no longer hosts the criterion. ≥floor stale-reference regression survives; re-review should re-derive and complete the single-home consolidation." }
---
<!-- Generated view. Entries are ordered by location and carry a renderer-owned projection (tier/file/anchor/title) of each finding; edit only membership: which ids appear and their defer_reason/note. Substance lives in docs/findings/F-NNNN.md. -->
