# pi-loom — Extension Specification

<a id="overview"></a>
## Overview

`pi-loom` is a [Pi Coding Agent](https://github.com/badlogic/pi-mono) extension that adds a domain-specific scripting language for prompts and agentic operations.

A `.loom` file interleaves code with literal text destined for the model. Loom evaluation appends turns to a conversation: the *caller's* current conversation in `prompt` mode, or a separate conversation in `subagent` mode. The state-isolation contract for subagent invocation is enumerated in [Pi Integration Contract — Subagent state-isolation matrix](./spec_topics/pi-integration-contract.md#subagent-state-isolation-matrix); the *callable set* concept that matrix references is defined in [Glossary — `callable set`](./spec_topics/glossary.md). Mode is selected per-loom by the required `mode:` frontmatter field — see [Parameters and Frontmatter](./spec_topics/frontmatter.md). On the success outcome, evaluation also produces a *final value* — the value of the loom's tail expression, or — if an explicit `return expr` executes first — the operand of that `return`, per [Function Definitions — Final value](./spec_topics/functions.md#final-value-language-definition) — consumed by `invoke` callers and propagated across the subagent boundary per [Invocation — Typed return](./spec_topics/invocation.md#typed-return). The loom language itself has no file-writing, network, or process-spawning primitive; effects of those kinds occur only through allowlisted tools, and the language's effect surface is owned by [Runtime Value Model — Effects](./spec_topics/runtime-value-model.md#effects).

<a id="terminal-outcomes-aggregator"></a>Loom evaluation produces one of three terminal outcomes: it succeeds (turns appended; final value available to programmatic callers), it fails (by returning `Err`, by panicking, or via a [query-terminating](./spec_topics/glossary.md#query-terminating) enforcement point of one of the runtime-class [hard ceilings](#hard-runtime-ceilings) below — query-terminating points route via panic or `Err`, with the per-arm classification *for ceiling #4* owned by [Hard Runtime Ceilings — Per-boundary destination/surface table (ceiling #4)](./spec_topics/hard-ceilings.md#ceiling-4-table); the following hard-ceiling cases are excluded from the Failure-cause enumeration: (a) binder argument-binding failure — including ceiling #4's slash-load `params` arm cross-routed through ceiling #3 per [Hard Runtime Ceilings — Per-boundary destination/surface table (ceiling #4)](./spec_topics/hard-ceilings.md#ceiling-4-table) (slash-load only; the `invoke(...)` `params` arm of ceiling #4 IS an evaluation Failure that surfaces as `Err(InvokeInfraError { cause: "validation", ... })` per [Invocation — Failures](./spec_topics/invocation.md)) — per the binder argument-binding failure item in [Errors and Results — Terminal outcomes](./spec_topics/errors-and-results.md#terminal-outcomes); see ceiling #3 in [Hard ceilings](#hard-runtime-ceilings), (b) ceiling #4's [in-loop](./spec_topics/glossary.md#in-loop) model-driven tool-call args row (which feeds a tool-error result back to the model rather than terminating the query)), or it is cancelled per [Cancellation](./spec_topics/cancellation.md). The cancellation signal is carried by the runtime-owned `loomAbort.signal` defined in [Pi Integration Contract — Cancellation source](./spec_topics/pi-integration-contract.md#cancellation-source); the runtime forwards Pi's per-handler `ctx.signal` (and the tool-exposed `signal` parameter, and parent-`invoke` signals) into `loomAbort` per the per-entry-point rules in [Cancellation — Forwarding into `loomAbort`](./spec_topics/cancellation.md) and [Pi Integration Contract — Cancellation source](./spec_topics/pi-integration-contract.md#cancellation-source). The closed enumeration of these three terminal outcomes — and the per-cause caller surface map (what an `invoke` parent sees, what a slash caller sees, what the operator (per [Glossary — operator](./spec_topics/glossary.md#operator)) observes per channel) — are owned by [Errors and Results — Terminal outcomes](./spec_topics/errors-and-results.md#terminal-outcomes); the *partial-append contract* (turns appended before the terminal event remain in the driven conversation; the runtime performs no implicit rollback; mid-stream user-visible streaming fragments are governed separately) is owned by [Errors and Results — Partial-append contract](./spec_topics/errors-and-results.md#partial-append-contract). Failures that occur before evaluation begins are owned, with their closed enumeration and per-cause routing rule, by [Errors and Results — Terminal outcomes](./spec_topics/errors-and-results.md#terminal-outcomes); they never become evaluation outcomes. The success / fail / cancelled trichotomy applies only once evaluation has begun.

<a id="file-extension-grammar"></a>A loom is stored in one of two file extensions that share a single grammar and type system. `.loom` files are invocable as slash commands (see [Slash-Command Invocation](./spec_topics/slash-invocation.md)); `.warp` files are library modules whose top level is restricted to the declaration forms enumerated in [Imports — Permitted top-level forms](./spec_topics/imports.md#permitted-top-level-forms) (currently five: `import`, `export`, `fn`, `schema`, `enum` — *orientation aggregator per [Governance — GOV-12](./spec_topics/governance.md)*). `.warp` files are never directly invoked. Path literals and discovery enforce these extensions per the rules in [Imports](./spec_topics/imports.md), [Invocation](./spec_topics/invocation.md), and [Discovery](./spec_topics/discovery.md); see those pages for the diagnostic codes that fire on each violation. See [Discovery — File-extension namespace](./spec_topics/discovery.md#file-extension-namespace) for the namespace-clearance note.

---

## Orientation

### Prerequisites

**Pi SDK and capabilities.** *Orientation; this paragraph is informative.* The host is `@mariozechner/pi-coding-agent`. The runtime additionally pins `@mariozechner/pi-agent-core`, `@mariozechner/pi-ai`, and `@mariozechner/pi-tui` as direct `peerDependencies` at the same tilde-pinned minor-version line as `pi-coding-agent` (the literal range is owned by [Pi Integration Contract — Host prerequisites — Pi SDK pin](./spec_topics/pi-integration-contract.md#pi-sdk-pin), which `spec.md` references by location rather than restating); the lock-step rule and the rationale for declaring all four explicitly (belt-and-braces against package managers that do not auto-deduplicate transitive peer-dep ranges) are owned by the same PIC section. The fifth Pi-bundled package the runtime imports, `typebox` (only `Type.Unsafe`, per [PIC — Inventory-closure audit — Target surface categories (typebox carve-out)](./spec_topics/pi-integration-contract.md#audit-target-surface-categories)), is declared as `"typebox": "*"` per Pi's bundled-package convention so the host's bundled version wins; the load-bearing host-shape check that `Type.Unsafe` is a callable function is owned by [Pi Integration Contract — Step 0 (e)](./spec_topics/pi-integration-contract.md#entry-capability-probe), which routes a missing or renamed member to `loom/load/host-incompatible` with `details.kind = "typebox-shape"`. At extension-factory entry the runtime runs the capability probe owned by [Pi Integration Contract — Step 0 (Capability probe)](./spec_topics/pi-integration-contract.md#entry-capability-probe), which is the single load-bearing check. The probe's algorithm, refusal rule, `details.kind` discriminators, and the `loom/load/host-incompatible` emission contract live entirely on that page; this paragraph carries no MUSTs of its own. The runtime's hand-authored TypeScript runtime source under `src/` (the *audited source tree*, equal to `src/**/*.ts` minus the excluded files enumerated by [PIC — Inventory-closure audit — Audit scope](./spec_topics/pi-integration-contract.md#audit-scope) — see that anchor for the precise file set, including which test, type-declaration, and brand-string-assertion files are excluded (non-`.ts` extensions like `.tsx` / `.js` / `.json` are out of scope by not matching the include glob, not by enumerated exclusion)) references no Pi surface outside `SDK_SURFACE_INVENTORY` or a declared `// allow-pi-surface:` exemption (per [PIC — Inventory-closure audit — Exemption mechanism](./spec_topics/pi-integration-contract.md#audit-exemption-mechanism)). Define *surface-set closure* as the property that every Pi-side surface reference in the audited source tree resolves to an `SDK_SURFACE_INVENTORY` entry, the typebox sibling allow-lists (`{ Type }` named-import allow-list and `{ Unsafe }` member-access allow-list, per [PIC — Inventory-closure audit — Target surface categories (typebox carve-out)](./spec_topics/pi-integration-contract.md#audit-target-surface-categories)), or a declared `// allow-pi-surface:` exemption. Surface-set closure is enforced by the [PIC — inventory-closure audit](./spec_topics/pi-integration-contract.md#sdk-cap-inventory-closure-audit). The *seven-capability cardinality* claim is a separate assertion mechanically enforced by [PIC step 2(a)](./spec_topics/pi-integration-contract.md#bump-step-2-positive)'s `CAPABILITY_OBLIGATIONS.length === 7` assertion against the `CAPABILITY_OBLIGATIONS` constant pinned in the extension module's pinned-constants block; the capability enumeration below and the PIC-side enumeration of items 1–7 are contributor-discipline restatements of that constant (per [PIC step 2(b) branch (2)](./spec_topics/pi-integration-contract.md#bump-step-2b-promote)'s catch-all co-edit obligation), not what the assertion inspects, and the cardinality claim is not enforced by the inventory-closure audit. The seven SDK capabilities the runtime depends on are anchored under [PIC — SDK capability inventory](./spec_topics/pi-integration-contract.md#sdk-capability-inventory); the bullets below name-link each item back to its anchored obligation:

- **Slash-command registration** — see [Pi Integration Contract — SDK capability inventory item 1](./spec_topics/pi-integration-contract.md#sdk-cap-slash-command-registration).
- **Prompt-mode conversation drive** — see [Pi Integration Contract — SDK capability inventory item 2](./spec_topics/pi-integration-contract.md#sdk-cap-prompt-conversation-drive).
- **Subagent-mode isolated session** — see [Pi Integration Contract — SDK capability inventory item 3](./spec_topics/pi-integration-contract.md#sdk-cap-subagent-isolated-session).
- **Tool registration and gating** — see [Pi Integration Contract — SDK capability inventory item 4](./spec_topics/pi-integration-contract.md#sdk-cap-tool-registration-gating).
- **Cancellation propagation** — see [Pi Integration Contract — SDK capability inventory item 5](./spec_topics/pi-integration-contract.md#sdk-cap-cancellation-propagation).
- **Custom-message channel and renderer** — see [Pi Integration Contract — SDK capability inventory item 6](./spec_topics/pi-integration-contract.md#sdk-cap-custom-message-renderer).
- **Binder LLM model** — see [Pi Integration Contract — SDK capability inventory item 7](./spec_topics/pi-integration-contract.md#sdk-cap-binder-llm-model).

The re-validation obligation that gates widening `peerDependencies` against this surface is owned by [Pi Integration Contract — SDK capability inventory — Re-validation on `peerDependencies` widening](./spec_topics/pi-integration-contract.md#sdk-cap-peer-dep-revalidation).

**Host runtime.** *Orientation aggregator (per [Governance — GOV-12](./spec_topics/governance.md)).* The loom runtime executes inside the Pi extension host process under three host preconditions. All three are operative rules whose detection / refusal / discriminator surfaces live entirely in [Pi Integration Contract — Step 0 (Capability probe)](./spec_topics/pi-integration-contract.md#entry-capability-probe); the bullets below carry only orientation context and forward-link there.

1. **Node version floor.** *Orientation; the operative rule lives in PIC.* The runtime requires Node `>=22.19.0`, verified equal to `@mariozechner/pi-coding-agent`'s `engines.node` floor by a build-time `package.json` `engines.node` literal-read test. The literal, the SemVer-comparison contract (the call to `semver.satisfies(...)` is the recommended non-normative recipe, not a library prescription), the `details.kind = "node-floor"` discriminator, and the `loom/load/host-incompatible` emission contract are all owned by [Pi Integration Contract — Step 0 (a)](./spec_topics/pi-integration-contract.md#entry-capability-probe); a Pi minor bump that moves the floor follows [Pi Integration Contract — Pi version bump procedure](./spec_topics/pi-integration-contract.md#pi-version-bump-procedure).

2. **Pi-supplied `AbortSignal` / `AbortController` shape.** *Orientation; the canonical member-with-kind enumeration lives in PIC.* The runtime requires the WHATWG `AbortSignal` and `AbortController` constructors and a small set of named members; the canonical enumeration with per-member kind tags (the source of truth the build-time SDK surface-inventory test consumes) lives at [Pi Integration Contract — Step 0 (b)](./spec_topics/pi-integration-contract.md#entry-capability-probe). Runtime call sites for each member are described in [Pi Integration Contract — Host prerequisites #4 and Cancellation source](./spec_topics/pi-integration-contract.md) and [Cancellation](./spec_topics/cancellation.md).

3. **Pi SDK named-capability surface.** *Orientation; the operative rule lives in PIC.* The seven SDK capabilities the runtime depends on are anchored under [Pi Integration Contract — SDK capability inventory](./spec_topics/pi-integration-contract.md#sdk-capability-inventory). Five (items 1, 2, 3, 4, 6) are factory-probable and verified at extension-factory entry; item 5 (cancellation propagation) is covered by obligation 2 above; item 7 (binder LLM model) is verified at per-loom load time and surfaces as `loom/load/binder-model-unresolved` per [Slash-Command Argument Binding — Strict-capability requirement](./spec_topics/binder.md#strict-capability-requirement), not as `loom/load/host-incompatible`. The detection algorithm, refusal rule, and the `details.kind` discriminator surface for the factory-probable subset are owned by [Pi Integration Contract — Step 0 (c)+(d)](./spec_topics/pi-integration-contract.md#entry-capability-probe).

Engine-level value-model assumptions (IEEE-754, native `Map`/`Set`, native `JSON.stringify`, `Object.is` equality) are NOT host preconditions in the probed sense; they are a non-checked invariant of the runtime value model and are owned by [Runtime Value Model — JavaScript engine assumptions](./spec_topics/runtime-value-model.md#javascript-engine-assumptions). V1 targets Node exclusively (Bun, Deno, browser-embed are out of V1 scope).

<a id="session-model"></a>*Session model.* The runtime's session-level contract decomposes into the eight sub-obligations below; each carries a stable page-local anchor (`sm-1-…` … `sm-8-…`) so inbound citations can pin a single sub-obligation without the others. The umbrella `session-model` anchor is preserved for orientation-level inbound links.

<a id="sm-anchor-scheme-stability"></a>*Anchor-scheme stability and citation guidance.* "Stable" here means that a published `sm-N-…` anchor is not reused for a different obligation; a future split of an SM-N (e.g. the in-place `sm-3` → `sm-3a`/`sm-3b` and `sm-7` → `sm-7a` … `sm-7e` decompositions already below) appends new sub-letters rather than renumbering existing siblings, and a retired SM-N's anchor is not recycled. *Orientation-level* inbound links — index entries, reading-order pointers, and aggregator references that cite the session model as a unit (e.g. the [V1 non-goals](#v1-non-goals) aggregator's mention below) — are the intended use of the umbrella `session-model` anchor; an inbound citation that pins a single sub-obligation should target the corresponding `sm-N-…` anchor instead. The same orientation-vs-obligation distinction extends to the SM-3 and SM-7 sub-umbrella anchors: `#sm-3-session-shutdown-handler` and `#sm-7-mode-qualified-concurrency` are orientation-level pointers over their respective sub-letters (`sm-3a`/`sm-3b` and `sm-7a` … `sm-7e`), and an inbound citation pinning a single sub-obligation should target the specific sub-letter anchor; a future sub-letter addition (e.g. an SM-7f) is therefore not implicitly aggregated into a pre-existing citation's scope. Pre-decomposition inbound `#session-model` callsites already in `docs/spec_topics/pi-integration-contract.md` and `docs/spec_topics/future-considerations.md` resolve via the preserved umbrella anchor; whether to retarget any individual callsite to a specific `sm-N-…` anchor is left to the topic-page edits that maintain those callsites, and is not asserted by this paragraph.

- <a id="sm-1-single-active-session-binding"></a> **SM-1 — Single-active-session binding.** A Pi extension instance is bound to exactly one active user session at a time per [Pi Integration Contract — Session-binding contract](./spec_topics/pi-integration-contract.md#session-binding-contract). Concurrent *user sessions* in the same host process are out of scope for V1 — the runtime *presupposes* a single active user session per extension instance rather than having audited Pi's full extension API surface for a concurrent-sessions facet, and the full presupposition framing and the build-time / editorial review surfaces that would catch a future Pi minor introducing such a facet are owned by [Future Considerations — V1 non-goals](./spec_topics/future-considerations.md#v1-non-goals), "No concurrent user sessions in the same host process".

- <a id="sm-2-closed-shutdown-reason-set"></a> **SM-2 — Closed `session_shutdown` reason set.** Pi fires `session_shutdown` (`SessionShutdownEvent` in `@mariozechner/pi-coding-agent`'s `dist/core/extensions/types.d.ts`) with `event.reason: "quit" | "reload" | "new" | "resume" | "fork"` (the closed normative set; the SDK type is the source of truth for both the payload path and the closed reason set, so a future SDK pin bump diff-audits against that interface). An `event.reason` outside that closed set (or one whose property access throws) routes through the full teardown sequence and emits one `loom/host/session-shutdown-reason-unknown` (W, runtime) diagnostic via the teardown-handler last-resort sink defined in [Pi Integration Contract — Extension entry point — Diagnostic-emission isolation](./spec_topics/pi-integration-contract.md#diagnostic-emission-isolation) (visibility of that sink is best-effort per PIC and a failing emission never unwinds the teardown handler); the validation, fail-safe routing, and `details.event.reason` widening on `loom/runtime/cancelled-by-session-shutdown` are owned by [Pi Integration Contract — Extension entry point, step 4](./spec_topics/pi-integration-contract.md).

- <a id="sm-3-session-shutdown-handler"></a> **SM-3 — Session-shutdown handler.** SM-3 groups the two handler-internal obligations that run inside the `session_shutdown` handler — the per-reason fixed teardown sequence (SM-3a) and the PIC-owned session-swap semantics for in-flight invocations (SM-3b, whose contract is owned by PIC rather than by the loom runtime's handler) — each carrying a stable page-local anchor (`sm-3a-…` / `sm-3b-…`) so inbound citations can pin a single sub-obligation without the other. The umbrella `sm-3-session-shutdown-handler` anchor is preserved for orientation-level inbound links.

  - <a id="sm-3a-teardown-sequence"></a> **SM-3a — Fixed teardown sequence.** The runtime's handler runs the full fixed teardown sequence — including watcher closure in sub-step 4 — for every reason in the closed set. The fixed teardown sequence itself is owned by [Pi Integration Contract — Extension entry point, step 4](./spec_topics/pi-integration-contract.md).

  - <a id="sm-3b-session-swap-semantics"></a> **SM-3b — Session-swap semantics for in-flight invocations.** Session-swap semantics for in-flight invocations — the abort-and-await sequence, the per-invocation operator-visibility surface, partial-append fate, and the `invoke`-parent observation rule — are owned by [Pi Integration Contract — Session-swap behaviour for in-flight invocations](./spec_topics/pi-integration-contract.md#session-shutdown-semantics).

- <a id="sm-4-session-only-degraded-state"></a> **SM-4 — Post-teardown degraded state for the session-only reasons.** On the *session-only reasons* (`event.reason ∈ {"new", "resume", "fork"}`) the runtime remains live afterwards in a deterministically degraded state until operator-issued `/reload`. The degraded-state contract is owned by [Pi Integration Contract — `reason: "new" | "resume" | "fork"` edge-case bullet](./spec_topics/pi-integration-contract.md#session-only-reason-degraded-state).

- <a id="sm-5-tag-transition-predicate"></a> **SM-5 — Tag-transition predicate scope.** PIC's **tag-transition predicate** covers the [session-only reasons](#sm-4-session-only-degraded-state) directly *plus* the snapshot-failure and unknown-reason-with-successful-snapshot paths. The post-sub-step-4 `LoomRegistry` drain-state transition and the `loom /<name>: extension degraded; /reload to recover` system note that fronts subsequent slash invocations (with editor-driven `.loom` and settings edits silently no-opping until recovery) follow the tag-transition predicate. The predicate definition and the per-path scoping for tag-transition and slash-note coverage are owned by [Pi Integration Contract — `reason: "new" | "resume" | "fork"` edge-case bullet](./spec_topics/pi-integration-contract.md#session-only-reason-degraded-state).

- <a id="sm-6-diagnostic-emission-predicate"></a> **SM-6 — Diagnostic-emission predicate scope.** PIC's **diagnostic-emission predicate** — narrower than [SM-5's tag-transition predicate](#sm-5-tag-transition-predicate) — is scoped to the [session-only reasons](#sm-4-session-only-degraded-state) AND a successful snapshot read of the SDK pinned-constant; the snapshot-failure path is excluded by the snapshot-success conjunct. The new `loom/host/session-shutdown-runtime-degraded` (W, runtime) diagnostic follows the diagnostic-emission predicate (the snapshot-failure and unknown-reason paths cover their own diagnostic-channel observability via `loom/host/session-shutdown-pinned-constant-unreadable` and `loom/host/session-shutdown-reason-unknown` rather than a co-emitted `runtime-degraded` row). The predicate definition and the per-path scoping for emission coverage are owned by [Pi Integration Contract — `reason: "new" | "resume" | "fork"` edge-case bullet](./spec_topics/pi-integration-contract.md#session-only-reason-degraded-state).

- <a id="sm-7-mode-qualified-concurrency"></a> **SM-7 — Mode-qualified isolation contract.** Concurrent loom invocations within a session carry **mode-qualified** isolation; the contract decomposes into the five sub-obligations below, each with a stable page-local anchor (`sm-7a-…` … `sm-7e-…`) so inbound citations can pin a single sub-obligation without the others. The umbrella `sm-7-mode-qualified-concurrency` anchor is preserved for orientation-level inbound links.

  - <a id="sm-7a-cancellation-independence"></a> **SM-7a — Cancellation independence across modes.** *Cancellation* is always independent: every invocation — prompt mode and subagent mode alike — carries its own `AbortController` (`loomAbort`) per [Cancellation — Signal source](./spec_topics/cancellation.md) and is tracked as a distinct entry in an extension-scoped registry of in-flight invocations whose normative shape, lifecycle, and teardown semantics live at [Pi Integration Contract — `ActiveInvocationRegistry`](./spec_topics/pi-integration-contract.md#active-invocation-registry).

  - <a id="sm-7b-subagent-mode-isolation"></a> **SM-7b — Subagent-mode transcript and tool-table isolation.** *Transcript and tool-table isolation* are **subagent-mode-only** — a subagent-mode invocation runs its own private `AgentSession` (no shared transcript or `tools:` table) per [Implementation Notes — Per-invocation single-threaded execution](./spec_topics/implementation-notes.md), while a prompt-mode invocation drives the user session's transcript by definition.

  - <a id="sm-7c-prompt-mode-sequential-execution"></a> **SM-7c — Prompt-mode sequential execution.** *Prompt-mode bodies execute strictly sequentially* within a single user session: at most one prompt-mode body — top-level slash dispatches and nested prompt → prompt `invoke(...)` calls alike — holds an open `pi.setActiveTools` snapshot/restore window at a time, supported by (i) <a id="sm-7c-slash-handler-serialisation"></a>Pi's per-session slash-handler serialisation pinned at [Pi Integration Contract — Tool-registration lifetime and visibility](./spec_topics/pi-integration-contract.md), (ii) <a id="sm-7c-load-time-prompt-callee-rejection"></a>the load-time rejection of prompt-mode `.loom` callees in another loom's `tools:` per [Parameters and Frontmatter — `tools`](./spec_topics/frontmatter.md#tools) (so the model cannot fan out parallel prompt-mode tool calls), (iii) <a id="sm-7c-invoke-suspension"></a>the rule, owned by [Invocation — Cross-mode semantics](./spec_topics/invocation.md), that `invoke(...)` to a prompt-mode callee suspends the parent's body until the child returns and that V1 exposes no parallel-`invoke` surface by which a parent could spawn sibling prompt-mode children concurrently, and (iv) <a id="sm-7c-subagent-to-prompt-fanout-closed"></a>the [Invocation — Cross-mode semantics](./spec_topics/invocation.md) rule closing the remaining `subagent → prompt` fan-out path into the user session. The three potential sources of in-session overlap therefore produce genuine concurrency **only between subagent-mode invocations**: parallel tool calls into the same `.loom` callable can target only subagent-mode callees (prompt-mode callees in `tools:` are rejected at load time per [Parameters and Frontmatter — `tools`](./spec_topics/frontmatter.md#tools)); sibling `invoke(...)` sites can overlap only when the children resolve to subagent mode (per [SM-7c-invoke-suspension](#sm-7c-invoke-suspension)); and independent slash dispatches against the same session cannot overlap at all per [SM-7c-slash-handler-serialisation](#sm-7c-slash-handler-serialisation).

  - <a id="sm-7d-no-cap-no-scheduler"></a> **SM-7d — V1 no-cap / no-scheduler disposition.** V1 concurrency disposition for in-flight invocations — no admission cap, no scheduler interposed above Pi's event loop — is owned by [Implementation Notes — No invocation cap](./spec_topics/implementation-notes.md#no-invocation-cap).

  - <a id="sm-7e-downward-only-cancellation"></a> **SM-7e — Downward-only cancellation propagation.** Cancellation propagates downward only (parent → children) per [Cancellation — Propagation](./spec_topics/cancellation.md); a sibling invocation's cancellation does not affect its siblings.

- <a id="sm-8-per-invocation-budget-non-sharing"></a> **SM-8 — Per-invocation budget non-sharing.** Per-invocation budgets (binder retry budget per [Slash-Command Argument Binding — Failure modes](./spec_topics/binder.md), `tool_loop.max_rounds` per [Hard ceilings](#hard-runtime-ceilings), `invoke`-chain depth) are **scoped to a single invocation** and are not shared, pooled, or replenished across sibling invocations.

### Scope

This subsection pins five cross-cutting V1 dispositions that no single topic page enumerates as a unit. The bullets are *informative orientation*: each one forward-links the topic page that owns the normative contract, and the two no-seam V1 dispositions recorded here as scope bullets — Trust boundary and Source-language stability — are also indexed under [Future Considerations — V1 non-goals](./spec_topics/future-considerations.md#v1-non-goals); the editorial criterion applied when promoting a no-seam V1 disposition to a standalone Scope bullet (applied as orientation guidance, not a normative rule, and not in the GOV-12 lock-step scope) is whether the disposition is *load-bearing for understanding the V1 design at the orientation level* — i.e. whether it constrains how a reader interprets every other Scope bullet (Trust boundary defines V1's privilege model; Source-language stability defines V1's release-process invariant); other no-seam dispositions are recorded only under the [V1 non-goals](#v1-non-goals) aggregator below; the consolidated V1 non-goals list (cross-cutting dispositions V1 ships without a forward-compatibility seam) is owned by [Future Considerations — V1 non-goals](./spec_topics/future-considerations.md#v1-non-goals) and forward-linked from [V1 non-goals](#v1-non-goals) below. The aggregator-vs-source lock-step convention for keeping these bullets honest as topic pages evolve is owned by [Governance — GOV-12](./spec_topics/governance.md).

- **Trust boundary.** V1 looms execute inside the Pi extension-host process at full Node host-process privilege. V1 imposes no loom-level sandbox: filesystem, network, and Pi-API access available to loom code and to its declared callables are exactly those available to any Pi extension running in the same process — Pi's `ExtensionAPI` and `ExtensionContext` surfaces (declared in `@mariozechner/pi-coding-agent`'s `dist/core/extensions/types.d.ts` across the release range pinned by [Pi Integration Contract — Host prerequisites — Pi SDK pin](./spec_topics/pi-integration-contract.md#pi-sdk-pin)) expose no per-extension privilege facet, and the resource-loader and tool-host surfaces grant capabilities at the host-process level; Pi exposes no per-extension privilege scoping the runtime can rely on as a security boundary. A future Pi minor that adds such a facet would surface at the build-time SDK surface-inventory assertion run by [Pi version bump procedure](./spec_topics/pi-integration-contract.md#pi-version-bump-procedure) before the spec contract drifts. The runtime itself interposes no additional access channels (sandbox, capability filter, mediated proxy) per [Pi Integration Contract — No additional access channels](./spec_topics/pi-integration-contract.md#no-extra-mediation). The loom's declared *callable set* (the entries listed under the `tools:` frontmatter field; default rule for absent or empty `tools:` is owned by [Parameters and Frontmatter — `tools`](./spec_topics/frontmatter.md#tools)) is a configuration knob over the *model's* reachable callable set, NOT a host-process sandbox: it is enforced mechanically by the per-mode wiring rule owned by [Pi Integration Contract — Tool-registration lifetime and visibility](./spec_topics/pi-integration-contract.md#tool-registration-lifetime-and-visibility), which owns both the per-mode composition of that set and the SDK-call mechanism. Any callable a loom declares exposes the full underlying capability of that callable to the loom's model; the runtime applies no privilege classification or filtering. `bash` and `read` are illustrative — the rule does not depend on a callable being deemed "privileged." Host-side denials of filesystem, network, or Pi-API access reach loom code only through the tool that issued the request, and silent success on denial is forbidden; the complete enumeration of observable `execute()` outcomes is owned by [Tool Calls — Failures](./spec_topics/tool-calls.md) and [Pi Integration Contract — Tool execution from loom code](./spec_topics/pi-integration-contract.md#tool-execution-from-loom-code). A per-loom capability model is **out of scope for V1**; the disposition is owned by the **No per-loom sandbox or capability model** bullet under [Future Considerations — V1 non-goals](./spec_topics/future-considerations.md#v1-non-goals).

- <a id="source-language-stability"></a> **Source-language stability.** A `.loom` or `.warp` file that loads cleanly under loom 1.0.0 is expected to load under every V1.x release and to produce, for any given input, identical (a) return values, (b) ordered diagnostic-code sequences, and (c) `loom-system-note` content strings. Wall-clock timing, token counts, and log-line volume are explicitly excluded from this expectation. This is a release-process goal, not a normative obligation; the equivalence statement, the V1.0 review-vs-mechanical-gate posture, and the deferred-conformance-suite framing are owned by [Governance — GOV-15](./spec_topics/governance.md#gov-15) with the companion review-posture rule at [GOV-14](./spec_topics/governance.md#gov-14). Introducing a new hard runtime ceiling within V1.x — or retiring or relaxing an existing one — is a recognised carve-out to that equivalence per [GOV-15](./spec_topics/governance.md#gov-15); the in-scope input set (defined post-hoc over the inter-release diff), the carve-out's diagnostic-emissions scope (per [GOV-15 — Ceiling-set carve-out](./spec_topics/governance.md#ceiling-set-carve-out)), and the attribution test that distinguishes carve-out-eligible edits from collateral edits are all owned by [GOV-15](./spec_topics/governance.md#gov-15). The long-term migration framing (no major-version migration mechanism) is owned by [Future Considerations — V1 non-goals](./spec_topics/future-considerations.md#v1-non-goals). Migration across major versions is out of V1 scope.

- **Runtime observability.** *Operator*-facing runtime failure events are emitted on the Pi `loom-system-note` channel via the *always-log set* (see [Glossary](./spec_topics/glossary.md)) defined in [Pi Integration Contract — Runtime event channel](./spec_topics/pi-integration-contract.md). On the success side, no `loom-system-note` event on this channel keys on the `Ok(v)` termination outcome itself — see the *Success-side null-policy* paragraph in [Pi Integration Contract — Runtime event channel](./spec_topics/pi-integration-contract.md) (central owner) and [Slash-Command Invocation — Once a loom is invoked](./spec_topics/slash-invocation.md) (per-mode operator-surface owner). Diagnostics for parse / load / type / runtime-panic batches share the same channel under a disjoint `details` shape (see [Diagnostics](./spec_topics/diagnostics.md)). Aggregation, latency histograms, per-loom token reports, and a consumer-facing read API are deferred (see [Future Considerations — Richer runtime-event telemetry](./spec_topics/future-considerations.md)).

- **Forward-compatibility seams.** V1 reserves 13 typed/structural seams in the runtime to enable a fixed inventory of deferred extensions backward-compatibly; each seam is pinned with a `> **V1 seam — <name>.**` blockquote on its owning topic page, and the inventory and per-seam contracts are owned by [Future Considerations — Surface extensions](./spec_topics/future-considerations.md#surface-extensions-v1-leaves-a-seam). Items with no dedicated topic-page seam (those riding the frontmatter unknown-key policy as a generic carrier, plus the upstream Pi-API-contingent items) are tracked separately under [Future Considerations — Surface extensions without a dedicated topic-page seam](./spec_topics/future-considerations.md#surface-extensions-no-dedicated-seam) and are not counted in the 13. Per [Governance — GOV-12](./spec_topics/governance.md), the count and the source list move in lock-step.

- <a id="hard-runtime-ceilings"></a> **Hard ceilings.** *Orientation aggregator (per [Governance — GOV-12](./spec_topics/governance.md)).* Each one-liner below uses per-ceiling role labels: ceilings #1 and #2 carry *bound owner* — the topic page that normatively sets the ceiling's bound (the depth limit, the `tool_loop` setting, etc.) — and *surface owner* — the topic page that normatively defines the breach surface (the diagnostic code, the `Err` variant, etc.). When both labels point to the same page, the bound and breach surface are co-located on that page. Ceilings #3 and #4 carry one or more labels naming the section(s) on the topic page(s) that own the relevant obligations. The four ceilings, with the per-ceiling routing-class one-liner each, are:

  1. **`invoke`-chain nesting depth 32** *(runtime)* — routing class: **runtime panic**. Bound owner: [Invocation — Invocation depth bound](./spec_topics/invocation.md); Surface owner: [Invocation — Invocation depth bound](./spec_topics/invocation.md).
  2. **`tool_loop.max_rounds`** per query, default 25 *(runtime)* — routing class: **recoverable `Err`**. Bound owner: [Parameters and Frontmatter — `tool_loop`](./spec_topics/frontmatter.md); Surface owner: [Errors and Results — QueryError variants](./spec_topics/errors-and-results.md#queryerror-variants).
  3. <a id="hard-ceiling-3"></a>**Binder per-class retry budget** *(load-time; not on the trichotomy)* — routing class: **load-time system note**. Sub-obligations HC3-a … HC3-e: [Hard Runtime Ceilings — Per-class retry budget (ceiling #3)](./spec_topics/hard-ceilings.md#hc3); operator-facing template-selection rule: [Slash-Command Argument Binding — Failure-mode templates](./spec_topics/binder.md#failure-mode-templates-normative).
  4. **JSON-document depth 5** *(runtime)* — routing class: **boundary-dependent**. Per-boundary destination/surface table: [Hard Runtime Ceilings — Per-boundary destination/surface table (ceiling #4)](./spec_topics/hard-ceilings.md#ceiling-4-table); Bound and breach-surface enforcement: [Schema Subset — Depth Enforcement](./spec_topics/schema-subset.md#depth-enforcement).

  <a id="ceiling-interaction-order"></a><a id="no-additional-ceilings"></a>Cross-ceiling content (the [ceiling-set invariants](./spec_topics/hard-ceilings.md#ceiling-set-invariants) — audience-coverage invariant and ceiling-#1 panic-uniqueness invariant, the binder per-class retry budget's HC3-a … HC3-e sub-obligations, ceiling #4's per-boundary destination/surface table, the [fixed evaluation order between ceilings (CIO-1 … CIO-6)](./spec_topics/hard-ceilings.md#ceiling-interaction-order), the `masked` field's closed identifier set and wire-location split, the [four NOCEIL-1 … NOCEIL-4 non-existence claims](./spec_topics/hard-ceilings.md#no-additional-ceilings), the worked consequences of the interaction order, and the four-axis [Audit methodology](./spec_topics/hard-ceilings.md#audit-methodology) that closes the NOCEIL-1 … NOCEIL-4 set) is owned by [Hard Runtime Ceilings](./spec_topics/hard-ceilings.md). Introducing, retiring, or relaxing a ceiling in this set is a recognised GOV-15 carve-out — see [GOV-15 — Ceiling-set carve-out](./spec_topics/governance.md#ceiling-set-carve-out) for the canonical scope (the parent rule lives at [GOV-15](./spec_topics/governance.md#gov-15)), named here in lock-step with that carve-out per [Governance — GOV-12](./spec_topics/governance.md).

<a id="loom-1-0-non-goals-aggregator"></a><a id="v1-non-goals"></a>
### V1 non-goals

*Orientation aggregator (per [Governance — GOV-12](./spec_topics/governance.md)).* The consolidated V1 non-goals list — cross-cutting V1 scope decisions where V1 ships without a forward-compatibility seam (a future revision adding the disposition is not anticipated by V1 and will require a migration) — is owned by [Future Considerations — V1 non-goals](./spec_topics/future-considerations.md#v1-non-goals); the seven items, separated by `;` so that the fan-out item's inner punctuation (commas and em-dashes) is visually distinguishable from the inter-item boundary, are: no per-loom sandbox or capability model; no formal source-language migration mechanism for major-version transitions; no non-Node JavaScript host support; no concurrent user sessions in the same host process; no parallel-`invoke` surface; no parallel fan-out *to* prompt-mode `.loom` callees (a prompt-mode `.loom` callable cannot appear in another loom's `tools:` table at all — rejected at load time per [Parameters and Frontmatter — `tools`](./spec_topics/frontmatter.md#tools) — so the model never sees one to fan out across, while Pi-host tools and subagent-mode `.loom` callees remain present in tool tables and may still be fanned out per [Tool Calls — Concurrency](./spec_topics/tool-calls.md#concurrency)); no admission cap on in-flight loom invocations.

The seam-vs-non-goal distinction is stated in category 4 of the intro list on [Future Considerations](./spec_topics/future-considerations.md); the canonical surface-extension inventory is owned by [Future Considerations — Surface extensions (V1 leaves a seam)](./spec_topics/future-considerations.md#surface-extensions-v1-leaves-a-seam) (e.g. per-call timeouts on queries / tool calls / invokes, the per-query overrides cascade). The aggregator-source page for this list (in GOV-12's *single-source-page* sense) is [Future Considerations — V1 non-goals](./spec_topics/future-considerations.md#v1-non-goals); the normative owners of each underlying disposition are a separate concept and remain on their topic pages; the [Prerequisites](#prerequisites) and [Scope](#scope) subsections and the [Session model](#session-model) paragraph above are themselves informative orientation aggregators, each forward-linking the topic-page sections it covers (per [Governance — GOV-12](./spec_topics/governance.md)), and each bullet's `Recorded at:` line in the [Future Considerations — V1 non-goals](./spec_topics/future-considerations.md#v1-non-goals) list cites the topic-page owner directly alongside any `spec.md` orientation cross-link. This aggregator and the [Future Considerations — V1 non-goals](./spec_topics/future-considerations.md#v1-non-goals) bullet list are kept in lock-step under [Governance — GOV-12](./spec_topics/governance.md); the Surface extensions cross-link above is a navigation pointer only and is not part of that lock-step scope.

### Reading order

Read these two topics first to understand the design:

- [Overview and Conceptual Model](./spec_topics/overview.md) — what a loom is, query-and-await, prompt vs. subagent mode.
- [Comparison with Existing Pi Features](./spec_topics/comparison.md) — loom vs. Pi `prompt` / `subagent`.

**Background (non-normative).** Skippable; explains design provenance, not requirements.

- [Influences](./spec_topics/influences.md) — what loom borrows from Rust, TypeScript, and what it doesn't.

---

## Language

Surface and semantics of the Loom language (shared by `.loom` and `.warp` files).

- [Lexical Structure](./spec_topics/lexical.md) — identifiers, keywords, comments, strings, numbers.
- [Type System](./spec_topics/type-system.md) — primitive, named, generic, union, literal, inline-object types.
  - [Schema Declarations](./spec_topics/schemas.md) — `schema X { ... }`, `schema X = ...`, `enum`, discriminated unions, recursion, wire-name renaming.
  - [Descriptions](./spec_topics/descriptions.md) — `///` doc comments, field separators.
  - [Schema Subset](./spec_topics/schema-subset.md) — JSON-Schema subset and lowering algorithm.
- [Parameters and Frontmatter](./spec_topics/frontmatter.md) — frontmatter fields, `params`, `tools`, `system`, `respond_repair`, template interpolation.
- [Query](./spec_topics/query.md) — `@`-templates, schema inference, respond-repair, `QueryError`.
- [Expression Sublanguage](./spec_topics/expressions.md) — supported forms, stdlib, operator precedence, grammar disambiguation, object/array construction.
- [Bindings and Mutability](./spec_topics/bindings.md) — `let`, `let mut`, reassignment.
- [Control Flow](./spec_topics/control-flow.md) — `if`, `for`, `while`, `break`, `continue`.
- [Errors and Results](./spec_topics/errors-and-results.md) — `match`, pattern grammar, `Result`, `?`, runtime panics.
- [Return Statement](./spec_topics/return.md) — `return expr` rules.
- [Function Definitions](./spec_topics/functions.md) — `fn`, hoisting, tail-expression returns.
- [Tool Calls](./spec_topics/tool-calls.md) — `<name>(args)`, `CodeToolError`.
- [Invocation](./spec_topics/invocation.md) — `invoke(...)`, cross-mode matrix, invoke errors, cycle detection.
- [Imports](./spec_topics/imports.md) — `.warp` library files, `import`/`export`, cycles.

---

## Extension Architecture

How loom integrates with the Pi runtime.

- [Pi Extension Integration](./spec_topics/pi-integration.md) — overall extension shape and index of subtopics.
- [Discovery](./spec_topics/discovery.md) — discovery sources, priority, cross-format collisions.
- [Slash-Command Invocation](./spec_topics/slash-invocation.md) — prompt-mode `Err` formatting, no-params overflow, call-chain note.
- [Slash-Command Argument Binding](./spec_topics/binder.md) — LLM-driven binder: model, context, envelope, defaulting, echo, failure modes.
- [Cancellation](./spec_topics/cancellation.md) — `AbortSignal` rules.
- [Diagnostics](./spec_topics/diagnostics.md) — diagnostic shape, code-registry rules, placeholder rendering, normative code registry, multi-error reporting.
- <a id="concurrency-model"></a> **Concurrency model.** The mode-qualified isolation contract for concurrent loom invocations within a session — covering cancellation independence ([SM-7a](#sm-7a-cancellation-independence)), subagent-mode transcript and tool-table isolation ([SM-7b](#sm-7b-subagent-mode-isolation)), prompt-mode sequential execution ([SM-7c](#sm-7c-prompt-mode-sequential-execution)), the V1 no-cap / no-scheduler disposition ([SM-7d](#sm-7d-no-cap-no-scheduler)), and downward-only cancellation propagation ([SM-7e](#sm-7e-downward-only-cancellation)) — is owned by the [Session model — SM-7a … SM-7e](#sm-7-mode-qualified-concurrency) sub-units (with the per-invocation budget non-sharing rule owned by the sibling [SM-8](#sm-8-per-invocation-budget-non-sharing) sub-unit).

---

## Implementation Notes

Implementer-facing notes about the runtime and Pi SDK contract.

- [Implementation Notes](./spec_topics/implementation-notes.md) — parser contract, runtime behaviour, schema-validation contract, single-threaded execution.
- [Runtime Value Model](./spec_topics/runtime-value-model.md) — JS representation of loom values, equality, wire-name translation.
- [Pi Integration Contract](./spec_topics/pi-integration-contract.md) — the named `@mariozechner/pi-coding-agent` surface the runtime depends on.
- [Hard Runtime Ceilings](./spec_topics/hard-ceilings.md) — cross-ceiling content (HC3-a…HC3-e, ceiling #4's per-boundary table, CIO-1…CIO-6, the `masked` field, NOCEIL-1…NOCEIL-4, worked consequences, the four-axis Audit methodology that closes the NOCEIL-1…NOCEIL-4 set); see also the four-item aggregator at [Orientation > Scope > Hard ceilings](#hard-runtime-ceilings).
- [Future Considerations](./spec_topics/future-considerations.md) — out-of-scope features.

---

## Appendix

- [Glossary](./spec_topics/glossary.md) — alphabetised list of coined terms with pointers to their canonical defining pages.
- [Grammar Appendix](./spec_topics/grammar.md) — normative productions for the literal sublanguage and the surface-syntax forms no single topic page owns.
- [Governance](./spec_topics/governance.md) — *spec-corpus governance, not implementation spec.* REQ-ID prefix table, retirement registry, the corpus-direction and binding-scope rules (GOV-17 / GOV-18), and the rules (GOV-1, GOV-3 through GOV-9, GOV-12, GOV-14 through GOV-16; GOV-2, GOV-10, GOV-11, GOV-13 retired) that govern REQ-ID coining, anchoring, and retirement.
- [Related Work](./spec_topics/related-work.md) — orchestration-layer and inference-layer neighbours.
