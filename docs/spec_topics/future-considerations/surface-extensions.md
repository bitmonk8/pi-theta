# Surface extensions

<a id="tooling-deferrals-no-theta-1-0-impact"></a>
## Tooling deferrals (no theta 1.0 impact)

- **LSP support** for `.theta` and `.thetalib` files (syntax highlighting, type checking, autocomplete).
- **`theta test` command** for dry-run execution that runs a theta against a recorded transcript or a stub model without hitting a live provider.
- **Richer runtime-event telemetry** — per-theta token aggregation, latency histograms, cost reporting, retention policies, and a consumer-facing read API for the `details: { event }` stream defined in [Pi Integration Contract — Runtime event channel](../pi-integration-contract.md). theta 1.0 ships only the per-occurrence emission and the `RuntimeEvent` payload shape; downstream consumers in theta 1.0 read from Pi's session transcript via existing surfaces.
- **Automated cadence for re-importing Pi patches** — theta 1.0 has no Renovate, no scheduled CI re-install, and no nightly cron that proactively re-imports candidate `@earendil-works/pi-coding-agent` patches into the theta contributor's typecheck cycle. The patch-skew window between a Pi patch landing on a user's machine and the theta-side snapshot edit shipping is therefore bounded only by contributor cadence; see [Pi Integration Contract — Patch-skew degradation contract](../pi-integration-contract/patch-skew-degradation.md#patch-skew-degradation-contract) for the theta 1.0 detection contract this defers.

---

<a id="surface-extensions-theta-1-0-leaves-a-seam"></a><a id="surface-extensions-v1-leaves-a-seam"></a>
## Surface extensions (theta 1.0 leaves a seam)

Each bullet below describes a deferred theta 1.0 extension and points at the normative paragraph that pins the theta 1.0 seam the extension consumes. The seams themselves live on the topic pages that own each surface; this page enumerates only what theta 1.0 chose not to do, and where to read the seam contract.

- **Per-call timeouts** on queries, tool calls, and invokes (theta 1.0 has only AbortSignal-driven cancellation; cf. [Cancellation](../cancellation.md)). The per-call options surface this item reserves is **partially consumed** as of theta 1.3: the call-site `with` clause ([Grammar Appendix — Call-site `with` clause](../grammar.md#call-site-with-clause)) surfaces `cwd` as the surface's first author-visible key on the tool-call and invoke surfaces, per [Invocation — Options surface](../invocation.md#options-surface); per-call `timeout` remains deferred and later slots in beside `cwd`, and the query-side options surface is unconsumed. Consuming one key neither adds nor removes an inventoried item nor any `> **theta 1.0 seam — <name>.**` blockquote — the item stays inventoried here, and the seam blockquotes pinning this surface stay in place (on the tool-call and invoke pages; the query-side blockquote, shared with the per-query-overrides item below, lives on [Query failure & repair](../query/query-failure-and-repair.md)) — so the "12 typed/structural seams" literal on [`spec.md` — Scope](../overview-and-orientation.md#scope) is unchanged, per [Governance — GOV-31](../governance/req-id-prefix-table-active-b.md#gov-31).
  *Anchored at:* [Query — Options surface](../query.md), [Tool Calls — Options surface](../tool-calls.md#options-surface), [Invocation — Options surface](../invocation.md#options-surface).
- **Pre-flight token-count check** before the runtime issues a user turn, so oversized rendered templates surface as `ContextOverflowError` without a wasted provider round-trip.
  *Anchored at:* [Query — theta 1.0 seam: pre-flight token-count nullability](../query/query-failure-and-repair.md#theta-1-0-seam-pre-flight-token-nullability) (the `tokens_used` / `tokens_limit` nullability is the seam; a pre-flight path may populate them from a local estimate without changing the variant shape).
- **Typed-query support for providers without named-tool forcing** (Gemini, OpenAI Responses, weak local models) via a JSON-mode / system-prompt fallback. theta 1.0 narrows typed queries to providers where pi-ai exposes named-tool forcing (per [Pi Integration Contract — Provider compatibility for typed queries](../pi-integration-contract.md)) and rejects unsupported providers with a load-time warning.
  *Anchored at:* [Pi Integration Contract — theta 1.0 seam: typed-query supported provider set](../pi-integration-contract/conversation-drive.md#theta-1-0-seam-typed-query-supported-provider-set).
- **Per-query overrides for `model`, `tools`, and `system`** (project → theta → query cascade).
  *Anchored at:* same query-options surface as per-call timeouts — [Query — Options surface](../query.md).
- **User-defined error types beyond `QueryError`.**
  *Anchored at:* [Errors and Results — theta 1.0 seam: discriminator type-openness](../errors-and-results/queryerror-variants.md#theta-1-0-seam-discriminator-type-openness) (one shared callout pins the seam for both this item and the `BinderError`-as-variant item below).
- **`BinderError` as a Theta-visible `QueryError` variant**, once thetas become first-class values invocable from non-theta programmatic harnesses that need to observe binder failures structurally.
  *Anchored at:* same discriminator-type-openness seam as user-defined error types — [Errors and Results — theta 1.0 seam: discriminator type-openness](../errors-and-results/queryerror-variants.md#theta-1-0-seam-discriminator-type-openness).
  *Depends on:* First-class theta values (for non-theta harnesses needing structured observation).
- <a id="binder-refinement-loop"></a>**Binder refinement loop:** multi-turn `needs_info` negotiation (binder asks the user a clarifying question, gets a reply, retries) instead of theta 1.0's single-shot "system note then stop" behaviour; the same future revision also begins surfacing `ambiguous.candidates` as a user-facing pick-one disambiguation rather than a terminating system note.
  *Anchored at:* [Binder — theta 1.0 seam: binder refinement loop](../binder/binder-bypass-and-envelope.md#theta-1-0-seam-binder-refinement-loop) (the three theta 1.0 carriers — envelope-schema three-arm discriminator, `ambiguous.candidates` field, distinct failure-arm template prefixes — are pinned with MUST there).
- **Named-argument / key=value invocation syntax.**
  *Anchored at:* [Invocation — Argument-binding style](../invocation.md) (the AST node carries a `style: "positional" | "named"` discriminator; theta 1.0 accepts only positional invocation syntax and emits only `"positional"`).
- **Richer expression sublanguage inside frontmatter `system:`** (full `${expr}` interpolation rather than just `${param}` paths).
  *Anchored at:* [Parameters and Frontmatter — theta 1.0 seam: `system:` expression sublanguage](../frontmatter/frontmatter-fields-b-and-templates.md#theta-1-0-seam-system-expression-sublanguage) (the `${…}` parser is the same entry point used by `@`...`` query templates, restricted at parse time to dotted paths by a parser-level filter rather than a hand-rolled regex; widening the filter is additive).
- **Package-style (`@scope/pkg`) and project-rooted (`/theta/...`) `import` paths** — theta 1.0 supports relative paths only.
  *Anchored at:* [Imports — theta 1.0 seam: `Resolver` interface](../imports.md#theta-1-0-seam-resolver-interface) (every `.thetalib` import flows through a single named `Resolver` seam; additional implementations may be registered without rewriting import-site code).
- <a id="mid-theta-user-session-replacement"></a>**Mid-theta user-session replacement.** A future feature that calls `ctx.newSession()`, `ctx.fork()`, or `ctx.switchSession()` from inside a running theta invalidates the factory-captured `pi` reference used by the prompt-mode driver. The future implementation will need to re-acquire `pi` via `withSession` before the next `sendUserMessage`.
  *Anchored at:* [Pi Integration Contract — theta 1.0 seam: mid-theta user-session replacement](../pi-integration-contract/conversation-drive.md#theta-1-0-seam-mid-theta-user-session-replacement) (the single-captured-reference MUST is pinned there; the theta 1.0 invariant "theta 1.0 thetas never trigger replacement" is the lifetime guarantee the seam preserves).
- <a id="symlink-resolution-hardening"></a>**Symlink-resolution hardening for invoke-path containment** — theta 1.0 implements the discovery-root containment check by `realpath`-then-`open`, accepting an irreducible kernel-level TOCTOU window between the two syscalls (see [Invocation — Resolution](../invocation.md)). A future pass replaces the two-step sequence with a single atomic resolve-and-open primitive — `openat2(..., RESOLVE_NO_SYMLINKS | RESOLVE_BENEATH)` on Linux, and the platform equivalents on macOS / Windows / other targets — closing the residual race without changing the theta 1.0-visible diagnostic codes (`theta/load/invoke-path-escape`) or `InvokeInfraError { cause: "load_failure" }` envelope.
  *Anchored at:* [Invocation — theta 1.0 seam: symlink-resolution hardening](../invocation.md#theta-1-0-seam-symlink-resolution-hardening) (the seam pins the identical-semantics + two-channel-diagnostic contract as a MUST and describes the single-named-function factoring as a non-normative convention; replacing its body is additive and does not perturb caller code).

### Surface extensions without a dedicated topic-page seam

<a id="surface-extensions-no-dedicated-seam"></a>

The five items below are **not** counted in the "Forward-compatibility seams" tally on [`spec.md` — Scope](../overview-and-orientation.md#scope): that tally's source set is the seams inventoried under [Surface extensions](#surface-extensions-v1-leaves-a-seam), and none of these five is inventoried there. Three ride [Parameters and Frontmatter — Unknown-key policy](../frontmatter.md) (or the closely related `tool_loop` field) as their forward-compatibility carrier: that policy provides forward tolerance for any unrecognised key but names no specific deferred field, and there is no per-item topic-page MUST that an implementer can fail to honour. Two are upstream Pi-API extensions whose theta 1.0 carrier is contingent on Pi extending its surface; if Pi never extends the surface, no theta 1.0 obligation activates. One of those two — the Pi-owned subagents item — nonetheless carries a defensive `> **theta 1.0 seam — <name>.**` blockquote (cited in its *Carrier* line) pinning the theta 1.0 collision-source-set invariant; because that blockquote pins an item in this sub-bucket rather than one inventoried under [Surface extensions](#surface-extensions-v1-leaves-a-seam), it is excluded from the 12-seam source set per [Governance — GOV-31](../governance/req-id-prefix-table-active-b.md#gov-31).

- **Per-theta `bind_temperature` knob**, if real usage shows authors need to tune the binder's nondeterminism budget.
  *Carrier:* [Parameters and Frontmatter — Unknown-key policy](../frontmatter.md).
- **Operator-level `theta.toolLoopMaxRounds` settings override** — theta 1.0 provides no operator-level fallback for `tool_loop.max_rounds`; the value is per-theta only. A future minor may add a `theta.toolLoopMaxRounds` settings key that supplies the default when `tool_loop:` is absent, mirroring the `bind_model` → `theta.binderModel` chain.
  *Carrier:* [Parameters and Frontmatter — `tool_loop`](../frontmatter.md) (the `tool_loop` field has no dedicated seam callout; the future settings key would be a new top-level configuration key, not a frontmatter widening).
- **User-overridable binder system prompt** — theta 1.0 fixes the binder prompt for predictability.
  *Carrier:* same unknown-key policy as `bind_temperature` — [Parameters and Frontmatter — Unknown-key policy](../frontmatter.md). The deferred extension also needs an injection point in the binder for an author-supplied prompt template; that injection point does not exist in theta 1.0.
- **Pi extension API: Pi-owned subagents exposed as enumerable slash commands.** Pi's `SlashCommandSource` union (`@earendil-works/pi-coding-agent`'s `core/slash-commands.d.ts`) is `"extension" | "prompt" | "skill"`; Pi-owned subagents are spawned via `createAgentSession(...)` and are not surfaced through `pi.getCommands()`, so the cross-format slash-name collision check in [Pi Integration Contract — Extension entry point](../pi-integration-contract.md) and [Discovery — Slash-name collision rules](../discovery.md) covers exactly three arms in theta 1.0.0. If a future Pi minor extends `SlashCommandSource` to include `"subagent"` (i.e. Pi-owned subagents become enumerable slash commands), the cross-format collision set widens to four arms and the cross-format collision test matrix gains a parallel `.md` subagent fixture.
  *Carrier:* [Pi Integration Contract — theta 1.0 seam: Pi-owned subagents collision source set](../pi-integration-contract/registration-steps.md#theta-1-0-seam-pi-owned-subagents-collision-source-set) pins the theta 1.0 invariant (the collision-candidate membership rule — `source ∈ {"prompt", "extension", "skill"}` — plus the future-widening seam) defensively; the future extension itself activates only if Pi extends `SlashCommandSource`, so no theta 1.0 forward-compatibility obligation rides this item beyond the invariant already captured at the cited callout.
- **Pi extension API: `argumentHint` field on `RegisteredCommand`** — Pi's built-in `.md` prompt templates carry an `argumentHint` that drives a distinct slot in the slash-command autocomplete dropdown, but extension-registered commands have no such slot. Theta's `argument-hint` frontmatter field is therefore consumed by the binder only in theta 1.0 (see [Slash-Command Argument Binding](../binder.md)). A future contributor either upstreams `argumentHint` to Pi's `RegisteredCommand` or revisits the theta 1.0 decision once Pi exposes the field.
  *Carrier:* [Parameters and Frontmatter](../frontmatter.md) (the binder simply has no hint to use beyond the frontmatter field) and [Slash-Command Invocation](../slash-invocation.md) (the `argument-hint` value is preserved on the theta's parsed AST and would only need to be threaded into the registration options).

### Surface extensions gated on an open design decision

<a id="surface-extensions-open-design"></a>

The item below is **not** counted in the "Forward-compatibility seams" tally on [`spec.md` — Scope](../overview-and-orientation.md#scope): unlike the committed deferrals inventoried under [Surface extensions](#surface-extensions-v1-leaves-a-seam), its committed theta 1.0 seam set is not yet settled, because the extension's eventual shape — and therefore which theta 1.0 carriers it composes with — depends on an open design decision (see the *Decision required* line on the item). Until that decision is made, the item reserves no committed forward-compatibility seam and so is excluded from the 12-seam source set per [Governance — GOV-31](../governance/req-id-prefix-table-active-b.md#gov-31). It nonetheless carries a defensive `> **theta 1.0 seam — <name>.**` blockquote (cited in its *Carrier* line) pinning a genuine theta 1.0 runtime invariant; because that blockquote pins an item in this sub-bucket rather than one inventoried under [Surface extensions](#surface-extensions-v1-leaves-a-seam), it is likewise excluded from the 12-seam source set, mirroring the Pi-owned-subagents blockquote in the sibling [Surface extensions without a dedicated topic-page seam](#surface-extensions-no-dedicated-seam) sub-bucket.

- **Automatic context escalation:** when binding fails without context, automatically retry with `bind_context: session` attached — trades a second binder call for a smoother success rate on context-sensitive thetas that forgot to opt in.
  *Carrier:* [Slash-Command Argument Binding — theta 1.0 seam: automatic context escalation](../binder/binder-model-and-context.md#theta-1-0-seam-automatic-context-escalation) pins the binder-invocation re-entrancy invariant defensively; the committed seam set for the extension itself is not settled until the *Decision required* line below resolves.
  *Depends on:* [Binder refinement loop](#binder-refinement-loop), only if escalation surfaces user-visible turns; otherwise independent.
  *Decision required before this item can be scoped:* whether automatic escalation surfaces a user-visible turn (composing with the [Binder refinement loop](#binder-refinement-loop)) or stays operator-only.

---

<a id="model-level-changes-no-theta-1-0-seam-expected"></a><a id="model-level-changes-no-v1-seam-expected"></a>
