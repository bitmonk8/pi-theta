# Version bump step2

    *Editorial-review checklist for unpinned host presuppositions.* On each Pi minor bump, the contributor MUST audit the candidate Pi minor against each of the loom 1.0 presuppositions enumerated in items (a)–(e) below, all five of which route their detection to editorial review under this procedure (the build-time SDK surface-inventory assertion is a presence check on the seven named SDK capabilities and does not enumerate behavioural invariants of these kinds), SHOULD additionally audit it against items (f) through (ag) below per each item's SHOULD-level scope and escalation clause, and MUST record the per-item audit outcome (pass / fail / N/A with one-line rationale; item (e) requires two sub-outcomes — see [(e.i)](#bump-checklist-slash-dispatch-serialisation-i) and [(e.ii)](#bump-checklist-slash-dispatch-serialisation-ii) below; items (f) through (ag) outcomes are recorded under the same shape when the SHOULD-level audit was performed) in the bump commit message:
    - (a) *Extension-instance survival across session-only `session_shutdown`* — sub-item (a) of [Host prerequisites for the degraded-state branch](./host-prerequisites.md#degraded-state-host-prerequisites) above.
    - (b) *Multi-`session_shutdown` delivery permitted to a single instance* — sub-item (b) of [Host prerequisites for the degraded-state branch](./host-prerequisites.md#degraded-state-host-prerequisites) above.
    - (c) *`/reload` and `ctx.reload()` recovery* — sub-item (c) of [Host prerequisites for the degraded-state branch](./host-prerequisites.md#degraded-state-host-prerequisites) above.
    - (d) *No Pi extension-host stdio-capture facet* — [Future Considerations — No reliance on a Pi extension-host stdio-capture facet](../future-considerations/model-changes-and-non-goals.md#pi-stdio-capture-facet).
    - <a id="bump-checklist-slash-dispatch-serialisation"></a>(e) *Per-session slash-command dispatch serialisation* — snapshot/restore behavioural precondition (b) of [Tool-registration lifetime and visibility — snapshot/restore Pi behavioural preconditions](./tool-registration-lifetime.md#snapshot-restore-pi-behavioural-preconditions) above. Diff the cited `AgentSession.prototype.prompt` body in `dist/core/agent-session.js` (the canonical baseline — i.e. the "from" side of the diff — is that file as it appears in the [loom 1.0 Pi-SDK pin](./host-prerequisites.md#pi-sdk-pin)'s `dist/*.js` snapshot on `@earendil-works/pi-coding-agent` from **Host prerequisites** above; the same loom 1.0-pin `dist/*.js` snapshot is the canonical baseline for every other `dist/*.js` artefact this recipe enumerates, so the diff is always loom 1.0-pin → candidate minor regardless of what the contributor happens to have installed locally) against the candidate minor, and enumerate every `session.prompt(...)` call site reachable from the per-mode entry point under the glob `dist/modes/*/*.js` in the candidate minor (the auditor MUST follow call-graph reachability from each per-mode entry rather than relying on literal substring grep on the file's text — the `session.prompt(...)` call may be wrapped in a callback registration during mode setup rather than appearing at the lexical top level of the file, so a literal grep can return zero direct matches at a driver file even when the call is reachable and is exactly the site the recipe means to enumerate) — the loom 1.0.0 driver set is the two files `dist/modes/interactive/interactive-mode.js` (where the loom 1.0 reachability path runs through the `Editor.onSubmit` callback registered during mode setup, cited under [snapshot/restore Pi behavioural preconditions](./tool-registration-lifetime.md#snapshot-restore-pi-behavioural-preconditions) above; a literal substring grep on this file may find zero direct `session.prompt(...)` matches at the loom 1.0 pin and the auditor MUST NOT record `pass` on [(e.ii)](#bump-checklist-slash-dispatch-serialisation-ii) on that basis alone) and `dist/modes/rpc/rpc-mode.js` (every `session.prompt(...)` call site reachable from that file's entry, neutrally pinned because PIC does not pin an rpc-mode class.method symbol), and any additional `.js` file matching the glob in the candidate minor that contains a `session.prompt(...)` call reachable from its entry point is audit-positive evidence of a new driver mode the cooperative-await argument under precondition (b) has not been load-tested against; a fail outcome — the auditor cannot demonstrate that the candidate minor's `prompt` body still establishes the [precondition (b)](./tool-registration-lifetime.md#snapshot-restore-pi-behavioural-preconditions) behavioural property above (i.e. that two loom-invocation slash dispatches against the same session cannot overlap their snapshot/restore windows — whether via the loom 1.0-pinned cooperative-`await` chain `prompt` → `_tryExecuteExtensionCommand` → `await command.handler(args, ctx)` end-to-end (which structurally witnesses the strictly stronger property of handler-body non-overlap and trivially establishes the narrower snapshot/restore-window non-overlap precondition (b) actually depends on, since the snapshot/restore window is bounded inside the handler body), or via any equivalent Pi-side mechanism that establishes the same non-overlap invariant; non-exhaustive examples of pattern shapes that trip the fail predicate include the `_tryExecuteExtensionCommand` step becoming fire-and-forget, the awaited dispatch being replaced by a queue whose per-session ordering is weaker than a serialised awaited dequeue (the comparator baseline — a per-session FIFO whose dequeue body is itself `await`ed end-to-end satisfies the non-overlap property and is a pass; a queue that dispatches the next handler before the previous handler's body resolves does not), the `await` being short-circuited around, handlers dispatched via `Promise.all` over a per-session batch, or microtask-deferred handlers whose bodies do not serialise; non-exhaustive examples of mechanism-preserving variants that satisfy the non-overlap property and are a pass include an explicit per-session lock wrapping the handler-body await chain, a single-consumer channel that dequeues each handler with `await` before pulling the next, and a microtask-deferred handler chain whose bodies still serialise via the same await semantics; **conservative-posture rule:** if the auditor cannot positively demonstrate the non-overlap property holds against the candidate minor — whether because the evidence trail has drifted, because the new shape's serialisation argument is unclear, or for any other reason — the audit outcome MUST be recorded as `fail`, never `N/A`), or any enumerated `session.prompt(...)` call site (in the loom 1.0.0 driver set files or in any newly-discovered glob match) dispatches without awaiting it before initiating the next, or any newly-discovered `dist/modes/*/*.js` file with a reachable `session.prompt(...)` call site (per [(e.ii)](#bump-checklist-slash-dispatch-serialisation-ii)'s call-graph reachability check) appears in the candidate minor, or the loom 1.0-cited evidence trail is no longer textually locatable in the candidate minor (the `AgentSession.prototype.prompt` body, the `_tryExecuteExtensionCommand` step inside it, the `Editor.onSubmit` site, or any of the cited `dist/*.js` paths cannot be identified by their loom 1.0 names or paths — e.g. because Pi has minified, re-bundled, renamed the `_`-prefixed symbol, or restructured the `dist/` tree, per the *known-fragile evidence* flagging in precondition (b) of [snapshot/restore Pi behavioural preconditions](./tool-registration-lifetime.md#snapshot-restore-pi-behavioural-preconditions) above — so the recipe cannot reach a pass verdict on textual diffability alone) **AND** the auditor cannot identify an equivalent reachable `session.prompt(...)` call site under [(e.ii)](#bump-checklist-slash-dispatch-serialisation-ii)'s call-graph reachability check from a per-mode entry point that supplies the same cooperative-await contribution as the un-locatable loom 1.0 site (e.g. for the `Editor.onSubmit` TUI-layer detail flagged as orthogonal under precondition (b) above, a renamed or replaced submit-pump callback whose body still awaits each `session.prompt(...)` call before initiating the next is the equivalent reachable site, and locating it satisfies [(e.ii)](#bump-checklist-slash-dispatch-serialisation-ii)'s reachability check and exempts the un-locatable loom 1.0 name from the textual-locatability fail trigger; the conservative-posture rule applies if the auditor cannot positively identify such an equivalent site) — requires adding a defensive per-extension-instance runtime mutex (wrapping the snapshot → swap → body → restore sequence and keyed on the factory-captured `pi: ExtensionAPI` reference — the only identity the loom holds at the snapshot/restore site, since `ExtensionCommandContext` is per-slash-handler-invocation and the user `AgentSession` is not exposed to prompt-mode looms; the per-extension-instance keying granularity is well-defined only while **both** the page-pinned one-`pi`-per-extension-instance lifetime (see the [loom 1.0 seam — mid-loom user-session replacement](./conversation-drive.md#v1-seam-mid-loom-user-session-replacement) paragraph above) AND the loom 1.0 one-active-user-session-per-extension-instance presupposition (pinned at *No concurrent user sessions in the same host process* in [Future Considerations](../future-considerations/model-changes-and-non-goals.md#v1-non-goals)) hold — together, on the recovery path, the keyed mutex is the unique serialisation point per user session (the mutex exists only when this recovery path is engaged on a fail outcome of this audit recipe — loom 1.0 ships without it on a pass outcome, per precondition (b)'s cooperative-`await` framing) — and a future Pi minor that weakens **either** keying-granularity invariant — the one-`pi`-per-extension-instance lifetime or the one-active-user-session-per-extension-instance presupposition — requires revisiting the keying granularity as part of this checklist item at the same time the precondition is re-validated (the closing "If either ... weakens" sentence under [snapshot/restore Pi behavioural preconditions](./tool-registration-lifetime.md#snapshot-restore-pi-behavioural-preconditions) above scopes only the two behavioural preconditions enumerated there and does not absorb the keying-granularity revisitation obligation)) in the same edit as the bump. The mutex's runtime contract — strictly serialising FIFO acquisition with no steady-state acquisition timeout, and a `session_shutdown` teardown interaction that is observed rather than separately bounded (the holder is cancelled by sub-step 2, releases on its `finally`, and a queued waiter falls through under sub-step 3's existing `SHUTDOWN_AWAIT_CAP_MS` budget) — is pinned at [snapshot/restore Pi behavioural preconditions — recovery-mutex runtime contract](./tool-registration-lifetime.md#recovery-mutex-runtime-contract) above and is not restated here. An `N/A` outcome on item (e) is defined specifically as: the candidate Pi minor has surfaced an explicit per-session dispatch-lock mechanism — i.e. a Pi-side construct (named per-session lock, single-consumer per-session dispatch channel, or other documented Pi-public surface) that establishes the [precondition (b)](./tool-registration-lifetime.md#snapshot-restore-pi-behavioural-preconditions) behavioural property (two loom-invocation slash dispatches against the same session cannot overlap their snapshot/restore windows) independently of the cooperative-`await` chain this recipe diffs, mooting the cooperative argument as the load-bearing source of the property. An `N/A` outcome under this definition is recorded as `N/A — superseded by <named Pi mechanism>` (the auditor names the Pi-side mechanism in the rationale slot), no recovery mutex is required, and both sub-outcomes **(e.i)** and **(e.ii)** below carry the same `N/A — superseded by ...` verdict (since the cooperative-`await` chain they enumerate is no longer the load-bearing source of the property). Item (e) covers two independently falsifiable sub-checks against distinct file groups, so its per-item rationale slot above MUST be recorded as two atomic sub-outcomes — <a id="bump-checklist-slash-dispatch-serialisation-i"></a>**(e.i)** *`AgentSession.prototype.prompt` body cooperative `await`* (the `prompt` → `_tryExecuteExtensionCommand` → `await command.handler(args, ctx)` chain in `dist/core/agent-session.js`) and <a id="bump-checklist-slash-dispatch-serialisation-ii"></a>**(e.ii)** *Per-mode driver-site cooperative `await`* (every `session.prompt(...)` call site reachable from the per-mode entry point under the `dist/modes/*/*.js` glob — the loom 1.0.0 driver set being `dist/modes/interactive/interactive-mode.js`, where the loom 1.0 reachability path is the `Editor.onSubmit` callback registered during mode setup rather than a lexical-top-level `session.prompt(...)` site, and `dist/modes/rpc/rpc-mode.js`, plus any newly-discovered glob match per the recipe above) — each carrying its own pass / fail / N/A verdict and one-line rationale, so a partial outcome (one sub-check passes, the other fails) is recorded unambiguously rather than collapsed under a single slot. The recovery-mutex prose above is stated once and applies to either sub-outcome: a fail (or non-positive verdict under the conservative-posture rule) on **(e.i)** or on **(e.ii)** independently triggers the defensive per-extension-instance runtime mutex in the same edit as the bump.
    - (f) *Session-binding contract — single-active-session guarantee* — [Session-binding contract](./host-prerequisites.md#session-binding-contract) paragraph in **Host prerequisites** above. The contributor SHOULD re-read the Pi-source paragraph cited under [Session-binding contract](./host-prerequisites.md#session-binding-contract) against the candidate Pi minor's lifecycle documentation and confirm the single-active-session guarantee pinned there still holds against the candidate minor. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed session-lifetime contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.
    - (g) *Pi-side slash-handler promise lifecycle presuppositions* — [Pi-side slash-handler promise lifecycle (consumption posture)](./host-interfaces-core.md#pi-slash-handler-promise-lifecycle-presupposition) paragraph under **Cancellation source** above. The contributor SHOULD re-read the candidate `@earendil-works/pi-coding-agent` minor's slash-command dispatch path — including the `AgentSession.prototype.prompt` body in `dist/core/agent-session.js` (the cooperative-`await` chain `prompt` → `_tryExecuteExtensionCommand` → `await command.handler(args, ctx)` already cited under [snapshot/restore Pi behavioural preconditions](./tool-registration-lifetime.md#snapshot-restore-pi-behavioural-preconditions) above and item (e) above) and any sibling site in the candidate minor that schedules or terminates the slash-handler's returned `Promise` — and confirm against the candidate minor that each of (i) the handler's returned `Promise` is awaited for the full invocation including any time after `ctx.signal` aborts, (ii) no internal deadline / force-resolve / abandon / detach is imposed on it, and (iii) `ctx.signal` is the only out-of-band Pi interaction with the in-flight handler still holds. If any of (i)–(iii) is falsified at the candidate minor, surface the divergence on the bump commit so the consumption-posture paragraph above can be amended in the same edit; PIC does not author the loom-side recovery for that divergence here. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed handler-promise-lifecycle contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.
    - <a id="bump-checklist-messages-chronological-order"></a>(h) *Loom-consumed `AgentMessage[]` chronological ordering (both delivering surfaces)* — [`SessionContext` and the `.messages` element shape](./host-interfaces-core.md#messages-chronological-order-presupposition) paragraph under **Host interfaces core** above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that both loom-consumed `AgentMessage[]` surfaces remain chronologically ordered oldest-to-newest: (1) the `SessionContext.messages` array `buildSessionContext(...)` returns, and (2) the `messages` field on the terminal `agent_end` variant of `AgentSessionEvent`. The [compact-transcript renderer](../binder/binder-model-and-context.md#compact-transcript-format-normative), the binder's [session-context truncation](../binder/binder-model-and-context.md#session-context-truncation-bind_context-session), and prompt-mode untyped-query [trailing-turn extraction](./conversation-drive.md#untyped-query-ok-extraction) consume surface (1), and subagent-mode untyped-query [trailing-turn extraction](./subagent.md#subagent-untyped-query-ok-extraction) consumes surface (2); all assume that order and neither the `AgentMessage[]` nor the `AgentSessionEvent` type encodes it. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed ordering contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.
    - <a id="bump-checklist-provider-overflow-wording"></a>(i) *Provider overflow-signature wording* — [Provider error mapping — provider-owned-wording presupposition](./provider-error-mapping.md#provider-overflow-wording-presupposition) paragraph above. The contributor SHOULD re-run the provider-error fixtures (the overflow-signature matches in the **Provider error mapping** table) against each supported provider's current error-body format and confirm each overflow signature still matches, since the matched text is provider-owned and is not gated by `@earendil-works/pi-ai`'s package version — a silent rewording downgrades a real overflow to `TransportError` with null token fields. Re-run the fixtures on each Pi minor bump and whenever a provider publishes an error-format or API-version change. The fixtures match the loom-side overflow signatures against a maintained corpus of captured provider error bodies in the repo's `npm test` suite rather than against live provider responses; keeping that corpus current — re-capturing each provider's error-body text when it publishes an error-format or API-version change — is the responsibility of the contributor performing the bump. This obligation is SHOULD-level only at loom 1.0; wiring the fixture re-run into the bump procedure as a mechanical CI gate is the post-loom 1.0.0 maintenance follow-up noted under the *Re-validation gate* in **Provider error mapping** above.
    - <a id="bump-checklist-slash-handler-liveness"></a>(j) *Slash-handler turn-liveness (eventual terminal `agent_end`)* — presupposition (iv) of the [Pi-side slash-handler promise lifecycle (consumption posture)](./host-interfaces-core.md#pi-slash-handler-promise-lifecycle-presupposition) paragraph under **Cancellation source** above. The contributor SHOULD confirm against the candidate Pi minor that a driven user turn (prompt mode) and a spawned `AgentSession` query (subagent mode) each still reach a terminal `agent_end` (`willRetry: false`) that settles `ctx.waitForIdle()` / resolves the session-local subscription, with no internal retry-cap or never-terminating path that abandons the turn without a terminal event. If falsified, surface the divergence on the bump commit so the consumption-posture paragraph and the prompt-mode *Hang handling* clause can be amended in the same edit; PIC does not author the loom-side recovery here. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed turn-liveness contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-model-registry-population"></a>(k) *Model-registry population before first load-pass `getAvailable()`* — [Host prerequisite for binder-model / `model:` load-time resolution](./host-prerequisites.md#model-registry-population-presupposition) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that `ctx.modelRegistry.getAvailable()` returns the fully-populated provider set — not a partial or still-warming view — by the time the first `session_start`-time per-loom load pass calls it, since binder-model and `model:` resolution are one-shot with no retry and a provider arriving after the load pass leaves the affected loom load-failed (`loom/load/binder-model-unresolved` or `loom/load/model-unresolved`) until `/reload`. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed registry-population-ordering contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-getcommands-completeness"></a>(l) *`pi.getCommands()` completeness at first `session_start`* — [Registration steps — first `session_start` collision-pass presupposition](./registration-steps.md#getcommands-completeness-presupposition) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that by the time Pi emits the first `session_start`, `pi.getCommands()` already enumerates every command Pi registers from prompt templates and skills, and that Pi's per-extension `session_start` emit order still lets a sibling extension register its commands either at factory time or in a `session_start` handler load-ordered before loom's — since step 3's first-pass cross-format collision check reads that snapshot once and a later-registering sibling silently misses the check (Pi disambiguates the duplicate with its own numeric-suffix form rather than loom emitting `loom/load/cross-format-collision`). This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed command-registration-ordering contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-prompt-mode-error-detection-fields"></a>(m) *Prompt-mode transport-failure `AssistantMessage` fields* — [Conversation drive — prompt-mode error detection](./conversation-drive.md#prompt-mode-error-detection) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor (and the `@earendil-works/pi-ai` it bundles) that the `AssistantMessage` type declared at `@earendil-works/pi-ai`'s `dist/types.d.ts` still carries both the `stopReason` field (whose value space still includes the `"error"` member the prompt-mode error-detection probe keys on) and the optional `errorMessage` field, and that a provider transport failure on a driven user turn still surfaces as `stopReason: "error"` on that turn's trailing `assistant` message. A silent rename, removal, or semantic change of either field — or of the `"error"` member of `StopReason` — would make the prompt-mode transport-failure probe miss real failures or misclassify them, with no SDK surface-inventory signal. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-agent-session-member-surface"></a>(n) *Loom-touched `AgentSession` member surface* — [`AgentSession` (member surface loom touches)](./host-interfaces-core.md#agentsession-interface) paragraph under **Host interfaces core** above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that the `AgentSession` handle declared at `dist/core/agent-session.d.ts` still carries the loom-touched members `dispose()`, `sendUserMessage(text)`, and `subscribe(listener)` with their pinned shapes. The `abort()` member is additionally factory-probed per [Step 0 (c)](./capability-probe.md#entry-capability-probe) above (a rename or drop surfaces as `loom/load/host-incompatible`), so it is covered there and not re-audited here. A silent rename or retype of `dispose`, `sendUserMessage`, or `subscribe` — none of which is factory-probed or enumerated by step 2(a)'s seven-capability literal-read assertion — would surface as a runtime `TypeError` at the subagent spawn / drive / teardown site rather than at load time or at the SDK surface-inventory assertion. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-subagent-spawn-satellite-types"></a>(o) *Subagent-spawn satellite types (`ResourceLoader`, `SessionManager.inMemory`, `DefaultResourceLoader`)* — [spawn-block satellite type pins](./subagent.md#subagent-spawn-satellite-types) under **Conversation drive — subagent mode** above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that (1) the `ResourceLoader` interface declared at `dist/core/resource-loader.d.ts` still carries the members the rule-4 adapter implements with compatible shapes — chiefly `getSystemPrompt()` (the sole `system:` delivery channel) and `getAppendSystemPrompt()`, plus the remaining members the adapter populates with empty / default / no-op values — (2) `SessionManager.inMemory` is still declared as `static inMemory(cwd?: string): SessionManager` at `dist/core/session-manager.d.ts`, and (3) the `DefaultResourceLoader` class and its `DefaultResourceLoaderOptions` constructor-options type (including the `systemPromptOverride` option) are still declared at `dist/core/resource-loader.d.ts`, so rule 4's rejection of the parent-session `DefaultResourceLoader` and of the `systemPromptOverride` construction channel still names a real Pi surface. Surfaces (1) and (2) are neither factory-probed nor enumerated by step 2(a)'s seven-capability literal-read assertion, so a silent member reshape on `ResourceLoader` or a move / rename of `SessionManager.inMemory` would surface as a runtime error at the subagent spawn site rather than at load time or at the SDK surface-inventory assertion. Surface (3) is not consumed by loom; it is re-validated here only so a Pi-side reshape that invalidates rule 4's rejection rationale is caught at the bump rather than going silently stale. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-estimatetokens-export"></a>(p) *`estimateTokens` named-export shape behind the `TokenEstimator` seam* — [`estimateTokens` (named export)](./host-interfaces-core.md#estimatetokens-named-export) paragraph under **Host interfaces core** above, the delegation target of the production `PiTokenEstimator` adapter behind [`TokenEstimator` seam (PIC-16)](./host-interfaces-services.md#tokenestimator-interface). The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that `estimateTokens` is still exported at the declaration cited in that paragraph with the pinned `(message: AgentMessage) => number` signature. The export is not factory-probed and is not enumerated by step 2(a)'s seven-capability literal-read assertion, so a silent rename, move, or signature change would surface as a runtime error at the `PiTokenEstimator` adapter rather than at load time or at the SDK surface-inventory assertion. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-pre-bind-and-renderer-resolution"></a>(q) *Pre-bind action-method throw closures and renderer-resolution behaviour* — [Registration steps — pre-bind throw-closure evidence](./registration-steps.md#pre-bind-throw-closure-evidence) above and [Extension bootstrap — Renderer registration](./extension-bootstrap-and-per-loom.md#renderer-registration) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that (1) the pre-bind extension runtime returned by `createExtensionRuntime()` still binds every session-state action method to a throwing closure that aborts factory load, with `Runner.bindCore()` (or its candidate-minor equivalent) overwriting the slots at bind time and the registration sinks plus `refreshTools` remaining callable before bind; and (2) Pi's renderer storage still overwrites last-wins on a duplicate `customType` (the loader `Map<string, MessageRenderer>`) and Pi's runner still resolves a `customType` by first-hit iteration in extension load order (`getMessageRenderer`). The loom 1.0-cited bundled symbols and `dist/*.js` paths for both surfaces are known-fragile evidence (a Pi patch may rename the `_`-prefixed loader / runner internals, re-bundle the `dist/` tree, or reword the throw string); where the cited names are no longer textually locatable, the auditor confirms the observable behaviours by an equivalent reachable site rather than recording `pass` on textual locatability alone. If either observable behaviour is falsified at the candidate minor, surface the divergence on the bump commit so the affected sites can be amended in the same edit; PIC does not author the loom-side recovery here. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-convert-to-llm-context-entry"></a>(r) *`convertToLlm` `CustomMessage`-to-LLM-context conversion* — [Custom-message channel persistence and LLM-context entry](./runtime-event-channel.md#custom-message-context-entry-presupposition) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor — by diffing the `convertToLlm` transformer in `dist/core/messages.js` against the [loom 1.0 Pi-SDK pin](./host-prerequisites.md#pi-sdk-pin)'s `dist/*.js` snapshot — that every `CustomMessage` (including a `display: false` `loom-system-note`) is still converted to a `{ role: "user", content }` provider-context entry on every subsequent provider call, and that neither `triggerTurn: false` nor the `display` flag excludes the message from the LLM context window. A silent change to this conversion — for example a new `CustomMessage` field that excludes such messages from context, or a transform that drops `display: false` notes — would falsify the operator-observability and durable-context claims in the cited paragraph with no SDK surface-inventory signal. The contributor SHOULD additionally confirm that the analogy referent `BashExecutionMessage.excludeFromContext` (declared at `dist/core/messages.d.ts`) still exists and that `CustomMessage` at that same declaration still exposes no equivalent context-exclusion opt-out, since the cited paragraph's no-opt-out claim is keyed on that analogy. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-settings-write-back-preservation"></a>(s) *Settings write-back key preservation for `loomPaths` and `looms.*`* — [Host prerequisites — Settings write-back key preservation](./host-prerequisites.md#settings-write-back-preservation-presupposition) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that Pi still preserves loom-owned keys when it serialises either settings file back to disk — i.e. a Pi-initiated rewrite of the project (`.pi/settings.json`) or global (`~/.pi/agent/settings.json`) file (for example via `/settings`) leaves the top-level `loomPaths` array and the `looms.*` namespace intact rather than stripping them as unrecognised keys. A silent change that drops unknown keys on write-back would erase loom configuration with no SDK surface-inventory signal, since the loom reads these keys itself and never observes Pi's serialiser. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-extension-runtime-invalidate"></a>(t) *`ExtensionRuntime.invalidate(...)` teardown sequencing and post-invalidation `pi.*` behaviour* — [Registration steps — step 4 `session_shutdown` teardown contract](./registration-steps.md#extension-runtime-invalidate-teardown) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that the `ExtensionRuntime` interface declared at `dist/core/extensions/types.d.ts` still carries the `invalidate(...)` member, that Pi still fires `session_shutdown` on the outgoing extension instance before it calls `ExtensionRuntime.invalidate(...)`, and that a `pi.*` call reaching an already-invalidated runtime still throws — the property the [`ActiveInvocationRegistry`](./active-invocation-registry.md#active-invocation-registry) teardown sequence and the five `console.error`-routed teardown diagnostics (per [Diagnostics — Persistent diagnostics](../diagnostics/diagnostic-shape.md)) depend on. Neither the member nor the post-invalidation throw behaviour is factory-probed or enumerated by step 2(a)'s seven-capability literal-read assertion, so a silent rename or behavioural change would surface as a runtime error mid-teardown rather than at load time or at the SDK surface-inventory assertion. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-complete-forced-tool"></a>(u) *`complete()` forced-tool behaviour and off-session-turn attachment* — [Conversation drive — `complete()` forced-tool behavioural presupposition](./conversation-drive.md#complete-forced-tool-presupposition) above. The contributor SHOULD re-run the per-provider forced-tool fixtures against each loom 1.0-supported provider (`anthropic-messages`, `openai-completions`, `mistral`, `amazon-bedrock`) and confirm that (1) `@earendil-works/pi-ai`'s `complete()` still forces the named tool when called with `options.toolChoice = { type: "tool", name }`, and (2) the call still attaches no turn to a concurrently-driven user or subagent `AgentSession`. Neither property is part of `@earendil-works/pi-ai`'s typed surface or enumerated by step 2(a)'s seven-capability literal-read assertion, so a silent regression of either would break every typed query and binder inference against the affected provider and surface only as the [typed-query diagnostic limitation](./conversation-drive.md#pic-typed-query-noncompliance)'s model-non-compliance misclassification rather than at load time or at the SDK surface-inventory assertion. The fixtures exercise the loom-side forced-tool path against a maintained corpus of captured per-provider `complete()` responses in the repo's `npm test` suite rather than against live provider calls; keeping that corpus current — re-capturing each provider's forced-tool and off-session-turn behaviour when it publishes an API-version change — is the responsibility of the contributor performing the bump. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-turn-lifecycle-event-delivery"></a>(v) *Turn-lifecycle event delivery and per-handler `ctx.signal` freshness (cancellation forwarding)* — presupposition (v) of the [Pi-side slash-handler promise lifecycle (consumption posture)](./host-interfaces-core.md#pi-slash-handler-promise-lifecycle-presupposition) paragraph under **Cancellation source** above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that Pi still delivers each of the five turn-lifecycle events the runtime's cancellation-forwarding handlers subscribe to — `tool_call`, `tool_result`, `message_update`, `turn_end`, and `agent_end` — to a subscribed extension during the active user turn for which `ctx.signal` was minted, and that `ctx.signal.aborted` / `ctx.signal.reason` read inside each of those handlers reflects the latest abort state for that turn. A silent rename, removal, gating, non-delivery, or per-handler `ctx.signal` semantic change of any of those events would degrade the Esc-during-`@`-query cancellation-forwarding path to a no-op with no build-time SDK surface-inventory signal. If falsified, surface the divergence on the bump commit so the consumption-posture paragraph above can be amended in the same edit; PIC does not author the loom-side recovery here. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed turn-lifecycle-event-delivery contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-cwd-project-root"></a>(w) *Factory-time `cwd == project root` launch contract* — [`FakeFileSystem` / `FileSystem` interface — factory-time `cwd == project root` presupposition](./host-interfaces-services.md#cwd-project-root-presupposition) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that Pi still launches the extension factory with `process.cwd()` set to the project root, so the value the production `PiFileSystem` captures once at construction is the directory the factory-time project-local discovery scan (`.pi/looms/` and project `.pi/settings.json`) is meant to resolve against. A silent change that launches the factory with `cwd` set elsewhere would resolve project-local looms and project settings against the wrong directory until the first `resources_discover` event arrives, with no build-time SDK surface-inventory signal. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed launch-cwd contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-teardown-session-swap-partition"></a>(x) *Teardown-vs-session-swap `event.reason` partition assignment* — sub-item (d) of [Host prerequisites for the degraded-state branch](./host-prerequisites.md#degraded-state-host-prerequisites) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that each `event.reason` literal still belongs to the same half of the teardown-vs-session-swap partition — `{"quit", "reload"}` tear the extension runtime down after the handler returns, `{"new", "resume", "fork"}` swap only the user session — since the [diagnostic-emission and tag-transition predicates](./session-only-degraded-state.md#session-only-reason-degraded-state) key on the partition assignment and the build-time `SessionShutdownEvent['reason']` type-equality assertion (per step 5 below) checks set membership only, not partition assignment. A Pi minor that moved a literal across the partition (e.g. making `"reload"` session-only, or `"fork"` always-tear-down) would silently invert the predicates with no build-time SDK surface-inventory signal. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed partition contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-dispose-mid-abort"></a>(y) *`AgentSession.dispose()` safe mid-unsettled-`abort()` (teardown overlap)* — [`dispose()`-mid-`abort()` consumption posture](./subagent.md#dispose-mid-abort-presupposition) under **PIC-9. Subagent session lifecycle** above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that calling `AgentSession.dispose()` while a prior `abort()` invoked by the cancellation listener is still unsettled remains safe, and that `abort()`'s underlying idle-wait still drives to completion through that subsequent `dispose()` call so the `SHUTDOWN_AWAIT_CAP_MS` budget covering the disposal phase remains sufficient. The `AgentSession` type surface encodes no ordering or lifetime relationship between an unsettled `abort()` and a later `dispose()`, so a silent change that made `dispose()` unsafe to call before `abort()` settles — or that detached the in-flight idle-wait from the `dispose()` path — would surface as a runtime hang or error at the subagent teardown site rather than at load time or at the SDK surface-inventory assertion. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-register-tool-post-startup"></a>(z) *`pi.registerTool` post-startup registration support* — [Registration steps — step 5 hot-reload registry-swap post-startup-registration presupposition](./registration-steps.md#register-tool-post-startup-presupposition) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that `pi.registerTool` still accepts a registration call issued after extension startup — i.e. from the watcher's debounced hot-reload rebuild on the bound runtime, not only from the extension factory — since step 5's build-aside-then-publish registry swap re-registers the tools synthesised from newly-distinct prompt-mode schema slugs through `pi.registerTool` long after the factory has run. The post-startup acceptance behaviour is neither factory-probed nor enumerated by step 2(a)'s seven-capability literal-read assertion (which checks only that `pi.registerTool` is present), so a Pi minor that restricted `pi.registerTool` to factory time would surface as a `loom/runtime/registry-swap-failed` diagnostic (see [Diagnostics](../diagnostics.md)) on the first content edit rather than at load time or at the SDK surface-inventory assertion. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-complete-retry-timing"></a>(aa) *`complete()` internal retry/backoff inter-attempt timing* — property (1) of the [Conversation drive — `complete()` retry-timing and cancellation behavioural presupposition](./conversation-drive.md#complete-retry-and-cancellation-presupposition) above. The contributor SHOULD re-run the per-provider retry-timing fixtures against each loom 1.0-supported provider (`anthropic-messages`, `openai-completions`, `mistral`, `amazon-bedrock`) and confirm that `@earendil-works/pi-ai`'s `complete()` still drives inter-attempt timing internally from its `StreamOptions.maxRetries` / `StreamOptions.maxRetryDelayMs` options — including honouring any server-requested wait such as an HTTP `Retry-After` — since the binder delegates all per-attempt backoff to `complete()` and implements no delay of its own (see the binder's [per-invocation retry budget](../binder/determinism-cancellation-failure.md#per-invocation-retry-budget)). Neither property is part of `@earendil-works/pi-ai`'s typed surface or enumerated by step 2(a)'s seven-capability literal-read assertion, so a silent regression would change the binder's observed inter-attempt latency with no load-time or SDK surface-inventory signal. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-complete-signal-honouring"></a>(ab) *`complete()` `options.signal` cancellation honouring* — property (2) of the [Conversation drive — `complete()` retry-timing and cancellation behavioural presupposition](./conversation-drive.md#complete-retry-and-cancellation-presupposition) above. The contributor SHOULD re-run the per-provider cancellation fixtures against each loom 1.0-supported provider (`anthropic-messages`, `openai-completions`, `mistral`, `amazon-bedrock`) and confirm that `@earendil-works/pi-ai`'s `complete()` still aborts the in-flight provider call and rejects the returned `Promise<AssistantMessage>` when the `AbortSignal` supplied as `options.signal` (the binder call passes `loomAbort.signal`) aborts, so the loom's [cancellation](../cancellation.md) chain tears the off-session binder call down rather than leaving it running to completion. The `options.signal` honouring is not part of `@earendil-works/pi-ai`'s typed surface or enumerated by step 2(a)'s seven-capability literal-read assertion, so a silent regression would leave a cancelled binder call running to its own resolution with no load-time or SDK surface-inventory signal. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-driven-turn-commit-ordering"></a>(ac) *Driven-turn session-commit ordering before `waitForIdle()` resolution* — [Conversation drive — driven-turn session-commit ordering (consumption posture)](./conversation-drive.md#driven-turn-commit-ordering-presupposition) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that by the time Pi settles `ctx.waitForIdle()` for a driven user turn — i.e. emits the terminal `agent_end` — the driven turn's trailing `assistant` message (including its `stopReason` and `errorMessage` fields) has already been committed to the session state the `ReadonlySessionManager` surface (`ctx.sessionManager.getEntries()` / `ctx.sessionManager.getLeafId()`) exposes, so the prompt-mode [error-detection probe](./conversation-drive.md#prompt-mode-error-detection) and [`Ok(string)` extraction](./conversation-drive.md#untyped-query-ok-extraction) read the completed turn rather than a still-pending one. This commit-vs-resolution ordering is not encoded by any typed contract on `ReadonlySessionManager` or the `waitForIdle()` surface and is not enumerated by step 2(a)'s seven-capability literal-read assertion, so a silent change that resolved `waitForIdle()` before flushing the turn would make the probe miss a real `stopReason: "error"` transport failure or extract an incomplete `Ok(string)` with no load-time or SDK surface-inventory signal. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-single-macrotask-abort-flip"></a>(ad) *Single-macrotask abort-flip (Esc-during-compute-bound-loop cancellation)* — presupposition (vi) of the [Pi-side slash-handler promise lifecycle (consumption posture)](./host-interfaces-core.md#pi-slash-handler-promise-lifecycle-presupposition) paragraph under **Cancellation source** above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that Pi still flips `loomAbort.signal.aborted` within a single macrotask — i.e. the abort runs synchronously inside one Pi-dispatched event handler — so the single macrotask turn the loop-iteration checkpoint yields before its signal-check (per [Cancellation — Granularity](../cancellation.md) and the [`Checkpoint` seam](./host-interfaces-services.md#checkpoint-seam) `loop-iter` production wiring) remains sufficient for a pending Esc to land before a compute-bound loop's next iteration. A Pi minor that deferred the flip across multiple macrotasks would let an Esc during a compute-bound loop be missed at the next iteration with no build-time SDK surface-inventory signal. If falsified, surface the divergence on the bump commit so the consumption-posture paragraph above and the loop-iteration checkpoint's single-macrotask-yield reasoning can be amended in the same edit; PIC does not author the loom-side recovery here. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed abort-flip-timing contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-privilege-scoping-facet"></a>(ae) *Per-extension privilege-scoping facet* — [`spec.md` — Orientation — Non-functional requirements — Trust boundary](../../spec/overview-and-orientation.md#non-functional-requirements) above and [Future Considerations — No per-loom sandbox or capability model](../future-considerations/model-changes-and-non-goals.md#loom-1-0-non-goals). The contributor SHOULD re-read the candidate `@earendil-works/pi-coding-agent` minor's `ExtensionAPI` and `ExtensionContext` declarations at `dist/core/extensions/types.d.ts` against the [loom 1.0 Pi-SDK pin](./host-prerequisites.md#pi-sdk-pin) and confirm no new per-extension privilege-scoping member has appeared. The loom 1.0 trust-boundary posture presupposes the absence of such a facet; this absence is neither factory-probed nor enumerated by step 2(a)'s seven-capability literal-read assertion (a presence check that does not walk `ExtensionAPI`'s member surface), so a Pi minor adding a privilege-scoping member (e.g. a hypothetical `pi.setExtensionPrivileges(...)`) would leave the spec's trust-boundary claim silently drifted with no build-time SDK surface-inventory signal. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed privilege-scoping contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-complete-onresponse-delivery"></a>(af) *`complete()` `onResponse` provider-response delivery* — [Provider error mapping — classifier input surface](./provider-error-mapping.md#classifier-input-surface) above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-ai` minor that `complete()` still invokes the `StreamOptions.onResponse` callback with a `ProviderResponse { status, headers }` after an HTTP response is received and before its body stream is consumed, for each loom 1.0-supported provider (`anthropic-messages`, `openai-completions`, `mistral`, `amazon-bedrock`) that produces an HTTP response, since the provider-error classifier reads its HTTP-status class from that callback and treats the absence of an `onResponse` invocation before a `stopReason: "error"` resolution as the network-level case. Neither the `onResponse` invocation nor `ProviderResponse`'s shape is part of `@earendil-works/pi-ai`'s typed surface or enumerated by step 2(a)'s seven-capability literal-read assertion, so a silent regression would misclassify HTTP-status-class failures — or collapse a real HTTP error into the network-level branch and over-flag `TransportError.retryable: true` — with no load-time or SDK surface-inventory signal. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    - <a id="bump-checklist-messages-leading-user"></a>(ag) *Loom-consumed `AgentMessage[]` leading-`user`-message guarantee (both delivering surfaces)* — [`SessionContext` and the `.messages` element shape](./host-interfaces-core.md#messages-leading-user-message-presupposition) paragraph under **Host interfaces core** above. The contributor SHOULD confirm against the candidate `@earendil-works/pi-coding-agent` minor that both loom-consumed `AgentMessage[]` surfaces remain non-empty-iff-first-element-is-`UserMessage`: (1) the `SessionContext.messages` array `buildSessionContext(...)` returns, and (2) the `messages` field on the terminal `agent_end` variant of `AgentSessionEvent`. The binder's [compact-transcript renderer](../binder/binder-model-and-context.md#compact-transcript-format-normative) and [session-context truncation](../binder/binder-model-and-context.md#session-context-truncation-bind_context-session) turn-grouping rule is total over the array only under this guarantee; a Pi minor that prepended a leading `assistant`, `toolResult`, or `custom` message (e.g. a session-resumed-from-snapshot feature that emits a `loom-system-note` before the first user input) would leave that leading run belonging to no turn, with no `AgentMessage[]` type signal. This obligation is SHOULD-level only at loom 1.0; it escalates to a build-time pin (on the same footing as the seven-capability literal-read assertion under step 2(a) above) once Pi exposes a typed contract that step 2(a)'s surface-inventory probe can mechanically verify, at which point the editorial audit prescribed here is superseded by the mechanical gate.

    Any future presupposition added to this page or to [`future-considerations.md`](../future-considerations.md) that routes its detection to editorial review under this procedure MUST be added to this checklist in the same edit; landing such a presupposition without extending the checklist leaves the detection mechanism reliant on contributor recall and is non-conformant with this step.
2. **Re-run the SDK surface-inventory tests.** The build-time test surface bundles two co-numbered assertions whose inputs and resolution recipes are distinct; PIC pins them as <a id="bump-step-2-positive"></a>**(a)** and <a id="bump-step-2-negative"></a>**(b)** below so a failing red can be unambiguously routed.

    **(a) Positive direction — literal-read of the imported namespace.** The literal-read assertion imports `@earendil-works/pi-coding-agent` at the candidate version and asserts each of the seven named SDK capabilities enumerated in [SDK capability inventory](./inventory-audit-intro.md#sdk-capability-inventory) above is present on the imported namespace. A red here means a capability has been removed or renamed; resolve by updating both the `SDK_SURFACE_INVENTORY` entry and the corresponding capability obligation in [SDK capability inventory](./inventory-audit-intro.md#sdk-capability-inventory) (and the matching probe-side description and the `spec.md` cross-reference list) in the same edit before the bump. Step 2(a) MUST additionally assert that the `CAPABILITY_OBLIGATIONS` constant has exactly seven entries (`CAPABILITY_OBLIGATIONS.length === 7`). `CAPABILITY_OBLIGATIONS` is a separate constant in the pinned-constants block, distinct from the [Step 0 (c)](./capability-probe.md#entry-capability-probe) factory-probable capability list (which is a five-element array of probe-time capability names — capabilities 1, 2, 3, 4, and 6 — whose entries enumerate the ten `typeof <path> === "function"` namespace-function paths that step (c) probes). The two constants have different cardinalities (7 vs 5), different runtime consumers (the literal-read assertion of step 2(a) above vs the [Step 0 (c)](./capability-probe.md#entry-capability-probe) capability probe), and different bump-procedure groupings (step 5 grouping (iv) below vs step 5 grouping (i) below). The left-hand side of that count-equality comparison is sourced from the `CAPABILITY_OBLIGATIONS` constant — a closed array of the seven anchor IDs [`sdk-cap-slash-command-registration`](./capability-inventory-items.md#sdk-cap-slash-command-registration) … [`sdk-cap-binder-llm-model`](./capability-inventory-items.md#sdk-cap-binder-llm-model), pinned in the same source-of-truth pinned-constants block in the extension module that holds the four Step-0 probe constants — and the assertion compares `CAPABILITY_OBLIGATIONS.length === 7`; adding an 8th capability obligation therefore requires extending `CAPABILITY_OBLIGATIONS` and bumping the integer literal in the same edit per [step 2(b) branch (2)](./version-bump-step2b.md#bump-step-2b-promote)'s co-edit checklist below. This assertion is the mechanical gate against drift between the `CAPABILITY_OBLIGATIONS` constant and the integer literal `7`. Drift between the markdown items-1–7 enumeration and the natural-language `"seven"` restatements elsewhere (this section's preamble sentence "The runtime depends on the seven named SDK capabilities enumerated below", the [`spec.md` — Orientation — Prerequisites](../../spec/overview-and-orientation.md#orientation) sentence "The seven SDK capabilities the runtime depends on …", and any other cardinality-bearing prose on the seven count) is contributor-discipline; [step 2(b) branch (2)](./version-bump-step2b.md#bump-step-2b-promote) below's catch-all co-edit obligation is the contributor-side mitigation. A red on this assertion means `CAPABILITY_OBLIGATIONS.length` and the integer literal `7` have diverged, and is resolved by [step 2(b) branch (2)](./version-bump-step2b.md#bump-step-2b-promote) below (which co-edits the integer literal alongside the inventory in the same commit). An unrecognised-`kind` literal-read failure — emitted by the routable-discriminator obligation pinned under [Target surface categories](./audit-recognised-shapes.md#audit-target-surface-categories) above when an `SDK_SURFACE_INVENTORY` entry's `kind` field is not recognised by the literal-read assertion's branch table — is resolved by **adding a recognising branch to the literal-read assertion's branch table for the new `kind`, in the same edit that adds the new entry** (on the same footing as [step 2(b) branch (2)](./version-bump-step2b.md#bump-step-2b-promote) below's promote-and-co-edit obligation, and consuming the new entry's per-kind payload field shape from the implementation's inventory the same edit lands per *Target surface categories* above). Step 2(a)'s (the SDK surface-inventory literal-read assertion's) import enumeration MUST cover `SessionShutdownEvent` as a type import; the existing `type-union-snapshot` entry at `path: "SessionShutdownEvent.reason"` satisfies this obligation by naming the type via its `path` field, so no new entry kind is introduced and the seven-capability count above is not perturbed. This sub-step remains the single owner of import-specifier re-pinning for both the seven function capabilities and that existing `type-union-snapshot` entry: a TS230x-family module-import-resolution failure (`TS2305`, `TS2724`, `TS2307`, `TS2614`, or any other TS code in the same family) routed here from step 5 below — including one originating from step 5's `SessionShutdownEvent['reason']` brand-string assertion when a Pi patch drops or renames `SessionShutdownEvent` on the package-root re-export surface — is resolved by re-pinning that type's import specifier (to whatever sub-export, or deep `dist/...` path as a last resort, the candidate Pi minor exposes the type on) in the same edit as the rest of the bump. *Category-(3) drift contributor obligation.* Step 2(a) additionally pins a contributor obligation against category-(3) staleness on a Pi minor bump (the bump-time analogue of the *Target surface categories* (3) framing under [Inventory-closure audit](./inventory-audit-intro.md#sdk-cap-inventory-closure-audit) above, which makes the SDK type declarations the single source of truth for category (3)'s closed in-scope set): on each bump, the contributor MUST cross-reference the candidate `@earendil-works/pi-coding-agent` minor's `.d.ts` declarations of `ExtensionContext` and `ExtensionCommandContext` against the `SDK_SURFACE_INVENTORY`'s category-(3) entries. A new SDK-side member the runtime does not yet consume is **not** required to be added to the inventory at the bump (the inventory tracks consumed surface, not the entire SDK type's member set); a new member the runtime starts to consume in the same bump MUST be added to `SDK_SURFACE_INVENTORY` as a category-(3) entry under [step 2(b) branch (2)](./version-bump-step2b.md#bump-step-2b-promote)'s promote arm in the same edit, on the same footing as any other newly consumed surface that branch (2) catches. This obligation is contributor-discipline rather than a type-aware mechanical gate (a mechanical gate would commit step 2 to a TypeScript-program load PIC declines to require), and is the bump-time counterpart to the [step 2(b) branch (2)](./version-bump-step2b.md#bump-step-2b-promote) promote arm that the inventory-closure audit catches at non-bump times. The literal-read assertion's branch table arm for category-(3) entries SHOULD additionally include a smoke check that asserts every category-(3) entry's `path` member-name segment (per the [*Category-(1)/(3) inventory join key (per-category specialisation)*](./audit-resolution.md#audit-category-1-3-inventory-join-key) sub-paragraph above, which pins the category-(3) lookup as a rightmost-dot-separated-segment match between the SDK type's member name and the entry's `path` field) still resolves on the imported `ExtensionContext` and `ExtensionCommandContext` types — for example, a per-entry `"<member>" in <importedExtensionContextLikePrototype>`-equivalent check, or its type-side analogue — so a Pi minor that drops or renames a category-(3) member the inventory enumerates surfaces as a literal-read failure rather than as a silent stale entry the contributor obligation above did not catch. The smoke check is `SHOULD` rather than `MUST` because the contributor obligation above is the primary gate; the smoke check is a belt-and-braces backstop and the implementation MAY route its concrete shape (per-entry membership probe vs. type-side assertion) without re-litigation here.
