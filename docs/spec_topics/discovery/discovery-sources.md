# Discovery sources

- Global: `~/.pi/agent/theta/*.theta`
- Project: `.pi/theta/*.theta`
- Packages: each installed pi-package's `pi.theta` manifest entry (preferred) or its conventional `theta/` directory (fallback) — see [Package discovery](./package-and-settings.md#package-discovery).
- Settings: `thetaPaths` array (in `~/.pi/agent/settings.json` or `.pi/settings.json`) — `string[]` of file or directory paths; per-entry schema under [Settings file reads](./package-and-settings.md#settings-file-reads).
- CLI: `--theta <paths>` (single flag; multiple paths joined with the OS path-list separator — `:` POSIX, `;` Windows; uses Node's `path.delimiter`). Each entry is a file or directory, sharing with a settings `thetaPaths` entry the file-vs-directory rule stated at [Discovery roots](#discovery-roots) below — but none of the schema's glob or `!`/`+`/`-` override grammar: glob expansion is the invoking shell's job for this single-invocation source and the extension performs none, and a leading `!`, `+` or `-` carries no override meaning here: the component is taken as a literal path, so one that names no existing path is reported as `theta/load/missing-source` (error) without the clean-leaf ancestor walk, while one that does name an existing path resolves like any other entry. Windows authors must use `;` to avoid colliding with drive-letter colons (`C:\foo`). Pi has no built-in `--theta` flag; the theta extension registers it itself via `pi.registerFlag('theta', { type: 'string', description: '…' })` (see [Pi Integration Contract](../pi-integration-contract.md)) and reads it with `pi.getFlag('theta')` during the discovery walk.

**Path derivation.** The `.pi` / `~/.pi/agent` spellings above are Pi's own vantage point on two host-location seam members, not literal paths. A PROJECT-relative path is `<cwd>/<host config-dir name>/…` — the host's own `CONFIG_DIR_NAME` (`.pi` on Pi, `.omp` on Oh-My-Pi), read through the [`FileSystem.configDirName()` seam member](../pi-integration-contract/host-interfaces-services.md#pic-13-host-config-locations) — so the project source is `<cwd>/<config-dir>/theta/`, and the path-bearing descriptor naming it in diagnostics is spelled from that same value (the per-source *category* label — the `<source>` token of the case-collision message — keeps the Pi spelling: it names the source category, not a directory the host read). A GLOBAL path hangs off the host's OWN resolved global agent directory — the host SDK's `getAgentDir()`, read through the [`FileSystem.globalAgentDir()` seam member](../pi-integration-contract/host-interfaces-services.md#pic-13-host-config-locations) — and never off a synthesised `<homedir>/<config-dir>/agent`, because a host may relocate that directory through an environment override or an active profile: the global source is `<globalAgentDir>/theta/` and the global settings file `<globalAgentDir>/settings.json`. Neither derivation performs `~` expansion — `cwd()` and `globalAgentDir()` both answer absolute paths, and a tilde inside a host's own override is expanded by that host — so [DISC-1](#disc-1)'s expansion rule reaches the `~`-prefixed paths an *author* writes (settings `thetaPaths` entries and `--theta` components) and the illustrative `~/.pi/agent/…` spellings on this page. On a default Pi install both derivations resolve to exactly the paths spelled above.

Discovery is **non-recursive** and matches only `*.theta`, mirroring Pi prompt-template behaviour. The `*.theta` glob is **byte-exact lowercase** per [Lexical — Extension matching](../lexical.md#extension-matching): a file named `Plan.THETA` does not match the discovery glob on any platform, even on case-insensitive filesystems where the OS would consider the name equivalent to `plan.theta`. `.thetalib` library files are never discovered as slash commands regardless of where they live; they are reached only via `import` (with paths resolved relative to the importing file).

<a id="file-extension-namespace"></a>

### File-extension namespace

The `.theta` and `.thetalib` file extensions are coined by this extension. Under the [theta 1.0 Pi-SDK pin range](../pi-integration-contract/host-prerequisites.md#pi-sdk-pin), no Pi-shipped surface and no other `@earendil-works/pi-coding-agent` extension claims either extension; the framing paragraph above documents the parallel manifest-namespace check (`pi.extensions`, `pi.skills`, `pi.prompts`, `pi.themes`, `pi.video`, `pi.image` are the only Pi-recognised manifest keys, and `pi.theta` is therefore safe to coin).

**No Pi-owned discovery path enumerates `.theta` or `.thetalib`.** Under the [theta 1.0 Pi-SDK pin range](../pi-integration-contract/host-prerequisites.md#pi-sdk-pin), Pi exposes exactly three slash-command sources (`source: "prompt" | "extension" | "skill"`, per `@earendil-works/pi-coding-agent`'s `core/slash-commands.d.ts`); `prompt` and `skill` enumerate `*.md` files only, and `extension` requires programmatic `pi.registerCommand` calls. The `resources_discover` event carries `skillPaths` / `promptPaths` / `themePaths` only — there is no `thetaPaths` slot — and the `pi` package-manifest namespace recognises only `extensions` / `skills` / `prompts` / `themes` / `video` / `image`. Therefore the only path by which a `.thetalib` file could become a slash command is through this extension's own discovery walk; that walk matches `*.theta` only (per [Directory Convention](../discovery.md)), so the `.thetalib`-cannot-be-slash-invoked guarantee in [`spec.md`](../../spec.md) Introduction holds by construction. A Pi minor that adds a fourth `SlashCommandSource` arm, a `thetaPaths` field on `ResourcesDiscoverResult`, or a generic file-extension registry MUST trigger a re-validation of this paragraph as part of the [Pi version bump procedure](../pi-integration-contract/version-bump-intro.md#pi-version-bump-procedure), in the same edit that widens `peerDependencies`.

<a id="theta-flag-namespace"></a>The `--theta` CLI flag name is likewise coined by this extension; the theta extension registers it via `pi.registerFlag('theta', …)` (see [Registration steps](../pi-integration-contract/registration-steps.md) step 1). The flag-name overlap and shared flag-value-slot behaviours this coining relies on — that `pi.registerFlag('theta', …)` does not throw on a flag-name overlap and that theta's own `pi.getFlag('theta')` read still observes theta's registration — are recorded as host presuppositions at [Host prerequisites — `pi.registerFlag('theta', …)` flag-name overlap and shared flag-value-slot behaviour](../pi-integration-contract/host-prerequisites.md#theta-flag-namespace-presupposition). There is no `theta/load/...` code for flag-name collision in theta 1.0.

Pi has no central file-extension registry: ownership is established de facto by each extension's discovery walker. Cross-extension collisions on `.theta` or `.thetalib` files — a hypothetical future extension that also walks `*.theta` — manifest through the existing slash-name collision rule below (see [Slash-name collision rules](#disc-4)), not through a separate file-extension rule; there is no `theta/load/extension-claimed-by-other-extension` code in theta 1.0.

<a id="discovery-roots"></a>

**Discovery roots.** A *discovery root* is the directory each source contributes to the discovery walk. The set of active roots for a Pi session is the union of:

- The global root `~/.pi/agent/theta/` (when present).
- The project root `.pi/theta/` (when present).
- Each scanned package's contributing directory (the package's `theta/` directory, or each directory reached through a `pi.theta` glob).
- Each settings `thetaPaths` entry, resolved as follows: a directory entry contributes its own path; a file entry contributes its parent directory.
- Each path component of the `--theta` CLI flag (after splitting on `path.delimiter`), resolved by the same file-vs-directory rule as settings entries.

The term is referenced normatively by the path-restriction rule on `invoke` and `.theta` `tools:` entries (see [Invocation — Resolution](../invocation.md)): a resolved callee path must lie within at least one active root. Roots are computed once per discovery pass and cached for the lifetime of the resolved registry; hot-reload (per [Implementation Notes](../implementation-notes.md)) re-runs the computation.

<a id="home-directory-expansion"></a>

<a id="disc-1"></a> **DISC-1.** **Home-directory expansion.** Wherever the theta extension reads, interprets, or emits a path beginning with `~/`, the leading `~` MUST be expanded via the `homedir()` member of the injected `FileSystem` seam (see [Pi Integration Contract — `FakeFileSystem` / `FileSystem` interface](../pi-integration-contract.md)), whose production implementation calls Node's `os.homedir()` (resolving to `$HOME` on POSIX and `%USERPROFILE%` on Windows). This rule applies uniformly to: the global discovery root `~/.pi/agent/theta/`; the global package roots `~/.pi/agent/npm/` and `~/.pi/agent/git/<host>/<path>/`; the global settings file `~/.pi/agent/settings.json`; every `~`-prefixed entry in the settings `thetaPaths` array; and every `~`-prefixed component of the `--theta` CLI flag (after splitting on `path.delimiter`). The `~user` form (tilde followed by a username) is **not** honoured — only the bare `~` followed by `/` (or end-of-string). Implementations MUST NOT read `process.env.HOME` or `process.env.USERPROFILE` directly, and MUST NOT use any platform-conditional branch — the seam is the single source of truth so that test fakes can override it.

**Source priority (high to low).** When the same slash name resolves from multiple sources, the higher-priority source wins and `theta/load/cross-source-shadow` is emitted naming both paths. Both paths are carried in the diagnostic's rendered `message` (per the [`theta/load/cross-source-shadow`](../diagnostics/code-registry-load.md) row's `'<higher>' wins over '<lower>'` template); no structured `details` payload is emitted for this code. This rule qualifies on distinct files: one physical file reached through two sources is a single candidate under the normalised forward-slash form described under "Path literals" in [Lexical Structure](../lexical.md), so a source reaching an already-discovered file under a different separator spelling adds no second candidate and draws no shadow — the same separator-normalized identity the child's [marked-root winner path](../pi-integration-contract/subagent.md#subagent-marked-root-winner-path) relies on.

1. CLI flag (`--theta <path>`) — explicit, single-invocation override.
2. Settings (`thetaPaths` array, project `settings.json` overriding global).
3. Project (`.pi/theta/`).
4. Packages (`theta/` directories or `pi.theta` entries).
5. Global (`~/.pi/agent/theta/`).

<a id="disc-2"></a> **DISC-2.** **Failure modes.** Each discovery source has a defined behaviour for a missing, unreadable, or wrong-type path. The asymmetry is deliberate: *conventional locations* (global directory, project directory, package `theta/` directories) silently tolerate absence — that is the normal case on a fresh install or in a project that ships no thetas — while *explicit references* (`pi.theta` entries, settings entries, `--theta` flags) surface a missing path as an error, because the author named it and expects it to resolve.

| Source | Missing path | Unreadable path | Path is wrong type (file vs dir) |
|---|---|---|---|
| Global `~/.pi/agent/theta/` | silent | warning | warning |
| Project `.pi/theta/` | silent | warning | warning |
| Package `theta/` directory | silent (package may ship none) | warning | warning |
| Package `pi.theta` entry | error (manifest names a missing path) | warning | error |
| Settings `thetaPaths` entry | error (config names a missing path) | warning | error |
| CLI `--theta <path>` | error (explicit user intent) | error | error |

Three rules apply on top of the table:

1. **Discoverable `.theta` files that are themselves unreadable** (broken symlink, transient I/O error, EACCES on the file itself) are reported as `theta/load/unreadable` *warnings* regardless of source, and the theta is not registered. The scan continues; one bad file does not poison the rest.
2. **All warnings and errors above are emitted via the standard diagnostics channel** ([Diagnostics](../diagnostics.md)) using codes `theta/load/missing-source`, `theta/load/unreadable-source`, `theta/load/wrong-type-source`, and `theta/load/unreadable`. Each diagnostic carries the source descriptor in its `message` so the author can locate the offending configuration — e.g. `"settings entry index 2"`, `"--theta flag #1"`, `` "package `foo` (pi.theta[0])" ``, `` "package `foo` theta/ directory" ``, `"global thetas directory"`, `"project .pi/theta/"`.
3. **Errors are fatal for the offending entry only**, not for the whole discovery pass: a bad `--theta` flag prevents *that* theta from registering and surfaces a `theta/load/missing-source` error, but other `--theta` flags and the other four sources still process to completion.

Implementation notes:

- A *clean leaf-`ENOENT`* is an `ENOENT` from `readdir` or `stat` on the candidate path whose ancestors all `lstat` successfully as directories the process can enter. Operationally: on `ENOENT`, walk the candidate path's ancestor chain root-first; if every ancestor `lstat`s `ok` and is a directory, classify the result as *missing* (silent for conventional roots, error for explicit references per the failure-modes table above); if any ancestor `lstat` returns `EACCES`, `EPERM`, `ENOTDIR`, or itself `ENOENT`, classify the result as *unreadable* and emit `theta/load/unreadable-source` at the source's own severity per the failure-modes table above (error for a `--theta` flag, warning elsewhere). **Conventional-root exemption.** The ancestor walk governs the *explicit references*. A source whose *Missing path* cell is silent — the global root, the project root, and a package's conventional `theta/` directory — is skipped silently on an `ENOENT` root whatever its ancestor chain looks like, and never reaches the walk: that root's own ancestor is the host config directory, and the config directory not existing is the ordinary form of "not present" — the universal case on a host that never creates it, where the walk's unreadable arm would instead put a spurious `theta/load/unreadable-source` warning on every session in every workspace and make the table's silent-on-missing cell unreachable for the commonest form of absence. A conventional root that *exists* but cannot be read (`EACCES` / `EPERM`) still classifies as *unreadable* and still warns. For the settings `thetaPaths` source and every ordinary (non-override-prefixed) `--theta` component the clean-leaf distinction is unchanged, because there a missing intermediate directory is a real signal about a path the user typed; an override-prefixed `--theta` component skips this walk on `ENOENT`, per the CLI-source bullet above. The rule applies uniformly on POSIX and Windows — the implementation has no platform branch — and resolves the case where Windows surfaces both "leaf is genuinely missing" and "a parent ACL denies enumeration" as the same `ENOENT` from `fs.readdir`. Use `lstat` (not `stat`) as the ancestor probe so a broken symlink at an ancestor classifies as *unreadable* rather than silently traversing. The candidate path itself is checked with `readdir` or `stat` first; only failure triggers the ancestor walk, and successful enumeration short-circuits. The `readdir`/`lstat` rejections these predicates branch on carry Node-style `.code` values, and the `FileSystem` seam's `FakeFileSystem` mirror reproduces those rejections so this ancestor walk is callable through the injected seam in tests — both load-bearing facts are established by [PIC-13](../pi-integration-contract/host-interfaces-services.md#pic-13).
- A symlink loop or other traversal failure *inside* a discovery root that does exist is an unreadable-source warning, not silence — the silent-on-missing rule applies to the *root* itself not existing, not to failures encountered while walking a root that does. A glob pattern's recursive enumeration of its static-prefix root is a walk of that root in this same sense: a `readdir` failure at the prefix root itself, or at any directory below it, or a failure to `lstat` an entry that same walk enumerated, is an unreadable-source diagnostic at the offending source's own severity, carrying that source's descriptor (the matching `thetaPaths` entry index, or the package's `pi.theta` descriptor). A clean-leaf `ENOENT` under the prefix instead leaves the pattern resolving to zero paths and stays silent. The glob-universe walk itself contributes at most one diagnostic per denied path per discovery pass, however many patterns share that static-prefix root — and none at all for a path that a per-match or per-source enumeration earlier in the same pass has already reported under `theta/load/unreadable-source` or `theta/load/missing-source`, because that report already names an offending configuration for it. Sources are not deduplicated against each other: a later-resolving source that crosses the same denied path reports it again under its own descriptor, since each source names its own configuration.
- A `--theta` flag (or settings entry) pointing at a directory is allowed and treated like a per-source root, and a directory entry is a valid path regardless of its contents: an empty directory — or one whose entries are all non-`.theta` files — enumerates zero thetas successfully and emits no diagnostic. The wrong-type rule fires only when the path resolves to something that is neither a regular `.theta` file nor a directory.

<a id="disc-3"></a> **DISC-3.** **Case-insensitive filesystem collisions.** Within a single discovery source, two `*.theta` files whose paths differ only in case (e.g., `Plan.theta` and `plan.theta` in the same `theta/` directory) collide on case-insensitive filesystems (Windows, macOS default) but coexist on case-sensitive ones (most Linux). To make behaviour identical across both, the loader compares discovered paths case-insensitively *per source* and emits a load-time *warning* `theta/load/case-collision` naming both paths; the lexicographically-first path under case-sensitive byte comparison wins. Cross-source priority (the table above) still applies on top — the rule is intra-source only. Path comparison uses the normalised forward-slash form described under "Path literals" in [Lexical Structure](../lexical.md).

**Non-canonical extension case.** Because the discovery glob is byte-exact lowercase per [Lexical — Extension matching](../lexical.md#extension-matching), files saved with a non-canonical extension case (`Plan.THETA`, `helper.Theta`, `personas.THETALIB`) are silently invisible to discovery on every platform. To surface this otherwise-undetectable authoring mistake, the loader emits a load-time *warning* `theta/load/non-canonical-extension` per source whenever it encounters, in a directory it is walking for discovery purposes, a file whose stem matches the slash-name regex `^[a-z0-9][a-z0-9_-]*$` and whose extension is a case-variant of `.theta` or `.thetalib` other than the lowercase canonical form. Files with stems that would not be valid slash names (`.config.THETA`, `notes.txt.THETA`, `Foo.THETA`) stay silent to avoid noise on incidental files. The warning is **per-source** (like `theta/load/case-collision`); deduplication uses the same normalised forward-slash form. On case-insensitive filesystems `Plan.theta` and `Plan.THETA` are alternate spellings of one directory entry. The loader deduplicates by canonical-path equality via `realpath` *before* the case-check fires — relying on the case-canonicalisation guarantee pinned by [PIC-13](../pi-integration-contract/host-interfaces-services.md#pic-13), under which case-variant inputs resolving to one directory entry yield byte-identical `realpath` output — so the warning is not emitted when the byte-exact `.theta` form resolves to the same canonical path. The warning does not cause the file to register — by the byte-exact rule it is not a `.theta` file at all; the warning's purpose is purely diagnostic.

**Filename validity.** The slash name is the theta's filename stem taken verbatim — no case-folding, no whitespace trimming, no character substitution. The accepted stem matches the regex `^[a-z0-9][a-z0-9_-]*$`: lowercase ASCII letters and digits, optionally separated by `-` or `_`, starting with a letter or digit. Stems that do not match (e.g. `foo bar.theta`, `Foo.theta`, `foo!.theta`, `--help.theta`, `.foo.theta`, `café.theta`) are rejected at load time with `theta/load/invalid-slash-name` (severity `error`); the file does not register and does not participate in collision detection. Hint: ``slash names must be lowercase kebab/snake; rename the file (e.g. `code-review.theta`)``. The validator runs *before* parse, so an invalid name short-circuits frontmatter and body parsing — the file produces exactly one diagnostic. Because the accepted character class is lower-case-only, the case-sensitivity question (`Foo.theta` vs `foo.theta`) reduces to "both stems are rejected"; the cross-format collision check below is therefore well-defined regardless of whether the host filesystem is case-sensitive. For an *explicit file reference* — a `--theta` path component or a settings `thetaPaths` literal or glob match — the stem is taken from the on-disk directory entry the filesystem resolves the reference to, not from the reference's own spelling; on a case-insensitive host a case-variant reference (`plan.theta` naming on-disk `Plan.theta`) is judged on the real stem, so the same file registers or is refused identically however the reference's stem is cased and consistently with the same file reached by directory enumeration.

<a id="disc-4"></a> **DISC-4.** **Slash-name collision rules.** When two or more candidates at the same priority level derive the same slash name, the theta extension emits `theta/load/cross-format-collision` through the diagnostics channel defined in [Diagnostics](../diagnostics.md) naming **every** colliding path. Which entries drop depends on which formats collide:

1. **Theta-vs-theta (same priority).** When the colliding candidates are all `.theta` files — two packages each shipping `code-review.theta`, two settings entries that both expand to a `code-review.theta`, or two `--theta` components that resolve to files with the same stem — **every colliding theta drops** and none register (not just two: three packages each shipping `lint.theta` produces a single error listing all three paths).
2. **Theta-vs-Pi-owned (cross-format).** When a `.theta` collides with a Pi-owned `.md` prompt, `.md` skill, or *another* extension's command at the cross-priority comparison the `session_start` handler performs (another extension's, never one of the theta extension instance's OWN prior `pi.registerCommand` registrations, which carry the same `source: "extension"` but are excluded from the comparison on every pass per [PIC-69](../pi-integration-contract/registration-steps.md#pic-69)), **only the colliding theta(s) drop** and the Pi-owned entry stays registered — the theta extension cannot unregister Pi-owned templates or another extension's commands (see [Registration steps — *Structural changes*](../pi-integration-contract/registration-steps.md#structural-changes-no-unregister)).

Every colliding path is carried in the diagnostic's rendered `message` via the `<paths>` placeholder (per the [`theta/load/cross-format-collision`](../diagnostics/code-registry-load.md) row, byte-exact per [Placeholder rendering](../diagnostics/placeholder-rendering-b.md)); no structured `details` payload is emitted for this code. The rule is symmetric across all source types and across all file formats.

Detection runs on the **final derived name** (after `pi.theta` mapping, with the filename stem taken verbatim for sources that have no `pi.theta` mapping), not the source filename. Settings entries that resolve to the same absolute path post-tilde-expansion are deduplicated silently before collision detection runs (this is package-level dedup, not a collision). The same dedup applies across `--theta` path components and inside the package walker.

When the theta extension discovers, on its `session_start` handler, that a candidate theta resolves to the same slash name as an already-registered Pi prompt template, skill, or *foreign* extension command (its own prior registration of that name is excluded per [PIC-69](../pi-integration-contract/registration-steps.md#pic-69), so a name the instance re-discovers after a hot-reload or a re-bind is re-owned, not dropped), the theta extension refuses to register the colliding theta and emits the same diagnostic naming both the `.theta` path and the colliding entry. The Pi-owned `.md` prompt template or `.md` skill (or the other extension's command) remains registered and continues to function — the theta loses, asymmetrically. Authors who want the theta to win must rename one of the two files. Cross-format shadowing in either direction is not supported in theta 1.0; the rule is symmetric in *which formats it spans* (`.md` prompts, `.md` skills, and other extensions' commands all preempt a same-named theta) but asymmetric in *which side wins* (the theta never preempts a non-theta registration, because the theta extension cannot unregister Pi-owned templates or another extension's commands — the registration timeline in [Pi Integration Contract — Extension entry point](../pi-integration-contract.md) explains why). The candidate slug for a `.md` prompt or skill is derived under the same `^[a-z0-9][a-z0-9_-]*$` rule used for `.theta` stems; `.md` files whose stems do not conform are skipped for collision purposes (they remain Pi's problem to surface). Theta-vs-theta same-name collisions across discovery sources are governed by the source-priority rule in the table above and are unaffected by this paragraph. If a colliding `.md` prompt template appears *after* a theta has already been registered (e.g. via a settings reload, a Pi extension activation, or `ctx.reload()` re-running prompt-template discovery), the next `session_start` cycle re-evaluates and drops the previously-registered theta's `ThetaRegistry` entry, emitting the same diagnostic. The post-drop dispatch behaviour of the orphaned `/<name>` — that the theta's earlier `pi.registerCommand(name, …)` registration survives (Pi exposes no symmetric `pi.unregisterCommand`) and a dispatch returns a fixed superseded system note instead of running the dropped theta — is owned by [Registration steps — *Structural changes*](../pi-integration-contract/registration-steps.md#structural-changes-no-unregister) and the [superseded-entry dispatch](../pi-integration-contract/registration-steps.md#superseded-entry-dispatch) sub-case.

```
project/                          # project-source layout
└── .pi/
    └── theta/
        ├── code-review.theta         # discovered → /code-review
        ├── architecture-brief.theta  # discovered → /architecture-brief
        ├── personas.thetalib            # library — importable, never a slash command
        └── shared/
            └── schemas.thetalib         # library in a subdirectory; importable via path
```

```
my-package/                       # package-source layout
└── theta/
    ├── code-review.theta         # discovered → /code-review
    └── shared/
        └── schemas.thetalib         # library — importable, never a slash command
```
