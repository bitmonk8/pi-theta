# Code registry parse

## Code registry

The V1 diagnostic registry is split across four pages — the `loom/parse/*` table on this page and the [`loom/load/*`](./code-registry-load.md), [`loom/runtime/*`](./code-registry-runtime.md), and [`loom/host/*`](./code-registry-host.md) continuation tables — which together enumerate every diagnostic the V1 spec defines. The column legend, the closed *Phase* taxonomy, and the *E/W* severity carve-out that govern all four tables are defined in [Diagnostic shape — Code registry rules (normative)](./diagnostic-shape.md#column-legend).

### `loom/parse/*` — lexical, parse, and type-system errors

| Code | Sev | Phase | Trigger | Spec rule | Hint | Message |
|---|---|---|---|---|---|---|
| `loom/parse/illegal-escape` | E | lex | Backslash followed by an unrecognised character inside a regular string literal. | [Lexical — String literals](../lexical.md) | — | `illegal escape sequence: \\<char>` |
| `loom/parse/invalid-unicode-escape` | E | lex | A recognised `\u{...}` escape whose value exceeds `U+10FFFF` or lies in the UTF-16 surrogate range `U+D800`–`U+DFFF`. | [Lexical — String literals](../lexical.md) | Use a Unicode scalar value: `≤ U+10FFFF` and outside `U+D800`–`U+DFFF`. | `invalid Unicode escape: value is not a Unicode scalar value` |
| `loom/parse/literal-newline-in-string` | E | lex | Literal newline inside a regular (single-line) string literal. | [Lexical — String literals](../lexical.md) | Use a `@`...`` query template, or split with `+ "\n" +`. | `literal newline in string literal` |
| `loom/parse/unterminated-string` | E | lex | EOF reached while scanning a string literal. | [Lexical — String literals](../lexical.md) | — | `unterminated string literal` |
| `loom/parse/invalid-path-separator` | E | lex | Backslash inside a path literal (`import`, `invoke`, `tools:` `.loom` entry). | [Lexical — Path literals](../lexical.md) | Use `/` separators only. | `invalid path separator: backslash in path literal` |
| `loom/parse/stray-backslash` | E | lex | Backslash byte at a source position outside any string literal, path literal, or `@`...`` query-template body. | [Lexical — Source files](../lexical.md#stray-backslash) | — | `stray backslash in source` |
| `loom/parse/invoke-non-loom-extension` | E | parse | An `invoke(...)` literal or a `tools:` `.loom` entry whose path string does not end in `.loom`. The check runs *after* `loom/parse/invalid-path-separator`, on the path literal as written (no realpath normalisation), and is byte-exact lowercase per [Lexical — Path literals](../lexical.md). | [Invocation — Resolution](../invocation.md) | invoke and `tools:` paths must end in `.loom`; use `import` for `.warp` library code. | `invoke path '<path>' does not end in .loom` |
| `loom/parse/import-non-warp-extension` | E | parse | An `import` path literal that does not end in `.warp` (including a `.loom` path). The check runs *after* `loom/parse/invalid-path-separator`, on the path literal as written (no realpath normalisation), and is byte-exact lowercase per [Lexical — Path literals](../lexical.md). | [Imports — Path resolution](../imports.md) | import paths must end in `.warp`; `.loom` files are not importable — use `invoke(...)` instead. | `import path '<path>' does not end in .warp` |
| `loom/parse/binding-case-mismatch` | E | parse | Identifier in a binding / parameter / fn-name / field-name position does not start with a lowercase letter or `_`. | [Lexical — Identifiers](../lexical.md) | — | `binding name must start with a lowercase letter or _` |
| `loom/parse/schema-case-mismatch` | E | parse | Identifier in a schema / enum / variant / type-alias position does not start with an uppercase letter. | [Lexical — Identifiers](../lexical.md) | — | `schema name must start with an uppercase letter` |
| `loom/parse/reserved-keyword-as-identifier` | E | parse | Reserved keyword used in an identifier position. | [Lexical — Reserved keywords](../lexical.md) | — | `reserved keyword '<keyword>' cannot be used as an identifier` |
| `loom/parse/single-line-if` | E | parse | `if` / `for` / `while` / `fn` body is not a braced block (e.g. `if (x) stmt`). | [Lexical — Statement terminators](../lexical.md) | Wrap the body in `{ ... }`. | `single-line body not permitted; wrap in { ... }` |
| `loom/parse/block-comment` | E | lex | `/* ... */` block comment used. | [Lexical — Comments](../lexical.md) | Use `//` or `///` line comments. | `block comments are not supported` |
| `loom/parse/integer-narrowing` | E | type | `number` value used where `integer` is expected (the `integer → number` widening is one-way). | [Lexical — Number literals](../lexical.md) | — | `cannot narrow number to integer` |
| `loom/parse/integer-literal-out-of-range` | E | lex | An `integer`-typed number literal whose absolute value exceeds the safe-integer bound `2^53 - 1`, judged on the lexed token before the parse-time unary-`-` fold. | [Lexical — Number literals](../lexical.md) | — | `integer literal exceeds the safe-integer range` |
| `loom/parse/number-literal-not-finite` | E | lex | A `number`-typed literal whose parsed value is not a finite IEEE-754 double (e.g. `1e400`, which parses to `Infinity`). | [Lexical — Number literals](../lexical.md) | — | `number literal is not a finite IEEE-754 double` |
| `loom/parse/unsupported-feature` | E | parse | A loom 1.0-deferred or non-Loom syntactic construct (arrow function, spread, optional chaining, `===`, bitwise op, comma op, nested template, etc.) appears in source. | [Expressions — Not supported](../expressions.md) | — | `unsupported syntactic feature: <construct>` |
| `loom/parse/immutable-rebinding` | E | parse | Reassignment of a `let` (non-`mut`) binding. | [Bindings — `let` vs `let mut`](../bindings.md) | Use `let mut` if mutation is intentional. | `cannot reassign immutable binding '<name>'` |
| `loom/parse/assignment-as-expression` | E | parse | Assignment used in expression position (e.g. `if (x = 1)`). | [Bindings — Reassignment is a statement](../bindings.md) | — | `assignment is not an expression` |
| `loom/parse/assignment-to-member-or-index` | E | parse | `obj.field = ...` or `arr[i] = ...`. | [Bindings — Mutability is binding-level only](../bindings.md) | Rebind the whole value with `let mut`. | `cannot assign to member or index; mutability is binding-level only` |
| `loom/parse/mut-on-immutable-context` | E | parse | `mut` modifier on a function parameter, `for` iteration variable, or `match` pattern binding. | [Bindings — Immutable contexts](../bindings.md) | — | `'mut' is not permitted in this binding position` |
| `loom/parse/mut-on-discard` | E | parse | `let mut _ = ...`. | [Bindings — Immutable contexts](../bindings.md) | — | `'mut' is not permitted on discard binding '_'` |
| `loom/parse/increment-decrement` | E | parse | `++` or `--` operator used. | [Bindings — Increment / decrement](../bindings.md) | Use `count += 1` / `count -= 1`. | `'<op>' operator is not supported` |
| `loom/parse/non-boolean-condition` | E | type | Non-`boolean` value used in `if` / `while` / ternary condition or as `&&` / `\|\|` operand. | [Expressions — Truthiness](../expressions.md) | Write `if (x != "")`, `if (xs.length > 0)`, etc. | `condition must be boolean; got <type>` |
| `loom/parse/comparison-chaining` | E | parse | Chained comparison such as `a < b < c`. | [Expressions — Operator precedence](../expressions.md) | Comparison operators do not chain; use `&&`. | `comparison operators do not chain; use &&` |
| `loom/parse/mixed-plus-operands` | E | type | `+` applied to a `number`/`integer` and a `string` (or any other mixed-type pair). | [Expressions — `+` operator](../expressions.md) | Convert explicitly or interpolate inside a string. | `'+' has mixed operand types: <left> and <right>` |
| `loom/parse/non-orderable-operands` | E | type | `<`, `<=`, `>`, or `>=` applied to a non-orderable operand pair — a numeric operand against a `string`, or an operand whose type is not `number`/`integer` or `string` (`boolean`, `null`, enum, union, object schema, or `array<T>`). | [Expressions — Ordering comparisons](../expressions.md) | Compare only numeric or only string operands; use `==` / `!=` for other types. | `'<op>' requires two numeric or two string operands; got <left> and <right>` |
| `loom/parse/non-indexable-receiver` | E | type | Indexed access `a[...]` whose receiver `a` is neither `array<T>` nor an object value (e.g. `s[i]` on a `string`). | [Expressions — Supported forms](../expressions.md) | Index only `array<T>` or object values; use `s.split(...)` for strings. | `indexed access requires an array<T> or object receiver; got <type>` |
| `loom/parse/non-string-object-index` | E | type | Indexed access `obj[k]` on an object-value receiver whose index expression `k` is not of type `string` (e.g. `obj[0]`). | [Expressions — Supported forms](../expressions.md) | Object index must be a `string` loom-side name; use `obj.field` for the per-field declared type. | `object index must be string; got <type>` |
| `loom/parse/array-element-type-mismatch` | E | type | Array literal element does not type-check against the surrounding sink's element type. | [Expressions — Array construction](../expressions.md) | — | `array element type mismatch at index <i>: expected <expected>, got <actual>` |
| `loom/parse/array-no-common-type` | E | type | Array literal whose elements have no common type and no sink to narrow against. | [Expressions — Array construction](../expressions.md) | Annotate the binding with `array<A \| B>` or use a single schema. | `array elements have no common type; annotate the binding with array<A \| B> or use a single schema` |
| `loom/parse/return-no-common-type` | E | type | A loom or annotation-less-`fn` body whose tail-expression type and reachable early-`return` operand types share no common upper bound and no sink narrows them. | [Function Definitions — Loom return type](../functions.md#loom-return-type) | Give the body a single return type, or add an explicit `fn` return annotation / sink that reconciles the operands. | `return operands have no common type; annotate the function return type or reconcile the operands` |
| `loom/parse/non-string-array-join` | E | type | `arr.join(...)` invoked on an array whose element type is not `string`. | [Expressions — `array<T>` stdlib](../expressions.md) | Map elements to strings first; no implicit type conversion in loom 1.0. | `array.join requires a string element type; got array<<element>>` |
| `loom/parse/extra-object-field` | E | parse | Schema constructor lists a field not declared by the schema. | [Expressions — Object construction](../expressions.md) | — | `extra field '<field>' on schema '<schema>'` |
| `loom/parse/missing-object-field` | E | parse | Schema constructor omits a declared (required) field. | [Expressions — Object construction](../expressions.md) | — | `missing field '<field>' on schema '<schema>'` |
| `loom/parse/bare-object-literal` | E | parse | Bare `{ field: expr }` (no schema name) used in expression position outside the two documented carve-outs (`params:` defaults; single argument of a Pi-tool call). | [Expressions — Object construction](../expressions.md) | Name the schema: `Schema { field: expr }`. | `bare object literal not permitted in this position; name the schema (Schema { ... })` |
| `loom/parse/default-not-literal` | E | parse | A `params:` default RHS contains a form outside the [Loom literal sublanguage](../grammar.md#loom-literal-sublanguage) (operator, function call, identifier reference other than `Enum.Variant`, `${...}` interpolation, or `@`...`` template). | [Frontmatter — Defaults](../frontmatter.md) | Replace the offending sub-expression with a literal value, or move the computation into loom body code. | `params default RHS must be a literal-sublanguage form; offending sub-expression: <expr>` |
| `loom/parse/non-trailing-default` | E | parse | A `params:` declaration places a non-defaulted field after a defaulted field in declaration order. | [Frontmatter — Defaults](../frontmatter.md) | Reorder `params:` so every defaulted field is trailing, or give the offending field a default. | `non-defaulted param '<field>' follows a defaulted param; defaulted params must be trailing` |
| `loom/parse/tool-arg-not-literal` | E | parse | The single-argument bare-object literal of a Pi-tool call contains a form outside the [Loom literal sublanguage](../grammar.md#loom-literal-sublanguage). | [Tool Calls — Argument shape](../tool-calls.md) | Bind the computed value via `let` first, then pass through a typed `params:` callee or `invoke(...)`. | `Pi-tool argument must be a literal-sublanguage form; offending sub-expression: <expr>` |
| `loom/parse/tool-arg-arity` | E | parse | A Pi-tool call site uses more than one positional argument (e.g. `read({...}, {...})`). | [Tool Calls — Argument shape](../tool-calls.md) | Pi tools take exactly one input object; merge the arguments. | `Pi tool '<name>' takes a single object argument; got <count>` |
| `loom/parse/let-without-initialiser` | E | parse | `let x: T` (annotation, no initialiser). Loom has no `undefined` value and no definite-assignment analysis. | [Grammar Appendix — `let` form](../grammar.md#let-form) | Initialise the binding at declaration, or restructure to bind once at the point a value is available. | `let binding '<name>' has no initialiser` |
| `loom/parse/let-rhs-type-mismatch` | E | type | The RHS initialiser of a typed binding `let x: T = expr` has a static type that is not compatible with the annotation `T` under [Type System — Type compatibility](../type-system.md#type-compatibility), where the RHS type is statically resolvable. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `let binding '<name>' initialiser type mismatch: expected <expected>, got <actual>` |
| `loom/parse/statement-in-arm-body` | E | parse | A `match` arm body is a bare `if` / `for` / `while` / `let` / assignment statement. Arm bodies are expressions; the ternary is the expression form of conditional. | [Grammar Appendix — `match` arm body](../grammar.md#match-arm-body) | Wrap the statements in a block expression `{ ... }` whose tail expression is the arm's value. | `match arm body must be an expression; wrap statements in a block expression { ... }` |
| `loom/parse/by-on-object-schema` | E | parse | `schema X by f { ... }` declaration with an object body. The `by` clause applies only to discriminated-union schemas. | [Grammar Appendix — `schema X by <field>`](../grammar.md#schema-x-by-field) | Use `schema X by f = A \| B \| …`, or drop the `by` clause if `X` is an object schema. | `the 'by' clause applies only to discriminated-union schemas (schema X by f = A \| B \| …)` |
| `loom/parse/doc-comment-misplaced` | E | parse | A `///` doc comment appears above a production other than `schema`, `enum`, schema field, enum variant, or `fn`. | [Grammar Appendix — `///` placement](../grammar.md#-placement) | Move the description to a legal anchor, or replace with a regular `//` comment. | `'///' doc comment is not legal above this production` |
| `loom/parse/generic-arity-mismatch` | E | parse | A generic-type application whose type-argument count does not match the constructor's declared arity in the closed `GenericType` set (`array` arity 1, `Result` arity 2) — e.g. `array<T, U>` or `Result<T>`. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | — | `generic type '<ctor>' expects <expected> type argument(s); got <actual>` |
| `loom/parse/void-in-non-return-position` | E | parse | `void` keyword used in a `Type` position other than a function/loom return type — a `let` annotation, schema or `params:` field, generic argument (`array<void>`, `Result<void, E>`), `invoke<void>` annotation, type ascription, or union arm. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | `void` is a return-only annotation; use a value type (or `null`) in this position. | `'void' is only permitted as a function or loom return type` |
| `loom/parse/result-in-schema-position` | E | parse | A `Result<T, E>` application appears in a lowered-schema position — a schema field type, a `params:` field type, or any type reachable transitively from those (including `array<T>` element types and union arms). `Result` is observed only by loom code and is never lowered to a JSON Schema fragment. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | `Result` has no lowered-schema form; use it only in `fn` / `let` / `invoke` positions, and feed the schema position a lowerable type. | `'Result' has no lowered-schema form and is not permitted in a schema-feeding position` |
| `loom/parse/unknown-identifier` | E | parse | Bare identifier in call or value position resolves to nothing in scope. | [Expressions — Identifier resolution](../expressions.md) | — | `unknown identifier '<name>'` |
| `loom/parse/unknown-method` | E | parse | Method or property accessed on a built-in type that the loom 1.0 stdlib does not expose. | [Expressions — Built-in methods](../expressions.md) | — | `unknown method '<method>' on type <type>` |
| `loom/parse/non-array-iterand` | E | type | `for x in expr` where `expr` is not `array<T>`. | [Control Flow — `for`/`in`](../control-flow.md) | Use `obj.keys()` for objects, `s.split(...)` for strings. | `'for' expects array<T> after 'in'; got <type>` |
| `loom/parse/break-outside-loop` | E | parse | `break` outside the body of a `for` / `while` loop. | [Control Flow — `break`/`continue`](../control-flow.md) | — | `'break' outside of a loop` |
| `loom/parse/continue-outside-loop` | E | parse | `continue` outside the body of a `for` / `while` loop. | [Control Flow — `break`/`continue`](../control-flow.md) | — | `'continue' outside of a loop` |
| `loom/parse/break-with-value` | E | parse | `break expr` (loom 1.0 `break` carries no value). | [Control Flow — `break`/`continue`](../control-flow.md) | — | `'break' takes no value in loom 1.0` |
| `loom/parse/illegal-template-escape` | E | lex | Backslash followed by an unrecognised character inside a `@`...`` query template body. | [Query — Escapes](../query.md) | — | `illegal escape sequence in @\`...\` template: \\<char>` |
| `loom/parse/unterminated-template` | E | lex | EOF reached while scanning a `@`...`` query template. | [Query — Multi-line templates](../query.md) | — | `unterminated @\`...\` query template` |
| `loom/parse/discarded-query-result` | E | parse | Bare `@`...`` expression-statement (the `Result` is dropped without `?` or `let _ =`). | [Query — Discarded query results](../query.md) | Use `?` to propagate failure or `let _ = @\`...\`` to discard explicitly. | `query result discarded; use ? to propagate failure or 'let _ = ...' to discard explicitly` |
| `loom/parse/empty-template` | W | parse | `@`...`` template's static body is empty or whitespace-only (ASCII whitespace per [System-note rendering](../binder/defaulting-system-note-echo.md#system-note-rendering) rule 1, not the regex `\s` class) after newline-trim and dedent. The loom still loads; the runtime short-circuits the query if the fully-rendered text is also empty. | [Query — Degenerate rendered templates](../query.md) | Add literal text or use `\\n` to keep an intentionally-blank prompt. | `query template body is empty after newline-trim and dedent` |
| `loom/parse/interpolated-result` | E | type | `${expr}` interpolation whose `expr` has Loom static type `Result<T, E>` (the runtime renderer raises the same code as a panic when the type is statically unresolvable). | [Query — Stringification of interpolated values](../query.md) | Unwrap with `?` or `match` before interpolating. | `Result value cannot be interpolated; unwrap with ? or match first` |
| `loom/parse/explicit-schema-mismatch` | W | parse | Both a binding annotation and an explicit `@<Schema>` ascription are present, and the ascription is not compatible with the annotation under [Type System — Type compatibility](../type-system.md#type-compatibility) (i.e. `ascription ⋢ annotation`). Safe widenings (e.g. `@<integer>` under a `number` annotation) do not warn. | [Query — Explicit form](../query.md) | — | `explicit @<Schema> ascription is not compatible with binding annotation` |
| `loom/parse/match-arm-type-mismatch` | E | type | A `match` arm's body type is not assignable to the common type of the other arms. | [Errors and Results — Arm syntax](../errors-and-results.md) | — | `match arm body type does not match the common type of the other arms` |
| `loom/parse/question-outside-result-fn` | E | type | `?` used in a function or top-level loom whose return type is not `Result<_, QueryError>` (and cannot be inferred to one). | [Errors and Results — `?` operator](../errors-and-results.md) | Declare the enclosing scope's return type as `Result<_, QueryError>`. | `'?' used in a scope whose return type is not Result<_, QueryError>` |
| `loom/parse/question-on-non-result` | E | type | `?` applied to an operand whose Loom static type is not `Result<_, QueryError>` (e.g. `let x = 5?`). Distinct from `loom/parse/question-outside-result-fn`, which checks the enclosing scope's return type rather than the operand type. | [Errors and Results — `?` operator](../errors-and-results/error-model.md#err-18) | Apply `?` only to a `Result`-typed operand (a `@`-query, `invoke(...)`, or `Ok(...)` / `Err(...)`). | `'?' requires a Result operand; got <type>` |
| `loom/parse/match-guard-not-supported` | E | parse | Guarded `match` arm (`Pattern if cond => ...`). | [Errors and Results — Pattern grammar](../errors-and-results.md) | Deferred to a future release. | `match guards are not supported in loom 1.0` |
| `loom/parse/rest-pattern-not-supported` | E | parse | Rest pattern (`[first, ...rest]`, `{ kind, ...other }`). | [Errors and Results — Pattern grammar](../errors-and-results.md) | Deferred to a future release. | `rest patterns are not supported in loom 1.0` |
| `loom/parse/bare-return-in-non-void` | E | type | Bare `return` (no argument) in a function or loom whose return type is not `void`. | [Return Statement](../return.md) | — | `missing return value` |
| `loom/parse/unreachable-code` | W | parse | Statement appears after a `return` in the same block. | [Return Statement](../return.md) | — | `unreachable code after return` |
| `loom/parse/nested-fn` | E | parse | `fn` declaration nested inside another `fn` body or a block. | [Function Definitions — Placement](../functions.md) | Top-level `fn` only in loom 1.0. | `nested 'fn' declarations are not supported in loom 1.0` |
| `loom/parse/function-as-value` | E | parse | Function name used outside call position (bound to `let`, passed as argument, etc.). | [Function Definitions — Placement](../functions.md) | — | `function '<name>' used outside call position; functions are not first-class in loom 1.0` |
| `loom/parse/redundant-wire-name` | W | parse | `field as "field"` rename whose wire name equals the loom-side name. | [Schemas — Wire-name renaming](../schemas.md) | Drop the `as` clause. | `redundant 'as' clause: wire name '<name>' equals the loom-side name` |
| `loom/parse/wire-name-collision` | E | parse | Two fields in the same schema share a wire name, or a wire name collides with another field's loom-side name. | [Schemas — Wire-name renaming](../schemas.md) | — | `wire name '<name>' collides with another field on schema '<schema>'` |
| `loom/parse/empty-schema-body` | E | parse | `schema X { }` declaration with no fields. | [Schemas — Object schemas](../schemas.md) | — | `'<X>' has no fields; an empty schema cannot be validated.` |
| `loom/parse/empty-enum-body` | E | parse | `enum X { }` declaration with no variants. | [Schemas — Enum declarations](../schemas.md) | — | `'<X>' has no variants; an empty enum cannot be validated.` |
| `loom/parse/unknown-variant` | E | parse | `Enum.Variant` reference where `Variant` is not a declared variant of `Enum`. | [Schemas — Variant access](../schemas.md) | — | `unknown variant '<variant>' on enum '<enum>'` |
| `loom/parse/unresolved-named-type` | E | parse | A `params:` right-hand side `NamedType` that names no in-scope `schema`/`enum` declaration or imported `.warp` symbol. Resolution is whole-file, so a frontmatter-to-body forward reference is not itself a failure. | [Frontmatter — params](../frontmatter.md) | — | `unresolved named type '<name>'` |
| `loom/parse/duplicate-enum-value` | E | parse | Two `enum` variants share an explicit string value. | [Schemas — Enum declarations](../schemas.md) | — | `duplicate enum value '<value>' across variants of enum '<enum>'` |
| `loom/parse/duplicate-enum-variant-name` | E | parse | Two variants in the same `enum` share an identifier, regardless of explicit-value assignment. The name-duplication check runs before `loom/parse/duplicate-enum-value`, so both implicit-value and distinct-explicit-value name collisions fail on this code. | [Schemas — Enum declarations](../schemas.md) | — | `duplicate variant name '<variant>' on enum '<enum>'` |
| `loom/parse/non-string-enum-value` | E | parse | `enum` variant explicit value is not a single string literal. | [Schemas — Enum declarations](../schemas.md) | loom 1.0 enums carry string values only. | `enum variant value must be a string literal; got <kind>` |
| `loom/parse/inline-enum` | E | parse | `enum["a", "b"]` or other inline-enum form. | [Schemas — Enum declarations](../schemas.md) | Use a literal-union (`"a" \| "b"`) or a top-level `enum` declaration. | `inline 'enum[...]' is not supported; use a top-level 'enum' declaration or a literal-union` |
| `loom/parse/ambiguous-discriminator` | E | parse | Discriminated-union detection finds more than one candidate field. | [Schemas — Discriminated unions](../schemas.md) | Declare explicitly with `by <field>`. | `ambiguous discriminator for <X>; candidates: <fields>. Declare explicitly with 'by <field>'.` |
| `loom/parse/missing-discriminator` | E | parse | Discriminated-union detection finds no candidate field. | [Schemas — Discriminated unions](../schemas.md) | Add a `kind` (or similar) field to each variant, or declare explicitly with `by <field>`. | `<X> is a union of object schemas with no shared single-literal discriminator field. Add a 'kind' (or similar) field to each variant, or declare explicitly with 'by <field>'.` |
| `loom/parse/duplicate-discriminator-value` | E | parse | Two variants of a discriminated union share the same discriminator value. | [Schemas — Discriminated unions](../schemas.md) | — | `duplicate discriminator value '<value>' across variants of <X>` |
| `loom/parse/nested-discriminator` | E | parse | Discriminator field is not at the top level of each variant (e.g. `kind: { type: "x" }`). | [Schemas — Discriminated unions](../schemas.md) | — | `discriminator field '<field>' must be at the top level of each variant of <X>` |
| `loom/parse/non-string-discriminator` | E | parse | Discriminator field's per-variant literal type is not `string` — i.e. a numeric or boolean literal `const`. Applies equally to implicit detection and to the explicit `by <field>` form. | [Schemas — Discriminated unions](../schemas.md) | Wrap the tag as a string (e.g. `kind: "v1"` rather than `kind: 1`). | `discriminator '<field>' on <X> must be a string-literal type; got <kind>` |
| `loom/parse/type-alias-cycle` | E | parse | A `schema X = ...` whose right-hand side reduces to `X` through type aliases only — directly (`schema X = X`) or transitively (`schema X = Y; schema Y = X`). Cycles that traverse at least one object-schema hop remain legal. Detection runs after schema-name resolution but before lowering. | [Schemas — Recursion](../schemas.md) | — | `type-alias cycle: <path>` |
| `loom/parse/system-on-prompt-mode` | E | parse | `system:` frontmatter field declared on a `mode: prompt` loom. | [Frontmatter — `system`](../frontmatter.md) | Either switch to `mode: subagent` or remove `system:`. | `'system:' is not permitted on a mode: prompt loom` |
| `loom/parse/system-interp-not-path` | E | parse | Body of a `${...}` interpolation in `system:` is not a bare identifier path (e.g. `${arr[0]}`, `${a + b}`, `${f(x)}`, `${a?.b}`, `${"x"}`). | [Frontmatter — `system` Interpolation](../frontmatter.md) | The `system:` slot accepts only bare identifier paths; see [future-considerations.md](../future-considerations.md) for richer expressions. | `'system:' interpolation body must be a bare identifier path` |
| `loom/parse/system-interp-unknown-param` | E | parse | Head identifier of a `system:` `${...}` path does not name a declared `params` field. | [Frontmatter — `system` Interpolation](../frontmatter.md) | — | `'system:' interpolation references unknown param '<name>'` |
| `loom/parse/system-interp-bad-field` | E | parse | A `.Ident` step in a `system:` `${...}` path does not name a reachable loom-side `params` object field, or descends into an array or un-narrowed discriminated union. | [Frontmatter — `system` Interpolation](../frontmatter.md) | — | `'system:' interpolation '.<field>' does not name a reachable object field on <path>` |
| `loom/parse/system-interp-unterminated` | E | parse | `${` in a `system:` value is not closed by a matching `}` before the YAML scalar ends. | [Frontmatter — `system` Interpolation](../frontmatter.md) | — | `'system:' interpolation '${' is not closed by a matching '}'` |
| `loom/parse/timeout-field-rejected` | E | parse | `timeout:` field declared on a query, tool call, or invoke (loom 1.0 has no per-call timeout). | [Cancellation](../cancellation.md) | Per-call timeouts are deferred. | `'timeout:' field is not supported in loom 1.0` |
| `loom/parse/bind-context-session-on-subagent` | W | parse | `bind_context: session` declared on a `mode: subagent` loom. | [Binder — Binder context](../binder.md) | Subagent-mode looms have no caller-session context. | `'bind_context: session' has no effect on a mode: subagent loom` |
| `loom/parse/bind-echo-on-bypass` | W | parse | An explicit `bind_echo: true` is declared on a single-string-bypass loom (`params:` declares exactly one no-default `string` field); a defaulted `bind_echo` does not fire this warning. The no-params bypass is owned by `loom/load/bind-echo-without-params`, not this code. | [Binder — Echo policy](../binder.md) | The single-string bypass auto-suppresses echo regardless. | `'bind_echo: true' has no effect on a single-string-bypass loom` |
| `loom/parse/warp-top-level-statement` | E | parse | `.warp` file contains a top-level statement, `let`, or query. | [Imports — `.warp` file rules](../imports.md) | Move the code into a `fn` body. | `top-level statement not permitted in .warp file; move into a fn body` |
| `loom/parse/import-name-collision` | E | parse | Two imports bring in the same symbol name, or an imported symbol collides with a top-level declaration in the same file. | [Imports — Name collisions](../imports.md) | Resolve with `as`-aliasing. | `imported symbol '<name>' collides with another import or top-level declaration` |
| `loom/parse/import-unknown-symbol` | E | parse | An `import { ... }` or `export { ... } from` specifier (including `as`-aliased forms) names a symbol that is neither a top-level declaration nor a transitive `export … from` re-export of the resolved `.warp` file. Fires after the resolved file's own parse completes and is collected by the multi-error batching rule rather than fast-failing; `<name>` renders the source symbol, not the alias. | [Imports — Unknown imported symbol](../imports.md) | — | `imported symbol '<name>' is not declared or re-exported by '<path>'` |
| `loom/parse/invoke-arg-type-mismatch` | E | type | `invoke(...)` argument does not type-check against the callee's declared `params` schema (when the callee is statically resolvable). | [Invocation — Argument binding](../invocation.md) | — | `invoke argument <i> ('<param>') type mismatch: expected <expected>, got <actual>` |
| `loom/parse/tool-arg-type-mismatch` | E | type | `<name>(args)` `.loom`-callable-call argument does not match the callee loom's `params` (when statically resolvable). Pi-tool argument type mismatches are not a parse error — they surface at runtime as `Err(CodeToolError { cause: "validation", ... })`; see [Tool Calls — Argument shape](../tool-calls.md). | [Tool Calls — Argument shape](../tool-calls.md) | — | `tool '<name>' argument type mismatch: expected <expected>, got <actual>` |
| `loom/parse/fn-arg-type-mismatch` | E | type | A plain top-level `fn` call `f(args)` — a same-file or imported `.warp` function call that is neither an `invoke(...)` nor a `.loom`-callable call — passes an argument whose static type is not compatible with the matched parameter's declared type. Always parse-time: top-level `fn` declarations are hoisted and always statically resolvable, so no runtime AJV safety net applies. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `fn '<name>' argument <i> ('<param>') type mismatch: expected <expected>, got <actual>` |
| `loom/parse/invoke-return-type-mismatch` | E | type | `invoke<Schema>(...)` annotation is structurally incompatible with the callee's inferred return type, where both annotation and callee are statically resolvable. | [Invocation — Typed return](../invocation.md) | Widen the annotation, narrow the callee, or drop the annotation and let the runtime AJV check decide. | `invoke<Schema> annotation incompatible with callee '<callee>' return type <actual>` |
| `loom/parse/invoke-arity-too-few` | E | parse | `invoke(...)` or `.loom` callable call passes fewer arguments than the callee's count of non-defaulted `params:`, where the callee is statically resolvable. | [Invocation — Argument arity](../invocation.md#argument-binding) | Provide the missing argument(s) or default the corresponding `params:` field on the callee. | `invoke '<callee>' passes too few arguments: expected <required> non-defaulted, got <provided>` |
| `loom/parse/invoke-arity-too-many` | E | parse | `invoke(...)` or `.loom` callable call passes more arguments than the callee's total `params:` count. Always parse-time — no runtime safety net is possible. | [Invocation — Argument arity](../invocation.md#argument-binding) | Drop the extra argument(s); positional binding has no destination for them. | `invoke '<callee>' passes too many arguments: expected at most <max>, got <provided>` |
