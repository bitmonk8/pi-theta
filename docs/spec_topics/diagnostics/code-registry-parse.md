# Code registry parse

## Code registry

The V1 diagnostic registry is split across four pages — the `theta/parse/*` table on this page and the [`theta/load/*`](./code-registry-load.md), [`theta/runtime/*`](./code-registry-runtime.md), and [`theta/host/*`](./code-registry-host.md) continuation tables — which together enumerate every diagnostic the V1 spec defines. The column legend, the closed *Phase* taxonomy, and the *E/W* severity carve-out that govern all four tables are defined in [Diagnostic shape — Code registry rules (normative)](./diagnostic-shape.md#column-legend).

### `theta/parse/*` — lexical, parse, and type-system errors

| Code | Sev | Phase | Trigger | Spec rule | Hint | Message |
|---|---|---|---|---|---|---|
| `theta/parse/illegal-escape` | E | lex | Backslash followed by an unrecognised character inside a regular string literal. | [Lexical — String literals](../lexical.md) | — | `illegal escape sequence: \\<char>` |
| `theta/parse/invalid-unicode-escape` | E | lex | A recognised `\u{...}` escape whose value exceeds `U+10FFFF` or lies in the UTF-16 surrogate range `U+D800`–`U+DFFF`. | [Lexical — String literals](../lexical.md) | Use a Unicode scalar value: `≤ U+10FFFF` and outside `U+D800`–`U+DFFF`. | `invalid Unicode escape: value is not a Unicode scalar value` |
| `theta/parse/literal-newline-in-string` | E | lex, parse | Literal newline inside a regular (single-line) string literal. Also fires inside a `params:` default right-hand side, which is a strict subset of the same expression grammar ([Theta literal sublanguage](../grammar.md#theta-literal-sublanguage)). | [Lexical — String literals](../lexical.md) | Use a `@`...`` query template, or split with `+ "\n" +`. At a `params:` default right-hand side, use the `\n` escape, or move the value into body code. | `literal newline in string literal` |
| `theta/parse/unterminated-string` | E | lex | EOF reached while scanning a string literal. | [Lexical — String literals](../lexical.md) | — | `unterminated string literal` |
| `theta/parse/invalid-path-separator` | E | lex | Backslash inside a path literal (`import`, `invoke`, `tools:` `.theta` entry). | [Lexical — Path literals](../lexical.md) | Use `/` separators only. | `invalid path separator: backslash in path literal` |
| `theta/parse/stray-backslash` | E | lex | Backslash byte at a source position outside any string literal, path literal, or `@`...`` query-template body. | [Lexical — Source files](../lexical.md#stray-backslash) | — | `stray backslash in source` |
| `theta/parse/invoke-non-theta-extension` | E | parse | An `invoke(...)` literal or a `tools:` `.theta` entry whose path string does not end in `.theta`. The check runs *after* `theta/parse/invalid-path-separator`, on the path literal as written (no realpath normalisation), and is byte-exact lowercase per [Lexical — Path literals](../lexical.md). | [Invocation — Resolution](../invocation.md) | invoke and `tools:` paths must end in `.theta`; use `import` for `.thetalib` library code. | `invoke path '<path>' does not end in .theta` |
| `theta/parse/import-non-thetalib-extension` | E | parse | An `import` path literal that does not end in `.thetalib` (including a `.theta` path). The check runs *after* `theta/parse/invalid-path-separator`, on the path literal as written (no realpath normalisation), and is byte-exact lowercase per [Lexical — Path literals](../lexical.md). | [Imports — Path resolution](../imports.md) | import paths must end in `.thetalib`; `.theta` files are not importable — use `invoke(...)` instead. | `import path '<path>' does not end in .thetalib` |
| `theta/parse/binding-case-mismatch` | E | parse | Identifier in a binding / parameter / fn-name / field-name position does not start with a lowercase letter or `_`. | [Lexical — Identifiers](../lexical.md) | — | `binding name must start with a lowercase letter or _` |
| `theta/parse/schema-case-mismatch` | E | parse | Identifier in a schema / enum / variant / type-alias position does not start with an uppercase letter. | [Lexical — Identifiers](../lexical.md) | — | `schema name must start with an uppercase letter` |
| `theta/parse/reserved-keyword-as-identifier` | E | parse | Reserved keyword used in an identifier position. | [Lexical — Reserved keywords](../lexical.md) | — | `reserved keyword '<keyword>' cannot be used as an identifier` |
| `theta/parse/single-line-if` | E | parse | `if` / `for` / `while` / `fn` body is not a braced block (e.g. `if (x) stmt`). | [Lexical — Statement terminators](../lexical.md) | Wrap the body in `{ ... }`. | `single-line body not permitted; wrap in { ... }` |
| `theta/parse/block-comment` | E | lex | `/* ... */` block comment used. | [Lexical — Comments](../lexical.md) | Use `//` or `///` line comments. | `block comments are not supported` |
| `theta/parse/integer-narrowing` | E | type | `number` value used where `integer` is expected (the `integer → number` widening is one-way). | [Lexical — Number literals](../lexical.md) | — | `cannot narrow number to integer` |
| `theta/parse/integer-literal-out-of-range` | E | lex | An `integer`-typed number literal whose absolute value exceeds the safe-integer bound `2^53 - 1`, judged on the lexed token before the parse-time unary-`-` fold. | [Lexical — Number literals](../lexical.md) | — | `integer literal exceeds the safe-integer range` |
| `theta/parse/number-literal-not-finite` | E | lex | A `number`-typed literal whose parsed value is not a finite IEEE-754 double (e.g. `1e400`, which parses to `Infinity`). | [Lexical — Number literals](../lexical.md) | — | `number literal is not a finite IEEE-754 double` |
| `theta/parse/unsupported-feature` | E | parse | A theta 1.0-deferred or non-Theta syntactic construct (arrow function, spread, optional chaining, `===`, bitwise op, comma op, nested template, etc.) appears in source. | [Expressions — Not supported](../expressions.md) | — | `unsupported syntactic feature: <construct>` |
| `theta/parse/immutable-rebinding` | E | parse | Reassignment of a `let` (non-`mut`) binding. | [Bindings — `let` vs `let mut`](../bindings.md) | Use `let mut` if mutation is intentional. | `cannot reassign immutable binding '<name>'` |
| `theta/parse/assignment-as-expression` | E | parse | Assignment used in expression position (e.g. `if (x = 1)`). | [Bindings — Reassignment is a statement](../bindings.md) | — | `assignment is not an expression` |
| `theta/parse/assignment-to-member-or-index` | E | parse | `obj.field = ...` or `arr[i] = ...`. | [Bindings — Mutability is binding-level only](../bindings.md) | Rebind the whole value with `let mut`. | `cannot assign to member or index; mutability is binding-level only` |
| `theta/parse/mut-on-immutable-context` | E | parse | `mut` modifier on a function parameter, `for` iteration variable, or `match` pattern binding. | [Bindings — Immutable contexts](../bindings.md) | — | `'mut' is not permitted in this binding position` |
| `theta/parse/mut-on-discard` | E | parse | `let mut _ = ...`. | [Bindings — Immutable contexts](../bindings.md) | — | `'mut' is not permitted on discard binding '_'` |
| `theta/parse/increment-decrement` | E | parse | `++` or `--` operator used. | [Bindings — Increment / decrement](../bindings.md) | Use `count += 1` / `count -= 1`. | `'<op>' operator is not supported` |
| `theta/parse/non-boolean-condition` | E | type | Non-`boolean` value used in `if` / `while` / ternary condition or as `&&` / `\|\|` operand. | [Expressions — Truthiness](../expressions.md) | Write `if (x != "")`, `if (xs.length > 0)`, etc. | `condition must be boolean; got <type>` |
| `theta/parse/comparison-chaining` | E | parse | Chained comparison such as `a < b < c`. | [Expressions — Operator precedence](../expressions.md) | Comparison operators do not chain; use `&&`. | `comparison operators do not chain; use &&` |
| `theta/parse/mixed-plus-operands` | E | type | `+` applied to a `number`/`integer` and a `string` (or any other mixed-type pair). | [Expressions — `+` operator](../expressions.md) | Convert explicitly or interpolate inside a string. | `'+' has mixed operand types: <left> and <right>` |
| `theta/parse/non-orderable-operands` | E | type | `<`, `<=`, `>`, or `>=` applied to a non-orderable operand pair — a numeric operand against a `string`, or an operand whose type is not `number`/`integer` or `string` (`boolean`, `null`, enum, union, object schema, or `array<T>`). | [Expressions — Ordering comparisons](../expressions.md) | Compare only numeric or only string operands; use `==` / `!=` for other types. | `'<op>' requires two numeric or two string operands; got <left> and <right>` |
| `theta/parse/non-indexable-receiver` | E | type | Indexed access `a[...]` whose receiver `a` is neither `array<T>` nor an object value (e.g. `s[i]` on a `string`). | [Expressions — Supported forms](../expressions.md) | Index only `array<T>` or object values; use `s.split(...)` for strings. | `indexed access requires an array<T> or object receiver; got <type>` |
| `theta/parse/non-string-object-index` | E | type | Indexed access `obj[k]` on an object-value receiver whose index expression `k` is not of type `string` (e.g. `obj[0]`). | [Expressions — Supported forms](../expressions.md) | Object index must be a `string` theta-side name; use `obj.field` for the per-field declared type. | `object index must be string; got <type>` |
| `theta/parse/array-element-type-mismatch` | E | type | Array literal element does not type-check against the surrounding sink's element type. | [Expressions — Array construction](../expressions.md) | — | `array element type mismatch at index <i>: expected <expected>, got <actual>` |
| `theta/parse/array-no-common-type` | E | type | Array literal whose elements have no common type and no sink to narrow against. | [Expressions — Array construction](../expressions.md) | Annotate the binding with `array<A \| B>` or use a single schema. | `array elements have no common type; annotate the binding with array<A \| B> or use a single schema` |
| `theta/parse/return-no-common-type` | E | type | A theta or annotation-less-`fn` body whose tail-expression type and early-`return` operand types (every `return` syntactically present, regardless of static reachability) share no common upper bound and no sink narrows them. | [Function Definitions — Theta return type](../functions.md#theta-return-type) | Give the body a single return type, or add an explicit `fn` return annotation / sink that reconciles the operands. | `return operands have no common type; annotate the function return type or reconcile the operands` |
| `theta/parse/non-string-array-join` | E | type | `arr.join(...)` invoked on an array whose element type is not `string`. | [Expressions — `array<T>` stdlib](../expressions.md) | Map elements to strings first; no implicit type conversion in theta 1.0. | `array.join requires a string element type; got array<<element>>` |
| `theta/parse/extra-object-field` | E | parse | Schema constructor lists a field not declared by the schema. | [Expressions — Object construction](../expressions.md) | — | `extra field '<field>' on schema '<schema>'` |
| `theta/parse/missing-object-field` | E | parse | Schema constructor omits a declared (required) field. | [Expressions — Object construction](../expressions.md) | — | `missing field '<field>' on schema '<schema>'` |
| `theta/parse/object-field-type-mismatch` | E | type | A schema-constructor field value has a static type that is not compatible with the schema's declared type for that field under [Type System — Type compatibility](../type-system.md#type-compatibility), where the field value's type is statically resolvable. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `field '<field>' on schema '<schema>' type mismatch: expected <expected>, got <actual>` |
| `theta/parse/bare-object-literal` | E | parse | Bare `{ field: expr }` (no schema name) used in expression position outside the two documented carve-outs (`params:` defaults; a direct argument of a Pi-tool call). | [Expressions — Object construction](../expressions.md) | Name the schema: `Schema { field: expr }`. | `bare object literal not permitted in this position; name the schema (Schema { ... })` |
| `theta/parse/default-not-literal` | E | parse | A `params:` default RHS contains a form outside the [Theta literal sublanguage](../grammar.md#theta-literal-sublanguage) (an operator other than the unary `-` carve-out for numeric literals, function call, identifier reference other than `Enum.Variant`, `${...}` interpolation, or `@`...`` template). | [Frontmatter — Defaults](../frontmatter.md) | Replace the offending sub-expression with a literal value, or move the computation into theta body code. | `params default RHS must be a literal-sublanguage form; offending sub-expression: <expr>` |
| `theta/parse/default-without-literal` | E | parse | A `params:` field declares a default — a top-level `=` in the field's scalar — whose right-hand side is an empty or whitespace-only string, at whatever quoted or block-scalar spelling delivers it. The declaration form `field: type = literal` admits no arm without a `literal`, which is what this row refuses; distinct from `theta/parse/default-not-literal`, which refuses a default that DOES spell a form outside the [Theta literal sublanguage](../grammar.md#theta-literal-sublanguage). A field already refused by the type-text stage (`theta/load/params-type-not-expression`) draws this row's diagnostic no more than it draws the other default-side ones, and a field refused by this row draws neither `theta/parse/literal-newline-in-string`, `theta/parse/default-not-literal`, nor `theta/parse/params-default-type-mismatch`. The theta is not registered. | [Frontmatter — Defaults](../frontmatter.md) | — | `params default for '<field>' is empty; '=' must be followed by a literal-sublanguage form` |
| `theta/parse/params-default-type-mismatch` | E | type | A `params:` field's declared-type half, paired with that field's own default-literal half (the two halves `splitParamValue` separates, `frontmatter.ts`), whose default literal's static type is not `T₁ ⊑ T₂` under [Type System — Type compatibility](../type-system.md#type-compatibility) (`T₂` the declared type), the relation resolved against an EMPTY type environment at this position (a `params:` right-hand side carries no local alias/schema bindings of its own, so every `NamedType` the declared-type half spells is unresolvable here). The declared-type shapes this position can decide under that empty environment include a primitive, a union of primitives, and `array<T>` nesting over either. The default-literal shapes it can decide include a string / number / boolean / `null` literal (a unary-`-` numeric literal included) and a homogeneous array literal of them. Every other pairing defers and draws nothing here — a declared type that is a `NamedType`, an alias, an inline object type, or a literal type; a default that is a bare-key object literal, an `Enum.Variant` access, a variant-schema construction, an empty or heterogeneous array literal, or text that parses to no literal — each resolves to the relation's `"unknown"` case (the Unresolvable-operands paragraph of [Type System — Type compatibility](../type-system.md#type-compatibility)) and is left to the runtime AJV safety net. Two precedence rules bound this row to one diagnostic per field: a field whose type half already drew `theta/load/params-type-not-expression` keeps that diagnostic alone, and a field whose default half already drew `theta/parse/literal-newline-in-string` or `theta/parse/default-not-literal` keeps that diagnostic alone — this row does not run for either. The theta is not registered. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `param '<param>' default type mismatch: expected <expected>, got <actual>` |
| `theta/parse/non-trailing-default` | E | parse | A `params:` declaration places a non-defaulted field after a defaulted field in declaration order. | [Frontmatter — Defaults](../frontmatter.md) | Reorder `params:` so every defaulted field is trailing, or give the offending field a default. | `non-defaulted param '<field>' follows a defaulted param; defaulted params must be trailing` |
| `theta/parse/tool-arg-arity` | E | parse | A Pi-tool call site uses more than one positional argument (e.g. `read({...}, {...})`). | [Tool Calls — Argument shape](../tool-calls.md) | Pi tools take exactly one input object; merge the arguments. | `Pi tool '<name>' takes a single object argument; got <count>` |
| `theta/parse/tool-arg-not-object-literal` | E | parse | A Pi-tool call site's single positional argument is not written inline as a bare object literal `{ ... }` — e.g. `read(args)` where `args` is a `let`-bound value or any other bare identifier. RFC 0002 ([Computed field values in Pi-tool arguments](../../rfcs/0002-computed-tool-arguments.md)) lifted the field-*value* literal restriction (values are now full Theta expressions) but kept the argument *shape* rule so the tool's registered input schema supplies the field names. Distinct from `theta/parse/bare-object-literal`, whose "name the schema" remedy misdirects here: the fix is to inline the object literal, not to name a schema (the external tool schema already supplies the shape). | [Tool Calls — Argument shape](../tool-calls.md) | Inline the fields at the call site: `read({ path: expr, ... })`. | `Pi tool '<name>' argument must be written inline as a bare object literal { ... }; a let-bound value cannot supply the field shape` |
| `theta/parse/tool-arg-schema-conflict` | E | type | A Pi-tool call-site field-value expression has a static type provably disjoint (empty accepted-value intersection under the [schema subset](../schema-subset.md) mapping) from the tool's registered input-schema type for that field. Scoped to provable disjointness only: a `format`, `pattern`, numeric refinement, or a widened union with at least one satisfiable arm is not provable and falls through to the runtime AJV check (`Err(CodeToolError { cause: "validation", ... })`); the check never rejects a value the runtime AJV check would accept. | [Tool Calls — Argument shape](../tool-calls.md) | Pass a value whose type intersects the tool's input-schema type for the field. | `Pi tool '<name>' argument field '<field>' type is provably disjoint from the input schema: expected <expected>, got <actual>` |
| `theta/parse/let-without-initialiser` | E | parse | `let x: T` (annotation, no initialiser). Theta has no `undefined` value and no definite-assignment analysis. | [Grammar Appendix — `let` form](../grammar.md#let-form) | Initialise the binding at declaration, or restructure to bind once at the point a value is available. | `let binding '<name>' has no initialiser` |
| `theta/parse/let-rhs-type-mismatch` | E | type | The RHS initialiser of a typed binding `let x: T = expr` has a static type that is not compatible with the annotation `T` under [Type System — Type compatibility](../type-system.md#type-compatibility), where the RHS type is statically resolvable. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `let binding '<name>' initialiser type mismatch: expected <expected>, got <actual>` |
| `theta/parse/statement-in-arm-body` | E | parse | A `match` arm body is a bare `if` / `for` / `while` / `let` / assignment statement. Arm bodies are expressions; the ternary is the expression form of conditional. | [Grammar Appendix — `match` arm body](../grammar.md#match-arm-body) | Wrap the statements in a block expression `{ ... }` whose tail expression is the arm's value. | `match arm body must be an expression; wrap statements in a block expression { ... }` |
| `theta/parse/by-on-object-schema` | E | parse | A `by` clause on an object body (`schema X by f { ... }`), or on a right-hand side of fewer than two arms (`schema X by f = Cat`, against `UnionRhs ::= Type ("\|" Type)+`). The `by` clause applies only to discriminated-union schemas; the cut this code makes is the declaration's shape — object body, or arm count — so a right-hand side of two or more arms is outside this row's emission set whatever its arms are, and whether such a union then needs a discriminator is the discriminator rows' subject (`theta/parse/missing-discriminator` and neighbours, over object-schema arms). | [Grammar Appendix — `schema X by <field>`](../grammar.md#schema-x-by-field) | Use `schema X by f = A \| B \| …`, or drop the `by` clause if `X` is an object schema. | `the 'by' clause applies only to discriminated-union schemas (schema X by f = A \| B \| …)` |
| `theta/parse/doc-comment-misplaced` | E | parse | A `///` doc comment appears above a production other than `schema`, `enum`, schema field, enum variant, or `fn`. | [Grammar Appendix — `///` placement](../grammar.md#-placement) | Move the description to a legal anchor, or replace with a regular `//` comment. | `'///' doc comment is not legal above this production` |
| `theta/parse/generic-arity-mismatch` | E | parse | A generic-type application whose type-argument count does not match the constructor's declared arity in the closed `GenericType` set (`array` arity 1, `Result` arity 2) — e.g. `array<T, U>` or `Result<T>`. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | — | `generic type '<ctor>' expects <expected> type argument(s); got <actual>` |
| `theta/parse/void-in-non-return-position` | E | parse | `void` keyword used in a `Type` position other than a function/theta return type — a `let` annotation, schema or `params:` field, generic argument (`array<void>`, `Result<void, E>`), `invoke<void>` annotation, type ascription, or union arm. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | `void` is a return-only annotation; use a value type (or `null`) in this position. | `'void' is only permitted as a function or theta return type` |
| `theta/parse/result-in-schema-position` | E | parse | A `Result<T, E>` application appears in a lowered-schema position — a schema field type, a `params:` field type, or any type reachable transitively from those (including `array<T>` element types and union arms). `Result` is observed only by theta code and is never lowered to a JSON Schema fragment. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | `Result` has no lowered-schema form; use it only in `fn` / `let` / `invoke` positions, and feed the schema position a lowerable type. | `'Result' has no lowered-schema form and is not permitted in a schema-feeding position` |
| `theta/parse/unknown-identifier` | E | parse | Bare identifier in call or value position resolves to nothing in scope. | [Expressions — Identifier resolution](../expressions.md) | — | `unknown identifier '<name>'` |
| `theta/parse/shadowed-callable-call` | E | parse | A bare-identifier call whose callee resolves to a local binding — a `let`, function parameter, `for` / `par for` variable, `match` pattern binding, or `params:` field — that shadows a name in the theta's callable set. Locals are not callable (functions are not first-class), so the call site is erroneous; binding the name without calling it stays legal. `<binder>` renders the shadowing binder and its line, e.g. `let binding at line 6` (borrowed from the nearest enclosing node for `fn` parameters and `match` bindings, which carry no ranges of their own — the `fn` declaration's and the arm body's start line respectively; a `params:` field carries none). | [Expressions — Identifier resolution](../expressions.md) | Rename the local binding, or give the `tools:` entry a distinct name with `as`. | `call of '<name>' resolves to the local <binder> that shadows the callable-set entry '<name>'; locals are not callable` |
| `theta/parse/unknown-method` | E | parse | Method or property accessed on a built-in type that the theta 1.0 stdlib does not expose. | [Expressions — Built-in methods](../expressions.md) | — | `unknown method '<method>' on type <type>` |
| `theta/parse/non-array-iterand` | E | type | `for x in expr` where `expr` is not `array<T>`. | [Control Flow — `for`/`in`](../control-flow.md) | Use `obj.keys()` for objects, `s.split(...)` for strings. | `'for' expects array<T> after 'in'; got <type>` |
| `theta/parse/break-outside-loop` | E | parse | `break` outside the body of a `for` / `while` loop. | [Control Flow — `break`/`continue`](../control-flow.md) | — | `'break' outside of a loop` |
| `theta/parse/continue-outside-loop` | E | parse | `continue` outside the body of a `for` / `while` loop. | [Control Flow — `break`/`continue`](../control-flow.md) | — | `'continue' outside of a loop` |
| `theta/parse/break-with-value` | E | parse | `break expr` (theta 1.0 `break` carries no value). | [Control Flow — `break`/`continue`](../control-flow.md) | — | `'break' takes no value in theta 1.0` |
| `theta/parse/illegal-template-escape` | E | lex | Backslash followed by an unrecognised character inside a `@`...`` query template body. | [Query — Escapes](../query.md) | — | `illegal escape sequence in @\`...\` template: \\<char>` |
| `theta/parse/unterminated-template` | E | lex | EOF reached while scanning a `@`...`` query template. | [Query — Multi-line templates](../query.md) | — | `unterminated @\`...\` query template` |
| `theta/parse/discarded-query-result` | E | parse | Bare `@`...`` expression-statement (the `Result` is dropped without `?` or `let _ =`). | [Query — Discarded query results](../query.md) | Use `?` to propagate failure or `let _ = @\`...\`` to discard explicitly. | `query result discarded; use ? to propagate failure or 'let _ = ...' to discard explicitly` |
| `theta/parse/empty-template` | W | parse | `@`...`` template's static body is empty or whitespace-only (ASCII whitespace per [System-note rendering](../binder/defaulting-system-note-echo.md#system-note-rendering) rule 1, not the regex `\s` class) after newline-trim and dedent. The theta still loads; the runtime short-circuits the query if the fully-rendered text is also empty. | [Query — Degenerate rendered templates](../query.md) | Add literal text or use `\\n` to keep an intentionally-blank prompt. | `query template body is empty after newline-trim and dedent` |
| `theta/parse/interpolated-result` | E | type | `${expr}` interpolation whose `expr` has Theta static type `Result<T, E>` (the runtime renderer raises the same code as a panic when the type is statically unresolvable); the runtime renderer also raises it when `expr`'s static type is `array<T>` or a Schema-typed object and the evaluated value holds a `Result` at any depth — a container's own static type is never `Result<T, E>`, so this sub-case is reached only at runtime, never at parse. | [Query — Stringification of interpolated values](../query.md) | Unwrap with `?` or `match` before interpolating. | `Result value cannot be interpolated; unwrap with ? or match first` |
| `theta/parse/explicit-schema-mismatch` | W | parse | Both a binding annotation and an explicit `@<Schema>` ascription are present, and the ascription is not compatible with the annotation under [Type System — Type compatibility](../type-system.md#type-compatibility) (i.e. `ascription ⋢ annotation`). Safe widenings (e.g. `@<integer>` under a `number` annotation) do not warn. | [Query — Explicit form](../query.md) | — | `explicit @<Schema> ascription is not compatible with binding annotation` |
| `theta/parse/empty-query-annotation` | E | parse | An explicit `@<…>` query annotation whose interior is empty or trims to empty — `@<>`, `@<  >`, a tab- or newline-only interior, or an unterminated `@<` at end of input (the capture runs to EOF and still trims to empty). The type grammar derives no empty `Type` ([Grammar Appendix — Type grammar](../grammar.md#type-grammar); [Type System](../type-system.md) applies the same grammar to the `@<T>` annotation position), so an empty ascription supplies no response schema — accepted silently it would mark the query typed while giving the runtime nothing to validate the response against, and (worse than absence) block the inference contexts a real ascription overrides. | [Query — Explicit form](../query.md) | Name a schema (`@<Schema>`) or drop the annotation for an untyped query. | `\`@<>\` query annotation is empty; write \`@<Schema>\` or drop the annotation for an untyped query` |
| `theta/parse/match-arm-type-mismatch` | E | type | A `match` arm's body type is not assignable to the common type of the other arms. | [Expression Sublanguage — Arm syntax](../expressions.md#match-expression) | — | `match arm body type does not match the common type of the other arms` |
| `theta/parse/question-outside-result-fn` | E | type | `?` used in a function or top-level theta whose return type is not `Result<T, QueryError>` for some `T` (and cannot be inferred to one). | [Expression Sublanguage — `?` operator](../expressions.md#question-operator) | Declare the enclosing scope's return type as `Result<T, QueryError>` for some `T`. | `'?' used in a scope whose return type is not Result<T, QueryError>` |
| `theta/parse/question-on-non-result` | E | type | `?` applied to an operand whose Theta static type is not `Result<T, QueryError>` for some `T` (e.g. `let x = 5?`). Distinct from `theta/parse/question-outside-result-fn`, which checks the enclosing scope's return type rather than the operand type. | [Expression Sublanguage — `?` operator](../expressions.md#err-18) | Apply `?` only to a `Result`-typed operand (a `@`-query, `invoke(...)`, or `Ok(...)` / `Err(...)`). | `'?' requires a Result operand; got <type>` |
| `theta/parse/match-guard-not-supported` | E | parse | Guarded `match` arm (`Pattern if cond => ...`). | [Expression Sublanguage — Pattern grammar](../expressions.md#pattern-grammar) | Deferred to a future release. | `match guards are not supported in theta 1.0` |
| `theta/parse/rest-pattern-not-supported` | E | parse | Rest pattern (`[first, ...rest]`, `{ kind, ...other }`). | [Expression Sublanguage — Pattern grammar](../expressions.md#pattern-grammar) | Deferred to a future release. | `rest patterns are not supported in theta 1.0` |
| `theta/parse/bare-return-in-non-void` | E | type | Bare `return` (no argument) in a function or theta whose return type is not `void`. | [Return Statement](../return.md) | — | `missing return value` |
| `theta/parse/unreachable-code` | W | parse | Statement appears after a `return` in the same block. | [Return Statement](../return.md) | — | `unreachable code after return` |
| `theta/parse/nested-fn` | E | parse | `fn` declaration nested inside another `fn` body or a block. | [Function Definitions — Placement](../functions.md) | Top-level `fn` only in theta 1.0. | `nested 'fn' declarations are not supported in theta 1.0` |
| `theta/parse/function-as-value` | E | parse | Function name used outside call position (bound to `let`, passed as argument, etc.). | [Function Definitions — Placement](../functions.md) | — | `function '<name>' used outside call position; functions are not first-class in theta 1.0` |
| `theta/parse/redundant-wire-name` | W | parse | `field as "field"` rename whose wire name equals the theta-side name. | [Schemas — Wire-name renaming](../schemas.md) | Drop the `as` clause. | `redundant 'as' clause: wire name '<name>' equals the theta-side name` |
| `theta/parse/wire-name-collision` | E | parse | Two fields in the same schema share a wire name, or a wire name collides with another field's theta-side name. | [Schemas — Wire-name renaming](../schemas.md) | — | `wire name '<name>' collides with another field on schema '<schema>'` |
| `theta/parse/empty-schema-body` | E | parse | A `schema` declaration whose shape yields no usable content (neither fields nor alias arms): an empty object body (`schema X { }`), a body whose first token is not a plain `ident: Type` field list, or no shape at all. An empty inline object type (`{}`) in any `Type` position, at any nesting depth. | [Schemas — Object schemas](../schemas.md) | — | `'<X>' has no fields; an empty schema cannot be validated.` |
| `theta/parse/duplicate-inline-field-name` | E | parse | Two or more entries of one inline object type (`ObjectType`)'s interior share a key. The inline spelling reuses the object-schema `Field` form and carries the same field semantics, so two entries sharing one key declare one wire name twice; the row applies in any `Type` position and at any nesting depth reachable through inline object fields and union arms. `ObjectType` spells a closing `}`, so an interior that never closes spells no inline object type and carries no comparison of its own. The comparison runs over the entries a brace-and-angle-aware split of the interior on top-level commas yields (quoted regions skipped), keyed on each entry's own text before its own top-level `:`, taken verbatim after `trim()` — the same split and the same text both lowerers key their `properties` and `required` writes on, so `"a"`, `'a'` and `a as "w"` are the keys they are written as, `{'a': …, "a": …}` is two keys and not one, and padding around an entry's pre-colon text is absorbed by the trim; no unquoting and no normalisation otherwise apply. An entry with no top-level `:`, or whose pre-colon text trims to empty, spells no key and contributes none; an entry whose TYPE position is empty still keeps its key. The key list this row compares is derived from exactly the bytes a `Type` position hands the lowerer, by the same split and the same colon, so a repeated key here is a property of the same text the lowering keys its `properties` and `required` writes on; where a position reconstructs its type-source text from lexer tokens rather than reading it verbatim, the two agree on that reconstruction too. One diagnostic per repeated name, at that name's second position — a name written three times draws one line, and two repeated names draw two; a body's own repeats are reported before those of the inline objects nested in its field types. Two shapes sit outside this row rather than under it: a key reused between an outer inline object and one nested inside it is two interiors and two entry lists, not a repeat; and a generic type argument's interior (`array<{ … }>`) is outside the trigger, because the lowering never divides that interior into fields and mints no duplicate `required` there. The subject the *Message* renders is that raw key, not necessarily identifier-shaped, which is why `<field>` carries a row-scoped carve-out on this row (see [Placeholder rendering — Source-derived placeholders](./placeholder-rendering-b.md#5-source-derived-placeholders)). The named-declaration spelling of the same two fields keeps `theta/parse/wire-name-collision`. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | — | `duplicate field name '<field>' within one inline object type` |
| `theta/parse/malformed-alias-rhs` | E | parse | The right-hand side of a `schema X = …` (or `schema X by f = …`) declaration is not an `AliasRhs ::= Type ("\|" Type)*`, in two shapes. *Empty arm position* — a top-level `\|` with no `Type` on one of its sides (`schema X = Cat \|`, `schema X = \| Cat`, `schema X = Cat \|\| Cat`). *Same-line residue* — a capture that completes with at least one arm, followed on the same source line as its last token by a boundary token of one of these kinds: an identifier, a keyword, or a string or number literal (`schema X = Cat Cat`, `schema X = -1`, `schema X = string "junk"`, `schema X = string \| integer 7`, `schema X = array<integer> 42`, `schema X = array<integer> let a = 1`), or a punctuation head among the query-template `@`, a bare backtick template, `(`, `[`, `!`, and a unary-negation `-` (`schema X = Cat (1)`, `schema X = Cat !true`). Three boundaries sit outside this row rather than under it: a stray `,`, `)`, `=` or `}` there keeps `theta/parse/unsupported-feature` (`schema X = Cat, Dog`, `schema X = Cat )`, `schema X = Cat = 1`, `schema X = Cat }`); a `{` there keeps `theta/parse/bare-object-literal` (`schema X = Cat { a: "x" }`); and an operator with no operand behind it is absorbed INTO the arm rather than left at the boundary (`schema X = Cat +`, `schema X = string +`), so nothing is left over at the boundary and no token stands there for this row to name — the arm itself, carrying the absorbed operator, is `theta/parse/schema-type-not-expression`'s to refuse, not this row's. A right-hand side that yields no arm at all is `theta/parse/empty-schema-body` rather than this code, and a token on the NEXT source line is not residue on either of two grounds: when no continuation trigger holds at the end of the declaration's line, the newline closes the declaration ([Grammar Appendix — Newline continuation](../grammar.md#newline-continuation)); when a trailing trigger holds so that the newline separates nothing (`schema X = array<integer>` with `let a = 1` on the next line), the right-hand side still ends at its last arm, ahead of that line's first token. The declaration keeps the arms it captured and its range, and a severed residue keeps the disposition it already had in statement position — it parses as the statement it spells when it spells one (`schema X = Cat Cat`), and is discarded unreported when it spells none (`schema X = Cat else`): this code adds an emission and changes no other observable. | [Schemas — Type-alias / union schema](../schemas.md) | Separate union arms with a single `\|`, and start a new line for text that is not part of the type. | `'<X>' has a malformed right-hand side; write a single type, or arms separated by single '\|', and nothing else on the declaration's line` |
| `theta/parse/schema-type-not-expression` | E | parse | A `schema` object-body field type, or an arm of a `schema X = …` (or `schema X by f = …`) alias/union declaration, carries text that derives from no `Type` production ([Grammar Appendix — Type grammar](../grammar.md#type-grammar)). The unit judged is a single brace-free FRAGMENT — the same unit `lowerTypeExpr`'s trailing catch-all is handed — not the whole arm or field type: a fragment is judged at the type's own top level (the whole arm or field type), inside a union arm at either position, inside a `GenericType` type argument, and inside an inline `ObjectType`'s field type at any nesting depth, so the judgement re-enters at every level a hoisted inline object's own field lowering reaches. The spelling that delivers the arm does not matter: the plain and `by` alias spellings, and the `.theta` and `.thetalib` extensions, all reach this judgement identically. Refused is a fragment every arm of the `Type` grammar's own recognisers — a `PrimitiveType`, a `NamedType`, a `GenericType`, an inline `ObjectType`, or a `LiteralType` — has declined: an operator absorbed into the fragment with no operand behind it (`Cat +`, `Cat .`, `string +`, `Cat *`, `Cat /`, `Cat %`, `Cat ==`, `Cat &&`, `Cat ?`, `Cat :`, `Cat +++`, `string ++`), an operator alone (`+`), a dangling, leading, or lone `\|` at the field position (`string\|`, `Cat\|`, `\|string`, `\|`), and punctuation text (`???`). Not this text is any `LiteralType` atom or union arm — it lowers under its own emission and draws no diagnostic here — nor is any fragment carrying a `{` or `}` anywhere, balanced or not: the brace frame owns every brace-carrying fragment and this row owns the brace-free ones, the same decline `theta/load/params-type-not-expression` shares for the `params:` position rather than a second copy, so a fragment that reaches the judgement WHOLE with a brace in it stays admitted with its own (possibly permissive) lowering at every `Type` position alike; the exemption is the fragment's own, not its enclosure's. One diagnostic per offending fragment, no dedup: two junk fragments in one declaration render two diagnostics with identical text and identical range, because the row's only placeholder names the declaration and not the fragment — each fragment is refused independently, so fixing one does not silently hide the other. Every diagnostic carries the DECLARATION's range (`SchemaFieldSource` and an arm string carry no range of their own). Two precedence rules keep this to one diagnostic per scope: a field or declaration that already carries an error-severity diagnostic from its own walk — a position rule (`void`, generic arity, `Result`, an inline `enum[...]`), a reserved keyword, or an unresolved name — keeps that diagnostic alone and draws no refusal; and a declaration `theta/parse/malformed-alias-rhs` already refused at parse time is skipped, so the refusal never cascades onto a right-hand side another row already named. `{}` keeps its own `theta/parse/empty-schema-body` alone (an admitted, empty `ObjectType`, not a fragment this row reaches), and `schema S { a: -1 }` keeps `theta/parse/empty-schema-body` alone because the malformed field list is dropped whole at parse time, so no field-type walk ever runs and no fragment reaches this judgement. The theta is not registered. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | Write a well-formed `Type` — drop the trailing operator or stray text; the position accepts a primitive, a named type, a generic, an inline object, a union, or a literal. | `'<X>' declares a type that is not a theta type expression` |
| `theta/parse/empty-enum-body` | E | parse | `enum X { }` declaration with no variants. | [Schemas — Enum declarations](../schemas.md) | — | `'<X>' has no variants; an empty enum cannot be validated.` |
| `theta/parse/unknown-variant` | E | parse | `Enum.Variant` reference where `Variant` is not a declared variant of `Enum`. | [Schemas — Variant access](../schemas.md) | — | `unknown variant '<variant>' on enum '<enum>'` |
| `theta/parse/unresolved-named-type` | E | parse | A `NamedType` that resolves to no declaration usable at the position it is written. The positions are the `params:` right-hand side, the `@<T>` query annotation, a `schema` body field type, the right-hand side of a `schema X = ...` alias/union declaration, and an object-constructor name (`Name { ... }`). Resolution is whole-file over the body's top-level declarations — a top-level `schema` / `enum` declaration or a symbol imported from a `.thetalib` resolves the name wherever in the body it is declared, so a frontmatter-to-body forward reference is not itself a failure. The object-constructor position carries the additional requirement that the resolved declaration be brace-constructible, which in this file means a `schema` with an object body: a name declared here as an `enum`, or as a `schema` without an object body, is not constructible and fires this code. An imported symbol always defers at the constructor position — the importer's parse holds neither its field bodies nor its kind — so the field-set checks do not run and this code does not fire. | [Frontmatter — params](../frontmatter.md); [Expressions — Object construction](../expressions.md#object-construction) | — | `unresolved named type '<name>'` |
| `theta/parse/duplicate-enum-value` | E | parse | Two `enum` variants share an explicit string value. | [Schemas — Enum declarations](../schemas.md) | — | `duplicate enum value '<value>' across variants of enum '<enum>'` |
| `theta/parse/duplicate-enum-variant-name` | E | parse | Two variants in the same `enum` share an identifier, regardless of explicit-value assignment. The name-duplication check runs before `theta/parse/duplicate-enum-value`, so both implicit-value and distinct-explicit-value name collisions fail on this code. | [Schemas — Enum declarations](../schemas.md) | — | `duplicate variant name '<variant>' on enum '<enum>'` |
| `theta/parse/non-string-enum-value` | E | parse | `enum` variant explicit value is not a single string literal. | [Schemas — Enum declarations](../schemas.md) | theta 1.0 enums carry string values only. | `enum variant value must be a string literal; got <kind>` |
| `theta/parse/inline-enum` | E | parse | `enum["a", "b"]` or other inline-enum form. | [Schemas — Enum declarations](../schemas.md) | Use a literal-union (`"a" \| "b"`) or a top-level `enum` declaration. | `inline 'enum[...]' is not supported; use a top-level 'enum' declaration or a literal-union` |
| `theta/parse/ambiguous-discriminator` | E | parse | Discriminated-union detection finds more than one candidate field. | [Schemas — Discriminated unions](../schemas.md) | Declare explicitly with `by <field>`. | `ambiguous discriminator for <X>; candidates: <fields>. Declare explicitly with 'by <field>'.` |
| `theta/parse/missing-discriminator` | E | parse | Discriminated-union detection finds no candidate field. | [Schemas — Discriminated unions](../schemas.md) | Add a `kind` (or similar) field to each variant, or declare explicitly with `by <field>`. | `<X> is a union of object schemas with no shared single-literal discriminator field. Add a 'kind' (or similar) field to each variant, or declare explicitly with 'by <field>'.` |
| `theta/parse/duplicate-discriminator-value` | E | parse | Two variants of a discriminated union share the same discriminator value. | [Schemas — Discriminated unions](../schemas.md) | — | `duplicate discriminator value '<value>' across variants of <X>` |
| `theta/parse/nested-discriminator` | E | parse | Discriminator field is not at the top level of each variant (e.g. `kind: { type: "x" }`). | [Schemas — Discriminated unions](../schemas.md) | — | `discriminator field '<field>' must be at the top level of each variant of <X>` |
| `theta/parse/non-string-discriminator` | E | parse | Discriminator field's per-variant literal type is not `string` — i.e. a numeric or boolean literal `const`. Applies equally to implicit detection and to the explicit `by <field>` form. | [Schemas — Discriminated unions](../schemas.md) | Wrap the tag as a string (e.g. `kind: "v1"` rather than `kind: 1`). | `discriminator '<field>' on <X> must be a string-literal type; got <kind>` |
| `theta/parse/type-alias-cycle` | E | parse | A `schema X = ...` whose right-hand side reduces to `X` through type aliases only — directly (`schema X = X`) or transitively (`schema X = Y; schema Y = X`). Cycles that traverse at least one object-schema hop remain legal. Detection runs after schema-name resolution but before lowering. | [Schemas — Recursion](../schemas.md) | — | `type-alias cycle: <path>` |
| `theta/parse/system-on-prompt-mode` | E | parse | `system:` frontmatter field declared on a `mode: prompt` theta. | [Frontmatter — `system`](../frontmatter.md) | Either switch to `mode: subagent` or remove `system:`. | `'system:' is not permitted on a mode: prompt theta` |
| `theta/parse/system-interp-not-path` | E | parse | Body of a `${...}` interpolation in `system:` is not a bare identifier path (e.g. `${arr[0]}`, `${a + b}`, `${f(x)}`, `${a?.b}`, `${"x"}`). | [Frontmatter — `system` Interpolation](../frontmatter.md) | The `system:` slot accepts only bare identifier paths; see [future-considerations.md](../future-considerations.md) for richer expressions. | `'system:' interpolation body must be a bare identifier path` |
| `theta/parse/system-interp-unknown-param` | E | parse | Head identifier of a `system:` `${...}` path does not name a declared `params` field. | [Frontmatter — `system` Interpolation](../frontmatter.md) | — | `'system:' interpolation references unknown param '<name>'` |
| `theta/parse/system-interp-bad-field` | E | parse | A `.Ident` step in a `system:` `${...}` path does not name a reachable theta-side `params` object field, or descends into an array or un-narrowed discriminated union. | [Frontmatter — `system` Interpolation](../frontmatter.md) | — | `'system:' interpolation '.<field>' does not name a reachable object field on <path>` |
| `theta/parse/system-interp-unterminated` | E | parse | `${` in a `system:` value is not closed by a matching `}` before the YAML scalar ends. | [Frontmatter — `system` Interpolation](../frontmatter.md) | — | `'system:' interpolation '${' is not closed by a matching '}'` |
| `theta/parse/timeout-field-rejected` | E | parse | `timeout:` field declared on a query, tool call, or invoke (theta 1.0 has no per-call timeout). | [Cancellation](../cancellation.md) | Per-call timeouts are deferred. | `'timeout:' field is not supported in theta 1.0` |
| `theta/parse/bind-context-session-on-subagent` | W | parse | `bind_context: session` declared on a `mode: subagent` theta. | [Binder — Binder context](../binder.md) | Subagent-mode thetas have no caller-session context. | `'bind_context: session' has no effect on a mode: subagent theta` |
| `theta/parse/bind-echo-on-bypass` | W | parse | An explicit `bind_echo: true` is declared on a single-string-bypass theta (`params:` declares exactly one no-default `string` field); a defaulted `bind_echo` does not fire this warning. The no-params bypass is owned by `theta/load/bind-echo-without-params`, not this code. | [Binder — Echo policy](../binder.md) | The single-string bypass auto-suppresses echo regardless. | `'bind_echo: true' has no effect on a single-string-bypass theta` |
| `theta/parse/thetalib-top-level-statement` | E | parse | `.thetalib` file contains a top-level statement, `let`, or query. | [Imports — `.thetalib` file rules](../imports.md) | Move the code into a `fn` body. | `top-level statement not permitted in .thetalib file; move into a fn body` |
| `theta/parse/import-name-collision` | E | parse | Two imports bring in the same symbol name, or an imported symbol collides with a top-level declaration in the same file. | [Imports — Name collisions](../imports.md) | Resolve with `as`-aliasing. | `imported symbol '<name>' collides with another import or top-level declaration` |
| `theta/parse/import-unknown-symbol` | E | parse | An `import { ... }` or `export { ... } from` specifier (including `as`-aliased forms) names a symbol that is neither a top-level declaration nor a transitive `export … from` re-export of the resolved `.thetalib` file. Fires after the resolved file's own parse completes and is collected by the multi-error batching rule rather than fast-failing; `<name>` renders the source symbol, not the alias. | [Imports — Unknown imported symbol](../imports.md) | — | `imported symbol '<name>' is not declared or re-exported by '<path>'` |
| `theta/parse/import-reserved-synthesised-name` | E | parse | A parsed `import` / `export` specifier's LOCAL binding (the `as` alias where present, else the source name) matches one of schema-subset.md §Synthesised names' four forms exactly — `__inline_<slug>`, `__theta_respond_<slug>`, `__theta_bind_<slug>`, `__theta_callee_<slug>__<post-rename-name>` — with `<slug>` exactly 16 lowercase hex characters (schema-subset.md §Canonical schema hash step 4). Checked per specifier at parse time, on both statement kinds: it fires whether or not the `.thetalib` path resolves, and whether or not a `from` clause follows the specifier list, co-emitting with `theta/parse/import-missing-from-clause` on a from-less list. `<name>` renders the LOCAL binding, not the source symbol. | [Lexical — Identifiers](../lexical.md) | Resolve with `as`-aliasing. | `imported symbol '<name>' binds a reserved synthesised name` |
| `theta/parse/import-missing-from-clause` | E | parse | A specifier list — on either an `import` or an `export` statement — not followed by a `from` clause naming a `.thetalib` path literal: the `from` keyword absent, or present with no `string` token after it. Covers the degenerate bare-keyword (`import`, `export`) and empty-list (`import {}`, `export {}`) spellings. Fires once per statement, ranged over the whole statement — never once per specifier — and co-emits with `theta/parse/import-reserved-synthesised-name` when a specifier's local binding is also a reserved synthesised name. | [Imports — Re-exports](../imports.md) | Add a `from` clause naming the `.thetalib` the symbols come from. | `import / export specifier list requires a 'from' clause with a .thetalib path literal` |
| `theta/parse/invoke-arg-type-mismatch` | E | type | `invoke(...)` argument does not type-check against the callee's declared `params` schema (when the callee is statically resolvable). | [Invocation — Argument binding](../invocation.md) | — | `invoke argument <i> ('<param>') type mismatch: expected <expected>, got <actual>` |
| `theta/parse/tool-arg-type-mismatch` | E | type | `<name>(args)` `.theta`-callable-call argument does not match the callee theta's `params` (when statically resolvable). Pi-tool argument type mismatches are not a parse error — they surface at runtime as `Err(CodeToolError { cause: "validation", ... })`; see [Tool Calls — Argument shape](../tool-calls.md). | [Tool Calls — Argument shape](../tool-calls.md) | — | `tool '<name>' argument type mismatch: expected <expected>, got <actual>` |
| `theta/parse/fn-arg-type-mismatch` | E | type | A plain top-level `fn` call `f(args)` — a same-file or imported `.thetalib` function call that is neither an `invoke(...)` nor a `.theta`-callable call — passes an argument whose static type is not compatible with the matched parameter's declared type. Always parse-time: top-level `fn` declarations are hoisted and always statically resolvable, so no runtime AJV safety net applies. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `fn '<name>' argument <i> ('<param>') type mismatch: expected <expected>, got <actual>` |
| `theta/parse/invoke-return-type-mismatch` | E | type | `invoke<Schema>(...)` annotation is structurally incompatible with the callee's inferred return type, where both annotation and callee are statically resolvable. Also fires for a `subagent fn` return annotation: `): T` declares the boundary Ok payload — the `invoke<T>` analogue ([Functions — FN-6](../functions.md#fn-6)) — and is checked against the body's inferred Ok payload the same way (`<callee>` renders the function name). | [Invocation — Typed return](../invocation.md) | Widen the annotation, narrow the callee, or drop the annotation and let the runtime AJV check decide. | `invoke<Schema> annotation incompatible with callee '<callee>' return type <actual>` |
| `theta/parse/invoke-arity-too-few` | E | parse | `invoke(...)` or `.theta` callable call passes fewer arguments than the callee's count of non-defaulted `params:`, where the callee is statically resolvable. | [Invocation — Argument arity](../invocation.md#argument-binding) | Provide the missing argument(s) or default the corresponding `params:` field on the callee. | `invoke '<callee>' passes too few arguments: expected <required> non-defaulted, got <provided>` |
| `theta/parse/invoke-arity-too-many` | E | parse | `invoke(...)` or `.theta` callable call passes more arguments than the callee's total `params:` count. Always parse-time — no runtime safety net is possible. | [Invocation — Argument arity](../invocation.md#argument-binding) | Drop the extra argument(s); positional binding has no destination for them. | `invoke '<callee>' passes too many arguments: expected at most <max>, got <provided>` |
