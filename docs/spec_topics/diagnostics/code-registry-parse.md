# Code registry parse

## Code registry

The V1 diagnostic registry is split across four pages — the `theta/parse/*` table on this page and the [`theta/load/*`](./code-registry-load.md), [`theta/runtime/*`](./code-registry-runtime.md), and [`theta/host/*`](./code-registry-host.md) continuation tables — which together enumerate every diagnostic the V1 spec defines. The column legend, the closed *Phase* taxonomy, and the *E/W* severity carve-out that govern all four tables are defined in [Diagnostic shape — Code registry rules (normative)](./diagnostic-shape.md#column-legend).

### `theta/parse/*` — lexical, parse, and type-system errors

| Code | Sev | Phase | Trigger | Spec rule | Hint | Message |
|---|---|---|---|---|---|---|
| `theta/parse/illegal-escape` | E | lex | Backslash followed by an unrecognised character inside a regular string literal. | [Lexical — String literals](../lexical.md) | — | `illegal escape sequence: \\<char>` |
| `theta/parse/invalid-unicode-escape` | E | lex | A recognised `\u{...}` escape whose value exceeds `U+10FFFF` or lies in the UTF-16 surrogate range `U+D800`–`U+DFFF`. | [Lexical — String literals](../lexical.md) | Use a Unicode scalar value: `≤ U+10FFFF` and outside `U+D800`–`U+DFFF`. | `invalid Unicode escape: value is not a Unicode scalar value` |
| `theta/parse/literal-newline-in-string` | E | lex, parse | Literal newline inside a regular (single-line) string literal. Also fires inside a `params:` default right-hand side, which is a strict subset of the same expression grammar ([Theta literal sublanguage](../grammar.md#theta-literal-sublanguage)). | [Lexical — String literals](../lexical.md) | Use a `@`...`` query template, or split with `+ "\n" +`. At a `params:` default right-hand side, use the `\n` escape, or move the value into body code. | `literal newline in string literal` |
| `theta/parse/unterminated-string` | E | lex, parse | EOF reached while scanning a string literal. Also fires inside a `params:` default right-hand side whose text carries a string literal that never closes — the position reaches no lexer, and the literal sublanguage is a strict subset of the same expression grammar ([Theta literal sublanguage](../grammar.md#theta-literal-sublanguage)). A field refused there draws none of `theta/parse/literal-newline-in-string`, `theta/parse/default-not-literal`, `theta/parse/default-without-literal` or `theta/parse/params-default-type-mismatch`, and the theta is not registered. This widening is a [Governance — Source-language stability](../governance/source-language-stability.md#gov-15) addition: every newly-refused spelling loaded cleanly before it. | [Lexical — String literals](../lexical.md) | — | `unterminated string literal` |
| `theta/parse/invalid-path-separator` | E | lex | Backslash inside a path literal (`import`, `invoke`, `tools:` `.theta` entry). | [Lexical — Path literals](../lexical.md) | Use `/` separators only. | `invalid path separator: backslash in path literal` |
| `theta/parse/stray-backslash` | E | lex | Backslash byte at a source position outside any string literal, path literal, or `@`...`` query-template body. | [Lexical — Source files](../lexical.md#stray-backslash) | — | `stray backslash in source` |
| `theta/parse/invoke-non-theta-extension` | E | parse | An `invoke(...)` literal or a `tools:` `.theta` entry whose path string does not end in `.theta`. The check runs *after* `theta/parse/invalid-path-separator`, on the path literal as written (no realpath normalisation), and is byte-exact lowercase per [Lexical — Path literals](../lexical.md). | [Invocation — Resolution](../invocation.md) | invoke and `tools:` paths must end in `.theta`; use `import` for `.thetalib` library code. | `invoke path '<path>' does not end in .theta` |
| `theta/parse/import-non-thetalib-extension` | E | parse | An `import` path literal that does not end in `.thetalib` (including a `.theta` path). The check runs *after* `theta/parse/invalid-path-separator`, on the path literal as written (no realpath normalisation), and is byte-exact lowercase per [Lexical — Path literals](../lexical.md). | [Imports — Path resolution](../imports.md) | import paths must end in `.thetalib`; `.theta` files are not importable — use `invoke(...)` instead. | `import path '<path>' does not end in .thetalib` |
| `theta/parse/binding-case-mismatch` | E | parse | Identifier in a binding / parameter / fn-name / field-name position does not start with a lowercase letter or `_`. | [Lexical — Identifiers](../lexical.md) | — | `binding name must start with a lowercase letter or _` |
| `theta/parse/schema-case-mismatch` | E | parse | Identifier in a schema / enum / variant / type-alias position does not start with an uppercase letter. | [Lexical — Identifiers](../lexical.md) | — | `schema name must start with an uppercase letter` |
| `theta/parse/reserved-keyword-as-identifier` | E | parse | Reserved keyword used in an identifier position. | [Lexical — Reserved keywords](../lexical.md) | — | `reserved keyword '<keyword>' cannot be used as an identifier` |
| `theta/parse/capitalised-pattern-head` | E | parse | A capitalised identifier in `match` pattern-head position that heads none of the admitted pattern productions: it is not the `Ok(p)` / `Err(p)` constructor spelling and it is not followed by `{`. | [Expressions — Pattern grammar](../expressions.md) | Lowercase the name to bind it, use `_`, or write the constructor `Ok(p)` / `Err(p)` or object `Ident { … }` form. | `capitalised pattern head '<name>' names no pattern production` |
| `theta/parse/single-line-if` | E | parse | `if` / `for` / `while` / `fn` body is not a braced block (e.g. `if (x) stmt`). | [Lexical — Statement terminators](../lexical.md) | Wrap the body in `{ ... }`. | `single-line body not permitted; wrap in { ... }` |
| `theta/parse/fn-param-list-unclosed` | E | parse | An `fn` declaration's parenthesised parameter list is never closed by a matching `)`: the parameter loop reaches EOF, or a block-open `{` is reached first — `{` derives from no `FnParam` position, so it cannot be a parameter and a `)` before it would already have exited the loop. Scoped to `fn` parameter lists alone; no other unbalanced bracket is judged by this row. Withheld when a parameter type capture consumed an unmatched `)` token — strictly more `)` punctuation tokens than `(` punctuation tokens over the token span the capture consumed (the unfloored `<` / `>` depth counter inside `parseType`, [bug 0124](../../bugs/0124-parsetype-trailing-punctuation-leniency.md)): the closer the author wrote for the list was then swallowed by the type, not omitted, so neither the verdict nor the recovery below fires and the input keeps its capture-level disposition. A `)` that is merely a character inside a string or template token of the type, or one half of a balanced `(...)` pair the type carries, is not a consumed closer and withholds nothing. Recovery: the cursor is left ON the `{`, so `parseBlock` takes it as the `FnBody` the author wrote and the statements that follow return to the top-level statement list. The theta is not registered. | [Grammar Appendix — `fn` declarations](../grammar.md#fn-declarations); [Function Definitions — FN-1](../functions.md#fn-1) | Close the parameter list with `)` before the `fn` body's `{`. | `fn parameter list is not closed by ')'` |
| `theta/parse/fn-param-not-identifier` | E | parse | An `fn` parameter-name position holds a `punct`, `number`, `string`, or `template` token — a shape `FnParam ::= Ident ":" Type` derives from no reading, `Ident` being `[A-Za-z_][A-Za-z0-9_]*` ([Lexical — Identifiers](../lexical.md)) — and the list still closes on its own `)`. A list that instead reaches EOF or a body-open `{` first is judged by `theta/parse/fn-param-list-unclosed` alone; this row does not double that verdict. Only the first such token in the list is reported. Withheld under the same absorbed-`)` condition as `theta/parse/fn-param-list-unclosed` above (a parameter type capture that consumed the list's own closer). Exempt on a token shifted into the name slot by a `mut` modifier consumed in the same loop iteration ([bug 0148](../../bugs/0148-reserved-keyword-fn-parameter-position-silent.md) §Fix (d)) — that shift is a recovery artefact, not an author-written name. No recovery: the recorded parameters and any statements the list absorbed are unchanged, and the theta is not registered. An `Ident` at the same position with no `":" Type` annotation is not judged by this row. | [Grammar Appendix — `fn` declarations](../grammar.md#fn-declarations); [Lexical — Identifiers](../lexical.md) | Give the parameter a legal identifier name; close the list before the token if it belongs to a following statement. | `fn parameter name must be an identifier` |
| `theta/parse/block-comment` | E | lex | `/* ... */` block comment used. | [Lexical — Comments](../lexical.md) | Use `//` or `///` line comments. | `block comments are not supported` |
| `theta/parse/integer-narrowing` | E | type | `number` value used where `integer` is expected (the `integer → number` widening is one-way), including a `match` object-pattern head's LITERAL field sub-pattern (`R { a: 1.0 }`'s `1.0`) judged against the head's declared field type: a pattern field literal is judged by its lexed SOURCE spelling ([Lexical — Number literals](../lexical.md)), the same reading the expression path applies, not by the parsed value's shape. This is a [Governance — Source-language stability](../governance/source-language-stability.md#gov-15) addition naming the newly-refused spellings `1.0`, `1e10`, and `1.5` in a `match` object-pattern field under an `integer`-declared field. | [Lexical — Number literals](../lexical.md) | — | `cannot narrow number to integer` |
| `theta/parse/integer-literal-out-of-range` | E | lex | An `integer`-typed number literal whose absolute value exceeds the safe-integer bound `2^53 - 1`, judged on the lexed token before the parse-time unary-`-` fold. | [Lexical — Number literals](../lexical.md) | — | `integer literal exceeds the safe-integer range` |
| `theta/parse/number-literal-not-finite` | E | lex | A `number`-typed literal whose parsed value is not a finite IEEE-754 double (e.g. `1e400`, which parses to `Infinity`). | [Lexical — Number literals](../lexical.md) | — | `number literal is not a finite IEEE-754 double` |
| `theta/parse/unsupported-feature` | E | parse | A theta 1.0-deferred or non-Theta syntactic construct (arrow function, spread, optional chaining, `===`, bitwise op, comma op, nested template, etc.) appears in source. | [Expressions — Not supported](../expressions.md) | — | `unsupported syntactic feature: <construct>` |
| `theta/parse/immutable-rebinding` | E | parse | Reassignment of a `let` (non-`mut`) binding. | [Bindings — `let` vs `let mut`](../bindings.md) | Use `let mut` if mutation is intentional. | `cannot reassign immutable binding '<name>'` |
| `theta/parse/assignment-as-expression` | E | parse | Assignment used in expression position (e.g. `if (x = 1)`). | [Bindings — Reassignment is a statement](../bindings.md) | — | `assignment is not an expression` |
| `theta/parse/assignment-to-member-or-index` | E | parse | `obj.field = ...` or `arr[i] = ...`. | [Bindings — Mutability is binding-level only](../bindings.md) | Rebind the whole value with `let mut`. | `cannot assign to member or index; mutability is binding-level only` |
| `theta/parse/mut-on-immutable-context` | E | parse | `mut` modifier on a function parameter, `for` iteration variable, or `match` pattern binding. | [Bindings — Immutable contexts](../bindings.md) | — | `'mut' is not permitted in this binding position` |
| `theta/parse/mut-on-discard` | E | parse | `let mut _ = ...`. | [Bindings — Immutable contexts](../bindings.md) | — | `'mut' is not permitted on discard binding '_'` |
| `theta/parse/increment-decrement` | E | parse | `++` or `--` operator used. | [Bindings — Increment / decrement](../bindings.md) | Use `count += 1` / `count -= 1`. | `'<op>' operator is not supported` |
| `theta/parse/non-boolean-condition` | E | type | Non-`boolean` value used in `if` / `while` / ternary condition or as `&&` / `\|\|` operand. | [Expressions — Truthiness](../expressions.md) | Write `if (x != "")`, `if (xs.length > 0)`, etc. | `condition must be boolean; got <type>` |
| `theta/parse/comparison-chaining` | E | parse | Chained comparison such as `a < b < c`. | [Expressions — Operator precedence](../expressions.md) | Comparison operators do not chain; use `&&`. | `comparison operators do not chain; use &&` |
| `theta/parse/mixed-plus-operands` | E | type | `+` applied to a `number`/`integer` and a `string` (or any other mixed-type pair). | [Expressions — `+` operator](../expressions.md) | Convert explicitly or interpolate inside a string. | `'+' has mixed operand types: <left> and <right>` |
| `theta/parse/non-orderable-operands` | E | type | `<`, `<=`, `>`, or `>=` applied to a non-orderable operand pair — a numeric operand against a `string`, or an operand whose type is not `number`/`integer` or `string` (`boolean`, `null`, enum, union, object schema, or `array<T>`). | [Expressions — Ordering comparisons](../expressions.md) | Compare only numeric or only string operands; use `==` / `!=` for other types. | `'<op>' requires two numeric or two string operands; got <left> and <right>` |
| `theta/parse/non-indexable-receiver` | E | type | Indexed access `a[...]` whose receiver `a` is neither `array<T>` nor an object value (e.g. `s[i]` on a `string`). | [Expressions — Supported forms](../expressions.md) | Index only `array<T>` or object values; use `s.split(...)` for strings. | `indexed access requires an array<T> or object receiver; got <type>` |
| `theta/parse/non-string-object-index` | E | type | Indexed access `obj[k]` on an object-value receiver whose index expression `k` is not of type `string` (e.g. `obj[0]`). | [Expressions — Supported forms](../expressions.md) | Object index must be a `string` theta-side name; use `obj.field` for the per-field declared type. | `object index must be string; got <type>` |
| `theta/parse/array-element-type-mismatch` | E | type | Array literal element does not type-check against the surrounding sink's element type. | [Expressions — Array construction](../expressions.md) | — | `array element type mismatch at index <i>: expected <expected>, got <actual>` |
| `theta/parse/array-no-common-type` | E | type | Array literal whose elements have no common type and no sink to narrow against. | [Expressions — Array construction](../expressions.md) | Annotate the binding with `array<A \| B>` or use a single schema. | `array elements have no common type; annotate the binding with array<A \| B> or use a single schema` |
| `theta/parse/return-no-common-type` | E | type | A theta or annotation-less-`fn` body whose tail-expression type and early-`return` operand types (every `return` syntactically present, regardless of static reachability) share no common upper bound and no sink narrows them. | [Function Definitions — Theta return type](../functions.md#theta-return-type) | Give the body a single return type, or add an explicit `fn` return annotation / sink that reconciles the operands. | `return operands have no common type; annotate the function return type or reconcile the operands` |
| `theta/parse/non-string-array-join` | E | type | `arr.join(...)` invoked on an array whose element type is not `string`. | [Expressions — `array<T>` stdlib](../expressions.md) | Map elements to strings first; no implicit type conversion in theta 1.0. | `array.join requires a string element type; got array<<element>>` |
| `theta/parse/extra-object-field` | E | parse | Schema constructor lists a field not declared by the schema, or a `match` object-pattern head resolving to a same-file `schema` lists a field the resolved declaration cannot carry: a same-file OBJECT-form `schema` judges the field against its declared field names, and a same-file ALIAS/UNION or head-only `schema` (fieldless by construction) reports every listed field as unsatisfiable. An imported symbol, an `enum`, and the builtin error-model name (`QueryError`) each defer at the pattern-head position — none supplies a same-file object body to judge the field list against. Omission stays legal at a pattern head (`theta/parse/missing-object-field` does not follow this row into that position): a pattern lists a subset of the declared fields by design. Reported at the whole object-pattern's range, the only range a `PatternNode` carries. This widening is a [Governance — Source-language stability](../governance/source-language-stability.md#gov-15) addition: every newly-refused spelling loaded cleanly before it. | [Expressions — Object construction](../expressions.md) | — | `extra field '<field>' on schema '<schema>'` |
| `theta/parse/missing-object-field` | E | parse | Schema constructor omits a declared (required) field. | [Expressions — Object construction](../expressions.md) | — | `missing field '<field>' on schema '<schema>'` |
| `theta/parse/object-field-type-mismatch` | E | type | A schema-constructor field value has a static type that is not compatible with the schema's declared type for that field under [Type System — Type compatibility](../type-system.md#type-compatibility), where the field value's type is statically resolvable, or a `match` object-pattern head resolving to a same-file object-form `schema` lists a LITERAL field sub-pattern (`R { a: 1 }`'s `1`) that is not compatible with the declared field's type under the same relation — a shorthand or a nested sub-pattern carries no literal to compare and draws nothing here. The pattern-position TYPE-2 outcome (`theta/parse/integer-narrowing`) is EMITTED at that position instead of this row's code whenever the relation answers the one-way `number`-under-`integer` case: the two codes are mutually exclusive per field, so this row's field-literal branch fires only for the two-way incompatible case. Reported at the whole object-pattern's range, the only range a `PatternNode` carries — unlike the constructor position, which points at the offending field value. This widening is a [Governance — Source-language stability](../governance/source-language-stability.md#gov-15) addition: every newly-refused spelling loaded cleanly before it. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `field '<field>' on schema '<schema>' type mismatch: expected <expected>, got <actual>` |
| `theta/parse/bare-object-literal` | E | parse | Bare `{ field: expr }` (no schema name) used in expression position outside the two documented carve-outs (`params:` defaults; a direct argument of a Pi-tool call). | [Expressions — Object construction](../expressions.md) | Name the schema: `Schema { field: expr }`. | `bare object literal not permitted in this position; name the schema (Schema { ... })` |
| `theta/parse/default-not-literal` | E | parse | A `params:` default RHS contains a form outside the [Theta literal sublanguage](../grammar.md#theta-literal-sublanguage) (an operator other than the unary `-` carve-out for numeric literals, function call, identifier reference other than `Enum.Variant`, `${...}` interpolation, or `@`...`` template). *Same-line residue* — a parse that admits a leading node and then leaves any token unconsumed also draws this row, at the top level of the default RHS or nested inside an array element or an object field value: the parser's own end-of-input cursor is the judge, not a second production. The residue may be a literal (`1 2`, `"a" "b"`, `true false`, `null null`), an identifier (`-1x`, `"a" junk here`), a function/tool call (`1 foo(2)`), a template or `${...}` interpolation (`1 ${x}`), an `@`...`` query template, stray punctuation or a comment-like tail (`1;`, `-5 # trailing`), or a numeric tail this position's own scanner splits off a decimal literal because it accepts only `[0-9.eE]` (`0x10` tokenises as the number `0` followed by the identifier `x10`; `1_000` as `1` followed by `_000`). The diagnostic names the RESIDUE — the unconsumed text, trimmed, from the first token the parse could not use through the end of source — not the leading expression it follows; a leading node the walk above already faults (an identifier, a call, a binary expression, and the rest of the non-literal set) keeps its OWN span and this clause does not run for it. The hexadecimal, octal, binary and underscore-separator numeric forms `theta/parse/unsupported-feature` claims in body position ([Lexical Structure — Number literals](../lexical.md)) draw this row instead at the `params:` default position, because this position's tokeniser is the literal sublanguage's own decimal-only scanner and does not lex through the theta lexer that recognises those forms. This widening is a [Governance — Source-language stability](../governance/source-language-stability.md#gov-15) addition: every newly-refused spelling loaded cleanly before it. | [Frontmatter — Defaults](../frontmatter.md) | Replace the offending sub-expression with a literal value, or move the computation into theta body code. | `params default RHS must be a literal-sublanguage form; offending sub-expression: <expr>` |
| `theta/parse/default-without-literal` | E | parse | A `params:` field declares a default — a top-level `=` in the field's scalar — whose right-hand side is an empty or whitespace-only string, at whatever quoted or block-scalar spelling delivers it. The declaration form `field: type = literal` admits no arm without a `literal`, which is what this row refuses; distinct from `theta/parse/default-not-literal`, which refuses a default that DOES spell a form outside the [Theta literal sublanguage](../grammar.md#theta-literal-sublanguage). A field already refused by the type-text stage (`theta/load/params-type-not-expression`) draws this row's diagnostic no more than it draws the other default-side ones, and a field refused by this row draws neither `theta/parse/literal-newline-in-string`, `theta/parse/default-not-literal`, nor `theta/parse/params-default-type-mismatch`. The theta is not registered. | [Frontmatter — Defaults](../frontmatter.md) | — | `params default for '<field>' is empty; '=' must be followed by a literal-sublanguage form` |
| `theta/parse/params-default-type-mismatch` | E | type | A `params:` field's declared-type half, paired with that field's own default-literal half (the two halves `splitParamValue` separates, `frontmatter.ts`), whose default literal's static type is not `T₁ ⊑ T₂` under [Type System — Type compatibility](../type-system.md#type-compatibility) (`T₂` the declared type), the relation resolved against an EMPTY type environment at this position (a `params:` right-hand side carries no local alias/schema bindings of its own, so every `NamedType` the declared-type half spells is unresolvable here). The declared-type shapes this position can decide under that empty environment include a primitive, a union of primitives, and `array<T>` nesting over either. The default-literal shapes it can decide include a string / number / boolean / `null` literal (a unary-`-` numeric literal included) and a homogeneous array literal of them. Every other pairing defers and draws nothing here — a declared type that is a `NamedType`, an alias, an inline object type, or a literal type; a default that is a bare-key object literal, an `Enum.Variant` access, a variant-schema construction, an empty or heterogeneous array literal, or text that parses to no literal — each resolves to the relation's `"unknown"` case (the Unresolvable-operands paragraph of [Type System — Type compatibility](../type-system.md#type-compatibility)) and is left to the runtime AJV safety net. Two precedence rules bound this row to one diagnostic per field: a field whose type half already drew `theta/load/params-type-not-expression` keeps that diagnostic alone, and a field whose default half already drew `theta/parse/literal-newline-in-string` or `theta/parse/default-not-literal` keeps that diagnostic alone — this row does not run for either. The theta is not registered. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `param '<param>' default type mismatch: expected <expected>, got <actual>` |
| `theta/parse/non-trailing-default` | E | parse | A `params:` declaration places a non-defaulted field after a defaulted field in declaration order. | [Frontmatter — Defaults](../frontmatter.md) | Reorder `params:` so every defaulted field is trailing, or give the offending field a default. | `non-defaulted param '<field>' follows a defaulted param; defaulted params must be trailing` |
| `theta/parse/tool-arg-arity` | E | parse | A Pi-tool call site uses more than one positional argument (e.g. `read({...}, {...})`). | [Tool Calls — Argument shape](../tool-calls.md) | Pi tools take exactly one input object; merge the arguments. | `Pi tool '<name>' takes a single object argument; got <count>` |
| `theta/parse/tool-arg-not-object-literal` | E | parse | A Pi-tool call site's single positional argument is not written inline as a bare object literal `{ ... }` — e.g. `read(args)` where `args` is a `let`-bound value or any other bare identifier. RFC 0002 ([Computed field values in Pi-tool arguments](../../rfcs/0002-computed-tool-arguments.md)) lifted the field-*value* literal restriction (values are now full Theta expressions) but kept the argument *shape* rule so the tool's registered input schema supplies the field names. Distinct from `theta/parse/bare-object-literal`, whose "name the schema" remedy misdirects here: the fix is to inline the object literal, not to name a schema (the external tool schema already supplies the shape). | [Tool Calls — Argument shape](../tool-calls.md) | Inline the fields at the call site: `read({ path: expr, ... })`. | `Pi tool '<name>' argument must be written inline as a bare object literal { ... }; a let-bound value cannot supply the field shape` |
| `theta/parse/tool-arg-schema-conflict` | E | type | A Pi-tool call-site field-value expression has a static type provably disjoint (empty accepted-value intersection under the [schema subset](../schema-subset.md) mapping) from the tool's registered input-schema type for that field. Scoped to provable disjointness only: a `format`, `pattern`, numeric refinement, or a widened union with at least one satisfiable arm is not provable and falls through to the runtime AJV check (`Err(CodeToolError { cause: "validation", ... })`); the check never rejects a value the runtime AJV check would accept. | [Tool Calls — Argument shape](../tool-calls.md) | Pass a value whose type intersects the tool's input-schema type for the field. | `Pi tool '<name>' argument field '<field>' type is provably disjoint from the input schema: expected <expected>, got <actual>` |
| `theta/parse/let-without-initialiser` | E | parse | `let x: T` (annotation, no initialiser). Theta has no `undefined` value and no definite-assignment analysis. | [Grammar Appendix — `let` form](../grammar.md#let-form) | Initialise the binding at declaration, or restructure to bind once at the point a value is available. | `let binding '<name>' has no initialiser` |
| `theta/parse/let-rhs-type-mismatch` | E | type | The RHS initialiser of a typed binding `let x: T = expr` has a static type that is not compatible with the annotation `T` under [Type System — Type compatibility](../type-system.md#type-compatibility), where the RHS type is statically resolvable. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `let binding '<name>' initialiser type mismatch: expected <expected>, got <actual>` |
| `theta/parse/reassign-rhs-type-mismatch` | E | type | The RHS of a reassignment statement — `x = expr`, or one of the five compound forms `+=`, `-=`, `*=`, `/=`, `%=` — has a static type that is not compatible with the target binding's declared or inferred type under [Type System — Type compatibility](../type-system.md#type-compatibility), where the RHS type is statically resolvable. A reassignment to an immutable (`let`, non-`mut`) target draws this row IN ADDITION TO `theta/parse/immutable-rebinding`: the two rows are different phases (`parse` for mutability, `type` for compatibility) and neither row's Trigger excludes the other. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `reassignment of '<name>' type mismatch: expected <expected>, got <actual>` |
| `theta/parse/statement-in-arm-body` | E | parse | A `match` arm body is a bare `if` / `for` / `while` / `let` / assignment statement. Arm bodies are expressions; the ternary is the expression form of conditional. | [Grammar Appendix — `match` arm body](../grammar.md#match-arm-body) | Wrap the statements in a block expression `{ ... }` whose tail expression is the arm's value. | `match arm body must be an expression; wrap statements in a block expression { ... }` |
| `theta/parse/block-expr-missing-tail` | E | parse | A `BlockExpr` at one of the two admitted expression-position sites — a `let` / `let mut` initialiser, a `match`-arm body — whose parsed body carries no tail expression (`BlockExpr ::= "{" Stmt* Expr "}"`; the tail `Expr` is required, unlike the implicit `null` tail `FnBody` / `StmtBlock` admit). | [Grammar Appendix — Block expressions](../grammar.md#block-expressions) | End the block with a value-producing expression, e.g. the binding the block computed. | `block expression must end in a tail expression` |
| `theta/parse/by-on-object-schema` | E | parse | A `by` clause on a declaration that is not a discriminated union: an object body (`schema X by f { ... }`); a right-hand side of fewer than two arms (`schema X by f = Cat`, against `UnionRhs ::= Type ("\|" Type)+`); or a right-hand side of two or more arms not every one of which is an object schema (`schema X by f = string \| integer`). An inline `ObjectType` arm is an object schema, and so is a bare identifier resolving to a declared OBJECT-form schema — with no hop through an alias declaration (`schema X by f = Cat \| Y` where `schema Y = string` is refused). The `by` clause applies only to discriminated-union schemas; the cut this code makes is whether the declaration IS one, so whether a union it admits then needs a discriminator is the discriminator rows' own subject (`theta/parse/missing-discriminator` and neighbours, over the same object-schema arms). Withheld when the declaration's own arm walk already drew an error-severity diagnostic of its own — an unresolved name, a reserved keyword, unspellable text — so that fault keeps its own code alone (`schema X by f = Ghost \| Dog` keeps `theta/parse/unresolved-named-type` alone). | [Grammar Appendix — `schema X by <field>`](../grammar.md#schema-x-by-field) | Use `schema X by f = A \| B \| …` where every arm is an object schema, or drop the `by` clause if `X` is an object schema. | `the 'by' clause applies only to discriminated-union schemas (schema X by f = A \| B \| …)` |
| `theta/parse/doc-comment-misplaced` | E | parse | A `///` doc comment appears above a production other than `schema`, `enum`, schema field, enum variant, or `fn`. | [Grammar Appendix — `///` placement](../grammar.md#-placement) | Move the description to a legal anchor, or replace with a regular `//` comment. | `'///' doc comment is not legal above this production` |
| `theta/parse/generic-arity-mismatch` | E | parse | A generic-type application whose type-argument count does not match the constructor's declared arity in the closed `GenericType` set (`array` arity 1, `Result` arity 2) — e.g. `array<T, U>` or `Result<T>`. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | — | `generic type '<ctor>' expects <expected> type argument(s); got <actual>` |
| `theta/parse/void-in-non-return-position` | E | parse | `void` keyword used in a `Type` position other than a function/theta return type — a `let` annotation, schema or `params:` field, generic argument (`array<void>`, `Result<void, E>`), `invoke<void>` annotation, type ascription, or union arm. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | `void` is a return-only annotation; use a value type (or `null`) in this position. | `'void' is only permitted as a function or theta return type` |
| `theta/parse/result-in-schema-position` | E | parse | A `Result<T, E>` application appears in a lowered-schema position — a schema field type, a `params:` field type, or any type reachable transitively from those (including `array<T>` element types and union arms). `Result` is observed only by theta code and is never lowered to a JSON Schema fragment. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | `Result` has no lowered-schema form; use it only in `fn` / `let` / `invoke` positions, and feed the schema position a lowerable type. | `'Result' has no lowered-schema form and is not permitted in a schema-feeding position` |
| `theta/parse/unknown-identifier` | E | parse | Bare identifier in call or value position resolves to nothing in scope. | [Expressions — Identifier resolution](../expressions.md) | — | `unknown identifier '<name>'` |
| `theta/parse/shadowed-callable-call` | E | parse | A bare-identifier call whose callee resolves to a local binding — a `let`, function parameter, `for` / `par for` variable, `match` pattern binding, or `params:` field — that shadows a name in the theta's callable set. Locals are not callable (functions are not first-class), so the call site is erroneous; binding the name without calling it stays legal. `<binder>` renders the shadowing binder and its line, e.g. `let binding at line 6` (borrowed from the nearest enclosing node for `fn` parameters and `match` bindings, which carry no ranges of their own — the `fn` declaration's and the arm body's start line respectively; a `params:` field carries none). | [Expressions — Identifier resolution](../expressions.md) | Rename the local binding, or give the `tools:` entry a distinct name with `as`. | `call of '<name>' resolves to the local <binder> that shadows the callable-set entry '<name>'; locals are not callable` |
| `theta/parse/unknown-method` | E | parse | Method or property accessed on a built-in type that the theta 1.0 stdlib does not expose. | [Expressions — Built-in methods](../expressions.md) | — | `unknown method '<method>' on type <type>` |
| `theta/parse/non-array-iterand` | E | type | `for x in expr` where `expr` is not `array<T>`. | [Control Flow — `for`/`in`](../control-flow.md) | Use `obj.keys()` for objects, `s.split(...)` for strings. | `'for' expects array<T> after 'in'; got <type>` |
| `theta/parse/break-outside-loop` | E | parse | `break` outside the body of a `for` / `while` loop. | [Control Flow — `break`/`continue`](../control-flow.md) | — | `'break' outside of a loop` |
| `theta/parse/continue-outside-loop` | E | parse | `continue` outside the body of a `for` / `while` loop. | [Control Flow — `break`/`continue`](../control-flow.md) | — | `'continue' outside of a loop` |
| `theta/parse/break-with-value` | E | parse | `break expr` (theta 1.0 `break` carries no value). | [Control Flow — `break`/`continue`](../control-flow.md) | — | `'break' takes no value in theta 1.0` |
| `theta/parse/par-query-in-body` | E | parse | An `@`...`` query against the enclosing conversation, written as a statement or as a sub-expression, anywhere in a `par for` body's own statement or expression tree, at any nesting depth: a conversation is a linear transcript and concurrent `@` queries against it have no defined interleaving. A nested `par for`'s own body is scanned by its own pass (only its iterand / `max` evaluate in this body's scope), so the emission is never doubled for a query inside it. The scan does not descend into a nested `fn`'s body, which is its own return scope — that declaration is `theta/parse/nested-fn` (FN-1). | [Control Flow — CTRL-4](../control-flow.md#ctrl-4) | — | `\`@\` query against the enclosing conversation is not permitted inside a 'par for' body` |
| `theta/parse/par-shared-mutation` | E | parse | A reassignment inside a `par for` body's own statement tree targeting a binding declared `let mut` outside the body and not shadowed by a body-local `let` of the same name. Outer bindings and the loop variable stay readable; a reassignment to a body-local binding is not refused under this rule. | [Control Flow — CTRL-4](../control-flow.md#ctrl-4) | — | `cannot assign to outer binding '<name>' from inside a 'par for' body` |
| `theta/parse/par-break-continue` | E | parse | A `break` or `continue` statement targeting the `par for` itself — nesting depth zero within the body's own statement tree. A `break` / `continue` nested inside a plain `for` / `while` within the body, targeting that inner loop, stays legal at any depth; contrast `theta/parse/par-return-in-body`, refused at every depth regardless of nesting. | [Control Flow — CTRL-4](../control-flow.md#ctrl-4) | — | `'<keyword>' is not permitted inside a 'par for' body` |
| `theta/parse/par-return-in-body` | E | parse | A `return` statement anywhere in a `par for` body's own statement tree, at any nesting depth — directly in the body, inside a nested `if` block, or inside a nested plain `for` / `while` — in both the valued and the bare spelling. The bare spelling is in class: the retained defensive runtime fold would give it `Ok(null)` at the iteration boundary, and the enclosing scope's return-type annotation is what judges its missing operand, so it is refused here too. The scan does not descend into a nested `fn`'s body, which is its own return scope — that declaration is `theta/parse/nested-fn` (FN-1). A nested `par for`'s own body is scanned by its own pass, so the emission is never doubled for a `return` inside it. `theta/parse/bare-return-in-non-void` and `theta/parse/unreachable-code` are not withheld beside this refusal. | [Control Flow — CTRL-4](../control-flow.md#ctrl-4) | Collect the value as the body's tail expression, or move the early exit outside the loop. | `'return' is not permitted inside a 'par for' body` |
| `theta/parse/illegal-template-escape` | E | lex | Backslash followed by an unrecognised character inside a `@`...`` query template body. | [Query — Escapes](../query.md) | — | `illegal escape sequence in @\`...\` template: \\<char>` |
| `theta/parse/unterminated-template` | E | lex | EOF reached while scanning a `@`...`` query template. | [Query — Multi-line templates](../query.md) | — | `unterminated @\`...\` query template` |
| `theta/parse/discarded-query-result` | E | parse | Bare `@`...`` expression-statement (the `Result` is dropped without `?` or `let _ =`). | [Query — Discarded query results](../query.md) | Use `?` to propagate failure or `let _ = @\`...\`` to discard explicitly. | `query result discarded; use ? to propagate failure or 'let _ = ...' to discard explicitly` |
| `theta/parse/empty-template` | W | parse | `@`...`` template's static body is empty or whitespace-only (ASCII whitespace per [System-note rendering](../binder/defaulting-system-note-echo.md#system-note-rendering) rule 1, not the regex `\s` class) after newline-trim and dedent. The theta still loads; the runtime short-circuits the query if the fully-rendered text is also empty. | [Query — Degenerate rendered templates](../query.md) | Add literal text or use `\\n` to keep an intentionally-blank prompt. | `query template body is empty after newline-trim and dedent` |
| `theta/parse/interpolated-result` | E | type | `${expr}` interpolation whose `expr` has Theta static type `Result<T, E>` (the runtime renderer raises the same code as a panic when the type is statically unresolvable); the runtime renderer also raises it when `expr`'s static type is `array<T>` or a Schema-typed object and the evaluated value holds a `Result` at any depth — a container's own static type is never `Result<T, E>`, so this sub-case is reached only at runtime, never at parse. | [Query — Stringification of interpolated values](../query.md) | Unwrap with `?` or `match` before interpolating. | `Result value cannot be interpolated; unwrap with ? or match first` |
| `theta/parse/explicit-schema-mismatch` | W | parse | Both a binding annotation and an explicit `@<Schema>` ascription are present, and the ascription is not compatible with the annotation under [Type System — Type compatibility](../type-system.md#type-compatibility) (i.e. `ascription ⋢ annotation`). Safe widenings (e.g. `@<integer>` under a `number` annotation) do not warn. | [Query — Explicit form](../query.md) | — | `explicit @<Schema> ascription is not compatible with binding annotation` |
| `theta/parse/empty-query-annotation` | E | parse | An explicit `@<…>` query annotation whose interior is empty or trims to empty — `@<>`, `@<  >`, a tab- or newline-only interior, or an unterminated `@<` at end of input (the capture runs to EOF and still trims to empty). The type grammar derives no empty `Type` ([Grammar Appendix — Type grammar](../grammar.md#type-grammar); [Type System](../type-system.md) applies the same grammar to the `@<T>` annotation position), so an empty ascription supplies no response schema — accepted silently it would mark the query typed while giving the runtime nothing to validate the response against, and (worse than absence) block the inference contexts a real ascription overrides. | [Query — Explicit form](../query.md) | Name a schema (`@<Schema>`) or drop the annotation for an untyped query. | `\`@<>\` query annotation is empty; write \`@<Schema>\` or drop the annotation for an untyped query` |
| `theta/parse/match-arm-type-mismatch` | E | type | A `match` arm's body type is not assignable to the common type of the other arms. | [Expression Sublanguage — Arm syntax](../expressions.md#match-expression) | — | `match arm body type does not match the common type of the other arms` |
| `theta/parse/question-outside-result-fn` | E | type | `?` used in a function or top-level theta whose return type is not `Result<T, QueryError>` for some `T` (and cannot be inferred to one). | [Expression Sublanguage — `?` operator](../expressions.md#question-operator) | Declare the enclosing scope's return type as `Result<T, QueryError>` for some `T`. | `'?' used in a scope whose return type is not Result<T, QueryError>` |
| `theta/parse/question-on-non-result` | E | type | `?` applied to an operand whose Theta static type is not `Result<T, QueryError>` for some `T` (e.g. `let x = 5?`). Distinct from `theta/parse/question-outside-result-fn`, which checks the enclosing scope's return type rather than the operand type. | [Expression Sublanguage — `?` operator](../expressions.md#err-18) | Apply `?` only to a `Result`-typed operand (a `@`-query, `invoke(...)`, or `Ok(...)` / `Err(...)`). | `'?' requires a Result operand; got <type>` |
| `theta/parse/match-guard-not-supported` | E | parse | Guarded `match` arm (`Pattern if cond => ...`). | [Expression Sublanguage — Pattern grammar](../expressions.md#pattern-grammar) | Deferred to a future release. | `match guards are not supported in theta 1.0` |
| `theta/parse/rest-pattern-not-supported` | E | parse | Rest pattern (`[first, ...rest]`, `{ kind, ...other }`). | [Expression Sublanguage — Pattern grammar](../expressions.md#pattern-grammar) | Deferred to a future release. | `rest patterns are not supported in theta 1.0` |
| `theta/parse/bare-return-in-non-void` | E | type | Bare `return` (no argument) in a function or theta whose return type is not `void`. | [Return Statement](../return.md) | — | `missing return value` |
| `theta/parse/unreachable-code` | W | parse | Statement appears after a `return` in the same block. | [Return Statement](../return.md) | — | `unreachable code after return` |
| `theta/parse/nested-fn` | E | parse | `fn` declaration nested inside another `fn` body or a block. | [Function Definitions — Placement](../functions.md) | Top-level `fn` only in theta 1.0. | `nested 'fn' declarations are not supported in theta 1.0` |
| `theta/parse/function-as-value` | E | parse | Function name used outside call position (bound to `let`, passed as argument, etc.). | [Function Definitions — Placement](../functions.md) | — | `function '<name>' used outside call position; functions are not first-class in theta 1.0` |
| `theta/parse/type-as-value` | E | parse | A bare `schema` or `enum` declaration name at a VALUE position: every position identifier resolution reaches where the name is read for its value. That rule generates the list, and the list is exhaustive. At statement level: a `let` initialiser, a reassignment right-hand side, an `if` or `while` condition, a `for` iterand, a `return` operand. At expression level: an argument of a call, method call, Pi-tool call, `invoke`, or `Result` constructor; either operand of a binary operator; a unary or `?` operand; a ternary's condition or either branch; an array element; an object-field value; a `match` scrutinee or arm body; the receiver of a member, index, or method access; an index subscript; a `par for` iterand or `max` operand. And a block's or the theta's tail expression. A declaration introduces a named **type** ([Schemas](../schemas.md)), so the name matches no arm of the four-arm resolution list ([Expressions — Identifier resolution](../expressions.md)) and denotes no value — the same ground on which a `fn` name outside call position is `theta/parse/function-as-value`, one row up. Three positions read the name as a type and sit outside this row: a schema constructor head (`Schema { … }`), an `Enum.Variant` receiver, and any `Type` position. Two further shapes sit outside it rather than under it. A name that a value-binding source also introduces — a `let`, a function parameter, a `for` / `par for` variable, a `match` pattern binding, a `params:` field, an imported symbol, or a callable-set entry — is read as that binding wherever the binding is in scope and draws nothing here, whatever it is also declared as; the binder enters scope ahead of the reads it covers, and that is the mechanism at every entry on this list. And a bare declared name as a DISCARDED expression statement is the no-op statement class, silent wherever it is written; the tail expression of a block or theta is not discarded and is inside the row. The call position (`Schema()`) is likewise outside this row and stays `theta/parse/unknown-identifier`'s, whose own trigger already covers a callee that resolves through no arm of that list. The theta is not registered. | [Expressions — Identifier resolution](../expressions.md) | Construct a value — `Schema { … }` for an object schema, `Enum.Variant` for an enum — or name a binding that holds one. | `type '<name>' used as a value; a schema or enum declaration names a type, not a value` |
| `theta/parse/redundant-wire-name` | W | parse | `field as "field"` rename whose wire name equals the theta-side name. | [Schemas — Wire-name renaming](../schemas.md) | Drop the `as` clause. | `redundant 'as' clause: wire name '<name>' equals the theta-side name` |
| `theta/parse/wire-name-collision` | E | parse | Two fields in the same schema share a wire name, or a wire name collides with another field's theta-side name. | [Schemas — Wire-name renaming](../schemas.md) | — | `wire name '<name>' collides with another field on schema '<schema>'` |
| `theta/parse/empty-schema-body` | E | parse | A `schema` declaration whose shape yields no usable content (neither fields nor alias arms): an empty object body (`schema X { }`), a body whose first token is not a plain `ident: Type` field list, or no shape at all. An empty inline object type (`{}`) in any `Type` position, at any nesting depth. The same code also covers an inline `ObjectType` interior's leading, doubled or lone top-level comma that opens an entry slot before any `Field` has derived in that interior (`{,a: integer}`, `{,}`, `{,,}`), rendered with the identical anonymous `{}` subject ([Placeholder rendering — Identifier-, descriptor-, and closed-enum placeholders](./placeholder-rendering-b.md#7-identifier--descriptor--and-closed-enum-placeholders)) and buffered at most ONCE per interior, since "'<X>' has no fields" cannot be true twice of one interior: `{,,}` spells two such slots and still draws one line ([bug 0257](../../bugs/0257-empty-inline-object-entry-slot-silently-tolerated.md) §Fix). A slot opened AFTER at least one `Field` has already derived in the interior is the partitioning sibling case, `theta/parse/malformed-schema-field` below, not this row. | [Schemas — Object schemas](../schemas.md) | — | `'<X>' has no fields; an empty schema cannot be validated.` |
| `theta/parse/malformed-schema-field` | E | parse | A `schema` object body's field capture (`SchemaShape ::= "{" Field ("," Field)* ","? "}"`, [Grammar Appendix — `schema X by <field>`](../grammar.md#schema-x-by-field)) that has already captured at least one `Field` and then reaches a token from which no further `Field` derives, in three shapes: the token where a field name belongs is neither an identifier nor a keyword; an `as` rename's wire-name token is not a string literal; or a field name is not followed by `:`. The same disposition reaches an inline `ObjectType` interior in any `Type` position and at any nesting depth (grammar.md §"Inline object types"), over the entries `TypeParser.parseObject`'s own field loop walks: the loop refuses each KEYLESS entry its entry walk reaches — one spelling no top-level `:` — with one diagnostic per such entry, before its own tolerant resync (`skipMalformedEntry`) carries the entry away ([bug 0244](../../bugs/0244-colon-less-inline-object-entry-silently-discarded.md) §Fix). A THIRD shape sits inside that same reach rather than beside it: an entry SLOT spelling no token at all — a doubled, leading or post-trailing top-level comma (`{a: integer,,b: string}`, `{,a: integer}`, `{a: integer,,}`) — refused here exactly when at least one `Field` has already derived earlier in the interior, one line per offending slot (`{a: integer,,,b: string}` draws two); a slot with no `Field` derived before it keeps `theta/parse/empty-schema-body` above instead. An entry standing IMMEDIATELY behind such a slot that itself qualifies for this row's own keyless-entry refusal draws that refusal ALONE: its line REPLACES the slot's rather than joining it, so `{a: integer,,void}` and `{,void}` still draw exactly one line each ([bug 0257](../../bugs/0257-empty-inline-object-entry-slot-silently-tolerated.md) §Fix). Two exclusions bound the inline reach. An entry that DOES spell a top-level `:` is out of this row's reach whatever junk stands behind the colon (`theta/parse/annotation-type-not-expression` or the tolerant skip judges that text instead) — including the entry that STRANDS a keyless entry behind it, `a: b c` in `{a: b c, d e}`: that colon-present entry keeps its own disposition and draws no line of its own ([bug 0252](../../bugs/0252-brace-and-angle-annotation-junk-exempt-from-refusal.md)'s landed decline, [bug 0244](../../bugs/0244-colon-less-inline-object-entry-silently-discarded.md)'s adjudication clauses 2 and 4). A keyless entry carrying a STRAY CLOSE TOKEN — a `}` or `>` whose innermost open frame is of another kind or absent — keeps [bug 0238](../../bugs/0238-stray-close-token-underflows-top-level-split.md)'s silent tolerant registration rather than drawing this row. A missing entry separator no longer strands the entry behind it: the loop RESYNCHRONISES depth-aware to this interior's next top-level `,` (or to the interior's own `}` / an enclosing generic's `>`, which it never consumes) exactly as it already does at a colon-gate failure, so the entry that a junk tail such as `a: b c` used to strand — `d e` in `{a: b c, d e}` — is now visited, and this row refuses it when it is keyless. At the GENERIC-ARGUMENT position (`array<{a: b c, d e}>`), the one position with no recogniser gate behind the loop, the document now refuses instead of registering ([bug 0256](../../bugs/0256-generic-argument-stranded-entry-registers-permissive.md) §Fix, the operator ruling's OPTION 1 — resync-and-tolerate). At an inline interior the diagnostic's range is the ENCLOSING DECLARATION's, because a `TypeNode` carries no range of its own — the same divergence `theta/parse/schema-type-not-expression`'s row states for its own field-type judgement. This row partitions with `theta/parse/empty-schema-body` immediately above on whether a `Field` derived before the offending token: an EMPTY captured prefix — no `Field` derived at all — keeps that row's declaration-subject disposition, because its Trigger's first-token clause already describes exactly that input and its Message (`'<X>' has no fields`) is true of it; this row fires only once at least one `Field` has derived, which puts the input outside every clause of the neighbour row's Trigger. At the `schema` object body, the diagnostic's range is the OFFENDING TOKEN's own span, never the declaration's, and every field captured before that token is RETAINED on the returned declaration — not discarded with the remainder of the malformed body — so the declaration's other checks run against what the author wrote: `theta/parse/by-on-object-schema`, the wire-name checks (`theta/parse/redundant-wire-name`, `theta/parse/wire-name-collision`), the field-type walk (which can itself draw `theta/parse/schema-type-not-expression` on a retained field's junk type), and the constructor field-set checks (`theta/parse/missing-object-field`, `theta/parse/extra-object-field`). The remainder of the brace group past the offending token is still consumed as a balanced group (or to end of input on an unbalanced body — an unfixed residual), so no further diagnostic reports on that remainder. | [Schemas — Object schemas](../schemas.md) | Each field is `name: Type` or `name as "WireName": Type`; fix or remove the field that does not fit that shape. | `malformed schema field; each field is 'name: Type' or 'name as "WireName": Type'` |
| `theta/parse/schema-body-unclosed` | E | parse | `SchemaShape ::= "{" Field ("," Field)* ","? "}"` ([Grammar Appendix — `schema X by <field>`](../grammar.md#schema-x-by-field)) makes the closing `}` a required terminal with no alternative. This row fires when the object body's field capture has derived at least one `Field` and then reaches END OF INPUT with no `}` ahead. It partitions with `theta/parse/empty-schema-body` above on the same captured-prefix test the neighbour rows share: an EMPTY captured prefix (`schema S {` at EOF) keeps that row ALONE, because its Trigger already describes the input and this row says nothing about it — whether that row's own Message should also name the missing `}` is a separate wording question this row does not open. Withheld when a field-TYPE capture consumed an unmatched `}` — strictly more `}` punctuation tokens than `{` punctuation tokens over the token span that capture consumed — because the closer the row would name was then swallowed by the type rather than left out, the same absorbed-closer ground `theta/parse/fn-param-list-unclosed` withholds on for `)`. A `}` that is merely a character inside a string or template token, or one half of a balanced `{...}` pair the type carries, is not a consumed closer and withholds nothing. The diagnostic's range is the body's OPENING `{`, mirroring `theta/parse/fn-param-list-unclosed`'s opening `(`; the captured prefix is RETAINED on the returned declaration, exactly as `theta/parse/malformed-schema-field` retains it, so the declaration's other checks run against what the author wrote. Scoped to the `schema` object body alone: every other unbalanced bracket is not judged by this row, exactly as `theta/parse/fn-param-list-unclosed` scopes itself to `fn` parameter lists — the `enum` variant list (`parseEnumVariants`) is judged by the sibling row `theta/parse/enum-body-unclosed` below instead. This row CO-FIRES with a field-type-level refusal on the same body (`theta/parse/schema-type-not-expression`) rather than suppressing it or being suppressed — the missing `}` is a fault independent of the field type's own, the same co-firing `fn f(a: string,` already shows for `theta/parse/fn-param-list-unclosed` beside `theta/parse/single-line-if`. This is a code addition under the [Governance — Source-language stability — Diagnostic-registry carve-out](../governance/source-language-stability.md#diagnostic-registry-carve-out), in scope for every input that did not previously emit this code: the inputs it newly names divide into those that loaded cleanly before it — the field-boundary truncations this row exists for — and those that already carried another row's refusal, which this row's emission joins rather than replaces. The theta is not registered. | [Grammar Appendix — `schema X by <field>`](../grammar.md#schema-x-by-field); [Schemas — Object schema](../schemas.md) | Close the body with `}`. | `schema object body is not closed by '}'` |
| `theta/parse/duplicate-inline-field-name` | E | parse | Two or more entries of one inline object type (`ObjectType`)'s interior share a key. The inline spelling reuses the object-schema `Field` form and carries the same field semantics, so two entries sharing one key declare one wire name twice; the row applies in any `Type` position and at any nesting depth reachable through inline object fields and union arms. `ObjectType` spells a closing `}`, so an interior that never closes spells no inline object type and carries no comparison of its own. The comparison runs over the entries a brace-and-angle-aware split of the interior on top-level commas yields (quoted regions skipped), keyed on each entry's own text before its own top-level `:`, taken verbatim after `trim()` — the same split and the same text both lowerers key their `properties` and `required` writes on, so `"a"`, `'a'` and `a as "w"` are the keys they are written as, `{'a': …, "a": …}` is two keys and not one, and padding around an entry's pre-colon text is absorbed by the trim; no unquoting and no normalisation otherwise apply. An entry with no top-level `:`, or whose pre-colon text trims to empty, spells no key and contributes none; an entry whose TYPE position is empty still keeps its key. The key list this row compares is derived from exactly the bytes a `Type` position hands the lowerer, by the same split and the same colon, so a repeated key here is a property of the same text the lowering keys its `properties` and `required` writes on; an inline object type's interior is read from the author's source bytes at every `Type` position ([bug 0228](../../bugs/0228-inline-object-type-source-token-join-corrupts-field-keys.md)), so this row's key list and the lowering's agree on the author's own spelling. One diagnostic per repeated name, at that name's second position — a name written three times draws one line, and two repeated names draw two; a body's own repeats are reported before those of the inline objects nested in its field types. One shape sits outside this row rather than under it: a key reused between an outer inline object and one nested inside it is two interiors and two entry lists, not a repeat. A generic type argument's interior (`array<{ … }>`) is judged exactly as any other interior is: the lowering never dividing that interior into fields bounds the WIRE consequence of a repeated key, not this row's judgement of the source. The subject the *Message* renders is that raw key, not necessarily identifier-shaped, which is why `<field>` carries a row-scoped carve-out on this row (see [Placeholder rendering — Source-derived placeholders](./placeholder-rendering-b.md#5-source-derived-placeholders)). The named-declaration spelling of the same two fields keeps `theta/parse/wire-name-collision`. A NON-repeating quoted key is `theta/parse/quoted-inline-field-name`'s subject rather than this row's, one row down. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | — | `duplicate field name '<field>' within one inline object type` |
| `theta/parse/quoted-inline-field-name` | E | parse | An entry of one inline object type (`ObjectType`)'s interior whose key — the same raw text before that entry's own top-level `:`, taken verbatim after `trim()`, that `theta/parse/duplicate-inline-field-name` above compares (the same `splitTopLevel` / `topLevelColon` functions, `./params`) — has `"` or `'` as its FIRST character, and does not repeat within the interior. The inline field-name slot reuses the object-schema `Field` form (grammar.md §"Inline object types"), and [Schemas — Object schemas](../schemas.md) fixes a field name there as an identifier; [Lexical Structure — Identifiers](../lexical.md) admits no quote character, so the declaration spelling of the same text is already refused (`schema S { "a": string }` draws `theta/parse/empty-schema-body`) — this row brings the inline position into agreement with it. The row applies in any `Type` position and at any nesting depth reachable through inline object fields and union arms, the same reach `theta/parse/duplicate-inline-field-name` has. `ObjectType` spells a closing `}`, so an interior that never closes spells no inline object type and carries no key for this row to judge — the same requirement the neighbour row reads. A generic type argument's interior is judged exactly as any other interior is, at every depth beneath it, on the same ground as the neighbour row: the lowering never divides that interior into fields, which bounds the WIRE consequence of a quoted key, not this row's judgement of the source (`array<{"a": string}>` draws this row too). One diagnostic per offending key, in source order — a body's own entries are reported before those of inline objects nested in its field types. Two shapes sit outside this row's emission set: a key whose first character is not a quote (an unparsed rename, `a as "w"`, whose raw key `aas"w"` / `a as "w"` starts with a letter and is `theta/parse/renamed-inline-field-name`'s subject, one row down, rather than this row's) and an entry that spells no key at all (no top-level `:`, or pre-colon text trimming to empty). A key that REPEATS within the interior is `theta/parse/duplicate-inline-field-name`'s subject alone and draws nothing here, whether the repeated spelling is quoted or not: `{"a": string, "a": integer}` is one `duplicate-inline-field-name` line, not a second line from this row. The subject the *Message* renders is that raw key, which is why `<field>` carries a second row-scoped carve-out beside the neighbour row's (see [Placeholder rendering — Source-derived placeholders](./placeholder-rendering-b.md#5-source-derived-placeholders)). | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | Field names are identifiers; declare the schema by name instead of inline to give a field a different wire name (`as "WireName"` is a schema-declaration-only clause). | `quoted field name '<field>' within one inline object type; field names are identifiers` |
| `theta/parse/renamed-inline-field-name` | E | parse | A non-repeating entry of one inline object type (`ObjectType`)'s interior — the same raw text before that entry's own top-level `:`, taken verbatim after `trim()`, that `theta/parse/duplicate-inline-field-name` and `theta/parse/quoted-inline-field-name` above compare (the same `splitTopLevel` / `topLevelColon` functions, `./params`) — whose text is identifier-SHAPED text followed by `as` followed by a single- or double-quoted string literal, with no trailing text: an inline `as "WireName"` rename ([Schemas — Wire-name renaming](../schemas.md#wire-name-renaming)). The shape predicate on the leading text is `[A-Za-z_][A-Za-z0-9_]*` — the same runtime string predicate `<key>` applies (see [Placeholder rendering — Source-derived placeholders](./placeholder-rendering-b.md#5-source-derived-placeholders)), asked of the raw entry text rather than of an `Ident` production a binding derives from. A leading text whose spelling is a reserved keyword (`{let as "w": integer}`) is identifier-shaped under that predicate and therefore INSIDE this row's emission set, drawing this row beside whatever `theta/parse/reserved-keyword-as-identifier` makes of the same entry; the inclusion is deliberate, because this row's subject is the raw key the lowering would mint as a property name and not an identifier binding, so it inherits none of the reserved-keyword exclusion the inline identifier rules carry. [Grammar Appendix — Type grammar](../grammar.md#type-grammar) §"Inline object types" states the inline `Field` form admits no rename; the clause is a schema-declaration-only spelling, and this row refuses the inline one instead of parsing it. Shares its neighbour rows' one remaining gate — `ObjectType` spells a closing `}`, so an interior that never closes carries no key for this row to judge — and shares their reach: any `Type` position and any nesting depth reachable through inline object fields and union arms, including through a generic type argument, on the same ground as the two neighbour rows: the lowering never divides that interior into fields, which bounds the WIRE consequence of a rename, not this row's judgement of the source. A key that REPEATS is `theta/parse/duplicate-inline-field-name`'s subject alone, whether the repeat is a rename spelling or not, and a key whose first character is a quote is `theta/parse/quoted-inline-field-name`'s subject alone; this row fires only for a key that does neither, so the precedence is: repeat first, quote-led second, rename third. An entry that spells no key at all — no top-level `:`, or pre-colon text trimming to empty — sits outside this row exactly as it sits outside both neighbour rows. An entry whose wire-name string carries an escaped quote (`{a as "w\"x": integer}`) is INSIDE this row: the colon scan honours the escape (`topLevelColon`, `./params`) exactly as the split beside it already did, so the entry's `:` is read at top level, the wire-name literal alternatives admit the escaped interior (`INLINE_FIELD_RENAME`, `../../../src/parser/type-grammar.ts`), and the entry is refused here rather than dropped ([bug 0229](../../bugs/0229-escaped-quote-wire-name-drops-inline-field.md)). The *Message* renders the pattern's own capture group — the theta-side identifier the rename is written on — rather than the raw key. Before [bug 0228](../../bugs/0228-inline-object-type-source-token-join-corrupts-field-keys.md)'s fix that choice was load-bearing: at ten of the eleven `Type` positions the surrounding document reconstructed the type source by joining lexer tokens with no separator, so the raw key arrived as `aas"w"` and not `a as "w"`, and rendering the identifier instead of the raw key was what let this row answer alike at every position. The raw key now agrees with the author's spelling at every position too, so the reason to render the identifier is no longer that agreement: it is that the identifier — not the raw key — is what this row's subject IS, the theta-side name a rename is written on (one grammar in every annotation position, [Type System](../type-system.md)). Because the rendered subject is the theta-side identifier and not the raw key, `<field>` needs no row-scoped carve-out here (see [Placeholder rendering — Source-derived placeholders](./placeholder-rendering-b.md#5-source-derived-placeholders)): it renders under the same identifier-shaped default every other `<field>` row but the three carved-out rows — `theta/parse/duplicate-inline-field-name`, `theta/parse/quoted-inline-field-name` and `theta/parse/inline-field-name-not-identifier` — uses. The named-declaration spelling of the same clause keeps `theta/parse/wire-name-collision` and `theta/parse/redundant-wire-name`; an inline object has no name for either code's `<schema>` placeholder to name, which this row avoids by refusing the spelling outright rather than widening either declaration-scoped row. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | Field names carry no wire-name rename inline; declare the schema by name and use `as "WireName"` there. | `wire-name rename on field '<field>' within one inline object type` |
| `theta/parse/inline-field-name-not-identifier` | E | parse | A non-repeating entry of one inline object type (`ObjectType`)'s interior — the same raw text before that entry's own top-level `:`, taken verbatim after `trim()`, that `theta/parse/duplicate-inline-field-name`, `theta/parse/quoted-inline-field-name` and `theta/parse/renamed-inline-field-name` above compare (the same `splitTopLevel` / `topLevelColon` functions, `./params`) — whose text does not match `[A-Za-z_][A-Za-z0-9_]*`: the raw key is not an `Ident` ([Lexical Structure — Identifiers](../lexical.md)), so the entry derives from no `Field` ([Schemas — Object schemas](../schemas.md) fixes a field name as an identifier). The row applies in any `Type` position and at any nesting depth reachable through inline object fields and union arms, the same reach the three rows above share. `ObjectType` spells a closing `}`, so an interior that never closes carries no key for this row to judge. A generic type argument's interior is judged exactly as any other interior is, on the same ground as the three rows above: the lowering never divides that interior into fields, which bounds the WIRE consequence of a non-identifier key, not this row's judgement of the source. Precedence, in order: a key that REPEATS is `theta/parse/duplicate-inline-field-name`'s subject alone; a key whose first character is a quote is `theta/parse/quoted-inline-field-name`'s subject alone; a key that is identifier-shaped text followed by `as` followed by a quoted string literal is `theta/parse/renamed-inline-field-name`'s subject alone; this row fires FOURTH and last, only for a key that is none of those three and is not itself `Ident`-shaped — a sibling refusal to `theta/parse/fn-param-not-identifier` (bug 0225), asked of a raw inline field-name key rather than of a parameter binding. A field this row refuses draws no other error-severity diagnostic on that field: a row that already drew an error-severity diagnostic keeps that diagnostic alone ([bug 0129](../../bugs/0129-empty-object-field-type-draws-two-diagnostics.md)'s count-consequence law), and the three rows this one is subordinate to have already claimed the field before this row's turn. The subject the *Message* renders is that raw key, not necessarily identifier-shaped, which is why `<field>` carries a row-scoped carve-out on this row (see [Placeholder rendering — Source-derived placeholders](./placeholder-rendering-b.md#5-source-derived-placeholders)). | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | Field names are identifiers; two identifiers separated by whitespace in a field-name position is not one name (`{a b: integer}` derives from no `ObjectType`). | `field name '<field>' within one inline object type is not an identifier` |
| `theta/parse/malformed-alias-rhs` | E | parse | The right-hand side of a `schema X = …` (or `schema X by f = …`) declaration is not an `AliasRhs ::= Type ("\|" Type)*`, in two shapes. *Empty arm position* — a top-level `\|` with no `Type` on one of its sides (`schema X = Cat \|`, `schema X = \| Cat`, `schema X = Cat \|\| Cat`). *Same-line residue* — a capture that completes with at least one arm, followed on the same source line as its last token by a boundary token of one of these kinds: an identifier, a keyword, or a string or number literal (`schema X = Cat Cat`, `schema X = -1`, `schema X = string "junk"`, `schema X = string \| integer 7`, `schema X = array<integer> 42`, `schema X = array<integer> let a = 1`), or a punctuation head among the query-template `@`, a bare backtick template, `(`, `[`, `!`, and a unary-negation `-` (`schema X = Cat (1)`, `schema X = Cat !true`). Three boundaries sit outside this row rather than under it: a stray `,`, `)`, `=` or `}` there keeps `theta/parse/unsupported-feature` (`schema X = Cat, Dog`, `schema X = Cat )`, `schema X = Cat = 1`, `schema X = Cat }`); a `{` there keeps `theta/parse/bare-object-literal` (`schema X = Cat { a: "x" }`); and an operator with no operand behind it is absorbed INTO the arm rather than left at the boundary (`schema X = Cat +`, `schema X = string +`), so nothing is left over at the boundary and no token stands there for this row to name — the arm itself, carrying the absorbed operator, is `theta/parse/schema-type-not-expression`'s to refuse, not this row's. A right-hand side that yields no arm at all is `theta/parse/empty-schema-body` rather than this code, and a token on the NEXT source line is not residue on either of two grounds: when no continuation trigger holds at the end of the declaration's line, the newline closes the declaration ([Grammar Appendix — Newline continuation](../grammar.md#newline-continuation)); when a trailing trigger holds so that the newline separates nothing (`schema X = array<integer>` with `let a = 1` on the next line), the right-hand side still ends at its last arm, ahead of that line's first token. The declaration keeps the arms it captured and its range, and a severed residue keeps the disposition it already had in statement position — it parses as the statement it spells when it spells one (`schema X = Cat Cat`), and is discarded unreported when it spells none (`schema X = Cat else`): this code adds an emission and changes no other observable. | [Schemas — Type-alias / union schema](../schemas.md) | Separate union arms with a single `\|`, and start a new line for text that is not part of the type. | `'<X>' has a malformed right-hand side; write a single type, or arms separated by single '\|', and nothing else on the declaration's line` |
| `theta/parse/schema-type-not-expression` | E | parse | A `schema` object-body field type, or an arm of a `schema X = …` (or `schema X by f = …`) alias/union declaration, carries text that derives from no `Type` production ([Grammar Appendix — Type grammar](../grammar.md#type-grammar)). The unit judged is a single brace-free FRAGMENT the source itself spells — the same unit `lowerTypeExpr`'s trailing catch-all is handed — not the whole arm or field type: a fragment is judged at the type's own top level (the whole arm or field type), inside a union arm at either position, inside a `GenericType` type argument, and inside an inline `ObjectType`'s field type at any nesting depth, so the judgement re-enters at every level a hoisted inline object's own field lowering reaches. Excluded from the judged set is a fragment the generic-argument split itself MANUFACTURES by cutting a `{...}`/`[...]` group the author wrote as one unit (`array<{a: string, b: integer, c: boolean}>`'s middle shard, `b: integer`, is one `ObjectType` cut in two by the split, not a fragment of the source): such a shard never reaches this row's sink (`LowerCtx.unspellable`), because the generic-argument recursion that produced it carries none ([bug 0204](../../bugs/0204-bracket-blind-split-shreds-inline-object-in-generic.md) §Fix (b)(3)). The exclusion is decided per SEGMENT of that split and never for the argument list: a segment is whole in the source when both comma boundaries delimiting it sat at `{...}`/`[...]` depth 0 (the interior's start and end count as such boundaries) and its own groups balance, and a whole segment keeps its judgement beside the manufactured ones — `array<{a: string, b: integer, c: boolean}, ???>` draws exactly one refusal, on the whole argument `???`, while the three pieces of the cut `{...}` group beside it draw none. In the other direction the exclusion is a deliberate UNDER-refusal, and this row states it rather than leaving it to be discovered: junk the author did write draws nothing here when a manufactured shard CARRIES it, because the shard is not text the source spells on its own — `array<{a: Cat +, b: integer, c: boolean}>` and `array<{a: string, b: integer, c: boolean} | Cat +>` are both admitted, the first with `Cat +` inside the piece `{a: Cat +` and the second with it inside the piece `c: boolean} | Cat +`. A cut group is under-refused only when it is itself DERIVABLE — an `ObjectType` (`{...}`, one of the `Type` grammar's six alternatives): a CLOSED `[...]` bracket group the split cuts derives from none of them at any depth ([schemas.md:93](../schemas.md), stated with no depth qualifier), so that group is not under-refused with its pieces — [bug 0217](../../bugs/0217-nested-inline-enum-in-generic-argument-draws-nothing.md) §Fix (b)(2) pushes its own source text into this row's sink exactly once, as a last resort when nothing the argument list's own segment recursion contributed to that sink is itself refusable, so `array<enum["a", "b"]>` draws this row's refusal naming the enclosing declaration on the group the author wrote, `enum["a", "b"]`, and never on either piece the split cut it into. The last resort is measured against the refusal, not the sink's contents: a whole argument beside the group that IS refusable keeps the construct's single refusal and the group adds none (`array<enum["a", "b"], ???>`, refused on `???`), while a whole brace-carrying argument beside it — text the brace exemption below declines — earns no refusal and suppresses none either (`pair<{a: string}, enum["x", "y"]>` is refused on `enum["x", "y"]`). A bracket group the source never CLOSES (`array<enum["a", "b">`) is outside that push and stays under-refused with its pieces: its extent is unknown, so no source text names the group, and the input keeps whatever this row's judgement of the whole captured arm or field type makes of it on its own. The spelling that delivers the arm does not matter: the plain and `by` alias spellings, and the `.theta` and `.thetalib` extensions, all reach this judgement identically. Refused is a fragment every arm of the `Type` grammar's own recognisers — a `PrimitiveType`, a `NamedType`, a `GenericType`, an inline `ObjectType`, or a `LiteralType` — has declined: an operator absorbed into the fragment with no operand behind it (`Cat +`, `Cat .`, `string +`, `Cat *`, `Cat /`, `Cat %`, `Cat ==`, `Cat &&`, `Cat ?`, `Cat :`, `Cat +++`, `string ++`), an operator alone (`+`), a dangling, leading, or lone `\|` at the field position (`string\|`, `Cat\|`, `\|string`, `\|`), and punctuation text (`???`). Not this text is any `LiteralType` atom or union arm — it lowers under its own emission and draws no diagnostic here — nor is any fragment carrying a `{` or `}` anywhere, balanced or not: the brace frame owns every brace-carrying fragment and this row owns the brace-free ones, the same decline `theta/load/params-type-not-expression` shares for the `params:` position rather than a second copy, so a fragment that reaches the judgement WHOLE with a brace in it stays admitted with its own (possibly permissive) lowering at every `Type` position alike; the exemption is the fragment's own, not its enclosure's. One diagnostic per offending fragment, no dedup: two junk fragments in one declaration render two diagnostics with identical text and identical range, because the row's only placeholder names the declaration and not the fragment — each fragment is refused independently, so fixing one does not silently hide the other. Every diagnostic carries the DECLARATION's range (`SchemaFieldSource` and an arm string carry no range of their own). Two precedence rules keep this to one diagnostic per scope: a field or declaration that already carries an error-severity diagnostic from its own walk — a position rule (`void`, generic arity, `Result`, an inline `enum[...]`), a reserved keyword, an unresolved name, or the inline interior's own `theta/parse/malformed-schema-field` on an entry `TypeParser.parseObject`'s entry walk reaches across a missing entry separator, so that `schema S { a: {a: b c, d e} }` draws that row ALONE ([bug 0256](../../bugs/0256-generic-argument-stranded-entry-registers-permissive.md) §Fix, the operator ruling's OPTION 1 — resync-and-tolerate) — keeps that diagnostic alone and draws no refusal; and a declaration `theta/parse/malformed-alias-rhs` already refused at parse time is skipped, so the refusal never cascades onto a right-hand side another row already named. `{}` keeps its own `theta/parse/empty-schema-body` alone (an admitted, empty `ObjectType`, not a fragment this row reaches), and `schema S { a: -1 }` draws this row BESIDE `theta/parse/malformed-schema-field`: the field `a` derives before the offending token, so `parseSchemaObjectBody` retains it rather than discarding the field list, the retained field reaches the checker-time field-type walk this row judges, and its junk type fragment (`-`) is refused here on its own terms. That co-firing emission is the BODY-LEVEL one — `parseSchemaObjectBody`'s refusal of the token from which no further `Field` derives, ranged at that token — while the emission that SUPPRESSES this row above is the INLINE INTERIOR's entry-walk refusal, ranged at the enclosing declaration; one code, two emission sites, and only the inline-interior site stands in front of this row. The theta is not registered. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | Write a well-formed `Type` — drop the trailing operator or stray text; the position accepts a primitive, a named type, a generic, an inline object, a union, or a literal. | `'<X>' declares a type that is not a theta type expression` |
| `theta/parse/annotation-type-not-expression` | E | parse | A `let` annotation, an `fn` parameter type, or an `fn` return type carries text that derives from no `Type` production ([Grammar Appendix — Type grammar](../grammar.md#type-grammar)). The unit judged is the WHOLE captured annotation, never a fragment of it: one diagnostic per offending annotation, not one per fragment as `theta/parse/schema-type-not-expression` above judges its two positions — an annotation carrying two junk arms (`integer-- \| string--`) still draws exactly one refusal, naming the annotation's own binder. Refused, at all three positions alike, at the `.theta` and `.thetalib` spellings, and through the `subagent fn` / `with` form: the twenty punctuation trailers absorbed with no operand behind them (`integer--`, `integer++`, `integer-`, `integer+`, `integer%`, `integer*`, `integer/`, `integer.`, `integer==`, `integer&&`, `integer\|\|`, `integer?`, `integer!`, `integer:`, `integer\|`, `integer~`, `integer^`, `integer@`, `integer#`, `integer$`), a trailing string literal (`integer"x"`), a trailing number literal only where the joined capture is not `Ident`-shaped (`integer1.5`, and see `integer1`'s exclusion below), and the leading (`--integer`), interior (`int--eger`), doubled (`integer--%%`), spaced (`integer --`, captured `integer--`), and bare (`--`) spellings — each refused one level down inside a `GenericType` argument (`array<integer-->`), a union arm (`integer-- \| string`, `integer \| string--`), and a single-enclosing inline `ObjectType` field (`{ b: integer-- }`) alike. **The `integer\|` trailer is refused at the `let` annotation and the `fn` parameter type, and NOT at the `fn` return slot**: there the trailing `\|` opens a union arm, so the capture absorbs the body that follows (`fn f(): integer\| { 1 }` captures `integer\|{1}`), and the brace the capture now carries is declined by the SHARED fragment-level decline below — the position asymmetry belongs to the capture, not to a judgement this row makes. Two declines bound the refused set, and they differ in provenance. The fragment-level decline is SHARED rather than privately copied — it is the decline `theta/load/params-type-not-expression` and `theta/parse/schema-type-not-expression` already carry: a `LiteralType` atom or union arm lowers under its own emission, and text carrying a `{` or `}` anywhere, balanced or not, belongs to the brace frame, not to this row. The SHRED decline, asked FIRST, is this position's alone and neither sibling row carries it. Text carrying a `[` or `]` is admitted rather than refused, unnarrowed: the shared traversal's generic-argument and union splits track angle-bracket depth (and, inside an inline object, brace depth) but never bracket depth, so such text COULD be split into a SHARD of a group the author wrote as one unit. Text carrying BOTH a brace and an angle bracket is narrowed to that same hazard's actual reach ([bug 0252](../../bugs/0252-brace-and-angle-annotation-junk-exempt-from-refusal.md) §Fix route (a)) rather than admitted on the bare presence of the two characters: when the text is NOT a single enclosing brace group (`isSingleEnclosingBraceGroup`, src/parser/params.ts — the same predicate the shared sink's own brace-group entry already decides membership by), the decline stands exactly as bug 0124 landed it, because only a brace group nested inside a generic argument or standing beside a top-level `|` can be cut into a shard by those splits (`Result<{a: string, b: integer, c: boolean}, QueryError>` is one well-formed annotation, not three junk fragments, and is not a single enclosing brace group, so it stays admitted on this ground). When the text IS a single enclosing brace group, the author wrote that group as one unit neither split can cut, so a KIND-MATCHED scan of its interior judges it directly instead of admitting it unseen: a close token that closes nothing, or closes a kind its own nearest unclosed opener did not open, derives from no `Type` production and is refused (`{a: integer, b > c, m: integer}`'s `>` closes no open `<` and refuses the whole annotation; `{a: integer--, c: array<integer>}`'s tokens each close their own kind, so the scan defers to the shared refusable-text sink below, which refuses the keyed junk tail `integer--` exactly as it does with no angle bracket present). Measured post-[bug 0204](../../bugs/0204-bracket-blind-split-shreds-inline-object-in-generic.md): with the pre-0252 blanket admission hypothetically restored over a text that is not a single enclosing brace group, the GENERIC-ARGUMENT half of that hazard no longer reaches judgement at all — `array<{a: string, b: integer, c: boolean}>` and the `Result<...>` example above both yield an EMPTY refusable set, where the same probe against the pre-0204 traversal yielded `["b: integer"]` — because bug 0204 §Fix (b)(3) already stops a generic-argument shard the split cuts from a group before it can reach the sink this decline guards; the UNION-split half of the hazard (a brace group whose own top-level `|` a union split can shred) is why the decline still declines such a text at all, since only that shape reaches a splitter able to shred it, and it is the reason a brace group that IS single-enclosing is judged rather than declined outright — nothing outside it can shred it. Two spellings that resemble this class are NOT refused by THIS row, because each is an `Ident` and therefore a `NamedType`: the prose spelling `thisisnotatype` (one `Ident` once the capture drops its interior spaces) and a number-literal trailer on an identifier (`integer1`). Both are `theta/parse/unresolved-named-type`'s question now that its widening ([bug 0262](../../bugs/0262-unresolved-named-type-silent-at-nine-reference-positions.md) §Fix) covers all three of this row's positions: `thisisnotatype` resolves against no declaration and draws that row's refusal at the `let` annotation and the `fn` parameter type (an undeclared name is an undeclared name regardless of spelling); `integer1` is an `Ident` on the same ground, so a bare, WRITTEN `integer1` is refused as `theta/parse/unresolved-named-type` at these positions exactly as `thisisnotatype` is. What that widening's suppression predicate withholds is a narrower class: an `Ident`-shaped spelling the capture ABSORBED from a DIFFERENT authoring mistake — `number1` captured from `fn f(): number 1`, `stringletx` captured from an unclosed parameter list — whose capture window already carries an error-severity diagnostic naming the real fault. Such an absorbed spelling stays silent under both rows alike; a spelling the author wrote does not. An annotation whose own type-side walk already drew an error-severity diagnostic — a position rule (`void` at a non-return position, a generic arity mismatch), or the inline interior's own `theta/parse/malformed-schema-field` on an entry `TypeParser.parseObject`'s entry walk reaches across a missing entry separator, so that `fn f(p: {a: b c, d e}): integer { 1 }` draws that row ALONE ([bug 0256](../../bugs/0256-generic-argument-stranded-entry-registers-permissive.md) §Fix, the operator ruling's OPTION 1 — resync-and-tolerate) — keeps that diagnostic ALONE and draws no refusal beside it; `theta/parse/result-in-schema-position` is not among the rules reachable here, since the three positions run their walk as `value`, `value` and `return`, never as a schema-feeding one. A refused annotation is ABSENT to the downstream consumers this row's withhold reaches — the `?`-scope check, the Result-certainty channel, the callee's parameter table, the binding record, the `fn` parameter scope, the `subagent fn` return-boundary check, and the QRY-4 explicit-schema check (`checkLetMismatch`, src/parser/query-schema-resolve.ts, joined by bug 0222) — rather than a declaration of some other type, though the mechanism differs by consumer. The `?`-scope check, the Result-certainty channel, the callee's parameter table and the QRY-4 explicit-schema check read the position exactly as an unannotated one would. The binding record and the `fn` parameter scope instead seed the binding WITHHELD, so every later structural read of it defers rather than judging a type the refused text never supported — the deferral direction the withhold machinery owns. The `subagent fn` return-boundary check is equivalent to neither: a refused return annotation is nonetheless SUPPLIED text, and FN-6 ([Functions — FN-6](../functions.md#fn-6)) validates supplied text at the boundary rather than inferring it, so the refusal skips the return-type inference an absent annotation runs instead ([Functions — FN-3](../functions.md#fn-3)) — inference that can itself draw `theta/parse/return-no-common-type` — while supporting no boundary verdict of its own either. Whatever the mechanism, the refusal fires alone at each and no sibling row reports on the refused text there. Four observable consequences bound this row's in-scope set from that side. A refused RETURN annotation is no declaration of a non-`Result` return type, so a body `?` under one draws no `theta/parse/question-outside-result-fn` beside the refusal; it is no declaration of a `Result` return type either, whatever prefix its text happens to carry, so interpolating the value of a call to that `fn` — through a binding or directly — draws no `theta/parse/interpolated-result`. Nor, at a `subagent fn`, is it read at the return-boundary check FN-6 equates with `invoke<T>`: a refusal whose junk sits one level down inside a `GenericType` argument (`array<integer-->`) still converts to an `array` outer shape, and `⊑`'s array arm would decide that outer shape against the body's inferred payload before it ever inspects the unresolvable element — so reading the refused text at that boundary would draw a `theta/parse/invoke-return-type-mismatch` no author earned, and the boundary check is skipped instead. A refused PARAMETER type is absent from the callee's parameter table, so an argument at that position draws no `theta/parse/fn-arg-type-mismatch`. That withhold is bounded to verdicts DERIVED from the refused text; an INDEPENDENT fault in the same declaration is outside it and keeps its own diagnostic beside the refusal. A bare `return` (no operand) under a refused return annotation still draws `theta/parse/bare-return-in-non-void`, because that row's own test is whether the captured return-annotation text spells exactly `void` — an absent annotation is alike a non-`void` scope, so the verdict is not derived from the refused text in any discriminating way. An annotation with no initialiser still draws `theta/parse/let-without-initialiser` beside the refusal, on a stricter form of the same independence: that row's trigger does not read the annotation's text at all, only whether an initialiser is syntactically present. The line between this pair and the four suppressed above is whether the annotation's ABSENCE would reach the same verdict: `theta/parse/question-outside-result-fn`, `theta/parse/interpolated-result`, `theta/parse/invoke-return-type-mismatch` and `theta/parse/fn-arg-type-mismatch` each need a declared type the annotation would have supplied, so an absent annotation reaches none of them and reading the refused text in its place would manufacture a diagnostic the absent treatment would not — true of `theta/parse/invoke-return-type-mismatch` as well, even though an absent return annotation reaches a DIFFERENT verdict instead, `theta/parse/return-no-common-type`, through the return-type inference a refusal never attempts; `theta/parse/bare-return-in-non-void` and `theta/parse/let-without-initialiser` ask a question an absent annotation answers identically, so reading the refused text changes no verdict and withholding it is not this row's business. The withheld reading is a DEFERRAL, not an assertion that the position is well-typed: it removes emissions that would have been derived from the refused text and adds none. The empty annotation (`let a: = 3`) is unaffected, silent before and after this row alike. Every diagnostic carries the declaration's range — a `let` statement's, or an `fn` declaration's — and names the `let` binding, the `fn` parameter, or the `fn` name, unlike `theta/parse/schema-type-not-expression`'s `<X>`, which names the enclosing declaration because neither of its two positions carries a binder of its own. The theta is not registered. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar) | Write a well-formed `Type` — drop the trailing operator, punctuation, or stray text; the position accepts a primitive, a named type, a generic, an inline object, a union, or a literal (the return slot also accepts `void`). | `'<name>' declares a type that is not a theta type expression` |
| `theta/parse/query-annotation-type-not-expression` | E | parse | An author-written `@<T>` (or bare `@Ident`) query ascription — `QueryExpr.ascriptionWritten === true` — carries text that derives from no `Type` production ([Grammar Appendix — Type grammar](../grammar.md#type-grammar)). Not this position: a `let x: T = @`…`` or QRY-2-inferred annotation propagated onto a query that carried none — that junk is the `let` annotation's own text, already refused at the `let` position by `theta/parse/annotation-type-not-expression`, and the judgement runs against `queryResponseAnnotation(schema)`'s `Result<T, E>` peel, never the raw captured text, so a `Result<Ghost, QueryError>` ascription is judged on `Ghost` and never names the builtin `QueryError` the peel exists to protect. The unit judged is the WHOLE captured annotation, never a fragment of it: one diagnostic per offending annotation, carrying the QUERY EXPRESSION's range (the annotation carries no range of its own) and naming nothing — unlike `theta/parse/annotation-type-not-expression`, this position has no binder of its own, since a bare `@<T>`…`` query statement declares nothing at all, so the Message is placeholder-free, exactly as this capture's sibling `theta/parse/empty-query-annotation` is. Refused, one level down inside a `GenericType` argument, a union arm, and a single-enclosing inline `ObjectType` field alike. The refused set is a strict SUPERSET of `theta/parse/annotation-type-not-expression`'s, not a copy of it. It contains, first, every spelling that row refuses: the twenty punctuation trailers absorbed with no operand behind them (`Ghost--`, `Ghost++`, `Ghost-`, `Ghost+`, `Ghost%`, `Ghost*`, `Ghost/`, `Ghost.`, `Ghost==`, `Ghost&&`, `Ghost\|\|`, `Ghost?`, `Ghost!`, `Ghost:`, `Ghost\|`, `Ghost~`, `Ghost^`, `Ghost@`, `Ghost#`, `Ghost$`), a trailing string literal (`Ghost"x"`), a trailing number literal only where the joined capture is not `Ident`-shaped (`Ghost1.5`, and see `Ghost1`'s exclusion below), and the leading (`--Ghost`), interior (`Gho--st`), doubled (`Ghost--%%`), spaced (`Ghost --`, captured `Ghost--`) and bare (`--`) spellings. It contains, beyond that row's set, three STRUCTURAL trailers: `,`, `)` and `=` (`Ghost,`, `Ghost)`, `Ghost=`). Those three are the differential that gives this row its identity. At the sibling row's positions each is a member of `parseType`'s stop set, so the capture ENDS at `Ghost` and the trailer never reaches a type-grammar judgement at all — the fault surfaces one layer out instead, as `theta/parse/let-without-initialiser` plus a stray-token `theta/parse/unsupported-feature`. At THIS capture, which is delimited by its own closing `>` and carries no stop set whatever, each JOINS the annotation text and is judged with it. Two declines bound the refused set, inherited VERBATIM from that row's recogniser (`annotationSourceIsNotTypeExpression`, src/parser/type-layer-checks.ts) rather than copied: a `LiteralType` atom or union arm lowers under its own emission and draws no diagnostic here, and text carrying a `[` or `]` is admitted rather than refused, unnarrowed, on the same ground that row's own copy of the sentence states. Text carrying BOTH a brace and an angle bracket inherits the narrowed boundary [bug 0252](../../bugs/0252-brace-and-angle-annotation-junk-exempt-from-refusal.md) §Fix route (a) gives that sentence — by design, since the recogniser this row consults is the shared function, rather than by a second copy of the boundary: when the text is NOT a single enclosing brace group (`isSingleEnclosingBraceGroup`, src/parser/params.ts), the decline stands exactly as bug 0124 landed it, because only a brace group nested inside a generic argument or standing beside a top-level `|` can be cut into a shard by those splits; when the text IS a single enclosing brace group, the same KIND-MATCHED scan the sibling row runs judges it directly at this `@<T>` position too: a close token that closes nothing, or closes a kind its own nearest unclosed opener did not open, derives from no `Type` production and is refused. [Bug 0204](../../bugs/0204-bracket-blind-split-shreds-inline-object-in-generic.md) §Fix (b)(3) measured that the GENERIC-ARGUMENT half of that hazard no longer reaches judgement at all, so the decline's own consequence over a text that is not a single enclosing brace group is narrower than it was when this row first inherited it; the union-split half is why the decline still declines such a text at all. Two spellings that resemble this class are NOT refused by THIS row, because each is an `Ident` and therefore a `NamedType`: `Ghost1` (a number-literal trailer on an identifier) and the prose spelling `thisisnotatype`. Both draw `theta/parse/unresolved-named-type`'s refusal instead, unchanged by that row's widening ([bug 0262](../../bugs/0262-unresolved-named-type-silent-at-nine-reference-positions.md) §Fix): the `@<T>` position already resolved a name before that widening, so `@<Ghost1>` and `@<thisisnotatype>` were refused at HEAD and stay refused — the widening's new positions and its artefact-suppression predicate do not reach this capture. An annotation whose own position-rule walk — `void` at a non-return position, a generic-arity mismatch, an empty inline object, a duplicate inline field name — already drew an error-severity diagnostic keeps that diagnostic ALONE and draws no refusal beside it. The empty interior (`@<>`, `@<  >`) stays `theta/parse/empty-query-annotation`'s alone — every input this row refuses is non-empty. The theta is not registered. | [Grammar Appendix — Type grammar](../grammar.md#type-grammar); [Query — Explicit form](../query.md) | Write a well-formed `Type` — drop the trailing operator, punctuation, or stray text; the position accepts a primitive, a named type, a generic, an inline object, a union, or a literal. | `\`@<...>\` query annotation declares a type that is not a theta type expression` |
| `theta/parse/empty-enum-body` | E | parse | `enum X { }` declaration with no variants. | [Schemas — Enum declarations](../schemas.md) | — | `'<X>' has no variants; an empty enum cannot be validated.` |
| `theta/parse/enum-body-unclosed` | E | parse | An `enum X { Variant, … }` declaration's closing `}` is spelled by [Schemas — Enum declarations](../schemas.md)'s shape line (`enum X { ... }`) and by its separator rule ("Variants are comma-separated; trailing comma optional") — prose only, since [Grammar Appendix](../grammar.md) carries no `EnumDecl` / enum-body production for this row to cite. This row fires when the variant loop (`parseEnumVariants`) has captured at least one variant and then reaches END OF INPUT with no `}` ahead. It partitions with `theta/parse/empty-enum-body` above on the same captured-prefix test: an EMPTY captured prefix (`enum E {` at EOF) keeps that row ALONE, because its Trigger already describes the input and this row says nothing about it. The diagnostic's range is the body's OPENING `{`, mirroring `theta/parse/schema-body-unclosed`'s `openTok` and `theta/parse/fn-param-list-unclosed`'s opening `(`. No withhold applies: unlike a schema field's type, no capture in this loop can absorb the body's own `}` — the `}` arm is the only place a `}` punctuation token is consumed and it always decrements the depth counter, and a `}` carried inside a string token is never counted by these punct-only depth arms, so `enum E { A, { }` (a `}` that closed an interior `{`, leaving depth 1) and `enum E { A, "}"` (a `}` inside a string token) are both still-open bodies this row correctly refuses. The captured names, values and variant decls are RETAINED on the returned declaration exactly as a closed body's are, so the declaration's other checks — `checkEnumDeclaration`'s duplicate-name, duplicate-value and non-string-value checks — run against what the author wrote. Scoped to the `enum` variant list alone (`parseEnumVariants`); the `schema` object body and every other unbalanced bracket are not judged by this row. This row CO-FIRES with a variant-level refusal on the same body (`theta/parse/non-string-enum-value`, `theta/parse/duplicate-enum-variant-name`, `theta/parse/duplicate-enum-value`, `theta/parse/reserved-keyword-as-identifier`, `theta/parse/unterminated-string`) rather than suppressing it or being suppressed — the missing `}` is a fault independent of a variant's own, the same co-firing `theta/parse/schema-body-unclosed` already states for its own position. This is a code addition under the [Governance — Source-language stability — Diagnostic-registry carve-out](../governance/source-language-stability.md#diagnostic-registry-carve-out), in scope for every input that did not previously emit this code: the inputs it newly names divide into those that loaded cleanly before it — the variant-boundary truncations this row exists for — and those that already carried another row's refusal, which this row's emission joins rather than replaces. The theta is not registered. | [Schemas — Enum declarations](../schemas.md) | Close the variant list with `}`. | `enum variant list is not closed by '}'` |
| `theta/parse/unknown-variant` | E | parse | `Enum.Variant` reference where `Variant` is not a declared variant of `Enum`. | [Schemas — Variant access](../schemas.md) | — | `unknown variant '<variant>' on enum '<enum>'` |
| `theta/parse/unresolved-named-type` | E | parse | A `NamedType` that resolves to no declaration usable at the position it is written. The positions are the `params:` right-hand side, the `@<T>` query annotation, a `schema` body field type, the right-hand side of a `schema X = ...` alias/union declaration, an object-constructor name (`Name { ... }`), a `match` object-pattern head (`Name { ... }`), a `let` annotation, an `fn` parameter type, an `fn` return type, and an `invoke<Type>` ascription ([bug 0262](../../bugs/0262-unresolved-named-type-silent-at-nine-reference-positions.md) §Fix, the FULL widening) — together with every generic type argument, union arm, `Result` argument, and inline object field nested inside one of those ten. `let x: Nope = 1` and `fn f(x: Nope): number { 1 }` now refuse this code exactly as `schema S { f: Nope }` always has. Resolution is whole-file over the body's top-level declarations — a top-level `schema` / `enum` declaration or a symbol imported from a `.thetalib` resolves the name wherever in the body it is declared, so a frontmatter-to-body forward reference is not itself a failure. The object-constructor position carries the additional requirement that the resolved declaration be brace-constructible, which in this file means a `schema` with an object body: a name declared here as an `enum`, or as a `schema` without an object body, is not constructible and fires this code. An imported symbol always defers at the constructor position — the importer's parse holds neither its field bodies nor its kind — so the field-set checks do not run and this code does not fire. The pattern-head position carries no such brace-constructible requirement: a pattern head REFERENCES a declaration rather than constructing one, so the head need only resolve to a same-file `schema` or `enum` declaration, an imported symbol, or a builtin error-model name (`QueryError`) — an `enum` name and an imported symbol defer here, where the object-constructor position fires. The same builtin error-model admission applies at the `let` annotation, the `fn` parameter type, the `fn` return type and the `invoke<Type>` ascription, so `fn rate_strictness(a: Author): Result<integer, QueryError> { ... }` keeps loading clean. A reserved-keyword spelling read where a `NamedType` is read is `theta/parse/reserved-keyword-as-identifier`'s to report at every position alike, never this row's — a reserved head (`let a: match = 3`) is silent under this row rather than misreported. A written annotation that response-schema inference carries onto a query the author left without one draws at most one refusal for the one annotation the author wrote: the `@<T>` query annotation is the propagated text's sole emitter, and the position that SUPPLIED the text withholds its own resolution of it rather than doubling the count. Three positions supply text that way — a `let` annotation (onto a bare-query initialiser, and onto a query written in a ternary branch or an array element of that initialiser), an `fn` return type (onto a query at a return position of the body, its tail or a `return` operand at any control-flow depth, and onto a query one crossed ternary or array-literal level inside one), and an `fn` parameter type (onto a query written as the matching argument of a call to that function in the same file). The withhold follows the TEXT rather than the spelling: an `array<T>` annotation reaching an element query as `T` withholds at the position that wrote `array<T>`, since the one written annotation is still what the query carries. At the `let` annotation, the `fn` parameter type, the `fn` return type and the `invoke<Type>` ascription, the refusal is further withheld when the CAPTURE'S OWN source window is already covered by an error-severity diagnostic naming the real fault — text the capture absorbed from a different authoring mistake, not a name the author wrote (`fn h(a: string` followed by `let x = 1` keeps `theta/parse/fn-param-list-unclosed` alone rather than also naming the absorbed `stringletx`). The window is the capture's, not the whole construct's: it runs from the construct's start up to the first source unit that FOLLOWS the capture, since only text ahead of that unit could have been absorbed into the capture — the body for an `fn` parameter type and an `fn` return type, the initialiser for a `let` annotation, the argument list for an `invoke<Type>` ascription. A fault inside the `fn` body, inside the `let` initialiser, or inside an `invoke` argument is therefore a second, independent authoring mistake and both diagnostics stand (`let a: Nope = "abc` with no closing quote keeps `theta/parse/literal-newline-in-string` and names `Nope` beside it), as is a fault ranged past the end of a `let` statement. Where the following source unit is absent — a body the parser never recovered, a `let` with no initialiser, an `invoke` with no arguments — the whole construct stands as the window. A refusal drawn for ANOTHER capture's head is never such cover: it names that head and says nothing about a head written at a reference position nested inside its window, so an `fn` parameter type and a `let` annotation in that function's body, or a `let` annotation and an `invoke<Type>` ascription in its own initialiser, each keep their own refusal. Cover requires the refusal to be RANGED OVER THE CONSTRUCT the capture belongs to: a refusal carrying an ENCLOSING declaration's range — an `fn` whose own header annotation is refused, whose diagnostic spans that declaration including its body — is not cover for a capture written inside that body, so `fn f(): integer-- { let y: Gone = 1  1 }` draws `theta/parse/annotation-type-not-expression` for the header AND names `Gone` ([bug 0272](../../bugs/0272-enclosing-annotation-refusal-swallows-nested-unresolved-head.md) §Fix route (b)). One written mistake draws one diagnostic naming it, and two written mistakes draw two — always across constructs, and inside ONE construct as well whenever the second mistake is a head this row itself names. A refusal ALREADY drawn over the capture's OWN construct is cover whichever of that construct's captures earned it and whichever code OTHER THAN THIS ROW'S OWN it carries, so two written mistakes inside ONE construct draw one diagnostic when the coverer names a DIFFERENT fault: `fn f(p: integer--, q: Gone): number { 1 }` draws the annotation refusal alone, and `fn f(): integer-- { fn g(z: Gone): number { 2 } … }` keeps the inner `fn`'s own `theta/parse/nested-fn` as cover for `Gone` — clause (iv)(3)'s decided same-construct behaviour ([bug 0262](../../bugs/0262-unresolved-named-type-silent-at-nine-reference-positions.md) §Fix, the operator ruling, ratified), stated here rather than left as a false absolute. This row's own refusal is never that cover, whichever construct it is ranged over, which is what keeps the never-cover sentence above true unqualified: `fn f(p: Nope, q: AlsoNope): number { 1 }` and `fn f(p: Nope): AlsoNope { 1 }` each draw TWO refusals, one naming each head, even though every one of them carries the whole declaration's range and so covers the construct in which the other head is judged. This narrowing adds emissions inside the in-scope set the carve-out below already names, not a second carve-out. This widening is a [Governance — Source-language stability](../governance/source-language-stability.md#gov-15) carve-out whose in-scope input set is a written, unresolvable `NamedType` head at one of these ten reference positions, and nothing else: every newly-refused spelling loaded cleanly before it. | [Frontmatter — params](../frontmatter.md); [Expressions — Object construction](../expressions.md#object-construction) | — | `unresolved named type '<name>'` |
| `theta/parse/duplicate-enum-value` | E | parse | Two `enum` variants share an explicit string value. | [Schemas — Enum declarations](../schemas.md) | — | `duplicate enum value '<value>' across variants of enum '<enum>'` |
| `theta/parse/duplicate-enum-variant-name` | E | parse | Two variants in the same `enum` share an identifier, regardless of explicit-value assignment. The name-duplication check runs before `theta/parse/duplicate-enum-value`, so both implicit-value and distinct-explicit-value name collisions fail on this code. | [Schemas — Enum declarations](../schemas.md) | — | `duplicate variant name '<variant>' on enum '<enum>'` |
| `theta/parse/non-string-enum-value` | E | parse | `enum` variant explicit value is not a single string literal. | [Schemas — Enum declarations](../schemas.md) | theta 1.0 enums carry string values only. | `enum variant value must be a string literal; got <kind>` |
| `theta/parse/inline-enum` | E | parse | `enum["a", "b"]` or another inline-enum spelling, matched at the START of a `schema` object-body field type's, a `schema X = …` alias/union arm's, or a `params:` field's own top-level captured text (`checkInlineEnumForm`'s anchored `/^\s*enum\s*\[/`, src/parser/schema-declarations.ts) — never one level down, inside a `GenericType` argument or an inline `ObjectType` field, at any of the three: a nested `enum[…]` there answers to `theta/parse/schema-type-not-expression` at the two schema positions and `theta/load/params-type-not-expression` at `params:` instead ([bug 0217](../../bugs/0217-nested-inline-enum-in-generic-argument-draws-nothing.md) §Fix (b)(2)), never to this row. At `params:` the match is asked of the field's WHOLE recovered type text, including an earlier union arm when the enum leads it, so `enum["x"] \| string` matches (the enum is first) while `string \| enum["x"]` does not — the same leading-arm-only anchoring the `schema` object-field position carries, unlike the alias/union position's per-arm pass, which fires on a second-position arm. At `params:` this row decides AHEAD of the TEXT stage of `theta/load/params-type-not-expression` — including that stage's brace exemption, so `p: 'enum[{a: string}]'`, which loaded cleanly before this widening, is refused here instead of there ([bug 0162](../../bugs/0162-inline-enum-trigger-misses-params-position.md) §Fix route (a); the addition is a [Governance — Source-language stability](../governance/source-language-stability.md#gov-15) carve-out for an input newly brought into the code's emission set) — and ahead of nothing else at that position: a field the NODE-SHAPE stage refused keeps `theta/load/params-type-not-expression` even when its recovered text matches the predicate (`p:` over a block mapping `enum["x"]: y`), and a field already carrying an error-severity diagnostic from its own type-side pass — a reserved keyword, an unresolved name, or one of the type grammar's own registered rejections — keeps that diagnostic and this row does not fire there, mirroring that row's own precedence rule 1. That ONE-diagnostic-per-field discipline is where the `params:` position parts from the two `schema` positions, which draw this row BESIDE such a diagnostic: `p: 'enum["x"] \| Ghost'` draws `theta/parse/unresolved-named-type` alone at `params:`, where the same text at a `schema` object field or an alias/union arm draws that row and this one together. A `params:` field this row refuses draws no default-side diagnostic either — `theta/parse/literal-newline-in-string`, `theta/parse/default-not-literal`, `theta/parse/default-without-literal` and `theta/parse/params-default-type-mismatch` are all withheld for it, as they are for a field the text stage refused, so `p: 'enum["x"] = junk here'` and `p: 'enum["x"] ='` each draw this row alone. The `@<T>` query annotation, an `fn` parameter type, an `fn` return type, and a `let` annotation draw nothing for this text and this row does not reach them (bug 0162 §Non-goals). | [Schemas — Enum declarations](../schemas.md) | Use a literal-union (`"a" \| "b"`) or a top-level `enum` declaration. | `inline 'enum[...]' is not supported; use a top-level 'enum' declaration or a literal-union` |
| `theta/parse/ambiguous-discriminator` | E | parse | Discriminated-union detection finds more than one candidate field. | [Schemas — Discriminated unions](../schemas.md) | Declare explicitly with `by <field>`. | `ambiguous discriminator for <X>; candidates: <fields>. Declare explicitly with 'by <field>'.` |
| `theta/parse/missing-discriminator` | E | parse | Discriminated-union detection finds no candidate field. | [Schemas — Discriminated unions](../schemas.md) | Add a `kind` (or similar) field to each variant, or declare explicitly with `by <field>`. | `<X> is a union of object schemas with no shared single-literal discriminator field. Add a 'kind' (or similar) field to each variant, or declare explicitly with 'by <field>'.` |
| `theta/parse/duplicate-discriminator-value` | E | parse | Two variants of a discriminated union share the same discriminator value. | [Schemas — Discriminated unions](../schemas.md) | — | `duplicate discriminator value '<value>' across variants of <X>` |
| `theta/parse/nested-discriminator` | E | parse | Discriminator field is not at the top level of each variant (e.g. `kind: { type: "x" }`). Excluded is an occurrence whose own field type IS an empty inline object (`{}`) — a construct `theta/parse/empty-schema-body` has already refused as ill-formed, so there is no discriminator tag at any level for this row to name; that occurrence keeps the refusal ALONE and this row withholds on it (bug 0129). A genuinely nested group whose own interior itself carries an empty object (`kind: {a: {}}`) is not excluded — the outer group is well-formed and names a real tag one level down — nor is a nested occurrence in ANY variant when a sibling variant's occurrence is the refused empty object; each variant's occurrence is judged on its own text. | [Schemas — Discriminated unions](../schemas.md) | — | `discriminator field '<field>' must be at the top level of each variant of <X>` |
| `theta/parse/absent-discriminator-field` | E | parse | An explicit `by <field>` clause naming a field at least one variant does not declare. Detection rule 1 — "be present in every variant" — binds the named field the same way the string-literal rule and the uniqueness rule already do; "absent" means absent from ANY variant, not from every variant, so a half-present field (present in one variant, missing from another) also refuses. Checked after `theta/parse/nested-discriminator`, so a present-and-nested occurrence keeps that more specific code even when a sibling variant lacks the field; checked before `theta/parse/non-literal-discriminator`, `theta/parse/non-string-discriminator` and `theta/parse/duplicate-discriminator-value`, which all presuppose a field that resolves. Because the explicit form replaces implicit detection rather than falling back to it, an absent field previously silenced every one of those rejections the same variants would otherwise draw; this row is that silence's disposition (bug 0046). | [Schemas — Discriminated unions](../schemas.md) | Declare the named field in every variant, or correct the spelling. | `discriminator '<field>' on <X> must be declared in every variant` |
| `theta/parse/non-string-discriminator` | E | parse | Discriminator field's per-variant literal type is not `string` — i.e. a numeric or boolean literal `const`. Applies equally to implicit detection and to the explicit `by <field>` form. | [Schemas — Discriminated unions](../schemas.md) | Wrap the tag as a string (e.g. `kind: "v1"` rather than `kind: 1`). | `discriminator '<field>' on <X> must be a string-literal type; got <kind>` |
| `theta/parse/non-literal-discriminator` | E | parse | The field an explicit `by <field>` clause names resolves in EVERY variant but its type is not a single literal `const` — a literal-union (`kind: "a" \| "b"`), a bare `string`, a declared `enum` name, `integer`, `array<string>`, a `"cat" \| null` union, a named object schema, or a brace-rooted union of `ObjectType` arms (`{a: X} \| {b: Y}`). Detection rule 2's single-literal property binds a named field exactly as the top-level rule and the uniqueness rule already do. A nested occurrence in any variant keeps `theta/parse/nested-discriminator` (checked first); a per-variant literal whose type is not `string` keeps `theta/parse/non-string-discriminator`; a `by` clause naming a field at least one variant does not declare is outside this row — that field never reaches `allLiteral` at all, and is `theta/parse/absent-discriminator-field` instead. Excluded, on the same ground as the neighbour row above: an occurrence whose field type IS an empty inline object (`{}`) has already been refused as ill-formed by `theta/parse/empty-schema-body`, so this row's own verdict — that the field's type is not a single literal — would be derived from that refused text rather than earned on its own, and the refusal keeps ALONE instead (bug 0129). NOT excluded are the union spellings this row already names: `{} \| null` (or `null \| {}`) and `{a: X} \| {b: Y}` are brace-rooted UNIONS, not empty objects — their own field type carries no refusal of its own, and this row's verdict is earned on the union shape independently of whichever arm happens to be empty. Implicit detection is unaffected and keeps `theta/parse/missing-discriminator`. | [Schemas — Discriminated unions](../schemas.md) | Give the named field a single string literal value in each variant (e.g. `kind: "cat"`). | `discriminator '<field>' on <X> must be a single string-literal type in every variant` |
| `theta/parse/type-alias-cycle` | E | parse | A `schema X = ...` whose right-hand side reduces to `X` through type aliases only — directly (`schema X = X`) or transitively (`schema X = Y; schema Y = X`). Cycles that traverse at least one object-schema hop remain legal. Detection runs after schema-name resolution but before lowering. | [Schemas — Recursion](../schemas.md) | — | `type-alias cycle: <path>` |
| `theta/parse/system-on-prompt-mode` | E | parse | `system:` frontmatter field declared on a `mode: prompt` theta. | [Frontmatter — `system`](../frontmatter.md) | Either switch to `mode: subagent` or remove `system:`. | `'system:' is not permitted on a mode: prompt theta` |
| `theta/parse/system-interp-not-path` | E | parse | Body of a `${...}` interpolation in `system:` is not a bare identifier path (e.g. `${arr[0]}`, `${a + b}`, `${f(x)}`, `${a?.b}`, `${"x"}`). | [Frontmatter — `system` Interpolation](../frontmatter.md) | The `system:` slot accepts only bare identifier paths; see [future-considerations.md](../future-considerations.md) for richer expressions. | `'system:' interpolation body must be a bare identifier path` |
| `theta/parse/system-interp-unknown-param` | E | parse | Head identifier of a `system:` `${...}` path does not name a declared `params` field. | [Frontmatter — `system` Interpolation](../frontmatter.md) | — | `'system:' interpolation references unknown param '<name>'` |
| `theta/parse/system-interp-bad-field` | E | parse | A `.Ident` step in a `system:` `${...}` path does not name a reachable theta-side `params` object field, or descends into an array or un-narrowed discriminated union. | [Frontmatter — `system` Interpolation](../frontmatter.md) | — | `'system:' interpolation '.<field>' does not name a reachable object field on <path>` |
| `theta/parse/system-interp-unterminated` | E | parse | `${` in a `system:` value is not closed by a matching `}` before the YAML scalar ends. | [Frontmatter — `system` Interpolation](../frontmatter.md) | — | `'system:' interpolation '${' is not closed by a matching '}'` |
| `theta/parse/timeout-field-rejected` | E | parse | `timeout:` field declared on a query, tool call, or invoke (theta 1.0 has no per-call timeout). | [Cancellation](../cancellation.md) | Per-call timeouts are deferred. | `'timeout:' field is not supported in theta 1.0` |
| `theta/parse/bind-context-session-on-subagent` | W | parse | `bind_context: session` declared on a `mode: subagent` theta. | [Binder — Binder context](../binder.md) | Subagent-mode thetas have no caller-session context. | `'bind_context: session' has no effect on a mode: subagent theta` |
| `theta/parse/bind-echo-on-bypass` | W | parse | An explicit `bind_echo: true` is declared on a single-string-bypass theta (`params:` declares exactly one no-default `string` field); a defaulted `bind_echo` does not fire this warning. The no-params bypass is owned by `theta/load/bind-echo-without-params`, not this code. | [Binder — Echo policy](../binder.md) | The single-string bypass auto-suppresses echo regardless. | `'bind_echo: true' has no effect on a single-string-bypass theta` |
| `theta/parse/thetalib-top-level-statement` | E | parse | `.thetalib` file contains a top-level statement, `let`, or query. | [Imports — `.thetalib` file rules](../imports.md) | Move the code into a `fn` body. | `top-level statement not permitted in .thetalib file; move into a fn body` |
| `theta/parse/import-name-collision` | E | parse | Two imports bring in the same symbol name, or an imported symbol collides with a top-level declaration in the same file. | [Imports — Name collisions](../imports.md) | Resolve with `as`-aliasing. | `imported symbol '<name>' collides with another import or top-level declaration` |
| `theta/parse/import-unknown-symbol` | E | parse | An `import { ... }` or `export { ... } from` specifier (including `as`-aliased forms) names a symbol that is neither a top-level declaration nor a transitive `export … from` re-export of the resolved `.thetalib` file. Fires after the resolved file's own parse completes and is collected by the multi-error batching rule rather than fast-failing; `<name>` renders the source symbol, not the alias. | [Imports — Unknown imported symbol](../imports.md) | — | `imported symbol '<name>' is not declared or re-exported by '<path>'` |
| `theta/parse/import-reserved-synthesised-name` | E | parse | A parsed `import` / `export` specifier's LOCAL binding (the `as` alias where present, else the source name) matches one of schema-subset.md §Synthesised names' four forms exactly — `__inline_<slug>`, `__theta_respond_<slug>`, `__theta_bind_<slug>`, `__theta_callee_<slug>__<post-rename-name>` — with `<slug>` exactly 16 lowercase hex characters (schema-subset.md §Canonical schema hash step 4). Checked per specifier at parse time, on both statement kinds: it fires whether or not the `.thetalib` path resolves, and whether or not a `from` clause follows the specifier list, co-emitting with `theta/parse/import-missing-from-clause` on a from-less list. `<name>` renders the LOCAL binding, not the source symbol. | [Lexical — Identifiers](../lexical.md) | Resolve with `as`-aliasing. | `imported symbol '<name>' binds a reserved synthesised name` |
| `theta/parse/import-missing-from-clause` | E | parse | A specifier list — on either an `import` or an `export` statement — not followed by a `from` clause naming a `.thetalib` path literal: the `from` keyword absent, or present with no `string` token after it. Covers the degenerate bare-keyword (`import`, `export`) and empty-list (`import {}`, `export {}`) spellings. Fires once per statement, ranged over the whole statement — never once per specifier — and co-emits with `theta/parse/import-reserved-synthesised-name` when a specifier's local binding is also a reserved synthesised name. | [Imports — Re-exports](../imports.md) | Add a `from` clause naming the `.thetalib` the symbols come from. | `import / export specifier list requires a 'from' clause with a .thetalib path literal` |
| `theta/parse/import-malformed-specifier-list` | E | parse | A specifier list on an `import` or `export` statement that violates `ImportSpec` / `ExportSpec`, in one of four shapes: (1) the list is absent entirely — no `{` — or (2) the list is present but produces zero specifiers, or (4) the list's separators do not conform — a specifier token where a specifier is already pending with no `,` consumed since it (a missing separator between two specifiers), a `,` where no specifier precedes it or a `,` is already pending (a stray or doubled separator, or a second trailing `,` beyond the one `","?` licenses), or a token the specifier loop's catch-all discarded rather than classified — each fired once PER STATEMENT, ranged over the whole statement; or (3) a specifier's `as` keyword is consumed with no following `Ident` alias, fired once per malformed specifier, ranged over that specifier. Shape (4) is silent when the recovered list is empty (shape (2)'s own subject) or when any specifier in it carried a dangling `as` (shape (3)'s own subject), so the three statement/specifier arms partition and at most one statement-ranged diagnostic of this code fires per statement. The statement arm (shapes 1, 2, 4) fires only where the trailing clause is well-formed — the `from` keyword present with a `string` token after it; a statement with the `from` keyword absent, or present with no `string` token after it, stays inside `theta/parse/import-missing-from-clause`'s Trigger, which already claims the bare-keyword (`import`, `export`) and empty-list (`import {}`, `export {}`) spellings, and the statement arm never co-emits with that code. That `string` token need not name a `.thetalib`, so the statement arm co-emits with `theta/parse/import-non-thetalib-extension` on a path the extension rule refuses; shape (4)'s missing-separator case can also conjure a specifier the author did not write, so it additionally co-emits with `theta/parse/import-reserved-synthesised-name` when the invented specifier's local binding is a reserved synthesised name. The specifier arm (shape 3) is unconditional and co-emits with `theta/parse/import-missing-from-clause` on a from-less list and with `theta/parse/import-reserved-synthesised-name` when the malformed specifier's local binding is also a reserved synthesised name. | [Imports — Re-exports](../imports.md) | — | `import / export specifier list must carry at least one specifier, each 'Name' or 'Name as Alias'` |
| `theta/parse/invoke-arg-type-mismatch` | E | type | `invoke(...)` argument does not type-check against the callee's declared `params` schema (when the callee is statically resolvable). | [Invocation — Argument binding](../invocation.md) | — | `invoke argument <i> ('<param>') type mismatch: expected <expected>, got <actual>` |
| `theta/parse/tool-arg-type-mismatch` | E | type | `<name>(args)` `.theta`-callable-call argument does not match the callee theta's `params` (when statically resolvable). Pi-tool argument type mismatches are not a parse error — they surface at runtime as `Err(CodeToolError { cause: "validation", ... })`; see [Tool Calls — Argument shape](../tool-calls.md). | [Tool Calls — Argument shape](../tool-calls.md) | — | `tool '<name>' argument type mismatch: expected <expected>, got <actual>` |
| `theta/parse/fn-arg-type-mismatch` | E | type | A plain top-level `fn` call `f(args)` — a same-file or imported `.thetalib` function call that is neither an `invoke(...)` nor a `.theta`-callable call — passes an argument whose static type is not compatible with the matched parameter's declared type, where the argument type is statically resolvable; an argument past the parser's static view reaches no `T₁ ⋢ T₂` verdict, so the check is skipped instead, at a structural parameter type (`array<T>`, an inline object type) exactly as at a primitive or named one (the Unresolvable operands paragraph of [Type System — Type compatibility](../type-system.md#type-compatibility)). The same-file half is judged at PARSE time: top-level `fn` declarations are hoisted and always statically resolvable, so no call to one is ever the unresolvable-callee case a runtime check would cover. The imported `.thetalib` half is judged at the LOAD pass instead, once the resolved library already exists as a parsed document (`checkImportedFnCallArgs`, `src/extension/invoke-static-checks.ts`, wired once per importing theta from `checkThetaImports`, `src/extension/import-static-checks.ts`): the expected side resolves through a `TypeEnv` built from the DECLARING library's own statements, never the importing file's, and `<name>` renders the CALL-SITE local spelling — the `as`-alias where one is written, else the source name. A call site INSIDE a `.thetalib` body against a symbol that library itself imported is out of reach for either half: the check runs over the importing theta's own top-level body only, never over a library body. The imported half judges a call only against a `fn` declared DIRECTLY in the library the `import` statement resolves to: a specifier that library provides through its own `export … from` re-export chain names no top-level declaration in that body, so such a call site is silent on both halves — a stated withhold on the same footing as the lib-to-lib fence, not a chain walk duplicated at a second call site. A callee whose own recorded parameter list carries a name no `Ident` derives withholds the ARITY verdict alone (`theta/parse/fn-arity-too-few` / `-too-many`) and still draws THIS row per surviving annotated slot, same-file and imported alike: the recorded parameter COUNT is a `theta/parse/fn-param-not-identifier` recovery artefact the author never wrote, while each surviving annotation is the author's own text and is judged where it is a type expression at all. Neither half's absence of a runtime net is itself a reason to emit on an unresolvable argument. | [Type System — Type compatibility](../type-system.md#type-compatibility) | — | `fn '<name>' argument <i> ('<param>') type mismatch: expected <expected>, got <actual>` |
| `theta/parse/invoke-return-type-mismatch` | E | type | `invoke<Schema>(...)` annotation is structurally incompatible with the callee's inferred return type, where both annotation and callee are statically resolvable. Also fires for a `subagent fn` return annotation: `): T` declares the boundary Ok payload — the `invoke<T>` analogue ([Functions — FN-6](../functions.md#fn-6)) — and is checked against the body's inferred Ok payload the same way (`<callee>` renders the function name). | [Invocation — Typed return](../invocation.md) | Widen the annotation, narrow the callee, or drop the annotation and let the runtime AJV check decide. | `invoke<Schema> annotation incompatible with callee '<callee>' return type <actual>` |
| `theta/parse/invoke-arity-too-few` | E | parse | `invoke(...)` or `.theta` callable call passes fewer arguments than the callee's count of non-defaulted `params:`, where the callee is statically resolvable. | [Invocation — Argument arity](../invocation.md#argument-binding) | Provide the missing argument(s) or default the corresponding `params:` field on the callee. | `invoke '<callee>' passes too few arguments: expected <required> non-defaulted, got <provided>` |
| `theta/parse/invoke-arity-too-many` | E | parse | `invoke(...)` or `.theta` callable call passes more arguments than the callee's total `params:` count. Always parse-time — no runtime safety net is possible. | [Invocation — Argument arity](../invocation.md#argument-binding) | Drop the extra argument(s); positional binding has no destination for them. | `invoke '<callee>' passes too many arguments: expected at most <max>, got <provided>` |
| `theta/parse/fn-arity-too-few` | E | type | A plain top-level `fn` call `f(args)` — a same-file or imported `.thetalib` function call (including a `subagent fn`, whose parameters bind positionally the same way — [Functions — FN-6](../functions.md#fn-6)) — passes fewer positional arguments than the callee's declared parameter count. The same-file arm is judged at PARSE time. The imported `.thetalib` arm is judged at the LOAD pass instead, once the resolved library already exists as a parsed document: `checkImportedFnCallArgs` (`src/extension/invoke-static-checks.ts`), wired once per importing theta from `checkThetaImports` (`src/extension/import-static-checks.ts`), reads the library's declared parameter count off the directly-resolved library body and renders `<name>` as the CALL-SITE local spelling — the `as`-alias where one is written, else the source name, never the library's own declared name. A call site INSIDE a `.thetalib` body against a symbol that library itself imported is out of reach for either arm: the check runs over the importing theta's own top-level body only, never over a library body. The imported arm resolves a callee only among the `fn` declarations DIRECTLY present in the library the `import` statement resolves to: a specifier that library provides through its own `export … from` re-export chain names no top-level declaration in that body, so its call site draws no arity verdict — a stated withhold on the same footing as the lib-to-lib fence above. Withheld when the callee's own recorded parameter list carries a name no `Ident` derives ([Lexical Structure — Identifiers](../lexical.md)), same-file and imported alike: a `theta/parse/fn-param-not-identifier` recovery artefact holds a count the author never wrote, so no arity verdict is drawn beside it. A `fn` parameter carries no default ([Grammar Appendix — `fn` declarations](../grammar.md#fn-declarations)), so the declared parameter count is both the minimum and the maximum required — unlike `invoke(...)`'s `params:`, there is no defaulted-field gap between the two — and a same-file `fn` is always statically resolvable with no runtime AJV net; the imported arm's library is likewise always resolved and in hand by the time the load pass reaches this check, so neither arm needs one either. Decided BEFORE per-argument type ([Invocation — Argument arity](../invocation.md#argument-binding)): on a mis-arity call neither the per-argument `theta/parse/fn-arg-type-mismatch` check nor the parameter-typed element sink runs. What the suppression does NOT reach is each argument expression's own judgement — an argument is still walked, under a null sink, so a sink-less refusal such as `theta/parse/array-no-common-type` is reported beside this row. | [Invocation — Argument arity](../invocation.md#argument-binding) | Provide the missing argument(s); a `fn` parameter carries no default, so every declared parameter needs one. | `fn '<name>' passes too few arguments: expected <required>, got <provided>` |
| `theta/parse/fn-arity-too-many` | E | type | The same plain top-level `fn` call site as `theta/parse/fn-arity-too-few` above — same-file arm at parse, imported `.thetalib` arm at the LOAD pass via the same `checkImportedFnCallArgs` (`src/extension/invoke-static-checks.ts`) wiring, the same junk-parameter-table withhold of the arity verdict alone, the same arity-before-type ordering, the same lib-to-lib call-site fence, the same direct-declaration-only callee resolution (a specifier the library provides through its own `export … from` re-export chain draws no arity verdict) — passes MORE positional arguments than the callee's declared parameter count. `<required>` is the same declared parameter count both rows read; the two rows differ only in direction and in the repair their *Hint* columns prescribe. | [Invocation — Argument arity](../invocation.md#argument-binding) | Drop the extra argument(s); positional binding has no destination for them. | `fn '<name>' passes too many arguments: expected <required>, got <provided>` |
