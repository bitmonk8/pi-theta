# `H8a` — Live production composition wiring and end-to-end acceptance

**Convention.** [`conventions.md`](./conventions.md) (phase categories — end-to-end harness; the [live-host acceptance pair exception](#horizontal-leaves--single-task)). Narrative spec references for the implementer: [`extension-bootstrap-and-per-loom.md`](../spec_topics/pi-integration-contract/extension-bootstrap-and-per-loom.md), [`registration-steps.md`](../spec_topics/pi-integration-contract/registration-steps.md), [`discovery.md`](../spec_topics/discovery.md).

**Adds.** The **production composition root** for the shipped extension: it replaces the `fixtures: []` stub in the default export ([`src/extension/factory.ts`](../../src/extension/factory.ts) `loomExtension`) with the real object graph the per-leaf gates verified only in isolation and the stub explicitly deferred to "a later leaf." At load it constructs the [`H3a`](./H3a-di-seam-skeleton.md) runtime root over the real host seams ([`V8b`](./V8b-filesystem-seam.md) `PiFileSystem`, [`V8c`](./V8c-schema-validator-seam.md) `SchemaValidator`, [`V8d`](./V8d-clock-id-seams.md) `Clock`/`IdSource`, [`V8e`](./V8e-watch-token-seams.md) `PiFileWatcher`/`TokenEstimator`), runs the [`V10a`](./V10a-discovery-walk.md)/[`V10b`](./V10b-package-discovery.md)/[`V10c`](./V10c-settings-merge.md)/[`V10d`](./V10d-reload-debounce.md) five-source discovery walk, maps each discovered `.loom` to the [`H4a`](./H4a-factory-shell-and-harness.md) `LoomFixture` the `session_start` handler registers via `pi.registerCommand`, and publishes/reloads the live [`V9b`](./V9b-registration-reload-wiring.md) `LoomRegistry` — so the shipped extension actually discovers, registers, and runs `.loom` commands. It closes no new spec REQ-ID; the [`extensions/index.ts`](../../extensions/index.ts) entry stays a thin delegating shim and all composition lives in `src/**`.

**Tests.**
- `Convention:` (live-host acceptance) the [`H8a-T`](./H8a-T-live-production-acceptance.md) opt-in live suite passes green: a `.loom` under the project source `<cwd>/.pi/looms/` registers a live slash command through the shipped entry, invoking it drives one real prompt-mode turn producing the fixture sentinel, an alternate discovery source (`--loom` or global `~/.pi/agent/looms/`) also registers, and a bounded schema-typed `@`-query validates against its declared schema — the four `H8a-T` bullets, now green.
- `Convention:` (No globals, statics, singletons; No ambient access; Specific exception types only; Sequential by default) the new `src/**` composition code passes the [`H2a`](./H2a-cross-cutting-gates.md) no-globals architectural test and blocking-runtime / `no-restricted-syntax` checks, the [`H3a`](./H3a-di-seam-skeleton.md) ambient-primitive scan, and the `no-broad-catch` lint — the runtime root is constructed per invocation with no module-level mutable state, and `extensions/index.ts` remains a thin shim carrying no rule-subject logic (release-time residue-inspection item 8 unaffected).

**Deps.** `H8a-T`, `H3a`, `H4a`, `M`, `V8b`, `V8c`, `V8d`, `V8e`, `V9b`, `V10a`, `V10b`, `V10c`, `V10d`, `V11a`, `V19e` — the runtime-root, factory/entry, prompt-mode pipeline, host seams, registration/registry, five-source discovery, binder-model resolution, and the [`V19e`](./V19e-composition-producer.md) whole-program composition producer (the parsed-`.loom` → runnable `LoomFixture` mapping this leaf wires into the shipped default export, itself built on the [`V19a`](./V19a-whole-program-parser.md) parser and [`V19c`](./V19c-statement-executor.md) interpreter this leaf's charter previously assumed already existed).

**Ships when.** `npm run test:live` passes green against a configured live provider — a `.loom` on disk under a real discovery source registers as a slash command in the shipped extension and its invocation drives a real model turn producing the fixture sentinel, an alternate source also registers, and a typed query validates against its schema — while `npm test`, `npm run typecheck`, `npm run lint`, and the `src/**` architectural gates stay green and `extensions/index.ts` remains a thin delegating shim.
