# `V20g-T` — Production-path language-surface conformance suite (tests)

**Convention.** [`conventions.md`](./conventions.md) (phase categories — end-to-end harness; the [live-host acceptance pair exception](#horizontal-leaves--single-task)). Narrative spec references for the implementer: [`extension-bootstrap-and-per-loom.md`](../spec_topics/pi-integration-contract/extension-bootstrap-and-per-loom.md), [`expressions.md`](../spec_topics/expressions.md), [`error-model`](../spec_topics/runtime-value-model.md). Closes no new spec REQ-ID.

**Adds.** A **standing acceptance suite that drives the full documented language surface THROUGH the production composition** — `composeLoomFixture` / the shipped [`extensions/index.ts`](../../extensions/index.ts) via the live SDK harness [`tests/live/harness.ts`](../../tests/live/harness.ts) — not isolated modules. It is the regression net for the meta-failure the hardening campaign exposed: 1539 isolated unit tests stayed green while the shipped dispatch was broken. The suite reds today because the gap fixes ([`V20a`](./V20a-tools-load-resolution.md)–[`V20f`](./V20f-discovery-invalid-extension.md)) are not yet landed, so the full-surface drive through production still surfaces the silent breakages. It lands as a dedicated runner excluded from the default `npm test` (sibling to `npm run test:live` / the `H9a` acceptance runner) and fails loudly when its live precondition is unmet (per *No silent test skipping*).

**Tests.**
- `Convention:` (production-path conformance — already-fixed surface) driving through the production composition, the behaviours the campaign fixed hold: method-call chains; `Ok`/`Err` + `match` + `?`; `Enum.Variant`; user `fn` calls incl. recursion; `@`...`` prose containing `//` / backslash; empty-template short-circuit → `Err(empty_template)`; object-pattern `{ field }` shorthand; the frontmatter/param/system load-time validations; and the structural parser rejections. This is the regression net that would have caught the wiring gaps. Reds today (suite not yet authored; drives the production path, not units).
- `Convention:` (production-path conformance — new gaps #1–#7) the same production drive exercises the [`V20a`](./V20a-tools-load-resolution.md)–[`V20f`](./V20f-discovery-invalid-extension.md) surfaces: `tools:` load-time resolution rejections; the type-layer diagnostics; the unimplemented lexer/parser rejections; runtime string-index not-indexable; pure-position nested `match`; and the `--loom` non-`.loom` `invalid-extension` code. Reds today because those fixes are unlanded.

**Deps.** `H8a`, `H8b`, `H9a`, `V19e`

**Ships when.** The opt-in conformance suite (added by this leaf, invoked by a dedicated runner excluded from the default `npm test`) exists, compiles, and — run with its live precondition satisfied — fails red for the intended reason: driving the full documented surface through the production composition surfaces the still-unlanded gap fixes rather than any harness/setup/credential throw; when the precondition is unmet the runner fails loudly naming it (never a silent skip).
