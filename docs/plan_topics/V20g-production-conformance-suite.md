# `V20g` — Production-path language-surface conformance suite

**Convention.** [`conventions.md`](./conventions.md) (phase categories — end-to-end harness; the [live-host acceptance pair exception](#horizontal-leaves--single-task)). Narrative spec references for the implementer: [`extension-bootstrap-and-per-loom.md`](../spec_topics/pi-integration-contract/extension-bootstrap-and-per-loom.md), [`expressions.md`](../spec_topics/expressions.md), [`runtime-value-model.md`](../spec_topics/runtime-value-model.md). Closes no new spec REQ-ID.

**Adds.** The **green standing conformance suite** that drives the full documented language surface through the production composition (`composeLoomFixture` / the shipped [`extensions/index.ts`](../../extensions/index.ts) via [`tests/live/harness.ts`](../../tests/live/harness.ts)), turning the [`V20g-T`](./V20g-T-production-conformance-suite.md) suite green once every gap fix has landed. It closes no new spec REQ-ID; it is the regression net that would have caught the wiring gaps — a production-dispatch acceptance layer over the behaviours the campaign fixed plus the [`V20a`](./V20a-tools-load-resolution.md)–[`V20f`](./V20f-discovery-invalid-extension.md) gaps. It depends on the gap-fix impl leaves it validates so it lands last. The runner stays excluded from the default `npm test`; [`extensions/index.ts`](../../extensions/index.ts) remains a thin shim.

**Tests.**
- `Convention:` (production-path conformance) the full-surface drive through the production composition passes green: method calls; `Ok`/`Err` + `match` + `?`; `Enum.Variant`; user `fn` recursion; `@`...`` prose with `//` / backslash; empty-template short-circuit; object-pattern shorthand; the load-time frontmatter/param/system validations; and the structural parser rejections — the now-fixed campaign surface.
- `Convention:` (production-path conformance — gaps #1–#7) `tools:` load-time resolution ([`V20a`](./V20a-tools-load-resolution.md)); the type-layer diagnostics ([`V20c`](./V20c-type-layer-diagnostics.md)) over the static-type substrate ([`V20b`](./V20b-static-type-inference.md)); the lexer/parser rejections ([`V20d`](./V20d-lexer-parser-diagnostics.md)); runtime string-index not-indexable ([`V20c`](./V20c-type-layer-diagnostics.md)); pure-position nested `match` ([`V20e`](./V20e-pure-async-unification.md)); and `--loom` non-`.loom` `invalid-extension` ([`V20f`](./V20f-discovery-invalid-extension.md)) all pass through the production drive.
- `Convention:` (No globals, statics, singletons; Specific exception types only; Sequential by default) any new `src/**` composition support passes the [`H2a`](./H2a-cross-cutting-gates.md) / [`H3a`](./H3a-di-seam-skeleton.md) gates and the `no-broad-catch` lint, and `extensions/index.ts` remains a thin shim.

**Deps.** `V20g-T`, `V20a`, `V20b`, `V20c`, `V20d`, `V20e`, `V20f`, `H8a`, `H8b`, `H9a`

**Ships when.** The opt-in conformance runner drives the full documented language surface through the production composition green — the campaign-fixed behaviours and the [`V20a`](./V20a-tools-load-resolution.md)–[`V20f`](./V20f-discovery-invalid-extension.md) gaps — while `npm test`, `npm run typecheck`, `npm run lint`, and the `src/**` architectural gates stay green and `extensions/index.ts` remains a thin delegating shim.
