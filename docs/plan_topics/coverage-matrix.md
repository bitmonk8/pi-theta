# Spec coverage matrix

Every executable spec REQ-ID is mapped here to the leaf(s) that close it. The matrix is the closed source of truth — at the V1.0 release gate, any executable REQ-ID without a mapping fails CI.

The matrix is empty: leaves have not yet been authored. Populate one row per REQ-ID as leaves are added per [`leaf-template.md`](./leaf-template.md). Use the REQ-ID prefix table in [`../spec_topics/governance.md`](../spec_topics/governance.md) to enumerate the REQ-IDs to cover.

| REQ-ID | Closing leaf(s) |
|---|---|
| _(none yet)_ | _(none yet)_ |
