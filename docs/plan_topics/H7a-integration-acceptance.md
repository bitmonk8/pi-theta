# `H7a` — Terminal integration-acceptance run (cross-slice end-to-end gate)

**Convention.** [`conventions.md`](./conventions.md) (phase categories — end-to-end harness).

**Adds.** A terminal integration-acceptance gate that runs a single representative multi-feature fixture `.loom` through the [`H4a`](./H4a-factory-shell-and-harness.md) end-to-end harness against the in-process Pi session double, driving the integrated pipeline — typed query → tool loop → code-tool invoke → schema lowering/validation → binder → cancellation — in a single run. It closes no new spec REQ-ID; it is a cross-slice integration-regression gate that exercises the composition the per-leaf gates verify only in isolation. Because the run executes against the session double, its fidelity is bounded by that double's contract ([`H4a`](./H4a-factory-shell-and-harness.md)); the gate targets the cross-slice composition the per-leaf gates cannot see, not host-level realism (the real-host backstop remains [`V18d`](./V18d-version-bump-acceptance.md)'s version-bump runtime-evidence gate). The committed golden transcript, golden diagnostics list, and permitted-code list are *derived*, not snapshotted from an unreviewed pipeline run: each golden turn and each `loom/...` code that maps onto a behaviour already gated by a leaf in **Deps** (`V5d`, `V8a`, `V11a`, `V11b`, `V11i`, `V11c`, `V11d`, `V11e`, `V11f`, `V13a`, `V13b`, `V13c`, `V13d`, `V13f`, `V14a`, `V14b`, `V16a`, `V17a`) is sourced from that leaf's already-established expected output — the permitted-code list from each Deps slice's full set of emittable codes, the golden diagnostics list and transcript from the subset that path actually emits — and the composed turn ordering no single per-leaf gate covers is human-reviewed against [`docs/spec.md`](../spec.md) and the cited spec topics at first commit. The permitted-code list is kept current under the same per-Deps-slice provenance obligation.

**Tests.**
- `Convention:` (phase categories — end-to-end harness) running the representative multi-feature fixture `.loom` through the `H4a` harness drives the integrated pipeline (typed query, tool loop, code-tool invoke, schema lowering/validation, binder, cancellation) and the run's appended turns match — in order, count, and per-turn content (each turn's text, tool arguments, and rendered envelope) — the committed golden transcript checked in alongside the fixture `.loom` (the enumerated, ordered turn sequence the composition produces).
- `Convention:` (phase categories — end-to-end harness) the same run emits exactly the `loom-system-note` diagnostics enumerated in the committed golden diagnostics list checked in alongside the fixture `.loom` — the set of `loom/...` codes the integrated fixture path emits, drawn only from codes the slices in **Deps** emit (no code that no slice in the composition emits) — each asserted against the diagnostics-registry *Message* strings per [`conventions.md`](./conventions.md) *Diagnostic message anchors*.
- `Convention:` (phase categories — end-to-end harness) a **committed permitted-code list** is checked in alongside the fixture `.loom`, next to the committed golden transcript and golden diagnostics list: it enumerates the union of `loom/...` codes the slices in **Deps** (`H4a`, `H4b`, `V5d`, `V8a`, `V11a`, `V11b`, `V11i`, `V11c`, `V11d`, `V11e`, `V11f`, `V13a`, `V13b`, `V13c`, `V13d`, `V13f`, `V14a`, `V14b`, `V16a`, `V17a`) *can* emit — deliberately broader than the golden diagnostics list (which enumerates the codes the deterministic fixture path actually emits) and a **superset** of it, so every code the deterministic run emits is permitted and the in-process gate and the real-host smoke cannot disagree on a code the fixture path actually emits. It is the single committed, reviewed reference set the [`H4a`](./H4a-factory-shell-and-harness.md) manual real-host smoke run's pass criterion (e) and the [`H6a`](./H6a-live-corpus-activation.md) release-gate evidence record check the live run's emitted codes against, replacing a per-run by-hand reconstruction of the union.
- `Convention:` (phase categories — end-to-end harness) the integrated run drives a binder-retry-class breach (ceiling #3, [`V11f`](./V11f-binder-retry-taxonomy.md)) co-occurring with a runtime-class ceiling (the `tool_loop.max_rounds` round boundary, ceiling #2, [`V13c`](./V13c-query-tool-loop.md)) through the live pipeline and asserts that the live sites' consultation of the [`V16a`](./V16a-ceiling-order-masked.md) arbitration seam surfaces exactly one ceiling in `CIO-5` order (#3 ahead of the runtime-class ceiling, no interleave) with `masked` enumerating the suppressed sibling — closing the live-site integration gap that `V16a`'s synthesised-candidate suite leaves open.

**Deps.** `H4a`, `H4b`, `V5d`, `V8a`, `V11a`, `V11b`, `V11i`, `V11c`, `V11d`, `V11e`, `V11f`, `V13a`, `V13b`, `V13c`, `V13d`, `V13f`, `V14a`, `V14b`, `V16a`, `V17a`

**Ships when.** `npm test` runs the terminal integration-acceptance fixture `.loom` end-to-end through the `H4a` harness against the in-process Pi session double, green: the integrated typed-query → tool-loop → code-tool-invoke → schema-lowering/validation → binder → cancellation pipeline produces appended turns matching the committed golden transcript (order, count, and per-turn content) and emits exactly the `loom-system-note` codes enumerated in the committed golden diagnostics list checked in alongside the fixture `.loom`, and a co-occurring ceiling #3 / runtime-class ceiling #2 breach driven through the live pipeline surfaces exactly one ceiling in `CIO-5` order (#3 ahead of the runtime-class ceiling) with `masked` enumerating the suppressed sibling.
