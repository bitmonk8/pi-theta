# `V20c` — Type-layer diagnostics wiring and runtime string-index correction

**Convention.** [`conventions.md`](./conventions.md) (phase categories — production-wiring). Narrative spec references for the implementer: [`expressions.md`](../spec_topics/expressions.md) (Truthiness, indexing, stdlib), [`control-flow.md`](../spec_topics/control-flow.md) (`for`/`in`), [`functions.md`](../spec_topics/functions.md) (return LUB), [`type-system.md`](../spec_topics/type-system.md), [`runtime-value-model.md`](../spec_topics/runtime-value-model.md). Closes no new spec REQ-ID.

**Adds.** **Bucket A (checkers now feedable)** plus **Bucket C (implemented wrongly):** wires the existing type-phase checkers to the [`V20b`](./V20b-static-type-inference.md) per-expression static-type lookup so they run in production, and corrects [`evaluateIndexAccess`](../../src/runtime/runtime-panics.ts) so `s[0]` surfaces the not-indexable error instead of returning a character. It closes no new spec REQ-ID; each diagnostic is an integration realisation of a code-keyed area owned on its original leaf (`cka-4` non-boolean-condition on [`V3a`](./V3a-expression-evaluator.md); `cka-5` non-array-iterand on [`V3c`](./V3c-control-flow.md); the `?`/LUB/narrowing/match/indexing/stdlib type codes on [`V4a`](./V4a-match-result.md) / [`V3a`](./V3a-expression-evaluator.md) / [`V3d`](./V3d-functions-and-return.md) / [`V2b`](./V2b-type-compat-engine.md) / [`V3g`](./V3g-expression-stdlib-array.md) / [`V3h`](./V3h-expression-stdlib-object.md); the runtime not-indexable panic on [`V4b`](./V4b-runtime-panics.md)). The wiring consumes the [`V20b`](./V20b-static-type-inference.md) substrate seam; the checker wiring and runtime fix stay constructor-injected with no module-level mutable state.

**Tests.**
- `loom/parse/non-boolean-condition`: `if 1` / `while 1` / non-`boolean` ternary or `&&`/`||` operand fires in production (owned on [`V3a`](./V3a-expression-evaluator.md)).
- `loom/parse/non-array-iterand`: `for x in <non-array>` fires in production (owned on [`V3c`](./V3c-control-flow.md)).
- `loom/parse/question-on-non-result` / `loom/parse/question-outside-result-fn`: `?` misuse fires (owned on [`V4a`](./V4a-match-result.md)).
- `loom/parse/array-no-common-type`, `loom/parse/return-no-common-type`, `loom/parse/integer-narrowing`, `loom/parse/match-arm-type-mismatch`: the type-LUB / narrowing / match-arm rejects fire (owned on [`V3a`](./V3a-expression-evaluator.md) / [`V3d`](./V3d-functions-and-return.md) / [`V2b`](./V2b-type-compat-engine.md) / [`V4a`](./V4a-match-result.md)).
- `loom/parse/non-indexable-receiver` (incl. `string`), `loom/parse/non-string-object-index`, `loom/parse/non-string-array-join`: the indexing / stdlib type rejects fire (owned on [`V3a`](./V3a-expression-evaluator.md) / [`V3h`](./V3h-expression-stdlib-object.md) / [`V3g`](./V3g-expression-stdlib-array.md)).
- `Convention:` (runtime string-index correction) `s[0]` at runtime surfaces the not-indexable error rather than a character (owned on [`V4b`](./V4b-runtime-panics.md)).
- `Convention:` (No globals, statics, singletons; Specific exception types only; Sequential by default) the new `src/**` wiring passes the [`H2a`](./H2a-cross-cutting-gates.md) / [`H3a`](./H3a-di-seam-skeleton.md) gates and the `no-broad-catch` lint.

**Deps.** `V20c-T`, `V20b`, `V3a`, `V3c`, `V4a`, `V4b`, `V13b`, `V19c`

**Ships when.** `npm test` proves the type-phase checkers fire in production against the [`V20b`](./V20b-static-type-inference.md) static-type lookup (non-boolean condition, non-array iterand, `?` misuse, array/return LUB, integer narrowing, match-arm mismatch, non-indexable/object-index/array-join) and that `s[0]` surfaces the not-indexable runtime error, while `npm run typecheck` / `npm run lint` / the `src/**` architectural gates stay green.
