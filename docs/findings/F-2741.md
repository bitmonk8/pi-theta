---
id: F-2741
corpus: plan
fix_surface: [plan]
status: triaged
shape: single
state: shaped
tier: low
lens: ["plan-lens-ordering"]
category: ["ordering"]
factors: { blast_radius: narrow, centrality: supporting, core_premise_blocker: no }
location:
  - { path: "docs/plan_topics/V17b-forwarding-listener-throw-trap.md", anchor: "Deps field" }
  - { path: "docs/plan_topics/V17b-T-forwarding-listener-throw-trap.md", anchor: "Deps field" }
title: "V17b/V17b-T assert against `InvokeInfraError{cause:'internal_error'}` and `loom/runtime/internal-error` but declare no Deps reaching their origin leaves"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Finding

`V17b` (forwarding-listener throw-trap) and its paired tests task `V17b-T` both assert that an injected throw from a forwarding listener "routes through the runtime-defect surface on the `loom/runtime/internal-error` channel (and, at an `invoke` parent, surfaces via the `cause: "internal_error"` arm of `InvokeInfraError`)". Both leaves declare `Deps. V17b-T, V17a, H4a` (impl) and `Deps. V17a, H4a` (tests). Neither construct the assertion exercises is reachable through that dependency closure: `V17a`'s deps are `V8a, V4d, V9a, H4a, H4b`, and walking outward (`V8a → H3a, V8d`; `V4d → V5d`; `V9a → H4a, V7d → V7a`) reaches neither the leaf that registers the `loom/runtime/internal-error` channel nor the leaf that establishes the `InvokeInfraError{cause:"internal_error"}` arm.

The `loom/runtime/internal-error` channel is introduced at `V4b` (Adds: "the `internal-error` surface for unexpected throws"; Tests: "An unexpected thrown value surfaces as `loom/runtime/internal-error`"). The `InvokeInfraError { cause: "internal_error" }` arm — routing a dispatch-site failure through that same runtime-defect surface — is established at `V9e` (active-invocation-registry §"Dispatch-site setup wrap failure path": "each route through the runtime-defect surface (`loom/runtime/internal-error` / `InvokeInfraError { cause: "internal_error" }`)"). `V17b` mirrors that exact pairing but lists no edge to either leaf.

Per `conventions.md`, the dependency DAG — not phase numbering — is the contract that governs build order: "Slices are roughly ordered by their dependency DAG … Reorder freely as long as the deps DAG is respected." The default linear order happens to place `V4`/`V9` before `V17`, masking the gap, but a sharded or reordered build that respects only the declared DAG may float `V17b`/`V17b-T` ahead of `V4b`/`V9e`. At that point the test asserting against the `InvokeInfraError{internal_error}` arm and the `loom/runtime/internal-error` channel has no defined symbol to compile or run against.

## Consequence

**Severity:** correctness

Under the default linear order the build succeeds, so nothing reds out today. But the declared dependency DAG — the stated contract for any valid reordering — omits the edges to the leaves that introduce the `loom/runtime/internal-error` channel (`V4b`) and the `InvokeInfraError{cause:"internal_error"}` arm (`V9e`). A sharded or reordered build that respects only the DAG can schedule `V17b`/`V17b-T` before those constructs exist, at which point the throw-trap assertion cannot compile or run.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** cb8737a — pi-loom plan: resolve "Systemic leaf over-bundling across the plan corpus" (2026-06-12, Thomas Andersen)
**History:** `V17b`/`V17b-T` were created at cb8737a when the throw-trap was peeled off `V17a` to resolve the over-bundling finding. `git show cb8737a:docs/plan_topics/V17b-forwarding-listener-throw-trap.md` shows the leaf shipped at that first commit with `Deps. V17b-T, V17a, H4a` and the assertion already citing `loom/runtime/internal-error` and the `InvokeInfraError{cause:"internal_error"}` arm; a pickaxe (`git log -S 'V9e'`) over the file's history returns no commit, so the dependency on the origin leaves was never declared. The defect has been present since the leaf's inception.

## Solution Space

**Shape:** single

### Recommendation

In `docs/plan_topics/V17b-forwarding-listener-throw-trap.md`, add `V9e` and `V4b` to the **Deps** field so the closure reaches the constructs the throw-trap assertion exercises: `V9e` supplies the `InvokeInfraError { cause: "internal_error" }` arm and `V4b` supplies the `loom/runtime/internal-error` channel. The current `**Deps.** \`V17b-T\`, \`V17a\`, \`H4a\`` becomes `**Deps.** \`V17b-T\`, \`V17a\`, \`V9e\`, \`V4b\`, \`H4a\``.

Make the matching edit in `docs/plan_topics/V17b-T-forwarding-listener-throw-trap.md`: its `**Deps.** \`V17a\`, \`H4a\`` becomes `**Deps.** \`V17a\`, \`V9e\`, \`V4b\`, \`H4a\``, since the tests task is the leaf whose assertion physically references the channel and the `InvokeInfraError` arm.

Both `V9e` and `V4b` already exist in the plan; no new leaf IDs are introduced. Edge case for the fixer: `V9e`'s own declared closure (`V9b, V8d`) does not reach `V4b` either, so naming `V4b` directly on `V17b`/`V17b-T` is required — depending on `V9e` alone does not make the channel-registration leaf reachable.

## Relationships

- "V18c reads V9a constants but declares only V18a/V18b" — same-cluster (identical defect class — a leaf exercises a construct owned by another leaf while reaching it only transitively/by phase number rather than via a declared direct dep; resolves independently)
- "V17a/V17b reference invoke/query/tool-call constructs that land after V17 (implicit double-backed reliance)" — same-cluster (adjacent V17 ordering-lens finding on the same leaf pair; that one annotates test prose, this one declares deps — resolves independently)
