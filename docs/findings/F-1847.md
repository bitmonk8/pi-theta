---
id: F-1847
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/diagnostics/placeholder-rendering-b.md, anchor: "" }
lens: [spec-lens-testability]
tier: low
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [testability]
title: "§8 build-time enforcement meta-rule states no product behaviour"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---
## Finding

The "detected at build time by enumerating registry rows … and asserting that no test asserts strict equality" requirement is about the test suite's own assertions, not a runtime observable; it has no product Then-clause.

**Suggested fix:** Move the build-time prohibition to the test-plan/CI docs; keep only the product behaviour (renderers MUST produce byte-identical surround; tests MUST use anchored partial matches).
