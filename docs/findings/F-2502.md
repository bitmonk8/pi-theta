---
id: F-2502
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/errors-and-results/queryerror-variants.md, anchor: "`### Notes` (ERR-14 HTML comment); Invoke-variants intro prose (\"now self-referential\"); `ToolLoopExhaustedError` schema comment + `### Notes` prose (`last_tool_name` rationale); ERR-15 discriminator-type-openness callout (orphan anchor)" }
  - { path: docs/spec_topics/errors-and-results/error-model.md, anchor: "ERR-13 \"No rollback\" (reading-guide sentence)" }
lens: [spec-lens-cruft]
tier: low
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [cruft]
title: "Delete the ERR-14 relocation HTML comment"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The `### Notes` section of `docs/spec_topics/errors-and-results/queryerror-variants.md` carries an HTML comment recording that the ERR-14 `ValidationIssue`-ordering block was relocated to the Query-time variants subsection. It is invisible in rendered output and documents a completed move.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** 6750c81 ("spec: consolidate QueryError variants…"), 235f47a ("…resolve \"?-and-panics-do-not-unwind-prior-side-effects\""), 2e0dad2 ("…resolve \"HTML anchor dual-anchor authoring for v1- sites\""), f5e89f4 ("docs: enforce 100KB hard / 30KB recommended size cap on spec set"), c3ea1d1 ("…resolve \"ERR-14 ValidationIssue ordering buried in Notes\"")
**History:** The corpus is git-tracked; the five residues accumulated across separate edits rather than entering together. `git log -S` traces each defect token: the "now self-referential within this section" wording first appears with the `QueryError`-consolidation commit 6750c81 (2026-05-05); the ERR-13 "This note is complementary to…" sentence with 235f47a (2026-05-04); the duplicated `last_tool_name` forward-compat rationale predates the split (both copies present, last re-worded by the V1→loom-1.0 rename a6d82db); the `loom-1-0-seam-discriminator-type-openness` alias was born of the deliberate dual-anchor authoring in 2e0dad2 (2026-05-29), whose sibling `v1-*` anchor took the inbound links and left the `loom-1-0-*` form unreferenced. The 100KB size-cap split f5e89f4 (2026-06-04) then carved these pages out of the monolithic spec, collecting the residues into the errors-and-results cluster where they now read as accumulated decision-log noise; the ERR-14 relocation HTML comment was added afterward by c3ea1d1 (2026-06-04) as the leftover marker of that relocation. No single commit introduced the cluster — it is the interaction of these edits with the topic-file split.

## Solution Space

**Shape:** single

### Recommendation

Delete the comment `<!-- ERR-14 (`ValidationIssue` ordering) relocated to ### Query-time variants, co-located with the ValidationError schema, for discoverability. Anchors carried verbatim. -->` outright. The relocated ERR-14 block (with its `#err-14` / `#validation-issue-ordering` anchors) stands on its own; no replacement text is needed.

## Consequence

**Severity:** cosmetic

No implementer or runtime observer is affected: the HTML comment does not render, the temporal/reading-guide sentences and duplicated rationale constrain nothing, and the orphaned anchor has no inbound links. The cost is source-reading friction and accumulating decision-log noise that grows more confusing as the surrounding structure drifts from the moment of each edit.

## Relationships

- F-2503 "Strip the "now self-referential" temporal residue" — co-resolve
- F-2504 "Delete the ERR-13 reading-guide meta-sentence" — co-resolve
- F-2505 "Deduplicate the `last_tool_name` forward-compatibility rationale" — co-resolve
- F-2506 "Remove the orphaned `loom-1-0-seam-discriminator-type-openness` alias anchor" — co-resolve
