---
id: F-2434
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/binder/binder-bypass-and-envelope.md, anchor: "seam-obligation preamble (\"Three loom 1.0 carriers …\")" }
  - { path: docs/spec_topics/binder/defaulting-system-note-echo.md, anchor: "page H1 title" }
lens: [spec-lens-naming]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [naming]
title: "Define or retire the `carriers` coinage"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`binder-bypass-and-envelope.md`'s "Three loom 1.0 carriers preserve the post-loom 1.0 migration" coins "carrier" for the BNDR-1/2/3 seam obligations without defining it, and the term already carries an unrelated glossary meaning (`query-terminating-arm carrier`).

**Spec edits:** Replace "carriers" with established vocabulary already used on the page — e.g. "Three loom 1.0 normative obligations (BNDR-1, BNDR-2, BNDR-3) preserve the post-loom 1.0 migration; each is pinned individually below." Cite the three REQ-IDs directly so the referent is explicit and no new term is introduced.

**Relationship:** same-cluster — resolves at `binder-bypass-and-envelope.md` independently of split finding 2.

## Issue introduction

**Verdict:** single-commit
**Introducing commits:** 7a6cf96 — pi-loom spec: resolve "V1 seam-preservation MUSTs hidden inside the deferred-features narrative page" (2026-05-07, Thomas Andersen); f5e89f4 — docs: enforce 100KB hard / 30KB recommended size cap on spec set (2026-06-04, Thomas Andersen)
**History:** The "carrier" coinage entered at 7a6cf96 as "V1 carriers" when the deferred seam-preservation MUSTs were lifted into their own normative obligations; the V1→loom 1.0 prose rename (a6d82db, 2026-05-29) rephrased it to "loom 1.0 carriers" without defining it, and f5e89f4 carried it verbatim into the new `binder/` subtree. The `# Defaulting system note echo` title was authored fresh at f5e89f4, the commit that split the monolithic binder spec into the `binder/` cluster under the size-cap enforcement and created `defaulting-system-note-echo.md`. The two defects have distinct single-commit origins and do not interact.

## Solution Space

**Shape:** single

### Recommendation

Replace "carriers" with established vocabulary already used on the page — e.g. "Three loom 1.0 normative obligations (BNDR-1, BNDR-2, BNDR-3) preserve the post-loom 1.0 migration; each is pinned individually below." Cite the three REQ-IDs directly so the referent is explicit and no new term is introduced.

**Relationship:** same-cluster — resolves at `binder-bypass-and-envelope.md` independently of split finding 2.

## Consequence

**Severity:** cosmetic

A reader hits an undefined "carrier" coinage and a run-on page title; both are readability snags only. No implementer produces divergent behaviour and no runtime observer is affected.

## Relationships

- F-2435 "Rephrase the run-on defaulting-page title" — co-resolve
