---
id: F-1622
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/diagnostics.md, anchor: "hub intro paragraph" }
lens: [spec-lens-clarity]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [clarity]
title: "Diagnostics hub intro claims \"all fire after the extension is already live\" while `extension-bootstrap-failed` fires at factory time"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The `diagnostics.md` hub intro paragraph asserts a universal precondition over the shard's diagnostics: "the diagnostics defined here all fire after the extension is already live (during scan, watcher reload, or slash-command execution)." This is false for at least two rows in the same shard: `loom/load/extension-bootstrap-failed` fires at factory-time / `session_start`-time SDK registration, and `loom/load/host-incompatible` fires from the factory-time capability probe — both before the extension is live. The intro forward-links `extension-bootstrap-failed` in the immediately preceding clause, so the contradiction is internal to the paragraph. The clause is the only place that states when the shard's diagnostics may be expected to fire, so a reader who trusts it mis-plans the delivery posture (assuming the renderer is registered) for the two bootstrap-phase rows.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** `2d2fc8e` ("pi-loom spec: resolve 'Pi SDK capability calls have no failure contract'", 2026-05-06) and `6829a79` ("pi-loom spec: resolve 'Pi-SDK hidden-assumption cluster …'", 2026-06-06)
**History:** The clause "all fire after the extension is already live (during scan, watcher reload, or slash-command execution)" entered `diagnostics.md` in `ac8c3af` (2026-05-04, "LoadExtensionsResult.errors is the wrong delivery channel for loom diagnostics") as part of a rewrite of the hub intro. At that revision the universal-quantifier reading was true: the diagnostics shard then defined no factory-time bootstrap codes. Two days later `2d2fc8e` added `loom/load/extension-bootstrap-failed` as a new row in the shard's load registry, with a *Trigger* describing factory-time SDK registration calls that threw — making the universal-quantifier intro false against the shard's own contents, without editing the intro. `6829a79` (2026-06-06) then revised the intro paragraph again to forward-link `[\`loom/load/extension-bootstrap-failed\`](./diagnostics/code-registry-load.md)` as the loom-emitted replacement for `LoadExtensionsResult.errors`, placing the forward-link in the same paragraph as — and immediately before — the surviving universal-quantifier clause. The placement made the contradiction acute (the linked row's trigger is factory-time bootstrap; the next clause says all rows in this shard fire after bootstrap), but the introducing condition is the May-6 row addition; the June-6 edit only sharpened the visible contradiction. `bf85092` (2026-06-06) further extended the row's trigger to cover `session_start`-time `pi.getCommands()` failures, broadening the contradiction marginally but not introducing it.

## Solution approach

Rewrite the universal-quantifier clause in the `diagnostics.md` hub intro so it partitions the shard's codes by emission phase instead of asserting one precondition over all of them, naming the bootstrap-phase exceptions `loom/load/extension-bootstrap-failed` and `loom/load/host-incompatible` explicitly rather than by code namespace. The rewritten clause should reference the fallback-chain routing those two rows mandate as the reason they emit before the renderer is live.

## Solution constraints

- Out of scope: the row trigger text and fallback-routing rationale in `diagnostics/code-registry-load.md` — the intro references that rationale, it does not relocate or own it.

## Relationships

None
