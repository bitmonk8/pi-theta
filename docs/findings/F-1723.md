---
id: F-1723
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/hard-ceilings/ceiling-invariants-and-audit.md, anchor: "*loom 1.0.0 rejected-candidate record (closed)* paragraph" }
lens: [spec-lens-cruft]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [cruft]
title: "`loom 1.0.0 rejected-candidate record (closed)` paragraph is decision-log cruft on a normative page"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The paragraph beginning `***loom 1.0.0 rejected-candidate record (closed).***` on `docs/spec_topics/hard-ceilings/ceiling-invariants-and-audit.md` records three candidate ceilings (regex backtracking, JSON Schema width pathologies, pattern-match recursion depth) considered and rejected during loom 1.0.0 authoring. It is self-labelled `(closed)` and instructs future spec-editors not to append to it, imposing no MUST/SHOULD and constraining no implementer, parser, runtime, or editor behaviour. Every fact it carries is already normative at the pages it forward-links to: the no-regex stance at [Expressions — Built-in methods and properties](../expressions.md#built-in-methods-and-properties), the JSON-Schema-width exclusions under *Explicitly not supported* in the [Schema Subset](../schema-subset.md) intro, and the `NOCEIL-3` / `NOCEIL-4` fall-through three paragraphs above in the same file. It is decision-log carry-over on an already-dense normative page.

## Issue introduction

**Verdict:** single-commit introduction
**Introducing commits:** `2ff03c2` (2026-05-10) — *pi-loom spec: resolve "Source-language stability bullet leaves equivalence undefined at the aggregator and silently contradicts the open-set ceiling clause"*
**History:** `git log -S "candidate ceiling cases considered and rejected"` and `git log -S "V1.0 rejected-candidate"` both name `2ff03c2` as the first commit to introduce the paragraph string. That commit added the entire *Audit methodology* / *Four-axis closure list* / *Routing obligation* / *V1.0 rejected-candidate record* / *Release gate* block to `docs/spec_topics/hard-ceilings.md` in one edit (`git show 2ff03c2 -- docs/spec_topics/hard-ceilings.md` shows the paragraph added as one of the new `***...***` blocks in a `+23 / -2` diff). One later commit moved it without re-authoring its content: `f5e89f4` (2026-06-04, *docs: enforce 100KB hard / 30KB recommended size cap on spec set*) split `hard-ceilings.md` into the index page plus the two children `ceilings-3-and-4.md` and `ceiling-invariants-and-audit.md`, relocated the paragraph wholesale into `hard-ceilings/ceiling-invariants-and-audit.md`, and performed the `V1.0` → `loom 1.0.0` rename pass. No subsequent commit edits the paragraph body. The defect is therefore a property of the introducing block as authored at `2ff03c2`, not of any later evolution: the "closed worked examples" framing and the explicit "do not append" instruction were present in the original prose.

## Solution approach

Delete the `***loom 1.0.0 rejected-candidate record (closed).***` paragraph from `docs/spec_topics/hard-ceilings/ceiling-invariants-and-audit.md`. No anchor, `<a id>`, or cross-reference targets the paragraph, so no link-repointing is required.

## Solution constraints

- Out of scope: the surrounding *Routing obligation for future ceilings* and *Release gate* paragraphs (owned by F-1734).

## Relationships

- F-1734 "Routing-obligation governance detail / inline GOV-30 expansion mixed into the ceiling page" — same-cluster (touches the surrounding *Routing obligation for future ceilings* / *Release gate* prose in the same section; resolves independently — moving the per-operation editor checklist to GOV-30 neither requires nor obviates deleting the rejected-candidate paragraph)
- F-1668 "Page title \"Ceiling invariants and audit\" mismatches content" — same-cluster (same page; renaming the page is orthogonal to deleting one paragraph from its body)
- F-1646 "NOCEIL-1 parse-time `timeout:` rejection asserted without backing/site enumeration" — same-cluster (same page; resolves independently)
