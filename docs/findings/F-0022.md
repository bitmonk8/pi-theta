---
id: F-0022
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9b-registration-drain-state.md, anchor: "Tests (IMPL bullet) + Ships-when" }
  - { path: docs/plan_topics/V9b-T-registration-drain-state.md, anchor: "Tests (IMPL bullet)" }
  - { path: docs/plan_topics/coverage-matrix.md, anchor: "IMPL row (line 103)" }
lens: [plan-lens-ordering, spec-lens-assumptions]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
shape: single
state: reduced
category: [assumptions, ordering]
title: "V9b's hot-reload cache-eviction obligation is sequenced before the parse cache and `.warp` import edges it operates on exist"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Problem

`V9b`'s third Tests bullet and its Ships-when require the in-process hot-reload re-parse path to drop the per-pass cache entry for the changed file and every transitive `.warp` importer as part of the `LoomRegistry` swap. That obligation operates on the static-resolution parse cache owned by `V15a` and the transitive `.warp` importer relation computable only over the import-edge graph `V15c` constructs. `V9b`'s `Deps` (`V9b-T, V9a, V10a, V8e, V6a`) list neither `V15a` nor `V15c`, and `V9b` is force-placed early via the `V9b → V11a → V9c`/`V9i`/`V9j` seam, so it reaches its exit gate before the parse cache or any `.warp` import edges exist — a one-way ordering inversion the coverage-matrix IMPL §Runtime *Static-resolution load pass* row encodes (cell `V15a, V9b, V7a`) without reconciling.

## Issue introduction

**Verdict:** single-commit

**Introducing commit:** `249cec520b8b6a4cf24f7611a8f165075509f4e5` — "pi-loom plan: resolve \"implementation-notes.md (IMPL) unmapped in coverage matrix\"" (2026-06-11)

**History:** `V15a` has owned the static-resolution parse cache since the initial plan build (`c6a664e`, 2026-06-10). The cross-leaf inversion was introduced by `249cec5` while closing the "implementation-notes.md (IMPL) unmapped in coverage matrix" finding: that commit split the IMPL §Static-resolution load pass coverage across `V15a` (parse-cache walk) and `V9b` (hot-reload re-walk), adding the eviction Tests bullet to `V9b`/`V9b-T` (`git show 249cec5` shows the `+ [implementation-notes.md — Static-resolution load pass] … drops the per-pass cache entry … every transitive .warp importer` line) and the matching coverage row, but did not add `V15a`/`V15c` to `V9b`'s Deps nor reconcile the sequencing against `V9b`'s early `V9b → V11a` placement. `V9b`'s Deps at that commit were `V9b-T, V9a, V10a, V8b`; subsequent commits adjusted the seam set (`V8e`, `V6a`) without ever closing the parse-cache / import-edge gap.

## Solution approach

Move the hot-reload cache-eviction obligation off `V9b`/`V9b-T` into a new vertical leaf pair (tests + implementation) whose `Deps` include `V9b`, `V15a`, and `V15c`, so it sequences after the static-resolution parse cache (`V15a`) and the `.warp` import edges (`V15c`) exist; link the new leaf under the V15 slice in `docs/plan.md`. Strike the `implementation-notes.md — Static-resolution load pass` IMPL Tests bullet from `V9b` and `V9b-T` and the matching cache-eviction clause from `V9b`'s Ships-when. Re-point the in-process hot-reload re-walk token in the coverage-matrix IMPL §Runtime *Static-resolution load pass* row from `V9b` to the new leaf (cell `V15a, V9b, V7a` → `V15a, <new>, V7a`), leaving `V9b`'s registry-swap rows and `V15a`'s parse-cache walk in place.

## Solution constraints

- Do not add `V15a`/`V15c` directly to `V9b`'s `Deps`: that pushes `V9b` (and hence `V11a` and `V9c`/`V9i`/`V9j`) past the whole V15 slice and breaks the V9/V11 interleave.
- Allocate the new leaf ID per the plan `H`/`M`/`V` leaf-ID scheme (next free letter in the target phase; never reuse a retired ID).
- The spec is read-only for this fix (`fix_surface` is plan-only).

## Relationships

- F-0026 "V9b filename slug `registration-drain-state` contradicts the leaf's retitled scope and disclaims its own ownership" — same-cluster (both touch the `V9b` leaf; resolve independently)
- F-0151 "`registration-steps.md` has normative MUSTs but no coverage-matrix row" — same-cluster (both touch `V9b`'s coverage-matrix mapping)
- F-0055 "H5a transitive-completeness arm's IMPL-row back-reference exclusion is not mechanically determinable" — decision-overlap (re-pointing the IMPL row's hot-reload token changes which tokens in that row are forward-closure vs back-reference)
