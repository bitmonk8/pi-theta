---
id: F-0379
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan.md, anchor: "§V9 — Extension host integration interleave note, and §V11 — Binder interleave note" }
lens: [plan-lens-ordering]
tier: medium
factors:
  blast_radius: narrow
  centrality: core
  core_premise_blocker: no
status: reduced
category: [ordering]
title: "V9/V11 interleave notes claim `V9c` is downstream of `V11a`, but `V9c`'s Deps omits `V11a`"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

Both interleave notes in `docs/plan.md` (the §V9 — Extension host integration note and the §V11 — Binder note) enumerate the seam as `V9b → V11a → V9c`/`V9i`/`V9j`, naming `V9c` as a `V11a`-downstream leaf. That contradicts the per-leaf Deps: `V9c`'s Deps is `{V9c-T, V9a, V8a, V17a}` and `V9c-T` likewise omits `V11a`, so the authoritative pickability signal says `V9c` does not wait on `V11a`. The leaf graph is the correct side — `V9c` is prompt-mode conversation drive, which runs in the host's existing user session and exercises no binder-model machinery, unlike `V9i` and `V9j` whose Deps genuinely carry `V11a`. A reader trusting the note will idle `V9c` until `V11a` lands (or "fix" the discrepancy by adding a spurious `V11a` edge), while a reader trusting Deps picks it up early.

## Issue introduction

**Verdict:** introduced-by-specific-commit
**Introducing commit:** `b9b427d` (2026-06-10) — "pi-loom plan: resolve \"slice-ordering narrative overpromises DAG order\""
**History:** The plan corpus is git-tracked. `docs/plan_topics/V9c-conversation-drive.md` was created at `c6a664e` (2026-06-10) with Deps `V9c-T, V9a, V9j, V8a`; `git log --follow -p` over the leaf shows its Deps never carried `V11a` (the only later Deps edits added `V17a` at `ecedd5f` and removed `V9j` at `0c3a2a2`). The two interleave notes were first authored in `b9b427d` (2026-06-10): `git log -S'V9c`/`V9i`/`V9j' -- docs/plan.md` returns `b9b427d` as the sole introducing commit, and `git show b9b427d` shows both notes entering with the `V9b → V11a → V9c`/`V9i`/`V9j` enumeration already present. The note has therefore asserted `V9c`-downstream-of-`V11a` since its inception while `V9c`'s Deps never carried that edge — the self-contradiction is present since the note was introduced, not the product of a later drift. `V9i` and `V9j` independently carry `V11a` in their Deps, so the over-enumeration is specific to `V9c`.

## Solution approach

Strike `V9c` from the `V11a`-downstream enumeration in both interleave notes in `docs/plan.md`, leaving `V9i`/`V9j` so the seam reads `V9b → V11a → V9i`/`V9j` in each note. Reconcile the contradiction toward the leaf graph, not the narrative.

## Solution constraints

- Out of scope: do not add a `V11a` edge to `V9c`'s or `V9c-T`'s Deps — they correctly omit it.
- The spec is read-only for this fix.

## Relationships

None
