---
id: F-1509
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/runtime-event-channel.md, anchor: "**Runtime event channel** paragraph (the always-log set definition)" }
lens: [spec-lens-clarity]
tier: high
factors:
  blast_radius: broad
  centrality: core
  core_premise_blocker: no
status: reduced
category: [clarity]
title: "Always-log set's `QueryError`-`kind` membership is stated unqualified"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The opening definition of the always-log set in the **Runtime event channel** paragraph reads "The **always-log set** — `QueryError` `kind` values, binder failure causes, and `loom/runtime/*` panic codes — emit …". The phrase `` `QueryError` `kind` values `` is unqualified and reads as all nine `QueryError` kinds, but the membership is actually five: the Group A enumeration lists only `transport`, `model_tool`, `code_tool`, `tool_loop_exhausted`, and `invoke_infra`, and a later paragraph explicitly excludes `validation`, `context_overflow`, `cancelled`, and `invoke_callee`. A reader who treats the definition sentence as authoritative will wire the four excluded kinds into the always-log emission path, producing `loom-system-note` events the spec forbids — including at discard sites, where those four kinds must emit nothing.

## Issue introduction

**Verdict:** single-introducing-commit
**Introducing commit:** `038dab3af14036e8f40d1787ae1a4c5120bd3ab4` (2026-06-05) — "pi-loom spec: resolve \"always-log set definition narrower than membership\""
**History:** The corpus is git-tracked. `git log -G` on the always-log opening shows the sentence read "A subset of `QueryError` failures — the **always-log set** — emit …" before `038dab3`. That commit, resolving the sibling finding that the definition was *narrower* than its true membership (it named only `QueryError`, omitting binder failures and `loom/runtime/*` panics), rewrote the opening to "The **always-log set** — `QueryError` `kind` values, binder failure causes, and `loom/runtime/*` panic codes — emit …". In broadening the definition to cover all three member categories, the edit dropped the "subset" qualifier on the `QueryError` component, converting a correctly-scoped phrase into the unqualified "`QueryError` `kind` values" reading. The four-kind exclusion paragraph already existed at `038dab3` (confirmed via `git show 038dab3:…`), so the contradiction went live in that same commit. A later edit (`d75f2a8`, 2026-06-06) reworked the surrounding occurrence/origin prose but left the unqualified phrase intact. The defect is a regression introduced by a prior fix, not present since inception.

## Solution approach

Clarify the opening sentence of the **Runtime event channel** paragraph so the `QueryError` component reads as a subset of `QueryError` `kind` values rather than all of them, leaving binder failure causes and `loom/runtime/*` panic codes as the other two member categories. The qualifier attaches to the `QueryError` component only.

## Solution constraints

- Binder failure causes and `loom/runtime/*` panic codes remain full members of the always-log set (they are not `QueryError` kinds); do not narrow the set to `QueryError` kinds alone.
- Out of scope: the four-kind exclusion paragraph and the Group A / Group B enumerations stay the authoritative membership statement — do not move or duplicate the enumeration into the opening sentence.

## Relationships

- F-1595 "\"Failures\" opening sentence over-broad for `.loom` callables" — same-cluster (same defect pattern — an unqualified opening sentence about `QueryError`-variant routing that contradicts a later paragraph; resolves independently on `tool-calls.md`)
