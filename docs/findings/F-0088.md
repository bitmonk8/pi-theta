---
id: F-0088
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/coverage-matrix.md, anchor: "code-keyed obligation-area table (~lines 71–104)" }
lens: [spec-lens-traceability]
tier: high
factors:
  blast_radius: narrow
  centrality: core
  core_premise_blocker: no
status: reduced
shape: single
state: reduced
category: [traceability]
title: "V11g closes a spec obligation that no coverage-matrix row maps to it"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Problem

Leaf `V11g` ("Fill-if-absent defaulting and post-merge AJV validation") binds the post-default-merge AJV validation obligation in `binder/defaulting-system-note-echo.md` §Defaulting (anchor `#post-default-merge-ajv-validation`), but `docs/plan_topics/coverage-matrix.md` carries no row whose closing leaf is `V11g`. The only matrix row citing that spec file covers §System-note rendering and closes at `V11e`; the fill-if-absent + post-default-merge AJV obligation is mapped nowhere. Without a witnessing cell the H5a closing gate cannot witness V11g's closure, and the obligation either fails the H6a release gate or ships untraced.

## Issue introduction

**Verdict:** introduced-by-commit
**Introducing commits:** `cb8737a` ("pi-loom plan: resolve \"Systemic leaf over-bundling across the plan corpus\"")
**History:** The plan corpus is git-tracked. `git log --diff-filter=A --follow` shows `docs/plan_topics/V11g-defaulting-revalidation.md` and `V11g-T-defaulting-revalidation.md` were created in `cb8737a` with their current fill-if-absent/AJV semantics (carved out during the over-bundling split). The same commit edited `coverage-matrix.md` — adding rows for sibling splits (BNDR-6→`V11h`, the PIC-10/11/13 `V8a`/`V8c`/`V8b` split, `V5f`, `V3e`) — but added no row for V11g's spec target, so the gap entered the corpus with the leaf. An earlier `V11g` token in the matrix (`Schema Declarations — recursion | V11g, V11h`) belonged to the pre-pivot scaffold and was removed in `15f69aa`; it is unrelated to the current leaf's obligation.

## Solution approach

Add one code-keyed row to the obligation-area table in `docs/plan_topics/coverage-matrix.md` citing `binder/defaulting-system-note-echo.md` §Defaulting — the fill-if-absent default-fill and the post-default-merge AJV validation MUST at anchor `#post-default-merge-ajv-validation` — with `V11g` as the closing leaf, marked `(un-anchored; GOV-22 residue)` consistent with the sibling binder rows.

## Solution constraints

- Out of scope: the spec is read-only for this fix; do not edit `binder/defaulting-system-note-echo.md` (the obligation already exists there — the matrix reads from the spec).
- Leave the existing §System-note rendering row's closing leaf as `V11e`, and do not add `V11g-T` (the matrix lists implementation leaves only, per the `## Plan rules` matrix convention).

## Relationships

- "V15d / V15d-T have no coverage-matrix row (untraceable)" — same-cluster (same defect class; independent matrix row)
- "V17b / V17b-T have no coverage-matrix row (untraceable)" — same-cluster (same defect class; independent matrix row)
- "V9b-T primary spec target `registration-steps.md` absent from coverage matrix" — same-cluster (same defect class; independent matrix row)
- "SNK-a…SNK-k inline labels have no coverage-matrix row" — same-cluster (same defect class; independent matrix row)
- "C8 — V2b's \"deferral marker\" Class-2 seam is not consumed by either named consumer" — same-cluster (touches V11g's Deps/Adds but resolves independently)
