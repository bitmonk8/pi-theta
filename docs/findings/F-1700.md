---
id: F-1700
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/session-only-degraded-state.md, anchor: "§ Predicate split" }
lens: [spec-lens-naming]
tier: low
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [naming]
title: "§\"Predicate split\" heading describes structure, not subject"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The `## Predicate split` section of `session-only-degraded-state.md` defines two distinct, separately-governing predicates: the **diagnostic-emission** predicate (narrow — `pinnedConstantReadOk && capturedEventReason ∈ {"new","resume","fork"}`, gating the `runtime-degraded` emission) and the **tag-transition** predicate (broad — `capturedEventReason ∉ {"quit","reload"}`, gating the unconditional `"degraded-needs-reload"` tag write). The heading text "Predicate split" names the section's *internal shape* (that there happen to be two predicates) rather than its subject. A reader scanning the page table of contents cannot tell that this is where the emission and tag-write conditions are pinned.

The section is already a widely-cited anchor concept: `code-registry-host.md` and `drain-state-contract.md` refer to "the *Predicate split* clause" in normative prose, and plan leaves `V9h`/`V9l`/`V9l-T` plus `coverage-matrix.md` cite it as `§Predicate split`. A descriptive title that mirrors the two predicate names the section's own table already uses ("Diagnostic-emission" / "Tag-transition") makes the section discoverable from the TOC without changing any content.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** f5e89f4 — docs: enforce 100KB hard / 30KB recommended size cap on spec set (2026-06-04, Thomas Andersen)
**History:** `session-only-degraded-state.md` was created as a new file in commit f5e89f4 when the oversized PIC spec was split to meet the corpus size cap. The `## Predicate split` heading was present in that first commit (pickaxe on `## Predicate split` returns only f5e89f4) and has never been renamed in any subsequent edit.

## Solution Space

**Shape:** single

### Recommendation

Rename `## Predicate split` to `## Diagnostic-emission and tag-transition predicates`, matching the two predicate names the section's own table already uses. In the same edit, sweep every prose reference that names the section so the cross-references keep resolving: the "the *Predicate split* clause" mentions in `code-registry-host.md` and `drain-state-contract.md`, and the `§Predicate split` citations in plan leaves `V9h`, `V9l`, `V9l-T`, and `coverage-matrix.md`. The section carries no `<a id>` anchor today, so no anchor link breaks; adding a stable REQ-ID anchor to the section is a separate concern (tracked under the un-anchored-MUST work) and is out of scope for this rename.

## Consequence

**Severity:** cosmetic

A reader scanning the page TOC sees only "Predicate split" and cannot tell the section defines the narrow diagnostic-emission predicate and the broad tag-transition predicate; the label names the section's internal structure rather than its subject. No implemented behaviour diverges.

## Relationships

- F-1791 "\"value-independent seam\" undefined jargon" — same-cluster (jargon inside the same § Predicate split section; resolves independently)
