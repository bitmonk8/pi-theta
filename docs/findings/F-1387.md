---
id: F-1387
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/host-interfaces-core.md, anchor: "the `SessionContext` shape paragraph immediately following `<a id='sessioncontext-shape'>` (the two `<a id='messages-chronological-order-presupposition'>` and `<a id='messages-leading-user-message-presupposition'>` blocks)" }
  - { path: docs/spec_topics/pi-integration-contract/host-prerequisites.md, anchor: "the named-presupposition stack (where the relocated blocks land, with sibling cross-cites added to match the existing chain)" }
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2.md, anchor: "checklist items `(h)` and `(ag)` (only the link-target paragraph anchors change; item text unaffected)" }
  - { path: docs/spec_topics/binder/binder-model-and-context.md, anchor: "citations to `host-interfaces-core.md#messages-chronological-order-presupposition` and `host-interfaces-core.md#messages-leading-user-message-presupposition` (links repointed to `host-prerequisites.md`)" }
  - { path: docs/spec_topics/pi-integration-contract/conversation-drive.md, anchor: "same link-repointing" }
  - { path: docs/spec_topics/pi-integration-contract/subagent.md, anchor: "same link-repointing" }
lens: [spec-lens-placement]
tier: medium
status: reduced
category: [placement]
title: "`.messages` chronological-order and leading-`UserMessage` presuppositions belong on `host-prerequisites.md`"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

`host-interfaces-core.md` carries two named Pi behavioural presuppositions —
`messages-chronological-order-presupposition` (every loom-consumed
`AgentMessage[]` surface is chronologically ordered oldest-to-newest) and
`messages-leading-user-message-presupposition` (each such array is non-empty
iff its first element is a `UserMessage`) — attached to the `SessionContext`
shape paragraph (`<a id="sessioncontext-shape">`). Each block is framed
identically to the host-behaviour presuppositions inventoried on
`host-prerequisites.md` (a "loom 1.0 treats it as a **presupposition**"
clause, an *Authoritative source* with known-fragile `dist/...` evidence, and
a version-bump re-audit hook — checklist items (h) and (ag) in
`version-bump-step2.md`), yet they live two files over inside an SDK
type-contract paragraph. A reviewer enumerating presuppositions off the
inventory page does not see them, and a bump contributor working the inventory
page can miss the link to checklist items (h)/(ag).

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:**

- `aebec4e` (2026-06-04) — *pi-loom spec: resolve "Pi version-bump gate cannot detect three unstated host/provider presuppositions"* — added the `messages-chronological-order-presupposition` block to `host-interfaces-core.md` and wired checklist item (h) in `version-bump-step2.md`.
- `ce4e476` (2026-06-06) — *pi-loom spec: resolve "Turn-grouping undefined for leading non-user messages"* — added the sibling `messages-leading-user-message-presupposition` block to `host-interfaces-core.md` and wired checklist item (ag) in `version-bump-step2.md`.

**History:** Each block was introduced inline at the `SessionContext` type-contract paragraph by the resolving commit, in the same file where the consuming `AgentMessage[]` shape is pinned. Neither commit ever placed the presupposition on `host-prerequisites.md`; the cited paragraph has been at its current site since the resolving commit. `host-prerequisites.md` existed at both times (it was created earlier and already carried `degraded-state-host-prerequisites`, `model-registry-population-presupposition`, `settings-write-back-preservation-presupposition`, `operator-always-present-presupposition`, and `loom-flag-namespace-presupposition`); the cross-cite stack on those siblings was extended by later commits (e.g. `db69f2b` adding `loom-flag-namespace-presupposition`) without picking up the two `.messages` blocks. The placement defect is therefore the cumulative omission across two independently-driven resolving commits rather than a single commit's mistake — neither commit's author was working on the inventory side, and the inventory page's sibling-stack was extended elsewhere without back-references to either `.messages` block.

## Solution approach

Move both the `<a id="messages-chronological-order-presupposition">` and
`<a id="messages-leading-user-message-presupposition">` blocks from
`host-interfaces-core.md` into the named-presupposition stack on
`host-prerequisites.md`, preserving both anchor IDs verbatim so existing
inbound links resolve unchanged. Repoint the file portion of every citation of
these two anchors from `host-interfaces-core.md#...` to
`host-prerequisites.md#...` — the consumers in `binder-model-and-context.md`,
`conversation-drive.md`, and `subagent.md`, the in-page back-reference within
`host-interfaces-core.md` itself, and the link targets of `version-bump-step2.md`
checklist items (h) and (ag) (item text unchanged). Add a forward-link from the
`sessioncontext-shape` paragraph in `host-interfaces-core.md` to the relocated
blocks so a reader of the type-contract paragraph still reaches the constraints
its consumers rely on.

## Solution constraints

- Out of scope: the `cwd-project-root-presupposition` (currently in `host-interfaces-services.md`) and the six slash-handler-promise-lifecycle presuppositions (currently in `host-interfaces-core.md`); their relocation onto `host-prerequisites.md` is owned by sibling same-cluster findings.

## Relationships

- "PIC-13 `cwd-project-root-presupposition` belongs in host-prerequisites.md" — same-cluster (same placement defect on the same target page, one presupposition currently in `host-interfaces-services.md`; resolvable as a separate co-edit)
- "Six slash-handler-promise-lifecycle presuppositions belong in host-prerequisites.md" — same-cluster (same placement defect on the same target page, the six (i)–(vi) lifecycle presuppositions currently in `host-interfaces-core.md`; resolvable independently)
