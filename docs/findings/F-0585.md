---
id: F-0585
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/conventions.md, anchor: "§Cross-cutting rules — No-globals: pi-sdk-boundary allow-bro" }
lens: []
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: []
title: "§Cross-cutting rules — No-globals: pi-sdk-boundary allow-broad-catch `<spec-page>` field undefined"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

- **Description:** The `// allow-broad-catch: <token> — <spec-page>` grammar gives no literal form for the `<spec-page>` field at the `pi-sdk-boundary` site; the resolver checks only the token, so authors write the field inconsistently, undermining grep-based allow-list audits.
- **Suggested fix:** Give the site a concrete fixed `<spec-page>` literal convention (e.g. the conventions.md *Specific exception types only* anchor).
- **Lenses:** spec-lens-implementability (shard-01)
- **Importance:** NIT
- **Factors:** Blast radius: narrow; Centrality: fringe; Core-premise blocker: no
