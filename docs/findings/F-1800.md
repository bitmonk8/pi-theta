---
id: F-1800
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/session-only-degraded-state.md, anchor: "*Predicate split* → 'Seam minimality (normative)' paragraph" }
lens: [spec-lens-prescription, spec-lens-testability]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [prescription, testability]
title: "Narrow 'Seam minimality (normative)' to the observable contract"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---
## Finding

The clause's "MUST NOT introduce any handler-scoped state seam beyond `pinnedConstantReadOk`/`capturedEventReason` — no `branchTakenWasClosedSet`, no roll-up, no `!pinnedConstantReadOk` disjunct, no sentinel-equality check" prohibits internal implementation structure whose observable consequences are already pinned by acceptance criteria (b)/(d)/(e). It elevates an implementation choice to a normative MUST NOT on a non-observable surface.

## Issue introduction

## Solution Space

**Shape:** single

### Recommendation

In the *Predicate split* section, rewrite the **Seam minimality (normative)** paragraph to state the requirement observably: the diagnostic-emission and tag-transition predicates MUST be evaluated from the handler-entry-captured `pinnedConstantReadOk` and `capturedEventReason` values, producing the per-branch outcomes acceptance (a)–(e) assert. Retain as a normative observable point the load-bearing rule that the discriminator reads the **captured** value rather than performing a fresh `event.reason` property access (a fresh access bypasses the catch-arm classification and is non-conformant). Demote the enumerated forbidden-variable list to a non-normative implementation note explaining that those alternatives are observationally redundant given the captured-value predicates.

## Consequence

## Relationships

- F-1801 "Add the captured-value-vs-fresh-read witnessing fixture" — same-cluster (resolves independently of the fixture gap; narrowing the prose does not add the missing test)
- F-1791 "\"value-independent seam\" undefined jargon" — same-cluster (same complement seam on this page; resolves independently as a clarity edit)
- F-1700 "\"Predicate split\" heading uninformative" — same-cluster (same section; naming concern resolved independently)
- F-1685 "PIC-30 all-three-throw corner case has no acceptance fixture" — same-cluster (parallel missing-fixture gap in the same degraded-state machine on the sibling drain-state-contract page)
