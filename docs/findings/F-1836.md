---
id: F-1836
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/diagnostics/code-registry-load.md, anchor: "" }
lens: [spec-lens-clarity]
tier: low
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [clarity]
title: "`argument-hint-not-displayed` — \"the binder still receives the hint\" ambiguous"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---
## Finding

"binder" elsewhere is loom's model-call argument-binding engine, but `argument-hint:` is Pi autocomplete metadata; "the binder still receives the hint" is unclear about the concrete recipient/effect.

**Suggested fix:** Name the concrete recipient (e.g. "passed to loom's registered command metadata") or state the hint has no effect when `description:` is absent.
