---
id: F-0556
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V13c-query-tool-loop.md, anchor: "Ships when" }
lens: [plan-lens-validation]
tier: low
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [validation]
title: "V13c Ships-when omits the depth-6 cross-ceiling `masked` co-fire vector"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`V13c` (`docs/plan_topics/V13c-query-tool-loop.md`) carries a Tests bullet for the typed depth-6 co-fire vector: a depth-6 typed-query response trips the loom-owned depth walk (`V5e`) before AJV, ceiling #4 surfaces as `Err(QueryError { kind: "validation", cause: "schema_validation" })` with `schema_keyword: "maxDepth"`, and the surface's `masked` field enumerates the co-satisfied-but-masked ceiling #2 — `masked:["ceiling#2"]` (`CIO-4`/`CIO-6`). The paired `V13c-T` authors this same vector red-first.

V13c's **Ships when** gate, however, reads `npm test` *advances the tool loop, runs the forced-respond branch, and asserts the `max_rounds:0` and exhaustion paths.* It names three of the leaf's behavioural vectors but omits the depth-6 cross-ceiling co-fire vector — the single most complex behaviour the leaf closes and the one assertion that witnesses cross-ceiling arbitration precedence (the `V16a` seam this leaf binds) and the depth-walk-before-AJV ordering.

The omission is asymmetric: the Ships-when enumeration reads as exhaustive (it explicitly names the lesser `max_rounds:0` and exhaustion paths) yet drops the highest-value vector. A reviewer reading the exit gate to confirm acceptance has no observable criterion pointing at the co-fire assertion, even though the §Per-phase TDD ritual's all-`-T`-green requirement nominally backstops it. The cross-ceiling vector is exactly the behaviour an implementer is most likely to under-implement, so its absence from the externally observable acceptance statement is the wrong place for a silent gap.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** c6a664e (2026-06-10, "pi-loom plan: build/update plan for spec.md + review")
**History:** `V13c`'s Ships-when has named only the tool-loop / forced-respond / `max_rounds:0` / exhaustion paths since the leaf first appeared in c6a664e, while a depth-6 co-fire Tests bullet has been present from the same commit (refined in 0dd2436, 2026-06-11, "V13c exhaustion bullet leaves depth-6 surfaced ceiling unstated"). A brief window in 0dd2436 extended Ships-when with the swallowing-handler suppression clause, but eaa5000 (2026-06-16, "V13c bundles four independent spec obligations into one leaf") re-split the leaf and restored the Ships-when to the four-path enumeration that omits the co-fire vector. The Tests/Ships-when asymmetry over the depth-6 vector has therefore existed continuously since inception; no later edit introduced it.

## Solution Space

**Shape:** single

### Recommendation

Extend `V13c`'s **Ships when** in `docs/plan_topics/V13c-query-tool-loop.md` so the gate prose names the depth-6 cross-ceiling co-fire assertion alongside the paths it already lists. Add a clause asserting that `npm test` exercises the typed depth-6 vector and observes ceiling #4 surfacing as the `validation`/`schema_keyword: "maxDepth"` `Err` with `masked:["ceiling#2"]` (the `CIO-4`/`CIO-6` co-fire surface). The edit is confined to V13c's Ships-when line; the Tests bullet already states the vector verbatim, so reuse its observable terms. `V13c-T` needs no change for this finding. The spec is read-only for this fix.

## Consequence

**Severity:** advisory

The depth-6 co-fire vector remains nominally gated by the ritual's all-`V13c-T`-green requirement, so the leaf cannot be tagged complete with the test red. But the externally observable exit criterion (the Ships-when prose) selectively enumerates the easier vectors and drops the cross-ceiling one, so a reviewer cross-checking acceptance against Ships-when gets no signal that the highest-value assertion is in scope — the vector most likely to be under-implemented is the one the gate prose fails to name.

## Relationships

- F-0558 "V13c / V14a — ERR-13 delegated live-carrier witness neither red-first nor gated (validation)" — co-resolve (the same Ships-when enumeration also omits the ERR-13 vector; the gate-naming edit closes both Ships-when gaps, though the ERR-13 finding additionally requires red-first authoring in V13c-T)
- "V13c-T — omits the \"trips the depth walk (V5e) before AJV\" clause present in V13c (clarity)" — same-cluster (both touch the depth-6 co-fire vector across the V13c/V13c-T pair; resolve independently)
