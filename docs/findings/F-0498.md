---
id: F-0498
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V3a-expression-evaluator.md, anchor: "Tests / Ships when" }
lens: [plan-lens-validation]
tier: medium
factors:
  blast_radius: broad
  centrality: core
  core_premise_blocker: no
status: triaged
category: [validation]
title: "V3a names identifier-resolution precedence in Adds but no test asserts the shadowing order"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`V3a`'s **Adds** lists "identifier resolution (local > fn > import > callable)" as a load-bearing evaluator behaviour, and `expressions.md` §Identifier resolution defines it as a normative first-match-wins ordering with explicit shadowing semantics ("Local bindings (1) shadow everything else lexically", collisions across tiers (2)–(4) rejected at load time, no match → `loom/parse/unknown-identifier`).

None of `V3a`'s four **Tests** bullets exercises this rule. The bullets cover operator precedence/associativity, short-circuit observability, equality/ordering/arithmetic widening, and the `loom/parse/non-boolean-condition` truthiness reject — none asserts which binding wins when a name is visible at more than one tier. An evaluator that resolves the tiers in the wrong order (e.g. an import shadowing a local, or a `fn` shadowing a local) satisfies every existing `V3a` assertion and ships green.

The resolution rule is named in the leaf's own `Adds`, so this is the *No silent test skipping* failure mode the plan forbids: a behaviour the leaf claims to add but does not assert. Note a scope subtlety the fix must respect — `V3a`'s `Deps` are `V3a-T`, `V2b`, `V2c`; the tier-3 (import) and tier-4 (callable-set) binding sources are introduced by later leaves (`V15c` imports, `V6c` tools/callable set), so a fully four-tier assertion is not constructible at `V3a`'s dep scope.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** c6a664e (`pi-loom plan: build/update plan for spec.md + review`, 2026-06-10)
**History:** `V3a` was created in c6a664e with "identifier resolution (local > fn > import > callable)" already in its `Adds` and no corresponding `Tests` bullet. `git log -S 'local > fn > import > callable'` returns only c6a664e for this file, and `git log -p --follow` over the leaf shows the `Adds` clause present from creation while the `Tests` section never gained an identifier-resolution assertion across the later edits (stdlib split-out 2a4f797/20b4689, heading-scope edit 8704946, etc.). The gap has existed in every revision of the leaf.

## Solution Space

**Shape:** single

### Recommendation

Add a `Tests` bullet to `docs/plan_topics/V3a-expression-evaluator.md` asserting identifier-resolution order, citing `expressions.md` §Identifier resolution (`expressions.md#identifier-resolution`, EXPR code-keyed area). The bullet's assertions, scoped to what is constructible at `V3a`'s dep scope (`V3a-T`, `V2b`, `V2c`):

- A local `let` binding / function parameter shadows a same-file top-level `fn` of the same name (tier 1 beats tier 2 — first match wins).
- A bare identifier with no binding at any tier resolves to `loom/parse/unknown-identifier`.

Extend `Ships when.` to state `npm test` proves the local-shadows-`fn` resolution order and the `loom/parse/unknown-identifier` no-match disposition.

Edge cases the implementer must watch:
- Tier-3 (import) and tier-4 (callable-set) precedence, and the load-time collision rejection across tiers (2)–(4), depend on binding sources introduced by `V15c` (imports) and `V6c` (callable set), which are outside `V3a`'s `Deps`. If those facets are not constructible at `V3a`, they should be closed by a citing test at the leaf that introduces the binding source (`V15c` / `V6c`) rather than forced into `V3a`; the `cka-3` row already lists `V3a` as a closer for the EXPR area, so any such co-witness must keep the §Identifier-resolution obligation traceable.
- The assertion must check the resolved *binding*, not merely that a name resolves to something, so a wrong-tier resolution reds.

## Consequence

**Severity:** correctness

A wrong resolution order (an import or `fn` shadowing a local, or a missed load-time collision rejection) passes every existing `V3a` gate and ships undetected; two reasonable implementers could pick different shadowing behaviours and both go green. The defect is contained (the leaf is still pickable), so it is correctness rather than blocking.

## Relationships

- F-0499 "V3a — minor behaviors named in Adds without a naming assertion" — same-cluster (same leaf, same Adds-named-but-untested pattern over different behaviours; resolves independently)
- "V3a — short-circuit "observable effect does not run" has no spec-defined observable at V3a's dep scope" — same-cluster (same leaf and shares the V3a-dep-scope constructibility constraint, but a distinct concern)
