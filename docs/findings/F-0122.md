---
id: F-0122
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/coverage-matrix.md, anchor: "Code-keyed obligation areas table" }
  - { path: docs/plan_topics/V3c-control-flow.md, anchor: "V3 control-flow implementation leaf" }
  - { path: docs/plan_topics/V3c-T-control-flow.md, anchor: "V3 control-flow tests leaf" }
lens: [plan-lens-validation]
tier: high
factors:
  blast_radius: broad
  centrality: core
  core_premise_blocker: no
status: reduced
shape: single
state: reduced
category: [validation]
title: "V3c asserts only CTRL-1 — the five control-flow reject-path diagnostics are closed by no leaf"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Problem

`V3c` builds `if` / `for` / `while` / `break` / `continue`, but its sole Tests bullet (mirrored in `V3c-T`) is `CTRL-1`, the once-only iterand-snapshot semantics. The five normative control-flow reject-path diagnostics for those constructs — `loom/parse/non-array-iterand`, `loom/parse/break-outside-loop`, `loom/parse/continue-outside-loop`, `loom/parse/break-with-value`, and `loom/parse/non-boolean-condition` — are asserted by no leaf's Tests anywhere in the plan. The `Code-keyed obligation areas` table in `coverage-matrix.md` also has no `control-flow.md` row, so the release closing gate has no mapping to enumerate these codes against. `V3c` thus reaches its `Ships when` gate with every loop/branch reject path unverified.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `c6a664e` (2026-06-10, "pi-loom plan: build/update plan for spec.md + review")
**History:** `git log --follow` shows `docs/plan_topics/V3c-control-flow.md` was created in `c6a664e` with a single `CTRL-1` Tests bullet and has never been modified since. `git log -S` for each of the five diagnostic codes across `docs/plan_topics/` and `docs/plan.md` returns no commits — the codes have never appeared in any leaf's Tests. The `coverage-matrix.md` code-keyed obligation table has carried no `control-flow.md` row across its history. The gap is original to the plan build; no later edit introduced or removed it.

## Solution approach

Add Tests bullets to `V3c` and `V3c-T` asserting the four loop/branch reject codes (`loom/parse/non-array-iterand`, `loom/parse/break-outside-loop`, `loom/parse/continue-outside-loop`, `loom/parse/break-with-value`) and extend each leaf's `Ships when` to name the new firings. Add a `loom/parse/non-boolean-condition` Tests bullet to `V3a` and `V3a-T`, since `V3a` owns boolean-only truthiness across `if` / `while` / ternary / `&&` / `||`, and extend its `Ships when`. Add the matching code-keyed `coverage-matrix.md` rows: a `control-flow.md` row naming `V3c` for the four loop/branch codes, and `loom/parse/non-boolean-condition` under the `expressions.md (EXPR)` area naming `V3a`.

## Solution constraints

- Source every expected diagnostic message string from the `code-registry-parse.md` Message column per `conventions.md` §Diagnostic message anchors.

## Relationships

- "V3a / V3a-T heading "Expression evaluator and stdlib" misleads" — same-cluster (the `non-boolean-condition` assertion lands in V3a; touches the same leaf but resolves independently)
- "V4a / V4a-T — `question-outside-result-fn` / `question-on-non-result` cited as bare identifiers with no REQ-ID / no `loom/` prefix" — same-cluster (sibling shard-03 diagnostic-code anchoring/coverage gap; resolves independently)
- "SNK-a…SNK-k inline labels have no coverage-matrix row" — same-cluster (same code-keyed coverage-matrix mapping-gap defect class)
