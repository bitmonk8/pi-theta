---
id: F-1260
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/conversation-drive.md, anchor: no-extra-mediation }
lens: [spec-lens-placement, spec-lens-scope]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [placement, scope]
title: "no-extra-mediation trust-boundary NFR sits on conversation-drive.md instead of its runtime-wide owner"
relations:
  - { kind: related, id: F-1261 }
  - { kind: related, id: F-1262 }
  - { kind: related, id: F-1263 }
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---
## Finding

The "No additional access channels" paragraph (the runtime's no-sandbox / no-capability-filter / no-mediated-proxy obligation, anchor `#no-extra-mediation`) is a runtime-wide trust-boundary NFR, but it sits as the *first* paragraph of `conversation-drive.md` — a prompt-mode driver page — because the conversation-drive section is where the rule's denial-routing-through-tools sub-clause was first restated. `overview-and-orientation.md`'s Non-functional-requirements §"Trust boundary" bullet already forward-links to it as the normative source. Placement under "conversation drive" mis-scopes a runtime-wide NFR as a prompt-mode concern.

## Solution Space

**Shape:** single

### Recommendation

Move the paragraph and its `<a id="no-extra-mediation"></a>` anchor off `conversation-drive.md` to the parent integration page (`pi-integration.md`) or a dedicated trust-boundary topic page; restate the denial-routing-through-tools sub-clause at the new home; leave a one-sentence cross-reference at the old site. Preserve the anchor so existing citations (`overview-and-orientation.md`, `future-considerations/model-changes-and-non-goals.md`, `runtime-value-model.md`) continue to resolve — migrate the anchor in the same edit. Do not touch the degraded-state material (owned by F-1166).

## Consequence

**Severity:** advisory

Navigability/scope only: every citation currently resolves, but `conversation-drive.md`'s lead-section scope reads as a runtime-wide trust rule rather than a prompt-mode driver concern.

## Issue introduction

**Verdict:** single-commit
**Introducing commits:**
- `52219c4` (2026-05-06) — "Address all 41 spec-review findings" — first introduced the *No additional access channels* paragraph at this location.

**History:** Split from F-1143 (shard-07 placement cluster) under the finding-atomicity invariant; this is the standalone obligation for the `no-extra-mediation` NFR relocation.

## Relationships

- F-1261 "Extension-manifest paragraphs sit on host-prerequisites.md" — same-cluster (shard-07 placement; resolves independently)
- F-1262 "PIC-7 reason-set pin split from its handler" — same-cluster (shard-07 placement; resolves independently)
- F-1263 "Two named host presuppositions inline in registration-steps.md" — same-cluster (shard-07 placement; resolves independently)