---
id: F-2648
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/runtime-event-channel.md, anchor: "cascade-twin synonym, group A/B labels, external-actor dedup MUST, rationale cruft" }
  - { path: docs/spec_topics/pi-integration-contract/unknown-reason-rule.md, anchor: "H1 heading casing, PIC-48 editorial MUSTs, operator-key MUSTs, evidence cruft" }
  - { path: docs/spec_topics/pi-integration-contract/drain-state-contract.md, anchor: "duplicated `\"degraded-needs-reload\"` arm" }
  - { path: docs/spec_topics/pi-integration-contract/diagnostic-emission-isolation.md, anchor: "author/operator guidance, rationale cruft" }
  - { path: docs/spec_topics/pi-integration-contract/session-shutdown-semantics.md, anchor: "`dispose()`/`abort()` presupposition origin, deferred items inline" }
  - { path: docs/spec_topics/pi-integration-contract/session-only-degraded-state.md, anchor: "conditional/deferred items inline" }
  - { path: docs/spec_topics/pi-integration-contract/host-prerequisites.md, anchor: "relocation destination for the host presupposition" }
  - { path: docs/spec_topics/governance/, anchor: "relocation destination for the PIC-48 editorial-maintenance MUSTs" }
lens: [spec-lens-naming, spec-lens-placement, spec-lens-scope, spec-lens-cruft, spec-lens-testability]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [naming, placement, scope, cruft, testability]
title: "Demote external-actor (consumer/operator-tooling) MUSTs to non-normative guidance"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
decision_axes: 2
---

## Problem

Several MUSTs bind external actors that loom cannot conformance-test. Examples include the consumer-facing dedup obligation in PIC-1 clause (g) of `runtime-event-channel.md` (`#pic-1` — "consumers MUST NOT include `masked` in the dedup tuple") and the operator-tooling key obligations around `startsWith("throw:")` in `unknown-reason-rule.md`. As written these are non-falsifiable: loom has no surface on which to demonstrate conformance for a consumer or operator-tool behaviour.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** d4370cb (2026-05-06, "resolve 'Consumers MUST deduplicate is a normative obligation on undefined party'" — introduced the cascade-twin/boundary-re-emission synonym pairing and the non-normative consumer-dedup framing); bf42043 (2026-06-27, F-1509 — introduced the "Group A" subset framing of the always-log set); a9fb344 (2026-06-25, F-1350 — coined PIC-45..48, including the PIC-48 anchor-stable editorial-maintenance MUSTs); f5e89f4 (2026-06-04, the 100KB/30KB size-cap split — created `unknown-reason-rule.md`/`drain-state-contract.md` as standalone pages, producing the `# Unknown reason rule` H1 vs hyphenated-term casing split and the duplicated `"degraded-needs-reload"` arm across the freshly-split pages).
**History:** The corpus is git-tracked. No single commit introduced the cluster; the residue accreted across heterogeneous edits. Several of the introducing commits were themselves prior spec-review fixes that resolved an adjacent finding but left naming/placement/scope residue behind (e.g. d4370cb fixed a normative-obligation-on-undefined-party finding while introducing the synonym pair and the consumer-facing dedup wording; bf42043/F-1509 qualified the always-log subset while solidifying the opaque "Group A/B" labels). The size-cap file split (f5e89f4) mechanically severed previously-colocated content, creating the heading-casing mismatch and the cross-page arm duplication. The defect token greps (`git log -S` over "cascade twin", "boundary re-emission", "group A", "degraded-needs-reload", "PIC-48", and `git log --follow` on the two split pages) confirm the heterogeneous, accreted entry pattern rather than a localisable single-commit origin.

## Solution approach

Restate the external-actor obligations as non-normative guidance ("consumers / operator tooling may …") at their current sites in `runtime-event-channel.md` (PIC-1 clause (g)) and `unknown-reason-rule.md`. Where a genuine loom-side obligation underlies the demoted MUST, recast it as a falsifiable MUST on loom's own emission behaviour (e.g. the runtime emitting a stable, parseable discriminator) rather than on the external consumer.

## Solution constraints

- Out of scope: the "cascade twin" / "boundary re-emission" synonym (F-2650) and the "group A" / "group B" label glosses (F-2651), even where they sit in the same paragraphs being demoted.

## Relationships

- F-2643 "Relocate the `dispose()`/`abort()` host presupposition to host-prerequisites" — co-resolve
- F-2644 "Relocate author/operator guidance + missing-terminal-event ambiguity note to orientation/observability" — co-resolve
- F-2645 "Relocate PIC-48 anchor-stable editorial-maintenance MUSTs to governance" — co-resolve
- F-2647 "Pull deferred/Phase-2/SHOULD-conditional items out of normative teardown sections" — co-resolve
- F-2649 "Delete rationale / known-fragile-evidence / governance-versioning cruft" — co-resolve
- F-2650 "Declare "boundary re-emission" and "cascade twin" equivalent (or unify on one)" — co-resolve
- F-2651 "Gloss the opaque "group A" / "group B" labels" — co-resolve
- F-2652 "Align the `unknown-reason-rule.md` H1 with the hyphenated term" — co-resolve
