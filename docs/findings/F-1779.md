---
id: F-1779
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/host-interfaces-core.md, anchor: "§*Tool execution from loom code* (anchor `#tool-execution-from-loom-code`)" }
  - { path: docs/spec_topics/tool-calls.md, anchor: "§*Failures* + §*Outcome enumeration (normative)*" }
  - { path: docs/spec_topics/cancellation.md, anchor: "§*Race semantics — late-settlement discard at the tool-call checkpoint* + CNCL-1/2/3" }
lens: [spec-lens-scope]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [scope]
title: "\"Tool execution from loom code\" mixes runtime-semantics into a host-interface page"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
decision_axes: 3
---

## Problem

The `#tool-execution-from-loom-code` surface on `host-interfaces-core.md` mixes loom-runtime behaviour into a page whose contract is a Pi-owned host-interface inventory re-validated on each Pi minor bump. The runtime-side rules it pins — the envelope-to-string lowering, the 4096-byte UTF-8 truncation rule, the `Ok("")` non-text-discard contract, the `execute()`-throw error lowering, the *Outcome routing summary*, and the CNCL-1/2/3 late-settlement discard plus swallowing-handler MUST — are not snapshots of any `@earendil-works` type and restate normative text whose canonical homes are `tool-calls.md`, `cancellation.md`, and `errors-and-results/queryerror-variants.md`. This produces parallel normative text that any future edit to lowering, truncation, or discard must keep in sync across four files, and forces re-validation reviewers to partition each paragraph into Pi-owned snapshot versus runtime contract.

## Issue introduction

**Verdict:** present-since-inception

**Introducing commits:** `fae85d3` *Tighten spec for implementation-plan readiness* (2026-05-04) introduced the §*Tool execution from loom code* surface verbatim into the monolithic `spec.md`, already mixing the host-interface bullets (`toolCallId`, `params`, `signal`, `onUpdate`, `ctx`) with the runtime-side lowering rule (`{ content, isError }` → string return value; `Err(QueryError { kind: "tool_call", cause: "execution", … })`) under a single section.

**History:** `git log --reverse -G "Tool execution from loom code"` lands on `fae85d3` as the introducing commit; subsequent edits accreted the runtime-side content rather than relocating it. `8f8acbf` (2026-05-05) added the 4096-byte UTF-8 truncation rule (resolving "tool-error message truncation rule sloppy") at the same site; `11ef3b9` (resolving "Tool failure modes beyond throw / isError: true unspecified") introduced the non-settling / non-conforming-shape language; `2ebe288` (2026-05-07) added the post-cancel late-settlement discard and the swallowing-handler MUST; `5f04646` added the non-text-discard `Ok("")` paragraph; `482098b` (2026-06-04) split the late-settlement discard into the CNCL-1/2/3 sub-anchors but kept the rule in PIC. `fecb504` split `spec.md` into per-topic files (the section moved into `spec_topics/pi-integration-contract.md` unchanged), `31ff060` moved that file under `docs/`, and `f5e89f4` (2026-06-04) split the 576KB `pi-integration-contract.md` into the index+children layout, creating the present `host-interfaces-core.md` file with the section's content carried forward verbatim. The scope mixing is therefore traceable to the section's first appearance: every subsequent commit treated PIC as the canonical home for the runtime rule and reinforced the placement rather than challenging it.

## Solution approach

Move the envelope-to-string lowering, the 4096-byte UTF-8 truncation rule, the `Ok("")` non-text-discard contract, the `execute()`-throw error lowering, and the *Outcome routing summary* from `host-interfaces-core.md` §*Tool execution from loom code* to `tool-calls.md` (under §*Failures* / §*Outcome enumeration (normative)*), and move the swallowing-handler MUST and the discard-total-ness language to `cancellation.md` alongside CNCL-1/2/3. Keep on `host-interfaces-core.md` under the `#tool-execution-from-loom-code` anchor the host-interface portion — the `execute(...)` parameter sheet and the `AgentToolResult` / `{ content, isError }` envelope declaration — and add a forward-link to the relocated runtime contract. Repoint each inbound citation whose pointed prose moved (notably `coverage-matrix.md` `cka-46`) to its new home.

## Solution constraints

- Preserve the `#tool-execution-from-loom-code` anchor on `host-interfaces-core.md` so inbound citations from `coverage-matrix.md` `cka-46`, plan leaves `V14a`/`V14c`/`V14f`, `query/query-tool-loop.md`, `tool-calls.md`, `cancellation.md`, and `diagnostics/code-registry-runtime.md` continue to resolve.
- Out of scope: the §*per-mode override semantics* table (`signal`/`sessionManager`/`abort()` rows) and the `loom-direct:` `toolCallId` UUID-body / PIC-20 pin owned by F-1396 — both are host-interface content and stay on the page.

## Relationships

None
