---
id: F-0568
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V15c-imports.md, anchor: "**Ships when** field" }
lens: [plan-lens-validation]
tier: nit
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [validation]
title: "V15c — Ships-when under-enumerates two import diagnostics its Tests exercise"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`V15c`'s **Tests** section exercises five diagnostic surfaces for the `.warp` import path. Its import-cycle bullet bundles three of them: `loom/load/import-cycle`, plus `import-unknown-symbol` and `import-name-collision` ("a `.warp` static-graph cycle fires with its path; `import-unknown-symbol` / `import-name-collision` fire").

`V15c`'s **Ships when** gate, however, enumerates only the resolver-success bind, the non-permitted top-level-form rejection, the `import-non-warp-extension` rejection, `import-cycle`, and `loom/load/unresolvable-warp-path` (`IMP-1`). It names neither `import-unknown-symbol` nor `import-name-collision`. The leaf's externally-observable exit gate therefore under-enumerates two diagnostics the leaf is responsible for and its paired tests task already asserts.

The two diagnostics are still covered transitively: `V15c-T` carries the same import-cycle test bullet, and the per-phase TDD ritual requires every Tests REQ-ID/diagnostic to red first and then go green before the implementation leaf ships. The gap is confined to the leaf-local **Ships when** acceptance text, which is the artefact a reviewer reads to confirm the leaf's observable change.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commit:** `c6a664e` (2026-06-10) "pi-loom plan: build/update plan for spec.md + review"
**History:** The plan corpus is git-tracked. `git log --follow` on `docs/plan_topics/V15c-imports.md` returns four commits (`c6a664e`, `2316cac`, `f930314`, `6b7c7e8`). The file's first authored version (`c6a664e`) already named `import-unknown-symbol` / `import-name-collision` in the Tests import-cycle bullet while its Ships-when read only "resolves a `.warp` import, rejects a non-permitted top-level form, and fires `import-cycle`" — the two diagnostics were absent from Ships-when from inception. `git log -G "import-unknown-symbol"` shows the token entered only at `c6a664e` and was never edited since. The three later commits each expanded Ships-when (adding re-export visibility, `import-non-warp-extension`, then `unresolvable-warp-path`/`IMP-1`) to resolve other findings but never added the two cycle-adjacent diagnostics, so the gap persisted unchanged across the file's whole history.

## Solution Space

**Shape:** single

### Recommendation

In `docs/plan_topics/V15c-imports.md`, extend the **Ships when** sentence to additionally require that `import-unknown-symbol` and `import-name-collision` fire — the two diagnostics `V15c`'s Tests already exercise on the static-graph cycle case. Insert a clause naming both diagnostics firing (alongside the existing `import-cycle` clause) so the leaf-local exit gate enumerates every diagnostic the leaf owns. No spec edit and no Tests change are required; the Tests bullet already carries both diagnostics.

## Consequence

**Severity:** advisory

A reviewer treating **Ships when** as the leaf's acceptance gate would not require observable evidence that `import-unknown-symbol` / `import-name-collision` fire, even though the leaf owns both diagnostics. The implementer still produces a working leaf because `V15c-T`'s tests and the TDD ritual force both diagnostics green; the defect is a guidance/observability gap in the leaf-local exit gate, not a coverage hole.

## Relationships

- F-0567 "V15c — Spec-field closure omits the `FileSystem.readdir` (PIC-13) contract the Resolver requires (implementability)" — same-cluster (also edits the V15c leaf; resolves independently)
- F-0461 "§Numbered REQ-IDs / Code-keyed areas — imports.md diagnostic codes & visibility rules (V15i) have no cka-N entry" — same-cluster (concerns coverage-matrix tracking of the same imports.md diagnostic codes; resolves independently)
