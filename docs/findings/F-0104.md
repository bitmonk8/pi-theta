---
id: F-0104
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/coverage-matrix.md, anchor: "Code-keyed obligation-area table (and the `PIC-17, PIC-18` numbered row)" }
lens: [spec-lens-traceability]
tier: high
factors:
  blast_radius: narrow
  centrality: core
  core_premise_blocker: no
status: reduced
shape: single
state: reduced
category: [traceability]
title: "V15d is untraceable: prompt→prompt suspend / `setActiveTools` snapshot-restore obligation has no coverage-matrix row"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Problem

`V15d` ("Prompt→prompt parent-suspend and `setActiveTools` snapshot/restore") is a full leaf in the V15 slice, listed in `plan.md` and carried in `H5b`'s coverage-producing Deps set, but no row in `coverage-matrix.md` names `V15d` as a closing leaf. The leaf was peeled off `V15a`, yet the split left `V15a`'s `INV` rows mapped to `V15a` and never added a row for the facet that moved to `V15d`. The obligations `V15d` actually closes — the un-anchored prompt→prompt parent-suspend and whole-body `setActiveTools` snapshot/restore-in-`finally` MUSTs in `invocation.md` §Cross-mode semantics (GOV-22 residue, no numbered REQ-ID), and the prompt→prompt-`invoke`-path facet of the `PIC-17` step-4 `finally` restore — are mapped to no leaf and credited only to `V9c` respectively. The closing gate has no row to witness `V15d`'s closure, so it reds against the uncovered obligation once `H6a` flips the matrix to its live-corpus footing.

## Issue introduction

**Verdict:** introduced-by-commit (single commit)
**Introducing commit:** `cb8737a` — "pi-loom plan: resolve \"Systemic leaf over-bundling across the plan corpus\"" (Thomas Andersen, 2026-06-12)
**History:** The plan corpus is git-tracked. `git log --diff-filter=A --follow` shows `V15d-prompt-suspend-snapshot.md` (and `V15d-T`) were first added in `cb8737a`, the leaf-over-bundling split that peeled `V15d` off `V15a`. `git log -S 'V15d' -- docs/plan_topics/coverage-matrix.md` returns no commits — `V15d` has never appeared in the matrix. `cb8737a` did edit `coverage-matrix.md` (+10/−7) and correctly added rows for its other split products (`V3e`, `V5f`, `V8c`/`V8d`/`V8e`, `V9m`, `V11h`), but omitted the three leaves whose obligations are un-anchored (`V15d`, `V11g`, `V17b`). The gap entered in a single commit, as a partial matrix update accompanying the split.

## Solution approach

Add `V15d` to `coverage-matrix.md` as a closing leaf for the obligations it owns. In the Code-keyed obligation areas table, add a row mapping the un-anchored `invocation.md` §Cross-mode semantics prompt→prompt parent-suspend / whole-body `setActiveTools` snapshot-restore obligation (GOV-22 residue) to `V15d`. Then add `V15d` as a co-witness on the existing `| PIC-17, PIC-18 | `V9c` |` numbered row so the prompt→prompt-`invoke`-path facet of the `PIC-17` step-4 `finally` restore traces to its producing leaf.

## Solution constraints

- Keep `V15a`'s existing `INV` rows unchanged — the facet moved out of `V15a`, but its remaining rows still describe obligations `V15a` closes.
- On the `PIC-17, PIC-18` row, `V9c` retains its per-query closure; add `V15d` as a co-witness rather than replacing `V9c`.
- Out of scope: the sibling `V11g`, `V17b`, and `V9b-T` coverage-matrix rows — each is owned by its own finding and resolves independently.

## Relationships

- "V11g / V11g-T have no coverage-matrix row (untraceable)" — same-cluster (sibling leaf with the same omitted-row defect; resolves independently)
- "V17b / V17b-T have no coverage-matrix row (untraceable)" — same-cluster (sibling leaf with the same omitted-row defect; resolves independently)
- "V9b-T primary spec target `registration-steps.md` absent from coverage matrix" — same-cluster (coverage-matrix traceability gap; independent fix)
