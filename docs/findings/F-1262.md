---
id: F-1262
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/host-prerequisites.md, anchor: session-binding-contract }
lens: [spec-lens-placement, spec-lens-scope]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: triaged
category: [placement, scope]
title: "PIC-7 reason-set pin is split from the session-shutdown handler that consumes it"
relations:
  - { kind: related, id: F-1260 }
  - { kind: related, id: F-1261 }
  - { kind: related, id: F-1263 }
  - { kind: related, id: F-1166 }
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---
## Finding

PIC-7 *Session-binding contract* is defined on `host-prerequisites.md` (single-active-session premise + `SessionShutdownEvent['reason']` union pin + cross-links), but the handler that consumes the reason-set membership and the teardown-vs-session-swap partition discriminator lives on `session-shutdown-semantics.md`. The reason-set pin and the partition discriminator are split from the handler that branches on them.

## Solution Space

**Shape:** single

### Recommendation

Keep the single-active-session premise sentence on `host-prerequisites.md` (a genuine Pi-side prerequisite). Relocate the `SessionShutdownEvent['reason']` union pin and the teardown-vs-session-swap partition framing onto `session-shutdown-semantics.md` next to the handler that consumes them; re-home the `pic-7` anchor on the handler-side body; leave a one-line pointer at the prerequisites site. Ensure V18c checklist item (f) and `version-bump-intro.md`'s bidirectional `SessionShutdownEvent['reason']` type-equality citation continue to resolve, and update `session-model-and-appendix.md` SM-3b anchor citations in the same edit.

## Consequence

**Severity:** advisory

Navigability only (the cross-links resolve), but the closed reason set is defined away from the handler that branches on it.

## Issue introduction

**Verdict:** single-commit
**Introducing commits:**
- `d4ac81b` (2026-06-04) — "resolve 'PIC sections beyond Probe-wide invariants — missing REQ-ID anchors'" — coined the `pic-7` anchor on the Session-binding-contract paragraph, fixing the split between PIC-7 metadata and the handler at that location.

**History:** Split from F-1143 (shard-07 placement cluster) under the finding-atomicity invariant; standalone PIC-7 split-definition obligation.

## Relationships

- F-1166 "Session-only degraded-state feature — record governed-by-rebind and remove the feature" — must-follow (F-1166 reworks/removes session-shutdown degraded material on `session-shutdown-semantics.md`; land the `pic-7` re-home after that rework so the anchors settle once)
- F-1260 "no-extra-mediation NFR on conversation-drive.md" — same-cluster (shard-07 placement; resolves independently)
- F-1261 "Extension-manifest paragraphs on host-prerequisites.md" — same-cluster (shard-07 placement; resolves independently)
- F-1263 "Two named host presuppositions inline in registration-steps.md" — same-cluster (shard-07 placement; resolves independently)