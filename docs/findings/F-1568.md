---
id: F-1568
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/diagnostics/placeholder-rendering-a.md, anchor: "Closure paragraph" }
lens: [spec-lens-traceability]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [traceability]
title: "GOV-7/GOV-8 closure cross-reference points at the `spec.md` TOC stub, which defines neither rule"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

The **Closure** paragraph in `placeholder-rendering-a.md` cites GOV-7 and GOV-8 as the rules that make placeholder closure a versioned breaking change, linking them via `[`spec.md` — Governance](../../spec.md)`. That target resolves to `docs/spec.md`, a nine-line table-of-contents stub that carries no Governance section and no GOV-7/GOV-8 content; the link also has no `#` fragment, so it lands the reader on a contentless index page. GOV-7 and GOV-8 are actually defined on `governance/req-id-prefix-table-active-b.md` behind `<a id="gov-7"></a>` and `<a id="gov-8"></a>`. The broken citation also fails GOV-9's cross-link contract requiring REQ-ID references to resolve as `#prefix-tail` fragments at the depended-upon anchor.

## Issue introduction

**Verdict:** single introducing commit (refactor-induced)
**Introducing commit:** `f5e89f4` (2026-06-04) — *docs: enforce 100KB hard / 30KB recommended size cap on spec set*
**History:** The closure cross-reference was authored at `61ea1a5` (2026-05-06, *pi-loom spec: resolve "Diagnostic message placeholders have no defined rendering"*) inside the then-monolithic spec, as `[`spec.md` — Governance](../spec.md)` on what was `spec_topics/diagnostics.md`. At that time the link was correct: `git show 61ea1a5:spec.md` is a 177-line document whose Governance appendix defined `GOV-7 (mutation procedures)` and `GOV-8 (REQ-ID lifecycle)` inline, and `../spec.md` resolved to it. The defect entered at the `f5e89f4` size-cap split, which in one commit (a) extracted the Governance appendix out of `spec.md` into the `spec_topics/governance/` subtree — GOV-7/GOV-8 landing in the newly created `req-id-prefix-table-active-b.md` — and (b) carved the placeholder-rendering closure paragraph into the new `placeholder-rendering-a.md`, mechanically rewriting its link to `../../spec.md` for the deeper path. `git show ab2b669:docs/spec.md` (the prior state) still contained the GOV-7/GOV-8 definitions, while `git show f5e89f4:docs/spec.md` shows zero governance lines remaining. The link target's content moved away in the very commit that carried the link forward, but the citation was adjusted only for relative depth, not repointed to the new owner page — leaving it aimed at a `spec.md` that had become a stub.

## Solution approach

Repoint the `[`spec.md` — Governance](../../spec.md)` link in the Closure paragraph to the rules' owner page `governance/req-id-prefix-table-active-b.md`, reachable as `../governance/req-id-prefix-table-active-b.md` from `spec_topics/diagnostics/`. Resolve GOV-7 at its `#gov-7` anchor and GOV-8 at its `#gov-8` anchor so each REQ-ID citation lands on its own anchor as a GOV-9 `#prefix-tail` fragment.

## Solution constraints

- Out of scope: the unlinked GOV-7/GOV-8 mention in the intro paragraph; only the Closure-paragraph link target is changed.

## Relationships

- F-1516 "Bare `../pi-integration-contract.md` links resolve to a TOC, not the normative rule (pervasive)" — same-cluster (identical defect class: a load-bearing link aimed at a TOC index page instead of the sub-file/anchor that owns the rule; resolves independently with the same `…/<file>.md#<anchor>` repoint)
