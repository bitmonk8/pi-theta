---
id: F-2720
corpus: plan
fix_surface: [plan]
status: triaged
shape: single
state: shaped
tier: low
lens: ["spec-lens-naming"]
category: ["naming"]
factors: { blast_radius: narrow, centrality: supporting, core_premise_blocker: no }
location:
  - { path: "docs/plan.md", anchor: "item 5 (How-to-use, line 11)" }
  - { path: "docs/plan_topics/coverage-matrix.md", anchor: "intro (line 3)" }
title: "Pre-activation gate state named two ways — 'non-gating' vs 'seeded-fixture footing' — with no equivalence bridge"
cycle_first_seen: 1
cycle_last_seen: 1
---
## Finding

The closing gate has exactly two states across its lifetime: the pre-activation state (the gate runs only against `H5a`'s seeded fixtures and does not red CI on live coverage) and the post-activation state that `H6a` flips it into (the gate reconciles the live spec/test corpus and reds CI on a live gap). Two independent vocabularies name that pair, and they are not the same words on the pre-activation side:

- `coverage-matrix.md` intro (line 3) calls the transition "the non-gating→gating flip to this live-corpus footing" — the pre-activation state is **"non-gating"**.
- `plan.md` item 5 (line 11) likewise calls it "non-gating→gating activation".
- `plan.md` §Release gate (line 234) instead calls the same transition flipping the gate "from its seeded-fixture footing to its live-corpus footing", and the rollback sentence calls reverting it "returns the gate to its `H5a` seeded-fixture footing" — the pre-activation state is **"seeded-fixture footing"**.

The post-activation state is the same phrase ("live-corpus footing") in both files, so that side is self-bridging. The pre-activation side is not: "non-gating" and "seeded-fixture footing" denote the same state but neither file states their equivalence. The two vocabularies are reconciled only in the `H6a` leaf (`H6a-live-corpus-activation.md`), which juxtaposes "non-gating→gating flip is active rather than still fixture-only" and "from its seeded-fixture footing to its live-corpus footing" in the same body. A reader of `coverage-matrix.md` or `plan.md` item 5 in isolation, who sees only "non-gating", must open the `H6a` leaf to confirm it is the same state `plan.md`'s §Release gate calls "seeded-fixture footing".

## Consequence

**Severity:** cosmetic

A reader of `coverage-matrix.md` or `plan.md` item 5 cannot confirm, without opening the `H6a` leaf, that the "non-gating" pre-activation state is the same state `plan.md`'s §Release gate calls "seeded-fixture footing". This is terminology-tracing friction only; the gate mechanism is unambiguous in `H6a`, so no implementer would diverge.

## Issue introduction

**Verdict:** single-commit
**Introducing commits:** 5353dd7 — pi-loom plan: resolve "Release-gate activation has no owning leaf" (2026-06-10, Thomas Andersen)
**History:** Commit 5353dd7 added the owning-leaf resolution that introduced both vocabularies at once — "non-gating" in `coverage-matrix.md`'s intro and `plan.md` item 5, and "seeded-fixture footing" in `plan.md`'s §Release gate — without authoring a bridge tying the two pre-activation names together. A later commit (ea6b1da, 2026-06-11, the rollback resolution) reused "seeded-fixture footing" in `plan.md`'s revert sentence but did not introduce the divergence, which has been present since 5353dd7.

## Solution Space

**Shape:** single

### Recommendation

Tie the two vocabularies together at the points where the pre-activation term first appears, so each name carries its synonym inline; keep both framings (the "non-gating→gating" framing describes the gate's CI behaviour, the "seeded-fixture footing → live-corpus footing" framing describes the corpus the gate runs against).

- In `docs/plan_topics/coverage-matrix.md` intro (line 3), where it reads "the non-gating→gating flip to this live-corpus footing", gloss the pre-activation term so it reads "the non-gating (seeded-fixture footing) → gating (live-corpus footing) flip".
- In `docs/plan.md` item 5 (line 11), where it reads "whose non-gating→gating activation is owned by the terminal `H6a` leaf", gloss it to "whose non-gating (seeded-fixture footing) → gating (live-corpus footing) activation is owned by the terminal `H6a` leaf".

`plan.md`'s §Release gate already uses "seeded-fixture footing → live-corpus footing", so once item 5 carries the parenthetical gloss the two `plan.md` occurrences are self-reconciling and a `coverage-matrix.md` reader sees both names in one phrase. Equivalent alternative content, if the author prefers a single vocabulary: replace every "non-gating"/"gating" pre/post-activation use with "seeded-fixture footing"/"live-corpus footing" — but the bridging gloss preserves the CI-behaviour framing and touches less text.

## Relationships

- "\"retired\" carries two meanings within coverage-matrix.md" — same-cluster (another `coverage-matrix.md` naming-lens fix; resolves independently of this one)
