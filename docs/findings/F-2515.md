---
id: F-2515
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/expressions.md, anchor: "`## Other arithmetic` (line 226)" }
lens: [spec-lens-naming]
tier: low
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [naming]
title: "\"Other arithmetic\" H2 has no antecedent \"arithmetic\" section"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

In `expressions.md` the operator coverage is organised under the H2 sequence `## Object construction` → `## Array construction` → `## Other arithmetic` → `## Ordering comparisons`. The `+` operator is described in a `**`+` operator.**` bold lead-in paragraph that sits at the tail of the `## Array construction` section. The next H2 is titled `## Other arithmetic`.

As a standalone sibling heading, "Other arithmetic" presupposes a preceding "Arithmetic" section that the reader can read it as a complement to — but none exists. The word "Other" was meaningful when it lead an inline paragraph immediately following the `+` paragraph ("arithmetic other than the `+` just covered"), but at H2 level it dangles: the immediately preceding heading is `## Array construction`, not an arithmetic heading. A reader scanning the table of contents or jumping to the heading cannot tell what the arithmetic is "other" than.

The fix is a pure heading rename to "Arithmetic operators" (or equivalent), which is self-describing and needs no antecedent.

## Issue introduction

**Verdict:** single-commit
**Introducing commits:** `c1e5d64` — "spec: REQ-ID infrastructure + Phase 12a heading promotion on five named pages (Decision #12, partial)"
**History:** Before `c1e5d64`, "Other arithmetic" was a bold inline lead-in (`**Other arithmetic.** -, *, /, % accept only numeric operands…`) within the operator-rules prose, where "Other" correctly read as "arithmetic other than the `+`/concatenation just described". `c1e5d64` mechanically promoted the inline bold labels on this page to standalone H2 headings (`-**Other arithmetic.**` → `+## Other arithmetic`) without re-checking that each label reads as a self-contained heading. The promotion stranded the relative "Other" with no antecedent "Arithmetic" heading. The defect has existed continuously since that commit (`git log -S '## Other arithmetic' --follow` on `docs/spec_topics/expressions.md`).

## Solution Space

**Shape:** single

### Recommendation

Rename the `## Other arithmetic` heading (line 226) to `## Arithmetic operators`. The section already covers `-`, `*`, `/`, `%`, and unary `-`; the new title is self-describing and removes the dangling "Other". No body text changes are required. Confirm no in-corpus cross-reference targets the `#other-arithmetic` GitHub anchor before renaming; if any exists, update it in lock-step (the rename changes the auto-generated anchor to `#arithmetic-operators`).

## Consequence

**Severity:** cosmetic

The heading is navigationally misleading but the operator semantics in the section body are unaffected; no implementer would diverge on behaviour. Purely an organisation/wording defect.

## Relationships

- "expressions.md — "context" used where glossary term is "type sink"" — same-cluster (both are naming-lens fixes on the adjacent Array-construction/operator surface; resolve independently)
