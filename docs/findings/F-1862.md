---
id: F-1862
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/version-bump-step2b.md, anchor: "" }
lens: [spec-lens-testability]
tier: low
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [testability]
title: "Branch (1)∧(2) overlap — discretionary MUST not verifiable"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---
## Finding

"a contributor MUST pick the branch whose criterion the violation satisfies" but the (1)∧(2) overlap is "a reviewer-discretion call … whether the runtime depends on the surface"; no deterministic oracle for "MUST pick correct branch".

**Suggested fix:** Give a deterministic tie-break (e.g. branch (2) iff the symbol is reachable from a production execution path; else (1)), or demote the MUST to SHOULD with an explicit discretionary carve-out for the overlap.
