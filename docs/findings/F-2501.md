---
id: F-2501
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/drain-state-contract.md, anchor: "*Acceptance criteria — `(drained: true, tag: undefined)` → arm (b)*; PIC-29" }
  - { path: docs/spec_topics/pi-integration-contract/conversation-drive.md, anchor: "forced respond turn \"attaches no turn to the driven session\" clause" }
lens: [spec-lens-testability]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [testability]
title: "Add an acceptance criterion for the forced-respond-turn no-transcript property"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

conversation-drive.md and implementation-notes.md state normatively that the forced respond turn attaches no turn to the driven session and is not rendered in the transcript, but no acceptance criterion verifies it; the only hook (bump-checklist item (u)) re-validates pi-ai's off-session behaviour, not loom's conformance, and V12a's "no card" test asserts a weaker property than message-list omission.

## Issue introduction

**Verdict:** later-commit-introduced (one introducing commit per vector)

**Introducing commits:**
- Drain-state arm-(b) vector: `8008b91` ("pi-loom spec-plan: fix F-1217 — add acceptance-criteria/fixture blocks for four Shard-08 MUSTs", 2026-06-25).
- Forced-respond no-transcript vector: `7f75947` ("pi-loom spec: resolve 'Typed-query forced-respond tool choice delivery channel'", 2026-06-05).

**History:** `git log -S` over `drain-state-contract.md` shows the *Acceptance criteria — `(drained: true, tag: undefined)` → arm (b)* section first appeared in `8008b91`; its item (b) enumerates all three arm-(b) tuples (`(false, "shutting-down")`, `(true, "shutting-down")`, `(true, undefined)`) but the commit authored a witnessed routing fixture only for `(true, undefined)`, leaving `(false, "shutting-down")` uncovered from the section's inception (the PIC-29 six-tuple decomposition itself predates it, having entered `drain-state-contract.md` at `f5e89f4`, 2026-06-04, when the contract was split into its own file). `git log -S 'attaches no turn to the driven session'` over `conversation-drive.md` and `git log -S 'not rendered as a transcript turn'` over `implementation-notes.md` both resolve to `7f75947`, which introduced the off-session delivery channel and its no-transcript property; no later commit added an acceptance criterion for it, so that vector is present-since-introduction.

## Solution Space

**Shape:** single

### Recommendation

Add an acceptance criterion asserting that after a typed query completes, the driven session's message list contains the free-phase turns but no turn corresponding to the forced respond turn (a session-message-list / transcript-contents assertion, stronger than "no rendered card"). Place it at the leaf that owns the forced-respond conformance fixture (the V12a / V13c surface, or the conversation-drive.md owner), and assert it in both prompt mode and subagent mode if both are in scope for the driving surface.

## Consequence

**Severity:** advisory

The normative rules (PIC-29's tuple-to-arm map; the no-transcript MUST) are unambiguous, so implementers do not diverge on intent; the gap is purely in the conformance surface. But a runtime carrying a fall-through bug — mis-routing `(false, "shutting-down")` into a half-torn-down dispatch, or appending the forced respond turn to the driven transcript — passes the suite with the current fixtures, so the defect ships latent.

## Relationships

- F-2500 "Add a witnessed routing fixture for the `(false, "shutting-down")` arm-(b) tuple" — co-resolve
