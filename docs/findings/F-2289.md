---
id: F-2289
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V4f-no-rollback.md, anchor: "`Tests.` (ERR-13 bullet)" }
  - { path: docs/plan_topics/V4f-T-no-rollback.md, anchor: "`Tests.` (ERR-13 bullet)" }
lens: [spec-lens-placement]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [placement]
title: "Architectural rationale embedded in the ERR-13 Tests bullet (both paired files)"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---
## Problem

The `ERR-13` `Tests.` bullet in both `V4f-no-rollback.md` and `V4f-T-no-rollback.md` ends with a rationale sentence ("The guarantee rests on the runtime having no compensating/rollback path, so the tests witness it on these enumerated authoring sites rather than proving the absolute absence of a compensating path exhaustively"). Per `conventions.md` §Leaf format, `Tests.` bullets carry the binding per-REQ-ID acceptance criteria while `Adds.` holds mechanism and design rationale. This sentence is design rationale — already carried correctly in `V4f`'s `Adds.` field — so its presence in `Tests.` duplicates the reasoning and blurs the binding/descriptive boundary.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** 098ec80
**History:** The `V4f` / `V4f-T` leaf pair was created by commit `098ec80` (2026-06-17, "pi-loom plan: resolve 'V4c bundles the ERR-8…ERR-12 trichotomy and the ERR-13 no-rollback proof'"), which split the ERR-13 no-rollback proof out of `V4c` into the new pair. `git log -S` over the rationale sentence in both files finds it added only at `098ec80` and never modified since, so the misplacement has been present in the `Tests.` bullet from the moment these files were authored.

## Solution approach

Delete the trailing rationale sentence ("The guarantee rests on the runtime having no compensating/rollback path…") from the `ERR-13` `Tests.` bullet in both `V4f-no-rollback.md` and `V4f-T-no-rollback.md`, leaving the enumerated authoring-site vectors and harness/seam modelling intact. No replacement is needed — the no-rollback-is-architectural rationale already lives in `V4f`'s `Adds.` field.

## Solution constraints

- Out of scope: the parallel rationale duplication in `V4f`'s `Ships when` gate, owned by F-1918.
- Keep the two paired files byte-identical in the `ERR-13` bullet body, per the paired-leaf consistency rule in `## Plan rules`.

## Relationships

- F-1918 "Architectural rationale duplicated into Ships-when" — same-cluster (same `V4f` leaf, a parallel duplication of the no-rollback rationale into the `Ships when` gate; resolves independently by striking the trailing sentence there)
