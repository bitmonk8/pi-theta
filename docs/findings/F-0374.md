---
id: F-0374
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9b-T-registration-reload-wiring.md, anchor: "Deps field" }
lens: [plan-lens-ordering]
tier: medium
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [ordering]
title: "`V9b-T` asserts the V6a model-reference-matcher seam but omits V6a from its Deps"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

`V9b-T`'s third Tests bullet ("model-reference-matcher production wiring") asserts the model-reference-matcher injection seam and the `findExactModelReferenceMatch` symbol, both production symbols owned by `V6a`. Its `Deps` field is `V9a, V10a, V8e`, none of which reach `V6a` transitively, so `V9b-T` becomes pickable before `V6a`'s injection-seam interface is declared. The suite would then reference a not-yet-declared production symbol and fail to type-check — a compile-time red that the conventions.md "Fail red for the intended reason" gate disqualifies, so `V9b-T`'s Ships-when cannot fire correctly. The paired implementation leaf `V9b` already lists `V6a` in its `Deps`; the tests-task leaf does not.

## Issue introduction

**Verdict:** single-commit
**Introducing commit:** `ccedadf` — "pi-loom plan: resolve \"V9b model-reference-matcher wiring untested\"" (2026-06-16)
**History:** Before `ccedadf`, the paired impl `V9b` already carried `V6a` in its Deps (`V9b-T, V9a, V10a, V8e, V6a`) and `V9b-T`'s Deps was `V9a, V10a, V8e`; neither leaf yet mentioned the model-reference matcher. `ccedadf` added the "model-reference-matcher production wiring" Tests bullet — which references the `V6a` injection seam, `findExactModelReferenceMatch`, and `loom/load/model-unresolved` — to **both** `V9b` and `V9b-T` (then named `*-registration-drain-state.md`), but updated the Deps only on `V9b` (already correct) and left `V9b-T`'s Deps unchanged at `V9a, V10a, V8e`. `git log -S 'findExactModelReferenceMatch' --follow` over the leaf confirms `ccedadf` is the sole commit that introduced the V6a reference into the tests-task leaf; the asymmetry — V6a-referencing assertion added without the matching V6a dep edge — entered at that commit.

## Solution approach

Add `V6a` to `V9b-T`'s `Deps` field in `docs/plan_topics/V9b-T-registration-reload-wiring.md`, so the tests-task leaf's dependency set covers the seam its third Tests bullet asserts, mirroring the paired `V9b`.

## Solution constraints

- None.

## Relationships

- F-0373 "Bundles an orthogonal model-reference-matcher wiring concern" — decision-dependency (if the model-matcher wiring + V6a injection is extracted to a sibling leaf, the V6a dep edge would land on that leaf's tests-task instead of `V9b-T`; resolve the extraction question first, then place the dep)
- F-0372 "injectReloadFailure` arm-selector signature is prose-only / undefined" — same-cluster (same V9b/V9b-T pair, resolves independently)
