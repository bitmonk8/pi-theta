---
id: F-2379
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9g-session-shutdown.md, anchor: "Deps bullet" }
lens: [plan-lens-ordering]
tier: low
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [ordering]
title: "`V9g` omits the `V9b` Deps edge despite back-filling V9b's step-4 `session_shutdown` subscription"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---
## Finding

`V9b` registration step 4 subscribes a `session_shutdown` handler (`pi.on("session_shutdown", handler)`) as the single teardown contract for the outgoing extension instance. `V9g` then authors the full five-sub-step teardown sequence that this handler runs — abort-and-await of in-flight invocations, per-step isolation, the bounded `Promise.allSettled` settle, the wrapped `loom/host/*` emissions — i.e. `V9g` back-fills the body of the handler that `V9b` step 4 stubs in. This is a genuine producer→consumer ordering relationship: `V9g`'s work only makes sense relative to the subscription point `V9b` establishes.

`V9g`'s `Deps.` is `V9g-T, V9a, V9e, V9h, V17a` — `V9b` is absent. The edge is currently satisfied only transitively (`V9g → V9e → V9b`, and also `V9g → V9h → V9m → V9b`), so the dep-driven build order happens to sequence `V9b` ahead of `V9g` today. The direct relationship is undeclared, which makes the ordering fragile: any restructuring that drops or re-routes the `V9e`/`V9h` edges would silently un-sequence `V9b` before `V9g` without any leaf-level signal.

The paired tests-task `V9g-T` carries `Deps. V9e, V9h, V17a` and likewise omits `V9b`, relying on the same transitive satisfaction.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `c6a664e` ("pi-loom plan: build/update plan for spec.md + review", 2026-06-10)
**History:** The plan corpus is git-tracked. `git log --diff-filter=A` shows both leaves first authored in `c6a664e`: `V9g` (then `V9g-session-shutdown.md`) was created with `Deps. V9g-T, V9e, V9h, V17a` and the five-sub-step `session_shutdown` handler already present, while the `V9b` leaf (then `V9b-registration-drain-state.md`) was created in the same commit with registration steps 1–5 including the step-4 `session_shutdown` subscribe. `git log -p --follow` over `V9g-session-shutdown.md` shows the Deps line was later edited only to add `V9a` (commit `a6e2ecb`); `V9b` has never appeared in `V9g`'s Deps. The undeclared `V9g→V9b` edge — and its transitive satisfaction through `V9e→V9b` — has therefore existed unchanged since the plan's initial build.

## Solution Space

**Shape:** single

### Recommendation

In `docs/plan_topics/V9g-session-shutdown.md`, add `V9b` to the `Deps.` bullet so the line reads `V9g-T`, `V9a`, `V9b`, `V9e`, `V9h`, `V17a`, with a parenthetical noting that `V9g`'s teardown sequence is the body of the `session_shutdown` handler `V9b` step 4 subscribes. Cite `V9b` by its existing ID exactly as other leaves reference it.

For parity, the same `V9b` edge may be added to `V9g-T`'s `Deps.` in `docs/plan_topics/V9g-T-session-shutdown.md`, since the tests task exercises the same back-filled handler against the subscription `V9b` establishes.

The spec is read-only for this fix; no `spec_topics/**` page changes. The edit is confined to the two plan leaf files' `Deps.` bullets.

## Consequence

**Severity:** advisory

The dep-driven build order is correct today because `V9e→V9b` (and `V9h→V9m→V9b`) transitively sequence `V9b` before `V9g`, so the leaf remains pickable and ships a working teardown handler. The defect is fragility, not breakage: the direct producer→consumer relationship between `V9g`'s handler body and `V9b`'s subscription point is invisible at the leaf level, so a later edit that re-routes the `V9e`/`V9h` edges could re-order `V9b` after `V9g` with no signal.

## Relationships


- F-2368 "Un-enumerated direct dependency on V9m's drain-state API (sub-step 1)" — same-cluster (also a missing direct edge on `V9g`'s Deps — `V9m` — resolves independently with its own edge)
- F-1928 "Bounded await relies on the injected Clock seam, not enumerated" — same-cluster (also a possibly-missing `V9g` Deps edge — `V8d` — resolves independently)
