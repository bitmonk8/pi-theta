---
id: F-1771
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/expressions.md, anchor: "Grammar disambiguation §\"Struct-expression in scrutinee position\"" }
lens: [spec-lens-clarity]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: triaged
category: [clarity]
title: "Struct-expression in scrutinee position — parenthesisation rule ambiguous"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`docs/spec_topics/expressions.md` §"Grammar disambiguation" states that "these positions require parentheses around any constructor" and gives the example `if (Author { name: "x", role: "r", experience_years: 0 } == author) { ... }`. The prose and the example describe two different rules and a parser implementer cannot tell which is normative:

- **Rule A (wrap the whole condition).** Whenever a scrutinee contains a constructor, the entire scrutinee expression must be parenthesised. The example satisfies this — the outer parens enclose the whole `Author { ... } == author` comparison. Under A, the bare form `if Author { ... } == author { ... }` is rejected and the directly-parenthesised form `if (Author { ... }) == author { ... }` is also rejected (the constructor sits in the scrutinee but the scrutinee as a whole is not parenthesised).
- **Rule B (wrap each constructor).** The constructor itself must be directly enclosed by parens, so that its `{` cannot collide with the body `{`. Under B, the minimal legal form is `if (Author { ... }) == author { ... }`; the form `if a == (Other { ... }) { ... }` is admitted; the example's wider parens are over-parenthesisation, not the rule the prose names.

The grammar production `IfStmt ::= "if" Expr StmtBlock` in `docs/spec_topics/grammar.md` §"Block expressions" admits a bare `Expr` for the condition (the lexical-structure prose only outlaws single-line bodies via `loom/parse/single-line-if`, not bare conditions), and `docs/spec_topics/control-flow.md`'s own example writes `if author.experience_years < 2 { ... }` with no outer parens. That demonstrates a bare condition is legal in general — so the scrutinee rule is an *added* requirement that fires only when a constructor is present, and the spec must say whether it fires at the whole-condition level (A) or at each constructor (B).

Two implementers will diverge: one writes the parser to demand an outer paren-expression directly under `if` / `while` / `match` / `for` whenever any descendant is a constructor (A); the other writes it to demand a paren-expression directly enclosing each constructor token in scrutinee position (B). Both are defensible readings of the current text; neither matches the example without leftover oddities.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `fae85d3` ("Tighten spec for implementation-plan readiness", 2026-05-04)
**History:** The "Grammar disambiguation" section, including the *Struct-expression in scrutinee position* bullet with its current prose ("These positions therefore require parentheses around any constructor") and example (`if (Author { … } == author) { … }`), was added in `fae85d3` when the spec was tightened for implementation-plan readiness. The prose/example mismatch entered the corpus with the rule and was preserved verbatim through the `fecb504` split that moved `spec.md` into per-topic shards under `spec_topics/`. No subsequent commit has touched this bullet (`git log -S "parentheses around any constructor"` returns only the introducing commit and the split).

## Solution Space

### Recommendation

In `expressions.md` §"Grammar disambiguation", make the constructor-parenthesisation rule local to each constructor. Change the rule sentence to: "Every constructor that appears in such a position must be directly enclosed by parens: `(Schema { ... })`." Rewrite the example to the minimally-parenthesised form `if (Author { name: "x", role: "r", experience_years: 0 }) == author { ... }`, where the parens directly enclose the constructor rather than the whole condition. Add one sentence stating that an outer paren around the whole condition is not required and does not by itself satisfy the rule for a constructor it merely contains.

Under this rule the scrutinee as a whole need not be parenthesised; bare conditions remain admissible (`if x < 2 { ... }`, `if a == (Other { ... }) { ... }`). The parser, on encountering a constructor token in scrutinee position (the condition of `if` / `while`, the scrutinee of `match`, or the iterated expression of `for`), demands that the constructor itself be directly wrapped in parens so its `{` cannot collide with the body `{`.

Edge cases the implementer must watch: (i) nested constructors (`Outer { inner: (Inner { ... }) }` in scrutinee position — only the *outermost* constructor of the scrutinee subterm needs the parens, since the inner constructor's braces are already inside the outer constructor's body and not in body-brace-collision position); (ii) constructors inside `${...}` template interpolations within a scrutinee `@`…`` query (the interpolation's own `{` boundary already disambiguates and no extra parens are required); (iii) constructors inside a `()` call-argument list within a scrutinee (the call's parens already disambiguate; the rule does not double-fire). The new diagnostic for a bare constructor in scrutinee position should reuse or sit beside the existing `loom/parse/single-line-if` neighbourhood in `code-registry-parse.md`, naming the construct ("constructor in `if` / `while` / `match` / `for` scrutinee must be parenthesised"). The diagnostic for an unparenthesised constructor inside an otherwise-parenthesised condition should be specific enough that the fix is obvious.

## Consequence

**Severity:** correctness

A parser written to rule A will reject inputs a parser written to rule B accepts (and vice-versa, for the directly-parenthesised-only form). The disagreement is mechanical and silent — the same source file produces different parse results on different conforming implementations, and the spec offers no diagnostic code to disambiguate.

## Relationships

