---
id: F-0429
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan.md, anchor: "" }
lens: [plan-lens-validation]
tier: nit
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [validation]
title: "no-`unregisterTool` negative invariant has no explicit check"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

The rule asserts the runtime never calls a non-existent `pi.unregisterTool`; self-enforcing (nothing to call), no Tests row gates it.

**Suggested fix:** Optional surface-inventory assertion that the Pi-call set contains no `unregisterTool` reference (guards future SDK-shape regression).

## Issue introduction

**Verdict:** indeterminate
Issue-introduction analysis was not available (L1 pre-fan-out confident-drop; enrichment skipped).

## Solution Space

**Shape:** single

## Consequence

**Severity:** nit

## Relationships

None
