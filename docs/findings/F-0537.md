---
id: F-0537
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9j-binder-inference-provider-mapping.md, anchor: "Spec field" }
lens: [spec-lens-implementability]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [implementability]
title: "V9j Spec field omits the normatively cross-linked pages its own mechanisms depend on"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---

## Finding

`V9j`'s **Spec** field lists exactly two pages — `binder-inference.md` and `provider-error-mapping.md`. The plan's reading discipline (How-to-use step 3) instructs an implementer to "read only the leaf and the spec topic(s) listed under its **Spec** field", so that two-page set is the closed reading corpus for the leaf. But the mechanisms `V9j` **Adds** — the forced-tool `complete()` envelope, `temperature:0`, the fixed-seed placement, and the malformed-envelope extraction — are pinned in pages outside that set, reached only by following normative cross-links *inside* `binder-inference.md`:

- **`temperature:0` and the fixed-seed value** are pinned at `binder/determinism-cancellation-failure.md` §Determinism. The seed *field-name* mapping is covered by the listed `provider-error-mapping.md`, but the seed *value* and the `temperature:0` constant are not.
- **The forced-tool / `options.toolChoice` mechanism** `V9j` uses to force the single binder tool is pinned at `pi-integration-contract/conversation-drive.md#complete-forced-tool-presupposition` (the per-provider `{ type: "tool", name }` → provider-native-payload behavioural presupposition). `binder-inference.md` defers to that anchor; nothing in `V9j`'s reading set carries it.
- **The malformed-envelope condition** (an `AssistantMessage` with no matching `ToolCall`) that `V9j`'s extraction step keys on is classified by `binder/determinism-cancellation-failure.md` §Failure-class taxonomy.
- **The binder envelope schema** that the forced tool's `parameters` wraps is owned by `binder/binder-bypass-and-envelope.md` (closed by `V11c`), and the system-prompt structure by the same page (closed by `V11d`).

An implementer honouring the closed-reading discipline lacks the determinism constants, the `toolChoice` forcing contract, and the malformed-envelope classification — all directly load-bearing for the call `V9j` constructs. The leaf either needs those pages added to its **Spec** field, or an explicit statement that the corresponding obligations are received as inputs owned by other leaves (`V11c` envelope schema, `V11d` system prompt, `V11f` failure taxonomy / retry budget).

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `c6a664e` (2026-06-10, "pi-loom plan: build/update plan for spec.md + review")
**History:** `docs/plan_topics/V9j-binder-inference-provider-mapping.md` was created in a single commit (`git log --follow` returns only `c6a664e`); its **Spec** field has listed exactly `binder-inference.md` and `provider-error-mapping.md` since the file first existed (`git log -L` over the `**Spec.**` line shows only the creating diff). The gap is original to the leaf, not introduced by a later edit.

## Solution Space

**Shape:** multiple

### Option A — Expand V9j's Spec field to its transitive normative closure

**Approach:** Add `binder/determinism-cancellation-failure.md`, `binder/binder-bypass-and-envelope.md`, and `pi-integration-contract/conversation-drive.md#complete-forced-tool-presupposition` to `V9j`'s **Spec** field, so the listed reading set covers every page its mechanisms depend on.

**Plan edits:** `V9j` **Spec** field gains three link entries.

**Spec edits:** None.

**Pros:** Single self-contained reading set; honours the How-to-use step-3 closed-world discipline literally; independent of any producer/consumer Deps wiring.

**Cons:** Re-lists pages already owned by `V11c` / `V11d` / `V11f`, implying `V9j` shares ownership of obligations it only consumes; widens the leaf's nominal surface.

**Risks:** A future spec re-organisation of those pages requires a manual `V9j` **Spec**-field update with no mechanical guard.

### Option B — Add only the directly-implemented pages and declare the rest as received inputs

**Approach:** `V9j` directly sets `temperature:0`, the fixed seed, and forces the tool, so add `binder/determinism-cancellation-failure.md` (§Determinism) and `conversation-drive.md#complete-forced-tool-presupposition` to the **Spec** field. For the envelope schema and the failure taxonomy / retry budget, add a note (in **Adds** or **Deps**) stating those obligations are received as inputs from `V11c` (envelope schema), `V11d` (system prompt), and `V11f` (failure taxonomy / retry budget).

**Plan edits:** `V9j` **Spec** field gains two entries; a received-inputs note names `V11c` / `V11d` / `V11f` as the owning leaves.

**Spec edits:** None.

**Pros:** Respects the leaf-ownership boundaries the plan already establishes (the `cka-34` / `cka-35` rows close only `binder-inference.md` and `provider-error-mapping.md` for `V9j`; `V11c`/`V11d`/`V11f` carry the other pages); keeps the reading surface minimal; co-resolves with the sibling unwired-seam finding.

**Cons:** Requires the producer→consumer relationship to `V11c`/`V11d`/`V11f` to be wired (currently absent — see Related Findings); more moving parts than Option A.

**Risks:** If the sibling wiring finding resolves the Deps differently, the received-input note could drift from the actual edges.

### Recommendation

Option B. It aligns with the ownership boundaries the plan already encodes — `V11c`/`V11d`/`V11f` own `binder-bypass-and-envelope.md` and `determinism-cancellation-failure.md` (their `cka` rows confirm it), so re-listing those pages on `V9j` (Option A) would duplicate ownership rather than fix the gap. Resolve the sibling "context-assembly seam is unwired" finding first, so the received-input note lands on a stable **Deps** baseline. Concretely: add `binder/determinism-cancellation-failure.md` and `conversation-drive.md#complete-forced-tool-presupposition` to `V9j`'s **Spec** field (`V9j` reads them directly to set `temperature:0`, the fixed seed, and the forced tool), and add a received-inputs note naming `V11c` (envelope schema), `V11d` (system prompt), and `V11f` (failure taxonomy / retry budget) as the owning leaves. Edge case: do not re-list the seed *field-name* mapping — it is already covered by the listed `provider-error-mapping.md`; only the fixed-seed *value* and `temperature:0` come from the determinism page.

## Consequence

**Severity:** correctness

An implementer following the closed-reading discipline implements `V9j`'s `complete()` call without the determinism pins (`temperature:0`, fixed seed), the per-provider `toolChoice` forcing contract, or the malformed-envelope classification, because none reside in the two listed pages. Two reasonable implementers would diverge — one following the in-page cross-links, one not — and the non-following implementation would silently miss spec-mandated determinism/forcing behaviour.

## Relationships

- F-0536 "V9j — binder `complete()` context-assembly seam is unwired (no producer→consumer edge) (implementability)" — decision-dependency (its Deps-wiring fix determines whether the received-input declaration of Option B is available; same leaf)
- F-0539 "V9n — Adds (V9j) vs Tests (V6a) name different delegation chains for the `provider` field (clarity)" — same-cluster (touches `V9j`'s provider-mapping surface; resolves independently)
