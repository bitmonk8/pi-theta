---
id: F-1699
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/host-prerequisites.md, anchor: "*Pi SDK pin* item 1, \"belt-and-braces\" sentence in the lock-step paragraph; *Deliberate deviation from Pi's bundled-package convention* sub-paragraph" }
lens: [spec-lens-assumptions]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [assumptions]
title: "pnpm / yarn \"no auto-dedup\" claim is the load-bearing rationale for the tilde-pin deviation but is unverified"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

`host-prerequisites.md` §`#pi-sdk-pin` justifies loom's deliberate deviation from Pi's `docs/packages.md` `"*"` convention — the four `@earendil-works/*` peers are tilde-pinned at the loom 1.0 Pi-SDK pin range instead of `"*"` — on the strength of a single behavioural claim about third-party package managers:

> `pnpm` (in its default `node-linker=isolated` mode) and `yarn` do not auto-deduplicate transitive peer-dep ranges, so a tilde-range pin in loom's `peerDependencies` causes the install to fail loudly when the host has resolved any of the four `@earendil-works/*` packages outside the pinned minor line, whereas a `"*"` declaration would silently accept the skew and defer the failure to a runtime `TypeError` at the first SDK call site.

The same claim recurs in the preceding "belt-and-braces" sentence of the lock-step paragraph and is the entire load-bearing benefit cited for accepting the deviation's documented cost ("gives up Pi-prescribed alignment"). It is the *only* reason given for not collapsing the four entries back to the `"*"` Pi convention.

The claim is stated as fact about the current behaviour of two named third-party tools, yet it carries no citation, no pnpm or yarn version qualifier (pnpm has shipped multiple `node-linker` modes; yarn `1.x` "classic" and yarn `2.x+` "Berry"/PnP resolve peer dependencies via materially different algorithms), and no fixture or build-time test pinning the behaviour to a regression check. If either tool deduplicates more aggressively than the spec asserts — or did at some point in the supported range — the install-time "fail loudly on skew" property the rationale promises never materialises and the deviation buys nothing in exchange for the alignment cost it documents itself paying.

The deviation does have other defensive gates (Step 0 (d)'s runtime peer-dep probe in `capability-probe.md` and the build-time `peerDependencies` literal-read assertion gated by [Pi version bump procedure — step 2](./version-bump-intro.md#pi-version-bump-procedure)), but the spec text positions those as *consequences* of the four-anchor invariant the install-time PM gate is supposed to underwrite, not as substitutes for it. The rationale paragraph therefore cannot be deleted without leaving the deviation un-justified, and cannot stand as written without being verified.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:**
- `0f466e6` (2026-05-06, "pi-loom spec: resolve \"lock-step minor invariant asserted without provenance\"") — first introduces the belt-and-braces sentence ending "…surfaces a clear install-time error under package managers (pnpm/yarn) that do not auto-deduplicate transitive peer-dep ranges". The PM behavioural claim enters the corpus here, with no citation, no version qualifier, and no test.
- `d8da74b` (2026-05-08, "pi-loom spec: resolve \"peerDependencies ranges silently deviate from Pi documented * convention\"") — adds the *Deliberate deviation from Pi's bundled-package convention* sub-paragraph, which restates and strengthens the same claim with a more specific shape ("`pnpm` (in its default `node-linker=isolated` mode) and `yarn` do not auto-deduplicate transitive peer-dep ranges") and elevates it to the load-bearing justification for the documented deviation. Still no citation; the added `node-linker=isolated` qualifier is itself unsourced.

**History:** The `02b11c9` (2026-06-25, "fix F-1348 — coin PIC-33..35 on host-prerequisites.md + repoint cka-21 to PIC-35") commit later coined PIC-33 / PIC-34 / PIC-35 around the same paragraph but did not edit the PM claim, so PIC-33/34 inherited the unverified rationale rather than introducing it. The corpus reorganisation `81bf52a` (2026-06-11, "docs: collapse spec/ section pages into spec_topics/") only relocated the text. Two distinct commits each authored unverified PM-behaviour text that now jointly carries the deviation's rationale; neither commit alone produces the current load-bearing form, so the defect is properly attributed to the interaction.

## Solution Space

**Shape:** single

### Recommendation

In the *Deliberate deviation from Pi's bundled-package convention* sub-paragraph of `host-prerequisites.md`§`#pi-sdk-pin`, demote the package-manager install-time behaviour to a non-load-bearing observation and rest the deviation's skew-detection on the two gates the corpus already mandates and tests.

Replace the load-bearing PM-behaviour sentence with:

> Install-time enforcement is package-manager-dependent and non-load-bearing; the build-time `peerDependencies` literal-read assertion ([`version-bump-intro.md` step 2](./version-bump-intro.md#pi-version-bump-procedure)) and the runtime Step 0 (d) peer-dep probe ([`capability-probe.md`](./capability-probe.md#entry-capability-probe)) are the two load-bearing gates that detect skew across the four `@earendil-works/*` peers; under package managers that do not auto-deduplicate transitive peer-dep ranges (observed under pnpm and yarn in practice; not pinned by this spec) the tilde range additionally causes the install itself to fail loudly, which is a useful bonus but not relied on.

Foreground the deviation's *primary* value as the four-anchor invariant the two pinned gates need: the build-time literal-read assertion requires a literal range to assert against, and a `"*"` declaration would defeat that assertion. State this four-anchor argument as the deviation's primary benefit in its own right — do not bury it in the same sentence as the PM-side note, and do not let the rewrite collapse to "install-time enforcement is non-load-bearing", which would mislead a future reader into thinking the deviation has no value and the `"*"` convention can be safely adopted.

Delete or downgrade the parallel PM-behaviour claim in the "belt-and-braces" sentence of the lock-step paragraph in the same edit so the demoted framing is consistent across the paragraph; leaving the old wording there while the deviation sub-paragraph demotes the claim re-creates dual-authority drift. Harmonise the phrasing with `spec.md`'s orientation block, which already treats `peerDependencies` install-time enforcement as package-manager-dependent (e.g. `npm install --engine-strict` is opt-in) and non-load-bearing.

Update the `H1a` *Adds* paragraph's "consumer-facing install-time skew-detection contract" phrasing in `plan_topics/H1a-scaffold-and-toolchain.md` in the same commit so the plan leaf no longer asserts an install-time skew-detection property the spec no longer claims; rewrite its one-line characterisation to point at the two load-bearing gates instead.

## Consequence

**Severity:** advisory

The current text reads as a verified behavioural statement about third-party tools and is the sole stated benefit for accepting a documented deviation from Pi's bundled-package convention. If the claim is wrong (or is right only under unstated PM versions / modes), a future maintainer auditing the deviation against `packages.md` finds a documented cost paid for a benefit that does not exist, and the natural fix — re-aligning the four entries to `"*"` — silently removes whatever install-time signal does happen to exist today. The build still ships because the runtime Step 0 (d) probe and the build-time literal-read assertion are independent gates; the cost is to maintainer reasoning, not to loom 1.0 runtime behaviour.

## Relationships

- F-1535 "Clause (a) / version-bump-step2 item (a) is an embedded decision-log / unresolved-TBD" — same-cluster (sits on the same `host-prerequisites.md` page but on a different sub-section; resolves independently)
