---
id: F-0354
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V4e-pre-evaluation-failures.md, anchor: "leaf body (Tests, Deps, Ships when)" }
  - { path: docs/plan_topics/V4e-T-pre-evaluation-failures.md, anchor: "paired tests leaf" }
  - { path: docs/plan_topics/coverage-matrix.md, anchor: "ERR-1 … ERR-7, ERR-16, CIO-1 rows" }
  - { path: docs/plan_topics/H5b-warn-only-canary.md, anchor: "Deps" }
  - { path: docs/plan.md, anchor: "§V4 — Errors and results index list" }
lens: [plan-lens-step-atomicity]
tier: medium
factors:
  blast_radius: moderate
  centrality: core
  core_premise_blocker: no
status: triaged
category: [step-atomicity]
title: "`V4e` bundles a heterogeneous reload-integration scenario (ERR-7) with seven routing checks"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---

## Finding

`V4e` packages all eight closed pre-evaluation failure causes — `ERR-1`…`ERR-7` plus `ERR-16` — under a single `Ships when` gate ("`npm test` proves all eight pre-eval causes route to `loom-system-note` without firing a turn"). Seven of these (`ERR-1`…`ERR-6`, `ERR-16`) are self-contained routing assertions: each drives one synthetic pre-eval failure and checks that it surfaces on the `loom-system-note` channel with `triggerTurn:false`. `ERR-16` additionally consults `V16a`'s cross-ceiling arbitration seam and wraps a `V5e` depth-walk breach, but it remains a single load-time routing check.

`ERR-7` is a different complexity class. It is a watcher-time reload integration scenario that injects a synthetic failure through `V9b`'s `ReloadFailureInjector.injectReloadFailure` interface and must exercise both the `V9b`-owned arms (the `loom/runtime/registry-swap-failed` registry-swap arm and the `.loom`/`.warp` re-parse arm) and `V10d`'s settings-re-merge arm simultaneously. It is the sole reason `V4e` carries `Deps` on `V9b` and `V10d`; the other seven causes need none of that wiring.

Because one `Ships when` gate covers all eight causes, progress on the seven routing checks is unobservable until the `ERR-7` reload-injection harness is also stood up, and a single-pass review must hold the routing taxonomy and the multi-arm reload-injection seam in scope at once.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commit:** `c6a664e` (2026-06-10, "pi-loom plan: build/update plan for spec.md + review")
**History:** `V4e` has bundled the full pre-evaluation failure set (`ERR-1`…`ERR-7` + `ERR-16`) under the "all eight pre-eval causes" `Ships when` since the leaf was first authored in `c6a664e`. Later commits (`49e3837`, `66acde6`, `b2d5a1f`, `d64dce5`, `fdce2a2`, all 2026-06-11) did not introduce the bundling; they progressively deepened `ERR-7`'s integration burden — pinning the `ReloadFailureInjector` seam, its single owner, and the registry-swap / re-parse / settings-re-merge arms — which sharpened the heterogeneity between `ERR-7` and the seven routing checks but left the single-leaf structure unchanged.

## Solution Space

**Shape:** multiple

This finding carries two obligations: a leaf-corpus split and a propagation pass through the coverage matrix and `H5b`'s Deps. Resolve them in order — the split first (it fixes the new leaf set), then the propagation on that stable baseline.

### Option A — Split the `V4e` leaf-pair (scope-bounding; resolve first)

**Approach.** Separate the seven routing checks from the `ERR-7` reload-integration scenario into two implementation leaves, each with its `-T` partner.

**Plan edits.**
- In `V4e-pre-evaluation-failures.md` and `V4e-T-pre-evaluation-failures.md`: keep `ERR-1`…`ERR-6` and `ERR-16`; remove the `ERR-7` Tests bullet; drop `V9b` and `V10d` from `Deps` (they exist only for `ERR-7`), retaining `V9a`, `V6a`, `V11f`, `V10a`, `V16a`, `V5e`. Reword `Ships when` from "all eight pre-eval causes" to the seven routing causes it now owns (`ERR-1`…`ERR-6`, `ERR-16`).
- Create a new `<new>` implementation leaf (and `<new>-T`) owning `ERR-7`, with `Deps` `V9b`, `V10d` (plus its `-T`), `Spec.` citing `error-model.md`, and a `Ships when` scoped to the single watcher-time reload-injection cause. Add the navigation bullet for it in `docs/plan.md` §V4.

**Spec edits.** None.

**Pros.** Routing leaf ships independently of the reload-injection harness; partial completion becomes observable; each leaf's review surface shrinks.

**Cons.** Adds one leaf + `-T` pair and a new leaf-ID allocation.

**Risks.** The new leaf ID must honour the project's leaf-ID scheme; leave it as the literal `<new>` placeholder until the implementer allocates a real ID (`no-invented-ids`).

### Option B — Propagate the split through the coverage matrix and `H5b` (resolve second)

**Approach.** Retarget every coverage-matrix and `H5b` reference of `V4e` to the post-split leaf set so the transitive-completeness gate stays green.

**Plan edits.**
- `coverage-matrix.md`: split `ERR-1 … ERR-7 | V4e` into `ERR-1 … ERR-6 | V4e` (routing leaf) and `ERR-7 | <new>` (reload leaf). `ERR-16 | V4e` and the `V4e` member of `CIO-1 | V16a, V4e, V11f` stay the routing leaf, since `ERR-16`'s slash-load consult lives there.
- `H5b-warn-only-canary.md`: ensure both post-split leaf IDs are reachable from `Deps` so `H5d`'s per-cell at-least-one check stays green for the new `ERR-7 | <new>` row (extend the `V4a`–`V4f` range or add the new leaf as a named entry, depending on the allocated ID).

**Spec edits.** None.

**Pros.** Coverage rows name real closing leaves; the transitive-completeness gate continues to back per-cell coverage.

**Cons.** Cannot land meaningfully before Option A fixes the new leaf IDs.

**Risks.** `H5d` tokenises the closing-leaf cell by backtick-delimited spans; the new leaf ID must be backtick-delimited in the matrix cell or it is silently dropped from the gate's set.

### Recommendation

Resolve Option A first — it bounds the new leaf IDs and the `ERR` partition — then Option B on that stable baseline. Retaining `V4e`'s existing ID for the routing leaf minimizes coverage-matrix and `H5b` churn. Watch that the `ERR-7` reload leaf's ID, if it falls outside `H5b`'s `V4a`–`V4f` range, is added to `H5b`'s Deps explicitly so the new `ERR-7` coverage row stays reachable.

## Consequence

**Severity:** advisory

A single `Ships when` over eight heterogeneous causes makes partial completion unobservable — the seven routing checks cannot be demonstrated done until the `ERR-7` watcher-time reload-injection harness (spanning `V9b`'s two arms and `V10d`'s settings-re-merge arm) is also wired, and the leaf cannot be picked up incrementally. The leaf is still implementable, so this degrades reviewability and incrementality rather than blocking it.

## Relationships

- F-0337 "Too large: 7+ distinct gate arms, each with independent recogniser/fixture; partial completion unverifiable" — same-cluster (sibling step-atomicity split, resolves independently)
- F-0336 "Too large: 7-category injection API + open design decision + protocol modelling for (f)/(g)" — same-cluster (sibling step-atomicity split, resolves independently)
- F-0350 "Too large: three independent type namespaces in one leaf" — same-cluster (sibling step-atomicity split, resolves independently)
- F-0319 "Facet-vs-co-witness partition not mechanically derivable for un-annotated multi-leaf cells (CIO-1, CIO-5)" — decision-dependency (both edit the `CIO-1` row; retargeting `V4e`→routing leaf constrains that finding's annotation fix)
