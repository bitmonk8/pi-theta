---
id: F-1801
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/pi-integration-contract/session-only-degraded-state.md, anchor: "*Acceptance criteria* list" }
lens: [spec-lens-prescription, spec-lens-testability]
tier: medium
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: reduced
category: [prescription, testability]
title: "Add the captured-value-vs-fresh-read witnessing fixture"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---
## Problem

The load-bearing captured-value protection — the discriminator reads the handler-scoped `capturedEventReason`, not a fresh `event.reason` access, as the **Seam minimality (normative)** captured-string-triplet pin requires — has no acceptance fixture covering a getter that yields a valid session-only reason on first read then throws on re-access. Fixture (d) covers only the unknown / `"<unreadable>"` path, so a non-conformant fresh-read implementation passes every current happy-path fixture.


## Issue introduction

**Verdict:** introduced-by-commit
**Introducing commits:** 52b9101
**History:** The entire session-only degraded-state branch — the *Predicate split* / "Seam minimality (normative)" prose and the per-branch acceptance fixtures — was authored wholesale in `52b9101` (2026-05-11, "resolve T24 — Fork-reason watcher closure leaves the extension in an unspecified, silently degraded state"). `git log -S` on `handler-scoped state seam beyond`, `captured-string triplet`, and `Predicate split` against the then-current `docs/spec_topics/pi-integration-contract.md` shows none of these tokens existed before `52b9101`; the internal-seam-prohibition MUST NOT and the acceptance list that omits a captured-value-vs-fresh-read fixture both entered the corpus in that single commit. The later split commit `f5e89f4` (2026-06-04) only condensed and relocated this content from `pi-integration-contract.md` into the standalone `session-only-degraded-state.md` page; it preserved both the prohibition and the fixture omission verbatim and so did not introduce the defect.

## Solution approach

Add an acceptance fixture to the *Acceptance criteria* list (the next free letter is `(n)`) witnessing the captured-string-triplet protection: an `event.reason` getter that returns a session-only member on the handler-entry capture read and throws on any subsequent access. Assert that the handler emits exactly one `runtime-degraded` row whose `details.event.reason` is the captured value, that no throw escapes the handler, and that the next slash invocation returns `"degraded; /reload to recover"`.

## Solution constraints

- Out of scope: narrowing the **Seam minimality (normative)** prose (owned by F-1800).

## Relationships

- F-1800 "Narrow \"Seam minimality (normative)\" to the observable contract" — same-cluster (resolves independently; adding the fixture does not narrow the over-prescriptive prose)
- F-1685 "PIC-30 all-three-throw corner case has no acceptance fixture" — same-cluster (parallel missing-fixture gap in the same degraded-state machine on the sibling drain-state-contract page)
