---
id: F-0416
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V4c-terminal-outcomes.md, anchor: "Tests (ERR-12) / Adds deferral note" }
  - { path: docs/plan_topics/V4f-no-rollback.md, anchor: "Tests (ERR-13) deferral note" }
  - { path: docs/plan_topics/V9i-subagent-isolation.md, anchor: "Tests" }
  - { path: docs/plan_topics/V13c-query-tool-loop.md, anchor: "Tests" }
  - { path: docs/plan_topics/V14a-tool-calls.md, anchor: "Tests" }
  - { path: docs/plan_topics/V15a-invocation-core.md, anchor: "Tests" }
  - { path: docs/plan_topics/coverage-matrix.md, anchor: "ERR-8…ERR-12 / ERR-13 rows" }
lens: [plan-lens-validation, spec-lens-assumptions]
tier: medium
factors:
  blast_radius: broad
  centrality: core
  core_premise_blocker: no
status: reduced
category: [validation, assumptions]
title: "V4c/V4f defer ERR-8…ERR-13 live-surface confirmation to downstream owners that never re-assert it"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: reduced
---

## Problem

`V4c` (ERR-8…ERR-12) and `V4f` (ERR-13) exercise their criteria only against the `H4a` session double and the `V17a` side-effect seam; their Tests bullets explicitly defer live-surface confirmation to downstream owners — ERR-12/ERR-8 to the live `V9i` subagent surface, and ERR-13 completed-callee finality to the live `V14a`/`V13c`/`V15a` surfaces. None of those downstream leaves re-asserts the deferred contract: `V9i` never re-drives ERR-8 (cancellation does not mutate committed turns) through the real subagent `AgentSession`, and `V14a`/`V13c`/`V15a` carry no completed-callee no-rollback bullet. Yet `coverage-matrix.md` maps ERR-8…ERR-12 → `V4c` and ERR-13 → `V4f` as the sole closing leaves, so the closing gate counts these REQ-IDs covered while the promised live-surface confirmation is never authored — a real subagent-cancellation mutation or completed-callee compensating-path regression can ship with every test green.

## Issue introduction

**Verdict:** single-commit (with later propagation)
**Introducing commits:**
- `e8f0236` (2026-06-11) — "pi-loom plan: resolve \"V4c-T/V4c assert no-rollback over later-slice surfaces\"" — re-pointed ERR-12/ERR-13 onto the `H4a` double and the `V17a` side-effect seam and added the "not the live `V9i` / `V14a`/`V13c`/`V15a` surface" deferral note. The same commit touched only `V4c`/`V4c-T`; it added no downstream live-surface re-assertion to `V9i`/`V14a`/`V13c`/`V15a`.
- `098ec80` (2026-06-17) — "pi-loom plan: resolve \"V4c bundles the ERR-8...ERR-12 trichotomy and the ERR-13 no-rollback proof\"" — split ERR-13 out into the new `V4f`, carrying the unwitnessed deferral note (and the `ERR-13 → V4f` matrix row) forward without adding the downstream witnesses.

**History:** Before `e8f0236`, `V4c` asserted ERR-12/ERR-13 directly over later-slice surfaces (the over-reach a prior review flagged). The `e8f0236` fix corrected that over-reach by deferring the assertions to the double, but left the promised live-surface confirmation unassigned to any downstream leaf; `098ec80` propagated the gap into `V4f`. The `V5e`/`V6d` deferrals named in the same finding entered through separate commits (`3fa39a9`, `3625ee0`) that *did* pair the deferral with downstream live-carrier witnesses, which is why those halves are already closed.

## Solution approach

Apply the delegated-live-carrier-witness pattern the plan already uses for `V5e` to `V4c`/`V4f`'s deferred criteria. Add a Tests bullet (with a `Deps` edge) to `V9i` that drives mid-stream cancellation inside the real subagent `AgentSession` and asserts ERR-8 holds there, and add a Tests bullet (with a `Deps` edge) to each of `V14a`, `V13c`, and `V15a` that drives a callee to completion on that leaf's production surface, fires a downstream `?`/panic/cancel, and asserts the completed callee's side effect persists with no compensating turn. Name the downstream witness leaves in the `V4c` and `V4f` deferral text so the deferral is traceable from both ends. Annotate the ERR-8…ERR-12 and ERR-13 rows in `coverage-matrix.md` with the witness leaves as co-witnesses so the closing gate sees the witness mapping.

## Solution constraints

- Out of scope: the `V5e` and `V6d` deferrals and their existing live-carrier witnesses (`V14e`, `V15h`, `V4e`, `V13a`, and `V13c`'s existing `V5e` routing bullet) are already closed — do not edit them beyond `V13c`'s new ERR-13 witness bullet.
- The spec is read-only for this fix.

## Relationships

- F-0336 "Too large: 7-category injection API + open design decision + protocol modelling for (f)/(g)" — decision-dependency (H4b categories (f)/(g) model the V4f-ERR-13 / V9i subagent scripting points the new live-carrier witnesses consume; how H4b shapes those constrains the witness wiring)
- F-0349 "Runtime AJV safety-net named in Adds/Ships-when but has no asserting step and no Deps" — same-cluster (same asserted-but-not-witnessed-on-a-real-surface validation pattern, different leaf)
- F-0321 "Double-vs-real-host fidelity has no mechanical detection; smoke gate is non-enforced sole backstop" — same-cluster (both concern double-vs-real witnessing gaps; that one is double-vs-real-host, this one is double-vs-live-integrated-surface)
