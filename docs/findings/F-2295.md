---
id: F-2295
corpus: plan
fix_surface: [plan]
location:
  - { path: docs/plan_topics/V9f-tool-registration-lifetime.md, anchor: Tests }
  - { path: docs/plan_topics/V9f-T-tool-registration-lifetime.md, anchor: Tests }
lens: [plan-lens-validation]
tier: low
factors:
  blast_radius: moderate
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [validation]
title: "V9f \"visibility tracks the invocation\" Tests bullet pins no observable"
cycle_first_seen: 1
cycle_last_seen: 1
shape: multiple
state: shaped
---
## Finding

`V9f` and its paired `V9f-T` carry the Tests bullet *"The callable set's visibility tracks the invocation."* Every other Tests bullet on the pair pins a concrete observable and a REQ-ID — `PIC-8` (restore-failure protocol), `PIC-19` (snapshot/swap-install failure), `PIC-44` (cache-hit byte-equality), and the `ToolDefinition.label` derivation (`cka-17`). This bullet alone names no observable: it does not say what the model's active set / callable visibility contains *during* the invocation window versus *after* it, so "done" for the bullet is undecidable.

The bullet restates the headline design property of `tool-registration-lifetime.md` §Tool-registration lifetime and visibility ("tool visibility tracks the loom's invocation rather than the process lifetime"). But the concrete observables that realise that property are already owned by other leaves: the prompt-mode per-query active-set snapshot/set/restore-in-`finally` facet of `PIC-17` is closed by `V9c` (with the prompt→prompt-invoke restore facet on `V15d`), and the subagent-mode isolated session with no shared transcript or tool table is closed by `V9i` (`PIC-9`). The coverage matrix confirms this split: `PIC-17`→`V9c`/`V15d`, and `V9f` closes only `PIC-8`, `PIC-19`, `PIC-44`, and `cka-17`. The bullet therefore adds no pinned, gated assertion that `V9f` uniquely owns; it duplicates owned coverage in vaguer terms.

The bullet is also not referenced by `V9f`'s Ships-when, which names only `PIC-8`, `PIC-19`, and the `ToolDefinition.label` derivation — so it gates nothing and an implementer marking the leaf done is not blocked by it, but is left unsure what (if anything) to assert for it.

## Issue introduction

**Verdict:** present-since-inception
**Introducing commits:** `c6a664e` (2026-06-10, "pi-loom plan: build/update plan for spec.md + review")
**History:** The bullet entered both `docs/plan_topics/V9f-tool-registration-lifetime.md` and `docs/plan_topics/V9f-T-tool-registration-lifetime.md` in the leaf-pair's creation commit `c6a664e`, originally as "The callable set's visibility tracks the invocation; `registration-cache-collision` disambiguates a slug clash." (`git log -S` over both files returns only `c6a664e`; `git log --diff-filter=A --follow` confirms `c6a664e` created the file). Later edits split the collision clause into the dedicated `PIC-44` bullet (`cfd76f7`, `66e4a27`) and reconciled the gating-window naming (`8bd82be`), but none of those passes pinned an observable for the residual "visibility tracks the invocation" half — the vague phrasing has been present since the leaf's inception.

## Solution Space

**Shape:** multiple

### Option A — Strike the bullet, deferring to the leaves that own the observable

**Approach.** Remove the "The callable set's visibility tracks the invocation." bullet from the Tests sections of both `V9f` and `V9f-T`.
**Plan edits.** Delete the bullet in `docs/plan_topics/V9f-tool-registration-lifetime.md` Tests and the identical bullet in `docs/plan_topics/V9f-T-tool-registration-lifetime.md` Tests. No other field changes.
**Spec edits.** None.
**Pros.** Removes the ambiguity outright with no duplication. The visibility observable stays asserted where the coverage matrix already maps it — `PIC-17` on `V9c` (prompt-mode per-query active-set snapshot/set/restore-in-`finally`) and `V15d` (prompt→prompt restore), plus the isolated subagent session with no shared tool table on `V9i` (`PIC-9`). `V9f`'s gated obligations (`PIC-8`, `PIC-19`, `PIC-44`, `cka-17`) are untouched, and Ships-when does not reference the bullet, so no exit gate changes.
**Cons.** `V9f`'s no-`unregisterTool`-but-still-gated design property gains no dedicated test of its own (it carries no REQ-ID and the registration-accumulation consequence is documented as cosmetic loom 1.0 behaviour in the spec).
**Risks.** Minimal.

### Option B — Restate to a `V9f`-distinct observable

**Approach.** Replace the bullet with a concrete observable `V9f` uniquely owns and `V9c`/`V9i` do not: the no-`unregisterTool` / persistent-registration-vs-per-invocation-visibility property — the loom-synthesised tool's registration persists for the process lifetime, yet the tool is reachable to the model only during the invocation (prompt mode: only inside the active-set gating window; subagent mode: only inside the spawned `AgentSession`, released at `dispose()`).
**Plan edits.** In `V9f` and `V9f-T` Tests, replace the bullet with that concrete per-mode observable, cross-referencing `V9c` `PIC-17` (prompt-mode active-set facet) and `V9i` `PIC-9` (subagent isolation) for the facets those leaves own rather than re-asserting them.
**Spec edits.** None (the property is the headline of `tool-registration-lifetime.md` §Tool-registration lifetime and visibility).
**Pros.** Gives `V9f` a pinned observable for the no-`unregisterTool` design property it actually owns.
**Cons.** Larger edit; risks re-asserting the `PIC-17`/`PIC-9` facets already owned by `V9c`/`V9i` unless the new wording is carefully scoped to the persistence-vs-visibility tension.
**Risks.** Duplication of owned coverage if the scoping is loose.

### Recommendation

Take Option A: delete the "The callable set's visibility tracks the invocation." bullet from both `docs/plan_topics/V9f-tool-registration-lifetime.md` and `docs/plan_topics/V9f-T-tool-registration-lifetime.md` Tests. The substantive visibility observable is already pinned and gated elsewhere — `V9c`/`V15d` close the `PIC-17` active-set facet and `V9i` closes the isolated-subagent-session facet (`PIC-9`), as the coverage matrix records — so the bullet adds no observable that `V9f` uniquely owns and only duplicates owned coverage in vaguer terms. The leaves owning those facets (`V9c`, `V9i`, `V15d`) and the spec topic are read-only for this fix. Edge case for the implementer: confirm Ships-when is untouched — it names only `PIC-8`, `PIC-19`, and the `ToolDefinition.label` derivation, so deleting the bullet changes no exit gate.

## Consequence

**Severity:** advisory

An implementer reading `V9f`/`V9f-T` cannot tell what this bullet requires them to assert, so it is either skipped or re-implemented as a copy of `V9c`'s `PIC-17` active-set check — adding a redundant test that duplicates coverage already owned elsewhere. The leaf's gated obligations (`PIC-8`, `PIC-19`, `PIC-44`, `cka-17`) are pinned and unaffected, so the leaf remains pickable and shippable; the cost is wasted or ambiguous effort, not a coverage gap.

## Relationships


- F-2318 "`(GOV-22 un-anchored residue)` annotation inconsistent with the paired file" — same-cluster (another `V9f` Tests-bullet wording defect; resolves independently)
- F-2342 "PIC-2 cross-body non-overlap silently relies on Pi's per-session slash-handler serialisation" — same-cluster (touches the same active-set gating-window machinery on `V9c`; resolves independently)
