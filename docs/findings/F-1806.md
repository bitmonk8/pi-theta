---
id: F-1806
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/implementation-notes.md, anchor: "" }
lens: [spec-lens-scope]
tier: low
factors:
  blast_radius: narrow
  centrality: fringe
  core_premise_blocker: no
status: triaged
category: [scope]
title: "npm package names / `package.json` placement in spec body"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---
## Finding

The dependencies paragraph names concrete libraries (`semver`, `chokidar`, `yaml`, AJV v8, Chevrotain) and their exact `dependencies`/`devDependencies` placement, turning the spec into a partial reference-implementation guide; the named seams (`Resolver`, `FileWatcher`, `Clock`, `SchemaValidator`) are the correct scope boundary.

**Suggested fix:** Remove package names / placement detail from the spec body; keep only the seam interfaces; move concrete choices to an implementation README.
