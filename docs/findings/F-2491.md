---
id: F-2491
corpus: spec
fix_surface: [spec]
location:
  - { path: docs/spec_topics/discovery/discovery-sources.md, anchor: "DISC-1 (Home-directory expansion)" }
  - { path: docs/spec_topics/discovery/discovery-sources.md, anchor: "DISC-4 (Slash-name collision rules)" }
  - { path: docs/spec_topics/errors-and-results/queryerror-variants.md, anchor: "`ContextOverflowError` / `raw_response`" }
  - { path: docs/spec_topics/discovery/package-and-settings.md, anchor: "DISC-5 (`pi.looms` pattern resolution)" }
  - { path: docs/spec_topics/errors-and-results/error-model.md, anchor: "pre-evaluation failure list (ERR-7)" }
lens: [spec-lens-clarity]
tier: low
factors:
  blast_radius: narrow
  centrality: supporting
  core_premise_blocker: no
status: triaged
category: [clarity]
title: "ERR-7: reframe the \"before evaluation begins\" umbrella for the asynchronous watcher-time class"
cycle_first_seen: 1
cycle_last_seen: 1
shape: single
state: shaped
---

## Finding

ERR-7 (watcher-time reload failures) is listed under `error-model.md`'s "complete … set of failures that occur *before* evaluation begins", whose umbrella attaches `triggerTurn: false` / no-final-value / not-cancellable to all eight items; watcher-time reload failures fire asynchronously at watcher/reload time, independent of any single evaluation, so the "before evaluation begins" framing is inaccurate for ERR-7.

## Issue introduction

**Verdict:** multi-commit-interaction
**Introducing commits:** f5e89f4 — docs: enforce 100KB hard / 30KB recommended size cap on spec set (2026-06-04, Thomas Andersen); 1cb54c7 — pi-loom spec: resolve "Glob !/+/- precedence and matcher engine unspecified for pi.looms / loomPaths" (2026-06-06, Thomas Andersen); 901d49c — pi-loom spec: resolve "ContextOverflowError raw_response schema reconciliation" (2026-06-08, Thomas Andersen)
**History:** The five bundled items entered independently across three commits rather than through a single causal change. The DISC-1 bare-`~` wording, the DISC-4 "after `pi.looms` mapping" phrasing, and the ERR-7 placement under "before evaluation begins" are all present at the inception of their topic files in f5e89f4, the spec-sharding commit. The DISC-5 "insertion order is preserved" sentence was added by 1cb54c7 when the `!`/`+`/`-` precedence rules were authored. The undefined "output-boundary overflow" term was introduced by 901d49c when the `ContextOverflowError.raw_response` schema was reconciled.

## Solution Space

**Shape:** single

### Recommendation

In `error-model.md`, reframe the umbrella sentence to admit the asynchronous class (e.g. "occur before, or independently of, any single loom evaluation"), or split ERR-7 into its own short framing note while preserving the shared `triggerTurn: false` / no-final-value / not-cancellable properties.

## Consequence

**Severity:** advisory

Each defect is a localised guidance gap rather than a behavioural fork: an implementer can still build a working system, but may waste effort (bare-`~` handling, the meaning of "output-boundary overflow"), implement a no-op (same-class insertion order), or mis-classify ERR-7's lifecycle. The undefined "output-boundary overflow" term is the most consequential since it gates when `raw_response` carries partial text, but the field is nullable and best-effort, so divergence is observable only as variation in a diagnostic payload.

## Relationships

- F-2487 "DISC-1: reconcile the `~/`-trigger with the honoured bare `~`" — co-resolve
- F-2488 "DISC-4: replace "after `pi.looms` mapping" with glob-resolution wording" — co-resolve
- F-2489 "define "output-boundary overflow" at first use" — co-resolve
- F-2490 "DISC-5: remove or repurpose the same-class insertion-order sentence" — co-resolve
