---
list: deferred
corpus: spec
order: by-importance-then-location
entries:
  - { id: F-1264, tier: nit, file: docs/spec_topics/pi-integration-contract/version-bump-step2.md, anchor: "checklist items (a)–(ao), step-2 sub-items (a)/(b) and the inline anchors `bump-step-2-positive`, `bump-step-2-negative`", title: "Editorial-review checklist `(a)`/`(b)` collide with step-2 sub-item `(a)`/`(b)` in `version-bump-step2.md`", defer_reason: nit }
  - { id: F-1289, tier: nit, file: docs/spec_topics/grammar.md, anchor: "`## Block expressions` (the statement-form `if`/`for`/`while` paragraph immediately after the `IfStmt` / `WhileStmt` / `ForStmt` productions)", title: "`StmtBlock` tail-`?` carve-out parenthetical is syntactically ambiguous", defer_reason: nit }
---
<!-- Generated view. Entries are ordered by importance (highest-importance last, addressed first by the bottom-up picker), then by location, and carry a renderer-owned projection (tier/file/anchor/title) of each finding; edit only membership: which ids appear and their defer_reason/note. Substance lives in docs/findings/F-NNNN.md. -->
